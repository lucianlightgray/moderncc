# <name>|<olevel>|<gate flag>|<extra env or ->
# Structural / pipeline-level gates (master gates, promotion, inlining, fixpoint).
# CAVEAT for cross-arch enablement: regdisp is x86_64-ONLY by construction
# (ast_regdisp_env compiles to 0 elsewhere), so it will report DID NOT FIRE on
# arm64/riscv64 and needs an arch guard before optfire runs on other triples.
# inline_capture and perfn_inproc both require MCC_AST_INLINE_PASS=0: do_inline
# in mccast.c is `&& !ast_inline_pass_env`, so the capture-time graft is dead
# whenever the post-capture inliner is on.
templates|-O2|reemit-templates|-
promote|-O2|promote-locals|-fpromote-leaf-callee
inline_capture|-O3|inline|-fno-inline-functions
inline_pass|-O2|inline-functions|-
cycle|-O3|opt-cycle|-
math_inline|-O2|builtin-math|-
regdisp|-Os|reg-disp|-
perfn_inproc|-O3|opt-perfn-inproc|-fno-inline-functions
# inline_void_discard is the regression cell for the graft/void asymmetry: a
# call whose result is thrown away with (void) records an Invoke node typed
# VT_VOID, the direct-call replay pushes nothing for it and the statement level
# skips the pop to match, but ast_inline_graft always pushed its result slot.
# The whole -finline -fno-inline-functions surface -- which is the only way to
# reach the capture-time graft, do_inline being `&& !ast_inline_pass_env` --
# was an internal compiler error until that push was balanced.
inline_void_discard|-O3|inline|-fno-inline-functions
