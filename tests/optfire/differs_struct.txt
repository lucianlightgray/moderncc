# <name>|<olevel>|<gate env>|<extra env or ->
# Structural / pipeline-level gates (master gates, promotion, inlining, fixpoint).
# CAVEAT for cross-arch enablement: regdisp is x86_64-ONLY by construction
# (ast_regdisp_env compiles to 0 elsewhere), so it will report DID NOT FIRE on
# arm64/riscv64 and needs an arch guard before optfire runs on other triples.
# inline_capture and perfn_inproc both require MCC_AST_INLINE_PASS=0: do_inline
# in mccast.c is `&& !ast_inline_pass_env`, so the capture-time graft is dead
# whenever the post-capture inliner is on.
templates|-O2|MCC_AST_TEMPLATES|-
promote|-O2|MCC_AST_PROMOTE|-
inline_capture|-O3|MCC_AST_INLINE|MCC_AST_INLINE_PASS=0
inline_pass|-O2|MCC_AST_INLINE_PASS|-
cycle|-O3|MCC_AST_CYCLE|-
math_inline|-O2|MCC_AST_MATH_INLINE|-
regdisp|-Os|MCC_AST_REGDISP|-
perfn_inproc|-O3|MCC_AST_PERFN_INPROC|MCC_AST_INLINE_PASS=0
