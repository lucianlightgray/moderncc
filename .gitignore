/cmake-*/
/.idea/
/vendor/
/dist/
/stage/
/vc140.pdb

# CI log bundles downloaded for triage (transient)
logs_*.zip

# valgrind core dumps
vgcore.*

# transient artifacts written by exec tests run from the repo root
fred.txt

# qemu-user core dumps from trap tests
qemu_*.core
*.core
