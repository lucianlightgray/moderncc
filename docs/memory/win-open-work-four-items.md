---
name: win-open-work-four-items
description: "The four \"actively open Windows\" TODO items implemented on branch win-open-work (ASan-PE, embed-jit resolver, ftoi desync, diff3 assert) — what landed, what's toolchain/HW/soak-gated"
metadata: 
  node_type: memory
  type: project
  originSessionId: 3101178a-590c-4bd0-badf-972cfce01ad3
---

Branch `win-open-work` (off `main` at `bf2ef3bf`, 2026-07-22) implemented the four Windows-actionable `docs/TODO.md` items. All land gated-OFF/byte-identical by default. **MERGED into `main` as `f1e24e71`** (pushed to origin) on top of the parallel main work (DWARF-extlink, i386 64-bit divmagic, opt-in `MCC_CROSS_OPTIMIZER`). Merge reconciliation: (1) dropped this branch's unconditional cross `MCC_CONFIG_OPTIMIZER+MCC_EMBED_JIT` append and folded `MCC_EMBED_JIT` into main's opt-in `MCC_CROSS_OPTIMIZER` (default OFF keeps cross byte-identical); (2) fixed a pre-existing main tracegate violation — `mccdbg.c:2084` `else {` lacked `MCC_TRACE("br\n")` (main's DWARF change; its Linux CI missed it). Merged-tree ctest: 99% pass, only host-env failures remain (`ci-pkg-smoke` archiver; 6× `*-docker` cross-exec fail on MSYS git-bash mangling docker workdir → `W:/`) — all logged under Tests/infra in TODO. M8 soak still runs in CI, not on this host.

Commits (order = safest→riskiest):
- `69bb952c` — **diff3 anti-false-green**: re-resolve CI step now re-sets `MCC_DIFF3_READY=1` on successful ref install; new `ci junit-assert <xml> --expect diff3/=1` verb in `tools/ci.c` (reuses junit-summary's parser) fails the Windows cell when refs resolved yet 0 diff3 tests ran. (NOTE 2026-07-23: the `ci emit`/`ci_artifacts.h` template system was removed — the `.github/workflows/*.yml` are now the canonical hand-edited source; `ci parity` still cross-checks presets. Workflows bootstrap the ci tool via the `.github/actions/ci` composite action.) Remaining: generalize the assertion to the other provisionable suites.
- `d8f9d844` — **i386/arm64-PE ASan native-shadow**: dropped `!MCC_TARGET_X86_64` guards (libmcc.c + mccpe.c `pe_add_runtime`); added `runtime/win32/lib/mccasan_{i386,arm64}_win32.c` (VEH retargeted: i386 Eax/Edx/Ecx on ud2→EXCEPTION_ILLEGAL_INSTRUCTION + 32-bit `[0x7fff8000,0x9fff8000)` shadow; arm64 X17/X16/X15 on brk→EXCEPTION_BREAKPOINT). i386-PE sets `LARGE_ADDRESS_AWARE` only when `do_asan_shadow` (else can't reserve shadow above 2GB). Inline probe + stack emitters already PE-safe. Runtime-proven: i386 native WOW64 + arm64 wine-Docker, heap/stack/global OOB→report. See [[moderncc-windows-sanitizers]].
- `27d256b1` — **i386-PE embed-jit resolver**: `pe_check_symbols` now matches decorated `__imp__NAME@N` via candidate set `{raw, raw-sans-_, raw-sans-@N, raw-sans-both}` (x86_64 unaffected — single raw lookup, byte-identical). The mingw-leaf members (ldexpl/__mingw_printf/strtoull) were never the blocker; their transitive kernel32 `__imp_@N` imports were, fixed by the same change. Wired `MCC_CONFIG_OPTIMIZER`+`MCC_EMBED_JIT` into the `MCC_X` cross loop. STILL OPEN (toolchain-gated, NOT a code bug): the per-arch baked `MCC_EMBED_JIT_BLOB` needs an i386/arm64-compiled engine archive; host mingw is x86_64-only. See [[coff-reader-embed-blob]].
- `b100da94` — **ftoi/itof AST desync**: bracket BOTH `gen_cvt_itof1` + `gen_cvt_ftoi1` in shared `gen_cast` (mccgen.c ~3420) with `ast_in_op++/--` (mirroring gen_complex_cast). One-sided ftoi-only regressed 3 x86_64 goldens because x86_64 unsigned/llong itof is also a libcall — must bracket both. Recorder-side; x86_64-win32 ratchet gap 215→213. **Nuance**: `bounds/stack_safe.c main`, now faithful, becomes eligible for the (default-on, deliberately-non-byte-faithful) -O2 register-promotion optimizer → one correct promoted-code byte delta. BEFORE flipping `MCC_JIT_I386_STUBS`: regen the `x86_64-linux` ratchet baseline on Linux (win32 build can't emit that key) + full i386 self-host + differential-fuzz soak.

Agents (opus) did each item sequentially (file overlaps: i386-gen.c, mccpe.c, CMakeLists.txt), I reviewed each diff. TODO annotated in `2e371f79`.
