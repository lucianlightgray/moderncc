# QUESTIONS

Open questions only, per `INSTRUCTIONS.md` §9. Append-only, pruned on answer: a question that is answered has its substance migrated to `DETAILS.md` and `TODO.md` and its block reduced to an `ARCHIVED.md` record.

A human answers by appending `**ANSWER:** …` to a block. Q-lin-10001 through Q-lin-10003 are the §0 assumptions, mirrored here as §0 requires rather than asked in-band.

_No open questions. Last resolved: Q-win-50034 — FLEET C23-STRICTNESS POLICY (human, 2026-08-20, SHA 23b90c3f): the disruptive C23 BREAKING changes (empty `()` == `(void)`, etc.) are honored ONLY under an explicit strict-ISO C23 std (`-std=c23`), NOT the default gnu23 mode — a deliberate divergence from gcc-16's strict default, chosen to keep existing K&R/C17 code + the runtime building by default and the o0-baseline byte-neutral. Shared gate: `mcc.h c23_strict_breaking(s)`. C23 FEATURES stay default-on. (T-mac-30142 landed under it; C99 implicit-int/T-mac-30038 is separate.) See ARCHIVED.md#Q-win-50034 and DETAILS.md#t-mac-30142-c23-empty-paren. Prior: Q-lin-10411 (`<threads.h>` backend) + Q-lin-10409 (o0-rebank AUTHORIZED; lin holds delicate front-end items), ANSWERED 2026-08-19T10:40Z SHA 5cb9e337._

### Q-mac-30258 — [mac-arm64] — 2026-08-20T05:30Z — BLOCKS: T-mac-30178
Should mcc bump its predefined `__GNUC__` from 4 to ≥7 (fleet-wide)? Context: mcc HAS the `_Float32`/`_Float64`/`_Float16`/`__float128` type keywords (like gcc≥7) but reports `__GNUC__ 4` (like a pre-7 compiler). glibc's `bits/floatn-common.h` uses `!__GNUC_PREREQ(7,0)` as its proxy for "compiler lacks the _FloatN keyword" and therefore emits `typedef float _Float32;` — which collided with a `_Float32` keyword and broke every glibc `#include` on lin/riscv64 (T-mac-30178, reverted 4ea50353). gcc coexists because gcc≥7 makes glibc SKIP that typedef. Bumping `__GNUC__`≥7 would let glibc skip the typedef (the clean gcc-matching fix) AND unblock adding the `_Float32`/`_Float64` keywords, BUT it also flips many OTHER glibc `__GNUC_PREREQ(7,…)` / `__GNUC_PREREQ(4,…)` feature guards fleet-wide (floatn typedefs, builtin availability, attribute gates) — a broad behavioral change needing cross-target verification, not a blind bump.
**Mode (b): no safe assumption.** T-mac-30178 is BLOCKED — the _Float32/_Float64 keywords stay reverted (build-correct) until this is decided. `__fp16`/`__float128`/`_Float16` keywords already ship (they happen not to collide with a glibc typedef on the tested sysroots — but the same class of risk exists, so the decision generalizes).
**Cost if wrong:** bumping without verification could silently flip glibc feature-macro behavior on lin/riscv64/win (mingw also checks `__GNUC__`) → subtle miscompiles or build breaks across the fleet; NOT bumping leaves `_Float32`/`_Float64` unavailable freestanding/on macOS (a minor C23-interchange-types gap). lin-x64 offered to run the qemu-riscv64+glibc validation leg once scoped.
REF: DETAILS.md#t-mac-30178-float32-keyword-reverted

### Q-win-50038 — [win-x64] — 2026-08-20T16:15Z — BLOCKS: T-win-50023, T-win-50024
The two arm64-Windows codegen tasks (T-win-50023 `__float128` ABI, T-win-50024 `integer_promotion`) PASS on x86_64-Windows (also LLP64) and fail ONLY the arm64-Windows exec cell, so the defects are arm64-Windows RUNTIME differences that require a Windows-on-ARM (arm64) execution environment to observe or verify a fix. No fleet box has one: win-x64 is x86_64; mac runs only x86_64-win32 under wine (not arm64-Windows PE); lin has no Windows. arm64pe_diff.py cannot substitute — the subjects are LLP64/LP64 datamodel-divergent, so the arm64-ELF byte oracle does not hold, and the bug is a runtime divergence between two LLP64 targets, not a codegen byte diff (verified this session: integer_promotion.c flagged SUSPICIOUS but only for the expected LLP64/LP64 addressing/size differences).
**Mode (b): no safe assumption.** T-win-50023/50024 BLOCKED until a WoA arm64 executor exists. arm64-Windows non-TLS codegen is otherwise fine; the divergence is unobservable without execution.
**Cost if wrong:** none from waiting (the exec cell already fails-closed); implementing blind risks "fixing" a phantom or masking the real arm64-Windows psABI issue.
REF: DETAILS.md#lin-x64-handoff-2026-08-16-preboot (the arm64-Windows-specific subject split)

### Q-win-50039 — [win-x64] — 2026-08-20T16:15Z — BLOCKS: T-win-50019
`slice/fault`'s 7 fault-recovery assertions fail on real Windows hardware (RTX 2060) because Windows handles a GPU timeout via TDR (Timeout Detection & Recovery — it RESETS the adapter) instead of the lavapipe-authored strand-the-dispatch / mark-device-unusable model mcc's fault contract encodes (evidence captured: 7 of 16 checks fail — all the recovery-model ones at suite_fault:4626-4655; dispatch itself works, the other 9 pass). Reconciling needs a design decision: EITHER rewrite the 7 assertions to accept TDR/hardware semantics, OR make mcc's fault handling TDR-aware (deep, T-lin-10033 gpu-lifecycle domain). This changes the fault-recovery CONTRACT that the gpu-lifecycle code and the CPU/lavapipe backends depend on, so it is the gpu-lifecycle owner's call, not a unilateral win test-rewrite.
**Mode (b): no safe assumption.** T-win-50019 BLOCKED pending the direction (adapt-mcc-to-TDR vs test-accepts-TDR, and if the latter, whether the contract stays lavapipe-shaped for non-TDR backends).
**Cost if wrong:** a unilateral test-rewrite that bakes in RTX-2060/TDR behavior could diverge the contract from what lavapipe / other-GPU backends expect, or mask a real fault-handling gap.
REF: DETAILS.md#t-lin-10092-win-requote-b-2026-08-15-15-of-9406

### Q-win-50040 — [win-x64] — 2026-08-20T16:15Z — BLOCKS: T-win-50021
The win embed-JIT binary is a permanent CRT-split: mcc's win32 runtime is MSVCRT-based (`runtime/win32/include/stdio.h` binds stdout via msvcrt `_iob`/`__iob_func`) while the embed-JIT engine blob is ucrt, so `puts`->msvcrt and `printf`->ucrt write to different stdout buffers -> header loss (concretely reproduced this session on full_language.c: embed-JIT emits 1475 lines vs AOT 1477 — the `puts("---- xxx_test ----")` headers are dropped; objdump confirms the binary imports BOTH msvcrt.dll AND the api-ms-win-crt-* ucrt sets). The complete fix = make the whole program single-CRT (all-ucrt: re-base mcc's win32 runtime stdio on `__acrt_iob_func`/ucrt), a FUNDAMENTAL change with fleet-wide implications for the working MSVCRT AOT path; multiple prior sessions found single-CRT attempts fragile/nondeterministic and the no-op `_set_invalid_parameter_handler` band-aid SILENTLY MISCOMPILES (685-line drift). The engine itself is HEALTHY — the 0xC0000409 crash is fixed (ab9fcf4d), strtold resolved (93c19c75), fd-shim landed (28703ba6), all `jit/selftest-*` green.
**Mode (b): no safe assumption.** T-win-50021 RELEASED from IN_PROGRESS to BLOCKED. Last green SHA 28703ba6 (fd-shim). Remaining work = the all-ucrt runtime-stdio rebase ONLY (engine crash/strtold/fd-shim all DONE); resume per DETAILS#t-win-50021-slice-2-DEFINITIVE-crt-mismatch-msvcrt-plus-ucrt-apisets.
**Direction needed:** invest a dedicated deep session in the all-ucrt win-runtime rebase (risk to the working AOT path, needs multi-rebuild validation), OR accept win-PE embed-JIT stdio-CRT-inconsistency as a known limitation and de-scope `mcctest-embedjit`/`smoke/engines` on win.
**Cost if wrong:** rushing the all-ucrt rebase risks regressing the working MSVCRT AOT path fleet-wide; the band-aid is a proven silent miscompile.
REF: DETAILS.md#t-win-50021-slice-2-DEFINITIVE-crt-mismatch-msvcrt-plus-ucrt-apisets
