# TODO

## How to process
- Mark an item in progress, commit+push to main, then start it.
- Implement gated behind a new env (default OFF ⇒ byte-identical); validate the gated-ON path to the M8 bar; independently re-verify the pass fires + correctness vs gcc; commit; prune the item.
- Completed items are pruned entirely; detail lives in git history.
- M8 bar: ctest byte-identity · `-O6` differential vs gcc/clang · 3-stage self-host fixpoint · UBSan/ASan · cross-arch (i386/arm32/riscv64/arm64) · differential fuzz (x86_64 + native arm64) · shadow-IV zero-divergence. The differential fuzzer now runs on native x86_64 AND arm64 (`ubuntu-24.04-arm`); only the shadow-IV oracle stays x86-only.
- Cross-arch checks: use `cmake-cross/mcc-i386` and `cmake-cross/mcc-arm64` (the `cmake-qemu-*` builds emit native x86_64 and lack the optimizer).
- Always enable TRACE while working: configure with `-DMCC_CONFIG_TRACE=ON` (defines `MCC_CONFIG_TRACE=1`, activating the `MCC_TRACE(...)` branch markers) and run the compiler with `-v128` (the `MCC_LOG_TRACE` bit, `1<<7`). `MCC_TRACE` writes to **stderr** (`mcc_trace_at`/`mcc_trace_at_v` in `src/mcclog.h`), so every branch emits a `[TRACE] file:line func:` line on stderr — capture it with `2>trace.log` (or pipe `2>&1 | grep`). Use these to follow the execution path and to confirm a new pass's branches actually fire (and that unintended ones don't). The logging layer is `MCCState`-free: it reads a free-standing `mcc_log_verbose` global (declared in `mcclog.h`, defined in `mcchost.c`, mirrored from the active state in `mcc_enter_state`/reset in `mcc_exit_state`); the explicit-source variants are `mcc_log_enabled_v`/`mcc_logf_v`/`MCC_TRACE_V`/`MCC_DEBUG_V`, which take a verbose byte. Standalone `MCC_INTERNAL`/non-amalgamated compiles of `mccast.c`/`mccstats.c` (e.g. `asttool`) `#include "mcclog.h"` in their `#ifndef MCC_TRACE` fallback and can trace — build that target with `MCC_CONFIG_TRACE=1` and set `mcc_log_verbose` (those targets don't parse `-v`).
- Drive each change with `grep` iteratively: grep the token/env/function/`ST_FUNC` name across `src/` to find every site → edit → rebuild → run with `-v128 2>trace.log` and grep `trace.log` (plus the ctest/differential logs) to validate the intended branch fired and byte-identity/gcc-parity held → repeat until the M8 bar is green. Preserve the `MCC_TRACE("br\n")`/`MCC_TRACE("enter\n")` markers on new branches so `tracegate` stays satisfied.

## JIT runtime (§26)
- Build a bounded UB-sound compile-time loop interpreter in `ast_eval_slice`; fold no-escape-pure slices to constants into the submitted arena.
- In-arena sub-region splice for slice ⊊ body (C2b partial-slice reconcile; needs a node-identity-stable splice primitive).
- Replace the JIT splicing counters (`MccjitCounterState`, the D5 hot-counter-driven splice/promote trigger in `mccjit_embed.c`) with **hierarchical hashes**: hash each spliceable slice and chain it into the hashes of its enclosing scopes/callers so a slice's identity carries its full dependency path. When a slice is re-optimized, its new hash propagates up the hierarchy, letting every upstream dependency (enclosing body, callers, dispatch/KGC slots) be found and patched to accommodate the optimized slice — instead of relying on per-slot call-count counters that don't know who depends on a slice. Ties into the node-identity-stable splice primitive (above), the KGC (code-hash, data-hash) key split (below, K2), and the `H_e` epoch hash (invertible slot-keyed edit patch) in the search long-tail; the hierarchical hash should be the shared identity those consume.
- Split the KGC key into (code-hash, data-hash); track poison per (code+data); feed poison as a search input (K2).
- Explore/investigate cross-code data-shape reuse of KGC entries (K10). Once the key is split (K2), add a secondary index over the *data-shape* alone — a canonicalized descriptor of the slice's operands (element dtype/width, layout/stride/alignment, value-range/known-bits, ABI class, loop trip-shape) that is independent of the code-hash. On a shape hit for *different* code whose own specialization is missing or cold, speculatively try the shape-matched cached codegen as an extra candidate for this unrelated code: the intuition is that a specialization proven good for one function's data may fit another function whose data "has the same shape," even though the source differs. JIT path: it's cheap to be wrong — benchmark the borrowed candidate against the code's own baseline (reuse the MCC_JIT_BENCH_ROUNDS min-converge harness), keep it only if it wins, and poison per (code-hash, shape) if it loses or is unsound so it isn't retried. AOT path: no runtime bench, so gate acceptance on the deterministic cost/emit-size model (the AOT-static sink scorer below) plus static-range soundness. Hard parts: the shape-fit predicate + canonicalization (when do two slices genuinely share a shape?), a soundness guard so a borrowed body is UB-sound for the new data (verify or poison, never silently miscompile), and bounding the extra lookups/benches so the speculation stays net-positive. Fold shape-poison into the unified {good,bad,unknown} classified set (L7A).
- Switch-table cover strategy row (dense→jump-table, sparse→perfect-hash; L6A).
- Unify KGC+poison into one {good,bad,unknown} LFU-bounded classified set (L7A).
- Persist poison to the mmap'd cache under the opt-in persistent-cache flag (J1A).
- Parser-less re-emit-only engine slice + `-ffunction-sections`/`--gc-sections` for the ~800 KB Tier-B embed; reconcile CMake `libmcc-static.a` so plain `-lmcc` prefers it (L9B).
- General value-range narrowing fold (J7A; needs the P1 VLat consumer).
- Small struct-by-value marshalling; promote `classify_x86_64_arg` (x86_64-gen.c) to `ST_FUNC` (K4A).
- Wire QSBR reclamation into the swap path + L2A quiescent points (function-entry + loop back-edges); un-defer J5 (K9).
- D3B nop-padded patchable-prologue in-place code-patch + int3/trap + dual-map page-flip patch rows (J10).
- Non-ABI register calling-convention kernel with boundary ABI harness (K7); inline-vs-shim search axis (K8); float + memory-boundary slices.
- JIT self-host bootstrap (`MCC_BUILD_JIT_SELFHOST` → `mcc-jit-selfhost` target, opt-in, EXPERIMENTAL, landed 2026-07-22): stage-0 `mcc` self-compiles `src/mcc.c --embed-jit -O1` → `mcc-jit`, which under `MCC_JIT=1` profile-guided-recompiles + hot-swaps its OWN functions (proven on linux/arm64: 59 `mccjit-boot` recompiles, many `route=kgc … swapped`, stats logged; `MCC_JIT=0` output correct). OPEN BUG blocking a usable self-host: full self-host **SIGBUSes** — a JIT'd variant of a complex mcc function corrupts the caller's frame (garbage return addr, faults at ~0x7); triple-INDEPENDENT JIT-codegen robustness defect (not the build machinery, not `__clear_cache`). Bisected (2026-07-22, linux/arm64): **deterministic** (even `int main(){return 0;}` crashes — it's the boot-time recompile+swap, not the compile work); **not the KGC guard** (`MCC_JIT_NO_KGC=1` still crashes → raw recompiled variant); **not any optimization** (`MCC_AST_PROMOTE/COLOR/SELECT/SPILL_SHARE/DIVMAGIC/VLAT=0`, even ALL-off, still crash → the variant *code* isn't the issue). So it's the **arm64 variant dual-map relocation / dispatch / frame for CALL-HEAVY functions**: the in-process `jit/selftest` fns (47/47 green) are small/leaf with ~no external calls, while mcc's own recompiled fns have many calls whose relocation into the W^X dual-map page (host_runmem_alloc) must resolve — a mis-applied CALL26/data reloc there (cf. this session's eh_frame/DWARF/`__clear_cache` arm64 reloc bugs) or a wrong variant frame would corrupt the caller. NEXT: capture a crashing variant's address (MCC_JIT_PERF_MAP or the verbose `variant=`/`entry=`), disasm the dual-map page, check its calls/relocs + prologue vs the AOT baseline. Prereq for: crash-free native self-host AND the cross-triple fanout (below). `mcc_add_jit_selfhost()` is already reusable per triple.
- Cross-triple JIT self-host fanout ("full cross enablement" — build a JIT-capable `mcc` for every target triple T, i.e. runs on T and JITs itself on T). BLOCKED on the self-host SIGBUS above (triple-independent) — fix that first, else every triple bootstraps a crashing binary. Per-triple 5-stage chain (the native path collapses stages 1+4 into the existing `mcc`): (1) `mcc-T` stage-0 as a host tool emitting T, built **with the optimizer** (`MCC_CROSS_OPTIMIZER`, done); (2) `mcc-T` self-compiles `src/libmcc.c` → `libmcc_jitengine-T.a` (T objects — the engine baked into T output must itself be T code, which only `mcc-T` can emit); (3) `bin2c` → `mccjit_blob-T.c`; (4) rebuild `mcc-T` **+ that blob** (`MCC_EMBED_JIT_BLOB`) so it can bake the T-engine; (5) `mcc-T --embed-jit` self-compiles `src/mcc.c` → `mcc-T-jit` (a T executable that JITs itself; runnable only on T HW/qemu). Feasibility matrix: **ELF (i386/x86_64/arm/arm64/riscv64) genuinely bootstrap** once the SIGBUS is fixed (build here; run/verify only native + x86_64/arm64/i386 via Docker, arm/riscv64 build-only, no qemu); **macOS-osx (x86_64/arm64) blocked** — Mach-O `--embed-jit` can't resolve `_mccjit_boot_swap` (separate bake bug); **PE (i386/x86_64/arm64/arm-win32, arm-wince) blocked** — needs the mingw-runtime embed path + the JIT is HW-gated. Wire per-triple in the cross loop reusing `mcc_add_jit_selfhost()`; gate macOS/PE with a STATUS naming their blocker rather than shipping broken targets.
- Flip `MCC_AST_JIT_EVAL_GATE` default-on after 3 clean self-host + fuzz soaks; extend the oracle to statement-level control flow (7A/M8).
- AOT-static sink scorer: deterministic cost/emit-size model + static-analysis ranges (AstVLat) + gain-ordered time-budgeted scheduling.
- K6/L8A data→code substitution via a synthetic `.init_array` ctor.
- Fix the `ast_reemit` segfault when recompiling a pointer-RETURNING function (pre-existing, string-independent).
- Structural fix for `mccjit_bench_pair`: paired per-round ratio + median to cancel common-mode contention (env-independent) — currently a bench tie-flake; changes production promotion semantics + other benches ⇒ needs its own soak.

## JIT arm64
- arm64 mode-6 dispatch for object output: harden the GOT/ABS64 slot so the function symbol survives external link, then drop the `!ast_search_env` guard.
- arm64 in-place trampoline patch row.
- Complete the arm64 W^X dual-map port [2B]: fix `mccjit_probe_exec_mem` (RWX→RW+mprotect-RX), then port `mccjit_make_counter_stub`/`_kgc_stub_n`/`_kgc_stub_fp`/`_make_trampoline` + the `src/mccast.c` mode-6 slot as split-page (coupled — no test greens until all done); validate `mccjit_make_kgc_stub_mixed` + its forwarding thunk (highest-risk, zero qemu coverage).

## JIT Windows / i386-PE
- x86_64-PE runtime-JIT self-host crash (`mcc --jit -O4+ -run src/mcc.c`) — FIXED. The `-run` MEMORY finalize (`mcc_relocate → pe_output_file → pe_add_runtime → mccjit_embed_finalize`) starts a NESTED `mcc_compile_string` of the boot-registry stub; its `mccgen_compile` re-ran `ast_reemit_forward_inlines()` over the process-global `ast_reemit_pool`, which is drained at each compile's end but NEVER reset — so the nested stub compile walked the PRIOR (src/mcc.c) compile's entries whose `Sym*`/`AstArena` were already freed → `sym->type.ref == -1` deref → SIGSEGV/`0xC0000005` at the compile→run transition. Only fires at -O4+ (the AST search graduates KGC entries → non-empty embed registry → the stub compile exists at all; -O1 leaves it empty → no nested compile → no crash), and is a latent arch/toolchain-independent cross-compile use-after-free that PE `-run` happened to hit (the arm64/ELF self-host reaches the same finalize on its `--embed-jit` bake). FIX: reset `ast_reemit_n`/`ast_inline_n = 0` in `ast_configure` (the per-compile init, `mccast.c:1275`, called from `mccgen.c:579`), so each compile starts with an empty pool. Verified: JIT `-run` self-host output byte-identical to AOT with mcc built by **MSVC (VS18 `cl` 19.51), mingw-gcc 15.2, clang/msvcrt 22.1, and mcc self-host**; `jit/`+`regression/` 49/49, `ast/exec/cli/embed/diff3` 851/851. Guarded by the `selfhost-jit` ctest (native label; guards the ELF/Mach-O self-host on the Linux/macOS cells — SKIPs on PE msvc+mingw because a SEPARATE PE `-run` fault still crashes the CI Windows cells even though it runs byte-identical on a local mingw-msvcrt/MSVC build, see the PE runtime-JIT self-host item below) + the `mcc-jit-selfcheck` convenience target (`tools/selfhost-jit.py`: `--jit -O4 -run src/mcc.c` then assert the JIT-recompiled compiler's object == a plain AOT `-c` object, catching a JIT miscompile of mcc, not just a crash). DISTINCT from the arm64 self-host SIGBUS (JIT §, baked-variant frame/reloc, still open) and the i386/arm64-PE embed-BAKE blocker (below) — neither is addressed here.
- Un-gate the i386-PE KGC/FP(x87)/mixed stub tail (gated behind MCC_JIT_I386_STUBS, default OFF). Mode-6 i386 entry dispatcher landed (`bd837321`); remaining M8 bar:
  (1) i386 AST-faithful-replay desync on float→int casts — FIXED (`b100da94`): the clean fix was to bracket BOTH the `gen_cvt_itof1` and `gen_cvt_ftoi1` dispatch in shared `gen_cast` (mccgen.c ~3420) with `ast_in_op++/--` symmetrically (mirroring `gen_complex_cast`), so the recorder models one depth-neutral Convert and skips the libcall's internal push/call/push churn — matching what x86_64/arm64/riscv64's single-hw-insn path already produces. The earlier one-sided ftoi-only bracket regressed 3 x86_64 goldens precisely because x86_64's unsigned/llong itof is ALSO a shared-mccgen libcall; bracketing both directions is what keeps the model consistent per-arch. Recorder-side only (emits no code); the x86_64-win32 ratchet gap set shrank 215→213 (`bounds/stack_safe.c` + `types/conversion.c` main now faithful, zero new desyncs), ast-verify-ratchet green, conversions match gcc on x86_64 + i386-WOW64. NB one second-order x86_64 byte delta: `stack_safe.c main`, now faithful, becomes eligible for the (already-default-on, deliberately-non-byte-faithful) `-O2` register-promotion optimizer and emits promoted-but-correct code — the intended "remove the prerequisite blocker" effect. The `x86_64-linux` ratchet baseline regen is now DONE (`bb1b044e`-followup): the deferral note "its gaps only shrink so it stays ratchet-safe meanwhile" was WRONG and turned CI #1082/#1086 red on every x86_64 job — the ratchet is two-sided (a baseline gap that becomes faithful is a fatal drift too, not just new gaps), so the deferred shrink (`bounds/stack_safe.c main` desync→faithful, 763→762; `types/conversion.c main` stays desync on linux, unlike win32) tripped it. Fixed by dropping that one entry from `verify-baseline/x86_64-linux.txt`. STILL BEFORE THE `MCC_JIT_I386_STUBS` FLIP: the full i386 self-host + differential-fuzz soak;
  (2) differential vs gcc/clang + self-host on i386, then flip the default.
- arm64-PE runtime-JIT (frameless-leaf return corruption + RtlAddFunctionTable/icache) — codegen/logic portion validated under wine (`a35d5ba3`: no mcc bug — the mode-6 slot-swap + AAPCS64 mixed-arg thunk run correct on the native arm64-PE loader). Native-fault subset genuinely needs arm64-Windows HW: `RtlAddFunctionTable`/SEH unwind walking JIT frames, `IC IVAU`/`DSB`/`ISB` icache coherence, and the frameless-leaf return corruption itself — all spin-not-fault under qemu/wine x86-TSO. (Note the cross `mcc-arm64-win32` has no `MCC_EMBED_JIT` and is an x86_64-host tool anyway, so the runtime-JIT stubs only fire in an arm64-native mcc.)
- MSVC-arm64 JIT-exec miscompile (wild-jump) — needs arm64-Windows HW (same x86-TSO masking).
- NOTE: the wine-arm64 codegen-validation path is a committed script `tools/arm64pe-wine-docker.sh` (debian-bookworm wine-8.0 arm64; args = the cross mcc + a staged win32 sysroot). Only native-fault / weak-memory bugs need real HW; codegen/logic is reproducible.
- Standalone `--embed-jit` blob for i386-PE / arm64-PE — compiler machinery + COFF engine-symbol resolution landed (`bd837321`/`4f091469`: i386 embed-slot codegen, i386-PE COFF `_`-undecoration in mccpe.c/mccelf.c, cross-embed link stubs in mccrun.c; 342/342 coff/embed/jit ctests, 0 unresolved with a hand-built engine archive). NOTE (2026-07-21): the long-assumed "short-import blocker" was a MIS-DIAGNOSIS — the vendored **winlibs mingw** import archives (`libkernel32.a`/`libmsvcrt.a`/…, all arches) are **100% long-import**, ZERO short-import members, so the i386/arm64-PE embed unresolved (~38 `__imp_` + ~13 mingw-leaf `strtoull`/`__mingw_printf`/`ldexpl`) come from the i386 stdcall `__imp__NAME@N` decoration + linking the mingw-leaf *code* members, NOT from an unparsed short-import format. (Short-import support was added anyway — `coff_short_import_info` in mccpe.c, consumed in `mcc_load_alacarte` — because MSVC/LLVM/Windows-SDK import libs DO use `00 00 FF FF` IMPORT_OBJECT_HEADER members; validated: mcc now links+runs against the SDK's `AdvAPI32.Lib` etc. See the `pe/short-import` ctest.) The two named resolver bugs are now FIXED (`27d256b1`): (a) `pe_check_symbols` matched an undefined `__imp_NAME` by stripping the prefix through `pe_export_name` (which pre-strips one leading `_`) and never stripped the stdcall `@N` suffix — so a COFF `__imp__Sleep@4` demand matched neither the long-import provider's decorated `_Sleep@4` nor a `.def` provider's undecorated `Sleep`; replaced with an explicit candidate set `{raw, raw-sans-leading-_, raw-sans-@N, raw-sans-both}` (x86_64 unaffected — no `_`/`@N` ⇒ single raw lookup, byte-for-byte identical). (b) The mingw-leaf members (`ldexpl`/`__mingw_printf`/`strtoull`) were never the blocker — they already bound via the i386 underscore fallback; their *transitive* kernel32 critical-section `__imp_@N` imports were the real unresolved set, fixed by the same change. Validated vs real winlibs i686 import libs: a program using dllimport IAT slots + those leaf fns links 0-unresolved with a valid kernel32 IAT, and an mcc-compiled i386-PE `hello.c` runs on WOW64. Also wired `MCC_CONFIG_OPTIMIZER`+`MCC_EMBED_JIT` into the `MCC_X` cross loop. STILL OPEN (toolchain-gated, not a code bug): the per-arch baked `MCC_EMBED_JIT_BLOB` for i386/arm64-PE — baking it needs the mcc engine archive compiled for i386/arm64, and this host's mingw is x86_64-only (no i386 libgcc, no arm64 compiler), so a full end-to-end cross `--embed-jit` exe can't be produced here (the cross mcc accepts `--embed-jit` + links an external engine via `MCC_EMBED_JIT_LIB`).

- Port `mccjit_make_kgc_stub_mixed` to the Win64 positional ABI (needs a per-arg class vector; returns NULL on WIN32 ⇒ `jit/selftest-mixed` skips).
- Enable the `--embed-jit` standalone-exe embed-blob on WIN32 — mcc's ELF-only linker can't consume the host CC's COFF `libmcc_jitengine.a` (guarded off, CMakeLists.txt ~1951).
- Fix CLI `--jit-functions=f` storing `"=f"` (leading `=` not stripped; API path unaffected).
- Port a real Windows JIT runtime for `MCC_EMBED_JIT` — glibc-only, gated OFF on WIN32 (needs `sys/mman.h`/pthread/fork equivalents).
- PE runtime-JIT self-host x86_64 residual crash (`mcc.exe --jit -O4 -run src/mcc.c` faults `0xC0000005`) — the `selfhost-jit` ctest SKIPs on Windows (`NOT WIN32` + `tools/selfhost-jit.py` self-skip). DISTINCT from the fixed i386 graft-inline frame under-allocation (that one was deterministic on winlibs-ucrt i686 and is now green; the fix left x86_64 codegen byte-identical). This residual crashes ONLY the x86_64 CI Windows cells (mingw x86_64 + msvc + sanitize-msvc, `exit 3221225477`). NOT LOCALLY REPRODUCIBLE despite exhaustive attempts on this host with the EXACT CI toolchain (winlibs gcc 16.1.0-ucrt x86_64): plain build 25/25 clean; superbuild-child config (`MCC_TOOLCHAIN_PROFILE=mingw` + `MCC_SUPERBUILD_CHILD=ON`) 15/15 clean; and forced raw-variant swapping (`MCC_JIT_NO_KGC=1`, `MCC_JIT_HOT_THRESHOLD=1`, `MCC_JIT_BENCH=1`) all exit 0. NOT the i386 frame bug: it left x86_64 codegen byte-identical, and BOTH the base `-run` image AND the JIT variant recompile go through the now-fixed `ast_reemit` (variants via `ast_reemit_with_gates`/`_extern` → `ast_reemit`). i386 never swaps a variant (all boot-sync slots `refused-unverified`/`kept-aot`), while x86_64 swaps many (`route=kgc … swapped`), so the fault is almost certainly in the x86_64-only swapped-variant path — the KGC-stub ABI (`mccjit_make_kgc_stub_n`/`_fp`/`_mixed`, x86_64 SysV/Win64 register marshalling) or a benchmark/search-worker timing/ASLR interaction that only the CI CPUs hit. NEXT: get a CI stack trace (temporarily un-skip + `MCC_JIT_VERBOSE`/`MCC_JIT_PERF_MAP` on a branch, or a crash-dump upload from the Windows cell) since local x86_64 won't fault; then disasm the swapped variant/stub at the fault like the i386 path. The i386 fix is the reference for the frame class; this is a separate x86_64 stub/variant defect.

## AOT foundations
- Close the riscv64 Tier-3 self-host gap (6A; makes the M8 cross-arch gate real).
- `MCC_AST_DIVMAGIC` item (b): optimal single-multiply form for the signed / `a==1` cases (needs real temp-materialization; the C path still uses the dup-based 2× mul-high).
- Fix the emit-time value-axis framework full-state save/restore (promotion-plan arrays + `nocode_wanted` + register-allocator/`vtop` state); then enable the inline + promote search axes (3A).
- Flip `MCC_AST_DIVMAGIC` default-on after the full M8 soak (4B). (Signed-32 divmagic miscompile FIXED `a1df06c1`; i386 64-bit divmagic runtime helper DONE `7026372e` — `__mcc_umulh64`/`__mcc_smulh64` in mccrt.c + i386 `gen_mulh` + MULHU/MULHS gates now include i386; validated 25/25 vs gcc under linux/386 with an optimizer-enabled i386 build.) (The signed-32 divmagic miscompile that blocked the flip is FIXED — `a1df06c1`: `ast_divmagic_try_signed` built the quotient as `q2(S32) + signbit(U32)`, whose usual-arithmetic-conversion result is *unsigned*, so a widening of `x/C` zero-extended a negative quotient instead of sign-extending; arch-neutral, reproduced+fixed on x86_64 + arm64. Remaining before the flip: regenerate every arch's `-O2+` byte-goldens and run the tens-of-thousands-of-seeds differential-fuzz/self-host/cross-arch soak.) SOAK EVIDENCE (2026-07-22, Windows/x86 host under Docker): an optimizer-enabled `mcc-i386` (hand-built `gcc -DMCC_CONFIG_OPTIMIZER=1 -DMCC_TARGET_I386=1 src/mcc.c` in an amd64 container) with `MCC_AST_DIVMAGIC=1` was diffed against a hardware-`idiv` oracle (each constant divide in its own leaf fn so divmagic fires — 0 residual const `idiv`, 74 `__mcc_?mulh64` helper calls emitted — vs a `volatile`-divisor reference in the same binary), linked with `i386-libmccrt.a` and run natively under `linux/386`: **8,611,396 checks, 0 fails** across signed/unsigned 32- AND 64-bit div+mod over a wide sweep with edge cases (INT_MIN, LLONG_MIN, ±primes, powers of two, 2^32±1, large constants); the gcc build of the same program returned the identical 8,611,396/0, confirming the oracle is sound. Far broader than the earlier 5400-iteration run; still not the multi-arch golden-regen + self-host + fuzz soak.) This soak is now a TRACKED docker-gated ctest — `i386-divmagic-soak-docker` (`tools/i386divmagic-docker.sh` + CMakeLists, SKIP 77 when docker/mcc-i386/`i386-libmccrt.a` absent; builds its own optimizer-enabled i386 mcc in-container since the normal cross build has none); validated through ctest on this host (47s, `checks=8611396 fails=0`). X86_64 FLIP-READINESS (2026-07-22): forcing `MCC_AST_DIVMAGIC=1` across the ENTIRE native x86_64-PE ctest suite passed **5501/5501, 0 fails** — no behavior regression (exec/exec-replay are correctness-preserving) AND no byte-golden breakage (ast-verify-ratchet tolerates it; the per-arch `dash-s-bytes` goldens use the optimizer-FREE cross mcc, so a runtime-default flip can't perturb them). So on x86_64 the flip is effectively byte-golden-free — the "regenerate every arch's -O2+ byte-goldens" concern does NOT materialize here. KEY: the only CI arches that carry the native AST optimizer are x86_64 + arm64 (i386/arm/riscv64 ship as cross builds WITHOUT `MCC_CONFIG_OPTIMIZER`, so divmagic is compiled out and they're unaffected by the runtime default). arm64 divmagic CORRECTNESS is now soak-proven too: `arm64-divmagic-soak-docker` (`tools/arm64divmagic-docker.sh`, sibling of the i386 soak) builds an optimizer-enabled arm64 cross mcc, compiles the same constant-divide soak with `MCC_AST_DIVMAGIC=1`, and runs it under qemu-aarch64 against a hardware-div oracle — **8,611,396 checks, 0 fails** (arm64 uses native `smulh`/`umulh`, so no runtime archive; mulhi=74 fired). Verified on this x86_64 host via qemu (runs on the x86_64 CI cross job; skips on arm64 runners which can't emulate the amd64 build stage, same as the sibling arm64-inline-docker). riscv64 divmagic is ALSO soak-guarded now — `riscv64-divmagic-soak-docker` (`tools/riscv64divmagic-docker.sh`, native `mulh`/`mulhu`/`mulhsu`, qemu-riscv64) — completing the cross-arch soak across every optimizer-capable arch (x86_64 full-suite + i386 + arm64 + riscv64; arm deferred behind its libcall-hazard audit). **That soak IMMEDIATELY caught a real, latent riscv64 codegen bug** (NOT divmagic-specific): `load_large_constant` (riscv64-gen.c) materialized 64-bit constants whose low-32 word lies in ~[0x7FF80000, 0x7FFFFFFF] off by 2^32 in the high word — the top `SIGN11` piece rounds to 0x800 → −2048, borrowing 2^32, and the `if (fc < 0) pi++` compensation only covered fc's sign bit, not this top-piece carry (e.g. the 65537 magic 0x7FFF80007FFF8001 became 0x7FFF7FFF7FFF8001 → signed 64-bit `x/65537` off-by-one for negative x, 106/8.6M). Fixed by also incrementing `pi` when the top piece carries (`else if (((unsigned)fc + (1<<19)) >> 20 >= 0x800) pi++`); validated mcc==gcc across 13 boundary constants + the 8,611,396-check soak now 0 fails. Remaining before the flip: the arm64-native FULL ctest suite with divmagic on (the arm64 analogue of the x86_64 5501/5501 run — needs an arm64 host, i.e. the `ubuntu-24.04-arm` CI cell) + the fuzz soak. Deliberately NOT flipped autonomously — a shipping-default correctness change warrants a deliberate call once arm64-native is confirmed green.
- Memo unification: migrate to `ComboMemo` + disk backing with a bulk-value compression mode; subsume the out-of-process superopt (5A/M2/M3).
- PR-C loop-IV monotonicity widening into `ast_vlat_context_at`, op-3/op-5 for-loops only; held until x86 fuzz soaks clean (P1/§32a).
- Predicate-vector 4th side-car index (P1).
- `context_in`/`context_out` memo-key consumer (P1).
- Descendant-indexed (DFS enter/exit) def/use extension for subtree-scoped write queries (P1).
- Flip `MCC_AST_VLAT` default-on; signed `/ %` (INT_MIN/−1 trap), `<<` value-count (P1/§29).
- Flip `MCC_AST_NARROW_ELIM` default-on: flow-sensitive facts + globals (§29).
- Flip `MCC_AST_ARGFWD` default-on; widen past single-use (§33c).
- Flip `MCC_AST_SPILL_SHARE` default-on; general per-value spill slots; riscv64 fixpoint (§36).
- Flip arm64 `opt_promote` default-on after tens-of-thousands-of-seeds soak; PR-3 callee-saved float pool v8–v15.
- riscv64 register promotion — DONE (`mccast.c:2516` promotion `#if` now includes RISCV64; `MCC_TREG_SAVED(x)`→s1..s11 in `riscv64-gen.h`/`.c` with `reg_classes[]` entry 0 + `ireg()`/`is_ireg()` physical mapping; callee pool s1..s11, leaf caller pool a2..a7, float pool fa2..fa5; soft-quad `long double` forces callful. GP + float promotion both fire+validated). Validation: OFF byte-identical (native x86_64 ctest 5491/5491, no drift); gate-fires objdump (s1..s11 across calls, elided reloads); qemu-riscv64 differential vs gcc 60,604 checks/0 fails — tracked as the `riscv64-promote-docker` ctest (docker-gated, SKIP 77). Default-on flip shares the global promotion soak (same status as arm64). Not yet done: callee-saved float pool (fs0-fs11), mirroring arm64's deferred v8-v15.
- Promotion arch scope (decision): arm (armv7) is register-adequate (`r4-r11`/`d8-d15`) but has a large hidden-libcall surface (`__aeabi_idiv` — no baseline hw divide, 64-bit `long long`, soft-float) ⇒ the callful-forcing hazard analysis must be far broader than arm64's single long-double case; defer behind a libcall-hazard audit. i386 is out of scope — `MCC_NB_REGS 5` models only eax/ecx/edx, ~3 callee-saved GP (`ebx/esi/edi`), no callee-saved SSE; marginal payoff at the highest miscompile risk (the current `#if` excludes it deliberately).
- Replace `ast_plan_promotion` heuristic with §36 coloring; fixpoint-gated + native arm64/riscv64.
- Tier-4 inline on riscv64 — VERIFIED (2026-07-22, `riscv64-inline-docker` ctest, qemu differential 42429/0 vs gcc -O2): scalar/double/pointer-loop/8-byte-reg-pair-struct callees all graft (0 residual `R_RISCV_CALL` relocs); `inline_spec.c` grafts+specializes choose/clampk/mul/addk with main replaying (RC=42). The >16B struct-by-value case (`sumbig`) now ALSO grafts (indirect hidden-pointer capture modeled — see the dedicated item just below; differential re-run 42429/0). MCC_CPU-gated `ast/replay-inline` fixture still not widened (dormant — no riscv64-host CI); coverage is the docker ctest.
- Indirect (hidden-pointer) large-struct-by-value in the Tier-4 inline capture — DONE (2026-07-22): `ast_inline_capture`/`ast_inline_graft` now model a param passed via a hidden pointer (`VT_LLOCAL|VT_LVAL|VT_STRUCT`) — a `param_indirect[]` flag on the pool/capture arrays; the graft path allocates a fresh local copy of the struct (replay arg → store), takes its address, and stores the pointer into the callee's hidden-pointer slot (the caller-made by-value temporary the psABI requires). Ungates `sumbig` graft-inlining on the indirect-ABI arches: **riscv64 + arm64 now GRAFT sumbig** (0 residual call relocs, differential 42429/0 vs gcc -O2 via `riscv64-inline-docker`/new `arm64-inline-docker`). Guards all green: native x86_64 ctest 5484/5484 incl. ast-verify-ratchet (x86_64 uses direct-stack VT_LOCAL — untouched); riscv64/arm64 differentials 0-fail; arm+i386 differentials 0-fail. NB **arm + i386 large-struct is a SEPARATE direct-copy gap** — their >16B struct-by-value is a direct stack copy (VT_LOCAL, like x86_64 SysV MEMORY), NOT the hidden-pointer indirect class, so it does NOT go through this path and stays retained (would need a distinct "large direct-copy arg" capture model). The arm64 MCC_CPU-gated `ast/replay-inline` fixture was deliberately NOT re-widened to include sumbig (dormant on non-arm64 hosts, and inline.c's main-context replay with sumbig grafted is not yet arch-verified in that exact fixture) — coverage is the docker ctest.

## Ungate campaign (flip every default-off feature on)
Endgame: once each gate's M8 soak is clean, flip it default-on and regenerate goldens. Beyond the per-gate flips in AOT foundations:
- Validation status (2026-07-21): `tests/qemu/native-optcheck.sh` ran the full `^exec/` suite under all flip-pending gates at once (`PROMOTE COLOR LICM_TEMP IVSR PRE REASSOC SETHI_LEAF NARROW_ELIM ARGFWD SPILL_SHARE VLAT DIVMAGIC`) on **arm64-Linux: 297/297, gated == default, zero regressions**; a 10-program mcc-vs-gcc differential battery (divmagic/select/reassoc/narrow/PRE/promotion-spill) also matched gcc on x86_64 + arm64. This is an all-gates smoke, NOT the per-gate tens-of-thousands-of-seeds soak the flips still require. **TODO (run on an x86 host — e.g. the Windows/x86 machine):** `MCC_GATES="MCC_AST_PROMOTE=1 MCC_AST_COLOR=1 MCC_AST_LICM_TEMP=1 MCC_AST_IVSR=1 MCC_AST_PRE=1 MCC_AST_REASSOC=1 MCC_AST_SETHI_LEAF=1 MCC_AST_NARROW_ELIM=1 MCC_AST_ARGFWD=1 MCC_AST_SPILL_SHARE=1 MCC_AST_VLAT=1 MCC_AST_DIVMAGIC=1" bash tests/qemu/native-optcheck.sh` — couldn't run here (the `mcc-qemu` image is arm64-only; the harness now falls back to `debian:bookworm-slim` + self-provisions cmake/gcc/ninja, so it runs on any x86 Docker host and now asserts gated-fail ≤ default-fail, exiting non-zero on a gate regression). DONE on the Windows/x86 host (2026-07-22): (a) added the missing MSYS docker-path guard (it `-v "$REPO":/work` with no `MSYS_NO_PATHCONV`, same `W:/` bug class as the docker ctests); (b) fixed a Windows-only bind-mount pathology — `cmake` configure over the Windows→Linux 9p/virtiofs mount stalled in D-state >11 min (thousands of small configure-time stats), so the harness now stages a container-local copy of the pieces the build reads (`src include runtime tests cmake CMakeLists.txt ...`, skipping `.git` + host `build-*/`) into `/src` and builds from there — no-op-cheap on Linux/macOS, RO mount keeps the host tree pristine. With the fix the full flip-gate run completes on **native x86_64 in a few minutes: default 296/297, gated (the full 12-gate set) 296/297 — gated == default, no gate regression** (the 1 failure is pre-existing — fails without gates too; unidentified, see Tests/infra follow-up). Separately, a per-gate mcc-vs-gcc DIFFERENTIAL soak on native x86_64 (optimizer-enabled mcc built in-container, freestanding leaf-fn programs, edge grid + tens of millions of xorshift-random inputs, gcc -O2 oracle) found **0 miscompiles**; 8/11 non-divmagic gates independently confirmed FIRING via objdump signature AND clean vs gcc — `SELECT` (cmov), `REASSOC`, `NARROW_ELIM`, `PRE`, `LICM_TEMP`, `IVSR`, `SETHI_LEAF`, `PROMOTE` (regs %r8d–%r10d, reloads elided). The remaining 3 were then ALSO isolated to fired-signatures (all 12 now fire + clean, 0 miscompiles), each with a trigger nuance worth recording: **`ARGFWD`** only fires via the replay-time inline-GRAFT path (`ast_inline_graft` ~mccast.c:2776) which is behind `ast_inline_env` (default `optimize>=3`), so it is **INERT at plain -O2** — at -O2 inlining runs through `ast_inline_pass_simple` which never reaches ARGFWD and short-circuits `do_inline`; isolate it with `MCC_AST_INLINE=1 MCC_AST_INLINE_PASS=0` then toggle ARGFWD (forwards the SValue in place, dropping the store-into-param-slot + reload; 400/400 clean). This means flipping `MCC_AST_ARGFWD` default-on has no effect at -O2 — only under graft-inline (-O3/`MCC_AST_INLINE`). **`SPILL_SHARE`** fires at plain -O2 given disjoint-but-call-spanning promoted live ranges that color to the same physical reg → `ast_promo_save_plan` (~3973) coalesces spill slots (marker `11->5` slots; frame `sub $0x100` → `$0xd0`; 2744/2744 clean). **`VLAT`** fires only via `ast_narrow_binary`'s `ranged` path (~5852, ops `/ % << >>` that base NARROW's `+ - * & | ^` can't touch); two constraints the prior attempt missed — the lattice refines ranges only from enclosing `if`-guards (masking `x & 0xFFFF` establishes nothing) and `ast_range_bound` REJECTS unsigned keys (~5873) which conflicts with `/`,`%` needing unsigned, so `TOK_SAR` (signed key, guard-provable range) is the isolable op: `if(v>=-1e6)if(v<=1e6) r=(int)(v>>3)` narrows the shift to 32-bit (`sar,%rax`→`sar,%eax`); VLAT-off keeps it 64-bit so NARROW does NOT subsume it (16068+23 inputs clean). KEY METHOD NOTE for future gate work: the AST reemit optimizer only runs at `-O1`/`-O2` (`ast_replay_env = optimize>=1 || embed_jit`); setting `MCC_AST_*=1` at `-O0` is INERT (zero codegen delta). i386 divmagic has its own tracked guard now (`i386-divmagic-soak-docker`, see `MCC_AST_DIVMAGIC` above). Still not the multi-arch golden-regen + self-host + tens-of-thousands-of-seeds-per-gate soak the flips ultimately require — but x86 correctness evidence for the whole pending set is now broad and clean. SELF-HOST portion of that soak is now ENFORCED (2026-07-22): the `selfhost-fixpoint-gates` ctest (see the self-host item under Tests/infra) proves mcc self-hosts to a **byte-identical 3-stage fixpoint** (o1==o2==o3) with all 12 gates forced on; a per-gate diagnostic further confirmed each gate INDIVIDUALLY holds the byte-identical fixpoint (PROMOTE/COLOR/LICM_TEMP/IVSR/PRE/REASSOC/SETHI_LEAF/NARROW_ELIM/ARGFWD/SPILL_SHARE/VLAT/DIVMAGIC all FIXPOINT-OK on x86_64-Linux) — the strongest form of the self-host soak (deterministic + self-consistent codegen under each optimization). **arm64-Linux self-host gates FOUND + FIXED a real `MCC_AST_PROMOTE` miscompile 2026-07-23** (CI run 8119113731, first native-arm64 execution of `selfhost-smoke-gates`/`selfhost-fixpoint-gates` — they're native-ELF-gated so macOS auto-skips and the prior per-gate fixpoint evidence was x86_64-ONLY): the knobs-on self-hosted mcc SIGSEGV'd compiling `t.c`. Bisected with `MCC_AST_PROMOTE_LIMIT` to the 462nd promoted fn — `sym_link` (mccgen.c, a leaf promoting the one pointer local `ps` into caller-saved x9). Root cause is arch-independent (`gv`, mccgen.c ~1978): the reg-reuse shortcut for loading an lvalue `*ps` whose address is already in a register reused that base reg as the LOAD DESTINATION (`ldr x9,[x9]`), bypassing `get_reg`'s `ast_pinned_regs` check — so it clobbered the promoted-and-pinned `ps` before the later `*ps = s` (which then stored through the wrong pointer). x86_64 was latently exposed to the same shortcut but happened not to hit it on `mcc.c`. FIX: guard the reuse with `ast_pinned_regs` (fall to `get_reg` when the base reg is pinned). Post-fix: arm64 AND x86_64 self-host plain + all-12-gates + byte-identical 3-stage fixpoint (o1==o2==o3) all green; native-arm64 ctest 0 failures. Lesson for the campaign: run the self-host fixpoint gate on arm64, not just x86_64 — pinned-reg codegen paths diverge by backend. Remaining for the flips: multi-arch golden-regen + the tens-of-thousands-of-seeds differential soak.
- Flip `MCC_AST_LICM_TEMP`, `MCC_AST_IVSR`, `MCC_AST_PRE`, `MCC_AST_SETHI_LEAF` default-on after soak.
- Resolve order-non-confluence, then flip `MCC_AST_REASSOC` default-on.
- Flip §27 `MCC_AST_INTERCHANGE`/`_FUSION`/`_TILE` default-on after soak.
- Wire `MCC_AST_COST` as the search/budget scorer, then flip it on.
- Flip the search family (`MCC_AST_SEARCH`/`_EMITSIZE`/`_EMITISO`/`_INLINE`/`_THREADS`/`_ORDERED`/`_ORDER`) on (needs 3A + §22 emit isolation).
- Flip `MCC_AST_PERFN_INPROC` on.
- Flip the runtime `MCC_AST_JIT` global gate + `_JIT_SPLICE`/`_JIT_DISPATCH` default-on after the full §26 validation-bar soak.

## Cross-arch parity (raise every arch to the x86_64 Tier-4 reference)
Goal: each arch matches x86_64 for self-host, promotion, cmov/csel, div-magic, JIT stub tail, ASan native-shadow, stack-protector, UBSan trap, TLS GD/LDM, Tier-4 replay-inline, over-align. Per-arch rollup (detail in the JIT/Sanitizers/AOT sections):
- riscv64: raise to Tier-4 — self-host, replay-inline, cmov, JIT stub tail, ASan native-shadow, stack-redzone (ASan). (register promotion DONE — `MCC_TREG_SAVED`→s1..s11, GP+float, qemu differential 0-fail via `riscv64-promote-docker`; callee-float pool deferred as arm64. stack-protector canary DONE; -fPIC/-fPIE TLS DONE via codegen-time local-exec: a PIC executable/PIE emits the proven TPREL LE sequence directly (`R_RISCV_TPREL_HI20/LO12_I`, byte-identical to the non-PIC compile, 0 residual dynamic-TLS relocs), mcc self-links + runs under qemu-riscv64. NB the `riscv64-link.c` GD→LE *linker* relaxation is an incomplete stub (rewrites auipc→lui but never neutralizes `call __tls_get_addr`), deliberately left broken by `04bc9749`; the GD path now honestly errors only for true `-shared`/DLL. Global-dynamic dynamic-TLS output for shared libs stays open.)
- i386: JIT stub tail. (Tier-4 replay-inline VERIFIED 2026-07-22 — `i386-inline-docker` ctest, differential 83911/0 vs gcc -m32 -O2 under linux/386: scalar/double/float/pointer/8-byte-struct-by-value all graft (0 residual `R_386` call relocs); `long long` and >struct desync→retained (correct — 64-bit stack-pair / large-copy not modeled by the Tier-4 capture, same class as arm64/riscv64 sumbig).) (64-bit div-magic helper DONE `7026372e` — but see the cross-optimizer blocker below: i386 reemit only fires with an optimizer-enabled cross build.) (native-shadow ASan heap+stack+global DONE — 32-bit shadow [0x7fff8000,0x9fff8000), qemu-i386 OOB→report proven; PIC/PIE stack path implemented but not yet runtime-tested.)
- arm (armv7): raise from Tier-2 — self-host, JIT stub tail. (Tier-4 replay-inline VERIFIED 2026-07-22 — `arm-inline-docker` ctest, differential 83911/0 vs arm-linux-gnueabihf-gcc -marm -O2 under qemu-arm: scalar/double/float/pointer/8-byte-reg-pair-struct all graft (0 residual `R_ARM_PC24`); `long long` (reg-pair) and >16B struct-by-value (AAPCS hidden pointer) desync→retained, same class as arm64/riscv64. Since the `R_ARM_CALL`/`R_ARM_JUMP24` interworking fix (`e74fe22a`) the differential links the struct-marshalling `__aeabi_memmove/memcpy` calls directly against the real Thumb armhf libc helpers with NO ARM-mode shim (cleanup done 2026-07-22; re-verified 83911/0 host-native under qemu-arm), so it now implicitly guards interworking too — a reloc regression would SIGILL. The dedicated guard is `arminterwork-docker.sh`. (native-shadow ASan heap+stack+global DONE — shares the i386 32-bit shadow; qemu-arm OOB→report proven; `-fPIC` (ET_EXEC) stack-redzone table load DONE — GOT-relative `ldr/add r0,r0,pc` via R_ARM_REL32 under `mcc_state->pic`, qemu-arm `-fPIC` stack-OOB→report proven; full `-pie` (ET_DYN) blocked by the shared mccasan PIE-startup bug below.) (-fPIC TLS GD/LDM + arm-link GD→LE relaxation DONE — standard __tls_get_addr scheme, mcc self-links + runs under qemu-arm, object ABI-conformant vs clang/binutils/lld shared links.)
- arm64: mode-6 object-output dispatch, in-place patch row, flip `opt_promote` on. (TLS DONE: `-fPIC`/`-fPIE` emits TLSDESC and mcc's own `arm64-link.c` relaxes it to local-exec for non-shared output — mcc self-links + runs global/static `__thread` under qemu-aarch64, relaxation byte-identical to lld; LE unchanged. Shared/DLL dynamic-TLS output still errors.)
- PE targets rollup — still open: full UBSan handler ABI (minimal-runtime only, needs `.rodata` descriptor tables), the i386/arm64-PE embed-jit per-arch baked blob (toolchain-gated, not a code bug), arm64-PE JIT native-fault class (HW-gated). (Landed: arm-PE UBSan recover, x86_64-PE asan-shadow, **i386/arm64-PE asan-shadow `d8f9d844`**, **i386-PE embed-jit resolver `27d256b1`**, arm64-PE JIT codegen-validated under wine.) (over-align on i386-PE/arm64-PE validated + ungated — no compiler gate existed; codegen matches ELF oracle byte-for-byte modulo LLP64 type sizes; i386-PE runs the alignas_over test natively; arm64-PE alignas_over runs OK under wine-arm64 in Docker.)
- Provide a weak-memory-model validator for aarch64/armv7 (qemu is x86-TSO). The linux-arm64 CI fuzz **band** is DONE and live: the differential fuzzer (portable gcc+clang majority-vote oracle, not the x86-only shadow-IV oracle) runs on native `ubuntu-24.04-arm` per-push — the CMakeLists guard (~3777) registers `fuzz/smoke`+`fuzz/matrix-*` on arm64 and the ci.yml `linux`/arm64 cell's ctest runs them (the fuzz-exclude at ci.yml ~128 is macOS-x86/Rosetta-only). NOTE: an extra nightly `campaign` soak was hand-added to matrix.yml and then dropped by commit `4abbede5` ("regenerate matrix.yml"). matrix.yml is now the **hand-edited source of truth** — the `ci_artifacts.h`/`ci emit` generator was removed (2026-07-23, commit cd7c042a), so re-add the soak by editing matrix.yml directly (fuzz-nightly.yml already runs the campaign via the `ci fuzz` verb). Still open: re-land the nightly soak; an armv7 cell; a dedicated concurrency/litmus validator (qemu x86-TSO can't catch weak-memory reordering).

## Parity gate triage (openability of the cross-arch gates)
Classification of every arch/host-scoped gate by how much work stands between it and default-on parity with the x86_64-ELF Tier-4 reference. Reference axes = the "Cross-arch parity" list above (self-host · promotion · cmov/csel · div-magic · JIT stub tail · ASan native-shadow · UBSan trap · TLS · Tier-4 replay-inline · over-align). The ~90 universal `MCC_AST_*` env gates (`mccast.c:1270`) are excluded — they toggle identically on every triple and belong to the "Ungate campaign", not parity.

- **Class 1 — openable now (validated; no per-triple codegen left):** (the `MCC_AST_SELECT`/`_ABS` flip and the UBSan test-gate widening both landed — detail in git.) re-audit of the `ast/replay-inline`/`-inline-spec` MCC_CPU test gates DONE (2026-07-22): riscv64 characterized (all cases graft except >16B struct-by-value; `inline_spec` fully holds) and given qemu-validated live coverage via the new `riscv64-inline-docker` ctest, but the MCC_CPU-gated fixtures were deliberately NOT widened — dormant (no riscv64/i386/arm-host CI build) and `inline.c` main desyncs on riscv64. i386/arm still unaudited; widening any MCC_CPU gate is gated on a host-arch CI build existing to exercise it (currently only x86_64 + arm64 hosts run).
- **Class 2 — bounded backend work, no HW needed (fully reproducible under qemu):**
  - riscv64 register promotion — DONE (`mccast.c:2516` now includes RISCV64; `MCC_TREG_SAVED`→s1..s11; validated OFF byte-identical + qemu differential 0-fail via `riscv64-promote-docker`; flip shares the global promotion soak). Callee-saved float pool still deferred (as arm64).
  - arm64 `opt_promote` default-on flip (pool exists `mccast.c:2538`; needs the seed soak).
  - i386 64-bit div-magic helper DONE (`7026372e`); signed-32 divmagic miscompile FIXED (`a1df06c1`). The `MCC_AST_DIVMAGIC` flip now only needs golden-regen + M8 soak. NB i386 divmagic only fires with an optimizer-enabled cross build (see cross-optimizer blocker below).
  - riscv64 Tier-4 (self-host gap + replay-inline ungate + JIT stub tail) — the last non-x86 arch; largest single lift.
  - i386/arm JIT stub tail. (Tier-4 replay-inline DONE for both — `i386-inline-docker`/`arm-inline-docker` ctests, 83911/0 each; scalar/fp/pointer/small-struct graft, long-long + large-struct correctly desync. Detail in the per-arch rollup above.)
  - i386-PE / arm64-PE ASan shadow — DONE (`d8f9d844`; runtime-proven i386 native + arm64 wine). Detail in the Sanitizers §.
- **Class 3 — blocked on a specific named bug (fix first):**
  - i386-PE embed-jit: the two named bugs are FIXED — the stdcall `__imp__NAME@N` matching in `pe_check_symbols` (`27d256b1`) and the float→int AST-replay desync (`b100da94`), both detailed in the JIT Windows/i386-PE §. What remains is NOT a code bug: the per-arch baked engine blob is toolchain-gated (needs an i386/arm64-compiled engine archive; this host's mingw is x86_64-only). (Short-import COFF reader for MSVC/LLVM/SDK libs landed separately — `coff_short_import_info`.)
  - True `-shared`/`.so` dynamic-TLS on arm/arm64/riscv64 — errors on all three; riscv64's `riscv64-link.c:373` GD→LE relaxation is a deliberately-incomplete stub.
  - i386 `-fPIC` TLS codegen — mcc emits `R_386_GOT32X` for a global `__thread` under PIC (crashes at runtime); emit a proper GD/LDM sequence, fall back to IE, or hard-error.
- **Class 4 — hard ceiling, cannot open without arm64-Windows silicon (qemu/wine mask via x86-TSO):** arm64-Windows MSVC JIT-exec miscompile; arm64-PE JIT native-fault subset (SEH/icache/frameless-leaf, codegen already wine-validated); the aarch64/armv7 weak-memory validator. Keep these explicitly `experimental`/skip-marked so they never false-green.
- **Cross-cutting enablers (each unblocks several parity axes at once):**
  - `.rodata` data-emission → full `__ubsan_handle_*` ABI (Sanitizers §) + §30 value-table dispatch.
  - Build the cross CMake targets (`mcc-i386`/`mcc-arm64`) with `MCC_CONFIG_OPTIMIZER` + `MCC_EMBED_JIT` — without them promotion/embed-jit silently no-op on cross, so those parity axes are untestable per-triple. CONFIRMED 2026-07-21: `cmake-cross/mcc-i386`'s compile defines are `-DMCC_TARGET_I386` with **NO `MCC_CONFIG_OPTIMIZER`**, so the ENTIRE AST reemit optimizer (divmagic, branchless select, promotion, replay-inline) is compiled out — `MCC_AST_VERIFY` emits nothing and even default-on `MCC_AST_SELECT` yields a branch (`jge`) not `cmov`. So NO i386 (or arm64-cross) reemit-parity axis actually fires or is validated in the normal cross build/CI; each was only ever proven via a hand-built optimizer-enabled cross compiler (e.g. the i386 64-bit divmagic 25/25 differential used a manual `clang … -DMCC_CONFIG_OPTIMIZER=1 src/mcc.c -o mcc-i386-opt`). This is the true gate on i386→Tier-4 parity being *real* rather than *latent*. Fix = add the optimizer (+embed-jit) to the cross target definitions in CMakeLists (mind CI build-time/size), then re-run the per-triple reemit differentials. PARTIAL 2026-07-21: added an opt-in CMake option `MCC_CROSS_OPTIMIZER` (default OFF, `VISIBLE_WHEN MCC_ENABLE_CROSS`) that appends `MCC_CONFIG_OPTIMIZER=1` to every `_cross_defs`; default-OFF verified byte-identical (cross build unchanged, CI-safe), ON verified to compile the optimizer into `mcc-i386` (+~46% binary: 698KB→1018KB — the size cost to weigh before any CI flip). ON path validated end-to-end: optimizer-enabled `mcc-i386` emits `cmovne` for `a<b?a:b` (was a branch) and the divmagic `mul/shr/sar` sequence for `/7` (was `idiv`); a 5400-iteration differential vs native gcc under `i386/debian` linux/386 Docker (signed/unsigned 32- AND 64-bit divmagic via `__mcc_umulh64`/`__mcc_smulh64` + branchless select) returned exit 0 = all match. `MCC_EMBED_JIT` on cross is now folded into the same opt-in (`MCC_CROSS_OPTIMIZER` ON also appends `MCC_EMBED_JIT=1`, satisfying the EMBED_JIT-needs-OPTIMIZER autocorrect), and the two i386-PE embed-jit resolver bugs it needs are fixed (`27d256b1`, see JIT Windows/i386-PE §). REMAINING: the per-arch baked `MCC_EMBED_JIT_BLOB` (toolchain-gated — needs an i386/arm64-compiled engine archive); arm64-cross reemit differential (ELF-arm64 runs under Docker arm64 / native); wire the flip into CI + golden regen + M8 soak before making it non-opt-in.
  - Skip-audit run-count assertion (anti-false-green): 27/54 `mcc_skip_test` sites are provisionable, not HW-gated (2 distinct ref compilers for diff3; `MCC_TEST_SH` on Windows; qemu-user+sysroots for cross-exec; `linux/386` docker for i386-fastcall). A triple can look at-parity while its proof silently no-ops (already tracked under Tests/infra skip-audit; linked here because it gates whether any parity claim above is *trustworthy*).

## Const-data (P2)
- Size-changing datacomp: `.init_array` decompress ctor (all 5 arches) + `__mcc_decompress` runtime (M6).
- TLS `tdata`→`tbss` + asan/bcheck zero-init `.bss` cases (M6z).
- §30 value-table dispatch (needs `.rodata` data-emission).

## CST (1A)
- slice-G multi-file `#include` stitching.
- `-g` from CST provenance (stands up the debugger + gdb suite); stop discarding the arena on the `--lsp`+`-g` path.
- Design `--hotreload` from reconciled CST snapshots.
- Revisit the Bind-marker (does CST supersede it?).
- Emit `CST_Error`/`CST_Missing` (error-recovery CST).

## Sanitizers
- Honor auto over-alignment under `-fsanitize=address` / `-b`.
- UBSan trap + `-recover` — **all 5 ELF arches landed (gated OFF); x86_64 ELF+PE validated, arm64/riscv64/i386/arm validated under qemu**. i386/arm had NO UBSan checks before, so their support added the underlying trap checks (div0, shift≥width, signed add/sub overflow; i386 also signed mul) plus the recover routing; `-fsanitize=undefined` trap mode now works on i386/arm (incl. PE, pure trap), and `-fsanitize-recover=undefined` works on i386/arm ELF. The `do_sanitize_recover` gate (mcc.h) is set by `-fsanitize-recover=undefined|all` / `-fno-sanitize-trap=undefined|all`, cleared by the inverse (libmcc.c ~2533, last-flag-wins). When ON, the per-arch emitter (`gen_ubsan_trap_or_call` x86_64-gen.c + i386-gen.c / `arm64_ubsan_emit_recover` arm64-gen.c / `riscv64_ubsan_emit_recover` riscv64-gen.c / `arm_ubsan_emit_recover` arm-gen.c) swaps the trap (`ud2`/`brk`/`ebreak`/`udf`) for a `call`/`bl` to `__ubsan_handle_<kind>_minimal` (kinds add/sub/mul/divrem_overflow, shift_out_of_bounds, type_mismatch_v1; all save the caller-saved GP set + link/return reg around the call; all emit precise per-op symbols, add/sub/mul distinct). The minimal-ABI handlers (runtime/lib/mccubsan.c, auto-linked via mcc_add_support in mccpe.c + mccelf.c for all ELF arches) log to stderr, RETURN, and register an `atexit` summary printing the recovered-event count — opt-in `MCC_UBSAN_EXITCODE=N` (or `UBSAN_OPTIONS=exitcode=N`) forces exit status N on any recovered UB. Per-check recover sets work: `-fsanitize-recover=<checklist>` / `-fno-sanitize-recover=…` / `-f[no-]sanitize-trap=…` parse a comma list (signed-integer-overflow / shift / integer-divide-by-zero / null / undefined / all) into a semantic bitmask (`MCC_SANR_*` in mcc.h; each arch maps its `UBK_*` enum via a local `ubsan_kind_recovers`), so a TU can recover some kinds and trap others (`=undefined` reproduces the old all-recover byte-for-byte). The cross build now stages per-arch `<arch>-mccubsan.o`/`<arch>-mccasan.o` (mcc_build_mccrt `prefix`+`san` params, sysroot-gated) so cross `-fsanitize-recover` links out-of-the-box. Default build is byte-identical (no `-fsanitize` ⇒ zero UBSan emission on every arch). Tests: `ubsan/recover_signed_add`; per-arch qemu freestanding matrices (real overflow/oob/div0 detected + recovered + continue, zero false positives); per-check add/subtract verified (only shift recovers under `=shift`; all-but-shift under `=undefined -fno-sanitize-recover=shift`). PE recover DONE on i386 and arm (`bb8573ae`; i386 earlier). Remaining: upgrade minimal→full ABI (emit `struct {SourceLocation,TypeDescriptor}` tables + operand values so the diagnostic names file:line and values) — large, needs `.rodata` descriptor emission. All ELF arches have full add/sub/mul/shift/div0 + div-INT_MIN/−1 coverage.
- clang-compatible `__ubsan_handle_*` diagnostic ABI — minimal-runtime variants DONE (see above). Full-runtime ABI (Data-pointer + ValueHandle args, SourceLocation/TypeDescriptor tables) is the remaining increment.
- ASan on PE — **x86_64-PE landed** (`cae17802`: `runtime/win32/lib/mccasan_win32.c` VirtualAlloc shadow + VEH `on_sigill` + HeapAlloc redzones; Win64-ABI stack prolog/epilog under `#ifdef MCC_TARGET_PE`, ELF byte-identical by construction; OOB→report validated natively, asan ctests 8/8). **i386-PE / arm64-PE ports DONE** (`d8f9d844`): dropped the `!MCC_TARGET_X86_64` hard-errors (libmcc.c + mccpe.c `pe_add_runtime`); added `runtime/win32/lib/mccasan_{i386,arm64}_win32.c` (copies of the x86_64 runtime with the VEH retargeted — i386 reads `Eax/Edx/Ecx` on `ud2`→`EXCEPTION_ILLEGAL_INSTRUCTION`; arm64 reads `X17/X16/X15` on `brk`→`EXCEPTION_BREAKPOINT`; i386 uses the 32-bit `[0x7fff8000,0x9fff8000)` shadow window); the per-arch inline probe + stack prolog/epilog already emitted PE-safe (i386-gen.c ASan stack blocks moved out of `#ifndef MCC_TARGET_PE`; arm64 needed none). i386-PE also sets `LARGE_ADDRESS_AWARE` when `do_asan_shadow` (default 2GB-limited exes can't reserve the shadow at 0x7fff8000). Runtime-proven: i386-PE heap/stack/global OOB→report + clean in-bounds native on WOW64; arm64-PE same under wine-arm64 in Docker; non-asan output byte-identical (i386/arm64/x86_64 md5s). Minor (inherited, both arches + ELF): the same-granule "…to the left" locator underflow in the shared `asan_locate`; msvcrt-internal CRT allocs aren't redzoned.
- Native-shadow ASan on riscv64: HEAP + STACK + GLOBAL redzones DONE (heap: inline `(addr>>3)+OFF` shadow check → `ebreak`/SIGTRAP + runtime shadow map/handler reading `__gregs[5/6/7]`; stack: `gen_asan_stack_prolog/epilog` emit `__asan_stack_enter/leave` reusing the arch-neutral `.asan_lstack` table + `gen_asan_stack_epilog_head`; globals: `__asan_globals` emission was already arch-neutral so needed zero riscv64 codegen). qemu-riscv64 proven: heap-OOB / stack-OOB (shadow 0xf2) / global-OOB (shadow 0xf9) → report, in-bounds clean. The Sv39 assumption was wrong — qemu-riscv64 user mode is Sv48-class, so the shadow reserves the x86_64 `[OFF,2^44+OFF)` span. REMAINING: access-type READ/WRITE + region-relative locator in reports; Sv39/bottom-up-mmap shadow-layout robustness; UBSan/ASan `ebreak` SIGTRAP coexistence.
- Port the native-shadow ASan report enhancements to x86_64 (3rd saved reg, qemu-amd64 validation), plus 39-bit-VA/bottom-up-mmap shadow robustness and the READ/WRITE + region-relative locator, then propagate to riscv64.
- Confirm the ShadowGap contiguous-mmap fix (commit `9623a4ed`) on real x86_64 CI — not locally reproducible (qemu can't run the `-fasan-shadow` runtime).
- Decide compiler-rt-interop vs `libmccsan`.
- Explore `-fsanitize-coverage`, `-fsanitize=cfi`, `_FORTIFY_SOURCE`, freestanding/KASAN-style runtime sanitizer.

## Tests / infra
- String-literal `L.N`/anon-symbol object-layout sensitivity (3 exec files excluded from object-diff) — ROOT-CAUSED: string literals are named `L.<v − SYM_FIRST_ANOM>` (`mccpp.c` ~691) off the global `anon_sym` counter, which is *shared* with anonymous structs/unions/compound-literals and struct tags (`mccgen.c` ~1229/4488/4704/4769). So a literal's symbol name shifts with unrelated *preceding* anon consumers and with any pass that allocates anon symbols in a different order/count — verified: `"hello"` is `L.2` compiled alone but `L.5` when a single `struct{union{int a;int b;};}` precedes it; same-source output is byte-deterministic, so the sensitivity is to code structure / opt-order / cross-compiler naming (gcc uses `.LC0`), not run-to-run nondeterminism. Fix (deferred, large): name rodata literals from a literal-local stable key (content hash or per-`.rodata` emission index) independent of `anon_sym`, then re-include the 3 files — but this renames every rodata-literal symbol ⇒ full object-golden regeneration + M8 soak.
- i386-fastcall-abi off-i386 residual: the ABI is now validated by the docker-gated `i386-fastcall-abi-docker` ctest (added inside the `TARGET mcc-i386` block; runs `tools/i386fastcall-docker.sh`, skips 77 when docker/mcc-i386 absent) — passes in `cmake-cross`. Optional leftover: teach the native `suite_i386fastcall` (tools/mccharness.c) a Docker execution backend so the mingw/`-m32` harness path also works off-i386, and confirm the project CI actually provides `linux/386` docker (else the new test just skips there).
- Audit `mcc_skip_test` per-triple ungating (i386-linux, aarch64/armv7-linux).
- Skip-audit + max-coverage provisioning (anti-false-green): many suites `mcc_skip_test`/`SKIP_RETURN_CODE 77` for *installable or configurable* reasons rather than genuine host incapability, and those skips are **silent** — a suite can no-op while `ctest`/CI stays green (e.g. diff3 with <2 distinct refs; the Windows reference-install step is `continue-on-error` and `MCC_DIFF3_READY` never fails the job). Build a per-host capability audit: enumerate every skip gate, classify each as legitimately HW/arch-gated (arm64-Windows HW, native-only) vs. provisionable-on-this-host, and document the exact install/config to un-skip each — diff3 needs 2 *distinct* reference compilers (`MCC_DIFF3_GCC`/`_CLANG`, or `MCC_DIFF3_EXTRA_REFS`); the structural cli/diff3 cases need binutils `nm`/`readelf` (dir cache var, ~CMakeLists:3615); diff3/preprocess on Windows need a POSIX shell (`MCC_TEST_SH`); i386-fastcall/`-fPIC`-TLS need `linux/386` docker; cross-arch exec needs qemu-user + sysroots (and clears `MCC_EMULATOR` gating where native). Then add a ctest/CI assertion that each expected suite actually *ran* ≥N tests on platforms that support it (parse `ctest-junit.xml` run-vs-skipped counts), failing the cell on a silent no-op so the differential/M8 gates can't false-green. FIRST INCREMENT DONE (`69bb952c`) for the diff3 case: the re-resolve step now re-sets `MCC_DIFF3_READY=1` on a successful ref install (closing the silent-skip window), and a new `ci junit-assert <xml> --expect diff3/=1` verb (reusing junit-summary's parser) fails the Windows cell when refs resolved yet 0 diff3 tests ran. Remaining: generalize the assertion across the other provisionable suites (preprocess/cli-structural/i386-fastcall/cross-exec) and the per-host capability audit above.
- RESOLVED (CI, 2026-07-22): the `msvc / x86_64` cell failing in `ci junit-assert … --expect diff3/=1` with **`cannot read cmake-msvc/ctest-junit.xml`** (CI #1082/#1086/#1093). Root cause was symptom (a) only, and it is a **ctest relative-path behavior change** shipped by the `windows-latest` VS18 image (ctest ≥4.3): `ctest --preset <p> --output-junit cmake-<p>/ctest-junit.xml` now resolves the `--output-junit` path relative to the **build directory** (`cmake-<p>/`) instead of the invocation CWD, so the XML landed at `cmake-<p>/cmake-<p>/ctest-junit.xml` and the repo-root lookup in the summary/assert steps missed it (ctest itself exits rc=0). Reproduced locally with the vendored ctest 4.3.3 (bare-relative → nested; absolute → correct). Fix: `ci run-preset`/`ci qemu` now pass `--output-junit` as an **absolute** path anchored at the invocation CWD (repo root) via a portable `ci_cwd` helper, so the XML lands at `<repo>/cmake-<p>/ctest-junit.xml` regardless of ctest version, matching where the workflow's `junit-summary`/`junit-assert` steps read it. Symptom (b) (all `diff3/*` Skipped) was transient to #1082/#1086 and had already self-resolved by #1093 (187 diff3 tests Passed there), so once the XML is readable `--expect diff3/=1` is satisfied. Unrelated to the ast-verify-ratchet drift (`b4b1c256`); this was Windows-runner-env, not repo codegen.
- RESOLVED (CI, 2026-07-22): `linux-gcc-cross / arm64` (+ clang) failing 3 docker ctests — `arm-inline-docker`, `riscv64-inline-docker`, `riscv64-promote-docker` — with a ~0.3s **Failed** (not Skipped) on CI #1091/#1092/#1093. Those runners are **arm64 hosts**, and the three scripts hardcode `docker run --platform linux/amd64` for their build stage; with no amd64 binfmt emulation on an arm64 host the run errors immediately and `set -e` propagated docker's exit 125 as a test failure. The sibling docker tests already skip cleanly there: `i386inline-docker.sh`/`i386-divmagic-soak` via a `linux/386` probe, and `extlink-docker.sh` via a `docker run --platform <plat> IMAGE true` capability probe (line 44) that maps can't-run → exit 77. Fix (`e2b21fe8`): added the same `docker run --platform linux/amd64 "$IMAGE" true` probe (→ exit 77) to the three scripts, so they SKIP on arm64 hosts while keeping full coverage on the x86_64 cross job (where all three Pass). Verified locally: the probe returns 0 on this x86_64 host and `riscv64promo-docker.sh` still passes end-to-end (`checks=60604 fails=0`). NB the ideal follow-up is to run these differentials *natively* on arm64 too (parameterize the build-stage platform like `extlink` does with `_extlink_host_plat`, since the qemu-user target stage is host-arch-independent) — currently arm64 hosts get zero coverage of these three, only x86_64 does.
- Normalize CMake incrementally (autodetect + fold `.cmake` files, verifiable target).
- Cut CI wall-clock: gate `bench`, shard macOS ctest, prune matrix re-runs. (Windows-job phase-timing + slowest-tests profiling landed `6ff9dbc1`.)
- macOS CI process-starvation (root cause fixed a0357638, pending CI confirmation): the runner exhausted `RLIMIT_NPROC` → ctest could no longer fork/exec test binaries → a `BAD_COMMAND` cascade across unrelated cells (preprocess/parts/jit/regression) plus starvation-induced fuzz "divergences". Root cause was the fuzz/diff3 shell timeout fallback (used when no `timeout`/`gtimeout`, the macOS default) leaking an orphaned `sleep 20` per wrapped compile/run — thousands of them under `--gates` — since it killed the watcher subshell but not its `sleep` child; now backgrounds `sleep` directly and kills it by PID. Harness retries (fuzz confirm-before-report, parts transient-spawn retry) remain as secondary nets. Contingency only if it recurs from raw concurrency (not the leak): a shared ctest `RESOURCE_LOCK` across `fuzz/*`+`parts/*`, or install coreutils on the macOS cell so `gtimeout` replaces the shell fallback entirely.
- Promote/inline replay-fidelity gap set (763 baseline gaps: 721 desync / 33 unfaithful / 9 stackresidue) — ROOT-CAUSED to a handful of AST-recorder bail sites in `mccast.c`, each an unmodeled construct that diverges the recorder's value-stack/AST model from real codegen. The largest *named* cluster — the glibc `<stdio.h>` inline family `putchar_unlocked`/`putc_unlocked`/`fputc_unlocked`, recurring across every `arch/*` baseline file — is `ast_hook_vstore` bailing at ~2409 on a store inside a ternary/`||` branch (`ast_tern_top > 0 || ast_lor_top > 0`), because `__putc_unlocked_body` expands to `cond ? (*p++ = c) : __overflow(...)` (verified: minimal `putchar_unlocked(65)` → `desync:2409`). Other hot bail sites: ~1452 (`ast_hook_vpush` value-stack-depth mismatch `ast_vn != rel-1` — a *downstream* detector; some earlier unmodeled push/pop already diverged the model), ~2135 (member/bitfield aggregate store), ~2294 (`ast_hook_call_begin` nested in-call). Fix (deferred, large, risky): model conditional stores under `?:`/`||` (the 2409 bail is a deliberate safety cutoff — correct store placement under the conditional is the hard part) plus the upstream constructs feeding 1452; needs the self-host + fuzz soak, and the AST substrate is desync-fragile (see arg-spill/frame-desync history).
- Link-time/ABI differential fuzzer (mix mcc `.o` with gcc `.o`). UNBLOCKED 2026-07-21: fixed a pervasive bug where mcc `.o` on every 64-bit ELF arch (x86_64/arm64/riscv64) could NOT be linked by GNU ld/lld — `mcc_eh_frame_fde` (mccdbg.c) emitted every FDE `initial_location` reloc with a hardcoded RELA addend of 0 (`dwarf_reloc` → `put_elf_reloca(...,0)`) and stashed `func_start` only in the in-place field. mcc's own relocator adds the in-place value (`*ptr += S+A-P`) so self-host worked, but standard RELA linkers use `S+A-P` and ignored the in-place value → all FDEs collapsed onto `.text+0` → GNU ld "`.eh_frame_hdr refers to overlapping FDEs`" hard-fail. Fix: on RELA arches carry `func_start` in the addend and write 0 in-place (REL arches — i386/arm — unchanged, addend 0 + in-place `func_start`). Self-host executables are byte-identical (reloc math: `0+S+func_start-P` == old `func_start+S+0-P`); only the `.o`'s in-place/addend split changes, now matching gcc. Validated: GNU ld links+runs correct on x86_64+arm64+riscv64 (were overlapping-FDE fails) + i386 regression clean, all under Docker vs native gcc; native ctest 624/624; dash-s-bytes ELF goldens green. COMPANION FIX (same root cause, DWARF): `-gdwarf` had the identical addend-0-on-RELA bug in `.debug_info` — `dwarf_reloc` (addend 0) + a non-zero in-place address at the subprogram `DW_AT_low_pc` (mccdbg.c ~2224 `func_ind`), lexical-block low_pc (`func_ind+cur->start`), static-var `DW_OP_addr` (`s->value`), and CU low_pc (`ind`). External GNU ld collapsed every subprogram low_pc onto `.text+0` (all funcs at one address → debugger can't distinguish them); mcc self-host worked because its relocator adds in-place. Fixed via a new `dwarf_reloc_addr(s,sym,rel,value)` helper (RELA: value→addend, 0 in-place; REL: unchanged) applied to those four sites. Validated: external GNU ld now yields distinct correct per-function low_pc on x86_64+arm64; native ctest 591/591 (dwarf/debug/exec/cli/dash-s-bytes) self-host byte-identical. Regression guard landed: `tools/extlink-docker.sh` + ctests `extlink-x86_64-docker`/`extlink-arm64-docker` (docker-gated, SKIP 77) link an mcc object with GNU ld (not mcc's own linker), require link+run and ≥5 distinct DWARF subprogram low_pc — nothing else in the suite externally links a 64-bit mcc object, which is why both bugs stayed latent. STABS (`-g` default) is external-link-correct — per-symbol relocs, verified via gdb; only `-gdwarf` had the low_pc bug. extlink now covers all three 64-bit RELA arches: `extlink-x86_64-docker` + `extlink-arm64-docker` (link+run+DWARF) and `extlink-riscv64-docker` (link+DWARF only via the riscv64 cross toolchain in a host-native container — no qemu-user here). Still TODO: the actual mcc↔gcc *mixed-object* ABI differential (extlink only links a single mcc object with GNU ld — it does NOT cross the mcc/gcc calling-convention boundary). The actual mcc↔gcc *mixed-object* ABI differential is now LANDED (2026-07-22): `tools/abidiff-docker.sh` + a self-contained no-system-headers corpus (`shared.h`/`lib.c`/`main.c` embedded in the script) declaring `struct Small`(reg-pair) / `Odd`(padding) / `Big`(indirect >16B) / `Mixed`(INTEGER+SSE) / `Float4`(HFA-float) / `DblPair`(HFA-double), plus char/short/int/longlong/stack args, float/double args, varargs, and struct-by-value returns; `main.c` computes each expected value inline and returns the 1-based index of the first mismatch (0 = all pass). It builds its own optimizer-enabled in-container mcc, compiles each TU with BOTH mcc and gcc, links every cross mix (mcc-lib+gcc-main, gcc-lib+mcc-main, both-mcc), and requires the exit code to match the all-gcc reference — any divergence is a real mcc struct/HFA/vararg ABI-boundary bug. VERIFIED on **arm64** (natively, no qemu on Apple Silicon), **riscv64** (LP64D, qemu-riscv64) AND **armv7** (AAPCS/hardfloat, qemu-arm) — the script grows a cross-toolchain + qemu-user path selected by `ARCH=riscv64|arm` (static-links so qemu needs no target sysroot, builds mcc with the container's native gcc but the target `-DMCC_TARGET_*`); the native arm64/amd64 path is unchanged. Every arch's all-gcc reference + all three cross mixes exit 0 — mcc's AAPCS64/LP64D/AAPCS struct/HFA/vararg marshalling is boundary-compatible with gcc. The armv7 cell additionally stresses ARM/Thumb interworking at the *call* boundary (mcc emits A32, gcc-armhf is Thumb ⇒ R_ARM_CALL/BLX substitution, cf. `e74fe22a`) — mcc-A32-lib called from gcc-Thumb-main and vice versa both run correct. Wired as docker-gated ctests `abi-diff-arm64-docker` (native), `abi-diff-riscv64-docker` + `abi-diff-arm-docker` (qemu), `abi-diff-amd64-docker` (SysV; all SKIP 77 when docker absent), in the `mcc-arm64`/`mcc-riscv64`/`mcc-arm`/`mcc-x86_64` blocks alongside extlink. CORPUS EXTENDED (2026-07-22) with harder aggregates — `Three`(12B 3×int) / `Nest`(nested) / `HFA3`(odd-count 3×float) / `IntFloat`(mixed 8B, one eightbyte) / `Uni`(union-by-value) / `Bits`(bitfields) / `HFA4`(32B 4×double — HFA on arm64/armv7, indirect on riscv64/x86_64) / `long double` scalar args+return — all green on **arm64/armv7** (24 checks); `long double` is boundary-conformant on all three non-x86 arches (no bug). The HFA4 pass + long-double additions FOUND a second real ABI bug — the **riscv64 caller FP-arg + indirect struct return** defect (next-next item) — now **FIXED** (`gfunc_call` sret-arg param-walk); `hfa4_make` runs un-guarded on all four arches. A `Wide{double a; long b; double c; long d;}` (32B mixed non-HFA, indirect return, interleaved FP+INT args — `c` is the 2nd FP arg that pre-fix went to a GP reg) locks in the general case; green on all four. Cross-arch verified the bug is riscv64-ONLY: armv7 `assign_regs` classifies args by their own type (VT_FLOAT/DOUBLE→VFP), not proto-param position, so no named/unnamed misalignment; arm64 passes the indirect result in the dedicated x8 (`--nb_args`); i386 passes all args on the stack. amd64 also RUN now: it needed an `__va_arg` link fix (mcc lowers SysV x86_64 `va_arg` to a runtime-helper call defined in `runtime/lib/va_list.c`; other arches inline it — the harness now compiles that helper with mcc and links it into every mix) and it **surfaced a real x86_64 ABI bug** — the mixed INTEGER+SSE `struct Mixed{int;double}` (next item), so that one case is `-DABI_SKIP_MIXED`-guarded on amd64 (still exercised on the 3 non-x86 arches, where mcc is conformant). Still open: the *fuzzed* version (this + extlink are fixed-corpus guards); a native (non-docker) Linux-CI variant so the guard also runs in the normal build; `long double` + returned-HFA edge cases.
- **DWARF `.debug_line` last-function coverage — FIXED 2026-07-22 (found by a new mcc-vs-gcc gdb/DWARF differential).** mcc emitted the `.debug_line` `DW_LNE_end_sequence` at the last *line-change* address instead of the code end, because the line-program PC only advances on a line change (`mccdbg.c` ~1384). So the LAST function in a CU got a zero-length final line range and gdb reported it in a backtrace as e.g. `main ()` with NO `at file:line` — the caller frame lost its source location, degrading `bt`/stepping for the last function. General (arch-independent `mccdbg.c`), found on arm64 via gdb (the only tool that consumes the line table end-to-end; extlink only checks distinct `DW_AT_low_pc`, and gdb needs real ptrace so it cannot run under qemu-user). FIX: before `end_sequence` in `mcc_debug_end`, advance the PC from `dwarf_line.last_pc` to `text_section->data_offset` (the code end) so the last line range spans to the function end. Confirmed on native arm64: gdb `bt` now prints `#1 main () at t.c:7` (was `main ()`); decoded line table now ends at the code end (0x844) not the last line-change. No regression: dwarf/debug + dash-s-bytes byte-golden ctests green (`-gdwarf`-only change, no codegen impact). Guard: `tools/dwarfgdb-docker.sh` + ctest `dwarf-gdb-docker` (host-native, docker+gdb-gated, SKIP 77) asserts every backtrace frame has a source line. A SECOND, more serious DWARF bug was then found the same way (2026-07-22) and FIXED: **`break FUNC` broke in the WRONG function for single-line functions.** mcc emits one line-table row per function at the *entry* (prologue start); it also emits `DW_LNS_set_prologue_end` (`mcc_debug_prolog_epilog`) but that only flags the NEXT row — and for a function whose declaration line == its first statement line (single-line functions like `long getval(int a,int b){ return a*b+a; }`), the next `mcc_debug_line` is suppressed (`if (last_line_num == f->line_num) return;`, mccdbg.c ~1386), so NO post-prologue row is emitted. gdb's `break FUNC` prologue-skip then overshoots to the NEXT function's entry: on native arm64, `break usept` set the breakpoint at `addup`'s address and gdb stopped in `addup` with garbage args (the "-O1 garbage locals" I first suspected were this same overshoot, not a location bug). FIX: a `dwarf_line.prologue_end` flag set in `mcc_debug_prolog_epilog(…,0)` that makes `mcc_debug_line` bypass the same-line suppression once, emitting a post-prologue row at `ind` to carry the prologue_end. Multi-line functions are unchanged (their line-change row already carries it); STABS unaffected (flag is dwarf-only). Validated on native arm64: `break usept` now stops in `usept` (`p.x/p.y/p.z` correct, `ptype` correct) and `break addup` reads `a=3 b=4 c=5`; dwarf/debug + dash-s-bytes ctests green (14/14, no golden break). The `dwarf-gdb-docker` guard now also asserts `break` lands inside a single-line function with correct args. The `dwarf-gdb-docker` guard was later extended (2026-07-22) to also single-step `steps(10)` and assert the locals read `a=11 b=22 c=19` — line-stepping + `DW_AT_location` accuracy, confirmed correct on arm64 at BOTH `-O0` and `-O1` (no bug). **x86_64 completion (2026-07-23):** the prologue_end-bypass fix above was INSUFFICIENT on x86 and the x86_64 CI stayed red on `dwarf-gdb-docker`. Making `mcc_debug_line` re-enter with `len_line==0` is not enough: the else-branch only ever APPENDS a row when `len_line!=0` or the pc-only special opcode fits (`n = len_pc*14 + 18 <= 255`). x86 leaf prologues are 19 bytes → `n = 284 > 255` → it emitted `DW_LNS_advance_pc` (advances the address register but appends NO row), so the prologue_end flag LEAKED onto the next function's entry row and `break getval` still overshot into `steps` (arm64's smaller prologue fit the special opcode, appended a row, and masked this — why arm64 passed but x86 didn't). FIX (`mccdbg.c` else-branch): track `row_emitted`; if nothing appended a row, emit `DW_LNS_copy` to materialize the post-prologue row at the advanced PC. Byte-identical for all multi-line functions and the special-opcode-fit path (incl. arm64's single-line case) — the only new output is one `DW_LNS_copy` in the previously-broken x86 single-line case. Validated in Docker (debian+gdb, `-DMCC_TARGET_X86_64=1`): `dwarf-gdb-docker` now exits 0 — backtrace frames keep source lines, `break getval` stops IN getval (`a=11 b=20` at t.c:7), stepping reads `a=11 b=22 c=19`. Open follow-ups: variable locations under heavier `-O1+` register promotion, lexical-block scoping; riscv64/armv7 not gdb-tested here (qemu ptrace) but share all fixes.
- **Runtime `.eh_frame` CFI (C++ exception unwinding through mcc-C frames) — VALIDATED CONFORMANT 2026-07-22 (new mcc-vs-g++ differential).** extlink only checks that mcc's FDEs LINK (no overlapping FDEs); nothing exercised the CFI at RUNTIME. New `tools/ehunwind-docker.sh` + ctests `eh-unwind-{arm64,riscv64,amd64}-docker`: a g++ `main` throws a C++ exception from a callback invoked by an mcc-compiled C bridge, and the `catch` only fires if libgcc's `_Unwind` can walk mcc's `.eh_frame` for the bridge frame (compared against an all-g++ reference). RESULT: mcc is conformant — the exception unwinds through mcc frames correctly on **arm64** (native) and **riscv64** (cross g++ + qemu), matching all-g++; no bug. armv7 SKIPs: ARM EABI uses EHABI (`.ARM.exidx`), not `.eh_frame`, and C code by default has NO unwind tables in BOTH mcc and gcc, so a C++ exception cannot unwind through a C frame on either (both terminate identically — mcc matches gcc, no divergence). So mcc's `.eh_frame` runtime CFI is now a permanent guard for the interop scenario (mcc-compiled C mixed into a C++ codebase) on the DWARF-CFI arches. Open: an armv7 `.ARM.exidx` emission path IF mcc-C ever needs to be unwindable-through in a C++/`-funwind-tables` context (matches gcc today, so not a bug — a feature gap only if that use case arises).
- **x86_64 mixed INTEGER+SSE small-struct ABI bug (found by the ABI differential, 2026-07-22).** mcc miscompiles the SysV passing of a ≤16B struct whose two eightbytes have *different* classes — canonical case `struct Mixed{int i; double d;}` (eightbyte-0 INTEGER, eightbyte-1 pure SSE). Root cause: `classify_x86_64_arg`/`classify_x86_64_merge` (x86_64-gen.c ~1062) compute a SINGLE whole-aggregate mode (and `merge` makes INTEGER win over SSE), then pass `reg_count` registers of that one class — so `Mixed` goes in two GP regs (rdi,rsi) instead of {rdi(INTEGER), xmm0(SSE)}. Confirmed via disasm: mcc's `mixed_sum` does `mov %rsi,-0x8(%rbp); addsd -0x8(%rbp),%xmm0` (reads `d` from rsi), so a gcc caller (which passes `d` in xmm0) → mcc callee yields 3.0 not 3.5. LATENT because self-host/self-tests compile both sides with mcc (self-consistent-but-non-conformant), and extlink links only single objects — nothing crossed to a conformant compiler until this differential. Also affects mixed-class RETURNS (`{double;int}`, `{int;double}` in rax+xmm0) and any {SSE,INTEGER}/{INTEGER,SSE} 9–16B aggregate. FIX (substantial, x86_64-only, needs the M8 soak): do proper per-eightbyte SysV classification — classify each eightbyte independently, thread the two half-classes through `gen_call` arg placement, `gen_func_prolog` param home-slot stores, and the return path, mirroring tcc's `regsize`/`sse` split. Reproducer: `tools/abidiff-docker.sh` with the `ABI_SKIP_MIXED` guard removed (the `Mixed` case then fails `mcc-callee` mixes on amd64). Homogeneous-per-eightbyte aggregates (all-INTEGER or all-SSE, incl. HFAs `Float4`/`DblPair`/`HFA3`) are correct — the whole-aggregate collapse only breaks when eightbytes differ.
- **riscv64 caller FP-arg + indirect struct return ABI bug — FIXED 2026-07-22 (found by the ABI differential).** When a riscv64 (LP64D) call BOTH returned a >16B struct indirectly (hidden sret pointer in a0) AND passed floating-point arguments, mcc's CALLER mis-allocated the FP arg registers: for `struct HFA4{double a,b,c,d;} hfa4_make(double,double,double,double)` (returns 32B → indirect) it put a,b,c in fa0,fa1,fa2 but the 4th double `d` in GP reg **a1** instead of **fa3**; a gcc callee read `fa3` (untouched) → `d=0`. ROOT CAUSE: the front-end prepends the hidden sret pointer as arg 0 (`mccgen.c` ~9718 `nb_args++`), but `gfunc_call` (riscv64-gen.c:594) started its `sa` param-proto walk at the first real param and advanced it once for the sret arg too — so every real arg matched the WRONG param and the trailing arg fell off the named list, where riscv64's unnamed-FP-in-GP rule (`reg_pass` named=0 → `MCC_RC_INT`) sent a trailing FP arg to a GP reg. INTEGER-arg cases (`big_scale`) were invisible (named/unnamed INTEGER both use GP); ≤16B struct returns (`dbl_swap`/`hfa3_scale`) don't prepend a sret arg; arm64/armv7 use a dedicated indirect-result reg (x8 / no sret arg). FIX: detect the sret arg (return type struct with size>16) and do NOT advance `sa` for arg 0, so real args realign with their params (fa3 restored). Validated: `abi-diff-riscv64-docker` now green with `hfa4_make` un-guarded (all 3 cross mixes) + riscv64-promote/inline docker differentials clean (no regression). Change is riscv64-gen.c only (other arches don't compile it). ASYMMETRIC-latent like the x86_64 mixed-SSE bug above (self-host compiles both sides with mcc; extlink links single objects) — this is why the mcc↔gcc differential was needed to surface it.
- **riscv64 FP-arg spill to GP registers — FIXED 2026-07-22 (found by the ABI differential; was PRE-EXISTING, unrelated to the sret fix).** The RISC-V LP64D convention passes FP args in fa0-fa7, and when those are exhausted passes remaining FP args "according to the integer calling convention" — i.e. in the GP arg registers a0-a7 (if free), and only then on the stack. mcc sent an exhausted FP arg straight to the stack (`gfunc_call` ~632 / func prolog ~928 tested only `areg[FP] >= limit`, never `areg[GP] < 8`), so a call with >8 FP args diverged from gcc: `struct Ten{double a..j;} ten_make(double a..j)` — a-h fill fa0-fa7, then mcc put i,j on the stack while gcc put them in a1,a2 → a gcc peer read `i=0 j=0`. KEY INSIGHT enabling a clean fix WITHOUT new `fmv`: mcc already places variadic doubles in GP regs (the `vmix_id`/`vsum_d` tests pass), because `reg_pass(named=0)` classifies them as INT and the emission loads the double's bits into a GP reg via an integer load. FIX: in both `gfunc_call` (caller) and the func prolog (callee), when a SCALAR FP arg finds its FP arg regs exhausted (`areg[1] >= 16` caller / `>= 8` callee) but a GP arg reg is free (`areg[0] < 8`), re-classify `prc[1] = MCC_RC_INT` — the arg then flows through the existing GP path (bits loaded/stored as integer; the callee's home-slot round-trip reloads it as a double), matching gcc with no fmv. Scalar-only (`prc[0] == 1`) so structs are untouched; gated on FP-exhaustion so <=8-FP-arg code is byte-identical. Validated: `abi-diff-riscv64-docker` green with `ten_make` un-guarded (all 3 cross mixes); riscv64-promote byte-identical vs pristine (no regression on ordinary code). riscv64-gen.c only. Reproducer: `tools/abidiff-docker.sh riscv64` (the un-guarded `ten_make` case).
- **riscv64 `_Complex` arg spilled to STACK instead of GP regs when FP regs exhausted — FIXED 2026-07-22 (found by a new `_Complex`/`__int128` ABI differential).** Same FLAVOR as the scalar FP-spill above but for a 2-FP-member aggregate. RISC-V passes a `float`/`double _Complex` (treated as a struct of two FP members) in FP regs ONLY IF BOTH members fit; otherwise it reverts to the INTEGER calling convention — `double _Complex` (16B) → 2 GP regs (a_n,a_n+1), `float _Complex` (8B) → 1 GP reg (both floats packed), or split/stack if GP also exhausted. mcc's `gfunc_call` (~642) and `gfunc_prolog` (~934) sent a 2-FP aggregate that could not fit in FP regs STRAIGHT TO THE STACK (`nregs==2 && prc[1]==prc[2]==MCC_RC_FLOAT && areg[FP]>=limit` → stack path), so `double _Complex cd_after7(double a..g, double _Complex z)` — a-g fill fa0-fa6, z needs 2 FP regs but only fa7 free → mcc stacked z while gcc/clang put it in a0,a1. Both cross-mixes failed, both-mcc passed (self-consistent-but-non-conformant). gcc==clang confirmed the placement (not impl-defined). FIX (mirrors the scalar fix, reuses existing machinery): when a 2-FP aggregate hits the FP-insufficient threshold (`areg[1]>=15` caller / `>=7` callee), re-invoke `reg_pass(type,prc,fieldofs,/*named=*/0)` — the `!named` path already classifies by SIZE into the integer convention (8B→1 GP, 16B→2 GP, correct fieldofs for integer loads), so the aggregate flows through the existing GP-reg/split/stack path exactly like a variadic/unnamed value. Handles BOTH float (1 reg) and double (2 reg) complex, plus any 2-double struct (e.g. HFA `DblPair`) past FP exhaustion. Gated on FP-exhaustion so ≤6-FP-arg code is byte-identical (verified: an FP-heavy `fp.c` incl. a non-exhausted `double _Complex` compiles byte-identical before/after). Validated on all four arches (`_Complex` differential PASS on arm64/riscv64/armv7/amd64 — riscv64 was the only one wrong; the others were already conformant). Regression guard: the `cx_cf_id`/`cx_cd_id`/`cx_cd_after7`/`cx_cf_after7` cases added to the abidiff corpus (helper-free: only pass/return/add, so no `__mcc_cmul` runtime dep). riscv64-gen.c only. NOTE: `__int128` is NOT a source-level type in mcc (VT_QLONG is internal-codegen-only, no `TOK___int128`), so the differential covers `_Complex` only. NOTE: mcc lowers `_Complex` MUL/DIV to its own runtime helpers `__mcc_cmul`/`__mcc_cmulf`/`__mcc_cmull` (`runtime/lib/complex.c`) like `__va_arg` — an mcc object doing complex mul/div must link the mcc runtime (gcc uses libgcc `__muldc3`, auto-linked); the corpus guard avoids mul/div so it needs no helper.
- **HFA/aggregate argument-register EXHAUSTION — VERIFIED CONFORMANT 2026-07-22 (no bug; probed as the sibling of the riscv64 `_Complex` fix).** Tested the AAPCS64 rule C.3 + NSRN=8 corner: an HFA that does not fit in the remaining SIMD arg registers goes ENTIRELY on the stack AND sets NSRN=8, so every SUBSEQUENT scalar FP arg also goes on the stack (it must NOT backfill the leftover reg). Cases: DblPair after 7 doubles (only v7 free, needs 2 → stack; trailing double → stack), Float4/HFA4 after 5 doubles, DblPair after 8 doubles (all v0-v7 used). mcc is CONFORMANT on arm64 (correct NSRN=8 no-backfill), riscv64 (the exhausted 2-FP DblPair goes to GP regs per the integer convention — same path as the `_Complex` fix, confirming that fix generalizes to non-complex 2-double structs), armv7 (VFP HFA) and x86_64 (SysV SSE→memory overflow). Regression-guarded by `hfx_nsrn`/`hfx_full` added to the abidiff corpus (all four arches green). This was the natural next FP-exhaustion probe after the `_Complex` bug; it came back clean — the exhaustion vein for aggregates is now conformant across all arches.
- **over-aligned struct-by-value register rounding — FIXED 2026-07-22 (arm64 + armv7; found by the ABI differential; obscure).** mcc over-applied the AAPCS64 16-byte NGRN even-rounding to *artificially* over-aligned structs. `struct A16{long long a,b;} __attribute__((aligned(16)))` passed after an int (`a16_after_int(int pad, struct A16 s)`): **gcc AND clang both put `s` in x1,x2** (consecutive; `pad` in x0) — the even-register rounding tracks NATURAL 16-byte alignment (`__int128`), NOT the `aligned` attribute. mcc rounded to x2,x3. Confirmed by disasm on native arm64 that mcc was CONSISTENT (not asymmetric — my earlier "caller wrong, callee correct" read was mistaken): BOTH mcc caller (`ldp x2,x3`) and callee (reads s.a/s.b from the x2,x3 home slots) rounded, so both-mcc passes and both cross-mixes fail vs gcc. arm64 FIX: new `arm64_natural_align16(CType*)` helper (recurses structs/arrays; true only for a fundamental 16-aligned member — VT_QLONG `__int128` / VT_QFLOAT / VT_LDOUBLE) gates `arm64_pcs_aux` (~1032) `if (align == 16 && arm64_natural_align16(type[i])) nx = (nx+1)&~1;`. Since both caller and callee share `arm64_pcs_aux`, the one-line gate fixes both. `__int128` still rounds (helper true); the `aligned`-attribute struct no longer rounds → matches gcc/clang. Validated: `abi-diff-arm64-docker` green with `a16` un-guarded; native arm64 macOS ctest green (no regression — the change only affects 16-aligned args reaching that PCS line). armv7 FIX (same shape, confirmed by qemu-arm: only the `aligned(16)` case diverged — natural-align-8 16B structs like `struct{ll,ll}` were already correct): armv7 has NO naturally-16-aligned fundamental type, so the register even-rounding must use the NATURAL member alignment, clamped to [4,8]. New `arm_pcs_natural_align(CType*)` (max natural member alignment, recursing structs/arrays) gates the rounding in BOTH `assign_regs` (caller ~915) AND `gfunc_prolog` (callee ~1328) — only when `align > 8`, so ≤8-aligned code is byte-identical; the stack-slot alignment paths (caller ~942, callee ~1337) still honor the attribute. Validated: `abi-diff-arm-docker` green with `a16` un-guarded (arm64 rechecked green too). riscv64 + x86_64 agree with gcc. Both arm arches now conformant; the guard is removed.
- **x86_64 packed struct-by-value divergence — UNVERIFIED (found by the ABI differential, 2026-07-22).** `struct Packed{char a; long long b; int c;} __attribute__((packed))` (13B, unaligned members) passed by value: on x86_64 an mcc caller AND callee diverge from gcc (both cross-mixes fail at exit=36, both-mcc passes → self-consistent-but-non-conformant), while arm64/riscv64/armv7 all match gcc. NOT yet clang-cross-checked, so it is not confirmed an mcc bug vs a gcc-vs-clang impl-defined corner (packed-struct-by-value register classification on SysV is a known impl-defined-ish area). NEXT: disasm mcc vs gcc vs clang on x86_64; if gcc==clang, it is a real mcc SysV packed-classification bug; if gcc≠clang, drop the guard as an ambiguous corner. Reproducer: `tools/abidiff-docker.sh amd64` with `ABI_SKIP_PACKED` removed. Guarded on amd64 (`ABI_SKIP_PACKED`) meanwhile; x86_64-only so out of the non-x86 scope but noted.
- **extern `__thread` reference emitted as STT_NOTYPE, not STT_TLS — FIXED 2026-07-22 (found by the ABI differential cross-object TLS test; ALL arches).** When mcc compiled a TU that only REFERENCED an external `__thread` variable (`extern __thread long g_tls; return g_tls;`), the undefined `g_tls` symbol in the object was `STT_NOTYPE`, not `STT_TLS`, so GNU ld refused to link it against a TLS definition (`g_tls: TLS definition in .tdata mismatches non-TLS reference` → `error adding symbols: bad value`). Same latent interop class as the extlink RELA-addend bug (mcc self-links leniently). ROOT CAUSE (isolated by tracing): the symbol is created CORRECTLY as STT_TLS (`put_extern_sym2` with `VT_TLS` set), then `mccelf_end_file` (mccelf.c:190) deliberately DOWNGRADES every undefined GLOBAL symbol to `STT_NOTYPE` when `output_type == MCC_OUTPUT_OBJ` — harmless for regular symbols (GNU ld tolerates a NOTYPE undef ref) but WRONG for TLS (an undef TLS ref must stay STT_TLS). FIX (one line): exclude `sym_type == STT_TLS` from that reset. Validated on native arm64: `readelf` now shows `UND TLS g_tls`; both-mcc and mcc-main+gcc-lib LINK with GNU ld and RUN correct (`a=314 b=314`). Cross-object TLS now passes UN-GUARDED on arm64/riscv64/armv7 (`abi-diff-*-docker`); native ctest 315/315 (dwarf/debug/dash-s-bytes/cli — non-TLS undef symbols still reset to NOTYPE, so those goldens are byte-identical). mccelf.c only; benefits all arches. The TLS-DEFINITION side was already correct (mcc emits STT_TLS for the def, so `mcc-lib+gcc-main` always linked). COVERAGE EXTENDED 2026-07-22 (no new bug — clean interop guard): the abidiff corpus now also crosses a **zero-init `__thread` (.tbss, default 0)** var (`g_tls_z` — the STT_TLS fix applies to the undef ref regardless of `.tdata`/`.tbss` section, and the def is emitted STT_TLS in `.tbss`), a **weak-def override** (`main.c` strong `wk_ovr=999` beats `lib.c` `__attribute__((weak)) wk_ovr=111`), and an **`__attribute__((alias))`** symbol — all conformant across every mcc/gcc mix on arm64/riscv64/armv7/amd64. This probe was motivated by the TLS bug living in the object-write finalizer (`mccelf_end_file`), which also manipulates symbol binding/type; weak/alias/common symbol-attribute emission was verified clean (readelf shows `OBJECT WEAK`, alias at the target address, common in `.bss`) — no finalizer bug there.
- Coverage-guided generation (gcov/Intel-PT into `tests/fuzz/gen.h`).
- EMI mutation (Orion/Athena/Hermes) targeting optimizer miscompiles.
- Self-host differential (compile `src/mcc.c` mcc vs gcc, diff over corpus). (The self-host *correctness smoke* — a distinct M8 sub-gate: mcc self-compiles `src/mcc.c -O2`, links a self-hosted mcc, and that mcc must build+run fib/quicksort correctly — is now WIRED as the native `selfhost-smoke` ctest, 2026-07-22. `tools/selfhost-smoke.py` was an unwired orphan and had ROTTED: it linked only `mccrt_blob.c.o`, but `MCC_EMBED_JIT_BLOB=1` (now default) makes `mcc.c`/`libmcc.c` reference `mccjit_blob`/`_len` → unresolved-symbol link fail; fixed to also link `CMakeFiles/mcc.dir/mccjit_blob.c.o` when the compile flags embed the JIT blob. ctest gated on `MCC_PYTHON3 AND MCC_EMBED_MCCRT` so it runs only on native-ELF (Linux; WIN32/Darwin force `MCC_EMBED_MCCRT=OFF` → auto-skips), verified registering+passing under Docker Linux and not registering on WIN32. A second variant `selfhost-smoke-gates` self-hosts with the full flip-pending gate set forced on (`MCC_AST_PROMOTE COLOR LICM_TEMP IVSR PRE REASSOC SETHI_LEAF NARROW_ELIM ARGFWD SPILL_SHARE VLAT DIVMAGIC` =1) — an enforced self-host-under-optimization soak feeding the ungate campaign (both pass green on Linux). The **3-stage self-host fixpoint** M8 bar is now ALSO enforced — `tools/selfhost-fixpoint.py` + ctest `selfhost-fixpoint`: stage0 `mcc` compiles+links `mcc1`, `mcc1`→`mcc2`, `mcc2`→o3, asserting o2==o3 (fixpoint) AND o1==o2 (host-cc- and mcc-built mcc emit identical code). Verified byte-identical on Linux (o1==o2==o3, ~2.26 MB, 3.5s). Same native-ELF gating. NB `o1==o2` (gcc-built mcc and mcc-built mcc emit BYTE-IDENTICAL code) IS the "mcc vs gcc" self-host differential in its strongest form — stronger than compiling a corpus with each and diffing runtime behavior, which can only catch *divergence*; identical objects mean the two compilers are the same. The general mcc-vs-gcc *program* differential over arbitrary sources is separately the differential fuzzer's domain (M8 bar: differential fuzz x86_64 + native arm64). So this item is DONE.)
- The 1 pre-existing `^exec/` failure on native x86_64-Linux (1/297, 2026-07-22) is a **non-reproducible environmental flake, NOT an mcc bug** — RESOLVED (2026-07-22): re-ran the exact in-Docker optimizer-ON Debug config **45×** (harness-default gate + the full 12-gate set + 30 persistent-container loop iterations, incl. `-j24` oversubscription with CPU-hog load) → **297/297 every run**. Key mechanic that rules out a miscompile: the base `exec/<name>` ctest variant runs mcc at **-O0** (`MCC_TEST_OPT` unset; `ast_replay_env = optimize>=1` ⇒ the AST reemit optimizer emits nothing), and the `^exec/` anchored filter (trailing slash) EXCLUDES the `exec-replay*`/`exec-promote*` -O1+ variants — so a base-exec default failure cannot be an optimizer-default-on miscompile. Near-certain culprit on recurrence: one of the 4 threading/atomic tests (`atomic_counter` = 48 threads × 65535 RMWs, `c11_threads`, `atomic_inlang_rmw`, `tls`) under Docker-Desktop-VM CPU contention (mcc atomics are library calls — identical codegen). `native-optcheck.sh` now runs the default pass with `--output-on-failure` + a `=== DEFAULT FAILURES (names) ===` echo, so any recurrence self-identifies by name. OPTIONAL hardening if it recurs: ctest `TIMEOUT`/`RESOURCE_LOCK` on those 4 tests, or reduce the atomic loop counts.

- Regenerate the `ast-verify-ratchet` baselines for the non-win32/linux targets — they SKIP until regenned.
- Model the AST recorder's `desync` bucket shapes (struct-by-value ABI, bitfields, VLAs, C11 atomics, `__attribute__((cleanup))`, `_Complex`, `_Noreturn`, inline asm) — the recorder currently declines these, each an unmodeled construct that can silently miscompile once the fidelity net is retired.
- Provide a runnable test for the kernel-fused libSystem path (`MCC_DARWIN_HOST=ON`) — nothing exercises it currently.
- Promote the `mingw i686` CI cell off `continue-on-error`/`matrix.experimental` (ci.c PLAN_MINGW `{"i686", 1}`) once i386-Windows codegen is proven.
- Confirm the `-fexcess-precision=standard` i386 WIN32 refflags fix (CMakeLists.txt ~3799) on the `windows-2025-vs2026` UCRT CI image — speculative, only reproduces there.
- Fix the `linux-clang`/`-cross`/`-release` mccrt-embed build break: host clang rejects `runtime/lib/builtin.c` (`__builtin_bswap64` alias) + `runtime/lib/atomic.c` (`__atomic_*` redeclare); `-fno-builtin` is insufficient — pick `#pragma redefine_extname` or force `MCC_EMBED_MCCRT=OFF` on clang hosts.
- Wire `tools/i386fastcall-docker.sh` into CMake as a docker-gated ctest (SKIP 77 when docker/`mcc-i386` absent) — committed but not integrated into ctest.
- Validate stack auto over-alignment on i386-PE and arm64-PE (the test stays x86_64-gated).

### Failing tests observed on this Windows host (build-win, 2026-07-22) — pre-existing, host-environment, unrelated to the win-open-work changes; logged for completeness regardless of history
- Docker-gated cross-exec tests hard-FAIL on this host with `docker: Error response from daemon: the working directory 'W:/' is invalid` — FIXED: MSYS/git-bash rewrote the container-side `/w` of `-v`/`-w` to `W:/`. Guarded each `docker run` in `tools/{i386fastcall,i386diff,i386tls,extlink}-docker.sh` with `MSYS_NO_PATHCONV=1 MSYS2_ARG_CONV_EXCL='*'` (the pattern `arm64pe-wine-docker.sh` already used; no-op on real POSIX/Linux CI). Validated end-to-end on this host vs native gcc under Docker: `extlink-x86_64` (link+run+6 distinct DWARF low_pc), `i386-fastcall` (3 ABI directions), `i386-tls` (non-PIC + GD/LDM), `i386-codegen-diff` (d9–d14 mcc==gcc at -O0/-O2) all exit 0 (were hard-failing). The `-v` mount passes the `/c/...` MSYS path literally and Docker Desktop accepts it.
- NOTE (not a repo defect, self-inflicted, recorded per request): a first regression-sweep invocation reported `cmake: command not found` / `ctest: command not found` — the CLion cmake/ctest bin dir wasn't on PATH in that shell (it has no gcc/cmake on PATH by default). Re-run with absolute `cmake.exe`/`ctest.exe` or prepend `.../CLion/bin/cmake/win/x64/bin`. No action needed on the repo.

## macOS (Mach-O / Darwin)
- Build a real Mach-O relocatable `.o` reader/writer ("A2") — `mcc_object_type` only sniffs ELF/ar today; unblocks bare-dylib frameworks, host-CC embed, and `ld` interop.
- Add a built-in ad-hoc codesigner — currently shells out to `/usr/bin/codesign`.
- Wire `machofat` into `dist-macos` packaging.

## Front-end / diagnostics
- Implement the empty-TU pedantic diagnostic ("ISO C requires a translation unit to contain at least one declaration") by tracking whether any decl was parsed while `file` is not the builtin `<command line>` preamble — deferred as low-value.

## Recurring codegen review checks (latent-bug-class guards)
- On any new front-end `r`-field flag, extend the `sv->r & ~(...)` mask in `arm64-gen.c` `load()` and the twin in `arm64-asm.c` `arm64_memory_needs_address_reg` — else arm64 `assert(0)` slips past x86/qemu CI.
- On any new §26 `ast_func_end` dispatcher work, keep the raw-x86 emission and its x86-only static helpers inside the `#if MCC_TARGET_I386||MCC_TARGET_X86_64` guard.
- Audit new pseudo-instruction cases in the arch-asm files for reads of `imm`/`op.e` without a preceding explicit `e.v` assignment (brace-init `Operand` union ⇒ host-compiler-dependent garbage).

## Search vocabulary / strategy long tail
- §27 loop-nest precision: symbolic bounds, fewer non-affine bail-outs, asttool dep suite (blocked by `MCC_INTERNAL`).
- §27 true 2-D loop tiling (strip both loops).
- §29 range/known-bits lattice remaining (PR-3+).
- §31 adaptive beam width; per-function scoping.
- §24 hot-slice budget allocation (needs §22 emit isolation).
- §25 `-g` hot-value cache.
- §33b post-graft window dataflow decision (splice-then-reanalyze vs two-pass).
- §33d seam peephole window; §33e window-level cache key; §25 scoring of the de-spill delta.
- §32c speculative arm insertion (revisit only with 3-stage self-host gate).
- §32a widening/fixpoint dataflow for cross-iteration value merging.
- §30 `switch`-arm detection form.
- §23 inline budgets as a §22 search value-axis (needs 3A); more param shapes; inline cross-TU static callees; heuristic non-static inlining.
- §22 arena-mutating pass-subset re-emit axis; promotion re-emit axis (needs 3A).
- §28 rewrite-rule IR; §28 instruction-level superoptimization.
- V-strategy variations (bfold/ident/narrow/cprop/cse/licm/dse/sccp/jt/bf/sethi/tco) — widen search vocabulary; see git history for per-row holds.
- Build the `H_e` epoch hash (invertible slot-keyed edit patch).
- `.rodata` data-emission project (prerequisite for §30).
- Design cross-TU LTO; separate `-O2`/`-O3` SSA drivers; time-budgeted engine; dependency-ordered `-O1`; broader template library.
- M7 formula-family unification; M7b graduate the disk search-memo into compiled-in strategies.
- FLOAT: emit-size scoring under the tick scheduler + JIT-runtime scoring; C11-thread pool with per-context state (needs §22 emit isolation).
- Revisit: `k` always-inline depth policy; size-gated outline; store factoring; template DSL past ~30 templates; per-function `-O1`; PP-as-executable-C JIT; human-friendly diagnostics vs terminal geometry.
