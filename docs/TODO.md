# TODO

Cut the AST recorder and the operation journal out and leave Replay_IR as the compiler's only intermediate representation. See **Cut to Replay_IR** at the end of this file for the staged plan and the decisions behind it; everything above it is the C2 work, which the plan no longer blocks on.

Two bars, both required. **Replay** (`rir_verify`) replays a captured body against the parser's own bytes. **C2** re-emits from the reconstructed arena and compares — the harder bar, and the one still open. Replay is at `faithful + empty == fn` on all twelve target keys at `-O0`/`-O1`/`-O2`/`-O3`, gated by the 48 `ast/rir-parity-*` cells. C2 is at an **18-body gap** over the eleven distinct keys on the `tests/exec` corpus, down from 114, with three keys at 100%. `tests/diff/full_language.c` is a separate and comparable-sized front; see the scoreboard.

## Scoreboard

Per key at HEAD, `c2ok/c2try (c2bytes+c2len+c2skip+c2invalid)`, `c2err=0` everywhere, produced by `C2_NO_EXTRA=1 tools/c2_sweep.sh <builddir> <key> -O1` against an mcc built `-DMCC_REPLAY_IR_C2=1` and run in place. The whole twelve-key sweep takes about 30 seconds; re-measure rather than copying a row forward.

Read `ok=` on every row before believing it. `rir_report` is an **atexit** handler, so a file that fails to compile still prints `[rir-total]` and still contributes bodies — counting `[rir-total]` lines is not an honesty check, and every scoreboard in this file before this one was measured on a population that silently counted partial output from 8 to 20 failing compiles per key. The sweep now counts only files that exited 0, exactly as `rir_parity.cmake` does, and prints that count.

`tests/exec` only, 277 files, `ok=` is the population that actually compiled:

| key | c2ok/c2try | gap | arenahasheq | ok |
| --- | --- | --- | --- | --- |
| x86_64 | 1130/1130 | 0 | 773/1166 | 270 |
| x86_64-osx | 1123/1123 | 0 | 772/1159 | 267 |
| x86_64-win32 | 1223/1223 | 0 | 835/1258 | 268 |
| arm64-osx | 1204/1205 | 1 | 812/1241 | 266 |
| arm64 | 1178/1180 | 2 | 795/1216 | 266 |
| arm64-win32 | 1280/1282 | 2 | 865/1318 | 265 |
| i386 | 1132/1134 | 2 | 760/1169 | 268 |
| arm | 1110/1112 | 2 | 750/1147 | 263 |
| i386-win32 | 1241/1243 | 2 | 838/1278 | 267 |
| arm-win32 / arm-wince | 1218/1221 each | 3 | 826/1256 | 262 |
| riscv64 | 1115/1119 | 4 (3 len + 1 bytes, plus 2 skip) | 760/1156 | 263 |

The five ELF cross keys carry **5 more compiled files each** than every board before this one, because the sweep used to put the glibc sysroot's include dir ahead of mcc's own: glibc's `complex.h` defines `I` as `(__extension__ 1.0iF)` and mcc does not lex the imaginary suffix, so the whole complex family plus `tgmath_dispatch` failed to compile on arm, arm64, i386 and riscv64 and on those keys only. `-I <repo>/runtime/include` now precedes `--sysroot`, exactly as the win32 and osx rows already did. **The gap total is still 18** — the added files are clean everywhere but riscv64 — but the composition moved, see below.

**`c2bytes` is 0 on eleven of twelve keys.** riscv64's 1 is `complex_annexg.c::main` (`op=gv idx=1396 firstdiff=1736`), a body that only entered the population with the include fix. Everywhere else every remaining divergence is a LENGTH difference, which is a different and more tractable question than a byte one.

The gap is **6 distinct bodies**, and about half of it is register allocation rather than model shape:

| body | keys | want/got |
| --- | --- | --- |
| `ternary_op.c::tst_yarpgen` | 6 | short by 8 to 12 |
| `overflow_narrow.c::main` | arm, i386, i386-win32, arm-win32 | short by 10 to 16 |
| `grep.c::pmatch` | arm64 -16, riscv64 +16 | |
| `utf8_string_literal.c::main` | arm-win32, arm64-win32 | short by 4 |
| `integer_promotion.c::main` | i386 +3, riscv64 -4 | |
| `struct_packed_indirect.c::main` | i386-win32 | long by 6 |
| plus riscv64's `complex_annexg.c::main` `c2bytes`, and its 2 `c2skip` | | |

riscv64's `complex_cmplx_special.c::main` `c2invalid` on leaf-regbank is **gone** — with mcc's own `complex.h` ahead of glibc's that body reconstructs and re-emits clean, and the key's fourth divergence is now the `complex_annexg` byte one instead. Do not go looking for the leaf-regbank invalid; it was an artifact of compiling the complex corpus against glibc headers.

`tests/diff/full_language.c` is a **second front, and now a measured one**. It needs `-I <repo root> -DCC_NAME=CC_gcc` to compile at all, and it enters the population on the 9 keys where the C2 probe's own error does not abort the compile — x86_64-osx, x86_64-win32, arm64, arm64-osx, arm64-win32, i386-win32, arm, arm-win32, arm-wince — so the default sweep prints `extra=` and `C2_NO_EXTRA=1` drops it for a like-for-like board. arm64 and arm are new to that list with the include fix, each contributing 12 non-ok bodies (4 `c2bytes`, 6 `c2len`, 2 `c2err`); the 20-body census below predates them and needs re-measuring.

Its census was **106 divergences over 20 distinct bodies**, 12 to 19 per key; it is **85** now. It is not the same work as the 18 above, in two ways that matter. First, **x86_64-osx and x86_64-win32 diverge here while reading 100% on `tests/exec`**, so this file reaches classes that corpus does not. Second, seven bodies diverge on **all seven keys** — `array_test` and `coherency_test` (genop), `struct_assign_test`, `s6_2_5_types` and `s6_3_conversions` (call), `optimize_out_test` (jmp), `s7_9_iso646_test` (store) — which makes them target-independent model-shape defects rather than the register-allocation residue that dominates what is left of `tests/exec`. Two op kinds appear that never appear in the corpus at all: `jmp` (`optimize_out_test`, `local_label_test`, `statement_expr_test`) and `cvt_csti` (`char_short_test`).

The rest: `longlong_test` (vstore, 5 keys), `char_short_test` (cvt_csti 5, genop 2), `statement_expr_test` (vstore 4, jmp 3), `s6_5_16_assign_test` (call 4, vstore 3), `check_opl_save_regs` (genop 4), `local_label_test` (jmp 6, call 1), `sigaddset1`/`sigdelset1`/`memcpy1`/`memcpy2` (genop 3 each), `fancy_copy` (gv 3), `fancy_copy2` (gv 2, load 1), `s7_1_ctype` (genop 2).

`array_test`, `s6_2_5_types` and `s6_3_conversions` are **closed** — the first by giving pointer arithmetic its own pointee-size pass, the other two by taking the parser's folded constant when a cast in the chain changed the value. That leaves 85 divergences, and `check_opl_save_regs` as the only RIR-model body still open on this front.

`check_opl_save_regs` — `void f(char *a, long long b, int c) { *a = b < 0 && !c; }`, want 52 got 32 — is a **`save_regs` placement** defect. The tree has one statement, `Store(Load(Ref a), Binary &&(...))`. Replay_IR has two: an extra `Store(temp, ADDR(Load(Ref a)))` from `JOP_STORE`'s lvalue arm, plus a `Load(temp)` in place of the target, so the trial spills and reloads `a` before the condition where the parser spills it after the `cmp` as part of the branch's own `save_regs`. The emitter does that spill itself; the arena should not carry it. Guarding `JOP_STORE` on `rir_lorn` does **nothing** — measured, no counter moves on any key — because the spill is not inside the landor region at the point the op is recorded. Find what region it *is* in before guarding again.

**Most of it is not a Replay_IR defect at all.****Most of it is not a Replay_IR defect at all.** Driving C2 from the tree's arena instead — `RIRC2TREE=1`, the control leg — reproduces the *same* divergence on 15 of x86_64-osx's 18: `char_short_test`, `coherency_test`, `fancy_copy`, `fancy_copy2`, `local_label_test`, `memcpy1`, `memcpy2`, `optimize_out_test`, `s6_3_conversions`, `s6_5_16_assign_test`, `s7_1_ctype`, `s7_9_iso646_test`, `sigaddset1`, `sigdelset1`, `statement_expr_test`. Only three are RIR-model defects: `check_opl_save_regs`, `s6_2_5_types`, `struct_assign_test`. And the tree leg is *worse* overall on this file — 33 divergences against Replay_IR's 18 — so Replay_IR already beats the recorder here by a wide margin.

Those 15 are defects in the shared replay path, `ast_replay_bb`/`ast_replay_value`, which both models drive. `optimize_out_test` is the clearest: the trial emits two `jmp`s the parser never wrote for the statically-dead arms of `if (0)`/`if (1)`, and the tree's arena emits them too. They still block the bar — "reproduce the parser's machine code byte-for-byte" does not care whose arena — but they are a different kind of work from modelling, they benefit both models, and they cannot be closed by changing the arena builder.

Caveat before trusting the split: `RIRC2TREE` falls back to the RIR arena when `ast_replay_ok(ast_cur)` is false, so a body can land in the shared set because the tree was never actually used. The three that DIFFER are certain; re-confirm any individual one in the shared set before attacking it as a replay defect.

On present evidence this front is **more tractable** than the `tests/exec` tail

**The gap does not move with `-O`.** Measured at HEAD, `-O2` and `-O3` are byte-identical to `-O1` on every counter — `c2ok`, every divergence class, and `arenahasheq` — on x86_64, arm64, i386, riscv64 and arm-win32. Forced `-O0` (`MCC_FORCE_REPLAY=1` plus the 38 derived `MCC_AST_*` gates) has now been measured on **all twelve keys**, not two: gaps are `x86_64 0, x86_64-osx 0, x86_64-win32 0, arm64 1, arm64-osx 1, arm64-win32 2, i386 2, i386-win32 2, arm 2, arm-win32 3, arm-wince 3, riscv64 3` — **16 over the eleven distinct keys against 18 at `-O1`**, on an `fn` three lower per ELF and Mach-O key, exactly the three `static inline` shims the census item below documents. The two that drop are arm64's second `c2len` and riscv64's third; x86_64's zero and i386's two hold at both levels. C2 replays the arena's own emission, and the optimizer passes are a separate question (C3), so a gap that only ever shrinks at `-O0` is what one would expect. It means the completion bar's "at every `-O`" collapses to one measurement: close the 18 at `-O1` and they are closed at every level. Re-check after any change that touches the capture path.

`arm-win32` and `arm-wince` share a define set and must read identically. Any sweep where those two rows differ has a harness bug, not a codegen one — cheapest available check that a run measured what it thinks.

## Close the C2 gap

**The one rule this session paid for four times: fix at the USE site, never at the capture site.** A change where a primitive is *consumed* — the wrap `JOP_GENOP` performs on its own operands, the `JOP_VSTORE` and `JOP_STORE` arms, the argcast loop — is bound to the right node by the op's own recorded snapshot, and every such change landed. A change where a cast or a lowering *happens* — a mark at `gen_cast`, a region bracketing `gen_negf` — perturbs the very stream `rir_verify` replays, and every such change failed: twice by dropping `faithful`, once by aborting the compiler. Read the banked negatives below before designing another attempt.

Instruments, in the order they pay: `RIRDUMP=1` for ops either side of the blamed index with byte windows; `-DRIR_DBG_OPTRACE=1` plus `RIRDBG=<funcname>` for the `[ent]` entry stream, `[stmt]` naming which entry pushed each BasicBlock statement, `[vst]` printing the call-tail spill test's inputs, `[gop]` printing the operand wrap's admission inputs, and diffable `PARSE`/`C2` op streams; `MCC_REPLAY_IR=6` for `[rir-dump]`/`[rir-diff]` node pairs. A structural `[rir-dump]` diff against the tree found most of what closed this session, each time from one body.

### What closed, and the shape that worked

- The **code-free cast on a 32-bit target** was the biggest class and is now down to `overflow_narrow.c::main`. `(int)` over a 64-bit value, or over an `unsigned int` `size_t`, changes neither size nor register, journals no `JOP_CVT_*`, and leaves the arena node wrong. All of it closed by widening the wrap `JOP_GENOP` already performs on its own operands: `rir_eff_size` and `rir_eff_unsigned` walk down through untyped Binary/Unary to the first typed node, and a genuinely **wider** grandchild, or an **unsigned** one where the operand's recorded type is signed and the same size, overrides the admission tests. Narrowing direction only. That closed `signbit_inline.c::main`, `popcount_inline.c::ref_clrsb64`, `div_mod_shift.c::main` and `cmp_invert.c::main`.
- The same class **at a call argument** closed `bitfields.c::main`: the argcast wrap skipped nodes that are already an `AST_Convert`, and now lets one through when that Convert is genuinely wider than the argument's recorded type.
- riscv64's **`gen_negf` spill** closed 3 of 4 bodies. `save_reg(gv(bt))` spills the double before the in-memory sign flip; `JOP_STORE` declined it because its guard wants an lvalue where this is a register-resident rvalue. The rvalue arm stores the VALUE and hands back **the slot as an lvalue** — handing back a `Load` over it dereferences twice and every body dies with "pointer expected". Restricted to float spills.

### Banked negatives — do not re-pay for these

- **Marking code-free `gen_cast` calls**, three variants: at every such call wrapping the shadow-stack top → `c2ok 531/1146`, `c2invalid=517`, two core dumps; narrowed to a const-only `AST_Binary` outside every region → `1110/1145`, `c2err=3`, `faithful` down one; bound to the operand the marker's own vstack snapshot names → `faithful` holds but `c2err` 0 to 1-3 on every key, x86_64 1130 to 1126, and neither target body moves. Stamping the bound node's type instead of wrapping is worse again (x86_64 1115, `c2err=5`).
- **Bracketing `gen_negf` as `RIR_R_FNEG`** on the `RIR_R_INC` pattern **aborts the compiler**: `ok=` falls by 2 to 4 on riscv64, arm64 and arm, `faithful` 1111 to 1076 and 1171 to 1134. Region markers are part of the captured stream.
- **Relaxing the `!lv` guard in `JOP_STORE` generally**: -30 `c2ok` on every key, `c2err` 2 to 7. Integer register spills are everywhere and each becomes a BasicBlock statement. The float restriction is what makes it safe.
- **Dropping the argcast wrap's `AST_Convert` skip** without the wider test **segfaults** every PE key — `ok=` falls by 1 to 2 on five keys. The crash was a null `type.ref` reaching `type_size` for an enum, latent in two existing helpers; they now take the operand's real ref.
- **Widening `rir_child_width_differs` itself** to the effective width: same `c2ok` as the landed shape, minus 14 to 20 `arenahasheq` on **every** key. Keep it on immediate children and OR the narrowing test in beside it.
- **Retyping the cast chain at its conversion op** for `overflow_narrow.c::main`, two variants, both **no-op** (native i386-win32, sweep byte-identical, body unchanged): typing the `JOP_CVT_SXTW` operand to `VT_INT` when it reconstructs wide (i386 does not emit SXTW here at all); and typing the `JOP_CVT_CSTI` Convert from its own recorded target `o->a0` instead of the snapshot, gated on a wide operand. Neither fires — the `(int)` narrow in `(long long)(int)(v>>3)` emits no op, so there is no op to attach to, and the `[gop]` tap reads `opdiff=0 widthdiff=0` at the widen. This confirms the conversion-op route is dead for the whole lost-intermediate class (`overflow_narrow`, `grep.c::pmatch`, `integer_promotion`): the intermediate has to be recorded as **new** data — a C2-only source-width field on the widening op, read by reconstruction and ignored by `rir_verify` so it cannot perturb replay — not inferred from anything already captured.

### Still open

- `overflow_narrow.c::main`, 4 keys, is the cast CHAIN `(long long)(int)(v >> 3)`. The trial omits `mov ecx,eax; sar ecx,31` because the `(int)` was free and the node stayed 64 bits, making the outer `(long long)` a no-op. **Not fixable at the conversion op**: `cvt_csti`'s operand snapshot reads `VT_QLONG`, the RESULT of the chain, not the intermediate. Needs the intermediate recorded and readable at a use site.
- `integer_promotion.c::main` (i386 +3, riscv64 -4) is **not** the plain width class — it is the `VT_INT128`/`VT_QLONG` item further down this file. The trial emits an extra `sbb ecx, 0` after `sub eax, 0x64`, carrying the borrow of a 64-bit subtract where the parser had already narrowed to 32 and left `ecx` dead. Located precisely: extend the `[gop]` tap with `ind=o->ind_pre - ast_body_ind_sv` and the divergence at byte 1266 is entry 978, `op=156 st=13 curt=0 hastype=0 opdiff=1 widthdiff=1 eff=8 wider=1 uns=1`. `st=13` is `VT_QLONG`, and both admission gates already pass, so the operand wrap DOES fire and wraps in a Convert to `VT_QLONG` — the width rules are not what is declining it. The parser's operand is QLONG-typed at that point and still emits a 32-bit subtract, so what the model is missing is the representation, not the width. Do not widen the operand wrap further for this body.
- `struct_packed_indirect.c::main`, i386-win32, long by 6: two extra `mov [ebp-X], eax` after a call that do **not** come from arena Store nodes — `[vst]` shows every call-tail spill in that body already suppressed. Register pressure, not model shape; same class as `ternary_op.c::tst_yarpgen`.
- `utf8_string_literal.c::main` (arm-win32, arm64-win32, short by 4) is **register choice**, not model shape. The parser emits `ldrh r0,[r0]` then `mov r1,r0`; the trial emits `ldrh r1,[r0]` and skips the move. Two `gv` ops in the parse (`[416]`, `[417]`) collapse to one in the trial because its first `gv` picked the argument register directly. Same family as `ternary_op.c::tst_yarpgen` and `struct_packed_indirect.c::main`. Its `[rir-diff]` also shows an extra BasicBlock statement (`n0` `nc=53` against the tree's 52) and the missing-Convert-over-Literal class on its `sizeof` comparison, so there are three things in this body; the length gap is the register one.
- `grep.c::pmatch` (arm64 -16, riscv64 +16) is the cast class in a place the operand wrap does **not** reach. The trial omits `sxtb x2, w2` after an `ldrb` — a char-to-int promotion that is real code on arm64. The `[gop]` tap fires only three times in that body and all three are at entries past 1366, while the blame is op index 299, so the JOP_GENOP wrap never runs on the divergent operand at all (it needs `vs_n - ast_base_depth >= 2 && rir_shn >= 2`). Find which pass consumes that operand before widening anything. **The consuming op is a JOP_GENOP whose guard fails at op 299**: a `[gopf]` companion tap (print at the genop entry when the 2-operand guard is NOT met) shows the top operand is an untyped `Load` (`ct=0`) whose recorded snapshot already reads `st=VT_INT` — the char-ness is gone from the snapshot, exactly like `overflow_narrow`. Even relaxing the guard would not help: the wrap at the `st != 0` loop requires `ast_kind == AST_Binary`, so a `Load` operand is skipped anyway, and the only recorded type is the promoted `int`, not `char`. This is the same lost-intermediate root as `overflow_narrow` and `integer_promotion`, not a separate "widen the guard" fix.
- Close `ternary_op.c::tst_yarpgen`, 6 keys and the largest single body left, want 460 got 452 on arm64-osx and short on every key. Blamed at `op=store idx=206 win=[364,368)`, a JOP_STORE register spill the parser makes mid-expression and the trial does not. This is the scheduling-and-allocation class, not a model-shape one: the body is a single yarpgen expression with enough live values to spill, and the trial reaching that point with fewer live registers is the symptom. Expect it to be the last one closed.
- riscv64's last `c2invalid` is `complex_cmplx_special.c::main` on `leaf-regbank`, a different rule from the `Store-target` one that four bodies used to fail — that one was closed by modelling the byte-offset store target as the `AST_OP_MEMBER` Unary it is (it already does `gaddrof; vpushi; gen_op('+'); vtop->r |= VT_LVAL`), reparenting via `ast_dup_sub` because reparenting in place reports "child parent link mismatch" instead.
- Resolve riscv64's 2 `c2skip` bodies, `bitfields.c::main` and `struct_packed_indirect.c::dump`, both `rir_arena_mismatch`. The tree's own answer for `gen_negf` is `ast_hook_bail()`, and a riscv64 probe with that guard bypassed aborts the compiler on `sv->r & VT_LVAL` at `riscv64-gen.c:438` — Replay_IR models it anyway and now closes 3 of the 4 bodies, so the refusal is the tree's, not a target limit.
- The PE `tgmath.h` shadowing is **not real** — settled on the macOS host, do not reorder any header lookup. `mcc-x86_64-win32` built `-DMCC_REPLAY_IR_C2=1` on native arm64-osx compiles `tgmath_dispatch.c` with rc=0 under all three flag sets (`-B runtime/win32 -B runtime -I runtime/include`, `-B runtime/win32 -B runtime` with no `-I`, `-B runtime/win32 -I runtime/include`), and the body is `fn=1 c2ok=1 c2bytes=0 c2len=0` — byte-perfect. Both hosts that can see it agree it compiles, so reordering would only risk moving the census for no defect.

## Raise arena fidelity

- Raise `arenahasheq`, 812 of 1241 on arm64-osx. Measure as `arenahasheq` and as diffs-per-body falling, not as first-divergence classes disappearing — fixing divergence #1 only exposes #2. Classify by **structural** dump diff, not by node index: `ast_intention_hash` is a structural walk that hashes neither `type_ref` nor `ival` on a `Ref`, so the large Convert-versus-Ref index-transposition class and every `ref`-only diff are not fidelity defects and cost nothing to ignore. Live classes: 38 field-only, 32 extra-Convert-over-Binary, 20 missing-Convert-over-Literal, 14 tree-extra-Store. The last two are understood and not cheap — the tree builds cast chains where one push record recovers only one link, and `tree-extra-Store` is dead code under `nocode_wanted` that the tree records and the op filter drops.
- The extra-Convert-over-Binary class is now **measured, not guessed**, and the measurement says do not touch it. `rir_cast_seq` counts `gen_cast` entries and `RIR_M_ARGCAST` carries, per argument, whether that argument's cast fired at all — the tree's own condition, since `ast_hook_convert` records on every `gen_cast` call rather than on a type change. Gating the whole argcast wrap on that flag reads **+25 arenahasheq and -16 c2ok on every key**: the wraps the tree does not make are load-bearing for emission. The flag is therefore consulted only for the untyped-Load case it was added for. Any future attempt to close the class has to explain that trade first.
- Land explicit operand binding as a side-table keyed by op index, not a node field and not positional binding.
- Widen C3 pass equivalence past the field-identical population — the same work as raising arena fidelity, since the paired population *is* the field-identical population. Add the optimized-versus-optimized byte tap by replicating C2's whole save/restore prologue just before the `orig`/`orig_rel` frees in `ast_func_end`, where `ind` is the end of the optimized body: a different point with different live state. Report the coverage difference separately rather than as a mismatch, because the tree runs passes only where `ast_replay_ok` holds while Replay_IR accepts every body. `folds`/`samefolds`/`pairfired` stay printed and unpinned — two arenas can reach the same hash by different fold counts.

## Finish the capture path

- Phase F: un-embed `RirOp` from `JrnOp`. This is a design change, not a lift — Replay_IR must own an op stream with its own vstack buffer (the shape `rir_mvs`/`mvs_off`/`mvs_n` already has for marks) instead of reading `jrn_ops[]` and indexing `jrn_vs[]`. `jrn_snap_vstack` falls out of the same change: it has no `mccgen.c` call site to hang on, so it cannot be lifted independently.
- Discharge F against the same obligation every prior slice met: byte-identical `MCC_REPLAY_IR=5` sweep log and identical object sha256 at `-O0`, `-O1`, `-O2`, `-O3` and forced `-O0`. That compares capture against parser, tree and journal at once and is strictly stronger than cross-checking two capture paths against each other. Replace site by site; do not run both paths side by side.

## Keep the measurement honest

- Bank the corpus census against header resolution, never against a remembered number. `MCC_CONFIG_AUTO_MCCDIR` resolves mcc's own freestanding headers from **argv[0]'s directory**, so a compiler built into a scratch dir with no sibling `include/` silently loses `stdarg.h`/`stdbool.h`/`stdatomic.h`/`threads.h`; a build omitting `-DMCC_CONFIG_MCCDIR` finds no system headers at all and reads 476; a glibc sysroot resolves `<threads.h>` to glibc's rather than mcc's shim and costs 25 functions. Every one of these leaves `rc`, the file count and the failing-file list unchanged — the census is the only thing that moves, which is what makes it a trap.
- Keep the C2 harness mirroring the tree's replay prologue exactly across `vstack`/`vtop`, `loc`, `anon_sym`, `ast_pinned_regs`, `ast_rp_bsym`, `ast_rp_csym`, `ast_rp_switch`, `ast_temp_frontier`, `ast_rp_nlabel`, `ast_fconst_i`, `ast_locrec_i`, `sym_free_first`. Leftover allocator state reads as a codegen difference; one omission, a dirty vstack, costs 194 bodies.
- Keep the `-O0` cells honest. `ast_replay_env` needs `optimize >= 1`, so `-O0` without `FORCE` journals nothing; the cell exits 1 on "0 bodies journalled" rather than 77, and `FORCE` without `SRCDIR` is fatal. The 38 `o4 || optimize >= 1` gate names are derived by regex over `${SRCDIR}/*.c` per run so a cross cell cannot drift from the native one. Forced `-O0` legitimately reads 3 bodies fewer than `-O1` on every ELF and Mach-O key and 0 fewer on the PE keys — `grep.c::tolower`, `c11_threads.c::thrd_equal`, `arm64.c::putchar`, all `static inline` shims whose out-of-line copy `-O1` emits and `-O0` does not. That is an emit difference, not a census loss.
- **Done** (`fix(macho)`): the cross `x86_64-osx` compiler now gets the macOS SDK include path when built on an Apple host — `mcc_add_macos_sdkincludepath` was `MCC_TARGET_IS_HOST`-only, now `MCC_TARGET_IS_HOST || __APPLE__`. On native arm64-osx the x86_64-osx cross went from the false `fn=483 c2ok=452/452` to a real full-corpus `fn=1219 c2ok=1183/1183` (bytes=0 len=0). A Linux-hosted cross-osx build is unchanged (`host_macos_sdk_root()` is NULL off Apple).
- **Done** (`fix(tools)`): `c2_sweep.sh` no longer silently drops `full_language.c`, no longer counts files that failed to compile, and says which population it measured. Three separate defects, each of which produced a plausible wrong row: the file's computed include `#include INC(42test)` → `<tests/diff/42test.h>` resolves only against the repo root, so it needs `-I <repo root>`; it also needs `-DCC_NAME=CC_gcc`, undefined without which it stops at `legacy_aggregates.h:1009` on **every** key including the host build; and `rir_report` is an atexit handler, so before the `ok=` counter a failing compile still printed `[rir-total]` and still contributed its partial body count. Adding only the `-I` made the file fail LATER rather than earlier and added 59 partial bodies per key to the census — strictly worse than dropping it. See the scoreboard for what it is worth once it does compile, and for why it is not a row on the board.
- **Done** (`fix(tools)`): `c2_sweep.sh` put the glibc sysroot's include dir **ahead of mcc's own** on the five ELF cross keys, so glibc's `complex.h` won and its `#define I (__extension__ 1.0iF)` hit an imaginary-literal suffix mcc does not lex — `invalid number`. Five files (`c11_complex_decls`, `c11_complex_convert`, `c11_complex_const_fold`, `complex_annexg`, `tgmath_dispatch`) failed to compile on arm, arm64, i386 and riscv64 and on no other key, and had been off the board since it existed. `-I $S/runtime/include` now precedes `--sysroot`, matching what the win32 and osx rows already did. `--sysroot` alone is not enough: mcc does not derive `<sysroot>/usr/include` from it, so the explicit sysroot `-I` has to stay, just second. The fix is worth +5 files and +10 bodies per ELF cross key, leaves the 18-body gap unchanged, and rewrote riscv64's fourth divergence from a `c2invalid` to a `c2bytes`.
- **Accept the imaginary-literal suffix in either order, and fold it to a constant** — the defect the include fix above is only routing around. It is two things, both reproducible on the host build with a one-line file. First, the lexer at `src/mccpp.c:2814` takes `i`/`I`/`j`/`J` only **after** the float/integer suffix, so `1.0fi` lexes and `1.0iF`, `1.0if` and `1.0jf` all die with `invalid number`; gcc accepts both orders and glibc's `complex.h` writes exactly the rejected one, `#define _Complex_I (__extension__ 1.0iF)`. Second, even the order that lexes does not fold: `double _Complex z = 1.0i;` at file scope reports `initializer element is not constant`, because `push_tokc` in `src/mccgen.c:9949` hands `tok_imaginary` to `gen_imaginary_complex(t)`, which builds a runtime value rather than a `VT_CONST` complex. Fixing only the suffix order gets the corpus past the preprocessor and straight into the second error, so land both. Closing this lets the ELF cross keys compile the complex family against the sysroot's real `complex.h` instead of mcc's shim, which is the honest configuration; keep the `-I runtime/include` ordering either way, since it is what the win32 and osx rows do.
- **Done** (`test(exec)`): the exec harness's own source moved `tests/exec/runner.c` → `tests/runner.c`, out of reach of every `find tests/exec -name '*.c'` in the tree. It was never a test, and it was counted as one by `c2_sweep.sh`, `verify_ratchet.cmake` and `journal_sweep.cmake` — 11 bodies of harness code in the C2 census and in the recorder's fidelity gap set on every key. `target_include_directories(exec_runner)` now points at `tests` and the two quoted includes are `support/hostcompat.h` and `exec/goldens.h`; the `! -name runner.c` filters in `treecheck.sh`, `no-compiler-abort.sh` and `selfhost-output-parity.py` are now dead but harmless. Two consequences to know: the stale `runner.c slurp unfaithful` row had to come out of `verify-baseline/x86_64-darwin.txt` or that key's ratchet fails on a "gap now FAITHFUL" drift, and **every `journal-baseline/*.depth.txt` ceiling is now NOT COMPARABLE** because its banked `rows=` counted runner.c's bodies. x86_64-linux is rebanked (`fix=29969 deep=10719 rows=1165 ops=791159`, was `31003/11021/1176/795697`); the other twelve depth files still need a rebank **in their own environment** — `cmake -DMCC=... -DCORPUS=tests/exec -DEXTRA=tests/diff/full_language.c -DBASELINE=... -DJBASELINE=... -DTMPDIR=... -DOPT=-O1 -DJOURNAL=1 -DJREGEN=1 -DJDEPTHREGEN=1 -P tests/ast/verify_ratchet.cmake` — and until then those keys are half-ratcheted: breadth still gates, depth reports SKIP.
- **The sweep's corpus is still a file glob, not the test list, and it drops per-test flags.** `find tests/exec -name '*.c'` now misses `runner.c` but still picks up the three secondary translation units `alias2.c`, `inline2.c`, `cmp_invert.c`, and it ignores each test's `flags` column from `tests/exec/goldens.h` and the `-I tests -I tests/support` the real runner passes. Two tests fail on **every** key purely for that: `trigraphs.c` needs `-trigraphs`, `builtins_extra.c` needs `-I tests/support` for `vlog.h`. A goldens-driven driver (274 tests, per-test flags, link-only flags stripped for `-c`) reads x86_64 `c2ok=1091/1091` at `-O1` and `1088/1088` at forced `-O0` — still a clean zero on a population that includes those two. Fold the goldens table into the sweep rather than the glob.
- **riscv64's sweep core dump is `full_language.c`, not the harness** — an earlier note here blamed `runner.c` and that was wrong. `mcc-riscv64` aborts on `src/arch/riscv64/riscv64-gen.c:431`, `store: Assertion '!is_float(bt) || is_freg(r) || bt == VT_LDOUBLE' failed`, after the C2 probe has printed its byte diff, which is why the file never enters the riscv64 population. Same neighbourhood as the `riscv64-gen.c:438` `sv->r & VT_LVAL` abort the `c2skip` item below records, and probably the same missing float-register path. Separately, `tests/runner.c` itself does **not** crash any target but does fail to compile on riscv64 alone — `runner.c:690: error: assignment of read-only location` on a `snprintf` call that every other key accepts — which is a real front-end defect now sitting outside the corpus, so nothing measures it.
- **Every remaining compile failure is target-inapplicable by construction**, checked file-by-file on all twelve keys: `fastcall` (i386 `pushl`), `arm64_encoding`/`arm64_extasm` (arm64 mnemonics), `asm_constraints_x86`/`asm_operand_modifiers`/`asm_goto`/`asm_lvalue_cast`/`al_ax_extend` (x86 asm), `winarm64_interlocked` (`windows.h`), `int128` (no `__int128` on 32-bit or PE), `bound_signal`/`versym` (POSIX-only on PE), `array_assignment` (invalid C, rejected on purpose). Each matches its own `req` field in `goldens.h` except two: `asm_outside_function` emits `ret` from an `__asm__` at file scope and so cannot assemble on the three arm keys, and `full_language.c` is aborted by the C2 probe on x86_64/i386/riscv64. Give `asm_outside_function` a `req` so the gap between "failed" and "not applicable" stops needing a person to re-derive it.
- **x86_64-osx measured on a Linux host is three files short and the reason is physical.** With no macOS SDK the Darwin cross falls through to the host's `/usr/include`, where `bits/stdint-intn.h` typedefs `int64_t` as `long int` against the target's `long long` — `incompatible redefinition`, which kills `atomic_counter`, `c11_threads` and `flexarray_runtime`. arm64-osx escapes only because its `int64_t` happens to agree with the host's. Both osx rows on this host are therefore compiled against glibc headers; the numbers are real C2 readings but not Darwin ones, and only an Apple host (or a vendored SDK) closes it.
- Extend the native-versus-cross cross-check to riscv64 and i386. arm64 has it: measured natively in a `linux/arm64` container, `c2ok=1175/1180` identical to the cross reading, so C2 is cross/native-invariant there. Neither of the other two can run natively on this host.
- **Ten of twelve keys are now confirmed host-invariant off macOS** — a second host stack reproduces the board. On a Windows x86_64 host, mcc built with the vendored winlibs gcc and `-DMCC_REPLAY_IR_C2=1` under `-DMCC_ENABLE_CROSS=ON -DMCC_CROSS_TARGETS="i386-win32;x86_64-win32;arm64-win32;arm-win32;arm-wince"` (the win32 keys need no sysroot; they use `runtime/win32`) reproduced all five PE-key rows byte-exact via `C2_NO_EXTRA=1 tools/c2_sweep.sh`, and the `arm-win32` ≡ `arm-wince` identity gate held natively. A WSL Ubuntu clone with the five ELF cross compilers and the four Gentoo stage3 glibc sysroots reproduced x86_64/i386/arm/arm64/riscv64 exactly on the same host. This is host-invariance (same source, different host), which is weaker than and distinct from the on-target-native check the line above wants for i386/riscv64 — that still needs i386/riscv64 silicon — but it does make the five PE keys checkable on real Windows without a mac.
- Measure the `MCC_AST_*` gate ledger at more than one `-O`. The six `STOREVAL_*` gates are worth +5 bodies at `-O0` and fire nowhere at `-O1`, so a single-gate ledger at one level — what `tools/gate-ledger.sh` produces — is a lower bound.
- Keep the fifteen files the repo-wide comment strip deliberately skipped, since in each the comment is the test payload and not prose. The ten `tests/diagnostics/dg-error/*.c` carry the expected diagnostic in a `/* dg-error: ... */` marker that `run_dgerror.cmake` greps out and hard-fails on when it is absent; `tests/exec/preprocessor/comment.c` is the comment-lexing permutation corpus whose golden turns on a backslash-spliced line comment swallowing the statement after it; `tests/cst/kinds/comment.c` and `tests/cst/hashinv/spaced.c` exercise CST comment promotion and the H_s/H_t comment-invariance split; `tests/preprocess/asm/gas_comments.S` is the GAS comment corpus; and `tests/exec/programs/grep.c` is greped as its own input by the `{SELF}` golden, which expects the trailing vim modeline in the output. The strip also preserved line numbering in `tests/**` C sources because `tests/exec/goldens.h` pins diagnostics as `file.c:line:`, so a reflow that renumbers those files breaks goldens without touching a single test assertion.
- Keep `src/mccrir.c` free of any `MCC_TRACE(` call; `tools/tracegate.c` checks a file only if it contains one, so the first call pulls that file's ~250 unannotated branches into the checked set at once. Run `./cmake-debug/tracegate src` and `./cmake-debug/schemagate src` before every push.

## Cut to Replay_IR

### The finding that sets the order

**The recorder's tree is the production optimizer at `-O1` and above, not a side-car.** `ast_replay_env = optimize >= 1 || embed_jit || MCC_FORCE_REPLAY` (`src/mccast.c:2035`) arms the whole recorder through `ast_try_active` (`src/mccast.c:16016`); `ast_replay_body(ast_cur)` then **overwrites** the parser's bytes at `src/mccast.c:18186`, and the parser's copy is restored only on `!faithful` (`src/mccast.c:18804`). The passes mutate `ast_cur` in place (`ast_run_strat_cycle`, `src/mccast.c:18306`) and `AST_PF_EMIT` re-emits it (`src/mccast.c:18420`). `ast_cur` is also the only source of JIT blobs and retain pools — six call sites, `src/mccast.c:18045, 18486, 18526, 18572, 18910, 18912-18913`.

So "delete `mccast.c`" would delete the optimizer and the JIT. The correct statement of the work is narrower and harder: **the recorder is a redundant second producer of the same arena, and Replay_IR takes its place.** The arena, the replay engine, the ~10,800 lines of passes and the slice/JIT infrastructure all survive; what dies is the 1,716-line hook recorder (`src/mccast.c:2311-4026`, 143 definitions, 126 call sites in `src/mccgen.c`, 78 declarations in `src/mccast.h`), the journal's verify half (~550 of `src/mccast.c:14495-15971`), and the measurement scaffolding that exists only to compare the two producers.

**`-O0` is the A/B precisely because the recorder is absent there.** At `-O0` no arena is allocated and the parser's bytes ship untouched, so every phase below must leave `-O0` objects byte-identical — a cut that moves an `-O0` byte has touched something it had no business touching. `MCC_FORCE_REPLAY` at `-O0` arms Replay_IR over every body independently of the recorder, which is what makes it a coverage proof rather than a comparison.

### Decisions taken

- **Cutover: per-body fallback.** Production uses `rir_arena` when that body's C2 check passes and keeps the parser's bytes when it does not. This is not new machinery — the `-O1` path already is *emit from the arena → compare against the parser → restore on mismatch*, and per-body fallback is that same shape with `rir_arena` substituted for `ast_cur`. It converts the remaining 18-body C2 gap from a precondition for deleting 4,500 lines into an optimization gap on 18 bodies per key. Close them because they are worth closing, not because the deletion waits on them.
- **Scope: refactor the optimizer and the JIT onto Replay_IR**, rather than leaving them mechanically re-producered. The pass drivers, the retain pools, the `-O3` forward-inline re-emit and the JIT blob producers each get rewired to the Replay_IR arena as a first-class source.
- **Renames: full split and `ast_*` → `ir_*`.** `src/mccast.c` splits into arena+replay, passes, and slice/search units, and the prefix goes with it. The `ir_`/`IR_` namespace is verified empty across `src/`, `tools/` and `include/`. Size: ~10,800 `ast_` and ~2,750 `AST_` tokens over `mccast.c`, `mccrir.c`, `mccgen.c`, `mccjit_embed.c`, `mccjit_intent.c`, `mccast.h` and `tools/asttool.c`.

**The split must extend the include chain, not create link units.** `mccast.c` and `mccrir.c` are `#include`d back to back into one translation unit (`src/libmcc.c:11-12`; `src/mccgen.c:15229-15235`), and `mccrir.c` reads `mccast.c`'s file statics — `jrn_ops`, `jrn_vs`, `ast_cur`, `ast_base_depth`, `ast_body_ind_sv`. The ordering is load-bearing for the journal's macro trick as well: `src/mccgen.c:143-234` `#define`s 61 codegen primitives to `jrn_` twins and `src/mccgen.c:15155-15234` `#undef`s all 61 *before* the includes, which is the only reason `JRN_W0(load, …)` can define `jrn_load` whose body calls `(load)(…)`.

### The gate at each phase

1. `-O0` objects byte-identical across the corpus, before and after.
2. `MCC_RIR_FORCE=1` at `-O0`: `faithful + empty == fn` on all twelve keys.
3. `C2_NO_EXTRA=1 tools/c2_sweep.sh` counters unmoved — for a re-homing or a rename, *unmoved by one* is the whole proof. Only P4 is allowed to move them.

### P0 — harness first, nothing deleted

- Replace `rir_parity.cmake:24-31`'s derived forced-`-O0` gate list with an explicit `MCC_RIR_FORCE=1`. It currently derives the 38 gate names by regexing `ast_env_gate("MCC_AST_*", o4 || s1->optimize >= 1)` out of `${SRCDIR}/*.c`, and hard-fails with "FORCE derived 0 gates" the moment those gates start dying — it breaks in P1, before anything interesting has happened.
- Add `tests/ast/rir_c2.cmake` as a **banked-gap ratchet**, not a 100% bar: per-key `c2ok/c2try` with the current 18 banked, failing on regression. The 100% bar belongs to the bodies, not to the deletion.
- Add the `-O0` object-identity cell. `tests/ast/journal_inert.cmake` already supports `-DRIR=1` (`:21-26`) and is the right driver.

### P1 — free cuts

Nothing here is in the `-O1` path and nothing here is read by Replay_IR.

- The journal's verify/report half: `src/mccast.c:14576-14596` (`JFIX_*`), `14620-14623`, `14687-14714`, `15354-15532`, `15540-15924`, and the `#else` inert stubs at `15928-15971`. **Keep `jrn_issue` (`15186-15352`) verbatim minus one line** — `jrn_regdiff_n++` at `15343` is its only write into the verify half — and keep `jrn_reset` minus its two verdict lines, `jrn_gap`, and `jrn_bad` (read by `rir_verify`, `src/mccrir.c:4257`). `jrn_run` (`15433-15483`) deletes entirely; `rir_run` already replaces it.
- Call sites: `jrn_configure` at `src/mccast.c:2026`, `jrn_started` at `16059/16065`, the `jrn_verify` block at `18074-18077`, and the three `jrn_oracle_*` calls at `18255`, `18854`, `18903`. The gate at `16060` becomes `if (rir_env && ast_try_active)`; `jrn_active = 1/0` at `16062-16064`/`18078-18080` **stays** — capture is what drives Replay_IR.
- `ast_hook_stmt` (`src/mccast.c:2311`) is an empty switch. `ast_hook_data` (`:4774`) is diagnostics only — the real `.data`→`.bss` and string-merge transforms are the parser's, at `src/mccgen.c:14383/14401/14418`.
- Tests: `ast-journal-parity-{O1,O2,O3}`, the 30 `tests/ast/journal-baseline/` files, `verify_ratchet.cmake`'s JOURNAL mode, `tools/jrn_sweep.sh`, and the 130 `journal-sweep-*`/`journal-native-*`/`journal-regen-*` targets at `CMakeLists.txt:4039-4127`. `tools/c2_sweep.sh` is already the sweep matrix's replacement.
- **Retire rather than port the journal's `raw=`/`rawb=` breadth bucket.** It is zero on 15 of 17 banked keys, and the two exceptions carry one stale row (`arch/arm64.c myprintf raw=14 rawb=688`) that only the baseline still reports. This also discharges the outstanding obligation to rebank twelve `.depth.txt` ceilings after the `runner.c` move — the files go away instead.

### P2 — decouple Replay_IR from recorder state

Replay_IR reads five recorder-owned globals; all must move before the recorder can die.

- **`ast_base_depth`** is the sharp one: assigned at `src/mccast.c:16047` *inside* `if (ast_try_active)`, and read at 27 sites in `src/mccrir.c`. Deleting the block silently zeroes it and every `vs_n - ast_base_depth` computation in `rir_op_effect`/`rir_mark_apply` goes wrong quietly. Own it in `rir_hook_body_begin` (`src/mccrir.c:493`, already called at the right point, `src/mccgen.c:14588`) as `(int)(vtop - vstack + 1)` — provably the same sample point.
- Its only other mutators are `ast_hook_cleanup_call_begin/end` (`src/mccast.c:3387-3400`), which rebase by `ast_cleanup_bias` and have no Replay_IR sibling. Add one. The bias reduces to save/restore of the current vstack depth, because `ast_hook_cleanup_call_begin` already early-returns unless `ast_vn == 0` (`src/mccast.c:3385`) — no shadow stack needs replicating.
- `ast_body_ind_sv` / `ast_reloc0_sv` (`src/mccast.c:16018-16020`) and a `rir_try_active` predicate move to `rir_hook_body_begin` as well. The predicate is `rir_env && !debug_modes && !cur_func_inline_extern` — **dropping `!ast_ret_bad`**, which is a limit of the AST *value* model (`long double`, `__int128`, `_Complex` returns) that Replay_IR does not share and is currently paying for. Expect this to widen the population and surface new failures; that is coverage arriving, and it must be measured at `-O0` before anything else in this phase lands.
- Re-home the reverse edges: `rir_dbg_on` and its two taps out of `src/mccast.c:14805/14944`; `ast_alloc_loc`/`ast_alloc_temp_loc`/`ast_fconst_*` into neutral frame and constant services with Replay_IR and the recorder as observers rather than a priority chain buried in `ast_fconst_reuse` (`src/mccast.c:2279`); `ast_sym_defer` → `sym_defer` (Replay_IR needs it too — `rir_snap_types` exists because `Sym*` are unstable).
- Model to copy: `rir_hook_slot_record/replay` (`src/mccgen.c:7679/7684`) is already a clean parser↔Replay_IR pair that never touches `mccast.c`.

*Gate: C2 counters unmoved on every key, except for the measured `ast_ret_bad` widening.*

### P3 — rebrand the capture substrate

`jrn_*` → `ir_cap_*`, `JrnOp` → `IrCapOp`, `JOP_*` → `IR_OP_*`, `JRN_OP_LIST` → `IR_OP_LIST`, `MCC_JOURNAL_HOOKS` → `MCC_IR_CAPTURE`; the record substrate moves out of `mccast.c` into its own unit next to `mccrir.c`. 612 `jrn_` sites and 187 `JRN_` sites across `mccast.c` (418/132), `mccgen.c` (130/46), `mccrir.c` (59/8), `mccasm.c` (3/0) and `mcc.h` (2/1).

**Do P1 first.** A blind rename collides on 20 names that already exist as `rir_*` — but 17 of them (`blame`, `configure`, `emit_line`, `env`, `fail_kind`, `fail_op`, `out`, `report`, `run`, `started`, `verify`, `tot_*`) die in P1, leaving only `jrn_n`, `jrn_ops`, `jrn_cap`, `jrn_vscap`, `jrn_active`, `jrn_reset` needing the distinct `ir_cap_` prefix.

Four places test each `MCC_JRN_HAVE_*` flag in lockstep — the `#define` table (`src/mccgen.c:154-233`), the `#undef` table (`:15167-15206`), the wrapper instantiations (`src/mccast.c:14932-15311`) and the `jrn_issue` dispatch (`:15219-15311`) — plus `mccrir.c` and the AST replayer. Move all six together or the build breaks per target, not globally. `tools/schemagate.c:296-299/873-889` needs its define spaces renamed with them.

*Gate: byte-identical objects at every `-O` on twelve keys. A rename must move nothing.*

### P4 — the cutover

The only phase with real risk, and the only one allowed to move a counter.

- `ast_func_end` takes its arena from Replay_IR. Per-body validity is the existing `ast_validate` + `rir_emit_safe` pre-flight (`src/mccrir.c:3580-3703`) plus the byte compare; failure falls through to the parser's bytes on the path that already exists at `src/mccast.c:18804`.
- Rewire the consumers, per the scope decision: the pass drivers (`ast_run_strat_cycle`, the loop transforms at `18276-18283`), register promotion, the retain pools (`ast_inline_retain`/`ast_reemit_retain`, `18912-18913`), the `-O3` forward-inline re-emit (`src/mccgen.c:962` → `ast_reemit`), and the JIT blob producers at `18045/18486/18526/18572/18910`. The JIT seam is thin: `mccjit_embed.c` is already arena-generic — every one of its 135 arena calls runs on `it.arena` from `mccjit_intent_deserialize` (`src/mccjit_intent.c:803`), never on `ast_cur` — so only the six producer sites change.
- Delete the `RIRC2TREE` control leg (`src/mccrir.c:4464`) and the tree-versus-tree counters `arenacmp`/`arenacounteq`/`arenahasheq`/`treenodes`. `arenahasheq` is today's headline fidelity metric and it measures against the thing being deleted; retire it deliberately and replace it with C2, which measures against the parser.
- Ungate `MCC_REPLAY_IR`: drop the CMake option, remove the `REPLAY_IR` row from `GATES[]` in `tests/fuzz/runner.c`, make **capture** unconditional while **verify** stays gated — `rir_verify` costs +7.7% wall clock on every compile (4.30 s against 4.63 s over 266 corpus compiles at `-O1`), which is not a tax for every user build. `MCC_REPLAY_IR_C2` deletes with its two `#ifndef` lines. Once ungated, `rir_parity.cmake` and `rir_c3.cmake`'s 77 skip paths are dead and must become hard failures, or a build that silently lost Replay_IR reports green.

*Gate: `-O0` unchanged; at `-O1`/`-O2`/`-O3`, every body is either byte-identical to the pre-cutover build or on a banked fallback list. Publish that list per key — it is the honest statement of what the 18-body gap now costs.*

### P5 — delete the recorder

1,716 lines of hooks and their 126 call sites; the shadow vstack (`ast_vs`, `ast_cf`, `ast_vn`); `ast_bail`/`ast_desync`/`AST_SET_BAIL`/`AST_SET_DESYNC`/`ast_gap_note`/`ast_replay_ok`; `ast_try_active`; the ~31 recorder-shape `MCC_AST_*` gates in `ast_configure` (`src/mccast.c:2055-2247`); `ast_ltemp_overlaps` if the LICM-temp table goes with it; the 4 `tests/ast/verify-baseline/` files; `ast-verify-ratchet-{O1,O2,O3}`, `ast/treecheck`, `ast/tracediff`, `tools/gate-ledger.sh`'s recorder half.

**The 44 `ast/replay-*` cells survive and become Replay_IR's regression suite for free** — they assert `[ast-replay]`/`[ast-promote]`/`[ast-inline]` markers, which are producer-agnostic. So do the 20 `asttool` suites, which never touch the recorder.

### P6 — split and rename

`mccast.c` (19,082 lines) splits along the boundaries the map already found: arena + replay + hashing (`85-1117`, `5640-7082`, `14413-14494`), the passes (`7083-14412`), slice/search/JIT infrastructure (`16076-18053`), and the drivers. Then `ast_*` → `ir_*`, `AST_*` → `IR_*` across the seven files and `tools/asttool.c`. Rename the things that were never AST while the diff is already open: `ast_data_all_zero`, `ast_strpool_find_or_add`, `ast_pinned_regs`, `ast_alloc_loc`, and the six `ast_*_env` codegen gates are all genuine compiler machinery carrying the prefix by accident. `tools/targetgate.c:3-7` whitelists `src/mccast.c` by name and needs the new unit names. Run `./cmake-debug/tracegate src` and `./cmake-debug/schemagate src` before every push, and keep the new units free of any `MCC_TRACE(` call for the reason recorded above.
