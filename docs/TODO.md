# TODO

## Instructions

Numbered by precedence: a lower number wins a conflict with a higher one.

1. **Default-OFF gate ⇒ byte-identical.** Every behaviour change lands behind a new env gate whose off state produces byte-identical output. Validate the gated-ON path before the default flips.
2. **The bar for a flip:** ctest byte-identity, `-O6` differential vs gcc and clang, 3-stage self-host fixpoint, `MCC_JIT=1` ≡ `MCC_JIT=0` on every triple that has a JIT, UBSan/ASan, cross-arch i386/arm/arm64/riscv64, differential fuzz on x86_64 and native arm64, shadow-IV zero divergence.
3. **Run the x86_64 3-stage self-host at `-O2`, `-O3` and `-Os` before flipping any AST or codegen gate default-on.** Fuzz, exec and cross-arch never build a TU heavy enough to expose a register-pressure or whole-TU bug. The gating invariant is `s3 == s4`; `s2 != s3` is expected once the `MCC_EMBED_*` defines are dropped. Byte-identity passes a *stable* miscompile, so also run something that executes the mcc-built mcc.
4. **A replay bug costs coverage; a model bug miscompiles at `-O2`+.** The always-on byte comparison rejects a bad replay before anything consumes it, but once a body is accepted the passes transform the model and re-emit against nothing. Gate model changes on `tests/exec/optimizer/assign_value_effects.c` and `side_effect_order.c` (they count side effects rather than checksumming results) plus the differential corpus.
5. **Never write code comments.** `src/`, `runtime/`, `tools/`, `tests/`, `examples/` stay comment-free. Only non-authorial markers survive: license/SPDX headers, tooling-semantic macros (`FALLTHROUGH`, `clang-format off`, `NOLINT`), and comments that are test payload.
6. **Trunk-based: no branches, no PRs.** Commit a verified fix and `git push origin main` directly. Keep the `Co-Authored-By` and `Claude-Session` trailers. Split unrelated fixes into separate commits. On a rejected push, `git pull --rebase origin main` and push again. If a push is *denied* (the classifier intermittently cites bypassing PR review), do not work around it — leave the commit local and say so in one line; a fast-forward push after a clean rebase often goes through where a diverged one was denied.
7. **Several sessions push to `main` concurrently.** Before starting a large item, `git fetch` and grep `git log origin/main` for the feature's key symbols so two sessions do not build the same thing. Before pushing, rebase; if the remote already landed the same work, drop the duplicate rather than force a conflicting merge.
8. **The differential fuzzer is the only pre-commit gate.** Register-pressure mode, gcc+clang consensus, 100+ seeds, sub-gates forced for a gated change, 0 miscompiles. CI covers ctest, self-host, cross-arch and object diffs. Extend `GATES[]` in `tests/fuzz/runner.c` for every new `MCC_AST_*` gate.
9. **Never false-green.** Work gated on hardware, CI or a toolchain is marked with its exact blocker and skip-marked (`SKIP_RETURN_CODE 77`, self-skip in the script), never quietly closed.
10. **Prune a completed item entirely** — git history holds the detail. Keep a measured negative result only as a one-line constraint on the surviving item.
11. **Never pre-judge an optimization out of scope.** Implement it and measure it. Report whether a technique *ever* fires, at function, TU and whole-corpus scope. "Fired but small here" and "never fired anywhere" are different verdicts, and only the second is a reason to change course — toward fixing the enabler, not deleting the technique.
12. **Every new optimizer gate states its JIT status.**
13. **Check the compile's exit status (or the output file's existence) before reading a counter or comparing objects.** `--stats` still prints its panel after a failed compile, and `cmp` on two files that were never written reports "differ". This trap has produced four confident false findings.
14. **Prefer the `--stats` counter (or an optfire `cdelta` cell) to an object diff when asking whether a pass fired.** An object diff conflates "did not fire" with "fired and emitted identical bytes". Anchor every counter and trace grep — `faithful` matches `unfaithful`, `SYNC` matches `DESYNC`, `verified` matches a refusal message.
15. **Judge loop and codegen transforms on instructions retired, not on static emitted size or memop counts.** Strength reduction deliberately grows the function and shrinks the hot path. A time delta with no instruction delta is code layout, not codegen.
16. **State the `-O` level next to every instruction count and the cache state next to every `-O4` number.** Give each `-O4` A/B its own fresh `XDG_CACHE_HOME`; a shared or home cache silently decides the result.
17. **Measure recorder and gate coverage on the full `src/mcc.c` TU**, never `tests/exec` or the optfire corpus — those contain almost no nested-struct member access and read several points high.
18. **A `0` from an instrument is not a finding** until it has produced a `1` on a known positive drawn from the same population, and until the summarising script has been checked against one raw line of its own input.
19. **`rc=$?` after a pipeline is the last command's status**, usually `grep`'s, not the compiler's.
20. **Read the gate's own comment before investigating the construct it names.** Four investigations found the answer already written there.
21. **Instrument, run, read — do not reason about what the recorder should do and then patch.** `tools/tracediff.sh <mcc> <src> <sideA> <sideB>` answers "why is this accepted under A and rejected under B" and prints the first divergence with the deciding values; each side takes env assignments and/or mcc flags. Use `MCC_TRACE_IF`, not `MCC_TRACE`, for anything dereferencing `vtop`.
22. **Work with TRACE on:** configure `-DMCC_CONFIG_TRACE=ON`, run `-v128 2>trace.log`. Scope it (`MCC_TRACE_FILE`/`MCC_TRACE_FUNC` substring lists, `MCC_TRACE_SKIP` exact names) — 80% of an unscoped trace is `mccpp.c` churn. `-v` is a category bitmask, not a level. `MCC_LOG=<mask>` is a verbosity floor and the only way to trace the link phase, where 1073 trace sites otherwise never fire.
23. **Do not byte-compare two self-compiled `src/mcc.c` objects across a compiler-source edit** — the amalgamation includes the file being edited, so that is two variables. Compile a fixed corpus with both binaries, and keep both binaries in the same directory.
24. **Never forward-declare another translation unit's `static`;** use `ST_FUNC`. Amalgamated builds cannot catch it — `cmake --preset linux-gcc-multisource` can, and is cheap.
25. **Build and test loop:** `cmake --build cmake-debug --target mcc -j`. Preset build dirs are `cmake-<preset>/`; run tests against the preset's own dir. AST unit tests live in `tools/asttool.c`; a new suite registers in both its main dispatch and the `foreach(_an ...)` list in `CMakeLists.txt`. New cli cases need a reconfigure before ctest sees them.
26. **Cross-arch checks use `cmake-cross/mcc-<arch>`.** The `cmake-qemu-*` builds emit native x86_64 and lack the optimizer. i386 ELF runs directly on x86_64. arm64/riscv64 need `--sysroot vendor/gentoo-stage3-<arch>-{glibc,musl}` plus `-B cmake-cross` for the arch-prefixed runtime archive, run under `qemu-<arch> -L <sysroot>`.
27. **i386 and 32-bit arm are the cross targets that catch bugs.** x86_64/arm64/riscv64 share the LP64, 8-byte-double, `FLT_EVAL_METHOD==0` regime. Assert alignment with `_Alignof`, guard excess-precision equality on `FLT_EVAL_METHOD==0`, and do not assert runtime alignment of over-aligned auto locals.
28. **Fast per-gate cross-arch validation** skips the cmake/JIT build: in a `debian:bookworm-slim` container build a per-target mcc straight from the amalgamation (`gcc -w -DMCC_CONFIG_OPTIMIZER=1 -DMCC_TARGET_<T> -Isrc -Iinclude -Isrc/formats -Isrc/objfmt -Isrc/arch/<a> -O0 -o mcc-<T> src/mcc.c`), compile freestanding programs, link with the matching cross gcc, run under qemu. `gcc-multilib` conflicts with the cross-gcc packages — use a separate container for i386.
29. **The Windows JIT/AOT-parity effort is Windows-scoped.** Guard new code behind `_WIN32`/`MCC_HOST_WIN32` or the host-abstraction layer, keep the POSIX paths byte-identical, and when triaging CI act only on Windows job failures unless asked otherwise.
30. **i386-win32 work uses the winlibs `i686-w64-mingw32` GCC** at `C:/Users/llg/opt/mingw32` (ucrt, real libgcc — `___chkstk_ms`, `___udivdi3`), side by side with the x86_64 scoop mingw; the scoop llvm-mingw i686 compiler is compiler-rt only and cannot link i386. Build with `cmake --preset cross` then `ninja -C cmake-cross mcc-i386-win32 i386-win32-libmccrt.a`; `mcc-i386-win32.exe` is a host x86_64 binary emitting i386-win32 PE that runs natively on WoW64. `tools/i386win32-soak.sh` (set `MCC_I386_MINGW` for a non-default path) proves the 3-stage AOT self-host fixpoint and runs the stub tail under `MCC_JIT_I386_STUBS=1`.
31. **Never add the single-precision float math family to the shared `runtime/win32/lib/msvcrt.def`.** 32-bit `msvcrt.dll` does not export `sqrtf`/`floorf`/`ceilf`/`expf`/`logf`/`log10f`/`powf`/`sinf`/`cosf`/`tanf`/`_hypotf` (the 64-bit one does), and the def is shared across win32 arches, so an import binding there yields an i386 PE that fails to load with `STATUS_ENTRYPOINT_NOT_FOUND` before `main`. The i386-only shim `runtime/win32/lib/mccmath.c` wraps the double entries and is pulled from `libmccrt.a` on demand so a real `-lm` still wins. UCRT is irrelevant to mcc's own output — mcc always targets msvcrt regardless of the reference compiler.
32. **Keep the Windows exec runner's watchdog.** `run_capture` in `tests/exec/runner.c` runs the child through cmd.exe inside a Job Object, polls the pipe against `MCC_TEST_TIMEOUT` and `TerminateJobObject`s the tree on deadline, and `main` sets `SEM_FAILCRITICALERRORS|SEM_NOGPFAULTERRORBOX`. Without both, a wedged or unloadable child stalls the suite until ctest's own timeout.
33. **Two build traps:** never pipe mcc's stdout to `head`/`grep` during a build (SIGPIPE kills it mid-link, leaving no output file), and never pass a `-DMCC_EMBED_*` define when linking a stage compiler.
34. **Reply style.** Succinct, plain words, no filler. Summaries are counts, not adjectives: name the important nouns, break them into subcategories, report numbers.
35. **The operation journal stays gated and alongside the AST recorder until it is at parity or better.** It is compile-time gated (`MCC_JOURNAL_HOOKS`) and runtime gated (`MCC_JOURNAL`), both default-off, with two inertness tests locking that the runtime gate off moves no bytes (`ast/journal-inert`) and that a `-DMCC_JOURNAL_HOOKS=0` build emits objects identical to the hooked build's (`ast/journal-inert-build`). Parity is decided mechanically by the journal column in `tests/ast/verify_ratchet.cmake`, not by judgement: if the AST recorder is faithful on a function the journal must be faithful too, and that delta is reported every run. Byte-faithful replay alone is not parity — the tree still exclusively carries the optimization passes, the loop transforms, `ast_intention_hash`/`ast_slice_ident_hash` and everything keyed on them, and the AOT/JIT equivalence invariant. Nothing is cut over until those are covered.

## Items

### JIT/AOT parity across every triple

- Fix `--embed-jit` in the DIST builds: every shipped mcc advertises the flag and cannot fulfil it. Found 2026-07-31 while running the `--embed-jit` soak the `MCC_MACHO_ADHOC_SIGN` item wants, now that the arm64 TLV work made that soak reachable. `cmake-dist-macos/mcc --embed-jit --jit-functions=hot` fails with `unresolved reference to '_mccjit_boot_swap'` and `_mccjit_set_search_budget`, under both signers, while the same command on the `macos` preset succeeds in 0.076 s. Cause: `libmcc_jitengine` is created only under `if(MCC_EMBED_JIT AND NOT MCC_BUILD_STATIC_LIB)`, and **every** dist preset inherits `MCC_BUILD_STATIC_LIB=ON` from `_dist` (dist-linux-gcc, dist-linux-clang, dist-macos, dist-mingw, dist-msvc all inherit it), so no blob is baked — but `MCC_EMBED_JIT=1` still reaches the binary, so the flag is offered. This is not macOS-specific; only the macOS leg has been reproduced.
  Two obvious fixes, both measured and both wrong. (a) Drop the `NOT MCC_BUILD_STATIC_LIB` guard so the target is always created: ninja refuses the configuration with `multiple rules generate libmcc-static.a`, because `mcc_libmcc_output` gives every STATIC target the same output name — and that name cannot simply be changed, since `src/mcc.c:1675` looks up `<exedir>/libmcc-static.a` by name to find the engine at runtime. (b) Fall back to `$<TARGET_FILE:libmcc>` as the blob source when `libmcc_jitengine` is absent, on the reasoning that static libmcc is the same `LIBMCC_SRC` archive: it configures and builds, and then `--embed-jit` HANGS past 10 minutes where the working path takes 0.076 s. The two archives are NOT the same shape, which is the thing to know before trying again: both are ~1.8 MB and both are named `libmcc-static.a`, but the working one (`libmcc_jitengine`, single-source) holds **2** members — `__.SYMDEF` plus one amalgamated `libmcc.c.o` — while dist's (`MCC_SINGLE_SOURCE=OFF`) holds **20**, the per-TU objects. Narrowed 2026-07-31: this is NOT general archive handling. A plain `mcc prog.c cmake-dist-macos/libmcc-static.a -o out` against that very same 20-member archive links and exits 0 in well under a minute. So the loop is in the embedded-blob path — the bin2c'd in-memory archive that `--embed-jit` links from — and not in the on-disk archive reader. Start there.
- Prove i386-linux self-hosts *under* its own JIT: the JIT is `-run`-only, so a `-c` self-compile reports `recompiles=0` and cannot exercise it. Needs `mcc32` running itself via `-run`.
- Fix the x86_64-PE runtime-JIT `0xC0000005` — now confirmed CI-**environment**-gated, not a live open question. As of CI run 30591788860 (2026-07-31, a green run) the `mingw / x86_64` cell still SKIPs `selfhost-jit`, and `selfhost-jit.py` only emits SKIP-77 on the `0xC0000005` exit (`-1073741819`/`3221225477`, line 126) — the engine IS baked there — so it still crashes on CI. It does NOT reproduce on either local x86_64 toolchain (winlibs-gcc-16.1-ucrt, mstorsjo-llvm-msvcrt; `selfhost-jit.py` PASS, in-memory JIT output byte-identical to AOT, 15 admission-widening stress runs clean). Same winlibs-16.1 toolchain both places → the differentiator is the OS: CI is windows-2025 (Server), local is Windows 10. So this needs a windows-2025 host (or the CI runner) to reproduce and debug; a local Win10 cannot, and speculative patches are inadmissible without a repro (instruction 21). The self-classification already reports status honestly and un-skips the moment it is fixed. The CI-crash instrument is now LANDED (2026-07-31): `mccjit_embed.c` arms a Windows vectored-exception handler (opt-in via `MCC_JIT_CRASH_DIAG`, which `selfhost-jit.py` sets on the child) that on `EXCEPTION_ACCESS_VIOLATION` dumps the faulting PC, the machine bytes there, the page protection, the fault address, and the recently-published JIT entries/slots + boot context (variant/baseline/entry), then returns `EXCEPTION_CONTINUE_SEARCH` so the process still dies 0xC0000005 and the cell self-skips unchanged; `selfhost-jit.py` captures the child stdout/stderr and re-emits it on the SKIP so the dump lands in the CI log. Windows-only, opt-in, POSIX byte-identical; validated on local Win10 (compiles under the CI mingw, `selfhost-jit` still PASSES with it armed) and the VEH's own format proven against both an execute-fault and a data-fault via a standalone harness — but the target crash itself still needs a windows-2025 run to fire it, so **next step is to read the next CI dump** and disassemble the swapped variant at the reported PC. Ruled out and do not re-try: the icache flush (landed, did not cure it); RWX page protection (the win32 `mmap` shim already maps `PROT_EXEC`→`PAGE_EXECUTE_READWRITE`, and ACG is not enabled on GH runners). It is the x86_64-only swapped-variant/KGC-stub path. arm64-Windows stays separately skipped.
- Fix arm64-Windows JIT parity: SEH unwind through JIT frames, `IC IVAU`/`DSB`/`ISB` coherence, frameless-leaf return corruption. Needs arm64-Windows hardware; qemu and wine mask all three via x86-TSO.
- Fix the arm64-Windows MSVC JIT-exec wild-jump miscompile. Needs arm64-Windows hardware.
- Root-cause the arm64-Windows `jit/selftest-nearmatch` SIGSEGV and remove the skip. The thread pool, the `mccjit_invoke` signature cast and local reproduction are ruled out; the fault appears once a scenario runs the accept/reject bench.
- Verify x86_64-macOS JIT parity. Needs real x86_64-macOS hardware — Rosetta hangs executing dynamically-generated x86_64 code.
- Prove riscv64 full self-compile byte-identity on native riscv64 silicon.
- Close the last two `clang -c` corpus cases. Re-swept 2026-07-31 on arm64-darwin over every `tests/exec/**/*.c` except the harness (274 files): clang compiles 257 of them, mcc links 242, and 240 of those match a clang-linked reference on stdout and exit status. The 15 link failures are all harness shape, not reader defects — cases that are one TU of a multi-TU program, or an `__asm__` alias/section construct with no definition in the file. What is left is `complex` and `complex_annexg`, both aborting at runtime: mcc's complex helpers take `void (T*, T, T, T, T)` where libgcc returns the pair by value — the same ABI decision already tracked under Codegen. `tls` is CLOSED by the Darwin TLV work (it links and runs on both arm64 and x86_64), and `runner` is the harness itself, not a corpus program. `tests/macho/clanglink.sh` covers 15 of the corpus; widen it once the complex pair lands.
- Exercise `--embed-jit` for the osx triples; nothing does, and the Mach-O archive reader is only tested against a synthetic fixture.
- Add a non-JIT `-O2 -run` correctness leg: compile and run a small workload with the mcc-built mcc and compare against the stage-0 mcc. A stable `-O2`/`-O3`-only self-compile miscompile currently slips both the fixpoint and `selfhost-jit`.
- Give the JIT-validated set the gates it is missing: `MCC_AST_CHAINSTORE`, `MCC_AST_PROMO_INCDEC`, `MCC_AST_IVSR_PTR`, `MCC_AST_REGDISP`, `MCC_AST_INDIRECT_LOAD`, `MCC_AST_LANDOR_FOLD`, `MCC_AST_CLEANUP_RET` and `-fc99-inline-body` are AOT-validated only.
- Auto-enable the mcc-built i386 embed-jit engine in the CI build — the mechanism is FIXED and validated as of 2026-07-31. The startup `0xC0000005` was an **unbound import**: a native i686 build bakes a *gcc* engine whose `libmingwex`/`mingw32`/`libmsvcrt` reach msvcrt via gcc import-library idata, which mcc leaves unregistered in `IMAGE_DIRECTORY_ENTRY_IMPORT`, so the loader jumps into a hint/name string at startup. Two fixes landed: (1) `mccpe.c` maps `IMAGE_REL_I386_SECREL` (0x0b) → `R_386_TLS_LE` so native-Windows-TLS objects link at all; (2) a new CMake cache var `MCC_EMBED_JIT_MCC_ENGINE_CC` (path to an mcc that emits this target's PE, e.g. `mcc-i386-win32`) compiles the blob engine WITH mcc and defines `MCC_EMBED_JIT_MCC_ENGINE`, so `--embed-jit` takes the clean bound-def path (`libmccrt` + mcc-def `msvcrt`/`kernel32`, no gcc footprint). Validated end-to-end: with the var set, `cmake-i686-embed`'s `mcc --embed-jit -O2` output RUNS CLEAN — `bake=40425` at both `MCC_JIT=0` (was the crash) and `MCC_JIT=1`. Opt-in and WIN32-gated: empty (default) keeps the gcc engine, so every other build is byte-unchanged (the mcc-built engine defines must match `libmcc_jitengine`, notably `MCC_EMBED_JIT=1`/`MCC_JIT_DEFAULT=1`, or `mccjit_embed.c`'s `mccjit_boot_swap`/`mccjit_set_search_budget` are gated out; the CMake path reuses the target's defs so this holds). Remaining to make `embed-jit-smoke` PASS on i386-PE (and unblock the i686 CI promotion below): set `MCC_EMBED_JIT_MCC_ENGINE_CC` automatically in the i386/i686 build. The i686 CI cell (`ci.yml`) is a *native* winlibs-GCC superbuild with no cross `mcc-i386-win32` present, so the self-contained route is a **two-stage self-bake**: add a bootstrap mcc target from `src/mcc.c` WITHOUT the blob (no `target_sources(mccjit_blob.c)` → emits i386 PE but does not depend on the engine bake, breaking the cycle), default `MCC_EMBED_JIT_MCC_ENGINE_CC` to `$<TARGET_FILE:that_bootstrap>` on WIN32-i386, and let it build the engine the real mcc embeds (an extra ~full mcc build on that cell; `mcc_static`/`mcc_dynamic` are reuse candidates if built there and blob-free). Alternatively a CI step builds the fast host-x86_64 `cross` mcc first and passes its path. arm64-PE needs an arm64 mingw; full `MCC_JIT=1` runtime on PE is the separate `0xC0000005` blocker.
- Wire the MSVC `--embed-jit` fix into CI. The ucrt/msvcrt CRT-model conflict is FIXED as of 2026-07-30 via `MCC_EMBED_JIT_MINGW_CC` (CMake): point it at a msvcrt-targeting mingw gcc and the `--embed-jit` blob engine is built with that gcc (reusing the cl target's exact define/include set) instead of cl, so it references only msvcrt/mingw symbols the baked libdirs resolve — not the UCRT stdio internals / vcruntime (`__GSHandlerCheck`) / `/RTC` (`_RTC_*`) cl emits. The `:2696` libdir bake now queries that gcc (`_jit_bake_cc`) rather than cl. Empty (default) keeps the cl blob, so the MSVC build is unchanged unless opted in; the mingw/ELF paths are untouched (the block is `CMAKE_C_COMPILER_ID STREQUAL MSVC`-gated). Validated end-to-end on `cmake-msvc/Release` with the scoop mstorsjo-msvcrt gcc: `mcc --embed-jit -O2` links (was unresolved-symbols), the output runs, and it re-runs correctly under `MCC_JIT=1` (embedded engine exercised). Remaining: give the MSVC CI cell a msvcrt-mingw and set `MCC_EMBED_JIT_MINGW_CC` so `embed-jit-smoke` runs there instead of skipping, and decide whether to auto-detect a mingw on MSVC hosts rather than requiring the explicit path.

### Known-open miscompiles and crashes

- Root-cause the `MCC_AST_IVSR_PTR` layout-lottery miscompile. **Currently LATENT — it stopped reproducing at 7ba7715d and `selfhost-fixpoint-memmodel-O3` is green again, but nothing fixed it**: the 227-line `mccast.c` change in 4afc4cc7..5d18df22 reshuffled frame layout, which is exactly what a layout lottery does. Do not close this item on the strength of a green cell. It was reproducible on arm64-darwin at f565eea4 with the journal hooks compiled in. `MCC_AST_IVSR_PTR=1` at `-O3` alone was the trigger (the other three memmodel knobs were innocent); the cell was green with the hooks compiled out and on x86_64, which is what pinned it to the hooks' effect on layout rather than to the hooks themselves. Mechanism, traced: an mcc-built mcc gets a garbage by-value `AstVLat` back through `ast_narrow_elim_fits`->`ast_narrow_elim_srcrange`->`ast_vlat_context_at` — `lo=7 hi=4` (lo>hi, impossible) with `kzero`/`kone` holding live heap pointers — so `ast_narrow_elim` concludes a `u16` fits in `u8` and drops the truncation, where a clang-built mcc correctly reads `lo=0 hi=65535` and declines. The emitted code stays equivalent here (`LDRB` vs `LDRH` feeding a `STURB`), so it costs self-host stability rather than a wrong answer, but the reasoning is unsound. Ruled out, do not re-derive: a single miscompiled function (bisect over the 262 IVSR sites converged, but no singleton reproduces — it is cumulative, so the single-culprit assumption the bisect rests on is invalid); miscompilation of the victim chain (all seven functions from `ast_narrow_elim` down to `ast_vlat_in_type` are byte-identical IVSR on vs off); the lattice table accessors (`ast_vlat_find` and `ast_vlat_transfer` are the only vlat functions IVSR changes, and both are clean alone and together); uninitialized locals (`-ftrivial-auto-var-init=zero` and `=pattern` agree); and ltemp stack aliasing — over-reserving the `ast_ivsr_ptr_run` slot appears to fix it, but the size sweep is non-monotone (8 BAD, 16 GOOD, 32 BAD, 40 GOOD, 48 BAD, 64 GOOD), so that is a layout lottery, not an aliasing threshold, and the apparent fix is luck. It is a heisenbug: a `getenv` probe in either `ast_narrow_elim` or `ast_vlat_context_at` makes it vanish, so any probe must be re-validated against a still-failing build before its trace is trusted. Next technique must not modify the binary: attach lldb to a reproducing stage1 mcc, break in `ast_narrow_elim_fits`, compute `ctx`'s frame address from the disassembly and set a hardware watchpoint to catch the writer in the act.
- Re-default `MCC_AST_REGPAIR` on for i386/arm. Its ON-path nested-register-pair miscompile is FIXED as of 2026-07-30 (`mccast.c:6392` invoke re-emit): a call whose wide-integer result is itself a nested call argument re-emits as a synthetic invoke that never recorded `wide_r2`, so `ast_wide_r2` returned `AST_R2_NONE` and the high half materialised from the absent constant `q.hi=0` (`mov reg,0` in place of `push edx`) — `bswap_inline.c`/`__builtin_bswap64(__builtin_bswap64(x))` via a wrapper inline returned 3 at `-O1`+. The fix reconstructs the pair from the ABI return registers (`REG_IRET`/`REG_IRE2`) for a `VT_LLONG` invoke return when `wide_r2` is unset, `#if`-gated to i386/arm so x86_64 is byte-identical. Validated: `bswap_inline` REGPAIR=1 rc=0 at -O1/-O2; i386 default (REGPAIR off) AOT self-host o1==o2==o3 + 36 stub-tail + 25 fuzz all green; i386 REGPAIR=1 self-hosts `src/mcc.c` to a fixpoint; x86_64-win32 self-host byte-identical. `MCC_AST_REGPAIR=1` is now in the fuzzer `GATES[]`, and `tools/i386win32-soak.sh`'s differential-fuzz leg forces the REGPAIR sub-gate: every generated program must match the gcc(O0,O2)+clang consensus at default gates AND with `MCC_AST_REGPAIR=1` (80 seeds, agree=80/diverge=0 as of 2026-07-30). Remaining before the default flip: only the **arm** leg — an int↔llong round-trip on native/qemu arm (the fix applies there via `R0`/`R1` but is untested; no arm sysroot is vendored here, so it needs a host that has one). `MCC_AST_VERIFY` misses this class (plain replay byte-identical; only the optimized/graft re-emit broke — instruction 4).
- Root-cause the `.data.rel.local` RELRO synthesis divergence: a gcc-built mcc emits a separate 8-byte `.data.rel.local`, an mcc-built mcc folds it into `.data`. It surfaces only when linking the larger `cmake-cross/<arch>-libmccrt.a`, and only in the linked executable (`fixpointgate.c` catches it, `selfhost-fixpoint.py` compares objects and does not). Find which C construct in mcc's own linker mcc miscompiles.
- win32 self-host nondeterminism REFUTED and wired into ctest as of 2026-07-30. `selfhost-fixpoint.py` and `selfhost-smoke.py` are now PE-aware (mcc.exe, no `-lm`/`-ldl`, `.o`/`.obj` blob, the `runtime/win32` header+CRT prefix), keyed off the `.exe` suffix so the POSIX path is byte-identical; the CMake guard that gated the whole self-host block on `MCC_EMBED_MCCRT OR Darwin` now also admits the mingw win32 build (via the sidecar `lib/libmccrt.a`, MSVC excluded — its VS/NMake generator emits no `compile_commands.json` the drivers read). On the native x86_64-win32 build the full mcc-built-by-mcc chain is byte-identical o1==o2==o3 at -O1/-O2/-O3/-Os and under the forced-gate set, and `selfhost-smoke`(+gates) produce correct executables. Remaining nicety: the memmodel legs stay arm64-only; an embed-jit-baked-only second leg is already covered because the win32 build bakes `MCC_EMBED_JIT_BLOB` and the drivers link it.
- Audit mcc's linker error paths: `mcc a.o b.o -o x` with a symbol defined twice prints `error: 'addup' defined twice`, writes the output and exits 0.
- Fix the `ast_reemit` segfault when recompiling a pointer-returning function.
- Fix the stack-overflow crash on a call returning a function pointer compared against a function designator. Minimal repro, no headers, crashes `-O1`/`-O2`/`-O3`/`-Os` and clean at `-O0`; gcc and clang both accept it:

  ```c
  static void h(int s) { (void)s; }
  extern void (*f(int, void (*)(int)))(int);
  static int out;
  void g(void) { void (*q)(int) = f(1, h); out = (q == h); }
  ```

  That is `signal()`'s exact signature, so it reaches the corpus through `tests/diff/parts/s7_13.h` — `tests/diff/parts/run_s7_13.c` segfaults the compiler at `-O1`+ on current `main`. The `setjmp`/`longjmp` and `_Alignas` statements in that body are innocent; bisecting reaches the two-line signal-handler comparison. Two defects, the second the more interesting: (1) `type_to_str` recurses without bound between `src/mccgen.c:3753` (pointer arm) and `:3781` (function arm) on this type graph, ping-ponging one func node and one ptr node until the stack is gone — 13083 frames before `main`; (2) it is only reached because AST replay re-issues the comparison through `gen_op(op=148)` at `src/mccgen.c:4053` with operand types that make `gen_op` format a diagnostic at all, via `ast_replay_body` -> `ast_replay_bb` -> `ast_replay_value` (`src/mccast.c:6293`). `-O0` is clean because replay does not run there, so the crash is a recorder defect surfacing as a diagnostic-formatting overflow, not a codegen bug. Not attributable to a gate: `MCC_AST_PROMOTE=0`, `MCC_AST_CSE_WINDOW=0`, `MCC_AST_TEMPLATES=0` and `MCC_AST_INLINE=0` all still crash. Bound the recursion in `type_to_str` regardless — it is reachable from any diagnostic on a cyclic type — and separately establish why replay's operand types diverge here.
- Fix the `-fasan-shadow` native reporter deadlock: `on_sigill` reads a 32-bit-truncated faulting address from `REG_RCX`, computes a shadow pointer outside the mapped region, and faults inside the handler. Validate the address and the dump range, root-cause the truncated register, and put a hard timeout on the five `cli/asan_shadow_*` cells that can wedge the run.
- Harden the JIT intent serializer against VLA functions, or reject VLA in `ast_jit_eligible`. Keep `mccjit_embed_stash_leaf` gated on `(ast_jit_dispatch_env || ast_jit_fns_n > 0)`.
- Stop `ast_jit_env` being true inside `mccjit_recompile_common`: it builds its state with `output_type=MEMORY`, so the recompile's own `ast_reemit_extern` re-applies the mode-6 stub and the recompile returns NULL. Either an `mccjit_recompiling` flag ANDed out, or gate on `s1->optimize >= 1`.
- Fix the nondeterministic use-after-free: `MCC_JIT=1 --stats` crashes on hello.c through `boot_swap → mcc_jit_recompile_blob → mccjit_salt_witness → mcc_trace_at`, likely a dangling `mcc_state` after `mcc_exit_state`/`mcc_delete` in `mccjit_recompile_common`. Embed-boot only; `-run` with stats works. Valgrind cannot run on this host.
- Fix the native-arm64 CI atomics failures on both host compilers, most likely a missing acquire/release barrier (`dmb`/`ldar`/`stlr`) in arm64 codegen. qemu-user runs on x86-TSO and cannot reproduce them. `linux-gcc`/arm64 also fails bounds and `linux-clang-release`/arm64 also fails complex.
- Design an auto `-B`/`-I` search that preserves self-host byte identity. A walk-up-from-cwd fallback fixes the papercut and breaks `selfhost-fixpoint`, because stages 1-3 build into a temp dir and would resolve to a different header set from stage 0. Options: record the producing build dir at compile time, or have the fixpoint pass `-B` explicitly. `-B` now accumulates; `runtime/win32/include` layers over `runtime/include`, so the win32 layout needs two prefixes.

### KGC verification and the JIT graduation path

- Make KGC verification amortize so widened admission pays. The verify stub costs ~9x per call on small hot functions and only amortizes via memoization, which requires TIER0 while newly-admitted functions are TIER1. Options: graduate to direct dispatch after N consecutive verified matches, find a sound TIER1 memoization key, or keep admission TIER0-only. Routing the sync path through `mcc_jit_recompile_blob_gated` does not help and breaks `regression/o4-aot-jit`.
- Stop near-match acceptance breaking `MCC_JIT=1` ≡ `MCC_JIT=0`. `MCC_JIT_NEARMATCH` is default-on and by design keeps a variant that mismatches the baseline on a small input set, so wider admission admits more divergence. This is a direct parity violation.
- Flip `MCC_JIT_PURITY_NOESCAPE` on once those two clear. It doubles the verified fraction on a scalar hot loop.
- Once verification succeeds *with* routing, have the JIT enumerate candidates from `eligible & ast_search_searchable(base)` and record the winner as `proven`. The graduation path is currently unreachable: KGC-on makes `mccjit_lazy_build_masked` return NULL, KGC-off installs an unrouted trampoline so `routed` stays 0, and `gs_bench_won` needs both. `MCC_JIT_NO_KGC=1` can never produce a proven record.
- Exercise the `proven`-outranks-`static` merge and probe rules against a record the JIT actually wrote; they are unit-tested only.
- Find a workload that produces two different gate configs for one slice ident, so `MCC_AST_SLICE_MULTI` has something to distinguish. Every configuration tried yields one config per ident with an identical mask.

### Recorder fidelity

- Triage every gap below against the operation journal rather than re-deriving it; the journal's own work is grouped in *Operation journal* below. `MCC_JOURNAL=1` records codegen operations linearly and replays them against the same byte oracle `ast_func_end` uses; `MCC_JOURNAL_ORACLE=<fn|1|all>` then prints, for a body the tree misses, the first byte where the tree replay leaves the parser *and* the journal op that emitted it — `b16` in `codegen/bswap_inline.c` resolves to "+4 is emitted by op #0 `bswap` spanning +0..+8", i.e. replay never issued the `rolw`. The journal is faithful on 1133 of 1133 non-empty bodies plus 35 vacuous of 1168 journalled over `tests/exec` + `tests/diff/full_language.c` at `-O1`, where the tree is faithful on 991 of the same 1168 — it is faithful on every function the tree fails. Do not port passes onto it; it is an oracle, not a replacement IR.
- Triage the declining bodies from the oracle's own census rather than by re-deriving each one. `MCC_JOURNAL_ORACLE` covers desync and bail as well as unfaithful, naming the journal op in flight at the byte where the tree gave up plus the recorder state there; every non-replayed gap attributes to a named op and the journal is `jfaithful` on all of them, so each printed op stream is certified. Cite the HOOK, not the line number — the codes are raw `src/mccast.c` lines and any edit above a site renumbers every one of them. Census over `tests/exec` + `tests/diff/full_language.c` at `-O1`, hook then count then code as of 2026-07-30: `ast_hook_bail` 18 (3416), `ast_hook_call_begin` callee-kind 11 (3839), `ast_hook_landor_next` second-consumption 9 (3075), `ast_hook_landor_operand` folded-operand 6 (3048), `ast_hook_vpush` value-kind 6 (2600), `ast_hook_if_begin` 6 (3119), `ast_hook_vstore` short-circuit region 5 (4023), `ast_hook_vpush` type 4 (2596), `ast_hook_label` 4 (3507), `ast_hook_vstore` ternary region 3 (4019), `ast_hook_call_begin` arg type 3 (3822), `ast_hook_goto` 3 (3525), then a 10-event tail across `ast_hook_switch_begin`, `ast_hook_do_begin`, `ast_hook_while_begin`, `ast_hook_ternary_begin`, `ast_hook_member_end` and `ast_hook_builtin_complex_end`.
- Model the chained-assignment value materialisation. The model has to represent both "the value is this constant" and "materialise it into a register once, before the first store"; a single leaf cannot express it. Two narrow fixes (skipping the finalize, preserving the constant form) are measured and rejected — keeping the register loses the materialisation, keeping the constant changes the store encoding. Do not make the marker emit at its use site; that was correct at `-O0`/`-O1` and miscompiled at `-O2`.
- Model a store inside a `&&`/`||` region (14 events) and inside a ternary region (4). The store has to become a child of the region operand's `AST_Binary` rather than a basic-block statement. Two attempts balanced the counts while leaving the model an operand short, so `ast_replay_ok` passed and replay segfaulted — assert on model shape, not on `ast_vn`. Relaxing the guard does not recover them.
- Model the identity-constant `&&`/`||` chain's second consumption (`ast_hook_landor_next`, 9 events). Three approaches are ruled out: decrementing and continuing (replay runs on a model missing an operand and segfaults), desyncing the whole identity-constant case (costs 17 faithful functions), and re-deciding the region's kind at the end (+2 faithful, 15 new desyncs). The region's representation is not the blocker. The companion question this item used to carry — why an operand reaches `ast_hook_landor_operand`'s deferred path with `ast_vn < 1` — is answered: it does not. That predicate was fused with `c >= 0` on one line; split, all 6 events are `c >= 0` and the `ast_vn < 1` arm takes zero, with the arm proven able to report (widening it to `ast_vn < 100` makes it fire). So that bucket is entirely the later-short-circuiting-constant case below, not a stack-depth bug.
- Model the short-circuiting `&&`/`||` fold and the materialized ending TOGETHER; neither closes anything alone. Measured 2026-07-31 over `tests/exec` + `tests/diff/full_language.c` at `-O1`: the materialized ending is worth 2 events, not the 20 this item used to claim, and BOTH are masked — `ast_hook_landor_end` returns early on `ast_desync || ast_bail`, and both functions have already declined at `ast_hook_landor_operand`'s short-circuit fold (`c != i`). They are the same two functions in both buckets: `codeopt.c` `broken_jumpopt` and `ternary_op.c` `tst_yarpgen`, each arriving at the operand hook with `ast_vn` 2. So fixing the materialized arm on its own converts zero functions, and fixing the operand arm on its own just moves them to the materialized arm. The pair is worth 2 functions total. The parser's ending is `vpop(); vpushi(i ^ f); gsym(t)` — the region's value is discarded and a constant replaces it — so the model needs "evaluate these children for effect, then this Literal", a shape the const path does not provide (it assumes the constant is already on `ast_vs`). Assert on model shape, not on `ast_vn`: the two prior attempts balanced the counts, left the model an operand short, and replay segfaulted. `MCC_AST_LANDOR_FOLD` (default OFF, byte-identical off over 1072 object pairs at `-O0`..`-O3`, AOT-validated only) already handles the third fold shape — an identity constant with another operand behind it (`c == i && tok == op`), which the parser resolves by `vpop()` and the recorder now mirrors by dropping the node. Worth +2 faithful, `u8_string.c` and `uU_char_constants.c`. The fourth shape, an identity constant as the LAST operand (`fp_builtins.c`), is measured and rejected: attaching it as the region's final child converts that function desync -> UNFAITHFUL, so the model would replay wrong bytes instead of declining.
- Model "this branch ends unreachable" so replay suppresses the same `jmp rel32` the parser drops after a `noreturn` call. This is the residual ±5 unfaithful class (13 events); it is a separate manifestation from the `nocode_wanted` desync site, so fixing that one does not move it.
- Investigate the remaining unfaithful bucket. Of 297 events, 54% are same-length instruction-order and register-choice differences, and 90 of 92 sampled byte-differs are reorder or register allocation only. Closing a meaningful part of it needs replay to reproduce the parser's scheduling and allocation, or a semantic-equivalence check to replace byte equality. A codegen change cannot reach it — parser and replay share `gen_cast`, so a fix moves both sides together.
- Make replay issue the specialised builtin inline expansions. Seven unfaithful functions (the `bswap` files ×4, `bitscan`, `signbit`, `alloca`) replay without the inline sequence the parser emitted — `b16`'s missing `rolw` is the canonical oracle example. Previously cited only as that demo; it had no fix item.
- Attribute and model the `ast_hook_if_begin` vn≠1 bail (`ast_hook_if_begin`, 6 functions: `grep.c` ×3, `codeopt.c`, `dead_code.c`, `ternary_op.c`) — the `if` condition did not reduce to exactly one modelled value. Uncovered by any prior item. The oracle now places 5 of the 6 with a `jmp` op in flight, so the condition's jump has already been emitted when the hook declines.
- Model the loop- and switch-begin bails under `nocode_wanted` (`ast_hook_while_begin`, `ast_hook_do_begin` and `ast_hook_switch_begin` — `kb_wait_1`, `kb_wait_2_1`, `kb_wait_4`). The unreachable-branch item covers only the `jmp rel32` suppression class, not these.
- Model the GNU omitted-operand ternary `a ?: b` (`ast_hook_ternary_begin`, `gnu_cond_omitted.c`). No item covered it.
- Model the one tree-empty body that is a real lost model: `test_symbolic_address_constraint_compile_only` in `tests/exec/arch/arm64_extasm.c`, `asm volatile("" : : "S"(sym))`, where the tree records nothing and the journal replays 2 ops faithfully. Enumerating the bucket (2026-07-30) split it 35 benign / 1 lost, not the "single largest cause of the delta" it was recorded as: `jrn_verify` flushes the trailing gap op before testing `jrn_n`, so `jempty` proves a zero-byte body, and 35 of the 36 are `jempty` with `ops=0` — nothing was emitted, so there was nothing to model and no journal win. The parity delta now excludes them. The residual one is inline asm, so it closes with the inline-asm item rather than on its own.
- Model the types `ast_bad_vtype` still refuses: `VT_STRUCT`, `VT_BITFIELD`, `_Complex`, `VT_QFLOAT`. `long double` and `__int128`/`VT_QLONG` now round-trip through the lazy `wide_hi`/`wide_r2` columns and are admitted at the value-model sites. The earlier measurement here — that letting `long double` through moved four functions desync to unfaithful and zero to faithful — was of a BROKEN relaxation: the guard was lifted while the leaf still dropped `q.hi` and `r2`, so replay materialised the wrong constant. With the value actually carried the result inverts. Note `ast_bad_type` is deliberately still strict and is a different predicate: it guards consumers that reason in 64 bits (`value64`/`ast_fold_eval`, the switch selector, `ast_eval_slice.h`), and relaxing it would fold a 128-bit literal through its low half — a miscompile, not a desync.
- Model a register-held lvalue (value-kind guard, 61 of 112 events at that site). The register is an allocation artifact, so the pointer's provenance has to be modelled instead of the register named.
- Model a struct-typed value that is not an aggregate lvalue (51 of 112 events at the same site).
- Model the 32-bit `int`↔`long long` conversion, which materialises into a register pair. Every function mixing 32- and 64-bit integers desyncs on i386 and arm, costing roughly 5 percentage points of coverage on a real TU. Needs the value model extended to register-resident and register-pair values.
- Byte-mirror the parser's void join so a void-arm ternary replays faithfully.
- Record a ternary whose value is discarded when it sits inside a larger function; today it contributes nothing and the arms' side effects are missing from the replay. `(void)(c ? 1 : 2)` stays empty by design.
- Model the store in `while ((h = f()))` inside the loop's condition region. Replay emits it once ahead of the loop, and the guard already lets it through, so widening the guard is not the fix. Needs a preamble child on the loop node, which changes its arity and every loop pass.
- Model assignment-as-a-value in a call argument (`f((a = g(v)) + 0)`, `use(s += g())`). `gfunc_call` pushes around the marker so the leftmost-leaf guard declines it and replay re-evaluates the RHS including the call. Same model-shape change as chained assignment.
- Make arm64's `VT_CMP` lazy. **The `cset`-peephole half of this item is DONE and did not pay what the item predicted.** `arm64_load_cmp` now inverts the already-emitted `cset`'s condition in place instead of emitting `eor #1` (`arm64_invert_last_cset`): every cset condition and its inverse differ in exactly bit 12 (EQ/NE, GE/LT, GT/LE, CS/CC, HI/LS), so it is a one-bit, length-preserving rewrite — nothing moves, so the jump-chain hazard that blocks byte-moving transforms here does not apply. It is guarded by a decode check (`(w & 0x7fff0fe0) == 0x1a9f07e0`, leaving sf/cond/Rd free) plus an Rd match against the value's own register, and falls back to the `eor` when either fails. Measured on `tests/exec` at -O1: **0 functions converted from unfaithful, not the 21 this item claimed** — tree-faithful is unchanged at 1018 — and the size win is 180 B of 2351104 B (0.008%), in 4 of 1239 functions, with 0 functions larger. So the instruction really is dropped on every `!` of a comparison, but `!cmp` is rare in this corpus and the recorder's 21 unfaithful bodies are NOT caused by the `eor`. Whatever coverage is there to win needs the lazy-`VT_CMP` half, and the 21-function figure should be re-derived rather than trusted.
- Fix the `MCC_AST_MEMBER_CONST` operand-order divergence: `ast_hook_member_end` records the enclosing `Binary '+'` with its operands transposed relative to codegen's evaluation order. Sethi-Ullman is ruled out. Benign (both operands are pure loads, `+` is commutative) but it costs four functions.
- Generate an `x86_64-darwin` recorder-fidelity baseline; `arm64-darwin` now exists but the x86_64 Mac cell still skips, and Rosetta cannot produce it (the sweep needs a native run). Close the 30-gap arm64/x86_64 delta: arm64-darwin carries 112 desyncs to x86_64-linux's 82, concentrated in `arch/arm64*.c` and the register-pair conversions the recorder does not model.
- Regenerate the `ast-verify-ratchet` baselines for the remaining targets; they skip until regenerated.
- Flip the four 2026-07-31 recorder gates together, not one at a time. `MCC_AST_INDIRECT_LOAD` (+10 faithful), `MCC_AST_LANDOR_FOLD` (+2), `MCC_AST_CLEANUP_RET` (+10) and `MCC_AST_VOIDRET_EXPR` (+1, and only in combination — alone it moves nothing) are all default-OFF, byte-identical off, and each was validated alone. Together, re-measured 2026-07-31 after the loop stopped: 1019 faithful / 53 unfaithful with them off, **1042 / 42** with all four on, over `tests/exec` + `tests/diff/full_language.c` at `-O1`. Clean with `MCC_AST_IVSR_PTR` and `MCC_AST_OPASSIGN` added, the pairing that crashed before the `verify_assign_cast` replay guard; all 11 `selfhost-*` cells pass and the 3-stage fixpoint is byte-identical at `-O2`/`-O3`/`-Os` with them on. `GATES[]` carries them individually plus a `RECORDER_2026_07_31` row forcing the combination. Remaining before the flip: differential fuzz on a native arm64 cell (CI only) and ONE baseline regen banking all 23 wins at once. The gate admits an `AST_Load` callee (`(*fp)(...)`, `t[i](...)`) in `ast_hook_call_begin` and is worth 10 functions, desync to faithful, taking that site from 11 events to 1 — `func_pointers.c`, `function_pointer.c`, `random_stuff.c` `tst_adr`, `scopes.c` `f4`, `declarators.c`, `typedef_semantics.c`, `types.c` ×4. Default OFF and byte-identical off (1068 object pairs over `tests/exec` at `-O0`/`-O1`/`-O2`/`-O3`); ON it changes 14 of those objects, which is the newly-admitted bodies reaching the optimizer. The `MCC_AST_IVSR_PTR` stack overflow that blocked it is fixed (`verify_assign_cast` now skips AST replay, the peer of the journal's own guard). Cleared with the gate forced on: full ctest green except the three ratchets reporting the 10 wins as unbanked, all 11 `selfhost-*` cells including `selfhost-jit` and the arm64/riscv64 legs, 3-stage self-host fixpoint byte-identical at `-O2`/`-O3`/`-Os`, and 120 gated differential-fuzz seeds with `INDIRECT_LOAD` in `GATES[]`. Also cleared: a shadow-IV sweep over `tests/exec` + `full_language.c` at `-O1`/`-O2` in a `-DMCC_CONFIG_AST_SHADOW=ON` build reports zero side-car divergences with the gate on (and the oracle is live — forcing `AST_MEMO_SHADOW`'s comparison wrong aborts it, so the zero is a reading and not a dead instrument), and the `-O6` exec sweep fails the same four goldens with the gate on and off, so the gate adds nothing there — those four are the separate `-O4`+ doubled-diagnostic item. Flip it with the other two 2026-07-31 gates; see the combined item above. An earlier attempt widened to `AST_Binary` and measured no change, because the array-of-fnptr callee is `AST_Load` and the `Binary '+'` is its child; that mis-identification is why the door was closed on the wrong kind.
- Model the constructs the recorder declines outright: struct-by-value ABI, bitfields, VLAs, C11 atomics, `_Complex`, `_Noreturn`, inline asm.
- Finish `__attribute__((cleanup))`: the last one is `test_ret2`, the nested-scope bare `return;`. `MCC_AST_CLEANUP_RET` (+10) models the return-path call and the spill — `struct.c` `main`, `cleanup.c` `test`, `test_cleanup1`..`8`; the six `structcpy` bodies needed no separate handling, because the spill is a `vstore` either way and only the operand type differs. `MCC_AST_VOIDRET_EXPR` (+1, and ONLY in combination — alone it moves nothing, because the body it fixes is unfaithful for the cleanup reason too) closes `test_ret`, which was never a nested-scope case: a `void` function doing `return expr;` has `b = 0`, so `ast_hook_return(0)` built a childless `Return` and dropped `ast_ret_val` — and the expression was `test_ret_subcall(that)`, a real call. The gate re-types that Invoke to `VT_VOID` and appends it as a statement, which is what the parser's evaluate-then-`vtop--` emits. Exactly 1 such body exists corpus-wide, so this is not a general win. Residual `test_ret2`: a bare `return;` inside a nested block runs BOTH scopes' cleanups, two `lea`+`mov`+`call` sequences, and the model records neither. Extending the Invoke reorder to fire when `ast_in_call` was already 0 (the bare-`return;` path) is measured and rejected — it moves `test_ret2` from unfaithful to `desync`, trading a wrong replay for a decline without converting anything.
- Attribute `stackresidue` to a site. `desync` and `bail` record `__LINE__`; `stackresidue` records nothing, so triaging it needs a debugger. Record the line where `ast_vn`/`ast_cf_top` first goes non-zero.
- Characterise the 15 functions that are unfaithful only on riscv64 (`asm_global_instr`, `asm_int_expr`, `ast_constparam_fold`, `ast_data_reemit`, `ast_ivsr_ptr_run`, `ast_ivsr_run`, `ast_jit_fold_consts`, `ast_memo_pack`, `ast_slice_disk_path`, `cstr_u8cat`, `emit_sizes`, `mccjit_recompile_profiled`, `mccjit_selftest_stage2`, `mccjit_selftest_strlit`, `mccjit_selftest_struct`).
- Model `VT_INT128` in the AST optimizer, widening `ast_hook_case` to 128-bit *first* or replay will re-emit truncated case values.
- Attribute `ast_hook_bail`'s remaining events to their caller: the hook is one function called from five places in `mccgen.c`, so every explicit bail reports the hook's own line. The oracle now separates the bucket by the journal op in flight — its 18 events split `axadd` 4, `call` 3, `axchg` 3 and a tail — which is enough to route most of them without touching the hook, so what is left is only the callers the op kind does not distinguish. Note `TOK_builtin_va_arg` in `mccgen.c` sets `ast_bail` raw rather than through `ast_hook_bail`, so it reports `bail:0` and records no decline site at all.

### Operation journal

Instruction 35 governs this whole section: nothing is cut over from the tree. *Recorder fidelity* above is what the journal is an oracle **for**; the items here are the journal's own work. Priority prefixes, ranked: **P0** unblocks a target or a flip and is on the critical path; **P1** retires honesty debt (the raw-blob census — bytes the journal reproduces by copying rather than by modelling); **P2** is model depth (the op-boundary fixup rate); **P3** is reporting. Groups after the first two are by target: read *Portable* plus your own target's group, and treat *Standing constraints* as binding on every group.

Ranked entry points if you want one line: the three **P0 JREGEN** items (arm64-darwin, x86_64-win32, plus the never-created arm64-linux key) are modelling that has already landed and is merely unbanked, so they are pure on-target runs; the win64 `alloca` replay divergence (win32 P0) is the only open *correctness* gap and is localised to one instruction; `VT_CMP`/`VT_JMP` (portable P2) is what everything structural is queued behind.

Where the honesty axis stands after the 2026-07-31 sweeps: **six of seven targets are at zero raw blobs, over the full corpus**, and the seventh has one function left by deliberate design.

| target | journalled | jfaithful | raw fns | raw B | banked |
|---|---|---|---|---|---|
| x86_64-linux | 1176 | 1176 | **0** | 0 | yes |
| i386-linux | 1171 | 1171 | **0** | 0 | yes |
| riscv64-linux | 1149 | 1149 | **0** | 0 | yes, 1 file excluded |
| arm64-linux | 1202 | 1202 | 1 | 688 | yes |
| i386-win32 | 1135 | 1135 | **0** | 0 | yes |
| x86_64-win32 | 1275 | 1258 | **0** | 0 | no, see group |
| arm64-darwin / x86_64-darwin | — | — | — | — | no, needs macOS |

arm64's single residual is `arch/arm64.c myprintf`, 14 gaps of `va_arg(ap, struct s7)` — the struct case `JOP_VA_ARG` declines on purpose, and it is the same boundary the recorder has for struct operands, not an unmodelled emitter. What is left on this axis is macOS hardware and one riscv64 codegen bug, not ops.

Model depth is untouched by all of it and remains the real distance to a journal that *derives* rather than *is told*: 31003 fixups over 795697 ops on the reference target.

**Cross-compiled sweeps are a valid substitute for the honesty baseline — measured, not assumed.** The old win32 entry asserted "no cross substitute, the objects must be produced on-target". That is now known to be too strong. Each of i386, arm64 and riscv64 was swept twice, once with the host cross compiler and once with a *native* mcc for that target — built by mcc itself and executed under `qemu-user` against the in-tree `vendor/gentoo-stage3-<arch>-glibc` sysroot, the same trick `tools/selfhost-cross-native.sh` already relies on. The honesty file came out **byte-identical on all three**. The depth ceiling did not: it is native+1 on arm64 (30522→30523) and riscv64 (29474→29475) and exactly equal on i386, so the +1 tracks 64-bit non-x86 targets and the banked ceilings use the **native** figure. Bank honesty from a cross run if you must; bank depth only from a native one.

The lever that made all of this measurable was the vendored sysroots. Sweeping a cross target with no `--sysroot` compiles only the freestanding corpus — 77 of 275 files — because everything including `<stdio.h>` dies. Pointing the sweep at `vendor/gentoo-stage3-<arch>-glibc` (or `vendor/winlibs-mingw-w64-16.1.0-ucrt-{x86_64,i686}` for PE) takes it to 235-261 of 275, comparable to x86_64's own 268. Any future per-target census that reports a suspiciously small `fn=` is missing its sysroot, not missing coverage.

#### State — all targets

- Journal state as of 2026-07-31. Compile-time gate `MCC_JOURNAL_HOOKS` now lives in **`src/mcc.h`**, not `mccgen.c` — it had to move so `mccasm.c` sees it, since that file is a separate TU under `MCC_SINGLE_SOURCE=OFF` and the `ast-journal-parity` cells do not require single-source; leaving it in `mccgen.c` would have made the multisource build silently journal less inline asm than the amalgamated one and fail its own honesty ratchet. Default on where `MCC_CONFIG_OPTIMIZER` and x86_64-or-arm64-or-i386-or-riscv64 hold, `-DMCC_JOURNAL_HOOKS=0` compiles it out; runtime gate `MCC_JOURNAL=1`. Per-primitive predicates stay in `mccgen.c` and now include `MCC_JRN_HAVE_CVT_SXTW`, `MCC_JRN_HAVE_XFERRET`, `MCC_JRN_HAVE_ROUND`, `MCC_JRN_HAVE_COPYSIGN`, `MCC_JRN_HAVE_GFUNC_RETURN`, `MCC_JRN_HAVE_VA_START`, `MCC_JRN_HAVE_VA_ARG`, `MCC_JRN_HAVE_VLA_RESULT`. Caveat for the next widening: `gen_round` and `gen_copysign` are declared **unguarded** in `mcc.h` but are not defined by every backend (round is x86_64+arm64, copysign adds riscv64), so the "mirror the declaration guards exactly" rule does not work for them — mirror the *definitions* or the link fails, which is exactly how i386 first broke. The op-boundary fixup rate is 3.88% (30837 of 794742 ops, residual topdiff 12039 + retype 11661 + pop 5293) but `cleanup.c` supplies 657017 of those ops at 0.07% and flatters the headline — excluding it the rate is 22.08%; 31.0% of bodies (351 of 1133) still hold a register live below the top two stack slots; `VT_CMP`/`VT_JMP` are unmodelled; and interchange/fusion/tiling/IVSR are tree surgery with no path over a linear form. `[jrn-regdiff]` is 0 across the sweep — `ast_pinned_regs` really is `gv`'s only non-derivable input.
- Model depth is ratcheted as of 2026-07-31 and is no longer the one axis with no regression guard. `verify_ratchet.cmake` now accumulates `fix=` and `deep=` (it already parsed them and threw them away) and compares them against `tests/ast/journal-baseline/<key>.depth.txt`, written by the same `JREGEN=1` flow. It hard-fails on any increase, prints an IMPROVED line and passes when either falls, and skips silently when no ceiling is banked for the target. Verified as a live instrument in both directions, not a dead one. x86_64-linux is banked at `fix=31003 deep=11021` over 795697 ops, identical at `-O1`/`-O2`/`-O3` — which is independent confirmation that the journal statistics really are `-O`-invariant and one ceiling per target is correct. No ceiling is banked for arm64-darwin or x86_64-win32 yet; they need an on-target JREGEN like the honesty files.
- Parity and honesty are decided by `ast-journal-parity-{O1,O2,O3}`, not by a hand sweep. The column extends `tests/ast/verify_ratchet.cmake`'s existing sweep with `MCC_JOURNAL=1` (5% slower, no second compile), hard-fails any function the tree reproduces byte-faithfully that the journal does not, counts the converse as the win, and prints `tree faithful N, journal faithful M, delta K` every run — currently delta 142, after excluding the 35 bodies both systems call vacuous. It also ratchets honesty against `tests/ast/journal-baseline/<key>.txt` exactly like the recorder baseline, failing on new raw blobs and on unbanked wins. `jempty` is accepted opposite a faithful tree, and opposite a tree-`empty` body it is excluded from the delta rather than counted as a journal win: the two disagree only on naming a zero-byte body. The journal statistics are byte-identical across `-O1`/`-O2`/`-O3`, so one baseline per target is correct. A hooked build with no baseline file still hard-enforces parity and the function census but then exits 77, so ctest reports the whole test as SKIP until it is regenerated on-target with `JREGEN=1`. A build without `MCC_JOURNAL_HOOKS` skips the column entirely.
- Inertness is portable and must stay green while the journal runs alongside the tree. `tests/ast/journal_inert.cmake` (ctest `ast/journal-inert`) compiles the corpus with and without `MCC_JOURNAL=1` and fails on any differing object; its hooks mode (ctest `ast/journal-inert-build`) cmp's every corpus object between the hooked mcc and a `-DMCC_JOURNAL_HOOKS=0` `mcc-nojrn`, with an anti-probe that fails outright if the hooks-off binary still journals. Both are `-P` cmake (not `.sh`) so they run on Win32 as well as POSIX; a build with no `MCC_JOURNAL_HOOKS` self-SKIPs via the probe.

#### Standing constraints — binding on every group, do not re-litigate

- Do not journal `vstore` for struct or bitfield operands. Journal replay runs from `ast_func_end`, after block scopes have popped and `sym_free` has clobbered function-local type Syms, so a restored operand's `type.ref` dangles onto the free list and the `VT_STRUCT`, bitfield and `verify_assign_cast` paths chase it — and the crash was invisible to the parity gate because the dying file silently shrank the function census, so watch `fn=` alongside the verdict counts. `jrn_vstore` admits ref-free operands only and `verify_assign_cast` early-outs under `jrn_replaying`.
- Do not journal `vpush_ref` as its own op. Replaying it re-runs `get_sym_ref`, minting a fresh `anon_sym++` and a fresh extern symbol, which changes relocation targets. Record the inner `vpushsym` with the `Sym` already created; `vset`/`vpush64`/`vpushs`/`vpushll`/`vseti`/`vpush`/`vpush_ref` all funnel through `vsetc`/`vpushi`/`vpushsym`.
- `JOP_MKPTR` must record the result type and never re-run `sym_push` at replay — a re-run mints a different Sym and `jrn_sv_eq` compares `type.ref` by pointer, the same lesson as `vpush_ref`. Writing the recorded ref at replay is safe only because every op's issue restores its own recorded vstack snapshot: the written ref is only ever pointer-compared at the next boundary, never dereferenced.
- Do not build a liveness model for the journal. `get_reg` scans exactly `vstack[0..vtop]` and nothing else, so the allocator has no liveness notion to model — the live set IS the vstack, and reconstructing it exactly re-derives allocation for free. Measured on the self-host TU: max registers ever simultaneously live is 7, int pressure never exceeds the 3-register pool, and 99.36% of `get_reg` calls find a free register without spilling. An operand-only (top-two) journal agrees with the real allocator only 94.96% of the time and no fixed window is ever sufficient — the deepest class-matching register sat 11 slots below `vtop`. The fix is exact stack-effect modelling, not live-in/live-out sets.
- `cur_switch` does not read a freed `switch_t` — investigated, no defect found, do not re-open without new evidence. `end_switch` unlinks before freeing (`cur_switch = sw->prev` then `mcc_free(sw)`) and `sw->prev` is live. An instrumented watchlist reported 81 apparent hits that were all allocator address reuse; re-run leaking instead of freeing, it reports zero. The predicate was never observed taking the wrong path in either direction. The real effect is replay-context only: journal replay runs from `ast_func_end`, where `cur_switch` no longer reflects record time, which is journal state rather than a compiler bug. Caveat: `gen_opl` is `#if MCC_PTR_SIZE == 4` so this is measured on `gen_opq`; the i386 site is covered by structural analogy only.
- Inline asm is NOT a raw-blob boundary for the journal — retired 2026-07-31, and this entry replaces the standing constraint that said it was permanent. That constraint conflated the recorder's inability with the journal's: the recorder cannot model inline asm because it would have to re-parse, but `asm_instr` (`src/mccasm.c:1460`) splits cleanly at the `skip(')')`/`save_regs(0)` boundary and the journal only ever needs the emit half. Two ops do it. `JOP_ASM` wraps `mcc_assemble_inline` and records the **post-substitution** template text, so replay re-assembles without re-parsing — `++asmgoto_n` never re-fires because the `LG.<n>` name is already baked into the recorded text and `get_asm_sym` is find-or-create, and asm-goto is the *easy* case rather than the hard one because its labels are relocation-mediated (`put_extern_sym2`, `mccasm.c:1531`) and resolved in `label_pop` *after* `ast_func_end`, so they never touch the `gjmp`/`rsym` chains. `JOP_ASMGEN` wraps the two `asm_gen_code` calls, recording `operands[]`, `clobber_regs[]`, `nb_operands`/`nb_outputs`, `is_output` and `out_reg` into the `jrn_raw` pool; `op->vt` needs no index conversion because `vstack` is `mcc_state->gen_vstack + 1`, a fixed array, so the pointers stay valid across replay. Both call sites are in `mccasm.c`, which sits *below* the `#undef` fence, so they are explicit `#ifdef MCC_JOURNAL_HOOKS` calls rather than macro interposition — the same treatment `gjmp_addr_acs` gets. The only parser hazard is that `mcc_assemble_inline` clobbers `tok`/`tokc`; `JOP_ASM`'s replay saves and restores both. The recorder's own inline-asm item is unaffected and still open.

#### Portable — every target

- **P2** Model `VT_CMP`/`VT_JMP` before attempting any transform that moves emitted code. A pending comparison lives in EFLAGS, occupies no register, can never be spilled (`save_reg_upstack` bails on `r >= VT_CONST`) and there is NO flag-liveness tracking anywhere — correctness today rests on emission order plus `vcheck_cmp()` materialising eagerly on every push/swap/rot/rev. Jump chains are absolute byte offsets threaded through the emitted displacement fields, so any insertion, deletion or motion of bytes between a link and its target silently corrupts them. Represent a chain as a symbolic label id and materialise offsets only at emission. This, not register allocation, is what blocks an editable IR, and it is the largest single item in this section.
- **P2** Shrink the 22.08% ex-`cleanup.c` fixup rate by modelling the vstack's exact stack effect below the top two slots — 351 of 1133 bodies hold a live register deeper than an operand-only model can see. The three residual buckets are topdiff 12039, retype 11661 and pop 5293; only topdiff/pop are addressed by depth, and `retype` has no triage item of its own yet, so split it before assuming the depth fix covers it. Constrained by the no-liveness-model entry above: the fix is exact stack-effect modelling.
- **P1** Journal `gcase_jumptable` (`src/mccgen.c:11944`). It is dormant in the `-O1` sweep corpus, so its raw-blob cost is unmeasured — build a switch-dense workload that reaches it and measure before modelling, per instruction 18.

#### x86_64-linux

- No open journal work. **The honesty axis is finished on this target as of 2026-07-31: 0 raw-blob functions, 0 raw ops, 0 B.** `tests/ast/journal-baseline/x86_64-linux.txt` is now an empty file and that is the correct content — every byte of every journalled body is reproduced by a modelled op, none by `memcpy`. Census: 1176 journalled, 1176 faithful (1141 jfaithful + 35 vacuous), 1141 clean of 1141 non-vacuous, delta 143 over the tree's 998, zero parity violations, identical at `-O1`/`-O2`/`-O3`. Got there from 7 dirty / 13 raw ops / 159 B by `JOP_ASM` + `JOP_ASMGEN` (see *Standing constraints*); the intermediate state after `JOP_ASM` alone was 5 functions / 85 B, because the residual was `asm_gen_code`'s direct emission on either side of the template, which is what `JOP_ASMGEN` closes. This is the reference target — regressions here are regressions in the portable group.

#### x86_64-win32 (PE, mingw)

- **P0** Root-cause the win64 `alloca` replay divergence. Found 2026-07-31; it is a *correctness* gap, not an honesty one, and it is the only thing standing between this target and a finished journal. **PE x86_64 only** — the identical sources are `jfaithful` on x86_64-linux, arm64-linux, riscv64-linux and i386-win32, so it is the win64 alloca sequence and not `alloca`/VLA in general.

  The full set is 17 bodies over `tests/exec` + `full_language.c` at `-O1`, blamed on two ops:

  | blame | count | bodies |
  |---|---|---|
  | `call@N` | 5 | `codegen/alloca_inline.c`: `big@2`, `nested@4`, `alive_after_scope@5`, `in_arg@7`, `loop_alloca@31` |
  | `vla_alloc@N` | 12 | `vla/basic.c main@14`; `vla/bug.c main@54`; `vla/continue.c test1@33 test2@14 test3@14 test4@14 test5@16`; `vla/label.c f1@9`; `vla/reuse.c main@31`; `bounds/bound_setjmp.c stack@34`; `features_c99_c11/alignas_over.c main@510`; `features_c99_c11/feature_macros.c main@166` |

  Localised to **one instruction**. Both blames converge on the shadow-space adjust that follows the alloca call: `objdump` on the real object shows `add $0x20,%rax` (`48 05 20 00 00 00`) at the point where the journal recorded the parser emitting `add rax, 0` and replay emitted `add rax, 0x32`. So the immediate is *derived at emission time* from state replay does not reproduce, not carried as a recorded constant. Reproduce it in one file with `MCC_JOURNAL=2` on `int use_vla(int n){int a[n];return a[0];}`: the trace shows `vla_alloc` spanning `ind` 34→54 with `[jrn-diff] firstdiff=35` landing exactly on that immediate, `oldlen=57 newlen=57`, and `[jrn-rel]` identical on both sides (`alloca+-4@44`), which rules out relocation drift.

  Do **not** re-derive these, all checked: it is not `gen_vla_alloc`'s `align > 16` arm (`src/arch/x86_64/x86_64-gen.c:3162-3167`), which emits a *paired* `add rax, align-1` + `and rax, -align` and the `and` is absent here; it is not symbol identity, since `external_helper_sym`→`external_global_sym` is find-or-create by token and idempotent across replay; and it is not a miscompile — journal-on and journal-off objects are byte-identical, so this costs oracle coverage only. It stayed invisible because the parity gate hard-fails only where the *tree* is faithful, and the tree fails on these bodies too, so they sat in the tolerated `_jrn_bad` bucket.
- **P1** Rebank `x86_64-win32.txt`, but only after the header item below. The banked file still carries its 24 pre-fix rows from 2026-07-30 and is stale by a wide margin: a full-corpus cross sweep puts PE64 at **zero raw blobs** over 1275 journalled bodies (1258 jfaithful + 35 vacuous, the 17-body shortfall being the alloca item above), with depth `fix=27318 deep=10579`. That measurement is real but **not bankable**, because it needed `-D__SIZEOF_INT128__=16` to get past mingw's headers, and a baseline taken in a doctored header environment silently encodes which files happened to compile. No `.depth.txt` is banked for this key either. Sequence: fix the header incompatibility, re-sweep with unmodified headers, then JREGEN both files.
- **P2** mcc targeting PE x86_64 cannot parse modern mingw headers, and this is what caps the target's corpus coverage. `_mingw.h:272` does `typedef int __int128 __attribute__((__mode__(TI)))` whenever `_WIN64 && !__SIZEOF_INT128__`; mcc-for-PE predefines `_WIN64` and `__GNUC__ 4` (check with `mcc -dM -E - </dev/null`) but not `__SIZEOF_INT128__`, because `MCC_HAVE_INT128` is `x86_64 && !PE` (`src/mcc.h:1072`). Every corpus file reaching `<stdio.h>` therefore dies with `'__int128' is not supported on this target`. Independent of the journal and older than it. i386-win32 is unaffected — the whole block is `#ifdef _WIN64`. Two honest fixes: give PE64 real `__int128` support, or stop claiming `__GNUC__ 4` on a target that cannot honour the corresponding type. Predefining `__SIZEOF_INT128__` is the one thing not to do — it asserts a capability mcc does not have and merely moves the failure into any code that then uses the type.
- Reproduction recipe for every sweep quoted in this group, so nobody re-derives the flags. `CORPUS` **must be absolute** — `verify_ratchet.cmake` calls `file(RELATIVE_PATH)` on it and a relative path fails with a bare `CMake Error ... (file)` that looks nothing like a path bug.
  ```
  W64=vendor/winlibs-mingw-w64-16.1.0-ucrt-x86_64/mingw64
  MCCW="cmake-cross/mcc-x86_64-win32 -I$W64/x86_64-w64-mingw32/include -I$W64/include"
  cmake -DMCC="<wrapper script around $MCCW>" \
        -DCORPUS=$PWD/tests/exec -DEXTRA=$PWD/tests/diff/full_language.c \
        -DBASELINE=$PWD/tests/ast/verify-baseline/x86_64-linux.txt \
        -DJBASELINE=$PWD/tests/ast/journal-baseline/x86_64-win32.txt \
        -DTMPDIR=/tmp/sw -DOPT=-O1 -DJOURNAL=1 -DJREGEN=1 \
        -P tests/ast/verify_ratchet.cmake
  ```
  `-DMCC` must be a single executable, so wrap the include flags in a one-line shim script rather than trying to pass them inline. Drop `JREGEN=1` to check against the banked files instead of rewriting them.
- Closed 2026-07-31, do not reopen: `gen_vla_result` is journalled as `JOP_VLA_RESULT` (gated `MCC_TARGET_X86_64 && MCC_TARGET_PE`; the enum slot and the `"vla_res"` name-table entry had been sitting unused since the op was first designed). Measured on the host-runnable `mcc-x86_64-win32` cross compiler: VLA bodies go `raw=3 rawb=12` → `raw=0` with journal-on/off objects byte-identical. The inline-asm residue this group used to carry is closed too, by the portable `JOP_ASM`/`JOP_ASMGEN` — those are not PE-gated and PE picks them up for free.
- Fold in while you are there: `AST_OP_VLA` replay at `src/mccast.c:6695` never calls `gen_vla_result` and has no PE branch at all, so on a PE x86_64 build any VLA body reaching AST replay drops the `mov %rax, ptr_slot` the parser emits at `src/mccgen.c:14079`. That is a latent **recorder** divergence, independent of the journal and of the alloca item above, and it will not show up in any journal column.
- **P3** MSVC-hosted cells still skip the parity and ratchet columns: the recorder/journal baselines are mingw-toolchain-specific and mcc-hosted-by-MSVC diverges. An honest skip per instruction 9, and the baseline-free inertness columns do run there — reopen only if an MSVC-hosted baseline is wanted as its own key.
- State. The banked `x86_64-win32.txt` is the 2026-07-30 file: 24 raw-blob functions, `ast-journal-parity-{O1,O2,O3}` green against it on-target, stable across `-O1`/`-O2`/`-O3`. Everything above says why it is now stale. It is a different file from `x86_64-linux.txt` **by construction, not by drift**, and it will stay different: PE journals a reduced op set — no `JOP_XFERRET` (`arch_transfer_ret_regs` is `X86_64 && !PE`-guarded end to end, and `MCC_JRN_HAVE_XFERRET` mirrors that), no `JOP_MULWIDEN` (`MCC_HAVE_INT128` is x86_64-non-PE), `JOP_X87POP` present, and `JOP_VLA_RESULT` present *only* here. So expect the op histogram to differ from linux's even once both honesty files are empty. `mcc_nojrn` builds on the native win32 x86_64 build (its guard only excludes cross builds), so both inertness columns run there.
- What a green on-target run should look like once the two items above land: 1275-ish journalled bodies, all `jfaithful` bar the vacuous, **zero** raw-blob rows, and a `.depth.txt` near `fix=27318 deep=10579`. Treat a materially smaller `fn=` as a missing-sysroot artifact rather than a real census — see the sysroot note in the section header.

#### x86_64-darwin

- **P0** Generate the journal honesty baseline and depth ceiling; no `x86_64-darwin.txt` exists, so every journal column SKIPs here and the target is measuring nothing at all. **Blocked on native x86_64-macOS hardware** — Rosetta cannot produce it, the sweep needs a native run. Same blocker as the x86_64-darwin recorder baseline in *Recorder fidelity*; do both in one sitting on the machine, and note the arm64-darwin JREGEN wants the same trip.
- This is now the **only** target family the repo cannot reach by emulation. Every other gap closed in the 2026-07-31 sweeps was closed with vendored sysroots plus `qemu-user`, or plain cross compilation; there is no equivalent for macOS, and Docker is no help either — host binfmt is registered `flags: OC` with no `F`, so foreign-arch containers fail with `exec /usr/bin/uname: no such file or directory` regardless of platform.
- Expect it to behave like x86_64-linux rather than like arm64-darwin: same `MCC_TARGET_X86_64` op set, so `JOP_XFERRET` and `JOP_MULWIDEN` are both present (unlike PE), and no arm64 ABI emitters to model. x86_64-linux finishes at zero raw blobs, so a clean Mach-O run should too; anything left over is Mach-O-specific and worth an item of its own.

#### arm64 (darwin, linux)

- **P0** JREGEN `arm64-darwin.txt` on native Apple hardware — the only arm64 item left, and pure execution: no modelling remains. The banked file is the 2026-07-30 census, **103 raw-blob functions / 141 raw ops / 1504 B**, and every one of those rows is now covered by an op that has landed, so the honesty ratchet will fail on-target with 103 `baseline raw blobs now CLEAN (regenerate to bank the win)` lines until someone runs it. No `.depth.txt` exists for this key either; the same `JREGEN=1` run writes both.

  What the 103 rows were, and what now covers each:

  | group | rows | raw ops | bytes | now covered by |
  |---|---|---|---|---|
  | `arch/arm64_extasm.c` (inline asm) | 33 | 38 | 624 | `JOP_ASM` + `JOP_ASMGEN` |
  | `arch/arm64_encoding.c` (inline asm) | 4 | 7 | 64 | `JOP_ASM` + `JOP_ASMGEN` |
  | `arch/arm64.c` ABI + `myprintf` | 32 | 46 | 468 | `JOP_RETVAL`, `JOP_VA_START`/`JOP_VA_ARG` |
  | `structs_unions/*` | 22 | 35 | 216 | `JOP_RETVAL` |
  | `cleanup.c` / complex / variadic / misc | 12 | 15 | 132 | `JOP_RETVAL`, va ops |

  Expected result: **1 row**, `arch/arm64.c myprintf raw=14 rawb=688` — the `va_arg(ap, struct s7)` sites `JOP_VA_ARG` declines by design. arm64-linux, swept both cross and native, lands on exactly that row and nothing else, so treat anything beyond it as a genuine Mach-O-specific finding rather than expected residue. Two things could legitimately differ on darwin and are worth checking rather than assuming: Mach-O relocation handling inside `ast_reloc_range_equiv`, and whether darwin's corpus reaches the same `myprintf` path at all.

  Run it exactly like the other targets — `CORPUS` **must be absolute** or `file(RELATIVE_PATH)` fails with an opaque `CMake Error ... (file)`:
  ```
  cmake -DMCC=$PWD/cmake-build/mcc -DCORPUS=$PWD/tests/exec \
        -DEXTRA=$PWD/tests/diff/full_language.c \
        -DBASELINE=$PWD/tests/ast/verify-baseline/arm64-darwin.txt \
        -DJBASELINE=$PWD/tests/ast/journal-baseline/arm64-darwin.txt \
        -DTMPDIR=/tmp/sw -DOPT=-O1 -DJOURNAL=1 -DJREGEN=1 \
        -P tests/ast/verify_ratchet.cmake
  ```
  Then re-run without `JREGEN=1` and confirm both `journal honesty matches baseline — OK` and `journal model depth matches ceiling — OK`. Bank the depth ceiling from this native run and not from any cross substitute: honesty is cross-invariant but depth is not, and it reads native+1 on 64-bit non-x86 targets (see the section header).
- **P1** While on the machine, do `x86_64-darwin` in the same sitting — it is listed in its own group below and has *no* journal baseline at all, so every column SKIPs there. Rosetta cannot produce it; it needs a native x86_64 macOS run, which is the one target family this repo still cannot reach by emulation. Both the recorder and journal baselines for that key are missing, so it is two JREGENs, not one.
- ~~generate `arm64-linux.txt`~~ DONE 2026-07-31 — banked at 1 raw-blob function (`arch/arm64.c myprintf`, 14 raw ops / 688 B, all struct `va_arg`) with ceiling `fix=30523 deep=10921`, over 1202 journalled bodies all faithful and zero parity violations. Validated twice: host cross, and a native arm64 mcc executed under `qemu-aarch64`; the honesty file is byte-identical between them.
- The arm64 census is retired by three ops, landed 2026-07-31. `JOP_RETVAL` wraps `gfunc_return` — arm64 is the only target where it is a backend override (`src/mcc.h:1867`, `src/arch/arm64/arm64-gen.c:1722`) rather than a `static` in `mccgen.c` built from already-journalled primitives, which is why arm64 needed it and nothing else did; the gate is `MCC_JRN_HAVE_GFUNC_RETURN` = `MCC_TARGET_ARM64`, mirroring the declaration guard. It needs no recorded payload: every call site passes `&func_vt`, a global, and the AST replay path at `src/mccast.c:6950` already calls `gfunc_return(&func_vt)` at replay time, which is what proves the global is live there. `JOP_VA_START` (arm64+riscv64) and `JOP_VA_ARG` (arm64) close the variadic tail. `JOP_VA_ARG` declines struct and array operands and lets them fall through to a raw gap, mirroring `jrn_vstore`'s ref-free admission — the recorded `CType`'s `.ref` would dangle onto the free list for a struct, which is the standing-constraint hazard; pointers and scalars are admitted and are safe because nothing dereferences the ref. Measured A/B on native arm64 cross: the `fr_s*`/`fr_hfa*` emitter family goes 13 dirty functions / 13 raw ops / 116 B → **0**, with `fix` 74→58 and `deep` 15→8 and objects byte-identical journal-on vs off; the variadic family goes 5 dirty / 10 raw ops / 380 B → 1 dirty / 1 raw op / 40 B, the residual being the declined struct `va_arg`. `fr_s17` never appeared in the census in the first place because size>16 routes to `gfunc_return`'s indirect `a[0]==1` arm, which is built from `vstore` and was already journalled.
- Note the prose this replaces was wrong on file attribution: the ~66 non-asm dirty functions are **not** all in `arch/arm64.c` (only 32 are). The rest are spread over `structs_unions/` (22), `features_c99_c11/cleanup.c` (5), the two complex files (2), the variadic files (3) and `random_stuff.c`/`scopes.c` (2). Same emitter, same fix — but do not go looking for 66 rows in one file.
- State: hooks are **done**. The gate (now in `src/mcc.h`, moved there so `mccasm.c` sees it too) admits `MCC_TARGET_ARM64`; primitives arm64 does not declare compile out behind predicates mirroring the `mcc.h` declaration guards — `MCC_JRN_HAVE_X86_PRIMS` (`gen_bswap`/`gen_bitscan`/`gen_signbit`/`gen_ffs`/the three `gen_atomic_*`/`gen_cvt_trunc32`/`gen_reg_addi`), `MCC_JRN_HAVE_STRUCT_COPY`, and `MCC_JRN_HAVE_ROUND`/`MCC_JRN_HAVE_COPYSIGN` added 2026-07-31; `mcc_nojrn`'s CPU gate admits arm64, so `ast/journal-inert-build` runs instead of skipping. Nothing is lost to the compiled-out primitives: bytes emitted off a journalled path are captured verbatim by the `JOP_RAW` gap in `jrn_gap()`.
- Last on-target measurement, 2026-07-30 and now superseded on the honesty axis: arm64-darwin 1237 of 1237 journalled bodies faithful at `-O1`/`-O2`/`-O3` against the tree's 1018 — delta +184 under the post-`5d18df22` accounting that stops counting the 35 vacuous bodies as a journal win (it read +219 before), zero parity violations. The faithful/parity half of that should be unchanged by the 2026-07-31 ops; only the 103-row honesty census is stale. arm64-linux, for comparison, reads 1202 journalled / 1202 faithful / delta 180 / 1 raw-blob row.

#### i386

- ~~Parenthesize `gen_opl`~~, ~~admit i386 to the gate~~ — both DONE 2026-07-31. `src/mccgen.c:2634` is now `static void (gen_opl)(int op)` (it was the only hooked name with a definition inside the macro window that was not yet parenthesized, because it is the only 32-bit-only one), and the gate admits i386 and riscv64. `gen_x87_pop`'s i386 twin now compiles and executes on the i386 legs, so that item is closed by measurement rather than by inspection.
- No open journal work. `i386-linux.txt` is banked **empty** and `i386-win32.txt` likewise: both are at zero raw blobs over the full corpus (1171 and 1135 journalled, all faithful, zero parity violations). i386-linux was validated twice — host cross and a native i386 mcc under `qemu-i386` — and is the one target where the cross and native depth ceilings agree exactly. i386-win32 is cross-only so far; its ceiling (`fix=26170 deep=10736`) may want a +1 rebank if a native mingw i686 cell ever runs it, per the pattern in the section header. The `MCC_MINGW_ARCH=i686` cell is still `experimental: 1` and non-gating (`tools/ci.c:453-459`); promoting it is now a CI decision, not a blocked measurement.

#### riscv64

- ~~Widen `MCC_JOURNAL_HOOKS` to riscv64~~ DONE 2026-07-31. riscv64 now has an oracle, and over the freestanding subset it is the cleanest target measured: **472 journalled, 472 faithful, 0 parity violations, 0 dirty functions, 0 raw ops, 0 B.**
- **P0** Fix the `arch_transfer_ret_regs` assertion that aborts riscv64 on `tests/exec/structs_unions/struct_byval.c`: `src/arch/riscv64/riscv64-gen.c:1010`, `assert(vtop->r == (VT_LOCAL | VT_LVAL))`. It is a **pre-existing riscv64 codegen bug, not a journal bug** — it reproduces with `MCC_JOURNAL_HOOKS` compiled out and with the runtime gate off, and it reproduces on a *native* riscv64 mcc under qemu as well as on the host cross, so it is neither a hooks artifact nor a cross artifact. It was invisible until 2026-07-31 only because no sysroot meant that file never compiled for riscv64 at all. It blocks the journal cell outright: the abort truncates the file mid-sweep and the journal ends up with one more row than the recorder (1173 vs 1172), which trips the census-shrink guard before honesty is ever compared. `riscv64-linux.txt` is banked **empty** and correct, measured with that one file excluded — 1149 journalled, 1149 faithful, zero raw blobs — but the cell cannot go green until this is fixed.
- **P1** Use the new oracle on the 15 functions that are unfaithful *only* on riscv64 (the characterisation item in *Recorder fidelity*). That was the concrete payoff of widening and it is now unblocked — `MCC_JOURNAL_ORACLE` will name the in-flight op for each.

### Optimization levels and the `-O4` search

- Re-decide whether the `-O4` search's exploratory workers should be silent for *errors* too, and whether `-O4` should have its own ctest `-O` axis. The doubled-diagnostic defect is fixed: `mcc_superopt_search` and `mcc_superopt_perfn` spawn one child per candidate configuration and every child re-parsed the TU and re-emitted every diagnostic onto the shared stderr, so a cold-cache `-O4` compile printed each warning 1339 times (a warm `~/.cache/mcc` hid it at 2 — measure this one with a fresh `XDG_CACHE_HOME` per side or the cache decides the answer). The exploratory children now carry `-w` and only the final authoritative spawn, whose object becomes the artifact, is unsuppressed; `cli/o4_search_does_not_repeat_diagnostics` locks `O1=1 O4=1`. Left open: a hard error still prints once per child before the search gives up, and `-O4` remains off the ctest `-O` axis, which is why this stood — the whole `-O6` exec sweep is 146 s at `-j8`, so a bounded cell is affordable if it is wanted.
- Decide whether the out-of-process superopt driver runs at `-O4` at all. On the five-kernel bench it is the best cell on zero kernels, ties on two, and loses 27% on spectral and 25% on matmul; driver-off is greater than or equal to driver-on everywhere, and buys 8-22% `.text` for it. Making it opt-in and shipping the in-process result is a semantic change to `-O4`, so it wants a decision.
- Replace the `text + spills * 48` scoring proxy. It is anti-correlated with speed on two of five kernels. The weight is mid-plateau, so this is the objective, not the tuning.
- Then floor the graft budget. `so_setenv_cfg` writes `inl ? limit : 0u` and `limit = gate >> 4` is 0 for every gate below 16, so every gate with the inline bit clear forces zero grafts — the whole spectral regression on the driver-off path. Both a narrow and an unconditional version fire and are byte-identical on the driver-ON path, so this cannot be measured until the two items above land.
- Make `-O4` a reproducible build. Same source, same flags, matmul differs 25% in instructions between a 577-entry `~/.cache/mcc` and a fresh `XDG_CACHE_HOME`, deterministically within each state.
- Make a memo hit make `-O4` faster. Eight successive compiles of an unchanged input each spend the full 4-second budget to reproduce a byte-identical binary. Add an early-out on a fully-exhausted checkpoint, and bank the `-O3` configuration in the checkpoint record so a hit hands back a real result.
- Say what `-O4` optimises in the help text, or make the axis selectable. `-Os` already exists for size, so users expect `-O4` to be fastest.
- Explain why the static cost model prefers a larger emit at a higher budget (one extra second of search costs 144 bytes at identical runtime, between `-O4` and `-O5`), and why the emit-size objective cannot reach the smaller configuration at all. The driver's accept logic is not at fault.
- Sandbox `so_run_score` or make it explicit-opt-in per invocation before considering `MCC_AST_JITSCORE` as a default. It builds `mcc -run <src>` and executes the user's program three times with no argv at compile time. Runtime scoring is worth 25% of instructions on matmul at nil compile cost.
- Fix the `-O1` `atoi` out-of-line copy. `__OPTIMIZE__` from `-O1` up turns on glibc's `__USE_EXTERN_INLINES`, mcc turns the `gnu_inline` wrapper into `VT_STATIC | VT_INLINE` and emits it out of line, so an un-inlined call reaches a local copy; gcc emits no out-of-line definition. Removes ~52 bytes of dead `.text` from every `-O1`+ TU that includes glibc headers, but it changes identifier binding, so it needs its own validation run.
- `MCC_AST_XMM_HI` is inert for two independent reasons, both measured 2026-07-31 — do not re-open as a gate bug, and do not expect FP promotion to fix it. The gate itself works: with it on, `reg_classes[XMM8..15]` carries `MCC_RC_FLOAT` for all eight. (1) It only affects `get_reg(MCC_RC_FLOAT)`, the SCRATCH allocator, and scratch FP demand never exceeds ONE — instrumenting `get_reg` gives max-simultaneously-live 1 on `fp_builtins.c` (19 allocations), on a `fabs`/`sqrt` accumulation loop (24), and on a loop written to hold 12 doubles live in the source (111). (2) Promotion, the one thing that does use many FP registers, never consults `reg_classes` at all: `ast_promo_xmm_leaf[]` already names 24..31 (the high eight) and replay assigns them by writing `sv.r = preg` directly in the `AST_Ref` case, so the class bit is bypassed. Confirmed by experiment: on an FP-heavy LEAF, `MCC_AST_PROMO_LEAF_XMM` `=0` vs `=1` changes bytes — promotion fires and does use the high registers — while `MCC_AST_XMM_HI` `=0` vs `=1` with promotion ON changes nothing. So the gate would only ever pay if several FP TEMPORARIES were live at once inside one expression, which is a codegen-shape question, not a register-pool one. The arm64 `v8`-`v15` / riscv64 `fs0`-`fs11` callee-saved pool items are about PROMOTION pools and are unaffected by any of this.
- Widen `MCC_AST_REGDISP`: it fires on one of five kernels, and `ast_regdisp_env`'s `!(vtop->r & (VT_SYM | VT_LVAL | VT_MUSTBOUND | VT_BOUNDED))` guard is a wide exclusion. Find why it misses the other four hot loops.
- Replace `ast_now_ms`'s `clock()` with a monotonic high-resolution source (`CLOCK_PROCESS_CPUTIME_ID`, `CLOCK_MONOTONIC` fallback, `uint64_t` nanoseconds), keeping a `clock()` fallback because the asttool harness includes `mccast.c` without the mcchost timer. Coarse resolution makes the stop point land on a different candidate run to run — a dense switch's jump-table dispatch flips between identical runs. Pair it with count-based ordering; precision alone does not restore the AOT==JIT invariant.

### Codegen

- Add auto-vectorization. Packed SIMD exists only in the assembler/disassembler tables; codegen emits zero packed ops and there is no `VT_VECTOR` or intrinsic path. First the backend (x86 `mulpd`/`addpd`/`movupd`/`unpcklpd`, arm64 `fmul`/`fadd .2d`, `ld1`/`st1`, `zip1/2`, a vector register class, a vector AST node the re-emit path lowers), then a straight-line SLP packer as a new AST strategy. Decide `VT_VECTOR` versus an emit-only pack-group annotation, and whether v1 is x86-only. Worth ~1.9x of matmul's gap; the other ~5x is scalar inefficiency.
- Design and implement an aggressive inline-and-register strategy, and start it from an enumerated table of directions rather than a single guess — the design space is wide and the first choice constrains the rest. Two halves. (a) **Inline past the expression-only whitelist.** `ast_inline_node_allowed` admits only value-shaped nodes (`Literal`/`Ref`/`Load`/`Convert`/`Binary`, an address/member `Unary`, a ternary `If`) and `ast_inline_graftable` wants a `BasicBlock` root reaching an `AST_Return`, so a callee carrying a `Jump`, a `Store` or a nested call is uninlinable today. Lower a control-flow callee inline as labels plus jumps to a single join point (`AST_Jump` op 4/5 already model label/goto, `ast_rp_label_get` already allocates replay labels), and promote the callee's frame slots into registers once inlined rather than materializing the caller's stack — the inline site knows the callee's whole live range, which is exactly the information `ast_plan_promotion` lacks at a call boundary. Depends on the `AST_Invoke` live-range split above, and on dead-static elimination for the `MCC_AST_INLINE_STATIC` flip. (b) **Fill idle register slots by rewriting the arithmetic.** When the colouring leaves registers free, rewrite formulas into the representation that uses them: hoist a loop-invariant divisor to a reciprocal multiply, swap `mul`/`div` where the result is bit-identical, and choose an integer or FP register file per expression rather than per type. Hard constraint: `x/c -> x*(1/c)` is bit-identical only when `1/c` is exactly representable (`c` a power of two); everything else changes results and belongs behind fast-math semantics, not behind a register-pressure heuristic — the differential fuzz builds its reference with `-ffp-contract=off -fno-fast-math` and will catch it. Integer `div` already has its mul-high path in `ast_divmagic_*`. Judge both halves on instructions retired, not emitted size: inlining a control-flow callee grows the function by construction.
- Make the `-O4` search preserve register promotion. With gates on, `advance()` has 7 stack memops at `-O2` and 18 at `-O4` for a nearly identical instruction count, because `ast_cost_score` cannot see spills and rates the two orders equivalent. Adding FP registers does not fix it. Same "eligible but the search does not select it" mechanism that keeps `ivsr`, `licm` and the math-inline from firing on `advance()`.
- Split each promotion candidate's live range at `AST_Invoke` boundaries. `has_call` is function-global, so a caller-saved FP register is unusable even for a value that never spans a call. The interval and colouring machinery already exists.
- Add the callee-saved FP pool: arm64 `v8`-`v15`, riscv64 `fs0`-`fs11`. Both backends model only 8 FP registers, all caller-saved, and their `fltr`/`freg` assert `r <= F(7)`, so there is no register id for the high half. Needs the FP register file, `reg_classes`, prolog/epilog ABI save-restore and `get_reg`'s scratch discipline extended. x86_64 SysV has no callee-saved xmm.
- Port `MCC_AST_REGDISP` to arm64, riscv64, i386 and arm: `load`/`store`/`gaddrof` recognition, a per-arch `gen_reg_addi` (LEA-equivalent, flags must survive), and lifting the `#ifdef MCC_TARGET_X86_64` on `ast_regdisp_env`. Then fold array indexing (`a[i].f` and the array-member case the x86_64 slice excludes).
- Promote a value-producing `i++`. The statement form is done; the expression form needs the old value preserved across the update.
- Admit a loop-invariant FP-constant or global-address load to the preheader hoist. `ast_ltemp_scan`/`_materialize` is integer-only via `ast_ident_intt`; gate on `ast_cost_spill`'s FP budget to respect pressure.
- Give the `-O4` search a promotion actuator: `MCC_AST_PROMO_LEAF_CALLEE`/`_LEAF_XMM` plus a bounded `MCC_AST_PROMO_POOL` as `so_axes[]` entries decoded from the budget radix, per-function via a spare `ast_fncfg_parse` bit. A bad pool choice is a miscompile, not a size regression, so restrict the axis to ABI-validated pool sizes and gate acceptance on an exec diff against the seed.
- Investigate whether arm64 should use PC-relative `adr`+`add` for local statics the way the PE path does, instead of always going through the GOT.
- Fix `MCC_AST_DIVMAGIC`'s non-local x86_64 mul-high register-allocation bug: with it on, stage 2 SIGSEGVs recompiling `src/mcc.c` through runaway self-recursion in a function containing no mul or div, while quotients are correct everywhere and i386/arm64 self-host cleanly. It blocks the default-on flip at `-O2`.
- Implement the optimal single-multiply div-magic form for the signed and `a == 1` cases; it needs real temp materialization. Reordering DIVMAGIC before CSE does not merge the duplicated mul-high subtrees.
- Add a per-backend `mulh` primitive (x86_64 `mulq`, arm64 `umulh`, riscv `mulhu`, i386 helper) so 64-bit div-magic becomes possible; the AST cannot express the high half of a 64×64 product.
- Add i386 `copysign` and `floor`/`ceil`/`trunc` (x87 needs a memory round-trip and rounding-control juggling).
- Implement the errno-preserving sqrt inline for unknown-sign arguments under default `-fmath-errno`: a per-arch `gen_sqrt_errno` interleaving `gtst`/`gjmp`/`gsym` with a `gfunc_call` and a `move_reg` merge, keeping the argument live across the call. It cannot be an AST rewrite — the optimizer is a CFG with no expression-level ternary. Only reachable by heavy errno+value fuzz.
- Inline the 128-bit overflow builtins (`__mcc_addo_ti`, `__mcc_mulo_ti`): `add`/`adc` plus `seto` over the register pair `gen_opq` already uses. Nothing may touch flags between the low-half and high-half instructions, and the exported runtime symbols are thin wrappers over static `_impl` functions.
- Implement native variable-count 128-bit shifts. The primitives exist (`gjmp_cond`/`gsym`, `shld`/`shrd`); the difficulty is a value-producing runtime branch on `count >= 64` whose arms must leave the result in the same registers, or a branchless cmov formulation around x86's 6-bit count mask. TDD against a 0..127 fuzz corpus.
- Extend `__int128` to arm64 and riscv64, or stop building `runtime/lib/int128.c` for them — they ship ~40 helper routines no front-end can call. Check the 16-byte alignment / even-register-start caveat when the type arrives. x86_64-PE needs its own ABI path because Win64 passes a 16-byte aggregate indirectly.
- Fold `/` and `%` of two 128-bit constants, and float↔128-bit conversion of a constant, so they can appear in a static initialiser. The float case needs a soft-float path or accepts double rounding through 80-bit `long double`.
- Support 128-bit bitfields, or leave them rejected: `BIT_POS`/`BIT_SIZE` are 6-bit fields and widening them touches every bitfield path.
- Add 128-bit `clz`/`ctz`/`popcount`, once a `__builtin_clzg`-style wide builtin exists.
- Decide the four complex helpers' ABI: mcc's are `void __mcc_cmul(T *res, T a, T b, T c, T d)`, libgcc's return the pair by value. Adopting libgcc's shape is a call-shape change — `gen_complex_call` builds its own call and would have to consume the return. Needs its own differential test for the NaN/infinity fixups.
- Implement dead-static elimination, then flip `MCC_AST_INLINE_STATIC`. mcc has none, so an in-place `static inline` is emitted whether or not anything calls it and inlining it is strictly additive (+755 bytes on mcc's own TU). `-ffunction-sections`/`-fdata-sections` plus an external `--gc-sections` already collects the orphan and makes the combination smaller than the default; the honest next step is internal section GC, now a section drop rather than byte surgery.
- Measure `AST_Poison` density per function and per TU across the self-compile and exec corpus, bucketed by planter (narrow-lift, DSE, SCCP branch-fold, jump-thread, `ast_bf_drop`), then decide between rewriting in place, an arena compaction pass before serialize, and skip-index querying. Every tombstone is walked by later passes and serialized as a live node. Node-index stability is the constraint.
- Model large direct-copy struct-by-value args so arm and i386 reach the inline capture path; their >16B by-value is a direct stack copy, not the hidden-pointer class.
- Name rodata literals from a literal-local stable key (content hash or per-section emission index) instead of the shared `anon_sym` counter, then re-include the three exec files excluded from object diffs. Renames every rodata literal symbol.

### Slice cache and the whole-AST JIT

- Align JIT and AOT arena normalization so a JIT-written proven ident matches an AOT-shaped ident. Cross-mode proven hits are not guaranteed in practice today, and this is what makes graduation only unit-proven.
- Build a per-slice optimizer that finds something the from-scratch downstream re-emit would not independently rediscover (per-slice gate search, strength reduction under a gate the downstream mask omits). Today folding a captured slice finds nothing, so the promotion gate correctly rejects and the splice branch is never exercised live.
- Add per-slice `mccjit_bench_pair` micro-bench tuples; per-slice currently uses the static gate while whole-function benching lives in `mccjit_lazy_search`.
- Chain a slice's hash into enclosing scopes, callers and KGC slots so re-optimizing one propagates upward and re-patches every dependent.
- Wire the splice primitive into the AOT in-place emit path. `ast_func_end`'s replay re-emits from three whole-function ordered cursors recorded at capture, and splicing a differently-shaped subtree desyncs them.
- Ship one arena per module instead of per-function intents, keeping a function index as a view over it.
- Define slice identity purely from the boundary — the values entering and leaving the sub-slice with widths normalized, plus internal node shape. No `fn_name`, no parameter ordinal, no source location.
- Build the Optimization Reconciler: re-wire a replacement slice's ins and outs so entry and exit stay coherent with the surrounding graph — register/stack locations, live ranges, the splice-point contract on both sides. It must handle fewer or more live-ins, a computation sunk past the exit, and a splice at a call site whose caller frame is already laid out. `ast_slice_splice` is node-identity-stable but does not reconcile a differently-shaped boundary.
- Key dispatch slots by slice identity rather than `__mccjit_slot_<fn>`, so a promoted slice inside `main` — or inside a function today's eligibility predicate rejects — is addressable.
- Add the scalarize↔aggregate layout axis: split a compound local whose fields are accessed independently into N scalars, and coalesce scalars with correlated liveness into one slot, both exposed to the search. Gates: no address-taken or escape on a scalarized aggregate, no aliasing or punning across split or fused members, live-range compatibility for a pack. Needs the ins/outs hash and the Reconciler.
- Flip slice width-normalization on. It takes cross-triple identity from 6 shared / 20 i386-only to 26 / 0 on the measured case, with negative controls confirming genuine 4- versus 8-byte operations stay distinct. Changing a slice ident changes which gate config warm-starts, so it changes codegen.
- Reduce the JIT handle table and wire format, each change proved by a round-trip fidelity test *and* a measured size or cost assertion written first: drop `handle_raw` from the wire (8 dead bytes per handle, never read back), presence-flag `token_v` (only consulted when there is no name), de-quadratic `mccjit_handles_intern` with a pointer→id index, presence-mask and LEB128 the always-8-byte per-node payload, and structurally dedup type records behind a proof that the rebuilt graph is unchanged. Any wire change bumps `MCCJIT_INTENT_FORMAT` and stale blobs must fail closed.
- Build a bounded UB-sound compile-time loop interpreter in `ast_eval_slice` and fold no-escape-pure slices to constants into the submitted arena.
- Split the KGC key into code-hash and data-hash, track poison per pair, feed poison as a search input, unify KGC and poison into one LFU-bounded {good,bad,unknown} set, and persist poison to the mmap'd cache under the opt-in flag.
- Add a secondary index over data shape alone (element dtype/width, layout/stride/alignment, value ranges, ABI class, loop trip shape) so a shape hit can offer another code's cached codegen as a speculative candidate — benchmarked and kept only if it wins on the JIT path, gated on the cost model and static-range soundness on the AOT path, poisoned per (code-hash, shape) on a loss.
- Add a switch-table cover strategy row: dense to jump table, sparse to perfect hash.
- Build a parser-less re-emit-only engine slice plus `-ffunction-sections`/`--gc-sections` for the ~800 KB Tier-B embed, and reconcile CMake so plain `-lmcc` prefers `libmcc-static.a`.
- Add a general value-range narrowing fold; it needs the VLat consumer.
- Add small struct-by-value marshalling and promote `classify_x86_64_arg` to `ST_FUNC`.
- Wire QSBR reclamation into the swap path with quiescent points at function entry and loop back-edges.
- Add the nop-padded patchable-prologue in-place code patch, the int3/trap row and the dual-map page-flip patch row.
- Add a non-ABI register calling-convention kernel with a boundary ABI harness, an inline-versus-shim search axis, and float plus memory-boundary slices.
- Flip `MCC_AST_JIT_EVAL_GATE` on after three clean self-host and fuzz soaks, and extend its oracle to statement-level control flow.
- Build the AOT-static sink scorer: deterministic cost/emit-size model, static ranges from AstVLat, gain-ordered time-budgeted scheduling.
- Add data-to-code substitution via a synthetic `.init_array` ctor.
- Fix `mccjit_bench_pair`'s common-mode contention with a paired per-round ratio plus median. It changes production promotion semantics, so it needs its own soak.
- Finish the `mccjit_embed.c` split: `mccjit_stub.c` (arch-conditional hand-emit), `mccjit_pool.c`, `mccjit_recompile.c`, `mccjit_boot.c`, `mccjit_selftest.c`. Verify the multisource preset after each.
- Route the search workers through the shared `mccjit_pool` instead of per-search `pthread_create`; `_Thread_local` arenas leak at thread exit, so the pool needs a fixed worker set or explicit teardown. Then wire the capture→score→select stats through the joined workers, and make the threaded path default once Linux and x86_64 CI validate the LE-slab TLS path and the x86_64 trampoline. If TLS cost bites, move the epoch-keyed caches into `AstArena` instead.

### Strategy scheduler

- Add a per-strategy and per-gate existence ledger orthogonal to the magnitude score: for each technique record whether it ever changed the AST at function, TU and whole-corpus scope, and surface never-fired techniques as an explicit report bucket. Today a static-cost benefit of 0 silently sinks a pass to the bottom of the round-robin, where a pass that would fire somewhere looks identical to one that fires nowhere.
- Fold a measured runtime signal into ROI so strength-reduction-class passes stop scoring 0 on the static cost model.
- Get `ivsr`, `licm` and promote to fire on `advance()`; it reaches the scheduler but the loop passes still are not selected.
- Wire ROI into the default `-O4` path; it currently needs `MCC_AST_SEARCH=1`, which the out-of-process superopt does not set on its workers.
- Fix the emit-time value-axis full-state save/restore (promotion-plan arrays, `nocode_wanted`, register-allocator and `vtop` state), then enable the inline and promote search axes.
- Migrate the memos to `ComboMemo` with disk backing and a bulk-value compression mode, subsuming the out-of-process superopt.
- Build the settled runner architecture: sequence-with-repetition candidates, DFS and BFS as selectable root runners and as strategy rows, runner identity in the memo key, one unified estimator interface where score predicts quality and forecast predicts eval cost.
- Resolve REASSOC pass-order confluence so the reassociated form is canonical.
- Wire `MCC_AST_COST` as the search and budget scorer, then flip it on.
- Flip the rest of the search family (`_EMITSIZE`, `_EMITISO`, `_INLINE`, `_THREADS`, `_ORDERED`, `_ORDER`) and `MCC_AST_PERFN_INPROC` after the emit-isolation prerequisite, the multi-arch golden regen, the seed soak and a self-host byte fixpoint under search-on; then decide whether to lower `MCC_AST_SEARCH`'s default below `-O4`. `MCC_AST_NO_CALLFUL` stays off — it is a search-axis constraint, not a win.
- Fix the graft-path guard that makes `MCC_AST_ARGFWD` unreachable (`do_inline`'s `!ast_inline_pass_env`, with `INLINE_PASS` default-on from `-O2`), then flip `optfire/default-argfwd`'s expectation from off to on. Widen ARGFWD past single-use.
- Flip `MCC_AST_VLAT` default-on (signed `/` and `%` with the `INT_MIN/-1` trap, `<<` value-count), `MCC_AST_NARROW_ELIM` (flow-sensitive facts plus globals), `MCC_AST_SPILL_SHARE` (general per-value spill slots, riscv64 fixpoint), arm64 `opt_promote`, `MCC_AST_OPASSIGN`, `MCC_AST_CHAINSTORE`, `MCC_AST_PROMO_ARROW`, `MCC_AST_PROMO_INCDEC`, `MCC_AST_IVSR_PTR`, `MCC_AST_RELOC_EQUIV`, arm64/riscv64 `MCC_AST_MATH_INLINE`, `MCC_AST_MATH_INLINE_PREPASS`, `MCC_AST_ROUND_INLINE`, `MCC_AST_COPYSIGN_INLINE`, `MCC_AST_MINMAX_INLINE`, `MCC_AST_FMA_INLINE`, `MCC_AST_FMOV_IMM`, `MCC_AST_XMM_HI`, `MCC_SO_SPILL_SCORE`, `MCC_SO_DEFAULT_SEED` — each after the per-gate multi-arch golden regen and tens-of-thousands-of-seeds soak on the x86_64 and arm64-native cells, plus its own remaining leg. `MCC_AST_DIVMAGIC` and `MCC_AST_ABS` wait on backend cmov/csel parity. `MCC_AST_JIT_SPLICE` stays off — it replaces the optimizer emit path and disables mode-6 dispatch.
- Re-decide the recorded `IVSR_PTR`-with-`MATH_PREP` blocker; the static memop regression that produced it does not reproduce as an instruction-count regression, and `IVSR_PTR` is worth −22.8% instructions on matmul once the loop counters are promoted.
- Replace `ast_plan_promotion`'s heuristic with the colouring allocator, fixpoint-gated and validated on native arm64 and riscv64.
- Decide promotion's arch scope: arm (armv7) is register-adequate but has a large hidden-libcall surface (`__aeabi_idiv`, 64-bit `long long`, soft-float) so it needs a broad libcall-hazard audit; i386 models only 5 registers and is out of scope.
- Work the search-vocabulary long tail: loop-nest symbolic bounds and fewer non-affine bail-outs; true 2-D tiling (strip both loops) and dependence-test precision; the remaining range/known-bits lattice; adaptive beam width and per-function scoping; hot-slice budget allocation; a `-g` hot-value cache; the post-graft window dataflow decision; the seam peephole window and its cache key; widening/fixpoint dataflow for cross-iteration merging; `switch`-arm detection; inline budgets as a value axis, more param shapes, cross-TU static callees, heuristic non-static inlining; the arena-mutating pass-subset and promotion re-emit axes; a rewrite-rule IR and instruction-level superoptimization; V-strategy variations to widen the vocabulary; the epoch hash; cross-TU LTO; separate `-O2`/`-O3` SSA drivers; a time-budgeted engine; dependency-ordered `-O1`; a broader template library; formula-family unification; graduating the disk search memo into compiled-in strategies; emit-size scoring under the tick scheduler with JIT-runtime scoring; a C11-thread pool with per-context state. Most need emit isolation first.

### Cross-arch parity

- riscv64 to Tier-4: self-host, replay-inline ungate, JIT stub tail, ASan native-shadow READ/WRITE access type and region-relative locator, Sv39 and bottom-up-mmap shadow-layout robustness, UBSan/ASan `ebreak` SIGTRAP coexistence, the callee-saved float pool.
- i386: JIT stub tail; runtime-test the implemented PIC/PIE ASan stack path.
- arm (armv7): self-host, JIT stub tail, and a working AOT-to-file link — mcc's arm objects stamp EABI attribute v0 so GNU ld rejects them, a static link hits GOT reloc 107 and `__ctzdi2`, and no arm32 sysroot is vendored. Full `-pie` ASan is blocked by the shared mccasan PIE-startup bug.
- arm64: mode-6 object-output dispatch and the in-place patch row. A true 3-stage self-host binary fixpoint is blocked by a TLS-LE reloc overflow in mcc's own linker.
- PE targets: the full UBSan handler ABI (minimal-runtime only today, needs `.rodata` descriptor tables).
- True `-shared`/`.so` dynamic TLS on arm, arm64 and riscv64 — all three error. riscv64's GD→LE relaxation rewrites `auipc`→`lui` but never neutralizes `call __tls_get_addr`.
- Build a weak-memory validator for aarch64 and armv7; qemu is x86-TSO. Re-land the nightly campaign soak by editing `matrix.yml` directly, add an armv7 cell, and add a dedicated concurrency/litmus validator.
- Land the `.rodata` data-emission project; it unblocks the full `__ubsan_handle_*` ABI and value-table switch dispatch at once.

### Sanitizers, const-data, CST

- Upgrade UBSan from minimal to the full ABI: emit `{SourceLocation, TypeDescriptor}` tables plus operand values so diagnostics name file, line and values. Needs `.rodata` descriptor emission.
- Add READ/WRITE access type and a region-relative locator to ASan reports, port them to x86_64 with qemu-amd64 validation, then propagate to riscv64.
- Fix the same-granule "to the left" locator underflow in the shared `asan_locate`; msvcrt-internal CRT allocations are not redzoned.
- Confirm the ShadowGap contiguous-mmap fix on real x86_64 CI; qemu cannot run the `-fasan-shadow` runtime.
- Decide compiler-rt interop versus `libmccsan`.
- Explore `-fsanitize-coverage`, `-fsanitize=cfi`, `_FORTIFY_SOURCE`, and a freestanding/KASAN-style runtime sanitizer. Honor auto over-alignment under `-fsanitize=address`/`-b`.
- Build the size-changing datacomp half: an `.init_array` decompress ctor on all five arches plus the `__mcc_decompress` runtime, and a third ping-pong buffer to hold the compressed blob.
- Extend `MCC_ZERO_BSS` to TLS `tdata`→`tbss` and the asan/bcheck zero-init cases.
- Add value-table `switch` dispatch; needs `.rodata` data emission.
- CST: multi-file `#include` stitching; `-g` from CST provenance (stands up the debugger and gdb suite) and stop discarding the arena on the `--lsp`+`-g` path; design `--hotreload` from reconciled snapshots; decide whether CST supersedes the Bind marker; emit `CST_Error`/`CST_Missing`.

### Infrastructure and tests

- Pick one naming scheme (`<area>-<triple>-<variant>` suggested) and apply it across workflow job ids, CMake targets and ctest cell names, so a failure name identifies the cell without a lookup.
- Add the `-O` level as a matrix axis for the remaining cross-arch cells. `-O1` is a subset of `-O2`; `-O4` runs the out-of-process superopt at ~20 s per compile and needs its own budget.
- Audit every `add_test` for whether it could run on more triples than it does, and whether it bundles independent assertions that would give more coverage split apart. The per-golden `exec/<name>` split is the model.
- Measure the install / build / test split on a real CI run before tiering the suite. Locally the suite is parallel-bound, the slowest 200 tests are 38% of wall, and the mass sits in 661 tests in the 0.5-2 s band — so moving a handful of hogs buys ~12%, not a halving. Also gate `bench`, shard macOS ctest, and prune matrix re-runs.
- Turn "mcc builds GCC" into a repeatable gate: decide where the tree comes from (~1.5 GB source, ~1.7 GB build), whether to gate on the full `c,c++` build or a subset like `all-gcc`, and remove the two workarounds (`-static-libgcc` acceptance and passing `libmccrt.a`'s members as plain objects). Keep the step that runs the produced `xgcc`/`xg++`, and state that mcc compiles 492 of 2727 compile commands.
- Audit `GATES[]` coverage on a schedule, not per-gate. Instruction 8 says extend it for every new `MCC_AST_*` gate, and the shortfall had grown unnoticed: measured 2026-07-31, `src/mccast.c` declares 119 `ast_env_gate` names and `GATES[]` carried 43. The gap is mostly benign — of the 80 unswept, most are default-ON (already exercised by the `-O0`..`-O3` sweep) or report/dump-only, and of the 17 that are default-OFF and not diagnostic, forcing each on over 60 corpus files at `-O1`/`-O2`/`-O3` changes ZERO objects for 14 of them, because they are `-O4` search knobs or otherwise inert below `-O4`. Only three moved bytes and now have rows: `MCC_AST_SLICE` (6 objects), `MCC_AST_NO_CALLFUL` (42) and `MCC_AST_JIT_SPLICE` (126); all three are clean over 60 seeds each. The residual gap is the `-O4`-only knobs, which the fuzzer cannot reach at all until it sweeps an `-O4` leg — that is the real coverage hole, not the row count. The `-O4` base leg now exists — `fuzz_runner --o4` (or `MCC_FUZZ_O4`) appends `-O4` to the base sweep and ctest cell `fuzz/o4` runs 8 programs in ~37 s — so the out-of-process superopt path is differentially tested for the first time. It is deliberately base-only: `-O4` costs 4010 ms per compile against 6 ms at `-O3`, so putting it in the per-gate loop would multiply that by the 46 `GATES[]` rows. That leaves the last hole: the 14 gates that are inert below `-O4` are still never forced, because forcing them needs exactly the `-O4`×gates product that is unaffordable in general. A targeted leg over just those 14 would be ~56 s per program, which is the number to decide against. Do NOT build the targeted `-O4`×14 fuzz leg: measured 2026-07-31, forcing each of the 14 at `-O4` over three corpus files moves bytes for only two (`MCC_AST_SEARCH_ORDER`, `MCC_AST_SEARCH_PTHREADS`), and that sample UNDER-reports — `MCC_AST_INLINE_STATIC` shows no change there but does change bytes on a file with an uncalled `static inline`, which is its trigger. So the blanket leg would spend ~56 s per program mostly on gates their inputs never reach. What is actually missing is a per-gate TRIGGER corpus: one small file per gate written to hit its construct, compiled with and without the gate, which answers "never fires anywhere" versus "did not fire here" (instruction 11) at a few seconds per gate instead of minutes per program.
- Commit the FP differential-fuzz generator into `tests/fuzz/` and wire a ctest cell; extend it past `+ - * / sqrt` to `long double` and an x86_64-with-SSE leg. Fuzz FP on arm64 or x86_64-with-SSE and build the reference with `-ffp-contract=off -fno-fast-math`; i386 is unreliable for FP differential.
- Build the per-host capability audit: enumerate every skip gate, classify each as genuinely HW/arch-gated or provisionable here, document the exact install to un-skip it, and assert each expected suite actually ran at least N tests on platforms that support it. Extend the landed diff3 assertion across preprocess, cli-structural, i386-fastcall and cross-exec. Audit `mcc_skip_test` per-triple ungating for i386-linux, aarch64-linux and armv7-linux.
- Teach the native `suite_i386fastcall` a Docker execution backend so the harness works off-i386, and confirm CI provides `linux/386` docker.
- Run the `arm-inline`, `riscv64-inline` and `riscv64-promote` docker differentials natively on arm64 by parameterizing the build-stage platform; arm64 hosts currently get zero coverage of them.
- Build the fuzzed link-time/ABI mixed-object differential (the current guard is fixed-corpus), a native non-docker Linux variant, and `long double` plus returned-HFA edge cases.
- Add coverage-guided generation (gcov or Intel-PT into `tests/fuzz/gen.h`) and EMI mutation targeting optimizer miscompiles.
- DWARF: variable locations under heavier `-O1`+ register promotion, and lexical-block scoping. riscv64 and armv7 are not gdb-tested here (qemu-user has no ptrace) but share the line-table fixes.
- Emit armv7 `.ARM.exidx` if mcc-compiled C ever needs to be unwindable-through in a C++ or `-funwind-tables` context.
- Implement `__attribute__((transparent_union))`. `dispatch_release` and the rest of libdispatch's object API take `dispatch_object_t`, which is one — mcc rejects the call outright (`cannot convert 'struct dispatch_queue_s *' to 'union <anonymous>'`), so no GCD code that manages object lifetimes compiles. `tests/darwin/libsystem_gcd.c` omits the releases for this reason.
- Confirm the `-fexcess-precision=standard` i386 WIN32 refflags fix. Doubly blocked from confirmation. (1) Locally (winlibs i686 gcc 16.1.0) the divergence does NOT reproduce: mcc-i386-win32 already matches the reference on `full_language.c` byte-for-byte, and adding/removing the excess-precision flag changes the reference output not at all — consistent with "only reproduces there". (2) On the mingw/i686 CI cell (windows-latest = windows-2025/VS2026) `mcctest` self-skips with rc 77 because the *reference* program (winlibs i686 gcc + `tests/support/msvcrt_start.c`, linked against system `msvcrt.dll`) crashes at runtime (`mccharness.c:481` "reference cc program crashed ... no differential oracle"), so the differential never runs there either. Fix the CI reference-program crash first (needs a native i686 mingw build — `MCC_MINGW_ARCH=i686` — not the `cmake-cross` host compiler, and the CI image's specific mingw), then the excess-precision divergence can be observed and the refflag confirmed.
- Run the arm64-PE stack auto over-alignment check on real arm64-Windows hardware. i386-PE is validated and gated (`alignas_over.c` compiles, links and runs OK on WoW64, over-aligned locals to 256B; the `tools/i386win32-soak.sh` AOT-exec leg guards it). arm64-PE is codegen-only here — `mcc-arm64-win32` compiles it cleanly but execution needs arm64-Windows.
- Re-promote the mingw/i686 CI cell off `continue-on-error` (drop `experimental` in `PLAN_MINGW`, `tools/ci.c`). i386-Windows codegen is proven (byte-identical 3-stage AOT self-host, JIT stub-tail selftests, i386 fuzz), but the cell also runs `embed-jit-smoke`, which still SKIPs on the i386-PE embedded-engine startup `0xC0000005` (see that item). Promoting is only honest once `embed-jit-smoke` PASSES — a green skip is not a green pass. A first premature promotion reded main and was reverted; re-check the whole i686 ctest is green (not just codegen) before re-doing it.
- Normalize CMake incrementally with autodetection, folded `.cmake` files and a verifiable target.
- Run `tools/runtime-bench.py` on the arm64-native CI cell and record a baseline JSON to diff against; there is no baseline store or differ yet, only `--json`. arm64-macOS now has real instruction counts (`/usr/bin/time -l`), so `--assert-gate-wins` could assert on instructions instead of the CPU-time proxy that keeps it x86_64-only.
- Flip `MCC_MACHO_ADHOC_SIGN` default-on and drop `host_codesign_adhoc`. **The reproducibility premise this item rested on is FALSE and the flip needs a different justification.** Measured on arm64 macOS 26.5.2: `/usr/bin/codesign -f -s -` is deterministic — three runs on an identical path are byte-identical, and so are two runs separated by 2 s, so there is no embedded timestamp. The apparent nondeterminism is the output *filename*, which both signers embed as the signing identifier (`macho_sign_ident` takes the basename): sign the same bytes as `d1` and `d2` and exactly the identifier characters differ. Compare with a fixed path before concluding anything here. What actually argues for the flip, both measured: (1) it is the only way to sign off-Darwin — `host_codesign_adhoc` spawns `codesign`, which does not exist on Linux, and arm64 Darwin refuses to execute an unsigned image, so a Linux-hosted mcc cannot produce a runnable arm64 Mach-O at all; and (2) it is ~2x faster per link, 20 links in 0.231 s against 0.475 s, the difference being one fork+exec each. Soak status: 409 of 409 `jit|macho|^exec/|selfhost` cells pass with `MCC_MACHO_ADHOC_SIGN=1` (env propagation through ctest verified with a probe build, not assumed), `codesign -v -v` calls the built-in signature `valid on disk` and says it `satisfies its Designated Requirement`, and the two signers' outputs differ in 336 bytes so the choice is observable. Prerequisites, both now discharged or reassigned. The hardened-runtime/entitlement question is ANSWERED: runtime JIT is the capability hardened runtime would gate, and all 49 `jit/` selftests pass under the built-in signer exactly as under codesign, with no entitlements and no special slots. The remaining CodeDirectory differences are cosmetic or notarization-only — measured against codesign on the same input, mcc writes a bare basename identifier where codesign appends a hash suffix, 0 special slots against 2, no requirements blob against an empty one, sha256-only against sha1+sha256, and 4 KB hash pages against the arm64-native 16 KB (which costs 4x the hashes: 776 against 194 on a 3.1 MB binary, ~19 KB, and is accepted either way). None of that is reachable without a Developer ID, which ad-hoc signing does not have. The dist-macos soak is done for the plain path — the preset configures, builds and links, and its output signs and runs under both signers — but the `--embed-jit` half is BLOCKED by the missing arm64 TLVP relocations (see that item), not by anything about signing; `--embed-jit` fails identically under both signers.
- Wire `machofat` into `dist-macos` packaging so the shipped mcc is universal. **Do NOT lift the SHA-256 and CodeDirectory emitter out of `mccmacho.c` into `tools/toolsupport.c` for this — that ~200-line refactor is unnecessary, measured 2026-07-31.** A slice's CodeDirectory covers only that slice's bytes at slice-relative offsets, so fusing already-signed slices preserves them: `machofat` with its `codesign` shell-out skipped produces a fat image `codesign -v -v` calls `valid on disk` and says `satisfies its Designated Requirement`, and it runs (verified against both `lipo -create` and machofat's own layout, arm64+x86_64). mcc always signs its Mach-O executables, so in the dist path the shell-out is pure redundancy. It earns its keep in exactly one case: a slice that arrives UNSIGNED. Then the fat is `code object is not signed at all / In architecture: arm64` and AMFI SIGKILLs it (rc 137, verified by stripping one slice with `codesign --remove-signature`). That case is also the one the shell-out cannot fix, because it only arises off-Darwin where `codesign` does not exist. So the two Darwin signing items collapse into one: flip `MCC_MACHO_ADHOC_SIGN` default-on (see that item) and every slice is self-signed everywhere, at which point `machofat` needs no signer at all and should verify-and-skip rather than re-sign. `machofat.c:167` is still the live `execlp`, and `dist-macos` still sets no architecture and never invokes `machofat`, but `tools/ci.c` already builds both arches as separate dist artifacts (`macos-arm64-clang`, `macos-x86_64-clang` against `PS_DIST_MACOS`), so the fuse inputs already exist and are already uploaded.
- Remove or replace the dangling `docs/NOTES.md`, `docs/JIT-PLAN.md`, `docs/AST.md`, `docs/SUBSTRATE-PLAN.md`, `SPLIT.md` and `GATED.md` pointers; none of those files exists in the repo or its history.
