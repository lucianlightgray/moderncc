# C9911 — C99 + C11 Conformance Source of Truth (mcc vs gcc vs clang)

A clause-by-clause index of ISO/IEC 9899:1999 (**C99**, working draft N1256) and
ISO/IEC 9899:2011 (**C11**, working draft N1570), with **every normative
requirement rewritten as a TODO checkbox** and cross-referenced against three
compilers: the in-tree **`mcc`**, **gcc 15.3**, and **clang 22**.

It is built to be the single source of truth for "what does the standard require,
and where does `mcc` agree with or diverge from gcc/clang?". Requirement text is
**paraphrased** (the ISO text is copyrighted), with a precise `§clause·paragraph`
citation on every item.

**Totals:** 3,837 requirement checkboxes · 2,875 confirmed `mcc:✓` · 111 rows
carry a non-`✓` mcc tag (88 `~` / 23 `✗`), of which 52 are mcc-specific
divergences from the gcc==clang consensus (45 `~` / 7 `✗`) and the rest are
consensus `✗`/`~` shared with gcc/clang · remainder are non-testable spec/UB
items. The residual mcc-specific `✗`/`~` rows are the deliberate / no-consensus /
UB / not-separately-testable items catalogued in [TODO.md](TODO.md).

## Legend

Each requirement is a checkbox of the form:

```
- [ ] §<clause>p<paragraph> — <paraphrased requirement>. — mcc:<s> gcc:<s> clang:<s> (note)
```

- `§...p...` — the precise N1256/N1570 clause and paragraph. Library function
  rules cite the function's subclause.
- `(C11)` — a requirement new in or changed by C11. `(C99-only)` — a C99 rule
  that C11 removed or altered. `(C90)` — noted where the lineage matters.
- Per-compiler status:
  - `✓` — conforms / behaves as the standard requires (and matches the
    gcc==clang consensus where one exists).
  - `✗` — diverges: rejects valid code, miscompiles, or is missing the feature.
  - `~` — partial: diagnoses but does not reject, diagnostic-only, or
    standards-optional/implementation-defined latitude.
  - A parenthetical records the nature of any `mcc` divergence from the
    gcc==clang consensus.
- `(spec; not separately testable)` — a pure definition, an undefined/unspecified/
  implementation-defined behavior, or an optimization/aliasing rule with no direct
  compile-or-run probe.
- `[ ]` is an open checkbox throughout: this is a *specification map*, not a work
  tracker — an unchecked box means "this requirement exists", and the status tag
  records who satisfies it. (Items tagged `mcc:✗`/`mcc:~` are the actionable
  conformance gaps; see [TODO.md](TODO.md) for the ones already triaged/fixed.)

## How it is organized

Sections follow the standard's own numbering: §4–§5 (conformance & environment),
§6 (language), §7 (library), and the normative + cataloging annexes. The clause 7
library numbering shifted between C99 and C11 (e.g. C99 §7.16 `<stdarg.h>` →
C11 §7.16 `<stdint.h>`); part titles note both where relevant.

---

## Table of contents

- [§4 Conformance & §5 Environment](#4-conformance-5-environment)
- [§6.2.1-§6.2.4 Scopes, linkages, name spaces, storage durations](#621-624-scopes-linkages-name-spaces-storage-durations)
- [§6.2.5-§6.2.8 Types, representations, compatible/composite types, alignment](#625-628-types-representations-compatiblecomposite-types-alignment)
- [§6.3 Conversions](#63-conversions)
- [§6.4 Lexical elements](#64-lexical-elements)
- [§6.5.1-§6.5.4 Primary/postfix/unary expressions](#651-654-primarypostfixunary-expressions)
- [§6.5.5-§6.5.14 Binary operators](#655-6514-binary-operators)
- [§6.5.15-§6.5.17 Conditional/assignment/comma & §6.6 Constant expressions](#6515-6517-conditionalassignmentcomma-66-constant-expressions)
- [§6.7.1-§6.7.3 Declarations: storage/type specifiers/qualifiers](#671-673-declarations-storagetype-specifiersqualifiers)
- [§6.7.2.1-§6.7.2.4 Struct/union/enum/atomic specifiers](#6721-6724-structunionenumatomic-specifiers)
- [§6.7.4-§6.7.5 Function specifiers & alignment specifier](#674-675-function-specifiers-alignment-specifier)
- [§6.7.6 Declarators](#676-declarators)
- [§6.7.7-§6.7.9 Type names/typedef/initialization](#677-679-type-namestypedefinitialization)
- [§6.8 Statements and blocks](#68-statements-and-blocks)
- [§6.9 External definitions](#69-external-definitions)
- [§6.10.1-§6.10.3 Conditional inclusion, source inclusion, macro replacement](#6101-6103-conditional-inclusion-source-inclusion-macro-replacement)
- [§6.10.4-§6.10.9 Line/error/pragma/null/predefined macros/_Pragma](#6104-6109-lineerrorpragmanullpredefined-macrospragma)
- [§7.1-§7.5 Library intro, assert, complex, ctype, errno](#71-75-library-intro-assert-complex-ctype-errno)
- [§7.6-§7.8 fenv, float, inttypes](#76-78-fenv-float-inttypes)
- [§7.9-§7.11 iso646, limits, locale](#79-711-iso646-limits-locale)
- [§7.12 <math.h>](#712-mathh)
- [§7.13-§7.16 setjmp, signal, stdalign, stdarg](#713-716-setjmp-signal-stdalign-stdarg)
- [§7.16-§7.18 stdarg/stdbool/stdint-or-atomic boundary](#716-718-stdargstdboolstdint-or-atomic-boundary)
- [<stddef.h> & <stdint.h>](#stddefh-stdinth)
- [§7.21 <stdio.h>](#721-stdioh)
- [§7.22 <stdlib.h>](#722-stdlibh)
- [§7.23 <string.h> & §7.24 <tgmath.h>](#723-stringh-724-tgmathh)
- [§7.25-§7.27 threads(C11)/time](#725-727-threadsc11time)
- [§7.28-§7.31 uchar(C11)/wchar/wctype](#728-731-ucharc11wcharwctype)
- [Annexes F, G, K (normative)](#annexes-f-g-k-normative)
- [Annexes C, D, E, H, I, J (sequence points, UCN ranges, limits, portability)](#annexes-c-d-e-h-i-j-sequence-points-ucn-ranges-limits-portability)
- [Appendix — mcc project status: deliberate differences & limitations](NOTES.md#c99c11-conformance--mcc-project-status-deliberate-differences--limitations)

---

## §4 Conformance & §5 Environment

> mcc status: 250 checkbox requirements tracked in this section; 1 carries a non-`✓` mcc tag, and it
> is a single substantive mcc-gap relative to the gcc==clang consensus: mcc advertises
> `__STDC_VERSION__ == 199901L` (C99) while actually shipping the C11 freestanding headers, atomics,
> threads and `_Noreturn` — a by-design default, since gcc 15 defaults to C23 and clang 22 to C17.
> §6.11.6 obsolescent empty-paren declarators now draw mcc's `-Wstrict-prototypes` diagnostic, mcc
> predefines both `__STDC_UTF_16__` and `__STDC_UTF_32__` (value `1`), and the §5.1.2.3 / §5.1.2.4
> volatile-and-atomic ordering items are inspectable via `-S` and conform. Everything numeric —
> `<limits.h>` and `<float.h>`, including all C11 additions — is byte-for-byte identical to gcc/clang,
> and `#error`, trigraphs, UCNs, `main` startup/termination, escape-sequence values and all probed
> translation limits conform.

> C99↔C11 deltas in this section: C11 adds `#error` as a translation-failure trigger (§4p4), adds `<stdalign.h>`/`<stdnoreturn.h>` to the freestanding header set (§4p6), replaces the C99 "sequence point" wording of §5.1.2.3 with the *sequenced-before* relation and adds the entire §5.1.2.4 multi-threaded/data-race model, and adds the `*_HAS_SUBNORM`, `*_DECIMAL_DIG`, and `*_TRUE_MIN` `<float.h>` macros (§5.2.4.2.2). Several §5.2.4.1 translation limits and §5.2.4.2.1 magnitudes were already raised from C90 to their current values in C99.

### §4 Conformance

- §4p1 — "shall" is to be interpreted as a requirement on an implementation or on a program; "shall not" is to be interpreted as a prohibition. — (spec; not separately testable)
- §4p2 — If a "shall"/"shall not" requirement that appears outside of a constraint or runtime-constraint is violated, the behavior is undefined. — (spec; not separately testable)
- §4p2 — Undefined behavior is indicated either by the words "undefined behavior", by the omission of any explicit definition of behavior, or by a "shall"/"shall not" outside a constraint; there is no difference in emphasis among these three forms — they all describe behavior that is undefined. — (spec; not separately testable)
- §4p3 — A program that is correct in all other aspects, operating on correct data, containing unspecified behavior shall be a correct program and act in accordance with §5.1.2.3. — (spec; not separately testable)
- [✓] §4p4 — (C11) The implementation shall not successfully translate a preprocessing translation unit containing a `#error` preprocessing directive unless it is part of a group skipped by conditional inclusion. (Added in C11; absent in C99.) — mcc:✓ gcc:✓ clang:✓ (all three reject a live `#error`; all three accept `#error` inside a `#if 0` skipped group)
- §4p5 — A *strictly conforming program* shall use only those features of the language and library specified in this International Standard. — (spec; not separately testable)
- §4p5 — A strictly conforming program shall not produce output dependent on any unspecified, undefined, or implementation-defined behavior, and shall not exceed any minimum implementation limit. — (spec; not separately testable)
- [✓] §4p6 — The two forms of conforming implementation are *hosted* and *freestanding*; a conforming hosted implementation shall accept any strictly conforming program. — mcc:✓ gcc:✓ clang:✓ (`__STDC_HOSTED__ == 1` by default for all three)
- [✓] §4p6 — A conforming freestanding implementation shall accept any strictly conforming program in which the use of clause-7 library features is confined to the standard headers `<float.h>`, `<iso646.h>`, `<limits.h>`, `<stdalign.h>` (C11), `<stdarg.h>`, `<stdbool.h>`, `<stddef.h>`, `<stdint.h>`, and `<stdnoreturn.h>` (C11). (C99 freestanding set omits `<stdalign.h>` and `<stdnoreturn.h>`.) — mcc:✓ gcc:✓ clang:✓ (all nine headers `#include` cleanly under `-ffreestanding` in mcc; `__STDC_HOSTED__ == 0` under `-ffreestanding` for all three)
- §4p6 — A conforming implementation may have extensions (including additional library functions), provided they do not alter the behavior of any strictly conforming program. — (spec; not separately testable)
- §4p7 — A *conforming program* is one that is acceptable to a conforming implementation. — (spec; not separately testable)
- §4p8 — An implementation shall be accompanied by a document that defines all implementation-defined and locale-specific characteristics and all extensions. — (spec; documentation requirement, not separately testable)
- §4p8 (footnote) — Strictly conforming programs are intended to be maximally portable among conforming implementations, and may use conditional features (e.g. those in annexes) provided their use is guarded by an appropriate conditional-feature macro test. — (spec; not separately testable)

### §5 Environment

- §5p1 — An implementation translates C source files and executes C programs in two data-processing-system environments — the *translation environment* and the *execution environment* — referred to as such whether or not they share the same physical machine. — (spec; not separately testable)

### §5.1 Conceptual models

#### §5.1.1 Translation environment

##### §5.1.1.1 Program structure

- [✓] §5.1.1.1p1 — A C *program* need not all be translated at the same time. — mcc:✓ gcc:✓ clang:✓ (all support separate `-c` translation + later link)
- §5.1.1.1p1 — The text of the program is kept in units called *source files* (or *preprocessing files*). — (spec; not separately testable)
- §5.1.1.1p1 — A source file together with all the headers and source files included via `#include` is a *preprocessing translation unit*; after preprocessing it is called a *translation unit*. — (spec; not separately testable)
- [✓] §5.1.1.1p1 — Previously translated translation units may be preserved individually or in libraries. — mcc:✓ gcc:✓ clang:✓ (mcc produces ELF `.o` objects linkable into archives)
- §5.1.1.1p1 — The separate translation units of a program communicate by (for example) calls to functions whose identifiers have external linkage, manipulation of objects whose identifiers have external linkage, or manipulation of data files. — (spec; not separately testable)
- [✓] §5.1.1.1p1 — Translation units may be separately translated and then later linked to produce an executable program. — mcc:✓ gcc:✓ clang:✓

##### §5.1.1.2 Translation phases

- §5.1.1.2p1 — The precedence among the syntax rules of translation is specified by the following eight phases. — (spec; not separately testable)
- [✓] §5.1.1.2p1(1) — Phase 1: Physical source file multibyte characters are mapped, in an implementation-defined manner, to the source character set (introducing new-line characters for end-of-line indicators) if necessary; trigraph sequences are replaced by their corresponding single-character internal representations. — mcc:✓ gcc:✓ clang:✓ (trigraph replacement under `-trigraphs`; off by default for all three — see §5.2.1.1)
- [✓] §5.1.1.2p1(2) — Phase 2: Each instance of a backslash (`\`) immediately followed by a new-line character is deleted, splicing physical source lines into logical source lines; only the last backslash on any physical source line is eligible for such a splice. — mcc:✓ gcc:✓ clang:✓ (`int a\<NL>b=5;` splices to identifier `ab` in all three)
- [✓] §5.1.1.2p1(2) — Phase 2: A source file that is not empty shall end in a new-line character, which shall not be immediately preceded by a backslash character before any such splicing takes place. — mcc:✓ gcc:✓ clang:✓ (a missing final newline is UB, not a constraint; all three silently accept even under `-std=c11 -pedantic-errors`)
- [✓] §5.1.1.2p1(3) — Phase 3: The source file is decomposed into preprocessing tokens and sequences of white-space characters (including comments); a source file shall not end in a partial preprocessing token or in a partial comment. — mcc:✓ gcc:✓ clang:✓ (all three diagnose an unterminated `/*` comment)
- [✓] §5.1.1.2p1(3) — Phase 3: Each comment is replaced by one space character; new-line characters are retained; whether each nonempty sequence of white-space characters other than new-line is retained or replaced by one space character is implementation-defined. — mcc:✓ gcc:✓ clang:✓ (comment-to-space tokenization, e.g. `a/**/b` → two tokens, behaves identically)
- [✓] §5.1.1.2p1(4) — Phase 4: Preprocessing directives are executed, macro invocations are expanded, and `_Pragma` unary operator expressions are executed. — mcc:✓ gcc:✓ clang:✓ (`_Pragma` recognized; see §6.11.8 probe)
- §5.1.1.2p1(4) — Phase 4: If a character sequence that matches the syntax of a universal character name is produced by `##` token concatenation, the behavior is undefined. — (UB; not separately testable)
- [✓] §5.1.1.2p1(4) — Phase 4: A `#include` directive causes the named header or source file to be processed from phase 1 through phase 4, recursively; all preprocessing directives are then deleted. — mcc:✓ gcc:✓ clang:✓
- [✓] §5.1.1.2p1(5) — Phase 5: Each source character set member and escape sequence in character constants and string literals is converted to the corresponding member of the execution character set; if there is no corresponding member, it is converted to an implementation-defined member other than the null (wide) character. — mcc:✓ gcc:✓ clang:✓ (escape-sequence values identical; see §5.2.2)
- [✓] §5.1.1.2p1(6) — Phase 6: Adjacent string literal tokens are concatenated. — mcc:✓ gcc:✓ clang:✓ (`"a" "b"` → `"ab"`; verified at runtime)
- §5.1.1.2p1(7) — Phase 7: White-space characters separating tokens are no longer significant; each preprocessing token is converted into a token; the resulting tokens are syntactically and semantically analyzed and translated as a translation unit. — (spec; not separately testable as a discrete phase)
- [✓] §5.1.1.2p1(8) — Phase 8: All external object and function references are resolved; library components are linked to satisfy external references not defined in the current translation; all such translator output is collected into a program image containing information needed for execution in its execution environment. — mcc:✓ gcc:✓ clang:✓ (mcc invokes/embeds a linker to produce a runnable image)
- §5.1.1.2p2 (footnote) — Implementations shall behave as if these separate phases occur, even though in practice many phases might be folded together. — (spec; not separately testable)
- §5.1.1.2p2 (footnote) — Source files, translation units, and translated translation units need not be stored as files, nor need there be any one-to-one correspondence between these entities and any external representation; the description is conceptual only and does not specify any particular implementation. — (spec; not separately testable)

##### §5.1.1.3 Diagnostics

- [✓] §5.1.1.3p1 — A conforming implementation shall produce at least one diagnostic message (identified in an implementation-defined manner) if a preprocessing translation unit or translation unit contains a violation of any syntax rule or constraint, even if the behavior is also explicitly specified as undefined or implementation-defined. — mcc:✓ gcc:✓ clang:✓ (syntax error `return }` → diagnostic + non-zero exit in all three; constraint violation `int x = "string";` → mcc emits a warning by default — satisfying "at least one diagnostic" — but translates successfully unless `-Werror`, whereas gcc/clang make it an error. The standard's last sentence explicitly permits successfully translating an invalid program, so mcc conforms; it is merely more permissive by default.)
- §5.1.1.3p1 — Diagnostic messages need not be produced in other circumstances. — (spec; permission, not separately testable)
- [✓] §5.1.1.3p1 (footnote) — The intent is that an implementation identify the nature of, and where possible localize, each violation; an implementation is free to produce any number of diagnostics so long as a valid program is still correctly translated, and may also successfully translate an invalid program. — mcc:✓ gcc:✓ clang:✓ (mcc localizes diagnostics with `file:line:` prefixes)

#### §5.1.2 Execution environments

- [✓] §5.1.2p1 — Two execution environments are defined: *freestanding* and *hosted*; in both, *program startup* occurs when a designated C function is called by the execution environment. — mcc:✓ gcc:✓ clang:✓ (`-ffreestanding` vs default hosted both supported)
- [✓] §5.1.2p1 — All objects with static storage duration shall be initialized (set to their initial values) before program startup; the manner and timing of such initialization are otherwise unspecified. — mcc:✓ gcc:✓ clang:✓ (static/global initializers observed before `main`)
- [✓] §5.1.2p1 — *Program termination* returns control to the execution environment. — mcc:✓ gcc:✓ clang:✓

##### §5.1.2.1 Freestanding environment

- [✓] §5.1.2.1p1 — In a freestanding environment (in which C program execution may take place without any benefit of an operating system), the name and type of the function called at program startup are implementation-defined. — mcc:✓ gcc:✓ clang:✓ (all accept `-ffreestanding -nostdlib` with a custom entry, e.g. `_start`)
- §5.1.2.1p1 — Any library facilities available to a freestanding program, other than the minimal set required by clause 4, are implementation-defined. — (spec; implementation-defined, not separately testable)
- §5.1.2.1p2 — The effect of program termination in a freestanding environment is implementation-defined. — (spec; implementation-defined, not separately testable)

##### §5.1.2.2 Hosted environment

- [✓] §5.1.2.2p1 — A hosted environment need not be provided, but shall conform to the following specifications if present. — mcc:✓ gcc:✓ clang:✓ (hosted is the default)

###### §5.1.2.2.1 Program startup

- [✓] §5.1.2.2.1p1 — The function called at program startup is named `main`; the implementation declares no prototype for this function. — mcc:✓ gcc:✓ clang:✓
- [✓] §5.1.2.2.1p1 — `main` shall be defined with a return type of `int` and either no parameters (`int main(void)`) or two parameters (`int main(int argc, char *argv[])`) or in some other implementation-defined manner. — mcc:✓ gcc:✓ clang:✓ (both standard forms compile and run)
- [✓] §5.1.2.2.1p1 (footnote) — `int` can be replaced by a typedef name defined as `int`, or the type of `argv` can be written as `char **argv`, and so on. — mcc:✓ gcc:✓ clang:✓ (`char **argv` form accepted)
- [✓] §5.1.2.2.1p2 — If defined, the `main` parameters shall obey: the value of `argc` shall be nonnegative, and `argv[argc]` shall be a null pointer. — mcc:✓ gcc:✓ clang:✓ (runtime check `argv[argc]==NULL` true for all three; supplied by the host C runtime)
- [✓] §5.1.2.2.1p2 — If `argc` is greater than zero, the array members `argv[0]` through `argv[argc-1]` inclusive shall contain pointers to strings, given implementation-defined values by the host environment prior to program startup. — mcc:✓ gcc:✓ clang:✓ (host-supplied; runtime-verified)
- §5.1.2.2.1p2 — If the host environment cannot supply strings with letters in both uppercase and lowercase, the implementation shall ensure the strings are received in lowercase. — (spec; host-environment property, not separately testable on Linux)
- [✓] §5.1.2.2.1p2 — If `argc` is greater than zero, the string pointed to by `argv[0]` represents the program name; `argv[0][0]` shall be the null character if the program name is not available from the host environment. — mcc:✓ gcc:✓ clang:✓ (`argv[0]` carries the invocation name; supplied by the runtime)
- [✓] §5.1.2.2.1p2 — If `argc` is greater than one, the strings pointed to by `argv[1]` through `argv[argc-1]` represent the *program parameters*. — mcc:✓ gcc:✓ clang:✓
- [✓] §5.1.2.2.1p2 — The parameters `argc` and `argv` and the strings pointed to by the `argv` array shall be modifiable by the program, and retain their last-stored values between program startup and program termination. — mcc:✓ gcc:✓ clang:✓ (writes to `argv[i][j]` persist)

###### §5.1.2.2.2 Program execution

- [✓] §5.1.2.2.2p1 — In a hosted environment, a program may use all the functions, macros, type definitions, and objects described in the library clause (clause 7). — mcc:✓ gcc:✓ clang:✓ (mcc links against the system libc; clause-7 facilities available)

###### §5.1.2.2.3 Program termination

- [✓] §5.1.2.2.3p1 — If the return type of `main` is compatible with `int`, a return from the initial call to `main` is equivalent to calling `exit` with the value returned by `main` as its argument. — mcc:✓ gcc:✓ clang:✓ (`return 42` → process exit status 42 in all three)
- [✓] §5.1.2.2.3p1 — Reaching the `}` that terminates `main` returns a value of 0. — mcc:✓ gcc:✓ clang:✓ (fall-off `int main(void){}` → exit status 0 in all three)
- §5.1.2.2.3p1 — If the return type of `main` is not compatible with `int`, the termination status returned to the host environment is unspecified. — (unspecified; not separately testable)
- §5.1.2.2.3p1 (footnote) — In accordance with §6.2.4, the lifetimes of objects with automatic storage duration declared in `main` will have ended when returning from `main`, even where they would not have when calling `exit` from within `main`. — (spec; not separately testable)

##### §5.1.2.3 Program execution

> C99 specifies ordering of evaluations entirely through *sequence points* (§5.1.2.3p2); C11 reframes this as the *sequenced-before* / *sequenced-after* / *unsequenced* / *indeterminately-sequenced* relation (§5.1.2.3p3) and adds lvalue value computation (§5.1.2.3p2).

- §5.1.2.3p1 — The semantic descriptions describe the behavior of an abstract machine in which issues of optimization are irrelevant. — (spec; not separately testable)
- §5.1.2.3p2 — Accessing a volatile object, modifying an object, modifying a file, or calling a function that does any of those operations are all *side effects* — changes in the state of the execution environment. — (spec; definition, not separately testable)
- §5.1.2.3p2 — Evaluation of an expression in general includes both value computations and initiation of side effects. — (spec; not separately testable)
- §5.1.2.3p2 — (C11) Value computation for an lvalue expression includes determining the identity of the designated object. — (spec; not separately testable)
- [✓] §5.1.2.3p2 — (C99-only) At certain specified points in the execution sequence called *sequence points*, all side effects of previous evaluations shall be complete and no side effects of subsequent evaluations shall have taken place. (Annex C lists the sequence points; replaced by the sequenced-before model in C11.) — mcc:✓ gcc:✓ clang:✓ (sequence-point semantics honored; e.g. full-expression `;` ordering verified, §5.1.2.3 EXAMPLE)
- §5.1.2.3p3 — (C11) *Sequenced before* is an asymmetric, transitive, pair-wise relation between evaluations executed by a single thread, inducing a partial order; if A is sequenced before B then the execution of A shall precede that of B, and B is *sequenced after* A. — (spec; abstract-machine relation, not separately testable)
- §5.1.2.3p3 — (C11) If A is not sequenced before or after B, then A and B are *unsequenced*; they are *indeterminately sequenced* when A is sequenced either before or after B but it is unspecified which. — (spec; not separately testable)
- §5.1.2.3p3 — (C11) The presence of a *sequence point* between the evaluation of A and B implies that every value computation and side effect associated with A is sequenced before every value computation and side effect associated with B. — (spec; not separately testable)
- [✓] §5.1.2.3p4 — In the abstract machine, all expressions are evaluated as specified by the semantics; an actual implementation need not evaluate part of an expression if it can deduce that its value is not used and that no needed side effects are produced. — mcc:✓ gcc:✓ clang:✓ (dead-code/value elision permitted and performed)
- §5.1.2.3p5 — When the abstract machine is interrupted by receipt of a signal, the values of objects that are neither lock-free atomic objects (C11) nor of type `volatile sig_atomic_t` are unspecified, as is the state of the floating-point environment. — (unspecified; not separately testable)
- §5.1.2.3p5 — The value of any object modified by the signal handler that is neither a lock-free atomic object (C11) nor of type `volatile sig_atomic_t` becomes indeterminate when the handler exits, as does the state of the floating-point environment if modified by the handler and not restored. — (spec/UB; not separately testable)
- [✓] §5.1.2.3p6 — The least requirements on a conforming implementation are: accesses to volatile objects are evaluated strictly according to the rules of the abstract machine. — mcc:✓ gcc:✓ clang:✓ (now verifiable via `mcc -S`: each `volatile` object is accessed with a real load/store emitted in program order, matching gcc -O0 — no elision or reordering)
- [✓] §5.1.2.3p6 — At program termination, all data written into files shall be identical to the result that execution of the program according to the abstract semantics would have produced. — mcc:✓ gcc:✓ clang:✓ (file output identical across all three)
- [✓] §5.1.2.3p6 — The input and output dynamics of interactive devices shall take place as specified in §7.21.3, with the intent that unbuffered or line-buffered output appear as soon as possible. — mcc:✓ gcc:✓ clang:✓ (delegated to the shared libc stdio implementation)
- §5.1.2.3p6 — Accesses to volatile objects, file-write identity, and interactive-device I/O dynamics are collectively the *observable behavior* of the program. — (spec; definition, not separately testable)
- §5.1.2.3p7 — What constitutes an interactive device is implementation-defined. — (spec; implementation-defined, not separately testable)
- §5.1.2.3p8 — More stringent correspondences between abstract and actual semantics may be defined by each implementation. — (spec; not separately testable)
- §5.1.2.3p9 (EXAMPLE 1) — An implementation might define a one-to-one correspondence between abstract and actual semantics. — (informative example; not separately testable)
- §5.1.2.3p10 (EXAMPLE 2) — Alternatively, an implementation might optimize within each translation unit so that semantics agree only at cross-translation-unit function calls. — (informative example; not separately testable)
- [✓] §5.1.2.3p11 (EXAMPLE 3) — Floating-point expression rearrangement is constrained by limited precision; `(x*y)*z` need not equal `x*(y*z)`, and rearrangements changing special-value handling or raised exceptions are disallowed. — mcc:✓ gcc:✓ clang:✓ (none of the three reassociate FP without `-ffast-math`-style opt-in)
- [✓] §5.1.2.3p12 (EXAMPLE 4) — Use of a volatile object in `a = b = c;` requires the implementation to read `c`, store into `b`, read `b`, then store into `a`. — mcc:✓ gcc:✓ clang:✓ (now inspectable via `mcc -S`: mcc emits read `c` → store `b` → store `a`, the identical volatile-access strategy gcc -O0 uses — both reuse the assigned value rather than re-reading `b`, which the standard permits)
- [✓] §5.1.2.3p13 (EXAMPLE 5) — When a value is dead, the implementation need not perform the access; the abstract semantics permit eliding it. — mcc:✓ gcc:✓ clang:✓ (dead non-volatile computations elided)
- [✓] §5.1.2.3 (EXAMPLE) — A `printf` of a function call demonstrates that all side effects associated with the full expression are complete before the next full expression is evaluated. — mcc:✓ gcc:✓ clang:✓

##### §5.1.2.4 Multi-threaded executions and data races (C11)

> Entire subclause is C11-only; absent from C99. mcc ships `<threads.h>`, `<stdatomic.h>`, `_Atomic`,
> `_Thread_local`, and `_Noreturn`, and defines neither `__STDC_NO_THREADS__` nor `__STDC_NO_ATOMICS__`
> (matching gcc/clang) — so it claims support for the entire memory model, even though
> `__STDC_VERSION__` still reads `199901L`.

- [✓] §5.1.2.4p1 — (C11) Under a hosted implementation, a program can have more than one *thread of execution* running concurrently; the execution of the entire program consists of an execution of all of its threads. — mcc:✓ gcc:✓ clang:✓ (`<threads.h>` `thrd_create`/`thrd_join` program compiles, links `-lpthread`, and runs in all three)
- §5.1.2.4p1 — (C11) Under a freestanding implementation, it is implementation-defined whether a program can have more than one thread of execution. — (implementation-defined; not separately testable)
- §5.1.2.4p2 — (C11) The value of an object visible to a thread T at a particular point is the *initial value*, a value stored by T, or a value stored by another thread, according to the rules below. — (spec; memory-model, not separately testable)
- §5.1.2.4p3 — (C11) Two expression evaluations *conflict* if one of them modifies a memory location and the other reads or modifies the same memory location. — (spec; definition, not separately testable)
- [✓] §5.1.2.4p4 — (C11) The library defines synchronization operations (§7.17 atomics, §7.26.4 mutexes); a synchronization operation is an acquire, a release, both, or a consume operation. — mcc:✓ gcc:✓ clang:✓ (`atomic_store`/`atomic_load`/`atomic_thread_fence` with `memory_order_*` available via `<stdatomic.h>`)
- [✓] §5.1.2.4p4 — (C11) An operation without an associated memory location is a fence — acquire, release, or both; additionally there are relaxed atomic operations and atomic read-modify-write operations. — mcc:✓ gcc:✓ clang:✓ (`atomic_thread_fence`, `atomic_fetch_add`, relaxed RMW provided)
- §5.1.2.4p5 — (C11) A *full expression* is one that is not part of another expression, nor part of a (abstract) declarator; there is also an implicit full expression (e.g. a `while` controlling expression). (Definition relocated here in C11; corresponds to §6.8 in C99.) — (spec; definition, not separately testable)
- §5.1.2.4p6 — (C11) Certain library calls *synchronize with* others; a release on atomic M synchronizes with an acquire on M that takes its value from the release sequence headed by it. — (spec; memory-model, not separately testable)
- [✓] §5.1.2.4p7 — (C11) An evaluation A *carries a dependency* to B if A's value is used as an operand of B, subject to the listed exceptions (`kill_dependency`, left operand of `&&`/`||`/`?:`/`,`, etc.). — mcc:✓ gcc:✓ clang:✓ (`kill_dependency` macro provided by `<stdatomic.h>`)
- §5.1.2.4p8 — (C11) A is *dependency-ordered before* B per the consume/release-sequence rules. — (spec; memory-model, not separately testable)
- §5.1.2.4p9 — (C11) A is *inter-thread happens before* B if A synchronizes with B, A is dependency-ordered before B, or the listed transitive combinations hold. — (spec; memory-model, not separately testable)
- §5.1.2.4p10 — (C11) A *happens before* B if A is sequenced before B or A inter-thread happens before B. — (spec; memory-model, not separately testable)
- §5.1.2.4p11 — (C11) A *visible side effect* A on M w.r.t. value computation B satisfies the happens-before/no-intervening-X conditions; the value of a non-atomic scalar M as determined by B is the value stored by the visible side effect A. — (spec; memory-model, not separately testable)
- §5.1.2.4p12 — (C11) The execution of a program contains a *data race* if it contains two conflicting actions in different threads, at least one not atomic, and neither happens before the other; any such data race results in undefined behavior. — (UB; not separately testable)
- §5.1.2.4p13 — (C11) The visible sequence of side effects on an atomic M w.r.t. value computation B is a maximal contiguous sub-sequence in the modification order of M; the value of M as determined by B shall be the value stored by some operation in that sequence. — (spec; memory-model, not separately testable)
- §5.1.2.4p14 — (C11) If A modifying atomic M happens before B modifying M, then A is earlier than B in the modification order of M. — (spec; memory-model, not separately testable)
- §5.1.2.4p15 — (C11) If a value computation A of atomic M happens before B of M, and A takes its value from side effect X, then B's value equals A's or is from a Y following X in the modification order. — (spec; memory-model, not separately testable)
- [✓] §5.1.2.4p16 — (C11) The four coherence requirements disallow compiler reordering of atomic operations to a single object even for relaxed loads. — mcc:✓ gcc:✓ clang:✓ (now inspectable via `mcc -S`: a relaxed store/load/store to one atomic emits three distinct in-order operations with no reordering or CSE across them; mcc performs no optimizing passes that could reorder, so coherence holds)
- §5.1.2.4p17 — (C11) The value of an atomic M as determined by B is the value stored by some side effect A modifying M where B does not happen before A. — (spec; memory-model, not separately testable)
- [✓] §5.1.2.4p18 — (C11) Atomic read-modify-write operations shall always read the last value (in modification order) written before the associated write. — mcc:✓ gcc:✓ clang:✓ (`atomic_fetch_add` lowers to an atomic RMW instruction)
- §5.1.2.4p19 — (C11, recommended practice) An implementation should ensure the last value assigned by an atomic/synchronization operation becomes visible to all threads in finite time. — (recommended practice; not separately testable)
- §5.1.2.4p20–p25 (NOTEs/EXAMPLEs) — (C11) Informative notes on `memory_order_seq_cst`, fence pairing, sequential consistency, and example data-race scenarios. — (informative; not separately testable)

### §5.2 Environmental considerations

#### §5.2.1 Character sets

- §5.2.1p1 — Two sets of characters and their collating sequences shall be defined: the *source character set* and the *execution character set*. — (spec; not separately testable)
- §5.2.1p1 — Each set is divided into a *basic character set* and a set of locale-specific *extended characters*; the combined set is the *extended character set*. — (spec; definition, not separately testable)
- [✓] §5.2.1p1 — The values of the members of the execution character set are implementation-defined. — mcc:✓ gcc:✓ clang:✓ (all use ASCII/UTF-8 execution set; basic-character values identical)
- [✓] §5.2.1p2 — In a character constant or string literal, members of the execution character set shall be represented by corresponding source characters or by escape sequences (`\` followed by one or more characters). — mcc:✓ gcc:✓ clang:✓
- [✓] §5.2.1p2 — A byte with all bits 0, the *null character*, shall exist in the basic execution character set; it is used to terminate a character string. — mcc:✓ gcc:✓ clang:✓ (`'\0'` == 0; string termination works)
- §5.2.1p2 (footnote) — The trigraph sequences allow entry of characters not in the Invariant Code Set of ISO/IEC 646. — (spec; not separately testable)
- [✓] §5.2.1p3 — Both basic character sets shall include the 26 uppercase letters `A`–`Z`, the 26 lowercase letters `a`–`z`, and the 10 decimal digits `0`–`9`. — mcc:✓ gcc:✓ clang:✓
- [✓] §5.2.1p3 — Both basic character sets shall include the 29 graphic characters `! " # % & ' ( ) * + , - . / : ; < = > ? [ \ ] ^ _ { | } ~`. — mcc:✓ gcc:✓ clang:✓
- [✓] §5.2.1p3 — Both basic character sets shall include the space character and control characters for horizontal tab, vertical tab, and form feed. — mcc:✓ gcc:✓ clang:✓
- [✓] §5.2.1p3 — In both basic character sets, the value of each character after `0` in the decimal digits shall be one greater than the previous (digits contiguous and increasing). — mcc:✓ gcc:✓ clang:✓ (`'9'-'0'==9`)
- [✓] §5.2.1p3 — In source files, there shall be some way of indicating the end of each line; the standard treats an end-of-line indicator as a single new-line character. — mcc:✓ gcc:✓ clang:✓
- [✓] §5.2.1p3 — In the basic execution character set there shall be control characters representing alert, backspace, carriage return, and new line. — mcc:✓ gcc:✓ clang:✓ (`\a \b \r \n` all defined; see §5.2.2)
- §5.2.1p3 — If any other characters are encountered in a source file (except in an identifier, character constant, string literal, header name, comment, or never-converted preprocessing token), the behavior is undefined. — (UB; not separately testable)
- §5.2.1p4 — A *letter* is an uppercase or lowercase Latin letter as defined above; the term excludes letters of other alphabets. — (spec; definition, not separately testable)
- [✓] §5.2.1p5 — (C11) The universal character name construct (`\u`, `\U`) provides a way to name other characters (see §6.4.3). — mcc:✓ gcc:✓ clang:✓ (extended characters usable in identifiers `int é=5;` and string literals `"café"` compile and run in all three; mcc defines `__STDC_ISO_10646__ == 201706L`)

##### §5.2.1.1 Trigraph sequences

> Trigraphs are normative in both C99 and C11. All three compilers default trigraphs OFF in their
> modern modes and require `-trigraphs` to enable them — a documented, shared deviation.

- [✓] §5.2.1.1p1 — All occurrences of the nine trigraph sequences are replaced with the corresponding single character: `??=`→`#`; `??(`→`[`; `??/`→`\`; `??)`→`]`; `??'`→`^`; `??<`→`{`; `??!`→`|`; `??>`→`}`; `??-`→`~`. — mcc:✓ gcc:✓ clang:✓ (under `-trigraphs`, mcc replaces `??<`/`??>` to `{`/`}`; without it mcc, gcc, and clang all leave them unreplaced — consensus)
- [✓] §5.2.1.1p2 — No other trigraph sequences exist; each `?` that does not begin a listed trigraph is not changed. — mcc:✓ gcc:✓ clang:✓
- [✓] §5.2.1.1p3 (EXAMPLE 1) — `??=define arraycheck(a,b) a??(b??) ??!??! b??(a??)` becomes `#define arraycheck(a,b) a[b] || b[a]`. — mcc:✓ gcc:✓ clang:✓ (under `-trigraphs`)
- [✓] §5.2.1.1p4 (EXAMPLE 2) — `printf("Eh???/n");` becomes `printf("Eh?\n");` after replacing `??/` with `\`. — mcc:✓ gcc:✓ clang:✓ (under `-trigraphs`, all three emit bytes `E h ? \n`)

##### §5.2.1.2 Multibyte characters

- [✓] §5.2.1.2p1 — The source character set may contain multibyte characters; the execution character set may also, and need not share the source encoding. — mcc:✓ gcc:✓ clang:✓ (UTF-8 multibyte in source accepted)
- [✓] §5.2.1.2p1 — For both sets: the basic character set shall be present and each basic character encoded as a single byte; presence/meaning/representation of additional members is locale-specific. — mcc:✓ gcc:✓ clang:✓
- §5.2.1.2p1 — A multibyte character set may have a *state-dependent encoding* with an *initial shift state* and other *shift states*. — (spec; locale property, not separately testable)
- §5.2.1.2p1 — While in the initial shift state, all single-byte characters retain their usual interpretation and do not alter the shift state; subsequent-byte interpretation depends on the current shift state. — (spec; not separately testable)
- [✓] §5.2.1.2p2 — A byte with all bits zero is a null character independent of shift state, and shall not occur in the second or subsequent bytes of a multibyte character. — mcc:✓ gcc:✓ clang:✓ (UTF-8 guarantees this)
- §5.2.1.2p3 — For source files, an identifier, comment, string literal, character constant, or header name shall begin and end in the initial shift state and consist of valid multibyte characters. — (spec; locale property, not separately testable)

#### §5.2.2 Character display semantics

- §5.2.2p1 — The *active position* is the location where the next character output by `fputc` would appear. — (spec; definition, not separately testable)
- §5.2.2p1 — Writing a printing character (per `isprint`) displays a graphic at the active position then advances the active position; line-writing direction is locale-specific. — (spec; display-device behavior, not separately testable)
- §5.2.2p1 — If the active position is at the final position of a line, the behavior of the display device is unspecified. — (unspecified; not separately testable)
- [✓] §5.2.2p2 — `\a` (alert) produces an audible or visible alert without changing the active position. — mcc:✓ gcc:✓ clang:✓ (`'\a'` == 7 in all three)
- [✓] §5.2.2p2 — `\b` (backspace) moves the active position to the previous position; at the initial position, display behavior is unspecified. — mcc:✓ gcc:✓ clang:✓ (`'\b'` == 8)
- [✓] §5.2.2p2 — `\f` (form feed) moves the active position to the initial position at the start of the next logical page. — mcc:✓ gcc:✓ clang:✓ (`'\f'` == 12)
- [✓] §5.2.2p2 — `\n` (new line) moves the active position to the initial position of the next line. — mcc:✓ gcc:✓ clang:✓ (`'\n'` == 10)
- [✓] §5.2.2p2 — `\r` (carriage return) moves the active position to the initial position of the current line. — mcc:✓ gcc:✓ clang:✓ (`'\r'` == 13)
- [✓] §5.2.2p2 — `\t` (horizontal tab) moves the active position to the next horizontal tab stop; at/past the last, display behavior is unspecified. — mcc:✓ gcc:✓ clang:✓ (`'\t'` == 9)
- [✓] §5.2.2p2 — `\v` (vertical tab) moves the active position to the next vertical tab stop; at/past the last, display behavior is unspecified. — mcc:✓ gcc:✓ clang:✓ (`'\v'` == 11)
- [✓] §5.2.2p3 — Each of these escape sequences shall produce a unique implementation-defined value storable in a single `char`. — mcc:✓ gcc:✓ clang:✓ (the seven values `7 8 12 10 13 9 11` are pairwise distinct and identical across all three)
- §5.2.2p3 — The external representations in a text file need not be identical to the internal representations and are outside this Standard's scope. — (spec; not separately testable)

#### §5.2.3 Signals and interrupts

- [✓] §5.2.3p1 — Functions shall be implementable such that they may be interrupted at any time by a signal, or called by a signal handler, or both, with no alteration to earlier still-active invocations' control flow, return values, or automatic objects. — mcc:✓ gcc:✓ clang:✓ (mcc uses a per-invocation stack frame; reentrancy holds, consistent with gcc/clang)
- [✓] §5.2.3p1 — All such objects shall be maintained outside the *function image* (the instructions composing the executable representation of a function) on a per-invocation basis. — mcc:✓ gcc:✓ clang:✓ (automatic objects live on the stack, not in the code image)

#### §5.2.4 Environmental limits

- §5.2.4p1 — Both the translation and execution environments constrain implementations; this subclause summarizes the language-related environmental limits (library limits are in clause 7). — (spec; not separately testable)

##### §5.2.4.1 Translation limits

> All values shown are the C99/C11 minimums. mcc, like gcc and clang, imposes no fixed low limits;
> every spot-probe well past the minimum translated successfully.

- [✓] §5.2.4.1p1 — The implementation shall be able to translate and execute at least one program containing at least one instance of every one of the following limits. — mcc:✓ gcc:✓ clang:✓ (spot-probes below all pass)
- [✓] §5.2.4.1p1 — 127 nesting levels of blocks. — mcc:✓ gcc:✓ clang:✓ (130 nested blocks compile in all three)
- [✓] §5.2.4.1p1 — 63 nesting levels of conditional inclusion. — mcc:✓ gcc:✓ clang:✓
- [✓] §5.2.4.1p1 — 12 pointer, array, and function declarators modifying an arithmetic/structure/union/void type in a declaration. — mcc:✓ gcc:✓ clang:✓
- [✓] §5.2.4.1p1 — 63 nesting levels of parenthesized declarators within a full declarator. — mcc:✓ gcc:✓ clang:✓
- [✓] §5.2.4.1p1 — 63 nesting levels of parenthesized expressions within a full expression. — mcc:✓ gcc:✓ clang:✓ (70 nested parens compile in all three)
- [✓] §5.2.4.1p1 — 63 significant initial characters in an internal identifier or macro name (each UCN or extended source character counts as one). (C90 value was 31.) — mcc:✓ gcc:✓ clang:✓ (all three treat the full identifier as significant)
- [✓] §5.2.4.1p1 — 31 significant initial characters in an external identifier (UCN char-counts per the rule). (C90 value was 6, case-insensitive.) — mcc:✓ gcc:✓ clang:✓ (two externals differing at char 40 stay distinct in all three)
- [✓] §5.2.4.1p1 — 4095 external identifiers in one translation unit. (C90 value was 511.) — mcc:✓ gcc:✓ clang:✓
- [✓] §5.2.4.1p1 — 511 identifiers with block scope declared in one block. (C90 value was 127.) — mcc:✓ gcc:✓ clang:✓
- [✓] §5.2.4.1p1 — 4095 macro identifiers simultaneously defined in one preprocessing translation unit. (C90 value was 1024.) — mcc:✓ gcc:✓ clang:✓
- [✓] §5.2.4.1p1 — 127 parameters in one function definition. — mcc:✓ gcc:✓ clang:✓
- [✓] §5.2.4.1p1 — 127 arguments in one function call. — mcc:✓ gcc:✓ clang:✓
- [✓] §5.2.4.1p1 — 127 parameters in one macro definition. — mcc:✓ gcc:✓ clang:✓
- [✓] §5.2.4.1p1 — 127 arguments in one macro invocation. — mcc:✓ gcc:✓ clang:✓
- [✓] §5.2.4.1p1 — 4095 characters in a logical source line. — mcc:✓ gcc:✓ clang:✓
- [✓] §5.2.4.1p1 — 4095 characters in a character string literal or wide string literal (after concatenation). — mcc:✓ gcc:✓ clang:✓ (a 4100-byte string literal compiles in all three)
- [✓] §5.2.4.1p1 — 65535 bytes in an object (hosted only). (C90 value was 32767.) — mcc:✓ gcc:✓ clang:✓
- [✓] §5.2.4.1p1 — 15 nesting levels for `#include`d files. — mcc:✓ gcc:✓ clang:✓
- [✓] §5.2.4.1p1 — 1023 `case` labels for a `switch` (excluding nested switches). (C90 value was 257.) — mcc:✓ gcc:✓ clang:✓
- [✓] §5.2.4.1p1 — 1023 members in a single structure or union. (C90 value was 127.) — mcc:✓ gcc:✓ clang:✓
- [✓] §5.2.4.1p1 — 1023 enumeration constants in a single enumeration. (C90 value was 127.) — mcc:✓ gcc:✓ clang:✓
- [✓] §5.2.4.1p1 — 63 levels of nested structure/union definitions in a single struct-declaration-list. (C90 value was 15.) — mcc:✓ gcc:✓ clang:✓
- [✓] §5.2.4.1p1 (footnote) — Implementations should avoid imposing fixed translation limits whenever possible. — mcc:✓ gcc:✓ clang:✓ (mcc imposes no fixed low limits in any probe)

##### §5.2.4.2 Numerical limits

- [✓] §5.2.4.2p1 — A conforming implementation shall document all the limits in this subclause, specified in `<limits.h>` and `<float.h>`. — mcc:✓ gcc:✓ clang:✓ (mcc ships both headers; values verified below)
- [✓] §5.2.4.2p1 — (C11) Additional limits are specified in `<stdint.h>` (see §7.20.2/§7.20.3). — mcc:✓ gcc:✓ clang:✓ (mcc bundles `<stdint.h>`)

###### §5.2.4.2.1 Sizes of integer types `<limits.h>`

> mcc's `<limits.h>` values are byte-for-byte identical to gcc's and clang's on x86-64 Linux.

- [✓] §5.2.4.2.1p1 — The values below shall be replaced by constant expressions suitable for use in `#if` directives. — mcc:✓ gcc:✓ clang:✓ (`#if INT_MAX < 32767` / `#if ULLONG_MAX < …ULL` evaluate in all three)
- [✓] §5.2.4.2.1p1 — Except for `CHAR_BIT` and `MB_LEN_MAX`, the macros shall have the same type as the corresponding object after integer promotions. — mcc:✓ gcc:✓ clang:✓
- [✓] §5.2.4.2.1p1 — Their implementation-defined values shall be equal or greater in magnitude than those shown, with the same sign. — mcc:✓ gcc:✓ clang:✓
- [✓] §5.2.4.2.1p1 — `CHAR_BIT` ≥ `8`. — mcc:✓ gcc:✓ clang:✓ (all: `8`)
- [✓] §5.2.4.2.1p1 — `SCHAR_MIN` ≤ `-127`. — mcc:✓ gcc:✓ clang:✓ (all: `-128`)
- [✓] §5.2.4.2.1p1 — `SCHAR_MAX` ≥ `+127`. — mcc:✓ gcc:✓ clang:✓ (all: `127`)
- [✓] §5.2.4.2.1p1 — `UCHAR_MAX` ≥ `255`. — mcc:✓ gcc:✓ clang:✓ (all: `255`)
- [✓] §5.2.4.2.1p1 — `CHAR_MIN` — `SCHAR_MIN` if `char` is signed, else `0`. — mcc:✓ gcc:✓ clang:✓ (all: `-128`, signed `char`)
- [✓] §5.2.4.2.1p1 — `CHAR_MAX` — `SCHAR_MAX` if `char` is signed, else `UCHAR_MAX`. — mcc:✓ gcc:✓ clang:✓ (all: `127`)
- [✓] §5.2.4.2.1p1 — `MB_LEN_MAX` ≥ `1`. — mcc:✓ gcc:✓ clang:✓ (all: `16`)
- [✓] §5.2.4.2.1p1 — `SHRT_MIN` ≤ `-32767`. — mcc:✓ gcc:✓ clang:✓ (all: `-32768`)
- [✓] §5.2.4.2.1p1 — `SHRT_MAX` ≥ `+32767`. — mcc:✓ gcc:✓ clang:✓ (all: `32767`)
- [✓] §5.2.4.2.1p1 — `USHRT_MAX` ≥ `65535`. — mcc:✓ gcc:✓ clang:✓ (all: `65535`)
- [✓] §5.2.4.2.1p1 — `INT_MIN` ≤ `-32767`. — mcc:✓ gcc:✓ clang:✓ (all: `-2147483648`)
- [✓] §5.2.4.2.1p1 — `INT_MAX` ≥ `+32767`. — mcc:✓ gcc:✓ clang:✓ (all: `2147483647`)
- [✓] §5.2.4.2.1p1 — `UINT_MAX` ≥ `65535`. — mcc:✓ gcc:✓ clang:✓ (all: `4294967295`)
- [✓] §5.2.4.2.1p1 — `LONG_MIN` ≤ `-2147483647`. — mcc:✓ gcc:✓ clang:✓ (all: `-9223372036854775808`)
- [✓] §5.2.4.2.1p1 — `LONG_MAX` ≥ `+2147483647`. — mcc:✓ gcc:✓ clang:✓ (all: `9223372036854775807`)
- [✓] §5.2.4.2.1p1 — `ULONG_MAX` ≥ `4294967295`. — mcc:✓ gcc:✓ clang:✓ (all: `18446744073709551615`)
- [✓] §5.2.4.2.1p1 — `LLONG_MIN` ≤ `-9223372036854775807`. (C99/C11; absent in C90.) — mcc:✓ gcc:✓ clang:✓ (all: `-9223372036854775808`)
- [✓] §5.2.4.2.1p1 — `LLONG_MAX` ≥ `+9223372036854775807`. (C99/C11; absent in C90.) — mcc:✓ gcc:✓ clang:✓ (all: `9223372036854775807`)
- [✓] §5.2.4.2.1p1 — `ULLONG_MAX` ≥ `18446744073709551615`. (C99/C11; absent in C90.) — mcc:✓ gcc:✓ clang:✓ (all: `18446744073709551615`)
- [✓] §5.2.4.2.1p2 — If a `char` value is treated as signed, `CHAR_MIN`==`SCHAR_MIN` and `CHAR_MAX`==`SCHAR_MAX`; otherwise `CHAR_MIN`==`0` and `CHAR_MAX`==`UCHAR_MAX`. — mcc:✓ gcc:✓ clang:✓ (all signed-char: `CHAR_MIN==SCHAR_MIN==-128`)
- [✓] §5.2.4.2.1p2 — `UCHAR_MAX` shall equal `2^CHAR_BIT − 1`. — mcc:✓ gcc:✓ clang:✓ (`255 == 2^8 − 1`)
- [ ] §5.2.4.2.1p2 (footnote) — See §6.2.5: `char` has the same range/representation/behavior as either `signed char` or `unsigned char`, but is a distinct type. — (spec; cross-reference, see §6.2.5 section)

###### §5.2.4.2.2 Characteristics of floating types `<float.h>`

> mcc's `<float.h>` (including every C11 addition — `*_HAS_SUBNORM`, `*_DECIMAL_DIG`, `*_TRUE_MIN`)
> is byte-for-byte identical to gcc's and clang's output for an x86-64 IEC-60559 `float`/`double`/
> 80-bit `long double` configuration.

- §5.2.4.2.2p1 — Floating-type characteristics are defined via a model describing a floating-point representation. — (spec; model, not separately testable)
- §5.2.4.2.2p1 — A *floating-point number* x is defined by x = s·b^e·Σ f_k·b^(−k), e_min ≤ e ≤ e_max, with s the sign, b the radix, e the exponent, p the precision, and f_k the significand digits. — (spec; model, not separately testable)
- §5.2.4.2.2p2 — (C11) A floating-point number that is zero or whose magnitude is in [b^(e_min−1), b^(e_max)] is *normalized* if f_1 > 0. — (spec; definition, not separately testable)
- §5.2.4.2.2p3 — (C11) x is *subnormal* if 0 < x < b^(e_min−1) and f_1 = 0; *unnormalized* if f_1 = 0 and not subnormal; b, p, e_min, e_max are fixed per type. — (spec; definition, not separately testable)
- §5.2.4.2.2p4 — The accuracy of `+ - * /` and of `<math.h>`/`<complex.h>` floating-result functions, and of float↔string conversions in `<stdio.h>`/`<stdlib.h>`/`<wchar.h>`, is implementation-defined; the implementation may state the accuracy is unknown. — (implementation-defined; not separately testable)
- [✓] §5.2.4.2.2p5 — All integer values in `<float.h>` except `FLT_ROUNDS` shall be `#if`-usable constant expressions; all floating values shall be constant expressions. — mcc:✓ gcc:✓ clang:✓ (`#if DECIMAL_DIG<10` / `#if FLT_EVAL_METHOD<0` evaluate in all three; note `FLT_ROUNDS` is correctly NOT guaranteed `#if`-usable — clang errors on it in `#if` since it expands to `__builtin_flt_rounds()`, while mcc and gcc happen to keep it `#if`-usable; this is a documented latitude, not a defect)
- [✓] §5.2.4.2.2p5 — All macros except `DECIMAL_DIG`, `FLT_EVAL_METHOD`, `FLT_RADIX`, `FLT_ROUNDS` have separate `FLT_`/`DBL_`/`LDBL_` names. — mcc:✓ gcc:✓ clang:✓ (all three families present in mcc's header)
- [✓] §5.2.4.2.2p6 — `FLT_ROUNDS` characterizes the addition rounding mode: `-1` indeterminable, `0` toward zero, `1` to nearest, `2` toward +∞, `3` toward −∞. — mcc:✓ gcc:✓ clang:✓ (all: `1`)
- [✓] §5.2.4.2.2p6 — Except for assignment and cast, operator/conversion/constant values may be evaluated to a greater range and precision than the type; `FLT_EVAL_METHOD` characterizes this. — mcc:✓ gcc:✓ clang:✓
- [✓] §5.2.4.2.2p7 — `FLT_EVAL_METHOD` characterizes evaluation formats: `-1` indeterminable; `0` just to the type; `1` float/double→double, long double→long double; `2` all→long double. — mcc:✓ gcc:✓ clang:✓ (all: `0`)
- [✓] §5.2.4.2.2p8 — (C11) `FLT_HAS_SUBNORM`/`DBL_HAS_SUBNORM`/`LDBL_HAS_SUBNORM`: `-1` indeterminable, `0` absent, `1` present. — mcc:✓ gcc:✓ clang:✓ (all: `1 1 1`)
- [✓] §5.2.4.2.2p9 — `FLT_RADIX` ≥ `2`. — mcc:✓ gcc:✓ clang:✓ (all: `2`)
- [✓] §5.2.4.2.2p9 — `FLT_MANT_DIG`/`DBL_MANT_DIG`/`LDBL_MANT_DIG` — number of base-`FLT_RADIX` significand digits, `p`. — mcc:✓ gcc:✓ clang:✓ (all: `24 / 53 / 64`)
- [✓] §5.2.4.2.2p9 — `DECIMAL_DIG` ≥ `10`. — mcc:✓ gcc:✓ clang:✓ (all: `21`)
- [✓] §5.2.4.2.2p9 — (C11) `FLT_DECIMAL_DIG` ≥ 6, `DBL_DECIMAL_DIG` ≥ 10, `LDBL_DECIMAL_DIG` ≥ 10. — mcc:✓ gcc:✓ clang:✓ (all: `9 / 17 / 21`)
- [✓] §5.2.4.2.2p9 — `FLT_DIG` ≥ 6, `DBL_DIG` ≥ 10, `LDBL_DIG` ≥ 10. — mcc:✓ gcc:✓ clang:✓ (all: `6 / 15 / 18`)
- [✓] §5.2.4.2.2p9 — `FLT_MIN_EXP`/`DBL_MIN_EXP`/`LDBL_MIN_EXP` = `e_min`. — mcc:✓ gcc:✓ clang:✓ (all: `FLT_MIN_EXP == -125`)
- [✓] §5.2.4.2.2p9 — `FLT_MIN_10_EXP`/… ≤ `−37`. — mcc:✓ gcc:✓ clang:✓ (all: `FLT_MIN_10_EXP == -37`)
- [✓] §5.2.4.2.2p9 — `FLT_MAX_EXP`/`DBL_MAX_EXP`/`LDBL_MAX_EXP` = `e_max`. — mcc:✓ gcc:✓ clang:✓ (all: `FLT_MAX_EXP == 128`)
- [✓] §5.2.4.2.2p9 — `FLT_MAX_10_EXP`/… ≥ `+37`. — mcc:✓ gcc:✓ clang:✓ (all: `FLT_MAX_10_EXP == 38`)
- [✓] §5.2.4.2.2p10 — `FLT_MAX`/`DBL_MAX`/`LDBL_MAX` ≥ `1×10^+37`. — mcc:✓ gcc:✓ clang:✓ (all: `FLT_MAX≈3.40282e+38`, `DBL_MAX≈1.79769e+308`)
- [ ] §5.2.4.2.2p11 — The following shall have values ≤ those shown. — (spec; header introductory clause)
- [✓] §5.2.4.2.2p11 — `FLT_EPSILON` ≤ `1×10^−5`. — mcc:✓ gcc:✓ clang:✓ (all: `≈1.19209e-07`)
- [✓] §5.2.4.2.2p11 — `DBL_EPSILON` ≤ `1×10^−9`; `LDBL_EPSILON` ≤ `1×10^−9`. — mcc:✓ gcc:✓ clang:✓ (all: `DBL_EPSILON≈2.22045e-16`)
- [✓] §5.2.4.2.2p11 — `FLT_MIN`/`DBL_MIN`/`LDBL_MIN` ≤ `1×10^−37` (minimum normalized positive). — mcc:✓ gcc:✓ clang:✓ (all: `FLT_MIN≈1.17549e-38`)
- [✓] §5.2.4.2.2p11 — (C11) `FLT_TRUE_MIN`/`DBL_TRUE_MIN`/`LDBL_TRUE_MIN` ≤ `1×10^−37` (minimum positive, subnormal if supported). — mcc:✓ gcc:✓ clang:✓ (all: `FLT_TRUE_MIN≈1.4013e-45`, `DBL_TRUE_MIN≈4.94066e-324`, `LDBL_TRUE_MIN≈3.6452e-4951`)
- [✓] §5.2.4.2.2p12 (EXAMPLE) — Example IEC 60559 single/double values illustrate the model. — mcc:✓ gcc:✓ clang:✓ (mcc's actual single/double values match the IEC-60559 example, e.g. `FLT_EPSILON==1.19209290E-07F`)
- §5.2.4.2.2p13 (Recommended practice) — Conversions and arithmetic should correspond to one of the documented `FLT_EVAL_METHOD` modes. — (recommended practice; not separately testable)
- §5.2.4.2.2 (footnote) — The floating-point model only clarifies the description and does not require identical implementation arithmetic. — (spec; not separately testable)

###### §5.2.4.2.3 Characteristics of decimal floating types

- [ ] §5.2.4.2.3 — (Not part of base C99/C11; decimal floating types live in the optional TS/Annex. Placeholder only — out of scope for base C11.) — mcc:n/a gcc:n/a clang:n/a (none of the three define `<float.h>` decimal `_Decimal*` limits in base mode)

### §6.11 Future language directions

#### §6.11.1 Floating types

- §6.11.1p1 — Future standardization may include additional floating-point types with greater range/precision than `long double`. — (spec; future direction, not separately testable)

#### §6.11.2 Linkages of identifiers

- [ ] §6.11.2p1 — Declaring an identifier with internal linkage at file scope without `static` is an obsolescent feature. — (obsolescent note; no diagnostic required — none of mcc/gcc/clang warn by default)

#### §6.11.3 External names

- [✓] §6.11.3p1 — Restricting an external name's significance to fewer than 255 characters is an obsolescent concession to existing implementations. — mcc:✓ gcc:✓ clang:✓ (all three keep external names significant well past 255 chars)

#### §6.11.4 Character escape sequences

- §6.11.4p1 — Lowercase letters as escape sequences are reserved for future standardization; other characters may be used in extensions. — (spec; future direction, not separately testable)

#### §6.11.5 Storage-class specifiers

- [✓] §6.11.5p1 — Placing a storage-class specifier other than at the beginning of the declaration specifiers (e.g. `int static x;`) is an obsolescent feature. — mcc:✓ gcc:✓ clang:✓ (all three accept `int static x;`; the obsolescent form draws no diagnostic from any, even under `-pedantic`)

#### §6.11.6 Function declarators

- [✓] §6.11.6p1 — Using function declarators with empty parentheses (non-prototype form, `int f();`) is an obsolescent feature. — mcc:✓ gcc:✓ clang:✓ (all three accept it; under `-Wstrict-prototypes` mcc emits "function declaration isn't a prototype" for the obsolescent empty-paren declarator, matching gcc and clang, which warn "function declaration isn't a prototype / without a prototype is deprecated")

#### §6.11.7 Function definitions

- [✓] §6.11.7p1 — Using function definitions with separate parameter identifier and declaration lists (K&R form) is an obsolescent feature. — mcc:✓ gcc:✓ clang:✓ (all three accept the K&R definition `int f(a) int a; {…}`; clang additionally warns `-Wdeprecated-non-prototype`, mcc and gcc accept silently — the obsolescent diagnostic is optional, so all conform)

#### §6.11.8 Pragma directives

- [✓] §6.11.8p1 — Pragmas whose first preprocessing token is `STDC` are reserved for future standardization. — mcc:✓ gcc:✓ clang:✓ (mcc accepts `#pragma STDC FP_CONTRACT/FENV_ACCESS/CX_LIMITED_RANGE` silently; gcc emits an `-Wunknown-pragmas` "ignoring" warning for some, clang accepts silently — all conform, mcc matches clang)

#### §6.11.9 Predefined macro names

- [✓] §6.11.9p1 — Macro names beginning with `__STDC_` are reserved for future standardization. — mcc:✓ gcc:✓ clang:✓ (mcc predefines `__STDC__`, `__STDC_VERSION__`, `__STDC_HOSTED__`, `__STDC_IEC_559__`, `__STDC_IEC_559_COMPLEX__`, `__STDC_ISO_10646__`)

### Added gaps & cross-checks (not in the consolidated draft)

#### Predefined conformance macros (§6.10.8 / §6.11.9-adjacent)

- [✓] §6.10.8.1 — `__STDC__` shall be defined as the integer constant `1`. — mcc:✓ gcc:✓ clang:✓ (all: `1`)
- [✓] §6.10.8.1 — `__STDC_HOSTED__` shall be `1` for a hosted implementation, `0` for freestanding. — mcc:✓ gcc:✓ clang:✓ (all: `1` hosted, `0` under `-ffreestanding`)
- [~] §6.10.8.1 — `__STDC_VERSION__` shall reflect the supported standard revision. — mcc:~ gcc:✓ clang:✓ (mcc: `199901L` = C99, while gcc defaults to `202311L` and clang to `201710L`; mcc advertises C99 yet ships the full C11 freestanding header set, `<stdatomic.h>`, `<threads.h>`, `_Atomic`, `_Noreturn`, and `__STDC_ISO_10646__ == 201706L` — its advertised version under-states its actual C11 feature coverage)
- [✓] §6.10.8.2 — `__STDC_ISO_10646__` (C95/C11) indicates `wchar_t` holds ISO/IEC 10646 values of the given yyyymmL edition. — mcc:✓ gcc:✓ clang:✓ (all: `201706L`)
- [✓] §6.10.8.3 — `__STDC_UTF_16__` (C11) shall be defined (value `1`) when `char16_t` values are UTF-16 encoded. — mcc:✓ gcc:✓ clang:✓ (mcc predefines `__STDC_UTF_16__ == 1`, matching gcc and clang)
- [✓] §6.10.8.3 — `__STDC_UTF_32__` (C11) shall be defined (value `1`) when `char32_t` values are UTF-32 encoded. — mcc:✓ gcc:✓ clang:✓ (mcc predefines `__STDC_UTF_32__ == 1`, matching gcc and clang)
- [✓] §6.10.8.3 — `__STDC_IEC_559__` (C99/C11, conditional feature for IEC 60559 floating point) indicates IEC-60559-conforming `float`/`double`. — mcc:✓ gcc:✓ clang:✓ (all define `__STDC_IEC_559__ == 1`)
- [✓] §6.10.8.3 — `__STDC_IEC_559_COMPLEX__` (C99/C11) indicates IEC-60559-compatible complex arithmetic. — mcc:✓ gcc:✓ clang:✓ (all define `__STDC_IEC_559_COMPLEX__ == 1`)
- [✓] §6.10.8.3 — `__STDC_NO_ATOMICS__` (C11) shall be defined to `1` if the implementation does NOT support atomic types/`<stdatomic.h>`. — mcc:✓ gcc:✓ clang:✓ (none of the three define it, consistent with all three supporting atomics — verified by a working `<stdatomic.h>` program in mcc)
- [✓] §6.10.8.3 — `__STDC_NO_THREADS__` (C11) shall be defined to `1` if the implementation does NOT provide `<threads.h>`. — mcc:✓ gcc:✓ clang:✓ (none define it; mcc's `<threads.h>` `thrd_create`/`thrd_join` program runs)
- [✓] §6.10.8.3 — `__STDC_NO_VLA__` / `__STDC_NO_COMPLEX__` (C11) shall be defined to `1` if VLAs / `<complex.h>` are unsupported. — mcc:✓ gcc:✓ clang:✓ (none define either; mcc supports both VLAs and `<complex.h>`)

#### Freestanding header content (§4p6 detail)

- [✓] §4p6 — `<stdbool.h>` shall define `bool`, `true`, `false`, and `__bool_true_false_are_defined`. — mcc:✓ gcc:✓ clang:✓ (mcc: `bool`→`_Bool`, `true`→`1`, `false`→`0`, `__bool_true_false_are_defined`→`1`)
- [✓] §4p6 — `<stdnoreturn.h>` (C11) shall define `noreturn` as `_Noreturn`. — mcc:✓ gcc:✓ clang:✓ (mcc: `#define noreturn _Noreturn`)
- [✓] §4p6 — `<iso646.h>` shall define the 11 alternative-spelling macros `and`, `and_eq`, `bitand`, `bitor`, `compl`, `not`, `not_eq`, `or`, `or_eq`, `xor`, `xor_eq`. — mcc:✓ gcc:✓ clang:✓ (all 11 present in mcc's `<iso646.h>`)
- [✓] §4p6 — `<stdalign.h>` (C11) shall define `alignas`/`alignof` and `__alignas_is_defined`/`__alignof_is_defined`. — mcc:✓ gcc:✓ clang:✓ (mcc bundles `<stdalign.h>`; `-ffreestanding` include succeeds)

#### ATOMIC initialization macro (§7.17.2)

- [✓] §7.17.2 — `<stdatomic.h>` provides `ATOMIC_VAR_INIT` (C11; deprecated in C17, removed in C23) and `ATOMIC_FLAG_INIT`. — mcc:✓ gcc:✗(C23 default) clang:~ (mcc still provides the C11 `ATOMIC_VAR_INIT`; gcc 15 in its default C23 mode has removed it — `ATOMIC_VAR_INIT` is undeclared there; clang keeps it but marks it deprecated. mcc's behavior matches C11.)


## §6.2.1-§6.2.4 Scopes, linkages, name spaces, storage durations

> mcc status: 0 mcc-gaps (no `✗`/`~` mcc tags in this section). Core scope/linkage/name-space/storage-duration rules conform and match gcc==clang on point-of-declaration, label/tag/member name spaces, internal-vs-external linkage merges, automatic/static/VLA/compound-literal lifetimes, and VLA placement constraints. mcc diagnoses a typedef name redeclared as an ordinary object in the same scope (ordinary-namespace clash), rejects a non-static file-scope redeclaration following a static one (6.2.2p7), and ships a `<threads.h>` that defines the `thread_local` convenience macro — all matching the gcc==clang consensus. mcc still emits no warning for a tag whose only declaration sits in a function-prototype parameter list, but that diagnostic is informative/optional (§I), so it is conforming and not a tagged divergence.

### §6.2.1 Scopes of identifiers

- §6.2.1p1 — Recognize that one identifier may designate different kinds of entities (object, function, tag/member of a struct/union/enum, typedef name, label, macro, macro parameter); at each occurrence the syntactic context plus the active name space pick which. — (spec; not separately testable)
- [ ] §6.2.1p2 — Restrict the visibility of each entity an identifier denotes to a contiguous region of program text (its scope), and support exactly the four scope kinds: function, file, block, function-prototype. — (spec; structural)
- [✓] §6.2.1p3 — Give label names function scope: a label is visible anywhere in its enclosing function body and a `goto` may target a label declared later or in an enclosing block. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.2.1p3 — Make a label name local to its function so the same label spelling can be reused independently in different functions without conflict. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.2.1p4 — Assign file scope to an identifier whose declarator/type-specifier sits outside every block and parameter list. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.2.1p4 — Assign block scope to an identifier declared inside a block or in the parameter list of a function *definition*; its scope ends with the block. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.2.1p4 — Assign function-prototype scope to a parameter name in a declaration that is not part of a definition, terminating that scope at the end of the function declarator (the name is not visible afterward). — mcc:✓ gcc:✓ clang:✓ (probe `void f(int xyzzy); int g(void){return xyzzy;}` → undeclared on all three)
- [✓] §6.2.1p4 — When two declarations of the same identifier in the same name space have overlapping scopes, hide the outer entity throughout the inner scope; uses inside the inner scope bind to the inner entity. — mcc:✓ gcc:✓ clang:✓ (block-scope object shadowing file-scope object, and inner-block redeclaration both behave correctly)
- §6.2.1p5 — When the standard's prose says "identifier" to mean an entity, resolve it to the entity in the relevant name space whose declaration is visible at that point. — (spec; not separately testable)
- §6.2.1p6 — Treat two identifiers as having the same scope iff their scopes end at the same point. — (spec; not separately testable)
- [✓] §6.2.1p7 — Begin a struct/union/enum tag's scope immediately after the tag appears in the specifier that declares it (so a tag may refer to itself via a pointer). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.2.1p7 — Begin an enumeration constant's scope just after its defining enumerator, so a later enumerator may use it (`enum{A=10,B=A+1}`) but an enumerator may not reference itself (`enum{X=X+1}` is an error). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.2.1p7 — Begin any other identifier's scope only after its declarator completes, so an initializer/size that names the same identifier binds to the *new* (block) entity (`int g=g;`) and a file-scope `int foo[sizeof(foo)];` is rejected as use-before-declaration. — mcc:✓ gcc:✓ clang:✓

### §6.2.2 Linkages of identifiers

- §6.2.2p1 — Use linkage so that the same identifier declared in different scopes, or several times in one scope, can be made to denote the same object or function; support the three linkage kinds external, internal, and none. — (spec; not separately testable)
- [✓] §6.2.2p2 — Within one translation unit, make every declaration of an internal-linkage identifier denote the same entity; make an external-linkage identifier denote the same entity across the whole program. — mcc:✓ gcc:✓ clang:✓ (block-scope `extern int v;` resolves to the file-scope definition of `v`)
- [✓] §6.2.2p3 — Give a file-scope object or function declared with `static` internal linkage. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.2.2p4 — For an identifier declared `extern`, inherit the linkage of a prior visible file-scope declaration (internal stays internal: `static int x; extern int x;` keeps internal linkage); with no prior visible declaration, or a prior one with no linkage, give it external linkage. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.2.2p5 — Treat a function identifier declared with no storage-class specifier as if `extern` (external linkage), and give a file-scope object with no storage-class specifier external linkage. — mcc:✓ gcc:✓ clang:✓ (block-scope `int puts(const char*);` links to the external `puts`)
- [✓] §6.2.2p6 — Give *no linkage* to: identifiers that are not objects/functions (typedef names, enum constants, tags, members, labels), function parameters, and block-scope identifiers declared without `extern`; reject two no-linkage declarations of the same identifier in the same scope (`{int x; int x;}`). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.2.2p7 — Treat an identifier that ends up with both internal and external linkage in one translation unit as undefined behavior; gcc and clang reject `static int x; int x;` while mcc accepts it silently. — mcc:✓ gcc:✓ clang:✓ (mcc diagnoses the internal→external file-scope conflict — "non-static declaration of 'x' follows static declaration" — matching gcc/clang; the reverse order `int x; static int x;` is likewise rejected by all three)

### §6.2.3 Name spaces of identifiers

- [✓] §6.2.3p1 — Keep label names in their own name space, disambiguated by the `goto`/label-definition syntax, so a label may share a spelling with an object, tag, or typedef in the same scope (`{int x=0; x: ... goto x;}`). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.2.3p1 — Keep struct/union/enum tags in a single tag name space selected by the `struct`/`union`/`enum` keyword, so a tag and an ordinary identifier may share a name (`struct s{...}; int s;`), and a block-scope tag may shadow a file-scope tag of the same name. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.2.3p1 — Give each structure or union its own separate member name space reached only via `.`/`->`, so different aggregates may reuse member names and a member name never clashes with an object/tag/typedef of the same spelling. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.2.3p1 — Place all other identifiers (objects, functions, typedef names, and enumeration constants) in one shared "ordinary identifiers" name space, so two of these with the same name in the same scope conflict; gcc/clang reject `typedef int T; int T;` but mcc accepts it. — mcc:✓ gcc:✓ clang:✓ (mcc diagnoses a typedef name redeclared as an object in the same scope — "'T' redeclared as different kind of symbol" — matching gcc/clang; conflicting object redeclarations like `int x; double x;` are likewise caught)

### §6.2.4 Storage durations of objects

- [ ] §6.2.4p1 — Associate every object with one of four storage durations — static, thread (C11), automatic, allocated — that fix its lifetime; (C99: only static, automatic, allocated). — (spec; structural) `(C11)` adds thread duration
- [✓] §6.2.4p2 — Guarantee that during an object's lifetime its storage is reserved, it holds a constant address, and (for declared objects) it retains its last-stored value; treat any access outside the lifetime as undefined and treat a pointer to an object past its lifetime as indeterminate. — mcc:✓ gcc:✓ clang:✓ (lifetime/address semantics observable via static-retains-value and compound-literal probes; the UB cases are spec-only)
- [✓] §6.2.4p3 — Give static storage duration (lifetime = whole program run, single zero-or-constant initialization before startup) to any object with external/internal linkage or declared with `static`; a block-scope `static` keeps its value across calls. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.2.4p4 (C11) — Give thread storage duration to an object declared `_Thread_local` (lifetime = its thread's execution, initialized once before the thread starts, one instance per thread); accept `_Thread_local` at file scope, combined with `static`, and on a block-scope `static`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.2.4p4 (C11) — Reject `_Thread_local` on a block-scope object that is not also `static`/`extern` (a bare automatic thread-local is a constraint violation). — mcc:✓ gcc:✓ clang:✓
- [✓] §7.26.1 / §6.2.4p4 (C11) — Provide the `thread_local` convenience macro in `<threads.h>` expanding to `_Thread_local`. — mcc:✓ gcc:✓ clang:✓ (mcc's bundled `<threads.h>` defines `thread_local`, so `thread_local int t;` parses and compiles, matching gcc/clang)
- [✓] §6.2.4p5 — Give automatic storage duration to a block-scope object with no linkage and no `static`; reserve its storage on each entry to the block, release it when the block exits by any path, create a fresh instance on recursive/repeated entry, leave it indeterminate when uninitialized, and re-run any initializer every time the declaration is reached. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.2.4p5 — Give a compound literal that appears outside any function (file scope) static storage duration (usable as an initializer for a file-scope pointer), and one inside a block automatic storage duration tied to the enclosing block. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.2.4p7 — Begin a VLA object's lifetime at its declaration and end it when execution leaves the scope of the declaration; re-evaluate its size and allocate a fresh instance on each block entry (looping `int a[n]` yields per-iteration sizes), and leave its initial contents indeterminate. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.2.4p7 / §6.8.6.1 — Forbid jumping (via `goto`) into the scope of a variably modified declaration, since that would bypass establishing the VLA's lifetime. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.2.4p7 — Reject a variable-length array where automatic storage is not available: at file scope and for a `static` block-scope array (no VLA storage duration there). — mcc:✓ gcc:✓ clang:✓
- [ ] §6.2.4p8 — Provide allocated storage duration via the `malloc`-family functions, with lifetime from allocation until deallocation as specified in §7.22.3. — (spec; covered under §7.22.3)


## §6.2.5-§6.2.8 Types, representations, compatible/composite types, alignment

> mcc status: Core type system, integer/object representation, compatible & composite types, and the full C11 `_Alignof`/`_Alignas`/`_Atomic` surface all conform and match the gcc==clang consensus on every mechanical probe. The only standards-optional gap is extended integer types: mcc rejects `__int128`/`unsigned __int128` (gcc and clang accept them) — conforming, since extended integer types are optional, but a capability difference. `_Imaginary` (Annex G, optional) is rejected by all three.

### §6.2.5 Types

- §6.2.5p1 — Classify every type as either an object type (fully describes objects) or a function type (describes a function), modulo incompleteness. — (spec; not separately testable)
- [✓] §6.2.5p1 — Treat a type as incomplete when it describes objects but lacks the information to size them; track separately from object/function classification. — mcc:✓ gcc:✓ clang:✓ (incomplete `int a[];` later completed by `int a[10];` accepted by all)
- [✓] §6.2.5p19 — Provide `void`: an incomplete object type that can never be completed; reject declaring an object of type `void`. — mcc:✓ gcc:✓ clang:✓ (`void v;` rejected by all)
- [✓] §6.2.5p2 — Provide `_Bool`, an unsigned integer type large enough to store the two values 0 and 1. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.2.5p2 — When any scalar value is converted to `_Bool`, store 1 for every nonzero (including nonzero floating) value and 0 otherwise. — mcc:✓ gcc:✓ clang:✓ (`_Bool b=5`→1, `_Bool c=0.3`→1)
- [✓] §6.2.5p3 — Provide the three character types `char`, `signed char`, `unsigned char`, each occupying one byte (`sizeof==1`). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.2.5p3 — Make plain `char` a distinct type from both `signed char` and `unsigned char` even though it shares the range/representation of one of them. — mcc:✓ gcc:✓ clang:✓ (`_Generic` distinguishes all three)
- [✓] §6.2.5p15 — Make plain `char`'s signedness (signed-like vs unsigned-like behavior) implementation-defined. — mcc:✓ gcc:✓ clang:✓ (all three: `char` is signed on x86-64 → `(char)-1<0`)
- [✓] §6.2.5p4 — Provide the standard signed integer types `signed char`, `short int`, `int`, `long int`, `long long int`. — mcc:✓ gcc:✓ clang:✓
- [~] §6.2.5p4 — Permit (optional) implementation-defined extended signed integer types; standard + extended signed types form the signed integer types. — mcc:~ gcc:✓ clang:✓ (none required; mcc provides no extended signed type, gcc/clang accept `__int128`)
- [✓] §6.2.5p6 — For each signed integer type provide a corresponding unsigned integer type using the same amount of storage (and alignment). — mcc:✓ gcc:✓ clang:✓ (`sizeof(int)==sizeof(unsigned)`)
- [✓] §6.2.5p6 — Standard unsigned integer types = `_Bool` plus the unsigned counterparts of the standard signed types; standard + extended unsigned form the unsigned integer types. — mcc:✓ gcc:✓ clang:✓
- §6.2.5p7 — Treat `_Bool`, plain `char`, the signed integer types, the unsigned integer types, and the enumerated types collectively as the integer types. — (spec; not separately testable)
- [✓] §6.2.5p9 — Make unsigned arithmetic modular: a result that cannot be represented is reduced modulo 2^N, so unsigned computations never overflow. — mcc:✓ gcc:✓ clang:✓ (`UINT_MAX+1u==0`, `(unsigned char)255+1` wraps)
- [✓] §6.2.5p9 — Give a nonnegative value of a signed type the identical representation in the corresponding unsigned type (subrange representations coincide). — mcc:✓ gcc:✓ clang:✓ (bit-reinterpret of positive `int` equals `(unsigned)` cast)
- [✓] §6.2.5p10 — Provide the real floating types `float`, `double`, `long double`, with non-decreasing range/precision in that order. — mcc:✓ gcc:✓ clang:✓ (`sizeof(float)<=sizeof(double)<=sizeof(long double)`)
- [✓] §6.2.5p11 — Provide the complex types `float _Complex`, `double _Complex`, `long double _Complex` (when complex is supported). — mcc:✓ gcc:✓ clang:✓
- [~] §6.2.5p11 — Optionally provide the imaginary types `float/double/long double _Imaginary` (Annex G); support is optional. — mcc:~ gcc:~ clang:~ (all three reject `_Imaginary` — "imaginary types are not supported"; conforming since optional)
- §6.2.5p11 — Group the real floating and complex types together as the floating types. — (spec; not separately testable)
- [✓] §6.2.5p13 — Lay out each complex type with the same representation and alignment as a two-element array of the corresponding real type, real part first, imaginary part second. — mcc:✓ gcc:✓ clang:✓ (`sizeof(double _Complex)==sizeof(double[2])`, `_Alignof(_Complex float)==_Alignof(float[2])`)
- §6.2.5p17 — Treat the integer and real floating types collectively as the real types. — (spec; not separately testable)
- §6.2.5p18 — Treat the integer and floating types collectively as the arithmetic types. — (spec; not separately testable)
- [✓] §6.2.5p16 — Provide enumerated types comprising a set of named integer constant values. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.2.5p16 — Implementation-defined: choose for each enum a compatible integer type able to represent all its enumerators (and that choice may differ per enum). — mcc:✓ gcc:✓ clang:✓ (all pick a 4-byte type for `enum E{A,B,C}`; `enum` value freely usable as/with `int`)
- [✓] §6.2.5p20 — Provide array types: a contiguously-allocated nonempty set of N objects of one element type; an array of unknown size is incomplete. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.2.5p20 — Provide structure types: a sequentially-allocated nonempty set of named members of (possibly different) types. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.2.5p20 — Provide union types: an overlapping set of named members that occupy the same storage. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.2.5p20 — Provide function types: a return type plus the number/types of parameters. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.2.5p20 — Provide pointer types, each derived from a referenced (function/object/incomplete) type. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.2.5p20 (C11) — Provide atomic types `_Atomic(T)` / `_Atomic T` derived from an existing type. — mcc:✓ gcc:✓ clang:✓ (`_Atomic int`/`_Atomic(long)` with `atomic_load`/`atomic_store`)
- §6.2.5p21 — Treat arithmetic types plus pointer types as scalar types; array and structure types as aggregate types. — (spec; not separately testable)
- [✓] §6.2.5p22 — Treat an array of unknown size and a struct/union of unknown content as incomplete types completable later in the same scope. — mcc:✓ gcc:✓ clang:✓ (tentative `int a[]` then `int a[10]`)
- [✓] §6.2.5p23 — Build derived types (array/struct/union/function/pointer/atomic) recursively from other types; there is no limit on derivation depth beyond translation limits. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.2.5p27 — Make the integer conversion rank ordering (prose introduced here, fully defined in §6.3.1.1) well-defined for usual-arithmetic-conversion purposes. — mcc:✓ gcc:✓ clang:✓ (rank-dependent promotions behave consistently; see §6.3.1.1 part)

### §6.2.6 Representations of types

#### §6.2.6.1 General

- [✓] §6.2.6.1p2 — Represent every value of type T as `sizeof(T)` bytes (the object representation), interpretable as an array of `unsigned char`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.2.6.1p3 — Represent values stored in `unsigned char` objects and unsigned bit-fields in pure binary with no padding bits (so `UCHAR_MAX == 2^CHAR_BIT − 1`). — mcc:✓ gcc:✓ clang:✓
- §6.2.6.1p4 — Leave the mapping of values to bytes unspecified for object types other than `unsigned char`, except where the standard fixes it. — (spec; not separately testable)
- [✓] §6.2.6.1p4 — Allow any object's representation to be inspected/copied byte-by-byte through an `unsigned char` lvalue (no aliasing violation, no trap on read of valid bytes). — mcc:✓ gcc:✓ clang:✓ (per-byte copy of an `int` reproduces the value)
- §6.2.6.1p5 — Permit certain bit patterns ("trap representations") that do not represent a value of the type; reading such a representation by a non-character lvalue is undefined. — (spec; not separately testable — UB)
- §6.2.6.1p6 — Define the value of a structure/union object's representation in terms of its member (and any bit-field) representations, with bytes outside members ("padding") taking unspecified values. — (spec; not separately testable)
- [✓] §6.2.6.1p6 — Allow structure/union padding bytes (and bytes beyond bit-field storage) to hold unspecified values; do not assume they are zero or stable. — mcc:✓ gcc:✓ clang:✓ (no implementation guarantees padding contents)
- [✓] §6.2.6.1p7 — Forbid padding at the very beginning of a structure object (first member shares the struct's address). — mcc:✓ gcc:✓ clang:✓
- §6.2.6.1p8 — When a value is stored into a member of a union/structure, leave bytes not corresponding to that member (including padding) with unspecified values. — (spec; not separately testable)

#### §6.2.6.2 Integer types

- [✓] §6.2.6.2p1 — For unsigned integer types other than `_Bool`, partition the bits into value bits and padding bits, and interpret the value bits as a pure-binary place-value magnitude. — mcc:✓ gcc:✓ clang:✓ (no padding bits on the targets; `UCHAR_MAX==255`, `CHAR_BIT==8`)
- [✓] §6.2.6.2p2 — For signed integer types, partition the bits into M value bits, padding bits, and exactly one sign bit; the value bits mirror those of the corresponding unsigned type so M ≤ N. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.2.6.2p2 — Implementation-defined: choose one of the three signed encodings — sign-and-magnitude, two's complement, or ones' complement — for how the sign bit modifies the value. — mcc:✓ gcc:✓ clang:✓ (all three are two's complement: `INT_MIN == -INT_MAX-1`)
- [✓] §6.2.6.2p2 — With the sign bit 0, give the signed value the same magnitude as the corresponding unsigned interpretation of the value bits. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.2.6.2p2 — Define the all-value-bits-zero, sign-bit-one pattern as either a negative zero (sign-and-magnitude / ones'-complement) or, for two's complement, the most negative value −2^M (or, implementation-defined, a trap representation). — mcc:✓ gcc:✓ clang:✓ (two's complement: that pattern is `INT_MIN`, not a trap)
- [✓] §6.2.6.2p3 — (C11) Specify whether a negative zero, if generated, is stored as-is or normalized to a normal zero by `&`, `|`, `^`, `~`, `<<`, `>>` — implementation-defined; only relevant on non-two's-complement hardware. — mcc:✓ gcc:✓ clang:✓ (n/a on two's-complement targets; consistent)
- [✓] §6.2.6.2p4 — Keep the value bits of a signed type and its corresponding unsigned type at identical positions so non-negative values share the same representation. — mcc:✓ gcc:✓ clang:✓ (positive `int` bit-pattern equals `(unsigned)` value)
- §6.2.6.2p5 — Treat any other combination of sign/value/padding bits not described above (where it occurs) as either a normal or a trap representation, implementation-defined. — (spec; not separately testable)

### §6.2.7 Compatible type and composite type

- [✓] §6.2.7p1 — Treat two types as compatible when they are the same type, subject to the additional structural rules of §6.7.2 (specifiers), §6.7.3 (qualifiers), and §6.7.6 (declarators). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.2.7p1 — Require identical (matching) qualifiers for two qualified types to be compatible; diagnose discarding qualifiers across pointer assignment. — mcc:✓ gcc:✓ clang:✓ (`int* = const int*` warns on all three)
- [✓] §6.2.7p1 — Within one translation unit, treat two declarations of the same struct/union/enum tag as referring to the same (compatible) type. — mcc:✓ gcc:✓ clang:✓ (re-declared `struct S;` shares the type)
- §6.2.7p1 — Across separate translation units, make two struct/union/enum types compatible iff tags match and members correspond (same names, same count, compatible member types, same order; same constant values for enums). — (spec; not separately testable in a single TU)
- [✓] §6.2.7p1 — Make an enumerated type compatible with the implementation-chosen integer type that represents it (so enum/integer mixing needs no diagnostic). — mcc:✓ gcc:✓ clang:✓ (`int i = e; e = (enum E)(i+1)` accepted)
- [✓] §6.2.7p3 — Build the composite type of two compatible types: for arrays, adopt a known constant bound from either side. — mcc:✓ gcc:✓ clang:✓ (`extern int a[]` + `int a[5]` → `sizeof(a)==5*sizeof(int)`)
- [✓] §6.2.7p3 — Composite type rule: if one of two compatible function types has a parameter-type list (prototype) and the other does not, use the prototyped one; combine recursively for return/parameter/derived types. — mcc:✓ gcc:✓ clang:✓ (prototype vs non-prototype function declarations reconciled; clang warns under newer prototype-deprecation rules but accepts)
- [✓] §6.2.7p3 — Form the composite of two compatible derived types by taking the composite of the corresponding referenced/element types and re-deriving. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.2.7p2 — Permit a later declaration to refer to an entity declared with a compatible (not necessarily identical) type; the effective type is the composite. — mcc:✓ gcc:✓ clang:✓

### §6.2.8 Alignment of objects (C11)

- [✓] §6.2.8p1 (C11) — Model each complete object type as having an alignment requirement: a power-of-two byte count constraining the addresses at which objects of that type may be placed. — mcc:✓ gcc:✓ clang:✓ (`_Alignof(char)==1`)
- [✓] §6.2.8p4 (C11) — Restrict every valid alignment value to a nonnegative integral power of two. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.2.8p2 (C11) — Define fundamental alignments as those not exceeding `_Alignof(max_align_t)`, and support them for all storage durations and object types. — mcc:✓ gcc:✓ clang:✓ (`_Alignof(max_align_t) >= _Alignof(double)`; `max_align_t` from `<stddef.h>`)
- [✓] §6.2.8p3 (C11) — Define extended (over-)alignments as those greater than `_Alignof(max_align_t)`; whether, and for which storage durations, they are supported is implementation-defined. — mcc:✓ gcc:✓ clang:✓ (`_Alignas(16)` over-aligned static accepted and honored)
- [✓] §6.2.8p4 (C11) — Make the alignment of an array type equal to the alignment of its element type. — mcc:✓ gcc:✓ clang:✓ (`_Alignof(float[2])==_Alignof(float)`)
- [✓] §6.2.8p5 (C11) — Let `_Alignof` yield, and `_Alignas` request, alignment; honor an `_Alignas` request that is at least the type's natural alignment (including on struct members). — mcc:✓ gcc:✓ clang:✓ (`struct{char c;_Alignas(8) int x;}` → `_Alignof>=8`; over-aligned buffer is 16-byte aligned at runtime)
- [✓] §6.2.8p5 (C11) — Diagnose (constraint violation) an `_Alignas` specifier weaker than the type's required alignment. — mcc:✓ gcc:✓ clang:✓ (`_Alignas(1) int` rejected by all)
- [✓] §6.2.8p5 (C11) — Treat `_Alignas(0)` (and an `_Alignas` with a zero expression) as having no effect on alignment. — mcc:✓ gcc:✓ clang:✓ (`_Alignas(0) int x;` accepted, alignment unchanged)
- [✓] §6.2.8p6 (C11) — Make alignment requirements equal for compatible/identical types (so `_Alignof` agrees across equivalent spellings, e.g. complex vs array-of-real). — mcc:✓ gcc:✓ clang:✓ (`_Alignof(double _Complex)==_Alignof(double)`)
- §6.2.8p7 (C11) — Treat accessing an object through a pointer to a type with a stricter alignment than the object actually has as undefined behavior (no requirement to diagnose). — (spec; not separately testable — UB)


## §6.3 Conversions

> mcc status: 41 TODOs, 1 mcc-gap (modifiable-lvalue enforcement is warn-only: assignment to a const-qualified lvalue or to a struct/union with a const member — incl. nested — is accepted with a diagnostic, where gcc/clang reject it as an error).

### §6.3 (introduction)
- [✓] §6.3p1 — Perform implicit operand conversions where an operator requires it: change an operand's value from its own type to another type as dictated by the operator together with the operand types, and treat this clause as specifying the result of every such required conversion (explicit casts trigger the same conversions). — (spec umbrella; exercised by the per-rule probes below) mcc:✓ gcc:✓ clang:✓
- [✓] §6.3p2 — Guarantee that converting a value to a type compatible with its own type leaves both the value and its representation unchanged. — mcc:✓ gcc:✓ clang:✓ (identity conversions preserve value)

### §6.3.1 Arithmetic operands

### §6.3.1.1 Boolean, characters, and integers
- [✓] §6.3.1.1p1 — Assign every integer type an integer conversion rank such that: no two distinct signed integer types share a rank; a signed type of greater precision/width has rank greater than or equal to one of lesser precision; the standard signed types rank long long > long > int > short > signed char; each unsigned integer type has the same rank as its corresponding signed type; _Bool ranks below every other standard integer type; char ranks equal to signed char and unsigned char; and the rank relation is consistent/transitive. — (spec; rank is a model, observed via UAC §6.3.1.8 probes) mcc:✓ gcc:✓ clang:✓
- [✓] §6.3.1.1p1 — Rank extended integer types: an extended signed integer type ranks relative to another signed type by precision (greater precision implies greater-or-equal rank), and its rank relative to same-precision standard types is implementation-defined but still consistent with the other ordering rules; each extended unsigned type shares its signed counterpart's rank. — (spec; no extended integer types beyond standard ones exposed) mcc:✓ gcc:✓ clang:✓
- §6.3.1.1p1 — Give an enumerated type the same integer conversion rank as the (implementation-chosen) integer type with which it is compatible. — (spec; not separately testable)
- [✓] §6.3.1.1p2 — Define and apply the integer promotions: an operand of an integer type whose conversion rank is less than or equal to that of int/unsigned int, or a bit-field of type _Bool, int, signed int, or unsigned int, is converted to int if int can represent all of its values and otherwise to unsigned int; operands of all other types are unchanged by the promotion; and the promotion preserves the value including its sign. — mcc:✓ gcc:✓ clang:✓ (sizeof(+(char/short/unsigned char))==sizeof(int))
- §6.3.1.1p2 footnote — Apply the integer promotions only in the contexts the standard explicitly names; elsewhere the conversion rank is used solely to define behavior (e.g. usual arithmetic conversions) and no promotion is performed. — (spec; not separately testable)

### §6.3.1.2 Boolean type
- [✓] §6.3.1.2p1 — When converting any scalar value to _Bool, produce 0 if the value compares equal to 0 and 1 otherwise. — mcc:✓ gcc:✓ clang:✓ ((_Bool)2.5==1, (_Bool)0.0==0, (_Bool)&x==1, (_Bool)(void*)0==0)

### §6.3.1.3 Signed and unsigned integers
- [✓] §6.3.1.3p1 — When an integer is converted to an integer type other than _Bool that can represent its value, leave the value unchanged. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.3.1.3p2 — When converting a value to an unsigned integer type that cannot represent it, reduce it modulo two-to-the-N (where N is the number of value bits) by repeatedly adding/subtracting one more than the maximum representable value until it lies in range. — mcc:✓ gcc:✓ clang:✓ ((unsigned char)257==1, (unsigned)-1==0xFFFFFFFF)
- [✓] §6.3.1.3p3 — When converting a value to a signed integer type that cannot represent it, either yield an implementation-defined result or raise an implementation-defined signal; document the choice. — mcc:✓ gcc:✓ clang:✓ (impl-defined; all three take low-order bits / modulo: (signed char)300==44, no trap)

### §6.3.1.4 Real floating and integer
- [✓] §6.3.1.4p1 — When converting a finite real floating value to an integer type, discard the fractional part (truncate toward zero), and treat the conversion as undefined behavior when the truncated integral part cannot be represented in the target integer type. — mcc:✓ gcc:✓ clang:✓ ((int)3.9==3, (int)-3.9==-3)
- [ ] §6.3.1.4p1 (C11) — Note the C11 addition that the FE_INVALID floating-point exception may be raised when the real value is not representable in the integer type. — (spec; C11 wording note, optional behavior)
- [✓] §6.3.1.4p2 — When converting an integer to a real floating type: keep the value if it is exactly representable; if it is within range but not exactly representable, round to an adjacent representable value in an implementation-defined direction; and treat as undefined behavior when the value is outside the representable range. — mcc:✓ gcc:✓ clang:✓
- [ ] §6.3.1.4p2 (C11) — Note the C11 addition that the inexact (rounded) case may raise FE_INEXACT and is governed by the current rounding mode. — (spec; C11 wording note)

### §6.3.1.5 Real floating types
- [✓] §6.3.1.5p1 — When converting to a real floating type of greater or equal range and precision (e.g. float→double, float/double→long double), preserve the value exactly. (C11 reframes this in terms of one format's value set including another's.) — mcc:✓ gcc:✓ clang:✓ ((double)(float)1.0f==1.0)
- [✓] §6.3.1.5p2 — When converting to a real floating type of narrower range/precision (or otherwise representing a value in narrower precision): keep the value if exactly representable; if within range but inexact, round to an adjacent representable value in an implementation-defined manner; and treat as undefined behavior when the value is outside the target's representable range. — mcc:✓ gcc:✓ clang:✓ ((float)0.1 != (double)0.1)
- [ ] §6.3.1.5p2 (C11) — Note the C11 addition that this narrowing conversion may raise FE_INEXACT and follows the current rounding mode. — (spec; C11 wording note)

### §6.3.1.6 Complex types
- [✓] §6.3.1.6p1 — When converting between complex types, convert the real part and the imaginary part independently, each according to the conversion rules for the corresponding real type. — mcc:✓ gcc:✓ clang:✓ ((float complex)(1.5+2.5i) keeps both parts)

### §6.3.1.7 Real and complex
- [✓] §6.3.1.7p1 — When converting a real value to a complex type, convert it (per the corresponding real-type rules) to become the result's real part and set the result's imaginary part to a positive zero. — mcc:✓ gcc:✓ clang:✓ (imag is +0.0, signbit clear)
- [✓] §6.3.1.7p2 — When converting a complex value to a real type, discard the imaginary part and convert the real part according to the corresponding real-type rules. — mcc:✓ gcc:✓ clang:✓ ((double)(5+9i)==5.0)

### §6.3.1.8 Usual arithmetic conversions
- [✓] §6.3.1.8p1 — Determine a common real type for two arithmetic operands via the usual arithmetic conversions, then convert each operand to a type whose corresponding real type is that common real type; the result is complex if either operand is complex (otherwise real), and the common real type also determines the expression's result type. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.3.1.8p1 — Floating selection: if either operand's corresponding real type is long double, make the common real type long double; otherwise if either is double, make it double; otherwise if either is float, make it float (converting the other operand, integer or floating, accordingly). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.3.1.8p1 — Integer path: if neither operand has a floating type, first apply the integer promotions to both operands, then apply the following same-/mixed-sign rules. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.3.1.8p1 — If after promotion both operands have the same type, perform no further conversion. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.3.1.8p1 — If both operands are signed or both unsigned (and types differ), convert the operand of lesser integer conversion rank to the type of the operand of greater rank. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.3.1.8p1 — If the operands have differing signedness and the unsigned operand has rank greater than or equal to the signed operand's rank, convert the signed operand to the unsigned operand's type. — mcc:✓ gcc:✓ clang:✓ (-1 < 0u evaluates false)
- [✓] §6.3.1.8p1 — If the operands have differing signedness and the signed operand's type can represent every value of the unsigned operand's type, convert the unsigned operand to the signed operand's type. — mcc:✓ gcc:✓ clang:✓ ((long)-1 < 0u is true on LP64 where long > unsigned int)
- [✓] §6.3.1.8p1 — In the remaining differing-signedness case, convert both operands to the unsigned integer type corresponding to the signed operand's type. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.3.1.8p2 — Allow floating operands and floating-expression results to be evaluated and represented with greater range and precision than the type requires (as reported by FLT_EVAL_METHOD), while leaving the operand types and the expression's declared result type unchanged. — mcc:✓ gcc:✓ clang:✓ (all report FLT_EVAL_METHOD==0 on x86-64)

### §6.3.2 Other operands

### §6.3.2.1 Lvalues, arrays, and function designators
- [~] §6.3.2.1p1 — Define an lvalue as an expression with object type, or an incomplete type other than void, that potentially designates an object; treat evaluating an lvalue that does not designate an object when evaluated as undefined behavior; and define a modifiable lvalue as an lvalue that is not an array type, not an incomplete type, not const-qualified, and—if a structure or union—has no const-qualified member (recursively, at any nesting depth). — mcc:~ gcc:✓ clang:✓ (mcc detects the violations — incl. nested const members — but only **warns** ("assignment of read-only location") and exits 0; gcc/clang reject as hard errors. Array assignment is correctly an error in all three.)
- [✓] §6.3.2.1p2 — Perform lvalue-to-rvalue conversion: except when it is the operand of sizeof, of unary &, of ++ or --, or is the left operand of the . operator or of an assignment operator, convert a non-array lvalue to the value stored in the designated object; the result has the lvalue's type stripped of qualifiers (and of _Atomic in C11) and is not itself an lvalue; and treat the conversion as undefined behavior if the lvalue has an incomplete type. — (spec; result-is-not-an-lvalue and qualifier-stripping are observable indirectly) mcc:✓ gcc:✓ clang:✓
- [ ] §6.3.2.1p2 (C11) — Additionally treat as undefined behavior reading, via lvalue-to-rvalue conversion, an object of automatic storage duration that could have been declared with register storage class (its address is never taken) while its stored value is indeterminate/uninitialized. — (spec; UB, not reliably diagnosable — mcc does track register-eligible params per recent §6.7.1 diag work but this read case is not diagnosed)
- [✓] §6.3.2.1p3 — Convert an expression of array type to a pointer to its initial element (yielding a non-lvalue of the element-pointer type), except when the array expression is the operand of sizeof, the operand of unary &, or a string literal used to initialize an array; note that no object need exist beyond the array's elements. — mcc:✓ gcc:✓ clang:✓ (sizeof a / &a keep array type; `int *p=a` decays; `char s[]="abc"` keeps sizeof 4)
- [✓] §6.3.2.1p4 — Convert a function designator (an expression of function type) to a pointer to that function, except when it is the operand of sizeof, of _Alignof (C11), or of unary &. — mcc:✓ gcc:✓ clang:✓ (function name assigned to a function pointer decays)

### §6.3.2.2 void
- [✓] §6.3.2.2p1 — Treat the (nonexistent) value of a void expression as unusable: never require or use such a value, allow any expression to be converted to void implicitly or by an explicit cast (its value/designator is then discarded but side effects still occur), and never implicitly convert a void expression to any non-void type. — mcc:✓ gcc:✓ clang:✓ (initializing int from a void call is an error: "cannot convert 'void' to 'int'"; `(void)expr` cast accepted)

### §6.3.2.3 Pointers
- [✓] §6.3.2.3p1 — Allow a pointer to void to be converted to or from a pointer to any object type and back, with the round-tripped pointer comparing equal to the original. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.3.2.3p2 — Allow a pointer to a qualified or unqualified type to be converted to a pointer to a more-qualified version of that type (additional qualifiers), preserving the pointer value. — mcc:✓ gcc:✓ clang:✓ (`int* -> const volatile int*` accepted, value preserved; dropping qualifiers warns in all three)
- [✓] §6.3.2.3p3 — Recognize a null pointer constant (an integer constant expression with value 0, or such an expression cast to void*); when converted to any pointer type it yields a null pointer that is guaranteed to compare unequal to any pointer to an actual object or function. — mcc:✓ gcc:✓ clang:✓ (`int *p=0`==NULL, p != &obj)
- [✓] §6.3.2.3p4 — Guarantee that two null pointers, converted to any (compatible) pointer types, compare equal to each other. — mcc:✓ gcc:✓ clang:✓ ((void*)0 == (char*)0)
- [✓] §6.3.2.3p5 — When converting an integer to a pointer type, produce an implementation-defined result that need not be correctly aligned, need not point at an entity of the referenced type, and may be a trap representation. — mcc:✓ gcc:✓ clang:✓ (impl-defined; intptr_t→pointer round-trips)
- [✓] §6.3.2.3p6 — When converting a pointer to an integer type, produce an implementation-defined result; if the result cannot be represented in the integer type the behavior is undefined, and the result need not be in the range of any integer type. — mcc:✓ gcc:✓ clang:✓ (impl-defined; pointer→intptr_t→pointer round-trips)
- [✓] §6.3.2.3p7 — Allow conversion of a pointer to one object type into a pointer to a different object type: if the result is not correctly aligned for the referenced type the behavior is undefined; otherwise converting it back yields a pointer equal to the original; and require that a pointer to an object converted to a pointer to a character type points at the object's lowest-addressed byte, with successive increments of the result pointing at the remaining bytes of the object in order. — mcc:✓ gcc:✓ clang:✓ ((unsigned char*)&obj == (void*)&obj; byte walk valid)
- [✓] §6.3.2.3p8 — Allow conversion of a pointer to a function of one type into a pointer to a function of another type and back to recover a pointer equal to the original; but treat a call through a function pointer whose type is incompatible with the called function's type as undefined behavior. — mcc:✓ gcc:✓ clang:✓ (round-trip through `void(*)(int)` recovers original; the incompatible-call clause is UB, not diagnosed)


## §6.4 Lexical elements

> mcc status: 152 TODO items below; mcc passes the mechanically-testable lexical core (all keywords, UCNs, hex floats, `long long`, C11 char/string prefixes, digraphs, `//` comments, maximal munch). 0 mcc-gaps: mcc flags `0b` binary integer constants and the `\e` non-standard escape under `-pedantic`/`-pedantic-errors`, matching gcc/clang. The `__func__` array `&` mismatch and the `'$'`/octal-range/large-int diagnostics are warnings vs errors, but those are diagnostic-strength only and match gcc, not conformance gaps.

### §6.4 General
- §6.4p1 (Syntax) — A *token* is one of: *keyword*, *identifier*, *constant*, *string-literal*, or *punctuator*. — (spec; not separately testable)
- §6.4p1 (Syntax) — A *preprocessing-token* is one of: *header-name*, *identifier*, *pp-number*, *character-constant*, *string-literal*, *punctuator*, or each non-white-space character that cannot be one of the preceding. — (spec; not separately testable)
- [✓] §6.4p1 (Constraints) — Each preprocessing token that is converted to a token shall have the lexical form of a keyword, an identifier, a constant, a string literal, or a punctuator. — mcc:✓ gcc:✓ clang:✓
- §6.4p2 — A token is the minimal lexical element of the language in translation phases 7 and 8 (categories: keywords, identifiers, constants, string literals, punctuators). — (spec; not separately testable)
- §6.4p2 — A preprocessing token is the minimal lexical element in translation phases 3 through 6 (categories: header names, identifiers, preprocessing numbers, character constants, string literals, punctuators, and single non-white-space characters that do not lexically match the other categories). — (spec; not separately testable)
- §6.4p2 — If a `'` or a `"` character matches the last (single-non-white-space-character) category, the behavior is undefined. — (spec/UB; not separately testable)
- [✓] §6.4p2 — Preprocessing tokens can be separated by white space, consisting of comments, or white-space characters (space, horizontal tab, new-line, vertical tab, and form-feed), or both. — mcc:✓ gcc:✓ clang:✓ (vertical-tab/form-feed between tokens accepted by all three)
- §6.4p2 — As described in §6.10, in certain circumstances during translation phase 4, white space (or its absence) serves as more than preprocessing-token separation. — (spec; not separately testable)
- §6.4p2 — White space may appear within a preprocessing token only as part of a header name or between the quotation characters in a character constant or string literal. — (spec; not separately testable)
- [✓] §6.4p3 — Where the input stream has been parsed into preprocessing tokens up to a given character, the next preprocessing token is the longest sequence of characters that could constitute a preprocessing token (maximal munch). — mcc:✓ gcc:✓ clang:✓ (`x+++++y` → `x ++ ++ + y`)
- [✓] §6.4p4 (EXAMPLE 1) — `x+++++y` is parsed as `x ++ ++ + y`, which violates a constraint on increment operators even though `x ++ + ++ y` might yield a correct expression. — mcc:✓ gcc:✓ clang:✓ (all reject: lvalue required as increment operand)

### §6.4.1 Keywords
> C99 added `inline`, `restrict`, `_Bool`, `_Complex`, `_Imaginary`; C11 added `_Alignas`, `_Alignof`, `_Atomic`, `_Generic`, `_Noreturn`, `_Static_assert`, `_Thread_local`.
- [✓] §6.4.1p1 (Syntax) — The following are keywords: `auto break case char const continue default do double else enum extern float for goto if inline int long register restrict return short signed sizeof static struct switch typedef union unsigned void volatile while _Bool _Complex _Imaginary`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.1p1 — `inline`, `restrict`, `_Bool`, `_Complex`, and `_Imaginary` are keywords added in C99 relative to C90. (C99) — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.1p1 — `_Alignas`, `_Alignof`, `_Atomic`, `_Generic`, `_Noreturn`, `_Static_assert`, and `_Thread_local` are also keywords. (C11) — mcc:✓ gcc:✓ clang:✓ (all seven accepted under `-std=c11`)
- [✓] §6.4.1p2 (Semantics) — The above tokens (case sensitive) are reserved for use as keywords, and shall not be used otherwise. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.1p2 — `_Imaginary` is reserved for specifying imaginary types even though imaginary-type support is an optional feature (Annex G). (C99) — mcc:✓ gcc:~ clang:✓ (mcc & clang treat `_Imaginary` as a reserved keyword → "imaginary types are not supported"; gcc does **not** reserve it and treats it as an ordinary identifier)

### §6.4.2 Identifiers

#### §6.4.2.1 General
- [✓] §6.4.2.1p1 (Syntax) — An *identifier* is `identifier-nondigit`, or `identifier identifier-nondigit`, or `identifier digit`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.2.1p1 (Syntax) — An *identifier-nondigit* is a `nondigit`, a `universal-character-name`, or other implementation-defined characters. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.2.1p1 (Syntax) — A *nondigit* is `_` or one of `a`–`z`, `A`–`Z`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.2.1p1 (Syntax) — A *digit* is one of `0`–`9`. — mcc:✓ gcc:✓ clang:✓
- §6.4.2.1p2 — An identifier is a sequence of nondigit characters (including underscore `_`, lowercase and uppercase Latin letters, and other characters) and digits, which designates one or more entities as described in §6.2.1. — (spec; not separately testable)
- [✓] §6.4.2.1p2 — Lowercase and uppercase letters are distinct. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.2.1p2 — There is no specific limit on the maximum length of an identifier. — mcc:✓ gcc:✓ clang:✓ (70+ char identifiers distinct, no truncation)
- [✓] §6.4.2.1p3 (Constraints) — Each universal character name in an identifier shall designate a character whose encoding in ISO/IEC 10646 falls into one of the ranges specified in Annex D. (C99) — mcc:✓ gcc:✓ clang:✓ (`A` rejected: "not valid in an identifier")
- [✓] §6.4.2.1p3 (Constraints) — The initial character of an identifier shall not be a universal character name designating a digit. (C99) — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.2.1p3 — An implementation may allow multibyte characters that are not part of the basic source character set to appear in identifiers; which characters and their correspondence to universal character names is implementation-defined. — mcc:✓ gcc:✓ clang:✓ (UTF-8 `À` and explicit `À` both accepted in identifiers)
- [✓] §6.4.2.1p4 — When preprocessing tokens are converted to tokens during translation phase 7, if a preprocessing token could be converted to either a keyword or an identifier, it is converted to a keyword. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.2.1p5 (Semantics) — As discussed in §5.2.4.1, an implementation may limit the number of significant initial characters in an identifier; the limit for an external name may be more restrictive than that for an internal name. — mcc:✓ gcc:✓ clang:✓
- [ ] §6.4.2.1p5 — The number of significant characters in an identifier is implementation-defined. — (impl-defined; mcc/gcc/clang all treat full length as significant)
- [✓] §6.4.2.1p5 — Any identifiers that differ in a significant character are different identifiers. — mcc:✓ gcc:✓ clang:✓
- §6.4.2.1p5 — If two identifiers differ only in nonsignificant characters, the behavior is undefined. — (spec/UB; not separately testable — no truncation observed)
- [✓] §5.2.4.1 (related limit) — The implementation shall support at least 63 significant initial characters in an internal identifier or a macro name. (C99; C90 required 31) — mcc:✓ gcc:✓ clang:✓ (70-char internal names distinct)
- [✓] §5.2.4.1 (related limit) — The implementation shall support at least 31 significant initial characters in an external identifier (with each universal character name or extended source character counting as a number of characters). — mcc:✓ gcc:✓ clang:✓

#### §6.4.2.2 Predefined identifiers (`__func__`)
> `__func__` is C99 (not present in C90).
- [✓] §6.4.2.2p1 (Semantics) — The identifier `__func__` shall be implicitly declared by the translator as if, immediately following the opening brace of each function definition, the declaration `static const char __func__[] = "function-name";` appeared, where *function-name* is the name of the lexically-enclosing function. (C99) — mcc:✓ gcc:✓ clang:✓ (`sizeof(__func__)` in `main` == 5; array type `const char[5]`)
- [✓] §6.4.2.2p2 (EXAMPLE) — Within a function `myfunc`, `__func__` provides the string `"myfunc"` (e.g. `printf("%s\n", __func__);` prints the function name). — mcc:✓ gcc:✓ clang:✓ (all print `main`)

### §6.4.3 Universal character names
> Universal character names are a C99 addition (not present in C90).
- [✓] §6.4.3p1 (Syntax) — A *universal-character-name* is `\u hex-quad` or `\U hex-quad hex-quad`. (C99) — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.3p1 (Syntax) — A *hex-quad* is four `hexadecimal-digit`s. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.3p2 (Constraints) — A universal character name shall not specify a character whose short identifier is less than `00A0` other than `0024` (`$`), `0040` (`@`), or `0060` (`` ` ``), nor one in the range `D800` through `DFFF` inclusive. — mcc:✓ gcc:✓ clang:~ (mcc & gcc accept the `$` exception per the standard; clang **rejects** `$` "character '$' cannot be specified by a universal character name", diverging from the spec exception)
- [✓] §6.4.3p3 (Semantics) — A universal character name designates a character in ISO/IEC 10646. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.3p3 — `\Unnnnnnnn` designates the character whose eight-digit short identifier is `nnnnnnnn`; `\unnnn` designates the character whose four-digit short identifier is `nnnn` (i.e. eight-digit short identifier `0000nnnn`). — mcc:✓ gcc:✓ clang:✓

### §6.4.4 Constants
- [✓] §6.4.4p1 (Syntax) — A *constant* is an `integer-constant`, a `floating-constant`, an `enumeration-constant`, or a `character-constant`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.4p2 (Constraints) — Each constant shall have a type and the value of a constant shall be in the range of representable values for its type. — mcc:✓ gcc:✓ clang:✓
- §6.4.4p3 (Semantics) — Each constant has a type, determined by its form and value, as detailed later. — (spec; not separately testable)

#### §6.4.4.1 Integer constants
> `long long`/`unsigned long long` types and the `ll`/`LL` suffix are C99; the extended-integer-type fallback (p6) is C99.
- [✓] §6.4.4.1p1 (Syntax) — An *integer-constant* is a `decimal-constant`, `octal-constant`, or `hexadecimal-constant`, each optionally followed by an `integer-suffix`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.4.1p1 (Syntax) — A *decimal-constant* is a `nonzero-digit`, or `decimal-constant digit`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.4.1p1 (Syntax) — An *octal-constant* is `0`, or `octal-constant octal-digit`. — mcc:✓ gcc:✓ clang:✓ (`0777` == 511)
- [✓] §6.4.4.1p1 (Syntax) — A *hexadecimal-constant* is a `hexadecimal-prefix` followed by one or more `hexadecimal-digit`s; the *hexadecimal-prefix* is `0x` or `0X`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.4.1p1 (Syntax) — A *nonzero-digit* is `1`–`9`; an *octal-digit* is `0`–`7`; a *hexadecimal-digit* is `0`–`9`, `a`–`f`, or `A`–`F`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.4.1p1 (Syntax) — An *integer-suffix* is: `unsigned-suffix long-suffix?`, `unsigned-suffix long-long-suffix`, `long-suffix unsigned-suffix?`, or `long-long-suffix unsigned-suffix?`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.4.1p1 (Syntax) — *unsigned-suffix* is `u` or `U`; *long-suffix* is `l` or `L`; *long-long-suffix* is `ll` or `LL`. (`ll`/`LL` are C99) — mcc:✓ gcc:✓ clang:✓
- §6.4.4.1p2 (Description) — An integer constant begins with a digit, but has no period or exponent part; it may have a prefix that specifies its base and a suffix that specifies its type. — (spec; not separately testable)
- [✓] §6.4.4.1p3 (Semantics) — The value of a decimal constant is computed base 10; that of an octal constant, base 8; that of a hexadecimal constant, base 16; the lexically first digit is the most significant. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.4.1p5 — The type of an integer constant is the first of the corresponding list (per its form/suffix) in which its value can be represented. — mcc:✓ gcc:✓ clang:✓ (`sizeof(0x7FFFFFFFFFFFFFFF)`==8, `sizeof(0xFFFFFFFFFFFFFFFF)`==8, `sizeof(1U)`==4)
- [✓] §6.4.4.1p5 (type list) — Unsuffixed *decimal* constant: `int`, `long int`, `long long int`. (`long long int` is C99) — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.4.1p5 (type list) — Unsuffixed *octal/hexadecimal* constant: `int`, `unsigned int`, `long int`, `unsigned long int`, `long long int`, `unsigned long long int`. (`(unsigned) long long int` are C99) — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.4.1p5 (type list) — Constant with `u`/`U` suffix (any base): `unsigned int`, `unsigned long int`, `unsigned long long int`. (`unsigned long long int` is C99) — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.4.1p5 (type list) — Decimal constant with `l`/`L` suffix: `long int`, `long long int`. (`long long int` is C99) — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.4.1p5 (type list) — Octal/hexadecimal constant with `l`/`L` suffix: `long int`, `unsigned long int`, `long long int`, `unsigned long long int`. (`(unsigned) long long int` are C99) — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.4.1p5 (type list) — Constant with both `u`/`U` and `l`/`L` suffix (any base): `unsigned long int`, `unsigned long long int`. (`unsigned long long int` is C99) — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.4.1p5 (type list) — Decimal constant with `ll`/`LL` suffix: `long long int`. (C99) — mcc:✓ gcc:✓ clang:✓ (`9223372036854775807LL` works)
- [✓] §6.4.4.1p5 (type list) — Octal/hexadecimal constant with `ll`/`LL` suffix: `long long int`, `unsigned long long int`. (C99) — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.4.1p5 (type list) — Constant with both `u`/`U` and `ll`/`LL` suffix (any base): `unsigned long long int`. (C99) — mcc:✓ gcc:✓ clang:✓ (`18446744073709551615ULL` works)
- [ ] §6.4.4.1p6 — If an integer constant cannot be represented by any type in its list, it may have an extended integer type, if the extended integer type can represent its value. (C99) — (impl-defined; no extended integer types in mcc/gcc/clang on x86-64)
- §6.4.4.1p6 — If all of the types in the list for the constant are signed, the extended integer type shall be signed. (C99) — (spec; not separately testable — no extended types)
- §6.4.4.1p6 — If all of the types in the list for the constant are unsigned, the extended integer type shall be unsigned. (C99) — (spec; not separately testable)
- §6.4.4.1p6 — If the list contains both signed and unsigned types, the extended integer type may be signed or unsigned. (C99) — (spec; not separately testable)
- [✓] §6.4.4.1p6 — If an integer constant cannot be represented by any type in its list and has no extended integer type, then the integer constant has no type (and the program is ill-formed; a diagnostic is required). (C99) — mcc:✓ gcc:✓ clang:✓ (mcc warns "integer constant overflow", gcc warns "too large", clang errors "too large to be represented in any integer type" — all diagnose)

#### §6.4.4.2 Floating constants
> Hexadecimal floating constants (the `0x…p±…` form, binary-exponent-part, and the p3/p7 hex-rounding rules) are C99.
- [✓] §6.4.4.2p1 (Syntax) — A *floating-constant* is a `decimal-floating-constant` or a `hexadecimal-floating-constant`. (hexadecimal form is C99) — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.4.2p1 (Syntax) — A *decimal-floating-constant* is `fractional-constant exponent-part? floating-suffix?`, or `digit-sequence exponent-part floating-suffix?`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.4.2p1 (Syntax) — A *hexadecimal-floating-constant* is `hexadecimal-prefix hexadecimal-fractional-constant binary-exponent-part floating-suffix?`, or `hexadecimal-prefix hexadecimal-digit-sequence binary-exponent-part floating-suffix?`. (C99) — mcc:✓ gcc:✓ clang:✓ (`0x1.8p1`==3.0, `0x1p-1f`==0.5, `0x1.0p4L`==16)
- [✓] §6.4.4.2p1 (Syntax) — A *fractional-constant* is `digit-sequence? . digit-sequence` or `digit-sequence .`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.4.2p1 (Syntax) — An *exponent-part* is `e sign? digit-sequence` or `E sign? digit-sequence`; *sign* is `+` or `-`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.4.2p1 (Syntax) — A *hexadecimal-fractional-constant* is `hexadecimal-digit-sequence? . hexadecimal-digit-sequence` or `hexadecimal-digit-sequence .`. (C99) — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.4.2p1 (Syntax) — A *binary-exponent-part* is `p sign? digit-sequence` or `P sign? digit-sequence`. (C99) — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.4.2p1 (Syntax) — A *floating-suffix* is one of `f`, `l`, `F`, `L`. — mcc:✓ gcc:✓ clang:✓
- §6.4.4.2p2 (Description) — A floating constant has a significand part that may be followed by an exponent part and a suffix that specifies its type. — (spec; not separately testable)
- §6.4.4.2p2 — The significand part's components may include a digit sequence (whole-number part), a period, and a digit sequence (fraction part). — (spec; not separately testable)
- [✓] §6.4.4.2p2 — The exponent part's components are an `e`, `E`, `p`, or `P` followed by an optionally signed digit sequence. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.4.2p2 — Either the whole-number part or the fraction part has to be present; for decimal floating constants, either the period or the exponent part has to be present; for hexadecimal floating constants the binary-exponent-part is required. — mcc:✓ gcc:✓ clang:✓ (`0x1.8` with no `p` rejected: "exponent expected"/"requires an exponent")
- [✓] §6.4.4.2p3 (Semantics) — The significand part is interpreted as a (decimal or hexadecimal) rational number; the digit sequence in the exponent part is interpreted as a decimal integer. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.4.2p3 — For decimal floating constants, the exponent indicates the power of 10 by which the significand part is to be scaled. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.4.2p3 — For hexadecimal floating constants, the exponent indicates the power of 2 by which the significand part is to be scaled. (C99) — mcc:✓ gcc:✓ clang:✓
- §6.4.4.2p3 — For decimal floating constants, and for hex floating constants when `FLT_RADIX` is not a power of 2, the result is the nearest representable value, or an adjacent representable value, chosen in an implementation-defined manner. — (impl-defined; not separately testable)
- [✓] §6.4.4.2p3 — For hexadecimal floating constants when `FLT_RADIX` is a power of 2, the result is correctly rounded. (C99) — mcc:✓ gcc:✓ clang:✓ (exact-power-of-two hex floats reproduce exactly)
- [✓] §6.4.4.2p4 — An unsuffixed floating constant has type `double`; if suffixed by `f`/`F`, `float`; if suffixed by `l`/`L`, `long double`. — mcc:✓ gcc:✓ clang:✓ (`sizeof` 8/4/16)
- §6.4.4.2p5 — Floating constants are converted to internal format as if at translation time. — (spec; not separately testable)
- §6.4.4.2p5 — The conversion of a floating constant shall not raise an exceptional condition or a floating-point exception at execution time. — (spec; not separately testable)
- [✓] §6.4.4.2p6 — All floating constants of the same source form shall convert to the same internal format with the same value. — mcc:✓ gcc:✓ clang:✓
- [✗] §6.4.4.2p7 (Recommended practice) — The implementation should produce a diagnostic if a hexadecimal constant cannot be represented exactly in its evaluation format, and should then proceed with translation. (C99) — mcc:✗ gcc:✗ clang:✗ (recommended practice only; none of the three diagnose inexact hex float constants — not a conformance failure)
- §6.4.4.2p7 (Recommended practice) — Translation-time conversion of floating constants should match execution-time `strtod`-style conversion for matching inputs/format/rounding. — (recommended practice; not separately testable)

#### §6.4.4.3 Enumeration constants
- [✓] §6.4.4.3p1 (Syntax) — An *enumeration-constant* is an `identifier`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.4.3p2 (Semantics) — An identifier declared as an enumeration constant has type `int`. — mcc:✓ gcc:✓ clang:✓ (`sizeof(A)`==4 for an enumerator)
- §6.4.4.3 (forward ref) — The semantics of declaring enumeration constants are described in §6.7.2.2. — (cross-ref; not separately testable)

#### §6.4.4.4 Character constants
> The `u'…'` and `U'…'` prefixes (and the `char16_t`/`char32_t` constraint/type wording) are C11; `L'…'` and universal character names are C99/earlier.
- [✓] §6.4.4.4p1 (Syntax) — A *character-constant* is `' c-char-sequence '`, `L' c-char-sequence '`, `u' c-char-sequence '`, or `U' c-char-sequence '`. (`u'…'` and `U'…'` are C11) — mcc:✓ gcc:✓ clang:✓ (`u'A'`==65, `U'B'`==66 via `<uchar.h>`)
- [✓] §6.4.4.4p1 (Syntax) — A *c-char-sequence* is one or more `c-char`; a *c-char* is any member of the source character set except `'`, `\`, or new-line, or an `escape-sequence`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.4.4p1 (Syntax) — An *escape-sequence* is a `simple-escape-sequence`, `octal-escape-sequence`, `hexadecimal-escape-sequence`, or `universal-character-name`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.4.4p1 (Syntax) — A *simple-escape-sequence* is one of `\'  \"  \?  \\  \a  \b  \f  \n  \r  \t  \v`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.4.4p1 (Syntax) — An *octal-escape-sequence* is `\` followed by one, two, or three octal digits. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.4.4p1 (Syntax) — A *hexadecimal-escape-sequence* is `\x` followed by one or more hexadecimal digits. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.4.4p2 (Description) — An integer character constant is a sequence of one or more multibyte characters enclosed in single-quotes; a wide character constant is the same, except prefixed by `L`, `u`, or `U`. (`u`/`U` prefixes are C11) — mcc:✓ gcc:✓ clang:✓
- §6.4.4.4p2 — With a few exceptions, the elements of the sequence are any members of the source character set; they are mapped in an implementation-defined manner to members of the execution character set. — (impl-defined; not separately testable)
- [✓] §6.4.4.4p3 — The escape sequences map: `\'`→`'`, `\"`→`"`, `\?`→`?`, `\\`→`\`, `\a`→alert, `\b`→backspace, `\f`→form feed, `\n`→new line, `\r`→carriage return, `\t`→horizontal tab, `\v`→vertical tab. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.4.4p4 — `"` and `?` are representable by themselves or by `\"`/`\?`; the single-quote `'` and the backslash `\` shall be `\'` and `\\` respectively. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.4.4p5 — The octal digits following the backslash form a single character; the numerical value of the octal integer specifies the desired value. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.4.4p6 — The hexadecimal digits following `\x` form a single character; the numerical value of the hexadecimal integer specifies the desired value. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.4.4p7 — A universal character name is constituted in the same way as for identifiers and string literals; the construction is described in §6.4.3. (C99) — mcc:✓ gcc:✓ clang:✓
- [~] §6.4.4.4p9 (Constraints) — The value of an octal or hexadecimal escape sequence shall be in the range of representable values for `unsigned char` (integer char constant), or the unsigned type corresponding to `wchar_t`/`char16_t`/`char32_t` (wide). (C11 extends to `u`/`U`) — mcc:~ gcc:~ clang:✓ (`'\477'`: mcc & gcc emit a **warning** "octal escape sequence out of range"; clang **errors**. All diagnose the constraint violation; mcc matches gcc)
- [✓] §6.4.4.4p10 (Semantics) — An integer character constant has type `int`. — mcc:✓ gcc:✓ clang:✓ (`sizeof('A')`==4)
- [✓] §6.4.4.4p10 — The value of an integer character constant containing a single character mapping to a single-byte execution character is the numerical value of that character interpreted as an integer. — mcc:✓ gcc:✓ clang:✓ (`'A'`==65)
- [✓] §6.4.4.4p10 — The value of an integer character constant with more than one character (e.g. `'ab'`) or a non-single-byte mapping is implementation-defined. — mcc:✓ gcc:✓ clang:✓ (all give `'ab'`==24930 with a multichar warning; impl-defined value matches)
- [✓] §6.4.4.4p10 — If an integer character constant contains a single character/escape, its value is that of a `char` object holding it, converted to `int` (so sign matters). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.4.4p11 — A wide character constant prefixed by `L` has type `wchar_t`; by `u` has type `char16_t`; by `U` has type `char32_t`. (`u`/`U` are C11) — mcc:✓ gcc:✓ clang:✓
- §6.4.4.4p11 — The value of a single-multibyte-character wide constant is the `mbtowc` wide value under an implementation-defined current locale. — (impl-defined; not separately testable)
- §6.4.4.4p11 — The value of a multi-character / multi-mapping / unrepresented wide character constant is implementation-defined. — (impl-defined; not separately testable)
- [✓] §6.4.4.4p12 (EXAMPLE 1) — `'\0'` is commonly used to represent the null character. — mcc:✓ gcc:✓ clang:✓
- §6.4.4.4p13 (EXAMPLE 2) — Distinct interpretations exist for `'\1234'`, `'\x1234'`, etc. — (example/impl-defined; not separately testable)
- [✓] §6.4.4.4p14 (EXAMPLE 3) — Even where `char` is signed, the value of `'\xFF'` is determined by conversion of the escape value to `char` then to `int`. — mcc:✓ gcc:✓ clang:✓

### §6.4.5 String literals
> The `u8`, `u`, and `U` encoding prefixes (and the p2 constraint, p5 mixed-prefix rule, and `char16_t`/`char32_t`/UTF-8 array-element rules) are C11; only `L` existed in C99.
- [✓] §6.4.5p1 (Syntax) — A *string-literal* is `encoding-prefix? " s-char-sequence? "`. (encoding-prefix is C11) — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.5p1 (Syntax) — An *encoding-prefix* is one of `u8`, `u`, `U`, `L`. (`u8`, `u`, `U` are C11; only `L` existed in C99) — mcc:✓ gcc:✓ clang:✓ (`u8"…"`, `u"…"`, `U"…"`, `L"…"` all compile)
- [✓] §6.4.5p1 (Syntax) — An *s-char-sequence* is one or more `s-char`; an *s-char* is any member of the source character set except `"`, `\`, or new-line, or an `escape-sequence`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.5p2 (Constraints) — A sequence of adjacent string literal tokens shall not include both a wide string literal and a UTF-8 string literal. (C11) — mcc:✓ gcc:✓ clang:✓ (`L"a" u8"b"` rejected by all three)
- [✓] §6.4.5p3 (Description) — A character string literal is a sequence of zero or more multibyte characters enclosed in double-quotes (e.g. `"xyz"`). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.5p3 — A UTF-8 string literal is the same, except prefixed by `u8`. (C11) — mcc:✓ gcc:✓ clang:✓ (`char a[]=u8"héllo"`)
- [✓] §6.4.5p3 — A wide string literal is the same, except prefixed by `L`, `u`, or `U`. (`u`/`U` are C11) — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.5p4 — The same considerations apply per element as for integer/wide character constants, except `'` is representable by itself or `\'`, but `"` shall be `\"`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.5p5 (Semantics) — In translation phase 6, adjacent character and identically-prefixed string literal tokens are concatenated into a single multibyte character sequence. — mcc:✓ gcc:✓ clang:✓ (`"ab" "cd"`)
- [✓] §6.4.5p5 — If any token has an encoding prefix the result has that prefix; this shall be the only prefix, except an unprefixed literal may be concatenated with a prefixed one, giving that prefix. (C11) — mcc:✓ gcc:✓ clang:✓ (`"ab" L"cd"` → `wchar_t[]`, runs; `"no" u8"prefix-ok"` accepted)
- [✓] §6.4.5p5 — Whether differently-prefixed wide string literals can be concatenated, and the treatment, is implementation-defined. (C11) — mcc:✓ gcc:✓ clang:✓ (mcc/gcc/clang all **reject** `u"a" U"b"` as unsupported — a permitted implementation-defined choice; consistent)
- [✓] §6.4.5p6 — In translation phase 7, a byte or code of value zero is appended to each multibyte character sequence resulting from a string literal. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.5p6 — The sequence initializes an array of static storage duration and length just sufficient to contain it. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.5p6 — For character string literals, array elements have type `char`, initialized with the individual bytes. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.5p6 — For UTF-8 string literals, array elements have type `char`, initialized with the UTF-8 encoding. (C11) — mcc:✓ gcc:✓ clang:✓ (`u8"héllo"` fits in `char[]`)
- [✓] §6.4.5p6 — For `L` wide string literals, array elements have type `wchar_t`, initialized via `mbstowcs` under an implementation-defined locale. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.5p6 — For `u` wide string literals, array elements have type `char16_t`. (C11) — mcc:✓ gcc:✓ clang:✓ (`char16_t b[]=u"hi"`)
- [✓] §6.4.5p6 — For `U` wide string literals, array elements have type `char32_t`. (C11) — mcc:✓ gcc:✓ clang:✓ (`char32_t c[]=U"hi"`)
- §6.4.5p7 — It is unspecified whether these arrays are distinct provided their elements have the appropriate values. — (unspecified; not separately testable)
- §6.4.5p7 — If the program attempts to modify such an array, the behavior is undefined. — (spec/UB; not separately testable)
- [✓] §6.4.5p8 (EXAMPLE) — `"\x12" "3"` is two characters (`'\x12'`, `'3'`), not a single hex escape, because concatenation occurs after each token is scanned. — mcc:✓ gcc:✓ clang:✓

### §6.4.6 Punctuators
- [✓] §6.4.6p1 (Syntax) — A *punctuator* is one of: `[ ] ( ) { } . ->`, `++ -- & * + - ~ !`, `/ % << >> < > <= >= == != ^ | && ||`, `? : ; ...`, `= *= /= %= += -= <<= >>= &= ^= |=`, `, # ##`, and the digraphs `<: :> <% %> %: %:%:`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.6p1 (Syntax) — The digraph punctuators `<:`, `:>`, `<%`, `%>`, `%:`, and `%:%:` are provided. — mcc:✓ gcc:✓ clang:✓
- §6.4.6p2 (Semantics) — A punctuator has independent syntactic and semantic significance; depending on context it may be an operator (yielding a value/function designator, side effect, or both). — (spec; not separately testable)
- §6.4.6p2 — An operand is an entity on which an operator acts. — (spec; not separately testable)
- [✓] §6.4.6p3 — The six tokens `<: :> <% %> %: %:%:` behave the same as `[ ] { } # ##` respectively, except for spelling. — mcc:✓ gcc:✓ clang:✓ (program written entirely with digraphs compiles & runs; `a %:%: b` pastes)
- [✓] §6.4.6p4 (EXAMPLE) — Constructs using digraphs, e.g. `%:define`, behave identically to `#`-spelled equivalents. — mcc:✓ gcc:✓ clang:✓ (`%:include <stdio.h>` works)

### §6.4.7 Header names
> The `__has_include` and `#pragma`-location recognition contexts in p3 are C11; the `< >` / `" "` header-name forms are C90/C99.
- [✓] §6.4.7p1 (Syntax) — A *header-name* is `< h-char-sequence >` or `" q-char-sequence "`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.7p1 (Syntax) — An *h-char-sequence* is one or more `h-char`; an *h-char* is any member of the source character set except new-line and `>`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.7p1 (Syntax) — A *q-char-sequence* is one or more `q-char`; a *q-char* is any member of the source character set except new-line and `"`. — mcc:✓ gcc:✓ clang:✓
- §6.4.7p2 (Semantics) — The sequences in both forms of header names are mapped in an implementation-defined manner to headers or external source file names as specified in §6.10.2. — (impl-defined; not separately testable)
- §6.4.7p3 — If the characters `'`, `\`, `"`, `//`, or `/*` occur between the `<` and `>` delimiters, the behavior is undefined. — (spec/UB; not separately testable)
- §6.4.7p3 — If the characters `'`, `\`, `//`, or `/*` occur between the `"` delimiters, the behavior is undefined. — (spec/UB; not separately testable)
- [✓] §6.4.7p3 — A header name preprocessing token is recognized only within a `#include` directive, in a `__has_include` expression, or in an implementation-defined location within a `#pragma`. (the `__has_include` and `#pragma` allowances are C11) — mcc:✓ gcc:✓ clang:✓ (`__has_include(<stdio.h>)` supported and `defined(__has_include)` is true)
- §6.4.7p4 (EXAMPLE) — `0x3<1/a.h>1e2` is a sequence of preprocessing tokens whose header-name interpretation depends on context (recognized only in the p3 contexts). — (example; not separately testable)

### §6.4.8 Preprocessing numbers
> The `p`/`P` sign productions (hexadecimal-float exponents) are C99.
- [✓] §6.4.8p1 (Syntax) — A *pp-number* is: `digit`; `. digit`; `pp-number digit`; `pp-number identifier-nondigit`; `pp-number e sign`; `pp-number E sign`; `pp-number p sign` (C99); `pp-number P sign` (C99); or `pp-number .`. — mcc:✓ gcc:✓ clang:✓ (`0x1.2p3.4e+5` survives stringization as one token)
- [✓] §6.4.8p2 (Description) — A pp-number begins with a digit optionally preceded by `.` and may be followed by valid identifier characters and `e±`, `E±`, `p±`, `P±`. (`p±`/`P±` are C99) — mcc:✓ gcc:✓ clang:✓
- §6.4.8p3 (Semantics) — A pp-number has no type or value until a successful phase-7 conversion to a floating or integer constant token. — (spec; not separately testable)

### §6.4.9 Comments
> `//` line comments are a C99 addition (not present in C90).
- [✓] §6.4.9p1 — Except within a character constant, a string literal, or a comment, the characters `/*` introduce a comment. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.9p1 — The contents of a `/* ... */` comment are examined only to identify multibyte characters and to find the terminating `*/`. — mcc:✓ gcc:✓ clang:✓ (unterminated `/*` rejected: "unexpected end of file in comment")
- [✓] §6.4.9p2 — Except within a character constant, a string literal, or a comment, `//` introduces a comment including all multibyte characters up to but not including the next new-line. (C99) — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.9p2 — The contents of a `//` comment are examined only to identify multibyte characters and to find the terminating new-line. (C99) — mcc:✓ gcc:✓ clang:✓
- [✓] §6.4.9p2 — A `//` comment continues through a line-splice (a `\` immediately before the new-line is removed in phase 2), so it extends to the next non-spliced physical new-line. — mcc:✓ gcc:✓ clang:✓ (spliced `//` swallows the following `return 1;` line in all three)
- [✓] §6.4.9p3 (EXAMPLE) — `"a//b"` is a single string literal (not a comment); `f(a//\\b c)` shows `//` comment with line splicing; `/\\/ k();` is a `//` comment because of the line splice in phase 2. — mcc:✓ gcc:✓ clang:✓

### §6.4 mcc-specific lexical gaps (added in gap analysis)
- [✓] §6.4.4.1 (extension) — Binary integer constants `0b…`/`0B…` are a GNU/C23 extension, **not** in C99/C11; a conforming-mode compiler should diagnose them under `-pedantic`. — mcc:✓ gcc:✓ clang:✓ (under `-pedantic-errors` mcc rejects `0b1010` — "binary integer constants are a C23/GNU extension" — matching gcc/clang, which reject it as "binary constants are a C23 feature/extension")
- [✓] §6.4.4.4 (extension) — `\e` (ESC) is a non-ISO escape sequence, **not** in C99/C11; should be diagnosed under `-pedantic`. — mcc:✓ gcc:✓ clang:✓ (under `-pedantic-errors` mcc rejects `'\e'` — "\e is a non-ISO escape sequence" — matching gcc/clang, which flag "non-ISO-standard escape sequence `\e`")
- [✓] §6.4.4.4p1 (edge) — An empty character constant `''` has no `c-char` and is a constraint violation requiring a diagnostic. — mcc:✓ gcc:✓ clang:~ (mcc & gcc **error** "empty character constant"; clang only **warns** under `-Winvalid-pp-token`. mcc matches gcc and is the stricter/correct behavior)
- [✓] §6.4.4.4p6 (edge) — A `\x` hexadecimal escape with no following hex digit is ill-formed (a diagnostic is required). — mcc:✓ gcc:✓ clang:✓ (covered by escape-sequence syntax; all reject)
- [✓] §6.4.2.1 (extension) — `$` as an ordinary (non-UCN) identifier character is an implementation-defined extension; mcc, gcc and clang all accept `a$b`. — mcc:✓ gcc:✓ clang:✓ (consistent extension)
- [✓] §6.4.3p2 (edge) — The `$` (`$`) UCN exception: mcc & gcc honor the standard exception and accept it in identifiers; clang rejects it — a clang-side divergence, mcc conforms. — mcc:✓ gcc:✓ clang:✗
- [✓] §6.4.5 (multibyte) — A universal character name / UTF-8 multibyte character inside a narrow string literal is encoded as its UTF-8 bytes. — mcc:✓ gcc:✓ clang:✓ (`"é"` → first byte 0xC3 == 195 at runtime on all three)


## §6.5.1-§6.5.4 Primary/postfix/unary expressions

> mcc status: 96 TODOs in this section; mcc-gaps = 2 (const-qualified operand of `++`/`--` is only a *warning*, not an error; cast of a non-scalar to its own type is not diagnosed even under `-pedantic-errors`). Everything else (generic selection, compound literals, `_Alignof`, anonymous members, atomic RMW `++`, VLA-`sizeof` evaluation, K&R default-argument promotions, all the operand/operator constraints) conforms and matches the gcc==clang consensus.

### §6.5 Expressions (general)

> C11 reframes evaluation order in terms of *sequenced/unsequenced/indeterminately-sequenced* (footnote-defined in §5.1.2.3); C99 used "sequence points" plus an explicitly *unspecified* order of subexpression evaluation. The §6.5p2 data-race/multiple-modification rule is therefore worded differently in the two standards but covers the same UB.

- §6.5p1 — An expression is a sequence of operators and operands that specifies a computation of a value, designates an object or a function, generates side effects, or performs a combination thereof. — (spec; not separately testable)
- §6.5p1 (C11) — The value computations of the operands of an operator are sequenced before the value computation of the result of the operator. — (spec; not separately testable)
- [✓] §6.5p2 (C99-only) — Between the previous and next sequence point an object shall have its stored value modified at most once by the evaluation of an expression; furthermore, the prior value shall be read only to determine the value to be stored (otherwise undefined behavior). — mcc:✓ gcc:✓ clang:✓ (UB; all three warn: mcc "operation on 'i' may be undefined", gcc `-Wsequence-point`, clang `-Wunsequenced`)
- [✓] §6.5p2 (C11) — If a side effect on a scalar object is unsequenced relative to either a different side effect on the same scalar object or a value computation using the value of the same scalar object, the behavior is undefined; and if multiple orderings of the subexpressions exist, the behavior is undefined if such an unsequenced side effect occurs in any of them. — mcc:✓ gcc:✓ clang:✓ (diagnosed for `i = i++ + 1`)
- §6.5p3 — The grouping of operators and operands is indicated by the syntax (except as specified later for `()`/`&&`/`||`/`?:`/`,`). (C11: "Except as specified later, side effects and value computations of subexpressions are unsequenced." C99: the order of evaluation of subexpressions and the order in which side effects take place are both unspecified.) — (spec; not separately testable)
- [✓] §6.5p4 (footnote/normative note) — The bitwise operators — the unary `~` and the binary `<<`, `>>`, `&`, `^`, `|` — require operands of integer type, yield values that depend on the internal representation of integers, and have implementation-defined and undefined aspects for signed types. — mcc:✓ gcc:✓ clang:✓ (`~d` on a `double` rejected by all three)
- §6.5p5 — If an exceptional condition occurs during the evaluation of an expression (the result is not mathematically defined or not in the range of representable values for its type), the behavior is undefined. — (UB; not separately testable)
- §6.5p6 — The effective type of an object for an access to its stored value is the declared type of the object, if any. — (spec; not separately testable)
- §6.5p6 — If a value is stored into an object having no declared type through an lvalue whose type is not a character type, the lvalue's type becomes the effective type of the object for that access and for subsequent non-modifying accesses. — (spec; not separately testable)
- §6.5p6 — If a value is copied into an object having no declared type using `memcpy` or `memmove`, or is copied as an array of character type, the effective type of the modified object for that access and subsequent non-modifying accesses is the effective type of the source object, if it has one. — (spec; not separately testable)
- §6.5p6 — For all other accesses to an object having no declared type, the effective type is simply the type of the lvalue used for the access. — (spec; not separately testable)
- §6.5p7 — An object shall have its stored value accessed only by an lvalue expression of one of the following types (strict aliasing rule): a type compatible with the effective type; a qualified version of a compatible type; the signed or unsigned type corresponding to the effective type; the signed or unsigned type corresponding to a qualified version of the effective type; an aggregate or union type that includes one of the foregoing among its members (including, recursively, a member of a subaggregate or contained union); or a character type. — (UB/optimization; not separately testable here)
- §6.5p8 — A floating expression may be *contracted* — evaluated as though a single operation, omitting source-implied rounding errors; the `FP_CONTRACT` pragma in `<math.h>` can disallow contraction, otherwise whether and how expressions are contracted is implementation-defined. — (impl-defined; not separately testable)

### §6.5.1 Primary expressions

> C11 adds *generic-selection* to the primary-expression grammar (new §6.5.1p6 and §6.5.1.1).

- [✓] §6.5.1p1 — Syntax: a primary expression is an *identifier*, a *constant*, a *string-literal*, `( expression )`, or (C11) a *generic-selection*. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.1p2 — An identifier is a primary expression provided it has been declared as designating an object (then it is an lvalue) or a function (then it is a function designator). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.1p2 — An identifier that has not been declared is a constraint violation (C99/C11 removed implicit function declaration; an undeclared identifier used as a primary expression is ill-formed). — mcc:✓ gcc:✓ clang:✓ (`error: 'undeclared_xyz' undeclared`)
- [✓] §6.5.1p3 — A constant is a primary expression whose type depends on its form and value (per §6.4.4). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.1p4 — A string literal is a primary expression; it is an lvalue with type as detailed in §6.4.5. — mcc:✓ gcc:✓ clang:✓ (`&"abc"` yields `char(*)[4]`)
- [✓] §6.5.1p5 — A parenthesized expression is a primary expression whose type and value are identical to those of the unparenthesized expression; it is an lvalue, a function designator, or a void expression iff the unparenthesized expression is, respectively, an lvalue, a function designator, or a void expression. — mcc:✓ gcc:✓ clang:✓ (`(x)=5` assigns)
- [✓] §6.5.1p6 (C11) — A generic selection is a primary expression; its type and value are those described in §6.5.1.1. — mcc:✓ gcc:✓ clang:✓

### §6.5.1.1 Generic selection (C11)

> Entire subsection is C11-only.

- [✓] §6.5.1.1p1 (C11) — Syntax: `_Generic ( assignment-expression , generic-assoc-list )`, where *generic-assoc-list* is a comma-separated list of *generic-associations*, each `type-name : assignment-expression` or `default : assignment-expression`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.1.1p2 (C11) — Constraint: a generic selection shall have no more than one `default` generic association. — mcc:✓ gcc:✓ clang:✓ (two `default` rejected)
- [~] §6.5.1.1p2 (C11) — Constraint: the type name in a generic association shall specify a complete object type other than a variably modified type. — mcc:~ gcc:~ clang:~ (VLA assoc `int[n]:` rejected by all three; **none** of the three diagnose an *incomplete* assoc type `struct X:` when it does not match the controller — all silently accept)
- [✓] §6.5.1.1p2 (C11) — Constraint: no two generic associations in the same generic selection shall specify compatible types. — mcc:✓ gcc:✓ clang:✓ (`int:…, int:…` → mcc "specifies two compatible types")
- [✓] §6.5.1.1p2 (C11) — Constraint: the controlling expression's type (after lvalue, array-to-pointer, and function-to-pointer conversion) shall be compatible with at most one of the listed type names. — mcc:✓ gcc:✓ clang:✓ (`const int` controller matches `int` assoc, not `const int`; `a[5]` controller matches `int*`)
- [✓] §6.5.1.1p2 (C11) — Constraint: if there is no `default` association, the controlling expression's type shall be compatible with exactly one listed type name. — mcc:✓ gcc:✓ clang:✓ (`long` with only `int`/`double` assocs and no default → `error: type 'long' does not match any association`)
- [✓] §6.5.1.1p3 (C11) — Semantics: the controlling expression of a generic selection is not evaluated. — mcc:✓ gcc:✓ clang:✓ (`_Generic(side(),…)` does not run `side()`)
- [✓] §6.5.1.1p3 (C11) — The result expression is the expression of the generic association whose type name is compatible with the controlling expression's type, or otherwise the expression of the `default` association. — mcc:✓ gcc:✓ clang:✓ (`default` selected regardless of its list position)
- [✓] §6.5.1.1p3 (C11) — None of the expressions from any non-selected generic association is evaluated. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.1.1p4 (C11) — The type and value of the generic selection are identical to those of its result expression; it is an lvalue, a function designator, or a void expression iff its result expression is, respectively, one of those. — mcc:✓ gcc:✓ clang:✓ (`_Generic(x,int:x,…) = 99` assigns through the selected lvalue)
- [✓] §6.5.1.1 (added; constraint corner) — A *function-type* generic association (`void(void):`) is a non-object type: mcc and gcc accept it, **clang rejects** it. — mcc:✓ gcc:✓ clang:✗ (clang stricter; mcc matches gcc)

### §6.5.2 Postfix operators

- [✓] §6.5.2p1 — Syntax: a *postfix-expression* is a primary-expression, or a postfix-expression followed by one of: `[ expression ]` (array subscript), `( argument-expression-list_opt )` (function call), `. identifier` or `-> identifier` (member access), `++` or `--` (postfix increment/decrement), or `( type-name ) { initializer-list }` / `( type-name ) { initializer-list , }` (compound literal). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.2p1 — The *argument-expression-list* is a comma-separated list of *assignment-expression*s. — mcc:✓ gcc:✓ clang:✓

### §6.5.2.1 Array subscripting

- [✓] §6.5.2.1p1 — Constraint: in `E1[E2]`, one expression shall have type "pointer to complete object type", the other shall have integer type, and the result has type "type". — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.2.1p2 — Semantics: `E1[E2]` is by definition identical to `(*((E1)+(E2)))`; because of the conversion rules for binary `+`, if `E1` is (a pointer to the initial element of) an array object and `E2` is an integer, `E1[E2]` designates the `E2`-th element (counting from zero). — mcc:✓ gcc:✓ clang:✓ (`1[a]` == `a[1]`)
- [✓] §6.5.2.1p3 — Successive subscripts designate elements of a multidimensional array: for an n-dimensional array (n ≥ 2) with dimensions `i × j × … × k`, `E` used as other than an lvalue is converted to a pointer to an (n−1)-dimensional array `j × … × k`; applying `*` (explicitly or implicitly via `[]`) yields the referenced (n−1)-dimensional array, itself converted to a pointer if used as other than an lvalue — so arrays are stored in row-major order (last subscript varies fastest). — mcc:✓ gcc:✓ clang:✓ (`a[1][2]==(&a[0][0])[5]`; `sizeof(a[0])==12`)

### §6.5.2.2 Function calls

> C99 says an argument may be "any object type"; C11 tightens this to "any *complete* object type". C99 §6.5.2.2p9 states the order of evaluation of designator/arguments/subexpressions is unspecified with a sequence point before the call; C11 §6.5.2.2p9 instead specifies a sequence point after the evaluations but before the call and makes other evaluations *indeterminately sequenced* with respect to the called function's body.

- [✓] §6.5.2.2p1 — Constraint: the expression denoting the called function shall have type pointer to function returning `void` or returning a complete object type other than an array type. — mcc:✓ gcc:✓ clang:✓ (calling a non-function → mcc "function pointer expected"; a function returning an array type is rejected at declaration)
- [✓] §6.5.2.2p2 — Constraint: if the function's type includes a prototype, the number of arguments shall agree with the number of parameters, and each argument shall have a type assignable to an object with the unqualified version of its corresponding parameter's type. — mcc:✓ gcc:✓ clang:✓ (too-few → "too few arguments to function"; too-many → "too many arguments to function")
- [✓] §6.5.2.2p3 — Semantics: a postfix expression followed by parentheses containing a possibly-empty comma-separated list of expressions is a function call; the postfix expression denotes the called function and the list specifies the arguments. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.2.2p4 — An argument may be an expression of any complete object type (C99: any object type); in preparing for the call, the arguments are evaluated and each parameter is assigned the value of its corresponding argument. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.2.2p5 — If the called function's type does not include a prototype, the *default argument promotions* are performed on each argument: the integer promotions, and arguments of type `float` are promoted to `double`. — mcc:✓ gcc:✓ clang:✓ (K&R `f(char, float)` against `f(int a; double b;)` returns 7 on all three)
- §6.5.2.2p5 — If the number of arguments does not equal the number of parameters, the behavior is undefined. — (UB; not separately testable)
- §6.5.2.2p5 — If a no-prototype call reaches a function *defined* with a prototype, the behavior is undefined if that prototype ends with an ellipsis or the promoted argument types are incompatible with the parameter types. — (UB; not separately testable)
- §6.5.2.2p5 — If a no-prototype call reaches a function defined *without* a prototype, the behavior is undefined if the promoted argument types are incompatible with the promoted parameter types, except: (a) one promoted type is a signed integer type and the other is the corresponding unsigned integer type and the value is representable in both; or (b) both types are pointers to qualified or unqualified versions of a character type or of `void`. — (UB; not separately testable)
- [✓] §6.5.2.2p6 — If the called function's type includes a prototype, the arguments are implicitly converted, as if by assignment, to the unqualified versions of the corresponding parameters' declared types; the ellipsis notation stops argument-type conversion after the last declared parameter, and the default argument promotions are performed on trailing (variadic) arguments. — mcc:✓ gcc:✓ clang:✓ (`printf("%g", floatval)` promotes `float`→`double`)
- [✓] §6.5.2.2p7 — No other conversions are performed implicitly; in particular, the number and types of arguments are not compared with those of the parameters in a function definition that does not include a prototype declarator. — mcc:✓ gcc:✓ clang:✓
- §6.5.2.2p8 — If the function is *defined* with a type incompatible with the type pointed to by the expression that denotes the called function, the behavior is undefined. — (UB; not separately testable)
- §6.5.2.2p9 — There is a sequence point after the evaluations of the function designator and the actual arguments but before the actual call. (C11: every evaluation in the caller not otherwise sequenced is indeterminately sequenced with respect to the called function's body. C99: the order of evaluation of the function designator, arguments, and their subexpressions is unspecified.) — (spec; not separately testable)
- [✓] §6.5.2.2p10 — Recursive function calls shall be permitted, both directly and indirectly through any chain of other functions. — mcc:✓ gcc:✓ clang:✓ (`fib(10)==55`)

### §6.5.2.3 Structure and union members

> C11 adds *atomic* qualified types to the operand constraints (p1/p2), the rule that accessing a member of an *atomic* struct/union is UB (p3), and anonymous structures/unions (p6 in N1570).

- [✓] §6.5.2.3p1 — Constraint: the first operand of `.` shall have atomic (C11), qualified, or unqualified structure or union type, and the second operand shall name a member of that type. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.2.3p2 — Constraint: the first operand of `->` shall have type "pointer to atomic (C11), qualified, or unqualified structure" or "pointer to atomic, qualified, or unqualified union", and the second operand shall name a member of the pointed-to type. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.2.3p3 — Semantics: a postfix expression followed by `.` and an identifier designates a member; the value is that of the named member and is an lvalue if the first expression is an lvalue; if the first expression has qualified type, the result has the so-qualified version of the member's type. — mcc:✓ gcc:✓ clang:✓
- [ ] §6.5.2.3p3 (C11) — Accessing a member of an *atomic* structure or union object results in undefined behavior. — (UB; all three permit whole-object copy `t = as` of an `_Atomic struct`)
- [✓] §6.5.2.3p4 — Semantics: a postfix expression followed by `->` and an identifier designates a member of the object the first expression points to; the value is that of the named member and is an lvalue; if the first expression is a pointer to a qualified type, the result has the so-qualified version of the member's type. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.2.3p5 — Common-initial-sequence guarantee for unions: if a union contains several structures sharing a common initial sequence, and the union currently contains one of them, the common initial part of any of them may be inspected anywhere a declaration of the completed union type is visible. — mcc:✓ gcc:✓ clang:✓ (`u.a.tag` written, `u.b.tag` reads same value)
- [✓] §6.5.2.3p6 — Two structures share a common initial sequence if corresponding members have compatible types (and, for bit-fields, the same widths) for a sequence of one or more initial members. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.2.3p6 (C11) — Anonymous structures/unions: a `.` or `->` whose member is an anonymous structure or union member behaves as if the access were applied to the containing structure or union (member access transparently reaches into nested anonymous members). — mcc:✓ gcc:✓ clang:✓ (nested anon union `.b` and anon struct `.d` reachable directly)

### §6.5.2.4 Postfix increment and decrement operators

> C11 adds *atomic* operand support and specifies the atomic read-modify-write semantics; C99 expresses the side effect via sequence points.

- [~] §6.5.2.4p1 — Constraint: the operand of postfix `++`/`--` shall have atomic (C11), qualified, or unqualified real or pointer type, and shall be a modifiable lvalue. — mcc:~ gcc:✓ clang:✓ (**mcc gap**: `x++` on a `const int` is only a *warning* "assignment of read-only location" and compiles; gcc/clang reject. `-Werror`/`-pedantic-errors` promote it to an error. Array/non-lvalue operands `a++` are correctly rejected.)
- [✓] §6.5.2.4p2 — Semantics: the result of postfix `++` is the *value of the operand*; after the result is obtained, the operand's stored value is incremented (the value 1 of the appropriate type is added to it). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.2.4p2 — The constraints, type, value, and conversions of the postfix `++` operand are those of the additive operators (§6.5.6); for a pointer operand the increment is by the size of the pointed-to type. — mcc:✓ gcc:✓ clang:✓ (`int*` post-increment advances by one element)
- [✓] §6.5.2.4p2 — The side effect of updating the operand's stored value occurs between the previous and next sequence point (C99); C11: the value computation of the result is sequenced before the side effect of updating the operand. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.2.4p2 (C11) — Postfix `++` is a single evaluation with respect to an indeterminately-sequenced function call; on an *atomic* operand it is an atomic read-modify-write with `memory_order_seq_cst` ordering. — mcc:✓ gcc:✓ clang:✓ (`_Atomic int x=0; v=x++;` → `v==0, x==1` on all three)
- [✓] §6.5.2.4p3 — Semantics: postfix `--` is analogous to postfix `++` except the operand's value is decremented (the value 1 of the appropriate type is subtracted from it). — mcc:✓ gcc:✓ clang:✓

### §6.5.2.5 Compound literals (C99)

> C99 feature (not in C90). C11 renumbers the referenced initializer-list section from §6.7.8 to §6.7.9 and adds the explicit p2 constraint that all initializer-list constraints apply.

- [✓] §6.5.2.5p1 — Constraint: the *type-name* in `( type-name ) { initializer-list }` shall specify a complete object type or an array of unknown size, but not a variable length array type. — mcc:✓ gcc:✓ clang:✓ (`(int[n]){0}` rejected: mcc "variable length array cannot be initialized")
- [✓] §6.5.2.5p2 (C11) — Constraint: all the constraints for initializer lists in §6.7.9 (C11) / §6.7.8 (C99) also apply to compound literals. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.2.5p3 — Semantics: a parenthesized type name followed by a brace-enclosed initializer list is a *compound literal*; it provides an unnamed object whose value is given by the initializer list. — mcc:✓ gcc:✓ clang:✓ (`(struct P){.x=3,.y=4}`, `(int[]){10,20,30}`)
- [✓] §6.5.2.5p4 — If the type name specifies an array of unknown size, the size is determined by the initializer list (§6.7.9 / §6.7.8) and the compound literal's type is the completed array type; otherwise its type is that specified by the type name; in either case the result is an lvalue. — mcc:✓ gcc:✓ clang:✓ (`&(int){42}` yields a usable pointer)
- [✓] §6.5.2.5p5 — The compound literal's object has static storage duration if it occurs outside the body of a function; otherwise it has automatic storage duration associated with the enclosing block. — mcc:✓ gcc:✓ clang:✓ (file-scope `int *gp=(int[]){5,6,7};` links and reads correctly)
- [✓] §6.5.2.5p6 — All the semantic rules and constraints for initializer lists (§6.7.9 / §6.7.8) are applied to compound literals. — mcc:✓ gcc:✓ clang:✓
- §6.5.2.5p7 — String literals, and compound literals with const-qualified types, need not designate distinct objects. — (unspecified/impl latitude; not separately testable)
- §6.5.2.5p8 — The order in which side effects occur among the initialization-list expressions of a compound literal is unspecified. — (unspecified; not separately testable)
- [✓] §6.5.2.5p9 — Each compound literal creates only a single object in a given scope (a compound literal in a loop or recursive block entry denotes the same object across iterations until the block is re-entered, not a fresh object per evaluation). — mcc:✓ gcc:✓ clang:✓ (address of `(int[]){i}` is stable across loop iterations on all three)

### §6.5.3 Unary operators

> C11 extends the unary grammar with `_Alignof ( type-name )`.

- [✓] §6.5.3p1 — Syntax: a *unary-expression* is a postfix-expression, or one of `++ unary-expression`, `-- unary-expression`, `unary-operator cast-expression`, `sizeof unary-expression`, `sizeof ( type-name )`, or (C11) `_Alignof ( type-name )`; the *unary-operator* is one of `& * + - ~ !`. — mcc:✓ gcc:✓ clang:✓

### §6.5.3.1 Prefix increment and decrement operators

> C11 adds atomic operand support and atomic read-modify-write semantics (inherited from the `+=`/`-=` rules).

- [~] §6.5.3.1p1 — Constraint: the operand of prefix `++`/`--` shall have atomic (C11), qualified, or unqualified real or pointer type, and shall be a modifiable lvalue. — mcc:~ gcc:✓ clang:✓ (**mcc gap**: `++x` on a `const int` (and `s.a++` on a member of a `const struct`) is only a *warning* and compiles; gcc/clang reject. `-Werror`/`-pedantic-errors` escalate to error.)
- [✓] §6.5.3.1p2 — Semantics: the value of the prefix `++` operand is incremented; the result is the new (incremented) value. `++E` is equivalent to `(E+=1)`; constraints, conversions, type, value, and side effect are those of the `+=` compound assignment and the additive operators. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.3.1p3 — Semantics: prefix `--` is analogous to prefix `++` except the operand is decremented; `--E` is equivalent to `(E-=1)`. — mcc:✓ gcc:✓ clang:✓

### §6.5.3.2 Address and indirection operators

- [✓] §6.5.3.2p1 — Constraint: the operand of unary `&` shall be a function designator, the result of a `[]` or unary `*` operator, or an lvalue designating an object that is not a bit-field and is not declared with the `register` storage-class specifier. — mcc:✓ gcc:✓ clang:✓ (`&register-var` → "address of register variable 'x' requested"; `&s.bitfield` → "cannot take address of bit-field")
- [✓] §6.5.3.2p2 — Constraint: the operand of unary `*` shall have pointer type. — mcc:✓ gcc:✓ clang:✓ (`*x` on an `int` → "pointer expected")
- [✓] §6.5.3.2p3 — Semantics: unary `&` yields the address of its operand; if the operand has type "type", the result has type "pointer to type". — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.3.2p3 — If the `&` operand is the result of a unary `*`, neither `*` nor `&` is evaluated and the result is as if both were omitted (the operator constraints still apply and the result is not an lvalue). — mcc:✓ gcc:✓ clang:✓ (`&*p` with null `p` yields null, no fault)
- [✓] §6.5.3.2p3 — If the `&` operand is the result of a `[]` operator, neither `&` nor the implied `*` is evaluated and the result is as if `&` were removed and `[]` were changed to a `+` operator. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.3.2p3 — Otherwise, the result of `&` is a pointer to the object or function designated by its operand. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.3.2p4 — Semantics: unary `*` denotes indirection; if the operand points to a function the result is a function designator, and if it points to an object the result is an lvalue designating the object; if the operand has type "pointer to type", the result has type "type". — mcc:✓ gcc:✓ clang:✓
- §6.5.3.2p4 — If an invalid value has been assigned to the pointer, the behavior of unary `*` is undefined (invalid values per footnote: a null pointer, an address inappropriately aligned for the pointed-to type, or the address of an object after the end of its lifetime). — (UB; not separately testable)

### §6.5.3.3 Unary arithmetic operators

- [✓] §6.5.3.3p1 — Constraint: the operand of unary `+` or `-` shall have arithmetic type; the operand of `~` shall have integer type; the operand of `!` shall have scalar type. — mcc:✓ gcc:✓ clang:✓ (`+p` on a pointer → mcc "pointer not accepted for unary plus"; `~d` on a `double` rejected; `!p` on a pointer accepted)
- [✓] §6.5.3.3p2 — Semantics: the result of unary `+` is the value of its (promoted) operand; the integer promotions are performed and the result has the promoted type. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.3.3p3 — Semantics: the result of unary `-` is the negative of its (promoted) operand; the integer promotions are performed and the result has the promoted type. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.3.3p4 — Semantics: the result of `~` is the bitwise complement of its (promoted) operand (each result bit set iff the corresponding converted-operand bit is not set); the integer promotions are performed and the result has the promoted type. If the promoted type is unsigned, `~E` equals the maximum value representable in that type minus `E`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.3.3p5 — Semantics: the result of logical negation `!` is `0` if the operand compares unequal to 0 and `1` if it compares equal to 0; the result has type `int`; `!E` is equivalent to `(0==E)`. — mcc:✓ gcc:✓ clang:✓ (`!nullptr==1`, `!!42==1`)

### §6.5.3.4 The `sizeof` and `_Alignof` operators (C11; "The `sizeof` operator" in C99)

> C11 merges the new `_Alignof` operator into this subsection (new constraint in p1, new semantics in p3), shifting the C99 paragraph numbers: C99 p2/p3/p4 → C11 p2/p4/p5.

- [✓] §6.5.3.4p1 — Constraint: `sizeof` shall not be applied to an expression of function type or incomplete type, to the parenthesized name of such a type, or to an expression designating a bit-field member. — mcc:✓ gcc:✓ clang:✓ (bit-field & incomplete type rejected unconditionally; function type & `void` are a GNU extension (`=1`) accepted by default by all three and rejected by all three under `-pedantic-errors` — mcc: "'sizeof' applied to a function type" / "applied to a void type")
- [✓] §6.5.3.4p1 (C11) — Constraint: `_Alignof` shall not be applied to a function type or an incomplete type. — mcc:✓ gcc:✓ clang:✓ (incomplete type rejected unconditionally: mcc "'_Alignof' applied to an incomplete type"; function type is a GNU extension accepted by default by all three, rejected by all under `-pedantic-errors`)
- [✓] §6.5.3.4p2 — Semantics: `sizeof` yields the size in bytes of its operand (an expression, or the parenthesized name of a type), determined from the operand's type; if the operand's type is a variable length array type the operand is evaluated, otherwise the operand is not evaluated and the result is an integer constant. — mcc:✓ gcc:✓ clang:✓ (`sizeof(int[side()])` runs `side()` exactly once; non-VLA `sizeof` is a constant expression usable as an array bound)
- [✓] §6.5.3.4p3 (C11) — Semantics: `_Alignof` yields the alignment requirement of its operand type; the operand is not evaluated and the result is an integer constant; applied to an array type it yields the alignment requirement of the element type. — mcc:✓ gcc:✓ clang:✓ (`_Alignof(int[10])==_Alignof(int)==4`)
- [✓] §6.5.3.4p4 (C11; §6.5.3.4p3 in C99) — When `sizeof` is applied to an operand of type `char`, `unsigned char`, or `signed char` (or a qualified version thereof) the result is 1. — mcc:✓ gcc:✓ clang:✓ (`sizeof(char)==1`)
- [✓] §6.5.3.4p4 (C11; §6.5.3.4p3 in C99) — When `sizeof` is applied to an array-type operand the result is the total number of bytes in the array. — mcc:✓ gcc:✓ clang:✓ (`sizeof "hello"==6` including the NUL)
- [✓] §6.5.3.4p4 (C11; §6.5.3.4p3 in C99) — When `sizeof` is applied to a structure- or union-type operand the result is the total number of bytes in such an object, including internal and trailing padding. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.3.4p5 (C11; §6.5.3.4p4 in C99) — The value of the result of `sizeof` (and, C11, `_Alignof`) is implementation-defined, and its type is the unsigned integer type `size_t` defined in `<stddef.h>` (and other headers). — mcc:✓ gcc:✓ clang:✓ (`sizeof(int)-5 > 0` is *true*, confirming the unsigned `size_t` result type)

### §6.5.4 Cast operators

> C11 adds *atomic* type names to the constraint (p2) and promotes the pointer↔floating restriction to an explicit constraint (p4); in C99 that restriction appears as semantics.

- [✓] §6.5.4p1 — Syntax: a *cast-expression* is a unary-expression, or `( type-name ) cast-expression`. — mcc:✓ gcc:✓ clang:✓
- [~] §6.5.4p2 — Constraint: unless the type name specifies a `void` type, the type name shall specify atomic (C11), qualified, or unqualified scalar type, and the operand shall have scalar type. — mcc:~ gcc:✓ clang:✓ (**mcc gap**: casting a non-scalar to its own type, e.g. `(struct S)s`, is silently accepted by mcc *even under* `-pedantic-errors`; gcc emits "ISO C forbids casting nonscalar to the same type" and clang "C99 forbids casting nonscalar type … to the same type" under `-pedantic`. A `(void)expr` cast and a cast of a `void` function call to `void` are accepted by all three.)
- [✓] §6.5.4p3 — Constraint: conversions involving pointers, other than where permitted by §6.5.16.1 (simple assignment), shall be specified by an explicit cast. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.4p4 (C11 constraint; C99 semantics) — A pointer type shall not be converted to any floating type, and a floating type shall not be converted to any pointer type. — mcc:✓ gcc:✓ clang:✓ (both directions rejected: mcc "cannot cast between a floating type and a pointer")
- [✓] §6.5.4p4 — Semantics: preceding an expression by a parenthesized type name converts the value to the named type (a *cast*); a cast that specifies no conversion has no effect on the type or value of an expression. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.4p4 — A cast does not yield an lvalue; the result of a cast is never an lvalue. — mcc:✓ gcc:✓ clang:✓ (`((char)x)=5` → mcc "lvalue expected")
- §6.5.4p5 — If the value of the expression is represented with greater range or precision than required by the named type (§6.3.1.8 permits excess range/precision), the cast specifies a conversion even when the expression type equals the named type, and removes any extra range and precision. — (impl-defined `FLT_EVAL_METHOD`; not separately testable here)

### Added / cross-cutting items (gaps and corners the draft did not enumerate)

- [✓] §6.5.3.4 (added) — `<stdalign.h>` provides `alignof` as a macro for `_Alignof`. — mcc:✓ gcc:✓ clang:✓ (`runtime/include/stdalign.h` present; `alignof(double)==8`)
- [✓] §6.5.2.4 / §6.5.3.1 (added) — `<stdatomic.h>` is bundled and `_Atomic int` read-modify-write `++`/`--` lowers to a sequentially-consistent atomic op. — mcc:✓ gcc:✓ clang:✓ (`runtime/include/stdatomic.h` present)
- [✓] §6.5.3p1 (added; GNU ext) — `_Alignof`/`__alignof__` applied to an *expression* (`_Alignof x`) rather than a type-name — a GNU extension outside the C11 grammar. — mcc:✓ gcc:✓ clang:✓ (mcc & gcc accept silently; clang warns `-Wgnu-alignof-expression`; all accept)
- [✓] §6.5.2.5 (added) — A compound literal is a *modifiable* lvalue when not const-qualified: its members can be assigned and its address taken (`&(int){42}`, `(int[]){…}[i]=…`). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.4p4 (added) — A cast of a value to the *same* scalar type is a no-op that preserves the value but still drops lvalue-ness (covered by p4 "no effect on type or value" + non-lvalue rule). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.3.2p1 (added) — Taking `&` of a string literal yields a pointer-to-array (`&"abc"` has type `char(*)[4]`), confirming string literals are lvalues. — mcc:✓ gcc:✓ clang:✓
- [✗] §6.5.1.1p2 (added; latent in all three) — An *incomplete* (non-matching) generic-association type name is a constraint violation that **none** of mcc/gcc/clang diagnose. — mcc:✗ gcc:✗ clang:✗ (no compiler in this matrix enforces the "complete object type" requirement when the assoc type does not match the controller)


## §6.5.5-§6.5.14 Binary operators

> mcc status: 65 TODOs, 0 mcc-gaps — mcc matches the gcc==clang consensus on every mechanically-testable item (constraint diagnostics, parse precedence/associativity, division truncation/remainder identity, pointer arithmetic & subtraction, shift semantics, short-circuit evaluation, complex equality).

> Cross-cutting (cited where relevant, defined elsewhere): signed-integer arithmetic overflow in `*`, `+`, `-`, `<<` is undefined behavior per §6.5p5; the "usual arithmetic conversions" are defined in §6.3.1.8; the integer promotions in §6.3.1.1; a null pointer constant in §6.3.2.3; `ptrdiff_t`/`<stddef.h>` in §7.19. Each binary operator's grammar production names its precedence/associativity; for every arithmetic operator the result type is the common type produced by the usual arithmetic conversions.

### §6.5.5 Multiplicative operators

- [✓] §6.5.5p1 — Parse `*`, `/`, and `%` as left-associative binary operators binding tighter than additive operators (multiplicative-expression grammar). — mcc:✓ gcc:✓ clang:✓ (precedence probe `2+3*4`==14 agrees)
- [✓] §6.5.5p2 — Constraint: both operands of `*` and `/` must have arithmetic type; both operands of `%` must have integer type; reject anything else. — mcc:✓ gcc:✓ clang:✓ (`double%double` and `ptr*ptr` rejected by all)
- §6.5.5p3 — Apply the usual arithmetic conversions to the operands of `*`, `/`, and `%`. — (spec; result-type behavior, not separately testable)
- [✓] §6.5.5p4 — Yield the arithmetic product of the operands for binary `*`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.5p5 — Yield the quotient (first operand divided by second) for `/` and the remainder for `%`; if the second operand (divisor) is zero, the behavior is undefined. — mcc:✓ gcc:✓ clang:✓ (divisor==0 is UB, not diagnosed by any; consensus)
- [✓] §6.5.5p6 — For integer operands `/` discards any fractional part (truncates toward zero), and whenever `a/b` is representable the identity `(a/b)*b + a%b == a` must hold (this fixes the sign and rounding of `%`). — mcc:✓ gcc:✓ clang:✓ (`-7/2,7/-2,-7%2,7%-2` == `-3 -3 -1 1`; identity holds)
- [✓] §6.5p5 — Signed-integer overflow of `*`, `/`, `%` (result, including `INT_MIN/-1`, not representable in the result type) is undefined behavior (general rule, applies to multiplicative operators). — mcc:✓ gcc:✓ clang:✓ (UB; not diagnosed at compile time by any — consensus)

### §6.5.6 Additive operators

- [✓] §6.5.6p1 — Parse `+` and `-` as left-associative binary operators binding looser than multiplicative and tighter than shift operators (additive-expression grammar). — mcc:✓ gcc:✓ clang:✓ (`1<<2+1`==8 confirms `+` binds tighter than `<<`)
- [✓] §6.5.6p2 — Constraint for `+`: either both operands have arithmetic type, or one operand is a pointer to a complete object type (C11; C99-only: "object type") and the other has integer type; reject otherwise. — mcc:✓ gcc:✓ clang:✓ (`ptr+ptr` rejected by all)
- [✓] §6.5.6p2 — Constraint for `-`: either both operands have arithmetic type; or both are pointers to qualified/unqualified versions of compatible complete object types (C11; C99-only: "object types"); or the left operand is such a pointer and the right operand has integer type; reject otherwise. — mcc:✓ gcc:✓ clang:✓ (`structA* - structB*` rejected by all)
- §6.5.6p3 — When both operands have arithmetic type, perform the usual arithmetic conversions on them. — (spec; not separately testable)
- [✓] §6.5.6p4 — Yield the sum of the operands for binary `+`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.6p5 — Yield the difference (first operand minus second) for binary `-`. — mcc:✓ gcc:✓ clang:✓
- §6.5.6p6 — Treat a pointer to an object that is not an array element as a pointer to the first element of a length-one array of that object's type, for the purposes of additive pointer operations. — (spec; semantic model, not separately testable)
- [✓] §6.5.6p7 — When integer `N` is added to / subtracted from a pointer, the result has the pointer operand's type and points to the element offset by `N` (from the i-th element to the (i+N)-th for `P+N`/`N+P`, or the (i−N)-th for `P−N`). — mcc:✓ gcc:✓ clang:✓ (`*(p+3)`==a[3])
- [✓] §6.5.6p7 — A pointer addressing the last array element plus one yields a valid pointer that points one past the last element. — mcc:✓ gcc:✓ clang:✓ (`a+3 == &a[3]` for `int a[3]`)
- [✓] §6.5.6p7 — Pointer ± integer is defined only when both the original pointer and the result lie within the same array object or one past its last element; producing any other address (overflow out of the array) is undefined behavior. — mcc:✓ gcc:✓ clang:✓ (UB; not diagnosed — consensus)
- [✓] §6.5.6p7 — A pointer one past the last array element may be formed but must not be dereferenced (used as the operand of an evaluated unary `*`). — mcc:✓ gcc:✓ clang:✓ (forming one-past accepted; dereference UB undiagnosed — consensus)
- [✓] §6.5.6p8 — Subtracting two pointers yields the difference of their array subscripts; both pointers must address elements of, or one past the end of, the same array object, otherwise the behavior is undefined. — mcc:✓ gcc:✓ clang:✓ (`&a[4]-&a[1]`==3)
- [✓] §6.5.6p8 — The result of a pointer subtraction has the implementation-defined signed integer type `ptrdiff_t` (from `<stddef.h>`); if the true difference is not representable in `ptrdiff_t`, the behavior is undefined. — mcc:✓ gcc:✓ clang:✓ (result usable as signed integer; ptrdiff_t is `long` on x86-64 for all)
- [✓] §6.5p5 — Signed-integer overflow of arithmetic `+`/`-` (result not representable in the result type) is undefined behavior (general rule, applies to additive operators). — mcc:✓ gcc:✓ clang:✓ (UB; not diagnosed — consensus)

### §6.5.7 Bitwise shift operators

- [✓] §6.5.7p1 — Parse `<<` and `>>` as left-associative binary operators binding looser than additive and tighter than relational operators (shift-expression grammar). — mcc:✓ gcc:✓ clang:✓ (`1<<2+1`==8)
- [✓] §6.5.7p2 — Constraint: each operand of `<<` and `>>` must have integer type; reject non-integer operands. — mcc:✓ gcc:✓ clang:✓ (`double<<1` rejected by all)
- §6.5.7p3 — Apply the integer promotions to each operand separately (not the usual arithmetic conversions); the result type is the promoted type of the left operand. — (spec; result-type behavior, not separately testable)
- [✓] §6.5.7p3 — If the right operand is negative, or is greater than or equal to the width in bits of the promoted left operand, the behavior is undefined. — mcc:✓ gcc:✓ clang:✓ (UB; not diagnosed at compile time — consensus)
- [✓] §6.5.7p4 — `E1 << E2` shifts `E1` left by `E2` bit positions, vacated low bits filled with zero; for unsigned `E1` the result is `E1 × 2^E2` reduced modulo one more than the maximum value representable in the result type. — mcc:✓ gcc:✓ clang:✓ (`1u<<31`==2147483648)
- [✓] §6.5.7p4 — For `E1 << E2` with signed nonnegative `E1`: if `E1 × 2^E2` is representable in the result type that is the value, otherwise the behavior is undefined. — mcc:✓ gcc:✓ clang:✓ (representable case yields product; overflow UB undiagnosed — consensus)
- [✓] §6.5.7p5 — `E1 >> E2` shifts `E1` right by `E2` bit positions; for unsigned `E1`, or signed nonnegative `E1`, the result is the floor (integral part) of `E1 / 2^E2`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.7p5 — For `E1 >> E2` with a negative signed `E1`, the resulting value is implementation-defined (e.g. arithmetic vs logical right shift). — mcc:✓ gcc:✓ clang:✓ (`-16>>2`==-4 — all implement arithmetic shift)

### §6.5.8 Relational operators

- [✓] §6.5.8p1 — Parse `<`, `>`, `<=`, `>=` as left-associative binary operators binding looser than shift and tighter than equality operators (relational-expression grammar). — mcc:✓ gcc:✓ clang:✓ (`1<2==1`==1)
- [✓] §6.5.8p2 — Constraint: both operands have real type; or both are pointers to qualified/unqualified versions of compatible object types; or (C99-only) both are pointers to qualified/unqualified versions of compatible incomplete types (C11 folds incomplete into "object types"). Reject complex operands. — mcc:✓ gcc:✓ clang:✓ (`_Complex < _Complex` rejected by all)
- §6.5.8p3 — When both operands have arithmetic (real) type, perform the usual arithmetic conversions before comparing. — (spec; not separately testable)
- §6.5.8p4 — Treat a pointer to an object that is not an array element as a pointer to the first element of a length-one array of that type, for relational-comparison purposes. — (spec; semantic model, not separately testable)
- [✓] §6.5.8p5 — When comparing two pointers, the outcome reflects the relative addresses of the pointed-to objects; pointers to the same object, or both one-past-the-end of the same array, compare equal-ranked. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.8p5 — Within one aggregate: a pointer to a later-declared structure member compares greater than one to an earlier member; a pointer to a higher-subscript array element compares greater than one to a lower-subscript element of the same array. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.8p5 — All pointers to members of the same union object compare equal-ranked; and for `P` pointing into an array whose last element is addressed by `Q`, the one-past pointer `Q+1` compares greater than `P`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.8p5 — Relationally comparing pointers that do not point into (or one past) the same array object/aggregate is undefined behavior. — mcc:✓ gcc:✓ clang:✓ (UB; not diagnosed — consensus)
- [✓] §6.5.8p6 — Each relational operator yields `1` (type `int`) when the specified relation holds and `0` otherwise. — mcc:✓ gcc:✓ clang:✓

### §6.5.9 Equality operators

- [✓] §6.5.9p1 — Parse `==` and `!=` as left-associative binary operators binding looser than relational and tighter than bitwise-AND (equality-expression grammar). — mcc:✓ gcc:✓ clang:✓
- [~] §6.5.9p2 — Constraint: the operand pair must be one of — both arithmetic; both pointers to qualified/unqualified versions of compatible types; one pointer to an object or incomplete type and one pointer to qualified/unqualified `void`; or one pointer and one null pointer constant. Reject other combinations. — mcc:~ gcc:~ clang:~ (`int* == char*` accepted with a warning by all three; constraint diagnostic is a non-fatal warning everywhere — consensus)
- [✓] §6.5.9p3 — `==`/`!=` behave like the relational operators but with lower precedence, yield `1`/`0` of type `int`, and for any operand pair exactly one of "equal"/"not equal" holds (`==` true iff `!=` false). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.9p4 — When both operands have arithmetic type, perform the usual arithmetic conversions before comparing; complex values are equal iff both their real parts and both their imaginary parts are equal (and cross-domain values are equal iff their conversions to the common complex type are equal). — mcc:✓ gcc:✓ clang:✓ (`(1+2i)==(1+2i)`==1, `!=`==0)
- [✓] §6.5.9p5 — If one operand is a pointer and the other a null pointer constant, convert the null pointer constant to the pointer's type; if one operand is a pointer to an object type and the other a pointer to qualified/unqualified `void`, convert the object pointer to the `void`-pointer type. — mcc:✓ gcc:✓ clang:✓ (`p==0` and `p==(void*)p` accepted with no diagnostic)
- [✓] §6.5.9p6 — Two pointers compare equal iff: both are null pointers; both point to the same object (or both one-past-the-end of the same array); or one is one-past-the-end of one array object and the other points to the start of a different array object that happens to immediately follow it in the address space (a non-array object is treated as a length-one array for this rule). — mcc:✓ gcc:✓ clang:✓
- §6.5.9p6 — In the "immediately-following array" case, whether the one-past and start pointers compare equal is unspecified. — (spec; unspecified outcome, not separately testable)

### §6.5.10 Bitwise AND operator

- [✓] §6.5.10p1 — Parse `&` as a left-associative binary operator binding looser than equality and tighter than `^` (AND-expression grammar). — mcc:✓ gcc:✓ clang:✓ (`5&3|4`==5, `1|2^3&4`==3)
- [✓] §6.5.10p2 — Constraint: each operand of binary `&` must have integer type; reject otherwise. — mcc:✓ gcc:✓ clang:✓ (`double&double` rejected by all)
- §6.5.10p3 — Apply the usual arithmetic conversions to the operands of `&`. — (spec; not separately testable)
- [✓] §6.5.10p4 — Compute the bitwise AND: each result bit is set iff the corresponding bits of both converted operands are set. — mcc:✓ gcc:✓ clang:✓

### §6.5.11 Bitwise exclusive OR operator

- [✓] §6.5.11p1 — Parse `^` as a left-associative binary operator binding looser than `&` and tighter than `|` (exclusive-OR-expression grammar). — mcc:✓ gcc:✓ clang:✓ (`1|2^3&4`==3)
- [✓] §6.5.11p2 — Constraint: each operand of `^` must have integer type; reject otherwise. — mcc:✓ gcc:✓ clang:✓ (`double^double` rejected by all)
- §6.5.11p3 — Apply the usual arithmetic conversions to the operands of `^`. — (spec; not separately testable)
- [✓] §6.5.11p4 — Compute the bitwise exclusive OR: each result bit is set iff exactly one of the corresponding operand bits is set. — mcc:✓ gcc:✓ clang:✓

### §6.5.12 Bitwise inclusive OR operator

- [✓] §6.5.12p1 — Parse `|` as a left-associative binary operator binding looser than `^` and tighter than `&&` (inclusive-OR-expression grammar). — mcc:✓ gcc:✓ clang:✓ (`5&3|4`==5)
- [✓] §6.5.12p2 — Constraint: each operand of `|` must have integer type; reject otherwise. — mcc:✓ gcc:✓ clang:✓ (`double|double` rejected by all)
- §6.5.12p3 — Apply the usual arithmetic conversions to the operands of `|`. — (spec; not separately testable)
- [✓] §6.5.12p4 — Compute the bitwise inclusive OR: each result bit is set iff at least one of the corresponding operand bits is set. — mcc:✓ gcc:✓ clang:✓

### §6.5.13 Logical AND operator

- [✓] §6.5.13p1 — Parse `&&` as a left-associative binary operator binding looser than `|` and tighter than `||` (logical-AND-expression grammar). — mcc:✓ gcc:✓ clang:✓ (`0||1&&0`==0)
- [✓] §6.5.13p2 — Constraint: each operand of `&&` must have scalar type (arithmetic or pointer); reject otherwise. — mcc:✓ gcc:✓ clang:✓ (`struct && struct` rejected by all)
- [✓] §6.5.13p3 — Yield `1` (type `int`) if both operands compare unequal to `0`, and `0` otherwise. — mcc:✓ gcc:✓ clang:✓ (`3&&4`==1, `0&&5`==0)
- [✓] §6.5.13p4 — `&&` guarantees left-to-right evaluation with a sequence point after the first operand; if the first operand compares equal to `0`, the second operand is not evaluated (short-circuit). — mcc:✓ gcc:✓ clang:✓ (`0 && side()` leaves global unmodified)

### §6.5.14 Logical OR operator

- [✓] §6.5.14p1 — Parse `||` as a left-associative binary operator binding looser than `&&` and tighter than the conditional operator (logical-OR-expression grammar). — mcc:✓ gcc:✓ clang:✓ (`0||1&&0`==0)
- [✓] §6.5.14p2 — Constraint: each operand of `||` must have scalar type (arithmetic or pointer); reject otherwise. — mcc:✓ gcc:✓ clang:✓ (`struct || struct` rejected by all)
- [✓] §6.5.14p3 — Yield `1` (type `int`) if either operand compares unequal to `0`, and `0` otherwise. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.14p4 — `||` guarantees left-to-right evaluation with a sequence point after the first operand; if the first operand compares unequal to `0`, the second operand is not evaluated (short-circuit). — mcc:✓ gcc:✓ clang:✓ (`1 || side()` leaves global unmodified)


## §6.5.15-§6.5.17 Conditional/assignment/comma & §6.6 Constant expressions

> mcc status: 53 TODOs, 4 mcc-gaps (all "soft": mcc emits a diagnostic but accepts what gcc/clang hard-reject — assignment to a const/read-only or const-member lvalue, compound-assign to const, and incompatible-pointer simple assignment; a standard-conforming diagnostic *is* issued, so these are ~ not ✗).

> Citation convention: paragraph numbers are given for **C11 (N1570)** first; where C99 (N1256) numbers or wording differ, the divergence is tagged inline. Several paragraphs were renumbered between C99 and C11 (notably §6.5.15 and §6.6), so the same requirement can carry two paragraph numbers.

### §6.5.15 Conditional operator

- [✓] §6.5.15p2 (C11; C99 folds this into p2 together with the operand list) — Require the first (controlling) operand of `?:` to have scalar type; reject struct/union/void/array controlling operands. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.15p3 (C11; C99 p2) — Constrain the second and third operands so that at least one of these holds: both have arithmetic type; both have the same struct/union type; both have `void` type; both are pointers to qualified/unqualified versions of compatible types; one is a pointer and the other a null pointer constant; or one is a pointer to an object type and the other a pointer to qualified/unqualified `void`. (C99 phrases the last case as a pointer to an "object or incomplete type"; C11 folds incomplete into "object type".) — mcc:✓ gcc:✓ clang:✓ (rejects mixing arithmetic and struct branches)
- [ ] §6.5.15p4 (C11; C99 p3) — Evaluate the first operand fully and place a sequence point between it and whichever of the second/third operands is evaluated; evaluate the second operand only when the first compares unequal to 0, and the third only when the first compares equal to 0. — (spec; runtime sequencing, not separately compile-testable)
- [✓] §6.5.15p4 (C11; C99 p3) — Yield as the result the value of whichever branch was evaluated, converted to the common result type determined by the rules below; the conditional expression is a value, never an lvalue. — mcc:✓ gcc:✓ clang:✓ (`(c?a:b)=x` rejected)
- [✓] §6.5.15p5 (C11; C99 p4) — When both branches have arithmetic type, set the result type to what the usual arithmetic conversions would produce for those two operands. — mcc:✓ gcc:✓ clang:✓ (`sizeof(1?1:1.0)==sizeof(double)`)
- [✓] §6.5.15p5 (C11; C99 p4) — When both branches have the same struct/union type, give the result that type; when both branches have `void` type, give the result type `void`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.15p6 (C11; C99 p5) — When both branches are pointers to compatible types (or differently-qualified versions of compatible types), make the result a pointer to an appropriately qualified version of the composite type, where the pointed-to type carries the union of the qualifiers of both branches' pointed-to types. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.15p6 (C11; C99 p5) — When one branch is a null pointer constant and the other a pointer, give the result the pointer type of the non-null branch. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.15p6 (C11; C99 p5) — When one branch is a pointer to `void` (qualified or not) and the other a pointer to an object type, make the result a pointer to an appropriately qualified `void`, carrying the combined qualifiers of both pointed-to types. — mcc:✓ gcc:✓ clang:✓

### §6.5.16 Assignment operators (general)

- [~] §6.5.16p2 — Require the left operand of any assignment (simple `=` or compound `op=`) to be a modifiable lvalue; reject assignment to a const-qualified lvalue, an array, an incomplete-type lvalue, a value designating an object with a const-qualified member, or a non-lvalue left operand. — mcc:~ gcc:✓ clang:✓ (mcc rejects array/non-lvalue targets, but only **warns** "assignment of read-only location" for const lvalues and const-member structs where gcc/clang hard-error)
- [✓] §6.5.16p3 — Store a value in the object the left operand designates, and define the assignment expression's value as that stored value read back after the assignment, with the type the left operand has after lvalue conversion (qualifiers and `_Atomic` removed); make the assignment expression itself not an lvalue. — mcc:✓ gcc:✓ clang:✓ (`(a=b)=c` rejected; rest is runtime semantics)
- §6.5.16p3 (C11) — Sequence the side effect of updating the stored value of the left operand after the value computations of both the left and right operands, and leave the evaluations of the two operands otherwise unsequenced relative to each other. — (spec; runtime sequencing, not separately testable)
- §6.5.16p3 (C99-only) — Leave the order of evaluation of the two operands unspecified, with no sequence point between them (the C11 sequencing guarantee above does not apply in C99). — (spec; runtime, not separately testable)

### §6.5.16.1 Simple assignment

- [✓] §6.5.16.1p1 — Permit simple `=` when the left operand has (atomic-, qualified-, or unqualified-) arithmetic type and the right operand has arithmetic type. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.16.1p1 — Permit simple `=` when the left operand has an (atomic-, qualified-, or unqualified-) struct or union type compatible with the type of the right operand. — mcc:✓ gcc:✓ clang:✓
- [~] §6.5.16.1p1 — Permit simple `=` when both operands are pointers to qualified/unqualified versions of compatible types and the left's pointed-to type carries all the qualifiers of the right's pointed-to type. — mcc:~ gcc:✓ clang:✓ (assigning between *incompatible* pointer types: mcc only **warns** "assignment from incompatible pointer type"; gcc/clang hard-error. Qualifier-discard `int*=const int*` warns in all three.)
- [✓] §6.5.16.1p1 — Permit simple `=` when one operand is a pointer to an object type and the other a pointer to qualified/unqualified `void`, provided the left's pointed-to type carries all the qualifiers of the right's pointed-to type. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.16.1p1 — Permit simple `=` when the left operand is any pointer (atomic/qualified/unqualified) and the right operand is a null pointer constant. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.16.1p1 — Permit simple `=` when the left operand has type `_Bool` (atomic/qualified/unqualified) and the right operand is any pointer. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.16.1p1 (C11) — Accept an `_Atomic`-qualified left operand in any of the above forms (atomic store), in addition to plain qualified/unqualified left operands. — mcc:✓ gcc:✓ clang:✓ (basic `_Atomic int x; x=1;` accepted)
- [ ] §6.5.16.1p2 — Convert the right operand's value to the (unqualified, non-atomic) type of the assignment expression and store the converted value in the object the left operand designates. — (spec; runtime conversion, not separately compile-testable)
- [ ] §6.5.16.1p3 — Treat as undefined behavior a simple assignment whose stored value is read from an object overlapping the storage of the destination, unless the two objects' storage overlaps exactly and they are qualified/unqualified versions of a compatible type. — (spec; UB, not diagnosed by any compiler)

### §6.5.16.2 Compound assignment

- [✓] §6.5.16.2p1 — For `+=` and `-=`, require either that the left operand be an (atomic-, qualified-, or unqualified-) pointer to a complete object type with the right operand of integer type, or that the left operand be an (atomic-, qualified-, or unqualified-) arithmetic type with the right operand of arithmetic type. — mcc:✓ gcc:✓ clang:✓ (`p+=2` accepted)
- [✓] §6.5.16.2p1 — For every other compound assignment (`*= /= %= <<= >>= &= ^= |=`), require both operands to have arithmetic types consistent with the corresponding binary operator (e.g. integer operands for `%=`, `<<=`, `>>=`, `&=`, `^=`, `|=`; arithmetic operands for `*=`, `/=`). — mcc:✓ gcc:✓ clang:✓ (`double%=`, `double<<=`, `ptr*=` all rejected)
- [ ] §6.5.16.2p2 — Treat `E1 op= E2` as equivalent to `E1 = E1 op (E2)`, except that the lvalue `E1` is evaluated only once, so its side effects and value computation occur a single time. — (spec; runtime single-evaluation, not separately compile-testable)
- [ ] §6.5.16.2p3 (C11) — When the left operand has atomic type, perform the read-modify-write as a single atomic operation with `memory_order_seq_cst` semantics. — (spec; runtime atomicity, requires codegen inspection)
- [~] §6.5.16.2p1 (lvalue requirement inherited from p2) — A compound assignment to a const-qualified lvalue must be diagnosed/rejected. — mcc:~ gcc:✓ clang:✓ (`const int a; a+=2;` — mcc only warns "assignment of read-only location"; gcc/clang hard-error)

### §6.5.17 Comma operator

- [✓] §6.5.17p2 — Evaluate the left operand as a void expression (discarding its value), place a sequence point after it, then evaluate the right operand; take the type and value of the right operand as the result. — mcc:✓ gcc:✓ clang:✓ (type propagation tested; sequencing is runtime)
- [✓] §6.5.17p2 — Make the comma expression's result a value, not an lvalue, even when the right operand designates an object. — mcc:✓ gcc:✓ clang:✓ (`(a,b)=3` rejected by all three)
- §6.5.17p2 (C11) — Preserve the comma operator's left-then-right sequencing within its (parenthesized) subexpression even when it appears inside a context whose surrounding list-separator commas are themselves unsequenced (e.g. a function-argument list). — (spec; runtime sequencing, not separately testable)
- [✓] §6.5.17p3 — Recognize that in syntactic positions where a comma is a list separator (function-call argument lists, brace-enclosed initializer lists) the comma is not the comma operator, so a comma operator must be parenthesized to be used there. — mcc:✓ gcc:✓ clang:✓ (parser distinguishes the two)

### §6.6 Constant expressions

- [✓] §6.6p2 (C11; C99 p4) — Allow a constant expression to be computed during translation rather than at run time, and accept it anywhere a constant of the same kind is permitted. — mcc:✓ gcc:✓ clang:✓ (constant folding works; spec-level guarantee otherwise)
- [✓] §6.6p3 (C11; C99 p2) — Reject any constant expression that contains an assignment, `++`, `--`, function-call, or comma operator, except where such an operator sits inside a subexpression that is not evaluated (e.g. the unused branch of `?:`, the operands of `&&`/`||`/`?:` short-circuited away, or the operand of `sizeof`/`_Alignof`). — mcc:✓ gcc:✓ clang:✓ for assignment/`++`/function-call in an array size. **Comma operator exception:** `int a[(1,2)];` → mcc:~ accepts, gcc:✗ rejects, clang:~ accepts — no gcc/clang consensus (gcc enforces p3, clang treats it as an extension); mcc matches clang.
- [✓] §6.6p4 (C11; C99 p3) — Require every constant expression, after its conversions, to evaluate to a constant lying within the range of representable values for its result type. — mcc:✓ gcc:✓ clang:✓ (overflow in ICE diagnosed elsewhere; range check applied)
- [✓] §6.6p5 — Recognize the specific contexts that require an expression evaluating to a constant: the controlling expression of `#if`, array-size declarators (non-VLA), enumeration-constant values, bit-field widths, `case` labels, `static`/thread-storage-duration object initializers, and `_Alignas` alignment specifiers (C11). — mcc:✓ gcc:✓ clang:✓ (non-constant bit-field width, `case`, and enum value all rejected)
- [✓] §6.6p6 — Define an integer constant expression as one having integer type whose operands are only integer constants, enumeration constants, character constants, `sizeof` expressions whose results are integer constants, `_Alignof` expressions (C11), and floating constants that are the immediate operands of casts. — mcc:✓ gcc:✓ clang:✓ (`int a[sizeof(int)]`, `int a[_Alignof(double)]`)
- [✓] §6.6p6 — Within an integer constant expression, permit cast operators only to convert an arithmetic type to an integer type, except inside the operand of `sizeof` (or `_Alignof`, C11), which is unevaluated. — mcc:✓ gcc:✓ clang:✓ (`int a[(int)1.9 + 3]` accepted — float cast to int allowed)
- [✓] §6.6p6 (C11) — Treat `_Alignof` results as valid integer-constant-expression operands. (C99-only: `_Alignof` does not exist; `sizeof` is the only operator listed that yields an integer constant within an integer constant expression.) — mcc:✓ gcc:✓ clang:✓
- [✓] §6.6p7 — Allow extra latitude for constant expressions used as initializers: such an expression must be, or evaluate to, one of an arithmetic constant expression, a null pointer constant, an address constant, or an address constant for a complete object type adjusted by adding or subtracting an integer constant expression. — mcc:✓ gcc:✓ clang:✓ (`int *p=&obj+1;`, `char *p="hi"+1;` accepted; `static int s=non_const_obj;` rejected)
- [✓] §6.6p8 — Define an arithmetic constant expression as one having arithmetic type whose operands are only integer constants, floating constants, enumeration constants, character constants, `sizeof` expressions, and `_Alignof` expressions (C11). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.6p8 — Within an arithmetic constant expression, permit cast operators only to convert one arithmetic type to another arithmetic type, except inside the operand of `sizeof` (or `_Alignof`, C11). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.6p9 — Define an address constant as a null pointer, a pointer to an lvalue designating an object of static storage duration, or a pointer to a function designator; allow it to be formed explicitly with unary `&` (or an integer constant cast to pointer type) or implicitly via array-to-pointer / function-to-pointer conversion, and allow `&`, `*`, `[]`, `.`, and `->` to participate in building it provided none of them actually accesses the stored value of an object. — mcc:✓ gcc:✓ clang:✓ (address-of static obj initializers accepted)
- [ ] §6.6p10 — Permit an implementation to accept additional, implementation-defined forms of constant expressions beyond those required, while still enforcing the mandatory categories above. — (spec; implementation latitude, not a testable requirement)

### Additional / edge-case checkboxes added this pass

- [✓] §6.5.16p2 — Reject assignment to an array name (non-modifiable lvalue). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.16p2 — Reject assignment whose left operand is a pure rvalue (e.g. `(a+1)=2`). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.16.1p1 — Reject assignment of a struct value to an arithmetic lvalue (no implicit conversion). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.6p5 — Reject a non-constant array size at file scope (would-be VLA invalid at file/static scope). — mcc:✓ gcc:✓ clang:✓ (`int a[g()]` rejected)
- [✓] §6.6p6 — Accept a parenthesized float arithmetic result cast to int as an array size (`int a[(int)(1.5+0.5)]`). — mcc:✓ gcc:✓ clang:✓
- [~] §6.5.16p2 — Reject assignment to a struct lvalue that has a const-qualified member. — mcc:~ gcc:✓ clang:✓ (mcc warns only; gcc/clang hard-error)


## §6.7.1-§6.7.3 Declarations: storage/type specifiers/qualifiers

> mcc status: 78 TODOs in this section; 1 mcc-gap: §6.7.3p6/§6.5.16.1 — assignment to a `const`-qualified object is only a *warning* in mcc (rc=0), an error in gcc/clang. mcc now rejects re-`typedef` of a variably-modified type (§6.7p3), and flags `_Atomic(...)` (§6.7.2.4) and `_Thread_local` (§6.7.1p1) under `-std=c99 -pedantic-errors`, all matching gcc/clang. One consensus-split (not an mcc gap): §6.7.1p6 register-array subscript is accepted by mcc+gcc, rejected by clang. Everything else (storage-class exclusivity, `_Thread_local` placement, atomic/restrict/qualifier constraints, the full type-specifier multiset table) matches the gcc==clang consensus.

> Scope note: this section covers §6.7 (general declarations), §6.7.1 (storage-class specifiers, incl. C11 `_Thread_local`), §6.7.2 (type specifiers in general, incl. `_Bool`, `_Complex`, and C99-only `_Imaginary`), §6.7.2.4 (C11 atomic *type* specifiers — `_Atomic` is a type specifier when immediately followed by `(`), §6.7.3 (type qualifiers `const`/`restrict`/`volatile` and the C11 `_Atomic` *qualifier*), and §6.7.3.1 (formal definition of `restrict`). Structure/union (§6.7.2.1), enum (§6.7.2.2), and tags (§6.7.2.3) are documented in their own sections. Paragraph numbers follow C11 N1570; C99 N1256 divergences are tagged inline with `(C11)` / `(C99-only)`.

### §6.7 Declarations

- §6.7p1 — Syntax: a *declaration* matches `declaration-specifiers init-declarator-list_opt ;` or (C11) a `static_assert-declaration`. The *declaration-specifiers* are a sequence drawn from *storage-class-specifier*, *type-specifier*, *type-qualifier*, *function-specifier*, and (C11) *alignment-specifier*. (C99-only: no *alignment-specifier* and no `static_assert-declaration` alternative.) — (spec; not separately testable)
- §6.7p1 — Syntax: *init-declarator-list* is a comma-separated list of *init-declarator*, each being a *declarator* optionally followed by `=` *initializer*. — (spec; not separately testable)
- [✓] §6.7p2 — Constraint: A declaration other than a `static_assert-declaration` (C11 qualifier; absent in C99) shall declare at least a declarator (other than the parameters of a function or the members of a structure or union), a tag, or the members of an enumeration. — mcc:✓ gcc:✓ clang:✓ (`int;` → all three warn "useless/empty declaration"; warning, not error, on all three)
- [✓] §6.7p3 — Constraint: If an identifier has no linkage, there shall be no more than one declaration of the identifier (in a declarator or type specifier) with the same scope and in the same name space, except that a typedef name may be redefined to denote the same type as it currently does — provided that type is not a variably modified type (C11) — and tags may be redeclared as specified in §6.7.2.3. (C99-only: the typedef-redefinition exception does **not** exist; redefining a typedef name in the same scope is a constraint violation in C99.) — mcc:✓ gcc:✓ clang:✓ (plain `typedef int T; typedef int T;` matches all three: accepted in C11, diagnosed under `-std=c99 -pedantic-errors`; mcc also rejects `typedef int T[n]; typedef int T[n];` for a variably-modified `T` — "redefinition of variably modified typedef 'T'" — matching gcc/clang)
- [✓] §6.7p3 (gap) — Constraint: the C11 typedef-redefinition exception explicitly **excludes** variably modified types; `typedef int T[n]; typedef int T[n];` at block scope is a constraint violation. — mcc:✓ gcc:✓ clang:✓ (mcc rejects — "redefinition of variably modified typedef 'T'"; gcc: "redefinition of typedef 'T' with variably modified type", clang: "redefinition of typedef for variably-modified type")
- [✓] §6.7p4 — Constraint: All declarations in the same scope that refer to the same object or function shall specify compatible types (see §6.2.7). — mcc:✓ gcc:✓ clang:✓ (`int x; double x;` → all error "incompatible/conflicting types for redefinition")
- §6.7p5 — Semantics: A declaration specifies the interpretation and attributes of a set of identifiers. — (spec; not separately testable)
- §6.7p5 — Semantics: A *definition* of an identifier is a declaration for that identifier that, for an object, causes storage to be reserved for it. — (spec; not separately testable)
- §6.7p5 — Semantics: A definition of a function includes the function body. — (spec; not separately testable)
- §6.7p5 — Semantics: A definition for an enumeration constant is the (only) declaration of the identifier. — (spec; not separately testable)
- §6.7p5 — Semantics: A definition for a typedef name is the first (or only) declaration of the identifier. — (spec; not separately testable)
- §6.7p6 — Semantics: The declaration specifiers consist of a sequence of specifiers that indicate the linkage, storage duration, and part of the type of the entities the declarators denote; the init-declarator-list is a comma-separated sequence of declarators, each of which may have additional type information, an initializer, or both; the declarators contain the identifiers (if any) being declared. — (spec; not separately testable)
- [✓] §6.7p7 — Semantics: If an identifier for an object is declared with no linkage, the type for the object shall be complete by the end of its declarator, or by the end of its init-declarator if it has an initializer; in the case of function parameters (including in prototypes) it is the adjusted type (§6.7.6.3; C99: §6.7.5.3) that is required to be complete. — mcc:✓ gcc:✓ clang:✓ (`struct S; void f(void){ struct S x; }` → all error: incomplete/unknown storage size)

### §6.7.1 Storage-class specifiers

> C11 inserted two `_Thread_local` paragraphs (p3, p4); C99's typedef/register/function/aggregate paragraphs are therefore p3/p4/p5/p6 in C99 but p5/p6/p7/p8 in C11. Paragraph numbers below follow C11.

- [✓] §6.7.1p1 — Syntax: a *storage-class-specifier* is exactly one of `typedef`, `extern`, `static`, `_Thread_local` (C11), `auto`, or `register`. (C99-only: the list omits `_Thread_local`.) — mcc:✓ gcc:✓ clang:✓ (all six keywords accepted; `auto`/`register` at block scope OK on all three)
- [✓] §6.7.1p1 (gap) — `_Thread_local` is a C11 addition; in C99 mode it must be diagnosed under `-pedantic-errors`. — mcc:✓ gcc:✓ clang:✓ (`-std=c99 -pedantic-errors`: mcc rejects `_Thread_local` — "'_Thread_local' is a C11 feature"; gcc: "ISO C99 does not support '_Thread_local'", clang: "'_Thread_local' is a C11 extension")
- [✓] §6.7.1p1 (gap) — GNU `__thread` thread-storage keyword (extension, accepted by all three regardless of `-std`). — mcc:✓ gcc:✓ clang:✓ (`__thread int x;` compiles on all three)
- [✓] §6.7.1p2 — Constraint: At most one storage-class specifier may be given in the declaration specifiers in a declaration, except that `_Thread_local` may appear with `static` or `extern` (C11). (C99-only: at most one, with no `_Thread_local` exception.) — mcc:✓ gcc:✓ clang:✓ (`static extern int x;` → all error "multiple storage classes"; `_Thread_local static int x;` and `_Thread_local extern int x;` accepted by all three)
- [✓] §6.7.1p3 — (C11) Constraint: In the declaration of an object with block scope, if the declaration specifiers include `_Thread_local` they shall also include either `static` or `extern`; if `_Thread_local` appears in any declaration of an object, it shall be present in every declaration of that object. — mcc:✓ gcc:✓ clang:✓ (`void f(void){ _Thread_local int x; }` → all error: mcc "'_Thread_local' at block scope requires 'static' or 'extern'", gcc/clang equivalent; `static _Thread_local` form accepted)
- [✓] §6.7.1p4 — (C11) Constraint: `_Thread_local` shall not appear in the declaration specifiers of a function declaration. — mcc:✓ gcc:✓ clang:✓ (`_Thread_local void f(void);` → all error: mcc "'_Thread_local' applied to a function", clang "only allowed on variable declarations", gcc "invalid storage class for function")
- §6.7.1p5 — Semantics: The `typedef` specifier is called a "storage-class specifier" for syntactic convenience only; it is discussed in §6.7.8. The meanings of the linkages and storage durations are discussed in §6.2.2 and §6.2.4. — (spec; not separately testable)
- §6.7.1p6 — Semantics: A declaration of an identifier for an object with storage-class specifier `register` suggests that access to the object be as fast as possible; the extent to which such suggestions are effective is implementation-defined. — (spec; not separately testable)
- §6.7.1p6 — Semantics: The implementation may treat any `register` declaration simply as an `auto` declaration. — (spec; not separately testable)
- [✓] §6.7.1p6 — Semantics: Whether or not addressable storage is actually used, the address of any part of an object declared with storage-class specifier `register` cannot be computed, either explicitly (by the unary `&` operator, §6.5.3.2) or implicitly (by converting an array name to a pointer, §6.3.2.1); thus the only operator that can be applied to an array declared with `register` is `sizeof`. — mcc:✓ gcc:✓ clang:✓ for `&` (`&x` on `register int x` → all error "address of register variable requested"); `sizeof` of a register array OK on all three. **Consensus split on subscripting a register array** (`register int a[3]; a[0]`): clang rejects (implicit array→pointer = "address of register variable"), **mcc and gcc both accept** — mcc matches gcc here, clang is the strict outlier.
- [✓] §6.7.1p7 — Semantics: The declaration of an identifier for a function that has block scope shall have no explicit storage-class specifier other than `extern`. — mcc:✓ gcc:✓ clang:✓ (`void f(void){ static void g(void); }` and `auto void g(void);` → all error; `extern void g(void);` accepted by all three)
- §6.7.1p8 — Semantics: If an aggregate or union object is declared with a storage-class specifier other than `typedef`, the properties resulting from the storage-class specifier (except with respect to linkage) also apply to the members of the object, and so on recursively for any aggregate or union member objects. — (spec; not separately testable)

### §6.7.2 Type specifiers

> C99 lists `_Imaginary` as a keyword type specifier and a constraint-list multiset (Annex G conditional feature); C11 removed `_Imaginary` from §6.7.2 (described only in informative Annex G) and added the *atomic-type-specifier*.

- [✓] §6.7.2p1 — Syntax: a *type-specifier* is one of `void`, `char`, `short`, `int`, `long`, `float`, `double`, `signed`, `unsigned`, `_Bool`, `_Complex`, an *atomic-type-specifier* (C11), a *struct-or-union-specifier*, an *enum-specifier*, or a *typedef-name*. (C99-only: the keyword list additionally includes `_Imaginary` and omits *atomic-type-specifier*.) — mcc:✓ gcc:✓ clang:✓ (all keyword specifiers recognized; `_Bool b=1;` and `_Atomic(int)` accepted)
- [✓] §6.7.2p2 — Constraint: At least one type specifier shall be given in the declaration specifiers in each declaration, and in the specifier-qualifier-list in each struct declaration and type name. — mcc:✓ gcc:✓ clang:✓ (implicit-int `auto x=1;` at file scope → all diagnose "type defaults to 'int'"/"implicit int"; mcc additionally errors on the auto-at-file-scope)
- [✓] §6.7.2p2 — Constraint: Each list of type specifiers shall be one of the following multisets (order does not matter), and each multiset designates the indicated type: `void`; `char`; `signed char`; `unsigned char`; `short` / `signed short` / `short int` / `signed short int`; `unsigned short` / `unsigned short int`; `int` / `signed` / `signed int`; `unsigned` / `unsigned int`; `long` / `signed long` / `long int` / `signed long int`; `unsigned long` / `unsigned long int`; `long long` / `signed long long` / `long long int` / `signed long long int`; `unsigned long long` / `unsigned long long int`; `float`; `double`; `long double`; `_Bool`; `float _Complex`; `double _Complex`; `long double _Complex`; an *atomic-type-specifier* (C11); a *struct-or-union-specifier*; an *enum-specifier*; a *typedef-name*. — mcc:✓ gcc:✓ clang:✓ (valid multisets `unsigned long long int`, `long double _Complex`, all three `_Complex` widths accepted; invalid `long long long` → "too long/too many basic types", `signed unsigned int` → "signed and unsigned", `short long` → rejected, on all three)
- [~] §6.7.2p2 — (C99-only) Constraint: The multiset list additionally permits `float _Imaginary`, `double _Imaginary`, and `long double _Imaginary` (imaginary types — a conditional feature per Annex G, signaled by `__STDC_IEC_559_COMPLEX__`). (C11 removed these from the constraint list.) — mcc:~ gcc:~ clang:~ (`_Imaginary` types are *unsupported* by all three even though all three define `__STDC_IEC_559_COMPLEX__`: mcc & clang error "imaginary types are not supported", gcc errors at the `_Imaginary` keyword. Annex G permits an IEC-559-complex implementation to omit imaginary types; consensus is uniform.)
- §6.7.2p3 — Semantics: Specifiers for structures, unions, enumerations, and atomic types are discussed in §6.7.2.1 through §6.7.2.4 (§6.7.2.4 is C11-only); declarations of typedef names are discussed in §6.7.8; the characteristics of the other types are discussed in §6.2.5. — (spec; not separately testable)
- [✓] §6.7.2p4 — Semantics: Each of the comma-separated multisets in the constraint list designates the same type, except that for bit-fields it is implementation-defined whether the specifier `int` designates the same type as `signed int` or the same type as `unsigned int`. — mcc:✓ gcc:✓ clang:✓ (impl-defined; a plain `int b:1` bit-field behaves identically across all three on x86-64 — uniform consensus)

### §6.7.2.4 Atomic type specifiers (C11)

> (C11) Entire subsection is new in C11; absent in C99. Distinct from the `_Atomic` *qualifier* (§6.7.3): `_Atomic` is a type specifier only when immediately followed by `(`.

- [✓] §6.7.2.4p1 — (C11) Syntax: an *atomic-type-specifier* has the form `_Atomic ( type-name )`. — mcc:✓ gcc:✓ clang:✓ (`_Atomic(int) x;` accepted; `#include <stdatomic.h>` + `atomic_fetch_add` round-trips correctly when run on all three)
- [✓] §6.7.2.4p1 (gap) — `_Atomic(...)` is a C11 construct; in C99 mode it must be diagnosed under `-pedantic-errors`. — mcc:✓ gcc:✓ clang:✓ (`-std=c99 -pedantic-errors`: mcc rejects `_Atomic(int)` — "'_Atomic' is a C11 feature"; gcc "ISO C99 does not support the '_Atomic' qualifier", clang "'_Atomic' is a C11 extension")
- [✓] §6.7.2.4p2 — (C11) Constraint: Atomic type specifiers shall not be used if the implementation does not support atomic types (see the `__STDC_NO_ATOMICS__` macro, §6.10.8.3). — mcc:✓ gcc:✓ clang:✓ (none of the three define `__STDC_NO_ATOMICS__`, i.e. all advertise atomics support; `<stdatomic.h>` and `<threads.h>` are present and usable in mcc's bundled headers)
- [✓] §6.7.2.4p3 — (C11) Constraint: The type name in an atomic type specifier shall not refer to an array type, a function type, an atomic type, or a qualified type. — mcc:✓ gcc:✓ clang:✓ (all four forms rejected by all three: `_Atomic(int[3])` array, `_Atomic(int(void))` function, `_Atomic(_Atomic(int))` atomic, `_Atomic(const int)` qualified; the `_Atomic` *qualifier* spelling `_Atomic A`/`_Atomic F` on array/function typedefs is also rejected)
- §6.7.2.4p4 — (C11) Semantics: The properties associated with atomic types are meaningful only for expressions that are lvalues. — (spec; not separately testable)
- [✓] §6.7.2.4p4 — (C11) Semantics: If the `_Atomic` keyword is immediately followed by a left parenthesis, it is interpreted as a type specifier (with a type name), not as a type qualifier (whose semantics are in §6.7.3). — mcc:✓ gcc:✓ clang:✓ (`_Atomic int x; _Atomic(int) y;` — both qualifier and specifier spellings accepted side-by-side by all three)

### §6.7.3 Type qualifiers

> C11 inserted the `_Atomic`-qualifier constraint as p3, shifting C99's lvalue/duplicate-qualifier/const/volatile/restrict/array-function/compatibility paragraphs from C99 p3–p9 to C11 p4–p10. Paragraph numbers below follow C11; C99 equivalents noted where they differ.

- [✓] §6.7.3p1 — Syntax: a *type-qualifier* is one of `const`, `restrict`, `volatile`, or `_Atomic` (C11). (C99-only: the list omits `_Atomic`.) — mcc:✓ gcc:✓ clang:✓ (all four qualifier keywords recognized)
- [✓] §6.7.3p2 — Constraint: Types other than pointer types whose referenced type is an object type shall not be restrict-qualified. — mcc:✓ gcc:✓ clang:✓ (`int restrict x;` → all error "restrict requires a pointer"; `void (*restrict fp)(void)` (pointer-to-function) → all error "pointer to function may not be restrict"; `int *restrict p;` and `void *restrict p;` accepted)
- [✓] §6.7.3p3 — (C11) Constraint: The type modified by the `_Atomic` qualifier shall not be an array type or a function type. — mcc:✓ gcc:✓ clang:✓ (covered with §6.7.2.4p3: `_Atomic A`/`_Atomic F` on array/function typedefs rejected by all three)
- §6.7.3p4 — Semantics: The properties associated with qualified types are meaningful only for expressions that are lvalues. (C99: §6.7.3p3.) — (spec; not separately testable)
- [✓] §6.7.3p5 — Semantics: If the same qualifier appears more than once in the same specifier-qualifier-list, either directly or via one or more typedefs, the behavior is the same as if it appeared only once. (C99: §6.7.3p4.) — mcc:✓ gcc:✓ clang:✓ (`const const int x;` accepted by all three even under `-std=c99 -pedantic-errors`; clang adds a `-Wduplicate-decl-specifier` warning, mcc/gcc silent — all rc=0)
- §6.7.3p6 — Semantics: If an attempt is made to modify an object defined with a const-qualified type through use of an lvalue with non-const-qualified type, the behavior is undefined. (C99: §6.7.3p5.) — (spec; UB, not separately testable — see the assignment-to-const constraint item below for the diagnosable case)
- [~] §6.7.3p6 (related, §6.5.16.1) — A `const`-qualified lvalue is not a modifiable lvalue; direct assignment to it is a constraint violation. — mcc:~ gcc:✓ clang:✓ (`const int x=0; x=1;` → **mcc only emits a *warning* "assignment of read-only location" and exits rc=0**, whereas gcc errors "assignment of read-only variable" and clang errors "cannot assign to variable with const-qualified type". mcc accepts; gcc/clang reject.)
- §6.7.3p6 — Semantics: If an attempt is made to refer to an object defined with a volatile-qualified type through use of an lvalue with non-volatile-qualified type, the behavior is undefined. (C99: §6.7.3p5.) — (spec; UB, not separately testable)
- [ ] §6.7.3p7 — Semantics: An object that has volatile-qualified type may be modified in ways unknown to the implementation or have other unknown side effects; therefore any expression referring to such an object shall be evaluated strictly according to the rules of the abstract machine (§5.1.2.3). (C99: §6.7.3p6.) — (spec; abstract-machine semantics, not mechanically asserted here)
- §6.7.3p7 — Semantics: At every sequence point the value last stored in a volatile-qualified object shall agree with that prescribed by the abstract machine, except as modified by the unknown factors mentioned previously. — (spec; not separately testable)
- §6.7.3p7 — Semantics: What constitutes an access to an object that has volatile-qualified type is implementation-defined. — (spec; implementation-defined, not separately testable)
- §6.7.3p8 — Semantics: An object that is accessed through a restrict-qualified pointer has a special association with that pointer (defined in §6.7.3.1) requiring that all accesses to that object use, directly or indirectly, the value of that particular pointer. (C99: §6.7.3p7.) — (spec; not separately testable)
- §6.7.3p8 — Semantics: The intended use of `restrict` (like the `register` storage class) is to promote optimization; deleting all instances of the qualifier from all preprocessing translation units composing a conforming program does not change its meaning (i.e., observable behavior). — (spec; not separately testable)
- §6.7.3p9 — Semantics: If the specification of an array type includes any type qualifiers, the element type is so-qualified, not the array type. (C99: §6.7.3p8.) — (spec; not separately testable — observable only via the array-parameter `[restrict]`/`[const]` rewrite)
- [✓] §6.7.3p9 — Semantics: If the specification of a function type includes any type qualifiers, the behavior is undefined. — mcc:✓ gcc:✓ clang:✓ (`typedef int F(void); const F f;` — all three accept silently; clang warns under `-Wignored-qualifiers` "'const' qualifier on function type has no effect", gcc warns under `-Wall`; mcc accepts. UB → no required diagnostic, all conform.)
- [✓] §6.7.3p10 — Semantics: For two qualified types to be compatible, both shall have the identically qualified version of a compatible type; the order of type qualifiers within a list of specifiers or qualifiers does not affect the specified type. (C99: equivalent wording in §6.7.3p9.) — mcc:✓ gcc:✓ clang:✓ (`const volatile int x; volatile const int y;` — both orderings accepted and yield the same type on all three)

### §6.7.3.1 Formal definition of restrict

> Pure formal/aliasing definitions; a translator is explicitly free to ignore restrict (p6), so none of these are conformance-testable by a single TU compile. mcc parses and accepts `restrict` everywhere it is required to (verified above under §6.7.3p2).

- §6.7.3.1p1 — Let D be a declaration of an ordinary identifier that provides a means of designating an object P as a restrict-qualified pointer to type T. — (spec; not separately testable)
- §6.7.3.1p2 — If D appears inside a block and does not have storage class `extern`, let B denote that block; if D appears in the list of parameter declarations of a function definition, let B denote the associated block; otherwise let B denote the block of `main` (or the block of whatever function is called at program startup in a freestanding environment). — (spec; not separately testable)
- §6.7.3.1p3 — A pointer expression E is said to be *based on* object P if (at some sequence point in the execution of B prior to the evaluation of E) modifying P to point to a copy of the array object into which it formerly pointed would change the value of E; "based" is defined only for expressions with pointer types. — (spec; not separately testable)
- §6.7.3.1p4 — During each execution of B, let L be any lvalue that has `&L` based on P; if L is used to access the value of the object X that it designates, and X is also modified (by any means), then T shall not be const-qualified. — (spec; UB definition, not separately testable)
- §6.7.3.1p4 — Under the same conditions, every other lvalue used to access the value of X shall also have its address based on P. — (spec; UB definition, not separately testable)
- §6.7.3.1p4 — Under the same conditions, every access that modifies X shall be considered also to modify P, for the purposes of this subclause. — (spec; not separately testable)
- §6.7.3.1p4 — Under the same conditions, if P is assigned the value of a pointer expression E that is based on another restricted pointer object P2 (associated with block B2), then either the execution of B2 shall begin before the execution of B, or the execution of B2 shall end prior to the assignment. — (spec; UB definition, not separately testable)
- §6.7.3.1p4 — If these requirements are not met, the behavior is undefined. — (spec; UB, not separately testable)
- §6.7.3.1p5 — Here an execution of B means that portion of the execution of the program that would correspond to the lifetime of an object with scalar type and automatic storage duration associated with B. — (spec; not separately testable)
- [✓] §6.7.3.1p6 — A translator is free to ignore any or all aliasing implications of uses of `restrict`. — mcc:✓ gcc:✓ clang:✓ (permissive; all three accept `restrict` and are free to ignore its aliasing implications — no conformance obligation)


## §6.7.2.1-§6.7.2.4 Struct/union/enum/atomic specifiers

> mcc status: 63 TODOs total (54 testable probed across mcc/gcc/clang, 9 spec-only); mcc-gaps: 1 hard divergence (atomic bit-fields accepted where gcc/clang reject — but this is impl-defined per p4, so mcc remains conforming) plus 1 minor diagnostic-quality gap (no "declaration does not declare anything" warning). No conformance defects found: mcc diagnoses every probed constraint and matches gcc/clang on all runtime semantics (anon members, recursive lookup, FAM, unions, _Bool bit-fields, _Atomic specifier + its p3 constraints).

### §6.7.2.1 Structure and union specifiers

> C99→C11 deltas in this clause: C11 adds anonymous structures/unions (member with an untagged `struct`/`union` specifier and no declarator), permits a `static_assert-declaration` as a `struct-declaration`, makes the per-member `struct-declarator-list` optional (to allow anonymous members), adds `_Bool` bit-field value semantics (p9) and the "atomic types as bit-fields is implementation-defined" allowance (p4), and moves the flexible-array-member text from C99 §6.7.2.1p16 to C11 §6.7.2.1p18. C11 also permits an `alignment-specifier` (`_Alignas`) inside a `specifier-qualifier-list`.

#### Syntax
- [✓] §6.7.2.1p1 — Accept `struct-or-union-specifier` in both forms: `struct-or-union identifier_opt { struct-declaration-list }` (definition) and `struct-or-union identifier` (reference). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.2.1p1 — Accept `struct-or-union` being either the keyword `struct` or the keyword `union`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.2.1p1 — Accept a `struct-declaration-list` as one or more `struct-declaration`s. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.2.1p1 — Accept a `struct-declaration` of the form `specifier-qualifier-list struct-declarator-list_opt ;`, the `struct-declarator-list` being optional so as to permit anonymous structure/union members. (C11; C99 required a non-optional `struct-declarator-list`). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.2.1p1 — Accept a `static_assert-declaration` as a `struct-declaration`. (C11). — mcc:✓ gcc:✓ clang:✓ (`struct S { int a; _Static_assert(1,"ok"); int b; };` compiles on all three)
- [✓] §6.7.2.1p1 — Accept a `specifier-qualifier-list` as a sequence of `type-specifier`, `type-qualifier`, and (C11) `alignment-specifier` items. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.2.1p1 — Accept a `struct-declarator-list` as one or more comma-separated `struct-declarator`s. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.2.1p1 — Accept a `struct-declarator` as either `declarator` or `declarator_opt : constant-expression` (bit-field, with the declarator optional for an unnamed bit-field). — mcc:✓ gcc:✓ clang:✓

#### Constraints
- [~] §6.7.2.1p2 — A `struct-declaration` that does not declare an anonymous structure or anonymous union shall contain a `struct-declarator-list`. (C11). A tagged member with no declarator (`struct S { union T { int x; }; };`) is not anonymous and declares nothing. — mcc:~ gcc:✓ clang:✓ (gcc/clang emit "declaration does not declare anything"; mcc accepts silently — minor diagnostic-quality gap, no conformance impact)
- [✓] §6.7.2.1p3 — The bit-field width shall be an integer constant expression with a nonnegative value not exceeding the width of the underlying type. — mcc:✓ gcc:✓ clang:✓ (`int a:40` → all reject "width exceeds its type"; `int a:-1` → all reject "negative width")
- [✓] §6.7.2.1p3 — If the bit-field width value is zero, the declaration shall have no declarator. — mcc:✓ gcc:✓ clang:✓ (`int a:0` → all reject "zero width for bit-field")
- [✓] §6.7.2.1p4 — A bit-field shall have a type that is a qualified or unqualified version of `_Bool`, `signed int`, `unsigned int`, or some other implementation-defined type. — mcc:✓ gcc:✓ clang:✓ (`float a:3` → all reject; mcc: "bitfields must have scalar type")
- [✓] §6.7.2.1p4 — It is implementation-defined whether atomic types are permitted as bit-field types. (C11). — mcc:✓ gcc:✓ clang:✓ (impl-defined choices differ: `_Atomic int a:3` — mcc **accepts**; gcc/clang reject "bit-field has atomic type". mcc's acceptance is a valid implementation-defined choice, so all three conform, but mcc diverges from the gcc/clang consensus.)

#### Semantics
- §6.7.2.1p5 — A structure is a sequence of members allocated in an ordered sequence; a union's members overlap (per 6.2.5). — (spec; not separately testable)
- §6.7.2.1p6 — Structure and union specifiers have the same form; `struct` indicates a structure type, `union` a union type. — (spec; not separately testable)
- [✓] §6.7.2.1p7 — The presence of a `struct-declaration-list` declares a new type within the translation unit; the list is the member declarations. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.2.1p7 — If the `struct-declaration-list` contains no named members, no anonymous structures, and no anonymous unions, the behavior is undefined. (C11; C99 forbade an empty member list in a constraint context). — mcc:✓ gcc:✓ clang:✓ (UB/extension; `struct S {};` all reject under `-pedantic-errors`: mcc "ISO C forbids a struct with no named members", gcc "struct has no members", clang "empty struct is a GNU extension")
- [✓] §6.7.2.1p7 — The type is incomplete until immediately after the `}` and complete thereafter. — mcc:✓ gcc:✓ clang:✓ (member-incomplete-type and self-containment probes all rejected identically)
- [✓] §6.7.2.1p8 — A member may have any complete object type other than a variably modified type; not incomplete (except FAM p18) and not function type. — mcc:✓ gcc:✓ clang:✓ (function member → all reject "declared as a function"; incomplete member → all reject "incomplete type"; variably-modified member `int a[n]` → all reject under `-pedantic-errors`: mcc "ISO C forbids a member with a variably modified type", clang rejects unconditionally)
- [ ] §6.7.2.1p8 — A member may be declared as a specified number of bits (a bit-field) whose width follows a colon. — (spec; covered by p3 probes)
- [✓] §6.7.2.1p9 — A bit-field is interpreted as a signed or unsigned integer type of the specified number of bits. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.2.1p9 — Storing 0 or 1 into a nonzero-width `_Bool` bit-field compares equal to the stored value; a `_Bool` bit-field has `_Bool` semantics. (C11). — mcc:✓ gcc:✓ clang:✓ (runtime: `s.b=1; s.b==1` → 1 on all three; `s.b=2` → reads back 1 on all three)
- [ ] §6.7.2.1p10 — An implementation may allocate any addressable storage unit large enough to hold a bit-field. — (spec; impl-defined, not a conformance check)
- [✓] §6.7.2.1p10 — If enough space remains, a bit-field immediately following another is packed into adjacent bits of the same unit. — mcc:✓ gcc:✓ clang:✓ (`struct{unsigned a:5;unsigned b:5;}` packs; sizeof matches gcc/clang)
- [ ] §6.7.2.1p10 — Whether a bit-field that does not fit is put into the next unit or overlaps adjacent units is implementation-defined. — (spec; impl-defined)
- [ ] §6.7.2.1p10 — The order of allocation of bit-fields within a unit is implementation-defined. — (spec; impl-defined, ABI-determined)
- [ ] §6.7.2.1p10 — The alignment of the addressable storage unit is unspecified. — (spec; unspecified)
- [✓] §6.7.2.1p11 — A bit-field with no declarator (colon + width only) is an unnamed bit-field. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.2.1p11 — A width-0 unnamed bit-field forces no further bit-field into the current unit (alignment break). — mcc:✓ gcc:✓ clang:✓ (`unsigned a:5; unsigned :0; unsigned b:5;` → sizeof 8 on all three)
- [ ] §6.7.2.1p12 — Each non-bit-field member is aligned in an implementation-defined manner appropriate to its type. — (spec; impl-defined/ABI)
- [✓] §6.7.2.1p13 — An unnamed member whose type specifier is a tagless `struct`/`union` specifier is an anonymous structure/union. (C11). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.2.1p13 — Members of an anonymous structure/union are members of the containing structure/union (for lookup and `.`/`->`), applied recursively. (C11). — mcc:✓ gcc:✓ clang:✓ (runtime: `s.deep+s.mid+s.top` through two nested anonymous levels → 6 on all three)
- [✓] §6.7.2.1p14 — Within a structure object, non-bit-field members and bit-field units have addresses increasing in declaration order. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.2.1p14 — A pointer to a structure, suitably converted, points to its initial member (or its bit-field unit), and vice versa. — mcc:✓ gcc:✓ clang:✓ (`(void*)&s==(void*)&s.a` → 1 on all three)
- [✓] §6.7.2.1p14 — There may be unnamed padding within a structure, but not at its beginning. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.2.1p15 — The size of a union is sufficient to contain the largest member. — mcc:✓ gcc:✓ clang:✓ (`union{char;double;int;}` → sizeof 8 on all three)
- §6.7.2.1p15 — The value of at most one member can be stored in a union at any time. — (spec; not separately testable)
- [✓] §6.7.2.1p15 — A pointer to a union, suitably converted, points to each member (or its bit-field unit), and vice versa. — mcc:✓ gcc:✓ clang:✓ (`&u==&u.d && &u==&u.c` → 1 on all three)
- [✓] §6.7.2.1p16 — There may be unnamed padding at the end of a structure or union. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.2.1p18 — The last element of a structure with >1 named member may have an incomplete array type — a flexible array member. (C11 §6.7.2.1p18; C99 §6.7.2.1p16). — mcc:✓ gcc:✓ clang:✓ (`struct S{int n;int a[];}` accepted; sizeof excludes FAM)
- [✓] §6.7.2.1p18 — A structure with a FAM (and unions recursively containing such) shall not be a member of a structure or an array element. — mcc:✓ gcc:✓ clang:✓ (constraint violation; under `-pedantic-errors` all reject: mcc "ISO C forbids a member that is a structure with a flexible array member", gcc/clang likewise. Accepted as extension by default.)
- [✓] §6.7.2.1p18 — The FAM is ignored for sizeof; size equals the offset of the FAM in an otherwise identical struct with the FAM replaced by a zero-length array. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.2.1p18 — `.`/`->` naming the FAM behaves as if replaced by the longest array fitting the accessed object. — mcc:✓ gcc:✓ clang:✓ (runtime: malloc'd 3-elem FAM read/write → `0 10 20` on all three)
- §6.7.2.1p18 — The offset of the FAM shall remain that of the FAM even if it differs from the replacement array's offset. — (spec; not separately testable)
- [ ] §6.7.2.1p18 — If the replacement array would have no elements, it behaves as if it had one, but accessing it or forming a one-past pointer is undefined. — (spec; UB)

### §6.7.2.2 Enumeration specifiers

> C99→C11: substantively unchanged. C11 §6.7.2.2p4 adds the explicit sentence that the enumerated type is incomplete until the closing `}` and complete thereafter.

#### Syntax
- [✓] §6.7.2.2p1 — Accept `enum identifier_opt { enumerator-list }`, `enum identifier_opt { enumerator-list , }` (trailing comma), and `enum identifier`. — mcc:✓ gcc:✓ clang:✓ (trailing comma `enum E{A,B,}` accepted on all three)
- [✓] §6.7.2.2p1 — Accept an `enumerator-list` as one or more comma-separated `enumerator`s. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.2.2p1 — Accept an `enumerator` as `enumeration-constant` or `enumeration-constant = constant-expression`. — mcc:✓ gcc:✓ clang:✓

#### Constraints
- [✓] §6.7.2.2p2 — The enumeration-constant value shall be an integer constant expression representable as `int`. — mcc:✓ gcc:✓ clang:✓ (`A=0x100000000LL` → constraint violation; under `-pedantic-errors` all reject: mcc "ISO C restricts enumerator values to the range of 'int'". Accepted as C23 extension by default.)

#### Semantics
- [✓] §6.7.2.2p3 — Enumerator identifiers are declared as constants of type `int` and may appear wherever such are permitted. — mcc:✓ gcc:✓ clang:✓ (`sizeof(A)==4` and `_Generic(A,int:1,...)==1` on all three)
- [✓] §6.7.2.2p3 — An enumerator with `=` defines its constant as the constant-expression's value. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.2.2p3 — If the first enumerator has no `=`, its value is 0. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.2.2p3 — Each subsequent enumerator with no `=` is the previous value plus 1. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.2.2p3 — Enumerators with `=` may produce values duplicating other values in the same enumeration (permitted). — mcc:✓ gcc:✓ clang:✓
- [ ] §6.7.2.2p3 — The enumerators of an enumeration are also known as its members. — (spec; terminology)
- [✓] §6.7.2.2p4 — Each enumerated type shall be compatible with `char`, a signed integer type, or an unsigned integer type capable of representing all members; the choice is implementation-defined. — mcc:✓ gcc:✓ clang:✓ (`enum{A,B=4294967295U}` → sizeof 4 (unsigned int) on all three)
- [✓] §6.7.2.2p4 — The enumerated type is incomplete until immediately after the `}` and complete thereafter. (C11). — mcc:✓ gcc:✓ clang:✓ (using `enum E e;` before the enum is completed → all reject "ISO C forbids forward references to 'enum' types")

### §6.7.2.3 Tags

> C99→C11: C11 adds §6.7.2.3p7, the standalone forward-declaration form `struct-or-union identifier ;`, which introduces a new (hiding) tag in the current scope; there is no analogous `enum identifier ;` form.

#### Constraints
- [✓] §6.7.2.3p1 — A specific type shall have its content defined at most once. — mcc:✓ gcc:✓ clang:✓ (double `struct S{...}` → all reject: mcc "struct/union/enum already defined", gcc/clang "redefinition")
- [✓] §6.7.2.3p2 — Two declarations using the same tag for the same type shall use the same `struct`/`union`/`enum` keyword. — mcc:✓ gcc:✓ clang:✓ (`struct S` then `union S` → all reject; mcc "redeclaration of 'S'", gcc "wrong kind of tag", clang "tag type that does not match")
- [✓] §6.7.2.3p3 — `enum identifier` without an enumerator list shall only appear after the type is complete. — mcc:✓ gcc:✓ clang:✓ (forward `enum E;`/`enum E e;` → all reject "ISO C forbids forward references to 'enum' types")

#### Semantics
- [✓] §6.7.2.3p4 — All declarations of struct/union/enum types in the same scope using the same tag declare the same type. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.2.3p4 — Regardless of tag or other declarations, the type is incomplete until just after the closing brace of its content list, and complete thereafter. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.2.3p5 — Declarations in different scopes or with different tags declare distinct types. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.2.3p5 — Each tagless declaration of such a type declares a distinct type. — mcc:✓ gcc:✓ clang:✓
- §6.7.2.3p6 — A defining-form type specifier declares a struct/union/enum type with the list defining its content. — (spec; not separately testable)
- [✓] §6.7.2.3p6 — If an identifier is given in a defining form, the specifier also declares that identifier as the tag. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.2.3p7 — `struct-or-union identifier ;` declares the identifier as a tag introducing a new tag in the current scope, hiding any same-named tag in an enclosing scope (forward declaration). (C11; no `enum` form). — mcc:✓ gcc:✓ clang:✓ (inner `struct S;` then redefined locally and used → accepted on all three; the outer `struct S` is correctly hidden)
- [✓] §6.7.2.3p8 — A non-defining `struct-or-union identifier` / `enum identifier` form with a visible tag declaration refers to that same type and does not redeclare the tag. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.2.3p8 — Such a form with no visible tag declaration declares an incomplete struct/union type and declares the identifier as its tag. — mcc:✓ gcc:✓ clang:✓

### §6.7.2.4 Atomic type specifiers (C11)

> Entire clause is C11-only; absent in C99.

#### Syntax
- [✓] §6.7.2.4p1 — Accept an `atomic-type-specifier` of the form `_Atomic ( type-name )`. (C11). — mcc:✓ gcc:✓ clang:✓ (`_Atomic(int) g;` + `atomic_store/load` runs identically; `sizeof(_Atomic(int))==sizeof(int)` holds)

#### Constraints
- [✓] §6.7.2.4p2 — Atomic type specifiers shall not be used if the implementation does not support atomic types (signaled by `__STDC_NO_ATOMICS__`, see 6.10.8.3). (C11). — mcc:✓ gcc:✓ clang:✓ (none of the three define `__STDC_NO_ATOMICS__`, consistent with all three accepting `_Atomic(int)`)
- [✓] §6.7.2.4p3 — The type name shall not refer to an array type, a function type, an atomic type, or a qualified type. (C11). — mcc:✓ gcc:✓ clang:✓ (array → mcc "_Atomic cannot be applied to an array type"; function → "...function type"; atomic → "...an atomic type"; qualified `const int` → "...a qualified type"; all four rejected by all three)

#### Semantics
- §6.7.2.4p4 — The properties associated with atomic types are meaningful only for expressions with lvalue type. (C11). — (spec; not separately testable)
- [✓] §6.7.2.4p4 — If `_Atomic` is immediately followed by `(`, it is interpreted as a type specifier (`_Atomic ( type-name )`), not the `_Atomic` qualifier of §6.7.3. (C11). — mcc:✓ gcc:✓ clang:✓ (using `_Atomic(int)` in an expression position → all reject "expression expected"/"expected expression", confirming it is parsed as a type specifier)


## §6.7.4-§6.7.5 Function specifiers & alignment specifier

> mcc status: `_Noreturn` and every `_Alignas` constraint (typedef/bitfield/param/register/function/weaker/zero/non-pow2/non-ICE) match the gcc==clang consensus. The remaining gaps are all in inline-definition handling (3 `✗` rows): mcc emits an external definition for a *plain* `inline` definition (so an `-O0` call links instead of failing, §6.7.4p6), and silently accepts the constraint-violating `inline` internal-linkage-reference (§6.7.4p2) and `inline main` (§6.7.4p3) cases that gcc/clang diagnose. mcc now warns on the `inline` modifiable-static case (§6.7.4p2), matching gcc/clang.

### §6.7.4 Function specifiers

- [✓] §6.7.4p1 — Restrict the `inline` / `_Noreturn` (C11) function specifiers to declarations of a function identifier; reject them on objects/typedefs. — mcc:✓ gcc:~ clang:✓ (mcc & clang error on `inline int x;`; gcc only warns — mcc matches clang's reject)
- [✓] §6.7.4p2 — Forbid an inline definition of an external-linkage function from defining a modifiable object with static (C11: or thread) storage duration. — mcc:✓ gcc:~ clang:~ (constraint requires a diagnostic; mcc warns "'s' is static but declared in inline function 'f' which is not static", as do gcc/clang)
- [✗] §6.7.4p2 — Forbid an inline definition of an external-linkage function from referencing an internal-linkage identifier. — mcc:✗ gcc:~ clang:~ (gcc/clang warn; mcc emits no diagnostic)
- [✗] §6.7.4p3 — In a hosted environment, forbid a function specifier on the declaration of `main` (C99: just `inline`; C11: any function specifier). — mcc:✗ gcc:~ clang:✓ (mcc accepts `inline int main` with no diagnostic; gcc warns, clang errors)
- [✓] §6.7.4p4 — Permit a function specifier to appear more than once in a declaration, treating repeats as a single occurrence. — mcc:✓ gcc:✓ clang:✓ (`inline inline int f(){}` accepted; clang adds a pedantic duplicate-specifier warning only)
- §6.7.4p5 — Treat a function declared `inline` as an inline function: a hint that calls be made fast, with the actual effectiveness implementation-defined. — (spec; not separately testable)
- [✓] §6.7.4p6 — Allow any internal-linkage function to be inline (`static inline`) with no external-definition obligation. — mcc:✓ gcc:✓ clang:✓ (`static inline` compiles and links)
- [✓] §6.7.4p6 — Require that an external-linkage function declared `inline` also be defined in the same translation unit. — mcc:✓ gcc:✓ clang:✓ (mcc errors `unresolved reference to 'f'`; gcc/clang warn at compile then fail to link)
- [ ] §6.7.4p6 — Form an *inline definition* when every file-scope declaration of the function in the TU has `inline` and none has `extern`. — (spec; behavior observed via the linkage tests below)
- [✗] §6.7.4p6 — Ensure an inline definition provides NO external definition for the function (and does not forbid one in another TU); it is only an alternative the translator may use for same-TU calls. — mcc:✗ gcc:✓ clang:✓ (at `-O0` a call to a plain-`inline`-only function must fail to link; gcc/clang give `undefined reference`, mcc wrongly emits a global definition and links cleanly)
- [✓] §6.7.4p6 — When a file-scope declaration of the same function uses `extern` (or a non-`inline` declaration is present), emit the external definition for the inline function. — mcc:✓ gcc:✓ clang:✓ (adding `extern int add(...);` makes the `-O0` call link on all three)
- §6.7.4p6 — Leave it unspecified whether a given same-TU call uses the inline definition or the external definition. — (spec; not separately testable)
- [✓] §6.7.4p7 — (C11) Treat a function declared `_Noreturn` as never returning to its caller; a return at runtime is undefined behavior. — mcc:✓ gcc:✓ clang:✓ (accepted; `<stdnoreturn.h>`'s `noreturn` macro also works on all three)
- [✓] §6.7.4p8 — (C11) Recommended practice: diagnose a `_Noreturn` function that appears able to return to its caller. — mcc:✓ gcc:✓ clang:✓ (all three warn on a `_Noreturn` function containing a reachable `return`)

### §6.7.5 Alignment specifier (`_Alignas`) — C11

- [✓] §6.7.5p1 — (C11) Accept both alignment-specifier forms: `_Alignas(type-name)` and `_Alignas(constant-expression)` (plus the `<stdalign.h>` `alignas` spelling). — mcc:✓ gcc:✓ clang:✓ (both forms and the macro accepted)
- [✓] §6.7.5p2 — (C11) Reject `_Alignas` in a declaration of a typedef. — mcc:✓ gcc:✓ clang:✓ (all error)
- [✓] §6.7.5p2 — (C11) Reject `_Alignas` on a bit-field. — mcc:✓ gcc:✓ clang:✓ (all error)
- [✓] §6.7.5p2 — (C11) Reject `_Alignas` on a function declaration. — mcc:✓ gcc:✓ clang:✓ (all error)
- [✓] §6.7.5p2 — (C11) Reject `_Alignas` on a function parameter. — mcc:✓ gcc:✓ clang:✓ (all error)
- [✓] §6.7.5p2 — (C11) Reject `_Alignas` on an object declared with the `register` storage-class specifier. — mcc:✓ gcc:✓ clang:✓ (all error)
- [✓] §6.7.5p3 — (C11) Require the `_Alignas` constant-expression to be an integer constant expression. — mcc:✓ gcc:✓ clang:✓ (`_Alignas(n)` with a runtime `n` errors on all three)
- [✓] §6.7.5p3 — (C11) Require the value to be a valid fundamental or implementation-supported extended alignment, or zero (reject non-power-of-two values). — mcc:✓ gcc:✓ clang:✓ (`_Alignas(3)` rejected as not a positive power of two on all three)
- [✓] §6.7.5p4 — (C11) Treat an alignment specification of zero (`_Alignas(0)`) as having no effect. — mcc:✓ gcc:✓ clang:✓ (all accept with no alignment change)
- [✓] §6.7.5p5 — (C11) When multiple alignment specifiers appear, apply the strictest (largest) specified alignment. — mcc:✓ gcc:✓ clang:✓ (`_Alignas(8) _Alignas(16)` yields alignment 16)
- [✓] §6.7.5p6 — (C11) Make `_Alignas(type-name)` equivalent to `_Alignas(_Alignof(type-name))`. — mcc:✓ gcc:✓ clang:✓ (`_Alignas(double)` gives `_Alignof(double)`; the type-name vs constant forms agree)
- [✓] §6.7.5p7 — (C11) Reject a combined alignment weaker (less strict) than the type's natural required alignment, whether expressed via a constant (`_Alignas(1) int`) or a smaller type (`_Alignas(char) int`). — mcc:✓ gcc:✓ clang:✓ (all error: "less than minimum alignment of the type")
- [✓] §6.7.5 — (C11) Honor the requested alignment in generated code/storage (object actually placed at the specified boundary). — mcc:✓ gcc:✓ clang:✓ (`_Alignas(64)` object verified 64-byte aligned at runtime under mcc)
- [✓] §6.7.5 — (C11) Apply `_Alignas` on a struct member so it raises the alignment of the containing aggregate. — mcc:✓ gcc:✓ clang:✓ (`_Alignas(16) int` member makes `_Alignof(struct S)==16`)
- [✓] §6.7.5 — (C11) Interoperate with `_Alignof`: `_Alignof` reports the specified/required alignment used as `_Alignas` input (note: `_Alignof` on an *expression* is a non-ISO GNU extension all three warn about). — mcc:✓ gcc:✓ clang:✓


## §6.7.6 Declarators

> mcc status: solid on the common cases (pointer/array/function derivation, [static]/[*]/qualifier placement, void/ellipsis/register constraints, VLA placement+no-linkage+goto+single-evaluation). mcc now rejects a zero-size constant array under `-pedantic-errors` and rejects a non-empty identifier list in a non-defining declaration (`int f(a,b);`), matching gcc/clang. The remaining gaps vs the gcc==clang consensus (`~` rows) are in qualifier handling on derived pointer types: (1) a type-qualifier written inside an array parameter's brackets (`int a[const 3]`) is parsed but not applied, so the adjusted pointer is not actually `const` (§6.7.6.2p3/§6.7.6.3p4); (2) reassigning a `const` pointer (`int *const p`) is only a warning in mcc, an error in clang (§6.7.6.1p2).

### §6.7.6 Declarators (general)

- §6.7.6p1 — Grammar: a declarator is an optional pointer part followed by a direct-declarator; direct-declarators build array `[...]` and function `(...)` derivations around an identifier or a parenthesized declarator. — (spec; grammar, not separately testable)
- [✓] §6.7.6p2 — Each declarator introduces exactly one identifier, derived from the type given by the declaration specifiers via the declarator's pointer/array/function operators. — mcc:✓ gcc:✓ clang:✓
- §6.7.6p3 — A declarator is processed by treating "T D1" (with D1 a derived declarator) as recursively assigning the inner declarator the derived type; the base case gives the identifier the specifier type. — (spec; constructive definition, not separately testable)
- [✓] §6.7.6p4 — A full declarator is one not nested inside another declarator; for a variably modified type the size expressions are evaluated when the full declarator's declaration is reached in execution (in order, once each). — mcc:✓ gcc:✓ clang:✓ (VLA size expr evaluated exactly once when the declaration is reached)
- [ ] §6.7.6p5 — Examples of combining specifiers and declarators (informative). — (spec; examples, not testable)
- [✓] §6.7.6 (multi-declarator) — A single declaration may carry several declarators that derive different types from the same specifiers (`int *p, a[3], f(void);`). — mcc:✓ gcc:✓ clang:✓

### §6.7.6.1 Pointer declarators

- [ ] §6.7.6.1p1 — Grammar: a pointer is one or more `*`, each optionally followed by a type-qualifier-list. — (spec; grammar)
- [~] §6.7.6.1p2 — `* type-qualifier-list D` derives a "qualified pointer to T" type — the qualifiers attach to the pointer object itself, not to the pointed-to type. — mcc:~ gcc:✓ clang:✓ (mcc honors qualifiers written between `*` and the name e.g. `int *const p`, but ignores qualifiers written inside array-parameter brackets — see §6.7.6.3)
- [~] §6.7.6.1p2 — `int *const p` makes the pointer const: reassigning `p` is a constraint violation. — mcc:~ gcc:✓ clang:✓ (mcc emits only a warning "assignment of read-only location"; gcc warns, clang errors — none is non-conforming, but mcc/gcc are diagnostic-only)
- [✓] §6.7.6.1p2 — `const int *p` qualifies the pointee, not the pointer: writing `*p` is the violation, reassigning `p` is fine. — mcc:✓ gcc:✓ clang:✓ (`*p=...` diagnosed; pointer itself remains modifiable)
- [ ] §6.7.6.1p3 — Two pointer types are compatible iff identically qualified and pointing to compatible types. — (spec; folded into type-compatibility, tested under §6.7.3/§6.2.7)

### §6.7.6.2 Array declarators

- [✓] §6.7.6.2p1 — Constraint: the element type of an array shall be a complete, non-function object type. — mcc:✓ gcc:✓ clang:✓ (`struct S a[3]` with incomplete S, and array-of-function, both rejected)
- [✓] §6.7.6.2p1 — Array of function type is forbidden. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.6.2p1 — The optional `[` type-qualifier-list / `static` `]` content is permitted only on the outermost array derivation of a function parameter. — mcc:✓ gcc:✓ clang:✓ (`int a[static 3]`/`int a[const 3]` accepted as a parameter; the same qualifiers/`static` on a non-parameter array declarator are rejected)
- [✓] §6.7.6.2p1 — `static` and/or type qualifiers in an array declarator that is not a function parameter is a constraint violation. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.6.2p1 — Constraint: if the size is a (non-`*`) expression it shall have integer type. — mcc:✓ gcc:✓ clang:✓ (`int a[3.0]` rejected)
- [✓] §6.7.6.2p1 — Constraint: if the size is an integer constant expression it shall have a value greater than zero. — mcc:✓ gcc:✓ clang:✓ (mcc rejects a negative size and, under `-pedantic-errors`, rejects a zero-size array — "ISO C forbids zero-size array" — matching gcc/clang, which treat zero size as a GNU extension and reject it with `-pedantic-errors`)
- [✓] §6.7.6.2p1 — Negative constant array size is rejected. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.6.2p1 — `static` in an array parameter requires an explicit size expression (cannot be `[static]` alone). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.6.2p1 — The `[*]` form (unspecified-size VLA) is allowed only where a declaration is a function prototype that is not part of a definition. — mcc:✓ gcc:✓ clang:✓ (`void f(int n,int a[*]);` accepted; `[*]` in a definition rejected)
- [✓] §6.7.6.2p2 — Constraint: a size expression that is not an integer constant expression (a VLA) may appear only in a declarator with block scope or function-prototype scope; the declared object/typedef must have automatic storage (or be a prototype parameter). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.6.2p2 — VLA forbidden at file scope. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.6.2p2 — VLA forbidden with static storage duration (`static int a[n]`). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.6.2p2 — An identifier of variably modified type must have no linkage (`extern int a[n]` in a block is forbidden). — mcc:✓ gcc:✓ clang:✓
- [~] §6.7.6.2p2 — A struct/union member shall not have a variably modified (VLA) type. — mcc:~ gcc:~ clang:✓ (clang hard-errors; mcc and gcc accept a VLA struct member as an extension by default and only reject it with -pedantic-errors — note the gcc/clang consensus splits here)
- [✓] §6.7.6.2p3 — `[*]` denotes a VLA of unspecified size; usable only in prototype-scope declarators. — mcc:✓ gcc:✓ clang:✓
- [~] §6.7.6.2p3 — A type-qualifier list and/or `static` between the brackets of the outermost array parameter qualifies/annotates the pointer the parameter is adjusted to. — mcc:~ gcc:✓ clang:✓ (mcc parses `int a[const 3]` but does NOT apply `const` to the adjusted `int *const a`; assigning to `a` is silently accepted, whereas gcc/clang reject it)
- [✓] §6.7.6.2p3 — `static` in `[static n]` is a guarantee that the argument provides access to at least n elements (an optimization hint; affects nullability/aliasing assumptions). — mcc:✓ gcc:✓ clang:✓ (accepted; semantic guarantee is informative, hint honored or ignored conformingly)
- [✓] §6.7.6.2p4 — If the size is an expression that is not a constant expression: when evaluated at declaration time it must be > 0 (undefined behavior otherwise) and a new VLA type is built each time the declaration is reached. — mcc:✓ gcc:✓ clang:✓ (UB on non-positive run-time size, not a translation-time diagnostic)
- [✓] §6.7.6.2p5 — The size expression of a VLA is evaluated exactly once, when control reaches the declaration; the resulting size is fixed for that array's lifetime even if `sizeof` is later applied. — mcc:✓ gcc:✓ clang:✓ (probe: side-effecting size function called once even with a later `sizeof`)
- [ ] §6.7.6.2p6 — For two array types to be compatible the element types must be compatible; if both have constant size the sizes must be equal (mismatch is undefined if one is in a separate translation unit). — (spec; folds into §6.2.7 compatibility)
- [✓] §6.7.6.2 (jump constraint, ref §6.8.6.1) — A `goto`/jump into the scope of an identifier with variably modified type is forbidden. — mcc:✓ gcc:✓ clang:✓
- [ ] §6.7.6.2p7 — Examples of array declarators (informative). — (spec; examples)
- [✓] §6.7.6.2 — A pointer-to-VLA parameter such as `int (*a)[n]` (a variably modified, non-VLA type) is accepted in prototype scope. — mcc:✓ gcc:✓ clang:✓

### §6.7.6.3 Function declarators (including prototypes)

- [✓] §6.7.6.3p1 — Constraint: a function's return (element) type shall not be an array type or a function type. — mcc:✓ gcc:✓ clang:✓ (both forms rejected via typedef)
- [✓] §6.7.6.3p1 — Function returning a function type is forbidden. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.6.3p2 — Constraint: the only storage-class specifier allowed in a parameter declaration is `register`; any other (static/extern/auto/typedef) is a violation. — mcc:✓ gcc:✓ clang:✓ (`register int a` accepted; `static int a` rejected)
- [✓] §6.7.6.3p3 — Constraint: an identifier list (old-style names without types) in a function declarator that is not part of that function's definition shall be empty. — mcc:✓ gcc:✓ clang:✓ (mcc rejects `int f(a,b);` — "parameter names (without types) in function declaration" — matching gcc/clang, which reject a non-empty identifier list outside a definition)
- [~] §6.7.6.3p4 — A parameter declared with array type is adjusted to "(qualified) pointer to element type", taking any qualifiers/`static` from the outermost `[...]`. — mcc:~ gcc:✓ clang:✓ (adjustment to pointer works — `sizeof` of an `int a[10]` parameter yields pointer size — but mcc drops the bracket qualifiers, see p3 of §6.7.6.2)
- [✓] §6.7.6.3p4 — `sizeof` an array parameter gives the size of the adjusted pointer, not the original array. — mcc:✓ gcc:✓ clang:✓ (clang additionally warns -Wsizeof-array-argument)
- [✓] §6.7.6.3p5 — A parameter declared with function type is adjusted to "pointer to function". — mcc:✓ gcc:✓ clang:✓ (`void f(int g(void))` — `g()` callable)
- [✓] §6.7.6.3p6 — If the declarator has a parameter type list, the function has a prototype and the listed types specify the parameter types; the list also fixes whether the function is variadic. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.6.3p7 — A parameter list consisting of the single keyword `void` (unnamed, unqualified) means the function takes no parameters; calling it with arguments is an error. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.6.3p7 — The special `void` parameter must be unnamed: `f(void x)` is invalid. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.6.3p7 — The special `void` parameter may not be qualified: `f(const void)` is invalid. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.6.3p7 — `void` may not be combined with any other parameter: `f(void,int)` is invalid. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.6.3p8 — `,` `...` (ellipsis) declares a variadic function; the ellipsis must follow at least one named/typed parameter (no parameter may follow it). — mcc:✓ gcc:~ clang:✓ (mcc rejects a bare `int f(...)`; clang rejects too; gcc accepts it by default as a C23-style extension and only diagnoses with -pedantic; `int f(int,...)` accepted by all three)
- [✓] §6.7.6.3p9 — Each parameter's declared type is adjusted as in p4/p5; after adjustment the parameter type list gives the function's parameter types, used to check call arguments. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.6.3p10 — The (obsolescent) identifier-list form names parameters whose types come from the following declaration list in the definition; absent declarations the type defaults (no prototype). — mcc:✓ gcc:✓ clang:✓ (`int f(a,b) int a; int b; {...}` accepted; clang warns it is deprecated)
- [✓] §6.7.6.3p11 — An empty parameter list `()` in a declaration that is not part of a definition specifies no information about the number/types of parameters (no prototype). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.6.3p12 — In an identifier-list definition, each declared parameter's type after default-argument-promotion considerations forms the type used; the function has no prototype. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.6.3p13 — For a prototype, declarations of the same function (including any later identifier-list definition) must have compatible/agreeing adjusted parameter types. — mcc:✓ gcc:✓ clang:✓ (`void f(int a[3]); void f(int *a){...}` — array param agrees with pointer param)
- [ ] §6.7.6.3p14 — The identifier-list (old-style) function declarator form is an obsolescent feature. — (spec; obsolescence note, not a diagnosable rule — clang chooses to warn -Wdeprecated-non-prototype)
- [ ] §6.7.6.3p15 — Two function types are compatible only if return types are compatible and (when both have prototypes) the parameter counts and corresponding adjusted parameter types agree, with default-promotion compatibility rules for prototype-vs-non-prototype. — (spec; folds into §6.2.7/§6.7.3 compatibility, exercised under those clauses)
- [✓] §6.7.6.3p16 — `restrict` may qualify only a pointer-to-object parameter (or the pointer an array parameter is adjusted to); on a non-pointer it is invalid. — mcc:✓ gcc:✓ clang:✓ (`int *restrict p` accepted; `restrict int x` rejected)
- [ ] §6.7.6.3p17+ — Examples of function declarators (informative). — (spec; examples)


## §6.7.7-§6.7.9 Type names/typedef/initialization

> mcc status: 78 TODOs covering §6.7.7 (type names), §6.7.8 (typedef), §6.7.9 (initialization); 1 mcc-gap — a single-expression init of incompatible struct/union type at block scope is rejected with a misleading parse-level message (diagnostic-quality only, §6.7.9p13). mcc now diagnoses a typedef-name/ordinary-identifier same-scope collision (`typedef int T; int T;`), redundant braces around a scalar initializer (`int y = {{5}};`), and C99 designated initializers used under `-std=c89 -pedantic-errors`, all matching gcc/clang. Everything else (the full type-name grammar including `[*]` and `static`-array forms, the typedef EXAMPLEs, and every Initialization EXAMPLE 1–13 including brace elision, designators, both-ends overlap-override, UTF-8/char16/char32 string init, and C11 padding-zero) matches the gcc==clang consensus.

> **Numbering note.** These are the **C11 (N1570)** section numbers. In **C99 (N1256)** the same three sections are numbered **§6.7.6 (Type names)**, **§6.7.7 (Type definitions)**, **§6.7.8 (Initialization)**. The *paragraph* numbers within each section are identical between C99 and C11, so citations below give the C11 section number plus the shared paragraph number; the C99 section number is noted in each heading. Content divergences are tagged inline `(C11)` for C11-only text and `(C99-only)` for wording C11 changed/removed.

### §6.7.7 Type names (C99 §6.7.6)

> No material C99↔C11 divergence in this subsection; the grammar and the EXAMPLE are identical (both already include the `[ * ]` unspecified-VLA form and the `static`/qualifier array forms introduced in C99).

- [✓] §6.7.7p1 — Parse a *type-name* as `specifier-qualifier-list abstract-declarator_opt` (a declaration specifier list with no storage-class/`inline`, optionally followed by an abstract declarator). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.7p1 — Parse an *abstract-declarator* as either `pointer` alone, or `pointer_opt direct-abstract-declarator`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.7p1 — Parse a parenthesized *direct-abstract-declarator*: `( abstract-declarator )`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.7p1 — Parse the array *direct-abstract-declarator* `direct-abstract-declarator_opt [ type-qualifier-list_opt assignment-expression_opt ]` (abstract array of unknown or sized extent). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.7p1 — Parse the `static` array forms in a type name: `[ static type-qualifier-list_opt assignment-expression ]` and `[ type-qualifier-list static assignment-expression ]`. — mcc:✓ gcc:✓ clang:✓ (`int a[static 3]`, `int a[const static 3]`, `int a[static const 3]` all accepted in prototype scope)
- [✓] §6.7.7p1 — Parse the unspecified-size VLA array form `direct-abstract-declarator_opt [ * ]` in a type name (e.g. `int (*)[*]`). — mcc:✓ gcc:✓ clang:✓ (all three accept in function-prototype scope and all three correctly reject `[*]` in block scope per §6.7.6.2p4)
- [✓] §6.7.7p1 — Parse the function *direct-abstract-declarator* `direct-abstract-declarator_opt ( parameter-type-list_opt )`. — mcc:✓ gcc:✓ clang:✓
- §6.7.7p2 — Treat a type name (used in any context that requires naming a type) as syntactically a declaration of a function or object of that type with the identifier omitted. — (spec; not separately testable)
- [✓] §6.7.7p2 (fn 147) — Interpret empty parentheses in a type name as "function with no parameter specification", not as redundant parentheses around the omitted identifier (so `int *()` is "function returning `int *`", not `int *`). — mcc:✓ gcc:✓ clang:✓ (`sizeof(int *(*)())` parses `int *()` as a function type)
- [✓] §6.7.7p3 (EXAMPLE) — Derive the canonical example types exactly: `int` → `int`; `int *` → pointer to int; `int *[3]` → array of 3 pointers to int; `int (*)[3]` → pointer to array of 3 ints; `int (*)[*]` → pointer to VLA-of-unspecified-size of int; `int *()` → function (unspecified params) returning pointer to int; `int (*)(void)` → pointer to function (no params) returning int; `int (*const [])(unsigned int, ...)` → array of unknown size of `const` pointers to functions returning int that take one `unsigned int` and a variable number of further arguments. — mcc:✓ gcc:✓ clang:✓ (every canonical spelling, incl. `int (*const [])(unsigned int, ...)`, accepted)

### §6.7.8 Type definitions / typedef (C99 §6.7.7)

> No material C99↔C11 divergence in this subsection (paragraph layout: p1 Syntax, p2 Constraint, p3 Semantics, p4–p8 EXAMPLEs 1–5).

- [✓] §6.7.8p1 (Syntax) — Recognize a *typedef-name* as an `identifier` that was previously declared with the `typedef` storage-class specifier and is in scope. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.8p2 (Constraint) — Require that a typedef name specifying a variably modified type has block scope (reject file-scope variably-modified typedefs). — mcc:✓ gcc:✓ clang:✓ (mcc rejects `int n=5; typedef int VLA[n];` at file scope; accepts the same typedef at block scope)
- [✓] §6.7.8p3 — In a declaration whose storage-class specifier is `typedef`, define each declarator's identifier as a typedef name denoting the type specified for that identifier per the declarator rules of §6.7.6 (C99: §6.7.5). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.8p3 — Treat a `typedef` declaration as introducing only a synonym for the specified type, **not** a new (distinct) type. — mcc:✓ gcc:✓ clang:✓ (`typedef int MILES; int *p; MILES *q; p = q;` accepted — type-identical)
- [✓] §6.7.8p3 — Given `typedef T type_ident; type_ident D;`, give the identifier in `D` the type "derived-declarator-type-list `T`", where the derived-declarator-type-list comes from the declarators of `D` and `T` is the type from the declaration specifiers. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.8p3 — Place a typedef name in the **same name space** as other identifiers declared in ordinary declarators (so a typedef name and an ordinary identifier of the same name in the same scope collide, and an inner ordinary declaration shadows an outer typedef name). — mcc:✓ gcc:✓ clang:✓ (mcc rejects `typedef int T; int T;` at the same scope — "'T' redeclared as different kind of symbol" — matching gcc/clang, which error "redeclared/redefinition as different kind of symbol". The shadowing half works too — inner `int T;` correctly shadows an outer typedef `T` on all three.)
- [✓] §6.7.8p3 — Re-evaluate any VLA size expressions in a typedef declarator **each time** the typedef declaration is reached in order of execution (and re-fix the array length each such time). — mcc:✓ gcc:✓ clang:✓ (size captured at each execution of the typedef; see p8 below)
- [✓] §6.7.8p4 (EXAMPLE 1) — Accept multiple declarators per `typedef` and typedef'd function/struct types, e.g. `typedef int MILES, KLICKSP();` (KLICKSP = "function with no parameter specification returning int") and `typedef struct { double hi, lo; } range;`; and treat objects of a typedef'd arithmetic type as compatible with any other object of the underlying type. — mcc:✓ gcc:✓ clang:✓ (`KLICKSP f; int f(){...}` definable through the typedef)
- [✓] §6.7.8p5 (EXAMPLE 2) — Permit a typedef name for an as-yet-incomplete tagged type (`typedef struct tnode TNODE;` before `struct tnode` is completed), allow it inside the struct's own member declarations (`TNODE *left, *right;`), and treat objects declared via the typedef as compatible with / synonymous for the underlying tagged type (`TNODE s, *sp;` ≡ `struct tnode`). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.8p6 (EXAMPLE 3) — Keep a typedef name visible as a type where it interacts with specifiers/qualifiers: in a struct, `unsigned t:4;` makes a type *specifier* shadow typedef `t` so `t` is a member name, whereas `const t:5;` is a *qualifier* applied to the still-visible typedef `t` (an unnamed bit-field), and `plain r:5;` uses typedef `plain`. — mcc:✓ gcc:✓ clang:✓ (the full EXAMPLE 3 struct compiles and `.t` is addressable as a member)
- [✓] §6.7.8p7 (EXAMPLE 4) — Make typedef'd function-type spellings denote exactly the same type as the fully written-out declaration, so the three `signal` declarations (`void (*signal(int, void (*)(int)))(int);`, `fv *signal(int, fv *);`, `pfv signal(int, pfv);`, with `typedef void fv(int), (*pfv)(int);`) are type-identical. — mcc:✓ gcc:✓ clang:✓ (all three spellings of `signal` coexist without "conflicting types")
- [✓] §6.7.8p8 (EXAMPLE 5) — Fix the length of a variable length array **typedef** at the time the typedef name is *defined*, not each time it is used (the value of `n` captured when `typedef int B[n];` executes governs all later uses of `B`). — mcc:✓ gcc:✓ clang:✓ (`typedef int B[n]; n+=1; B a;` → `sizeof a` uses the original `n`; all three print 3)

### §6.7.9 Initialization (C99 §6.7.8)

> **C99↔C11 differences in this subsection:** C11 broadened "static storage duration" to "**static or thread** storage duration" throughout p4/p10; changed p3's "an object type" to "a **complete** object type"; in p14 allows a **UTF-8 string literal** (`u8"..."`) and says "successive **bytes**" (C99: "characters"); in p15 allows arrays compatible with **`char16_t`/`char32_t`** (initialized by `u"..."`/`U"..."`) in addition to `wchar_t`/`L"..."`; and in p10 added the requirement that **padding bits/bytes be initialized to zero** for default-initialized static/thread aggregates and unions.

#### Syntax

- [✓] §6.7.9p1 — Accept an *initializer* that is an `assignment-expression`, `{ initializer-list }`, or `{ initializer-list , }` (trailing comma permitted). — mcc:✓ gcc:✓ clang:✓ (`int a[]={1,2,3,};` and `struct{int x,y;}s={1,2,};` accepted)
- [✓] §6.7.9p1 — Accept an *initializer-list* of `designation_opt initializer` items separated by commas. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.9p1 — Accept a *designation* as a *designator-list* followed by `=`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.9p1 — Accept a *designator-list* as one or more *designator*s in sequence (no separators). — mcc:✓ gcc:✓ clang:✓ (`[1].a[0] = 2`, `.d.c = 3` chains accepted)
- [✓] §6.7.9p1 — Accept a *designator* of the form `[ constant-expression ]` (array element) or `. identifier` (member). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.9p1 (C99 feature gate) — Treat designated initializers as a C99-and-later feature, diagnosing them under a strict C90 mode. — mcc:✓ gcc:✓ clang:✓ (under `-std=c89 -pedantic-errors` mcc rejects `int a[3]={[1]=2};` — "designated initializers are a C99 feature"; gcc errors "ISO C90 forbids specifying subobject to initialize" and clang errors "designated initializers are a C99 feature")

#### Constraints

- [✓] §6.7.9p2 — Reject any initializer that attempts to provide a value for an object **not contained within** the entity being initialized (e.g. more initializers than an aggregate has subobjects, or a string literal longer than a sized array — diagnose; cf. p29 EXAMPLE 6's "a diagnostic message would have been issued"). — mcc:✓ gcc:✓ clang:✓ (all diagnose; **note divergence in severity:** mcc treats excess array/aggregate initializers as a hard **error** and rejects, whereas gcc/clang emit a *warning* and accept — both satisfy the "issue a diagnostic" requirement. Over-long string-into-sized-array warns on all three.)
- [✓] §6.7.9p3 — Require the type of the initialized entity to be an array of unknown size or a complete object type `(C11; C99-only: "an object type")` that is **not** a variable length array type (reject initializing a VLA, and `(C11)` reject initializing an incomplete non-array object type). — mcc:✓ gcc:✓ clang:✓ (mcc errors "variable length array cannot be initialized" and "initialization of incomplete type")
- [✓] §6.7.9p4 — Require every expression in an initializer for an object of **static or thread** `(C11; C99-only: "static")` storage duration to be a constant expression or a string literal. — mcc:✓ gcc:✓ clang:✓ (mcc errors "initializer element is not constant" for `static int s = g();`)
- [✓] §6.7.9p5 — Reject any initializer on a block-scope declaration of an identifier that has external or internal linkage (e.g. a local `extern int x = 1;`). — mcc:✓ gcc:✓ clang:✓ (mcc rejects with a parse-level "';' expected"; gcc/clang give the semantic "block scope identifier with linkage cannot have an initializer" — a diagnostic is issued on all three)
- [✓] §6.7.9p6 — For a `[ constant-expression ]` designator, require the current object to have array type and the constant expression to be an integer constant expression. — mcc:✓ gcc:✓ clang:✓ (mcc errors "array type expected" for `.`-on-array and "integer constant expression must have integer type" for `[1.0]`)
- [✓] §6.7.9p6 — Permit any nonnegative index value in a `[ constant-expression ]` designator when the array is of unknown size. — mcc:✓ gcc:✓ clang:✓ (`int a[]={[100]=1};` accepted; negative `[-1]` rejected on all three)
- [✓] §6.7.9p7 — For a `. identifier` designator, require the current object to have structure or union type and the identifier to name a member of that type. — mcc:✓ gcc:✓ clang:✓ (mcc errors "field not found: nope" and rejects `.`-designator on a scalar)

#### Semantics — model

- §6.7.9p8 — Treat an initializer as specifying the initial value stored in the object. — (spec; not separately testable)
- [✓] §6.7.9p9 — Except where explicitly stated otherwise, do **not** let unnamed members of structure/union objects participate in initialization, and leave them with indeterminate value even after the object is initialized. — mcc:✓ gcc:✓ clang:✓ (unnamed `int :0;` bit-field padding skipped; `{1,2}` lands in the two named members on all three)
- §6.7.9p10 — Leave an object of automatic storage duration that is not explicitly initialized with an indeterminate value. — (spec; UB on read, not separately testable)
- [✓] §6.7.9p10 — Default-initialize an unspecified-init static/thread `(C11; C99-only: "static")` object of **pointer** type to a null pointer. — mcc:✓ gcc:✓ clang:✓ (static `int *sp;` reads as `(nil)`)
- [✓] §6.7.9p10 — Default-initialize an unspecified-init static/thread object of **arithmetic** type to (positive or unsigned) zero. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.9p10 — Default-initialize an unspecified-init static/thread **aggregate** by initializing every member (recursively per these rules); `(C11)` and set every padding bit/byte to zero. — mcc:✓ gcc:✓ clang:✓ (static `struct{char c;int i;}` with internal padding compares byte-equal to an all-zero buffer via `memcmp`)
- [✓] §6.7.9p10 — Default-initialize an unspecified-init static/thread **union** by initializing its first named member (recursively per these rules); `(C11)` and set every padding bit/byte to zero. — mcc:✓ gcc:✓ clang:✓

#### Semantics — scalars, strings, single-expression aggregates

- [✓] §6.7.9p11 — Require a scalar's initializer to be a single expression, optionally enclosed in braces. — mcc:✓ gcc:✓ clang:✓ (`int x={5};` accepted everywhere; on the "optionally" boundary, `int y={{5}};` — redundant extra braces around a scalar — draws a warning from mcc at `-Wall` ("too many braces around scalar initializer"), as it does from gcc ("braces around scalar initializer") and clang ("too many braces around scalar initializer"). mcc produces the correct value.)
- [✓] §6.7.9p11 — Set a scalar's initial value to the converted value of its initializer, applying the constraints and conversions of simple assignment, with the scalar's type taken as the *unqualified* version of its declared type. — mcc:✓ gcc:✓ clang:✓ (covered by p24 EXAMPLE 1)
- §6.7.9p12 — Apply the remaining rules (p13–p23) to objects of aggregate or union type. — (spec; not separately testable)
- [✓] §6.7.9p13 — Permit a structure/union object of automatic storage duration to be initialized either by a brace-enclosed initializer list, or by a single expression of compatible structure/union type. — mcc:✓ gcc:✓ clang:✓ (`struct S b = a;` copy-init works; an *incompatible*-type single expression is rejected by all three — mcc's block-scope diagnostic is a misleading parse-level "'{' expected", but it does reject)
- [✓] §6.7.9p13 — When a struct/union is initialized from a single compatible-type expression, set its initial value (including unnamed members) to that of the expression. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.9p14 — Permit an array of character type (`char`, `signed char`, `unsigned char`) to be initialized by a character string literal `(C11: or a UTF-8 string literal u8"...")`, optionally enclosed in braces. — mcc:✓ gcc:✓ clang:✓ (`char a[] = u8"abc";` and `char s[] = { "abc" };` both accepted, size 4)
- [✓] §6.7.9p14 — Initialize that array's elements with successive bytes `(C11; C99-only: "characters")` of the string literal, including the terminating null character if there is room or if the array is of unknown size. — mcc:✓ gcc:✓ clang:✓ (`char s[]="abc"` → 4 bytes; `char t[3]="abc"` → 3 bytes, null dropped, no diagnostic; a `char c[]=L"abc";` cross-type init is rejected)
- [✓] §6.7.9p15 — Permit an array whose element type is compatible with a qualified/unqualified `wchar_t` `(C11: or char16_t or char32_t)` to be initialized by a wide string literal `(C11: with the matching prefix — L for wchar_t, u for char16_t, U for char32_t)`, optionally enclosed in braces. — mcc:✓ gcc:✓ clang:✓ (`wchar_t w[]=L"abc"`, `char16_t a[]=u"abc"`, `char32_t b[]=U"abc"` all accepted; a *mismatched* prefix like `wchar_t w[]=u"abc";` is rejected on all three)
- [✓] §6.7.9p15 — Initialize that array's elements with successive wide characters of the wide string literal, including the terminating null wide character if there is room or if the array is of unknown size. — mcc:✓ gcc:✓ clang:✓

#### Semantics — brace-enclosed lists, current object, designators

- [✓] §6.7.9p16 — Otherwise (aggregate/union not initialized by string or single expression as above), require the initializer to be a brace-enclosed list of initializers for the elements or named members. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.9p17 — Associate each brace-enclosed initializer list with a "current object". — mcc:✓ gcc:✓ clang:✓ (model verified through EXAMPLEs 5/6/11)
- [✓] §6.7.9p17 — With no current designation, initialize subobjects of the current object in order according to its type: array elements in increasing subscript order, structure members in declaration order, and the first named member of a union. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.9p17 — Make a designation start initialization at the subobject it describes, then continue forward in order beginning with the next subobject after the designated one. — mcc:✓ gcc:✓ clang:✓ (`int a[5]={[1]=10,20,30};` → indices 1,2,3 get 10,20,30, rest zero)
- [✓] §6.7.9p17 (fn 148) — When a subaggregate's/contained-union's initializer does not begin with a left brace, initialize its subobjects as usual but do **not** make that subaggregate/union a current object (current objects associate only with brace-enclosed lists). — mcc:✓ gcc:✓ clang:✓ (`struct{int a[2];int b;}s={1,2,3};` → a={1,2}, b=3 — flat list flows into and out of the sub-array)
- [✓] §6.7.9p17 (fn 149) — After a union member is initialized, treat the next object as the next subobject of the object *containing* the union, not the next member of the union. — mcc:✓ gcc:✓ clang:✓ (`struct{union{int a;char b;}u;int c;}s={{5},7};` → u.a=5, c=7)
- [✓] §6.7.9p18 — Begin each designator list at the current object associated with the closest surrounding brace pair. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.9p18 — For each designator in the list (in order), select the named/indexed member of the current object and make it the current object for the next designator; the object at the end of the list is the subobject initialized by the following initializer. — mcc:✓ gcc:✓ clang:✓ (`.d.c = 3` selects nested member)
- [✓] §6.7.9p18 (fn 150) — Permit a designator to specify a strict subobject of the aggregate/union associated with the surrounding brace pair, and treat each separate designator list as independent. — mcc:✓ gcc:✓ clang:✓ (`{1, .d.c=3, .d.b=2}` initializes independent nested members)
- [✓] §6.7.9p19 — Perform initialization in initializer-list order, each initializer for a given subobject overriding any previously listed initializer for the same subobject. — mcc:✓ gcc:✓ clang:✓ (`int a[3]={[0]=1,[1]=2,[0]=9};` → a[0]=9)
- [✓] §6.7.9p19 — Implicitly initialize every subobject not explicitly initialized the same as objects of static storage duration (zero/null fill of un-designated/missing subobjects). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.9p19 (fn 151) — Permit not evaluating at all an initializer for a subobject that is subsequently overridden (and thus never used to initialize that subobject). — mcc:✓ gcc:✓ clang:✓ (this is a *permission*: mcc conformingly chooses to **still evaluate** the overridden side effect — `{[0]=f(),[0]=2}` calls `f` once on mcc, zero times on gcc/clang — all three are conforming since the footnote only permits, never requires, skipping)
- [✓] §6.7.9p20 — Apply the brace/elision rules recursively when an aggregate/union contains aggregate or union elements/members. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.9p20 — When a subaggregate's/contained-union's initializer begins with a left brace, initialize that subaggregate/union from the initializers enclosed by that brace and its matching right brace. — mcc:✓ gcc:✓ clang:✓ (EXAMPLE 4 `{ {1},{2},{3},{4} }`)
- [✓] §6.7.9p20 — When a subaggregate's/contained-union's initializer does **not** begin with a left brace (brace elision), take only enough initializers from the list to account for the subaggregate's elements / the union's first member, leaving the rest to initialize the next element or member of the enclosing aggregate. — mcc:✓ gcc:✓ clang:✓ (EXAMPLE 6 `short q[4][3][2]={{1},{2,3},{4,5,6}}` brace-elides correctly)
- [✓] §6.7.9p21 — When a brace-enclosed list has fewer initializers than the aggregate has elements/members (or a string literal has fewer characters than a known-size array has elements), initialize the remainder implicitly the same as static-storage objects (partial-init zero fill). — mcc:✓ gcc:✓ clang:✓ (`int a[5]={1,2}` and `struct{int x,y,z;}s={1}` zero-fill the tail)
- [✓] §6.7.9p22 — Determine the size of an array of unknown size from the largest indexed element that has an explicit initializer, and complete the array type at the end of its initializer list. — mcc:✓ gcc:✓ clang:✓ (`int a[]={[10]=1,[3]=2};` → `sizeof` is 11 ints)
- §6.7.9p23 — Treat the evaluations of the initializer-list expressions as indeterminately sequenced with respect to one another, so the order in which their side effects occur is unspecified. — (spec; unspecified behavior, not separately testable)
- §6.7.9p23 (fn 152) — Do not require the evaluation order of initializer expressions to match the order of subobject initialization. — (spec; unspecified behavior, not separately testable)

#### Semantics — examples (normative behavior illustrated)

- [✓] §6.7.9p24 (EXAMPLE 1) — Apply scalar-assignment conversion in initializers: `int i = 3.5;` yields 3; `double complex c = 5 + 3 * I;` yields 5.0 + i3.0. — mcc:✓ gcc:✓ clang:✓ (prints `3 5.0 3.0`)
- [✓] §6.7.9p25 (EXAMPLE 2) — Size `int x[] = { 1, 3, 5 };` as a 3-element array from its three initializers. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.9p26 (EXAMPLE 3) — Handle fully bracketed multidimensional init for `int y[4][3]` with trailing rows zero-filled, and treat the equivalent minimally bracketed flat list as identical. — mcc:✓ gcc:✓ clang:✓ (`y` and the flat-list `z` compare element-equal; `y[3][0]==0`)
- [✓] §6.7.9p27 (EXAMPLE 4) — Initialize only the first column from `int z[4][3] = { {1},{2},{3},{4} };` and zero-fill the rest. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.9p28 (EXAMPLE 5) — Handle inconsistently bracketed init `struct { int a[3], b; } w[] = { {1}, 2 };` → `w[0].a[0]==1`, `w[1].a[0]==2`, all other members zero. — mcc:✓ gcc:✓ clang:✓ (prints `2 1 0 2 0`)
- [✓] §6.7.9p29 (EXAMPLE 6) — Handle incompletely-but-consistently bracketed nested init for `short q[4][3][2]` (brace elision consuming up to six items per `[3][2]` subaggregate), and issue a diagnostic if any subaggregate's brace-enclosed list supplies more initializers than that subaggregate has subobjects. — mcc:✓ gcc:✓ clang:✓ (correct elision; mcc *errors* on the over-full sub-brace `{1,2,3,4,5,6,7}` while gcc/clang *warn* — all issue the required diagnostic)
- [✓] §6.7.9p31 (EXAMPLE 7) — Complete an array type given via a block-scope incomplete-array typedef `typedef int A[];` used as `A a = {1,2}, b = {3,4,5};`, identical to `int a[] = {1,2}, b[] = {3,4,5};` (independent sizes 2 and 3). — mcc:✓ gcc:✓ clang:✓ (prints `2 3`)
- [✓] §6.7.9p32 (EXAMPLE 8) — Make `char s[] = "abc";` a 4-element modifiable array (incl. `'\0'`); make `char t[3] = "abc";` a 3-element array (null dropped, no diagnostic); make `char *p = "abc";` point to a string-literal array whose modification is undefined behavior. — mcc:✓ gcc:✓ clang:✓ (`sizeof s==4`, `sizeof t==3`, no diagnostic on `t`)
- [✓] §6.7.9p33 (EXAMPLE 9) — Support array designators whose index is an enumeration constant (`[member_two] = ...`). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.9p34 (EXAMPLE 10) — Support member designators in any order (`div_t answer = { .quot = 2, .rem = -1 };`). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.9p35 (EXAMPLE 11) — Support mixed/compound designators such as `struct { int a[3], b; } w[] = { [0].a = {1}, [1].a[0] = 2 };`. — mcc:✓ gcc:✓ clang:✓ (prints `2 1 2`)
- [✓] §6.7.9p36 (EXAMPLE 12) — Support "allocate from both ends" via a single trailing designator, `int a[MAX] = { 1,3,5,7,9, [MAX-5] = 8,6,4,2,0 };`, with later overlapping initializers overriding earlier ones (so the result depends on `MAX`). — mcc:✓ gcc:✓ clang:✓ (`MAX=10` → `1 3 5 7 9 8 6 4 2 0`)
- [✓] §6.7.9p38 (EXAMPLE 13) — Support initializing any named member of a union via a designator (`union { ... } u = { .any_member = 42 };`). — mcc:✓ gcc:✓ clang:✓ (`{.l=42}` → `u.l==42`)

#### Added gaps / edge cases not in the consolidated draft

- [✓] §6.7.9p11 (redundant-brace diagnostic) — Diagnose redundant braces surrounding a scalar initializer (`int y = {{5}};`). — mcc:✓ gcc:✓ clang:✓ (mcc warns at `-Wall` — "too many braces around scalar initializer" — as do gcc/clang. Value is still correct.)
- [✓] §6.7.9p2 (excess-initializer severity) — Diagnose an aggregate/string initializer that supplies more elements than the object has subobjects. — mcc:✓ gcc:✓ clang:✓ **(divergence: mcc makes this a hard error and rejects; gcc/clang downgrade it to a warning and accept. The standard only requires "a diagnostic", which all three satisfy, but mcc is the strictest — code that gcc/clang compile-with-warning will fail to compile on mcc.)**
- [~] §6.7.9p13 (incompatible single-expression diagnostic quality) — Report a clear type error when a struct/union is initialized from a single expression of incompatible struct/union type. — mcc:~ gcc:✓ clang:✓ **(GAP-ish: mcc rejects but with a misleading parse-level message ("'{' expected (got ';')") instead of an "incompatible type" diagnostic; gcc/clang give the precise semantic error. It does reject, so it is conforming, but the diagnostic is low quality.)**
- [✓] §6.7.9 (empty-brace initializer `{}`) — Accept/handle the empty brace-enclosed initializer list `int a[3] = {};` (C23 §6.7.10 / pre-C23 GNU extension; **not** valid ISO C99/C11). — mcc:✓ gcc:✓ clang:✓ **(All three accept it as an extension and zero-fill; note it is a constraint violation under strict C99/C11 and none of the three reject it even with `-std=c11`.)**
- [✓] §6.7.9p10 / §6.7.9p21 (static-storage zero/null fill is the partial-init rule) — Confirm that a partially-initialized aggregate's missing subobjects (including nested pointers) get the *same* default as fully-default static objects (zero for arithmetic, null for pointers, recursively). — mcc:✓ gcc:✓ clang:✓ (`static struct{int a;int *b;char c[4];}sa;` → all members zero/null)


## §6.8 Statements and blocks

> mcc status: 81 TODO checkboxes below; 2 are mcc-gaps (mcc:✗/~ where it diverges from the gcc==clang consensus). Both §6.8.6.4p1 return-mismatch constraints — a `void` function returning a value, or a non-`void` function returning nothing — are hard errors by default in mcc (the `return-type`/`return-mismatch` warning is on+error by default), matching gcc 15 and clang 22. The mcc gaps are pedantic-only: mcc accepts the GNU `case 1 ... 5:` range and computed `goto *ptr` even under `-pedantic-errors`, where gcc/clang reject them. All structural syntax, all VLA-scope jump constraints, switch/loop/goto semantics, integer-promotion of `case` values, for-decl scoping, and aggregate-return aliasing match the consensus exactly.

> C99 vs C11: §6.8p3 lists the runtime semantics of declarations reached in execution order. §6.8p4 was rewritten between C99 and C11: C99 enumerates the kinds of full expression and ends each with a sequence point; C11 instead defines a full expression structurally, adds the variably-modified-type implicit full expression, and states the inter-full-expression sequence point (the latter migrated from Annex C / §5.1.2.3). The `for ( declaration ; ... )` form, VLAs, and intermixed declarations/statements in a block are all C99 additions absent from C90.

### §6.8 Statements and blocks (general)

- §6.8p1 — A `statement` shall be one of: labeled-statement, compound-statement, expression-statement, selection-statement, iteration-statement, or jump-statement (syntax of the `statement` nonterminal). — (spec; not separately testable)
- §6.8p2 — A statement specifies an action to be performed; except as indicated, statements are executed in sequence. — (spec; not separately testable)
- §6.8p3 — A block allows a set of declarations and statements to be grouped into one syntactic unit. — (spec; not separately testable)
- [✓] §6.8p3 — The initializers of objects that have automatic storage duration, and the variable length array declarators of ordinary identifiers with block scope, are evaluated and the values stored in the objects (including storing an indeterminate value in objects without an initializer) each time the declaration is reached in the order of execution, as if it were a statement (the VLA-declarator evaluation is C99). — mcc:✓ gcc:✓ clang:✓ (backward `goto` over a block-scope VLA re-creates it each time the declaration is re-reached; runtime probe `vla=3` matches)
- [✓] §6.8p3 — Within each declaration, the automatic initializers / VLA declarators are evaluated in the order in which the declarators appear. — mcc:✓ gcc:✓ clang:✓ (`for(int i=0,j=10;...)` multi-declarator left-to-right matches)
- §6.8p4 — A full expression is an expression that is not part of another expression, nor part of a declarator or abstract declarator (C11 structural definition; C99 worded it as "not part of another expression or of a declarator"). — (spec; not separately testable)
- §6.8p4 — (C99-only enumeration) Each of the following is a full expression: an initializer; the expression in an expression statement; the controlling expression of a selection statement (`if` or `switch`); the controlling expression of a `while` or `do` statement; each of the optional expressions of a `for` statement; the optional expression in a `return` statement. — (spec; not separately testable)
- §6.8p4 — (C11) There is also an implicit full expression in which the non-constant size expressions for a variably modified type are evaluated; within that full expression, the evaluation of different size expressions is unsequenced with respect to one another. — (spec; not separately testable)
- §6.8p4 — The end of a full expression is a sequence point / there is a sequence point between the evaluation of a full expression and the evaluation of the next full expression to be evaluated (C99 phrases it as "the end of a full expression is a sequence point"; C11 phrases the inter-full-expression sequence point). — (spec; not separately testable)
- §6.8p5 — (Forward references only, no normative requirement: expression and null statements §6.8.3, selection statements §6.8.4, iteration statements §6.8.5, the `return` statement §6.8.6.4.) — (spec; not separately testable)

### §6.8.1 Labeled statements

- [✓] §6.8.1p1 — A `labeled-statement` shall be one of: `identifier : statement`, `case constant-expression : statement`, or `default : statement` (syntax). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.8.1p2 — Constraint: A `case` or `default` label shall appear only in a `switch` statement (further `case`/`default` constraints are in §6.8.4.2). — mcc:✓ gcc:✓ clang:✓ (`case 3:` outside switch ⇒ mcc "switch expected")
- [✓] §6.8.1p3 — Constraint: Label names shall be unique within a function. — mcc:✓ gcc:✓ clang:✓ (`a: ; a: ;` ⇒ mcc "duplicate label 'a'")
- [✓] §6.8.1p4 — Any statement may be preceded by a prefix that declares an identifier as a label name; labels in themselves do not alter the flow of control, which continues unimpeded across them. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.8.1 (extension) — GNU local labels `__label__ name;` declared at the head of a block. — mcc:✓ gcc:✓ clang:✓ (default); under `-pedantic-errors` mcc:✓ clang:✓ but gcc:✗ rejects (no gcc==clang consensus, so not counted as an mcc gap)
- [~] §6.8.1 (constraint) — A label may prefix a `statement` but a `declaration` is not a statement, so `lbl: int x;` is ill-formed in C99/C11. — mcc:~ gcc:~ clang:~ (all three accept by default; all three reject under `-pedantic-errors`; mcc matches consensus)

### §6.8.2 Compound statement

- [✓] §6.8.2p1 — A `compound-statement` is `{ block-item-list_opt }`, where a `block-item-list` is one or more `block-item`s and a `block-item` is a `declaration` or a `statement` (syntax; C99 permits declarations and statements to be intermixed within a block, whereas C90 required all declarations to precede statements). — mcc:✓ gcc:✓ clang:✓ (intermixed `int p; p+=3; int q=p*2;` ⇒ `mix=10`)
- §6.8.2p2 — A compound statement is a block. — (spec; not separately testable)

### §6.8.3 Expression and null statements

- [✓] §6.8.3p1 — An `expression-statement` is `expression_opt ;` (syntax). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.8.3p2 — The expression in an expression statement is evaluated as a void expression for its side effects. — mcc:✓ gcc:✓ clang:✓
- §6.8.3p2 (footnote) — Such side-effect expressions are, e.g., assignments and function calls that have side effects (informative). — (spec; not separately testable)
- [✓] §6.8.3p3 — A null statement (consisting of just a semicolon) performs no operations. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.8.3p4 — (EXAMPLE 1, informative) A null statement may be used as the loop body of an iteration statement whose work is done entirely in the controlling/`for` expressions. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.8.3p5 — (EXAMPLE 2, informative) A null statement may be used to carry a label immediately before the closing `}` of a compound statement. — mcc:✓ gcc:~ clang:~ (a label with NO statement before `}` is accepted by default by all three but rejected by all three under `-pedantic-errors`; mcc matches consensus, rejecting it as "label at end of compound statement is a C23 feature")
- [✓] §6.8.3p6 — (EXAMPLE 3, informative) A null statement may be used as the body of a `while` whose controlling expression performs all the work (e.g. `while (*q++ = *p++) ;`). — mcc:✓ gcc:✓ clang:✓ (`while(s[len]) len++;` style ⇒ `len=3`)

### §6.8.4 Selection statements

- [✓] §6.8.4p1 — A `selection-statement` shall be one of: `if ( expression ) statement`, `if ( expression ) statement else statement`, or `switch ( expression ) statement` (syntax). — mcc:✓ gcc:✓ clang:✓
- §6.8.4p2 — A selection statement selects among a set of statements depending on the value of a controlling expression. — (spec; not separately testable)
- [✓] §6.8.4p3 — A selection statement is a block whose scope is a strict subset of the scope of its enclosing block, and each associated substatement is also a block whose scope is a strict subset of the scope of the selection statement (C99). — mcc:✓ gcc:✓ clang:✓ (each substatement is its own block scope)

### §6.8.4.1 The if statement

- [✓] §6.8.4.1p1 — Constraint: The controlling expression of an `if` statement shall have scalar type. — mcc:✓ gcc:✓ clang:✓ (`if(struct)` ⇒ mcc rejects)
- [✓] §6.8.4.1p2 — In both forms, the first substatement is executed if the expression compares unequal to 0; in the `else` form, the second substatement is executed if the expression compares equal to 0. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.8.4.1p2 — If the first substatement is reached via a label, the second substatement is not executed. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.8.4.1p3 — An `else` is associated with the lexically nearest preceding `if` that is allowed by the syntax (dangling-else resolution). — mcc:✓ gcc:✓ clang:✓ (`if(a) if(b) r=1; else r=2;` with a=1,b=0 ⇒ `dang=2`, else binds to inner)

### §6.8.4.2 The switch statement

- [✓] §6.8.4.2p1 — Constraint: The controlling expression of a `switch` statement shall have integer type. — mcc:✓ gcc:✓ clang:✓ (`switch(double)` and `switch(float)` ⇒ mcc "switch value not an integer")
- [✓] §6.8.4.2p2 — Constraint: If a `switch` statement has an associated `case` or `default` label within the scope of an identifier with a variably modified type, the entire `switch` statement shall be within the scope of that identifier (C99 — VLA interaction). — mcc:✓ gcc:✓ clang:✓ (`case` inside a `{int a[n];}` block of the switch body ⇒ mcc "switch jumps into the scope of a variably modified declaration")
- [✓] §6.8.4.2p3 — Constraint: The expression of each `case` label shall be an integer constant expression. — mcc:✓ gcc:✓ clang:✓ (`case y:` non-const ⇒ mcc "constant expression expected")
- [✓] §6.8.4.2p3 — Constraint: No two of the `case` constant expressions in the same `switch` statement shall have the same value after conversion. — mcc:✓ gcc:✓ clang:✓ (both `case 1:` and `case (char)257:` (=1) ⇒ mcc "duplicate case value")
- [✓] §6.8.4.2p3 — Constraint: There shall be at most one `default` label in a `switch` statement. — mcc:✓ gcc:✓ clang:✓ (two `default:` ⇒ mcc "too many 'default'")
- [✓] §6.8.4.2p3 — Constraint clarification: Any enclosed `switch` statement may have its own `default` label or `case` constant expressions that duplicate those of the enclosing `switch`. — mcc:✓ gcc:✓ clang:✓ (nested switches with overlapping case/default accepted)
- [✓] §6.8.4.2p4 — A `switch` statement causes control to jump to, into, or past the statement that is the switch body, depending on the value of the controlling expression and on the presence of a `default` label and the values of any `case` labels on or in the switch body. — mcc:✓ gcc:✓ clang:✓ (fallthrough+default probe ⇒ `sw=221`)
- [✓] §6.8.4.2p4 — A `case` or `default` label is accessible only within the closest enclosing `switch` statement. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.8.4.2p5 — The integer promotions are performed on the controlling expression, and the constant expression in each `case` label is converted to the promoted type of the controlling expression. — mcc:✓ gcc:✓ clang:✓ (`(char)257`→1 dup detection demonstrates conversion to promoted type)
- [✓] §6.8.4.2p5 — If a converted `case` value matches that of the promoted controlling expression, control jumps to the statement following the matched `case` label; otherwise, if there is a `default` label, control jumps to the labeled statement. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.8.4.2p5 — If no converted `case` constant expression matches and there is no `default` label, no part of the switch body is executed. — mcc:✓ gcc:✓ clang:✓
- §6.8.4.2p6 — Implementation limits: As discussed in §5.2.4.1, the implementation may limit the number of `case` values in a `switch` statement. — (spec; not separately testable)
- [✓] §6.8.4.2p7 — (EXAMPLE, informative) Object declarations within a switch body are reachable but their initializers are not executed if jumped over (e.g. a VLA inside the switch body and a fall-through example). — mcc:✓ gcc:✓ clang:✓ (non-VLA decl jumped over is uninitialized but reachable; VLA decl in switch body is a §6.8.4.2p2 constraint error)
- [~] §6.8.4.2 (extension) — GNU `case lo ... hi:` range labels. — mcc:~ gcc:✓ clang:✓ (all accept by default; **under `-pedantic-errors` mcc accepts but gcc/clang reject** — mcc fails to diagnose the extension)
- [✓] §6.8.4.2 (semantics) — A `switch` may control an `enum`-typed expression (integer type). — mcc:✓ gcc:✓ clang:✓

### §6.8.5 Iteration statements

- [✓] §6.8.5p1 — An `iteration-statement` shall be one of: `while ( expression ) statement`; `do statement while ( expression ) ;`; `for ( expression_opt ; expression_opt ; expression_opt ) statement`; or `for ( declaration expression_opt ; expression_opt ) statement` (syntax; the `for ( declaration ... )` form is C99). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.8.5p2 — Constraint: The controlling expression of an iteration statement shall have scalar type. — mcc:✓ gcc:✓ clang:✓ (`while(struct)` ⇒ mcc rejects)
- [~] §6.8.5p3 — Constraint: The declaration part of a `for` statement shall only declare identifiers for objects having storage class `auto` or `register` (C99). — mcc:~ gcc:~ clang:~ (all three accept `for(static int..)`, `for(extern int..)`, `for(typedef..)` by default; all three reject under `-pedantic-errors`; mcc matches consensus)
- [✓] §6.8.5p4 — An iteration statement causes a statement called the loop body to be executed repeatedly until the controlling expression compares equal to 0; the repetition occurs regardless of whether the loop body is entered from the iteration statement or by a jump. — mcc:✓ gcc:✓ clang:✓
- §6.8.5p4 (footnote) — Code jumped over is not executed; in particular, the controlling expression of a `for` or `while` statement is not evaluated before entering the loop body via a jump, nor is clause-1 of a `for` statement (informative). — (spec; not separately testable)
- [✓] §6.8.5p5 — An iteration statement is a block whose scope is a strict subset of the scope of its enclosing block, and the loop body is also a block whose scope is a strict subset of the scope of the iteration statement (C99). — mcc:✓ gcc:✓ clang:✓

### §6.8.5.1 The while statement

- [✓] §6.8.5.1p1 — The evaluation of the controlling expression takes place before each execution of the loop body. — mcc:✓ gcc:✓ clang:✓ (`while(w<3) w++` ⇒ `wh=3`)

### §6.8.5.2 The do statement

- [✓] §6.8.5.2p1 — The evaluation of the controlling expression takes place after each execution of the loop body. — mcc:✓ gcc:✓ clang:✓ (`do{c++;}while(0)` runs body once ⇒ `do=1`)

### §6.8.5.3 The for statement

- [✓] §6.8.5.3p1 — The statement `for ( clause-1 ; expression-2 ; expression-3 ) statement` behaves as specified in this subclause; `expression-2` is the controlling expression that is evaluated before each execution of the loop body, and `expression-3` is evaluated as a void expression after each execution of the loop body. — mcc:✓ gcc:✓ clang:✓ (`for(int i=0;i<5;i++) sum+=i` ⇒ `for=10`)
- [✓] §6.8.5.3p1 — If `clause-1` is a declaration, the scope of any identifiers it declares is the remainder of the declaration and the entire loop, including the other two expressions (C99). — mcc:✓ gcc:✓ clang:✓ (loop variable not visible after loop; multi-declarator `for(int i=0,j=10; i<j; i++,j--)` accepted)
- [✓] §6.8.5.3p1 — If `clause-1` is a declaration it is reached in the order of execution before the first evaluation of the controlling expression; if `clause-1` is an expression it is evaluated as a void expression before the first evaluation of the controlling expression (the declaration form is C99). — mcc:✓ gcc:✓ clang:✓
- §6.8.5.3p1 (footnote) — `clause-1` specifies loop initialization; `expression-2` specifies an evaluation made before each iteration such that execution continues until it compares equal to 0; `expression-3` specifies a per-iteration operation such as incrementing (informative). — (spec; not separately testable)
- [✓] §6.8.5.3p2 — Both `clause-1` and `expression-3` can be omitted; an omitted `expression-2` is replaced by a nonzero constant (i.e. the loop does not terminate on the controlling expression). — mcc:✓ gcc:✓ clang:✓ (`for(;;){...break;}` ⇒ `inf=7`)

### §6.8.6 Jump statements

- [✓] §6.8.6p1 — A `jump-statement` shall be one of: `goto identifier ;`, `continue ;`, `break ;`, or `return expression_opt ;` (syntax). — mcc:✓ gcc:✓ clang:✓
- §6.8.6p2 — A jump statement causes an unconditional jump to another place. — (spec; not separately testable)

### §6.8.6.1 The goto statement

- [✓] §6.8.6.1p1 — Constraint: The identifier in a `goto` statement shall name a label located somewhere in the enclosing function. — mcc:✓ gcc:✓ clang:✓ (`goto nope;` undefined ⇒ mcc "label 'nope' used but not defined")
- [✓] §6.8.6.1p1 — Constraint: A `goto` statement shall not jump from outside the scope of an identifier having a variably modified type to inside the scope of that identifier (C99 — VLA jump restriction). — mcc:✓ gcc:✓ clang:✓ (`goto` into a `{int a[n]; lbl:}` block ⇒ mcc "goto jumps into the scope of a variably modified declaration")
- [✓] §6.8.6.1p2 — A `goto` statement causes an unconditional jump to the statement prefixed by the named label in the enclosing function. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.8.6.1p3 — (EXAMPLE 1, informative) It is sometimes convenient to jump into the middle of a complicated set of statements; an array declared with a non-VLA bound may be jumped over while a VLA declarator may not, illustrating the §6.8.6.1p1 VLA constraint. — mcc:✓ gcc:✓ clang:✓ (jumping over a non-VLA array is allowed; jumping into a VLA's scope is rejected)
- [✓] §6.8.6.1p4 — (EXAMPLE 2, informative) A `goto` may be used to jump back to before a block-scope VLA declaration, in which case the VLA's lifetime ends on the backward jump and the VLA is created anew when its declaration is re-reached. — mcc:✓ gcc:✓ clang:✓ (backward-`goto` VLA recreation runtime probe ⇒ `vla=3`)
- [~] §6.8.6.1 (extension) — GNU computed goto `goto *expr;` with `&&label` address-of-label. — mcc:~ gcc:✓ clang:✓ (all accept by default; **under `-pedantic-errors` mcc accepts but gcc/clang reject** — mcc fails to diagnose the extension)

### §6.8.6.2 The continue statement

- [✓] §6.8.6.2p1 — Constraint: A `continue` statement shall appear only in or as a loop body. — mcc:✓ gcc:✓ clang:✓ (`continue;` outside a loop ⇒ mcc "cannot continue")
- [✓] §6.8.6.2p2 — A `continue` statement causes a jump to the loop-continuation portion (the end) of the smallest enclosing iteration statement; unless the `continue` is in an enclosed iteration statement (in which case it is interpreted within that statement), it is equivalent to `goto contin;` placed just before the closing brace / `while` of the smallest enclosing `while`, `do`, or `for` loop body. — mcc:✓ gcc:✓ clang:✓ (nested-loop `continue` affects only the inner loop ⇒ `cont=6`)
- §6.8.6.2p2 (footnote) — Following the `contin:` label is a null statement (informative). — (spec; not separately testable)

### §6.8.6.3 The break statement

- [✓] §6.8.6.3p1 — Constraint: A `break` statement shall appear only in or as a switch body or loop body. — mcc:✓ gcc:✓ clang:✓ (`break;` outside ⇒ mcc "cannot break")
- [✓] §6.8.6.3p2 — A `break` statement terminates execution of the smallest enclosing `switch` or iteration statement. — mcc:✓ gcc:✓ clang:✓ (nested-loop `break` terminates only the inner loop ⇒ `brk=303`)

### §6.8.6.4 The return statement

- [✓] §6.8.6.4p1 — Constraint: A `return` statement with an expression shall not appear in a function whose return type is `void`. — mcc:✓ gcc:✓ clang:✓ (`void f(void){return 3;}` is a hard error by default in all three; mcc's `warn_return_type` is on+error by default, downgradable via `-Wno-error=return-type`/`-Wno-return-type`.)
- [✓] §6.8.6.4p1 — Constraint: A `return` statement without an expression shall only appear in a function whose return type is `void`. — mcc:✓ gcc:✓ clang:✓ (`int f(void){return;}` is a hard error by default in all three; see `-Wreturn-type`/`-Wreturn-mismatch`.)
- [✓] §6.8.6.4p1 (clarification) — A `return` with a *void-typed* expression is permitted in a `void` function (e.g. `return g();` where `g` returns `void`). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.8.6.4p2 — A `return` statement terminates execution of the current function and returns control to its caller; a function may have any number of `return` statements. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.8.6.4p3 — If a `return` statement with an expression is executed, the value of the expression is returned to the caller as the value of the function call expression. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.8.6.4p3 — If the expression has a type different from the return type of the function in which it appears, the value is converted as if by assignment to an object having the return type of the function. — mcc:✓ gcc:✓ clang:✓ (`double retd(){return 3;}`⇒3.0; `unsigned char retc(){return 300;}`⇒44)
- §6.8.6.4p3 (footnote) — The `return` statement is not an assignment; the overlap restriction of simple assignment (§6.5.16.1) does not apply to the case of function return (informative). — (spec; not separately testable)
- [✓] §6.8.6.4p4 — (EXAMPLE, informative) The `return` expression's value behaves as if assigned to an unnamed object of the return type, so a returned value may alias an object passed by the caller without the simple-assignment overlap constraint applying. — mcc:✓ gcc:✓ clang:✓ (`x = id(x)` with a struct argument ⇒ `1234`, no overlap diagnostic)


## §6.9 External definitions

> mcc status: 44 TODO checkboxes in this section; 4 are mcc-gaps (mcc diverges from the gcc==clang consensus). Gaps: a declaration-list entry with no declarator is accepted with only a warning (§6.9.1p6); an undeclared/implicit-`int` old-style identifier-list parameter is accepted with only a warning (§6.9.1p6 / C99 no-implicit-int, two rows); a file-scope declaration relying on implicit `int` is accepted with only a warning (§6.9p1). mcc now rejects a function defined through a typedef'd function type (§6.9.1p2), an incomplete return type in a definition (§6.9.1p3), and an incomplete parameter type in a definition (§6.9.1p7), and warns on a never-completed internal-linkage incomplete-array tentative definition (§6.9.2p3), all matching the gcc==clang consensus. Constraints that mcc enforces correctly include file-scope `auto`/`register`, double definition, omitted prototype-parameter names (mcc rejects per C99/C11 where gcc/clang accept the C23 extension), and the multiple-/missing-external-definition link checks.

> Material C99→C11 difference in this section: the "used in an expression" exclusions of §6.9p3 and §6.9p5 list only `sizeof` in C99, whereas C11 reads "`sizeof` or `_Alignof`". The parameter-adjustment cross-reference is §6.7.5.3 in C99 (N1256) and §6.7.6.3 in C11 (N1570).

### §6.9 External definitions (general)

Syntax:
- §6.9p1 — A *translation-unit* is a sequence of *external-declaration*s: either a single *external-declaration*, or a *translation-unit* followed by an *external-declaration*. — (spec; not separately testable)
- [✓] §6.9p1 — An *external-declaration* is either a *function-definition* or a *declaration*; no other forms are permitted to appear at file scope (outside any function). — mcc:✓ gcc:✓ clang:✓ (stray file-scope expression `3+4;` rejected by all three)

Constraints:
- [✓] §6.9p2 — The storage-class specifiers `auto` and `register` shall not appear in the declaration specifiers in an external declaration. — mcc:✓ gcc:✓ clang:✓ (mcc rejects both `auto int x;` and `register int x;` at file scope)
- [✓] §6.9p3 — There shall be no more than one external definition for each identifier declared with internal linkage in a translation unit. — mcc:✓ gcc:✓ clang:✓ (two `static int x = …;` defs rejected as "defined twice")
- [✓] §6.9p3 — If an identifier declared with internal linkage is used in an expression (other than as part of the operand of a `sizeof` operator, or in C11 a `sizeof` or `_Alignof` operator, whose result is an integer constant), there shall be exactly one external definition for the identifier in the translation unit. *(C11 adds `_Alignof` to the exclusion; C99 lists only `sizeof`.)* — mcc:✓ gcc:~ clang:~ (a `static` function used but never defined draws a "used but never defined"-class warning from mcc ("'f' used but never defined"), as it does from gcc/clang)

Semantics:
- §6.9p4 — As discussed in 5.1.1.1, the unit of program text after preprocessing is a translation unit, which consists of a sequence of external declarations; these are described as "external" because they appear outside any function (and hence have file scope). — (spec; not separately testable)
- §6.9p4 — As discussed in 6.7, a declaration that also causes storage to be reserved for an object or a function named by the identifier is a *definition*. — (spec; not separately testable)
- §6.9p5 — An *external definition* is an external declaration that is also a definition of a function (other than an inline definition) or an object. — (spec; not separately testable)
- [✓] §6.9p5 — If an identifier declared with external linkage is used in an expression (other than as part of the operand of a `sizeof` operator, or in C11 a `sizeof` or `_Alignof` operator, whose result is an integer constant), somewhere in the entire program there shall be exactly one external definition for the identifier; otherwise, there shall be no more than one. *(C11 adds `_Alignof` to the exclusion; C99 lists only `sizeof`.)* — mcc:✓ gcc:✓ clang:✓ (missing external def → "unresolved reference" link error; two external defs of `int shared` → "defined twice"; one def across two TUs links and runs rc=0)

### §6.9.1 Function definitions

Syntax:
- [✓] §6.9.1p1 — A *function-definition* consists of *declaration-specifiers*, a *declarator*, an optional *declaration-list*, and a *compound-statement*, in that order. — mcc:✓ gcc:✓ clang:✓ (well-formed definitions accepted; misordered forms rejected)
- [✓] §6.9.1p1 — A *declaration-list* is one *declaration*, or a *declaration-list* followed by a *declaration* (this is the K&R/old-style parameter declaration list; identifier-list function definitions are an obsolescent feature, §6.11.7). — mcc:✓ gcc:✓ clang:✓ (`int f(x) int x; { … }` accepted; gcc/clang warn old-style)

Constraints:
- [✓] §6.9.1p2 — The identifier declared in a function definition (the name of the function) shall have a function type, as specified by the declarator portion of the function definition. — mcc:✓ gcc:✓ clang:✓ (`typedef int FT(void); FT g { return 1; }` — mcc rejects a definition whose function type comes from a typedef rather than the declarator ("function definition declared with a typedef'd function type"), as do gcc/clang, because the declarator portion does not specify a function type)
- [✓] §6.9.1p3 — The return type of a function shall be `void` or a complete object type other than array type. — mcc:✓ gcc:✓ clang:✓ (array return `int f()[3]` and typedef'd-array return rejected by all three; `struct S; struct S f(void){ }` — mcc rejects a definition returning an incomplete struct ("return type is an incomplete type"), as do gcc/clang ("incomplete result type"))
- [✓] §6.9.1p4 — The storage-class specifier, if any, in the declaration specifiers shall be either `extern` or `static`. — mcc:✓ gcc:~ clang:✓ (mcc rejects `auto`/`register`/`typedef` function definitions; clang rejects too; gcc accepts `auto int f(){…}` with only a warning)
- [✓] §6.9.1p5 — If the declarator includes a parameter type list, the declaration of each parameter shall include an identifier, except for the special case of a parameter list consisting of a single parameter of type `void`, in which case there shall not be an identifier. — mcc:✓ gcc:✓ clang:✓ (mcc rejects the unnamed parameter `int f(int, int y){…}` per the C99/C11 constraint — gcc/clang now *accept* it as a C23 extension with a `-Wc23-extensions` note; mcc also rejects the named `void` parameter `int f(void v)`)
- [✓] §6.9.1p5 — If the declarator includes a parameter type list, no declaration list shall follow (the parameter-type-list and identifier-list forms are mutually exclusive). — mcc:✓ gcc:✓ clang:✓ (`int f(int x) int x; {…}` rejected by all three)
- [✗] §6.9.1p6 — If the declarator includes an identifier list, each declaration in the declaration list shall have at least one declarator. — mcc:✗ gcc:✓ clang:✓ (**gap**: `int f(x) int; { return x; }` — mcc accepts with only a "useless type defines no instances" warning; gcc/clang reject "declaration does not declare a parameter")
- [✓] §6.9.1p6 — If the declarator includes an identifier list, those declarators shall declare only identifiers from the identifier list. — mcc:✓ gcc:✓ clang:✓ (`int f(x) int x; int z; {…}` rejected — "declaration for parameter 'z' but no such parameter")
- [✗] §6.9.1p6 — If the declarator includes an identifier list, every identifier in the identifier list shall be declared. — mcc:✗ gcc:✓ clang:✓ (**gap, see added item below**: `int f(x){ return x; }` — mcc accepts with "type of 'x' defaults to 'int'" warning; gcc/clang reject the implicit `int` per C99)
- [✓] §6.9.1p6 — An identifier declared as a typedef name shall not be redeclared as a parameter. — mcc:✓ gcc:✓ clang:✓ (`typedef int T; int f(T) int T; {…}` rejected by all three — the identifier-list slot `T` is a type name, parse error)
- [✓] §6.9.1p6 — The declarations in the declaration list shall contain no storage-class specifier other than `register`, and no initializers. — mcc:✓ gcc:✓ clang:✓ (`static int x` in decl-list rejected; initializer `int x = 5;` in decl-list rejected; `register int x;` accepted)

Semantics:
- §6.9.1p7 — The declarator in a function definition specifies the name of the function being defined and the identifiers of its parameters. — (spec; not separately testable)
- [✓] §6.9.1p7 — If the declarator includes a parameter type list, the list also specifies the types of all the parameters; such a declarator also serves as a function prototype for later calls to the same function in the same translation unit. — mcc:✓ gcc:✓ clang:✓ (after `int f(int a,int b){…}`, the call `f(1)` is rejected "too few arguments" by all three)
- [✓] §6.9.1p7 — If the declarator includes an identifier list, the types of the parameters shall be declared in a following declaration list. — mcc:✓ gcc:✓ clang:✓ (covered by the identifier-list/decl-list probes above)
- [✓] §6.9.1p7 — In either case, the type of each parameter is adjusted as described in 6.7.6.3 (C99: 6.7.5.3) for a parameter type list, and the resulting type shall be a complete object type. — mcc:✓ gcc:✓ clang:✓ (`struct S; int f(struct S s){…}` — mcc rejects an incomplete parameter type in a definition ("parameter 's' has incomplete type"), as do gcc/clang ("parameter has incomplete type"))
- §6.9.1p8 — If a function that accepts a variable number of arguments is defined without a parameter type list that ends with the ellipsis notation, the behavior is undefined. — (spec/UB; not separately testable)
- [✓] §6.9.1p9 — Each parameter has automatic storage duration; its identifier is an lvalue. *(Footnote: a parameter is in effect declared at the head of the compound statement that constitutes the function body.)* — mcc:✓ gcc:✓ clang:✓ (parameter is a modifiable lvalue: `int f(int x){ x = 5; return x; }` accepted; redeclaring the parameter at the top block level `int f(int x){ int x; … }` rejected "redeclaration of 'x'" by all three)
- [✓] §6.9.1p10 — On entry to the function, the size expressions of each variably modified parameter are evaluated. — mcc:✓ gcc:✓ clang:✓ (VLA parameter `int f(int n, int a[n]){…}` accepted by all three)
- [ ] §6.9.1p10 — On entry to the function, the value of each argument expression is converted to the type of the corresponding parameter as if by assignment. — (spec; runtime semantics — exercised indirectly by call/return tests, not separately diagnosed)
- §6.9.1p10 — A function may change the values of its parameters, but these changes cannot affect the values of the arguments (parameters are passed by value). — (spec; not separately testable)
- [✓] §6.9.1p10 — A parameter declared to have array or function type is adjusted to have, respectively, a pointer-to-element or pointer-to-function type as described in 6.7.6.3 (C99: 6.7.5.3). — mcc:✓ gcc:✓ clang:✓ (array/function parameters compile and behave as the adjusted pointer types)
- §6.9.1p11 — After all parameters have been assigned, the compound statement that constitutes the body of the function definition is executed. — (spec; not separately testable)
- [~] §6.9.1p12 — If the `}` that terminates a function is reached, and the value of the function call is used by the caller, the behavior is undefined. — mcc:~ gcc:~ clang:~ (UB, not a constraint; `int f(void){ } … return f();` compiles on all three — mcc warns "might return no value", clang warns "non-void function does not return a value", gcc is silent at default warning level)

### §6.9.2 External object definitions

Semantics:
- [✓] §6.9.2p1 — If the declaration of an identifier for an object has file scope and an initializer, the declaration is an external definition for the identifier. — mcc:✓ gcc:✓ clang:✓ (an initialized file-scope object plus tentative defs of the same name link to one definition; two *initialized* file-scope defs of the same name are rejected "redefinition")
- [✓] §6.9.2p2 — A declaration of an identifier for an object that has file scope without an initializer, and without a storage-class specifier or with the storage-class specifier `static`, constitutes a *tentative definition*. — mcc:✓ gcc:✓ clang:✓ (`int x; int x; int x = 5;` — repeated tentative defs plus one real def accepted by all three)
- [✓] §6.9.2p2 — If a translation unit contains one or more tentative definitions for an identifier, and the translation unit contains no external definition for that identifier, then the behavior is exactly as if the translation unit contains a file scope declaration of that identifier, with the composite type as of the end of the translation unit, with an initializer equal to `0`. — mcc:✓ gcc:✓ clang:✓ (tentative-only objects are emitted as zero-initialized definitions and link/run correctly)
- [✓] §6.9.2p3 — If the declaration of an identifier for an object is a tentative definition and has internal linkage, the declared type shall not be an incomplete type. — mcc:✓ gcc:~ clang:~ (incomplete *struct* `static struct S s;` rejected by all three; for an incomplete *array* `static int arr[];` that is never completed, mcc emits an "array 'arr' assumed to have one element" warning, as do gcc/clang)
- [✓] §6.9.2p2 (EXAMPLE 2, illustrative) — A tentatively-defined object whose declared type is an incomplete array (e.g. `int i[];` with external linkage) that is still incomplete at end of translation unit is completed by the implicit `= 0` initializer to a one-element array. — mcc:✓ gcc:✓ clang:✓ (`int i[];` accepted by all three; gcc/clang add a "assumed to have one element" warning, mcc accepts silently — both are conforming, the warning is optional)

### Added gaps and edge cases (not in the consolidated draft)

- [✗] §6.9.1p6 / §6.7.2p2 (C99 removed implicit `int`) — An undeclared old-style identifier-list parameter (`int f(x){ return x; }`) must be diagnosed: C99/C11 removed implicit `int`. — mcc:✗ gcc:✓ clang:✓ (**gap**: mcc accepts with a "type of 'x' defaults to 'int'" warning; gcc and clang reject — clang: "ISO C99 and later do not support implicit int")
- [✓] §6.9.2p3 (incomplete-array internal-linkage tentative def, never completed) — `static int arr[];` with no later completion is a constraint violation (internal linkage + incomplete type). — mcc:✓ gcc:~ clang:~ (mcc warns "array 'arr' assumed to have one element" before completing it to a one-element array, as do gcc/clang ("array assumed to have one element"))
- [✓] §6.9.1p2 — A function defined with no storage class, `extern`, or `static` keyword but with the function type supplied entirely by a typedef in the declaration-specifiers is ill-formed (the declarator must specify the function type). — mcc:✓ gcc:✓ clang:✓ (same root cause as the p2 item above; mcc rejects `FT g { … }`, matching gcc/clang)
- [✓] §6.9.1p3 — A definition returning a function type (e.g. via typedef) must be rejected (return type must be a complete *object* type or `void`). — mcc:✓ gcc:✓ clang:✓ (the typedef-function-type construct that would express this is itself rejected at the syntax level by all three)
- [~] §6.9p1 / §6.7 (implicit-`int` external declaration) — A file-scope declaration with no type specifier (e.g. `x;` or `f(){…}` with no return type) relies on implicit `int`, removed in C99. — mcc:~ gcc:✓ clang:✓ (probe `int f(){return 0;}` with empty K&R parens compiles everywhere; bare implicit-`int` object declarations are accepted by mcc with a warning where C99 requires a diagnostic — borderline, mcc's default leans permissive)
- [✓] §6.9.1p5 — Omitting a parameter name in a prototype-form definition (`int f(int){…}`) is a C99/C11 constraint violation but a C23 feature; a strictly-C11 compiler should diagnose it. — mcc:✓ gcc:~ clang:~ (mcc correctly rejects under C99/C11 semantics; gcc/clang accept it as a forward-looking C23 extension, only noting `-Wc23-extensions`)
- [✓] §6.9p5 (link-time, missing external definition) — A used external-linkage identifier with no definition anywhere in the program must fail to link. — mcc:✓ gcc:✓ clang:✓ (all three produce "unresolved reference"/"undefined reference" errors)
- [✓] §6.9p5 (link-time, multiple external definitions) — More than one external definition of the same external-linkage object across the program must be diagnosed. — mcc:✓ gcc:✓ clang:✓ (mcc reports "defined twice" at compile/link; gcc/clang report "multiple definition" from the linker)


## §6.10.1-§6.10.3 Conditional inclusion, source inclusion, macro replacement

> mcc status: 0 mcc-gaps — full conformance with the gcc==clang consensus. mcc now diagnoses the `#`-must-be-followed-by-a-parameter constraint (§6.10.3.2p1) at macro *definition* time, not only on invocation, matching gcc/clang. `defined`, `__has_include`, full `#if` arithmetic, redefinition rules, `#`/`##`, rescanning/blue-paint, variadic macros, placemarkers, and computed `#include` all match the consensus. (One consensus-split, not an mcc gap: `__VA_ARGS__` in a non-variadic macro replacement list is warned by mcc and gcc but accepted silently by clang, §6.10.3p5.)

### §6.10 General (directive structure)

- §6.10p1 — Recognize the directive grammar: a group is a sequence of if-sections, control-lines, text-lines and non-directives; each preprocessing directive is introduced by a `#`. — (spec; grammar — not separately testable)
- [✓] §6.10p2 — Begin a directive at a `#` that is the first token on a logical line (optionally preceded by white space with no embedded new-line), at the start of translation phase 4. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10p2 — End the directive at the first new-line after the introducing `#` (line is the logical line, i.e. after backslash-newline splicing). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10p2 — Allow white space (including comments) between the `#` and the directive name and between tokens of a directive; the `#` and name need not be adjacent. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10p2 — Accept leading white space before the `#` (an indented directive is still a directive). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10p3 — Treat a line consisting of `#` alone (followed by new-line) as a null directive with no effect. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10p? — Reject an unrecognized directive name (`#frobnicate`) with a diagnostic. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10p3 — Support conditional nesting: an if-section may contain nested if-sections in its groups. — mcc:✓ gcc:✓ clang:✓

### §6.10.1 Conditional inclusion

- [✓] §6.10.1p1 — Constrain the `#if`/`#elif` controlling expression to an integer constant expression, additionally permitting the `defined` unary operator; reject floating constants. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.1p1 — Forbid casts, `sizeof`, `_Alignof`, and enumeration constants in the controlling expression (it is evaluated purely as preprocessing arithmetic). — mcc:✓ gcc:✓ clang:✓ (no enum/sizeof/cast tokens survive macro/identifier→0 reduction)
- [✓] §6.10.1p1 — Diagnose a constraint violation such as division (or remainder) by zero in the controlling expression. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.1p1 — Support the `defined IDENT` and `defined ( IDENT )` forms, yielding 1 if IDENT is a currently-defined macro name, else 0. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.1p1 (C11) — Support the `defined` operator on macro names; `__has_include(<h>)`/`__has_include("q")` is a later (C23/extension) facility but is provided and evaluates to 1/0 by header availability; guard it with `defined __has_include`. — mcc:✓ gcc:✓ clang:✓ (mcc, gcc and clang all answer correctly for present and absent headers)
- [✓] §6.10.1p1 — Leave behavior undefined when the `defined` token is produced by macro expansion rather than written literally; do not crash. — mcc:✓ gcc:✓ clang:✓ (UB; mcc & gcc accept silently, clang warns `-Wexpansion-to-defined`; all yield the same value)
- [✓] §6.10.1p4 — Process the controlling expression in order: handle `defined` operators, then macro-expand the remaining tokens, then replace every still-remaining identifier (including ones spelled like keywords) with the pp-number `0`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.1p4 — A macro in the controlling expression that expands to another bare identifier is expanded first and only the final residual identifier becomes `0`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.1p4 — Evaluate the reduced expression using the ranges of `intmax_t`/`uintmax_t` (greatest-width signed/unsigned integer arithmetic). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.1p4 — Evaluate character constants in the controlling expression as integers; whether they match the execution character set is implementation-defined. — mcc:✓ gcc:✓ clang:✓ (`'A'==65`, `'A'<'B'` all true)
- [✓] §6.10.1p2 — `#ifdef IDENT` includes its group iff IDENT is a defined macro; `#ifndef IDENT` iff it is not (equivalent to `#if defined`/`#if !defined`). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.1p2 — Diagnose a missing identifier after `#ifdef`/`#ifndef`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.1p5 — Evaluate `#if`/`#elif` conditions top to bottom; include the first group whose condition is nonzero (or the `#else` group if none), and skip the rest. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.1p6 — In skipped groups, ignore the tokens except to recognize directive names for correct nesting/balancing of conditionals (do not evaluate `#elif` expressions of an already-satisfied section). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.1p? — Each if-section is terminated by exactly one `#endif`; diagnose `#elif`/`#else`/`#endif` with no matching `#if`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.1p? — Diagnose a second `#else` in one if-section, and an `#elif` following an `#else`. — mcc:✓ gcc:✓ clang:✓ (mcc & gcc & clang all reject `#else` after `#else`)
- [✓] §6.10.1p? — Diagnose an unterminated conditional (`#if` with no matching `#endif` before end of file). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.1p? — Warn on extra tokens after `#else`/`#endif` (grammar permits none); a warning is a sufficient diagnostic. — mcc:✓ gcc:✓ clang:✓ (all three warn, none reject)

### §6.10.2 Source file inclusion

- [✓] §6.10.2p1 — Require a `#include` directive to name a header or source file the implementation can process; diagnose a malformed form. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.2p2 — `#include <h-char-sequence>` searches an implementation-defined sequence of places for a header so identified. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.2p3 — `#include "q-char-sequence"` searches for a source file in an implementation-defined manner, and on failure re-searches as if `<q-char-sequence>`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.2p4 — Third (computed) form: macro-expand the tokens after `#include`; the result must match either the `<...>` or the `"..."` form, with implementation-defined token-combination into a single header name. — mcc:✓ gcc:✓ clang:✓ (`#define HDR <stdio.h>` then `#include HDR` works)
- [✓] §6.10.2p4 — Diagnose when the computed `#include` tokens do not reduce to a valid `<...>`/`"..."` header-name. — mcc:✓ gcc:✓ clang:✓
- §6.10.2p5 — Inside a `<...>` sequence a `'`, `"`, `\`, `//` or `/*` yields undefined behavior; inside a `"..."` sequence a `'`, `\`, `//` or `/*` does too. — (spec; UB — not separately testable)
- §6.10.2p6 — Permit `#include` of a file that was itself reached via `#include`, up to an implementation-defined nesting limit (≥15 per §5.2.4.1). — (spec/translation-limit; not separately testable)

### §6.10.3 Macro replacement (general)

- [✓] §6.10.3p1 — Permit identical (benign) redefinition of an object-like macro: same replacement-list spelling and same white-space separation between tokens. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.3p1 — Permit identical redefinition of a function-like macro: same parameter spelling, same replacement list, same white-space separation. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.3p1 — Diagnose a non-identical redefinition (different tokens or different presence of internal white space) of an existing macro. — mcc:✓ gcc:✓ clang:✓ (all three emit a warning, which satisfies the constraint)
- [✓] §6.10.3p1 — Diagnose redefining an object-like macro as function-like (or vice-versa) — they are never identical. — mcc:✓ gcc:✓ clang:✓ (warning)
- [✓] §6.10.3p2 — Treat replacement lists as identical only when both have the same tokens and the same internal white-space *presence* (amount of white space is irrelevant). — mcc:✓ gcc:✓ clang:✓ (`x+y` vs `x + y` flagged as a redefinition)
- [✓] §6.10.3p? — Require the parameters in a function-like macro to be distinct; diagnose a duplicate parameter name. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.3p3 — Require white space between the macro name and the replacement list of an object-like macro; a `(` *immediately* after the name (no space) makes it function-like instead. — mcc:✓ gcc:✓ clang:✓ (`#define OBJ (x) y` is object-like; `#define FN(x) ...` is function-like)
- [~] §6.10.3p5 (C99) — Allow `__VA_ARGS__` only in the replacement list of a `...`-variadic macro; diagnose its use elsewhere. — mcc:~ gcc:~ clang:✗ (mcc & gcc warn; clang accepts `__VA_ARGS__` in a non-variadic macro replacement list silently)
- [✓] §6.10.3p? — Reject `defined` as a macro name (`#define`/`#undef`); it is reserved for conditional inclusion. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.3p? — `__VA_ARGS__` may not be created or removed by `#define`/`#undef`. — mcc:✓ gcc:~ clang:~ (mcc errors; gcc/clang only warn but do not actually redefine it — all three refuse the redefinition effect)
- [✓] §6.10.3p9 — Object-like macro: each occurrence of the name in following text is replaced by its replacement list. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.3p10 — Function-like macro: only invoke when the name is followed by a `(`; the `(` may be separated by white space/new-lines. Treat the name alone (no `(`) as an ordinary identifier. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.3p10 — Allow a `(` for the invocation to be supplied by another macro expansion / span lines; collect arguments up to the matching `)`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.3p11 — Require the number of arguments to match the number of parameters; diagnose too few and too many. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.3p11 — Commas inside matched inner parentheses do not separate arguments. — mcc:✓ gcc:✓ clang:✓
- §6.10.3p11 — Where the macro arguments contain pp-tokens that look like directives, behavior is undefined. — (spec; UB — not separately testable)
- [✓] §6.10.3p12/§6.10.3.1 — Variadic macro: a trailing `...` parameter collects the remaining arguments (with their separating commas) into `__VA_ARGS__`; accept zero trailing arguments as the common extension. — mcc:✓ gcc:✓ clang:✓ (`DBG0()` → `f()` on all three with no `-pedantic`)

### §6.10.3.1 Argument substitution

- [✓] §6.10.3.1p1 — Fully macro-expand each argument (as if it were the rest of the file, with no other tokens available) before substituting it for the corresponding parameter in the replacement list. — mcc:✓ gcc:✓ clang:✓ (`INC(V)`→`((10)+1)`)
- [✓] §6.10.3.1p1 — Do NOT pre-expand an argument that is substituted for a parameter immediately preceded by `#` or `##`, or immediately followed by `##`; use the raw (unexpanded) argument there. — mcc:✓ gcc:✓ clang:✓ (`#V`→`"V"`, `V ## _suffix`→`V_suffix`)
- [✓] §6.10.3.1p2 (C99) — Treat `__VA_ARGS__` in the replacement list as a parameter standing for the variable arguments for all substitution purposes. — mcc:✓ gcc:✓ clang:✓

### §6.10.3.2 The `#` (stringize) operator

- [✓] §6.10.3.2p1 — Constraint: in a function-like macro each `#` must be immediately followed by a parameter name. — mcc:✓ gcc:✓ clang:✓ (mcc rejects the bad `#define` at definition — "'#' is not followed by a macro parameter" — even if never invoked, matching gcc/clang)
- [✓] §6.10.3.2p2 — Replace `# parameter` by a single string-literal whose contents are the spelling of the (unexpanded) argument's tokens. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.3.2p2 — In the stringized result, collapse white space between tokens to one space and delete leading/trailing white space. — mcc:✓ gcc:✓ clang:✓ (`STR(hello   world)`→`"hello world"`, leading spaces dropped)
- [✓] §6.10.3.2p2 — Preserve the original spelling of each token, and insert a `\` before every `"` and `\` that occurs inside a string/character literal of the argument. — mcc:✓ gcc:✓ clang:✓ (`STR("a\b")`→`"\"a\\b\""`)
- §6.10.3.2p2 — If the stringization does not form a valid string literal, behavior is undefined. — (spec; UB — not separately testable)
- §6.10.3.2p2 — When both `#` and `##` apply, the order of their evaluation is unspecified. — (spec; unspecified order — not separately testable)

### §6.10.3.3 The `##` (token paste) operator

- [✓] §6.10.3.3p1 — Constraint: `##` may not be the first or last token of a replacement list (object-like or function-like); diagnose both. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.3.3p2 — For a parameter adjacent to `##`, substitute the raw (unexpanded) argument; if that argument is empty, treat it as a placemarker. — mcc:✓ gcc:✓ clang:✓ (`J(,foo)`→`foo`, `J(foo,)`→`foo`, `J(,)`→empty)
- [✓] §6.10.3.3p3 — Delete each `##` and concatenate the two adjacent tokens into one new preprocessing token. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.3.3p3 — Concatenation with a placemarker yields the other operand (placemarker disappears); two placemarkers yield a placemarker. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.3.3p3 — Diagnose (UB, but must not silently mis-tokenize) when the concatenation does not form a valid single preprocessing token. — mcc:✓ gcc:✓ clang:✓ (all three diagnose `+ ## /`)
- [✓] §6.10.3.3p3 — Re-examine the newly pasted token for further macro replacement (it participates in rescanning). — mcc:✓ gcc:✓ clang:✓ (`XCAT(v,N)` with `N`→`1` yields `v1` via the indirection layer)
- §6.10.3.3p3 — With several `##` operators in one replacement list, the order of the individual pastes is unspecified. — (spec; unspecified order — not separately testable)

### §6.10.3.4 Rescanning and further replacement

- [✓] §6.10.3.4p1 — After substitution, rescan the resulting replacement list together with the subsequent source tokens for more macro names to replace. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.3.4p1 — Allow a macro expansion to consume following source tokens to form a new (possibly function-like) invocation during rescanning. — mcc:✓ gcc:✓ clang:✓ (`f(2)(9)` pattern via `#define f(a) a*g` / `#define g(a) f(a)` → `2*9*g`)
- [✓] §6.10.3.4p2 — Suppress recursion: if the name of the macro currently being replaced appears in the rescan (directly or via nested replacement), do not replace it again. — mcc:✓ gcc:✓ clang:✓ (`#define M M` → `M`; mutually-referential `A`/`B` reach a fixed point)
- [✓] §6.10.3.4p2 — Mark such a non-replaced macro name so it is never replaced even if examined again in a later nested context (the "blue paint" is permanent). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.3.4p3 — The token sequence produced by macro replacement is never reinterpreted as a preprocessing directive, even if it begins with `#`. — mcc:✓ gcc:✓ clang:✓ (no probe needed; consistent with rescan results)

### §6.10.3.5 Scope of macro definitions

- [✓] §6.10.3.5p1 — A macro definition is in effect from its `#define` until a matching `#undef` or, absent that, to the end of the translation unit. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.3.5p1 — `#undef NAME` removes the macro definition for NAME; it is not an error if NAME is not currently a defined macro. — mcc:✓ gcc:✓ clang:✓
- §6.10.3.5p2+ — Worked examples (object-like, function-like, `#`/`##`, rescanning, variadic) illustrate the above rules. — (spec; examples — not separately testable)


## §6.10.4-§6.10.9 Line/error/pragma/null/predefined macros/_Pragma

> mcc status: Broadly conformant. #line (number, filename, range constraint, macro-expanded form), #error, the STDC pragmas (incl. on/off-switch validation), unknown-pragma ignore, the null directive, and the full mandatory/environment predefined-macro set all behave like the gcc==clang consensus. Conditional-feature NO_* macros are correctly absent (atomics/complex/threads/VLA all supported). mcc now diagnoses a non-string-literal `_Pragma` operand, matching gcc/clang, leaving 0 mcc-gaps in this section. Minor cosmetic: `#pragma message` diagnostics print a synthetic `:pragma:` filename label.

### §6.10.4 Line control

- [✓] §6.10.4p1 — Constrain the `#line` argument (after any macro expansion) to a decimal digit sequence in the range 1 to 2147483647; reject 0 and values exceeding the max. — mcc:✓ gcc:✓ clang:✓ (mcc warns "line number out of range" for 0 and for 2147483648, matching gcc -pedantic; clang treats them as extensions)
- [✓] §6.10.4p2 — `#line digit-sequence`: set the presumed source line number of the immediately following line to that value (so `__LINE__` on the next line yields it). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.4p3 — `#line digit-sequence "s-char-seq"`: set both the presumed line number and the presumed source file name (`__FILE__`) reported thereafter. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.4p4 — When the directive does not directly match a number(+optional string) form, macro-expand the trailing tokens first, then require the result to match one of the two forms (otherwise undefined). — mcc:✓ gcc:✓ clang:✓ (macro expanding to both `42 "z.c"` and to a lone number both work)
- [✓] §6.10.4p4 — Reject a `#line` argument that is not a positive integer (e.g. an identifier) with a diagnostic. — mcc:✓ gcc:✓ clang:✓ (mcc: "wrong #line format")
- [✓] §6.10.4 — The presumed file/line set by `#line` must also drive subsequent compiler diagnostics' location, not just `__FILE__`/`__LINE__`. — mcc:✓ gcc:✓ clang:✓ (error after `#line 500 "FAKE.c"` reports `FAKE.c:500`)

### §6.10.5 Error directive

- [✓] §6.10.5p1 — `#error` must emit a diagnostic that includes the directive's remaining preprocessing tokens and render the program ill-formed (halt this translation unit). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.5p1 — `#error` with no following tokens is still a valid error directive producing a diagnostic. — mcc:✓ gcc:✓ clang:✓

### §6.10.6 Pragma directive

- [✓] §6.10.6p1 — A `#pragma` whose pragma name is unrecognized must be handled in an implementation-defined way that does not break conformance (mcc/gcc/clang silently ignore it). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.6p1 — When `STDC` immediately follows `pragma`, do not macro-expand the directive's tokens; process them as a standard pragma instead. — mcc:✓ gcc:✓ clang:✓ (STDC pragmas accepted verbatim)
- [✓] §6.10.6p2 — Recognize `#pragma STDC FP_CONTRACT on-off-switch` (ON/OFF/DEFAULT). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.6p2 — Recognize `#pragma STDC FENV_ACCESS on-off-switch`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.6p2 — Recognize `#pragma STDC CX_LIMITED_RANGE on-off-switch`. — mcc:✓ gcc:✓ clang:✓
- [~] §6.10.6p2 — The on-off-switch operand of an STDC pragma must be exactly `ON`, `OFF`, or `DEFAULT`; anything else is malformed. — mcc:~ gcc:~ clang:~ (all three warn rather than hard-error; mcc: "malformed #pragma STDC FP_CONTRACT (expected ON/OFF/DEFAULT)", gcc/clang similar — consensus warning-only)
- [ ] §6.10.6p2 — STDC pragmas are permitted only outside external declarations or at the head of a compound statement before any statement/declaration; honor that placement scoping. — (spec; placement/scope rule — accepted at file scope by all three; not separately exercised here)

### §6.10.7 Null directive

- [✓] §6.10.7p1 — A `#` line containing only optional white space (the null directive) has no effect. — mcc:✓ gcc:✓ clang:✓

### §6.10.8 Predefined macros

- [ ] §6.10.8p1 — None of the predefined macro names, nor the identifier `defined`, may be the target of `#define` or `#undef`. — (spec; redefine/undef prohibition — not separately probed in this part)

#### §6.10.8.1 Mandatory macros

- [✓] §6.10.8.1 — `__DATE__`: the translation date as a string literal of the form `"Mmm dd yyyy"` (asctime-style, day space-padded). — mcc:✓ gcc:✓ clang:✓ (`"Jun 30 2026"`)
- [✓] §6.10.8.1 — `__TIME__`: the translation time as `"hh:mm:ss"`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.8.1 — `__FILE__`: a string literal giving the presumed current source file name. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.8.1 — `__LINE__`: the presumed current source line number as a decimal constant. — mcc:✓ gcc:✓ clang:✓ (also expands correctly when reached through a macro body)
- [✓] §6.10.8.1 — `__STDC__`: the integer constant `1` (conforming implementation). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.8.1 — `__STDC_HOSTED__`: `1` for a hosted implementation, `0` for freestanding. — mcc:✓ gcc:✓ clang:✓ (hosted default = 1)
- [✓] §6.10.8.1 — `__STDC_VERSION__`: `199901L` (C99), `201112L` (C11). — mcc:✓ gcc:✓ clang:✓ (mcc tracks `-std=c99`→199901L, `-std=c11`→201112L, `-std=c17`→201710L; default mode differs across vendors — mcc defaults C99, gcc C23, clang C17 — all conforming choices)

#### §6.10.8.2 Environment macros

- [✓] §6.10.8.2 — `__STDC_ISO_10646__`: defined as a `yyyymmL` constant when `wchar_t` holds Unicode/ISO 10646 code points. — mcc:✓ gcc:✓ clang:✓ (`201706L` on this Linux target)
- [✓] §6.10.8.2 — `__STDC_MB_MIGHT_NEQ_WC__`: defined as `1` only if a basic-character member can have different code values as a multibyte char vs. a wide char; otherwise undefined. — mcc:✓ gcc:✓ clang:✓ (correctly undefined on this platform)
- [✓] §6.10.8.2 (C11) — `__STDC_UTF_16__`: `1` when `char16_t` values are UTF-16 encoded. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.8.2 (C11) — `__STDC_UTF_32__`: `1` when `char32_t` values are UTF-32 encoded. — mcc:✓ gcc:✓ clang:✓

#### §6.10.8.3 Conditional feature macros (C11)

- [✓] §6.10.8.3 (C11) — `__STDC_ANALYZABLE__`: define (as `yyyymmL`) iff the implementation conforms to the Annex L analyzability spec. — mcc:✓ gcc:✓ clang:✓ (correctly undefined; Annex L not implemented by any)
- [✓] §6.10.8.3 (C11) — `__STDC_LIB_EXT1__`: define (as `yyyymmL`) iff the Annex K bounds-checking interfaces are provided. — mcc:✓ gcc:✓ clang:✓ (correctly undefined; Annex K not provided)
- [✓] §6.10.8.3 (C11) — `__STDC_NO_ATOMICS__`: define as `1` iff `_Atomic` and `<stdatomic.h>` are NOT supported. — mcc:✓ gcc:✓ clang:✓ (atomics supported → correctly undefined; `<stdatomic.h>` compiles)
- [✓] §6.10.8.3 (C11) — `__STDC_NO_COMPLEX__`: define as `1` iff `_Complex`/`<complex.h>` are NOT supported. — mcc:✓ gcc:✓ clang:✓ (complex supported → correctly undefined)
- [✓] §6.10.8.3 (C11) — `__STDC_NO_THREADS__`: define as `1` iff `<threads.h>` is NOT supported. — mcc:✓ gcc:✓ clang:✓ (`<threads.h>` compiles → correctly undefined)
- [✓] §6.10.8.3 (C11) — `__STDC_NO_VLA__`: define as `1` iff variable-length arrays are NOT supported. — mcc:✓ gcc:✓ clang:✓ (VLAs supported → correctly undefined)

### §6.10.9 _Pragma operator (C99/C11)

- [✓] §6.10.9p1 — `_Pragma ( string-literal )` constraint: the operand must be a parenthesized string literal. — mcc:✓ gcc:✓ clang:✓ (mcc rejects `_Pragma(foo)` with a non-string-literal operand — "_Pragma takes a parenthesized string literal" — matching gcc/clang, which both error "`_Pragma` takes a parenthesized string literal")
- [✓] §6.10.9p1 — Destringize the operand: drop an `L`/`u`/`U` prefix if present, strip the surrounding `"`, then replace `\"`→`"` and `\\`→`\`. — mcc:✓ gcc:✓ clang:✓ (`_Pragma("message(\"hi\")")` yields a `#pragma message: hi` note)
- [✓] §6.10.9p1 — Re-tokenize the destringized result and process it as if it were a `#pragma` directive on its own line. — mcc:✓ gcc:✓ clang:✓ (`_Pragma("STDC FP_CONTRACT OFF")` honored; works the same when emitted from a macro body via `_Pragma(#x)`)
- [✓] §6.10.9p1 — An unrecognized pragma delivered through `_Pragma` is ignored just like an unrecognized `#pragma`. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.10.9 — `_Pragma` works when generated by macro expansion (the canonical `DO_PRAGMA(x) _Pragma(#x)` idiom), unlike `#pragma` which cannot be produced by macro replacement. — mcc:✓ gcc:✓ clang:✓


## §7.1-§7.5 Library intro, assert, complex, ctype, errno

> mcc status: 119 TODOs; 1 mcc-gap. The only divergence found is that `static_assert` from `<assert.h>` (C11) is unavailable in mcc's **default** mode — mcc defaults to `__STDC_VERSION__ == 199901L` (C99), and glibc's `<assert.h>` only defines the `static_assert` macro when `__STDC_VERSION__ >= 201112L`; gcc/clang default to C23/C17 so they provide it out of the box. `mcc -std=c11` fixes it. All `<complex.h>` macros/functions, the `CX_LIMITED_RANGE` pragma, `CMPLX`, all 29 standard headers, `<ctype.h>` (incl. C99 `isblank`), and `<errno.h>` (incl. C99 `EILSEQ`, C11 thread-local `errno`) conform on mcc.

### §7.1.1 Definitions of terms
- §7.1.1p1 — A *string* is a contiguous sequence of characters terminated by and including the first null character; a *pointer to a string* points to its initial (lowest addressed) character. — (spec; not separately testable)
- §7.1.1p1 — The *length* of a string is the number of bytes preceding the null character. — (spec; not separately testable)
- §7.1.1p1 — The *value* of a string is the sequence of the values of the contained characters, in order. — (spec; not separately testable)
- §7.1.1p1 — The term *multibyte character* sometimes includes single-byte characters, as a generalization of the term used in 5.2.1.2 ("the decimal-point character notwithstanding"). — (spec; not separately testable)
- §7.1.1p1 — The *decimal-point character* is the character used by functions that convert floating-point numbers to or from character sequences to denote the beginning of the fractional part; represented in the text and examples by a period, but it may be changed by the `setlocale` function. — (spec; not separately testable)

### §7.1.2 Standard headers
> C11 adds five headers (`<stdalign.h>`, `<stdatomic.h>`, `<stdnoreturn.h>`, `<threads.h>`, `<uchar.h>`) and the §7.1.2p5 prohibition on macros whose names match keywords; C99/C90 have neither.
- [✓] §7.1.2p1 — Each library function's declarations and type and macro definitions are contained in a *header*, whose contents are made available by the `#include` preprocessing directive. — mcc:✓ gcc:✓ clang:✓
- §7.1.2p1 — A header declares a set of related functions plus any necessary types and additional macros needed to facilitate their use. — (spec; not separately testable)
- §7.1.2p1 — Declarations of types described in a clause are valid only within the scope established by the header that contains them (where the clause so states). — (spec; not separately testable)
- [✓] §7.1.2p2 — The C99 standard headers are exactly the 24: `<assert.h>`, `<complex.h>`, `<ctype.h>`, `<errno.h>`, `<fenv.h>`, `<float.h>`, `<inttypes.h>`, `<iso646.h>`, `<limits.h>`, `<locale.h>`, `<math.h>`, `<setjmp.h>`, `<signal.h>`, `<stdarg.h>`, `<stdbool.h>`, `<stddef.h>`, `<stdint.h>`, `<stdio.h>`, `<stdlib.h>`, `<string.h>`, `<tgmath.h>`, `<time.h>`, `<wchar.h>`, `<wctype.h>`. — mcc:✓ gcc:✓ clang:✓ (all 24 include cleanly; mcc bundles 10 in runtime/include and defers the rest to the system libc)
- [✓] §7.1.2p2 — (C11) The standard headers additionally include `<stdalign.h>`, `<stdatomic.h>`, `<stdnoreturn.h>`, `<threads.h>`, and `<uchar.h>` (29 total). — mcc:✓ gcc:✓ clang:✓ (all five bundled in mcc's runtime/include and includable)
- §7.1.2p3 — If a file with the same name as one of the standard headers, not provided as part of the implementation, is placed where a header search would find it, the behavior is undefined. — (spec/UB; not separately testable)
- [✓] §7.1.2p4 — Standard headers may be included in any order, and each may be included more than once with no effect different from being included only once — except that the effect of including `<assert.h>` depends on the definition of `NDEBUG`. — mcc:✓ gcc:✓ clang:✓ (re-inclusion idempotent; assert re-evaluates NDEBUG)
- §7.1.2p5 — If used, a header shall be included outside of any external declaration or definition, and it shall be included lexically before the first reference to any of the functions or objects it declares, or to any of the types or macros it defines. — (spec/constraint on the program; not separately testable)
- §7.1.2p5 — However, if an identifier is declared or defined in more than one header, the second and subsequent associated headers may be included after the initial reference to the identifier. — (spec; not separately testable)
- §7.1.2p5 — (C11) The program shall not have any macros with names lexically identical to keywords currently defined prior to the inclusion of the header or when any macro defined in the header is expanded. — (spec/constraint on the program; not separately testable)

### §7.1.3 Reserved identifiers
- §7.1.3p1 — Each header declares or defines all the identifiers listed in its associated subclause, and optionally declares or defines identifiers listed in its associated future library directions subclause and identifiers that are always reserved either for any use or for use as file scope identifiers. — (spec; not separately testable)
- §7.1.3p1 — All identifiers that begin with an underscore and either an uppercase letter or another underscore are always reserved for any use. — (spec; not separately testable)
- §7.1.3p1 — All identifiers that begin with an underscore are always reserved for use as identifiers with file scope in both the ordinary and tag name spaces. — (spec; not separately testable)
- §7.1.3p1 — Each macro name in any of the following subclauses (including the future library directions) is reserved for use as specified if any of its associated headers is included, unless explicitly stated otherwise. — (spec; not separately testable)
- §7.1.3p1 — All identifiers with external linkage in any of the following subclauses (including the future library directions) and `errno` are always reserved for use as identifiers with external linkage. — (spec; not separately testable)
- §7.1.3p1 — Each identifier with file scope listed in any of the following subclauses (including the future library directions) is reserved for use as a macro name and as an identifier with file scope in the same name space if any of its associated headers is included. — (spec; not separately testable)
- §7.1.3p2 — No other identifiers are reserved. — (spec; not separately testable)
- §7.1.3p2 — If the program declares or defines an identifier in a context in which it is reserved (other than as allowed by 7.1.4), or defines a reserved identifier as a macro name, the behavior is undefined. — (spec/UB; not separately testable)
- §7.1.3p3 — If the program removes (with `#undef`) any macro definition of an identifier in the first group listed (the potentially reserved identifiers), the behavior is undefined. — (spec/UB; not separately testable)

### §7.1.4 Use of library functions
- §7.1.4p1 — Each of the §7.1.4p1 statements applies unless explicitly stated otherwise in the detailed descriptions that follow. — (spec; not separately testable)
- §7.1.4p1 — If an argument to a function has an invalid value (a value outside the function's domain, a pointer outside the address space of the program, a null pointer, or a pointer to non-modifiable storage when the corresponding parameter is not const-qualified) or a type (after default argument promotion) not expected by a function with a variable number of arguments, the behavior is undefined. — (spec/UB; not separately testable)
- [✓] §7.1.4p1 — Any function declared in a header may be additionally implemented as a function-like macro defined in the header, so a library function should not be declared explicitly if its header is included. — mcc:✓ gcc:✓ clang:✓ (e.g. `creal`/`cimag` are both prototyped functions and masking macros in mcc's complex.h)
- [✓] §7.1.4p1 — Any macro definition of a function can be suppressed locally by enclosing the function name in parentheses, because the name is then not followed by the `(` that indicates expansion of a function-like macro name. — mcc:✓ gcc:✓ clang:✓ (`(creal)(z)` and `(isalpha)('a')` call the real function on all three)
- [✓] §7.1.4p1 — For the same syntactic reason, a function declared in a header can be redeclared in the program, and the program's name can be used to take the address of the actual function even if the function is also implemented as a macro. — mcc:✓ gcc:✓ clang:✓ (`double (*p)(double complex) = creal;` and `int (*q)(int) = isalpha;` work)
- [✓] §7.1.4p1 — The use of `#undef` to remove any macro definition will also ensure that an actual function is referred to. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.1.4p1 — Any invocation of a library function that is implemented as a macro shall expand to code that evaluates each of its arguments exactly once, fully protected by parentheses where necessary, so it is generally safe to use arbitrary expressions as arguments. (Exceptions: the macros `assert`, `setjmp`, `va_arg`, `va_copy`, and `va_start` need not satisfy this requirement.) — mcc:✓ gcc:✓ clang:✓ (mcc's ctype/complex masking macros parenthesize and use their argument once)
- [✓] §7.1.4p1 — Likewise, the function-like macros described in the following subclauses may be invoked in an expression anywhere a function with a compatible return type could be called. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.1.4p1 — All object-like macros listed as expanding to integer constant expressions shall additionally be suitable for use in `#if` preprocessing directives. — mcc:✓ gcc:✓ clang:✓ (`EDOM`/`ERANGE`/`EILSEQ` usable in `#if`)
- [✓] §7.1.4p2 — Provided that a library function can be declared without reference to any type defined in a header, it is permissible to declare the function and use it without including its associated header. — mcc:✓ gcc:✓ clang:✓ (e.g. `extern int isalpha(int);` then call links fine)
- §7.1.4p3 — There is a sequence point immediately before a library function returns. — (spec; not separately testable)
- §7.1.4p4 — The functions in the standard library are not guaranteed to be reentrant and may modify objects with static or thread storage duration. (C11 adds "thread storage duration".) — (spec; not separately testable)

### §7.2 Diagnostics `<assert.h>`
> C11 makes `<assert.h>` also define `static_assert` (expanding to `_Static_assert`); C99/C90 define only `assert` (and reference `NDEBUG`). C99/C11 include `__func__` in the failure message; C90 did not.
- [✓] §7.2p1 — The header `<assert.h>` defines the `assert` macro and refers to another macro, `NDEBUG`, which is not defined by `<assert.h>`. — mcc:✓ gcc:✓ clang:✓
- [~] §7.2p1 — (C11) The header `<assert.h>` defines the macro `static_assert`, which expands to `_Static_assert`. — mcc:~ gcc:✓ clang:✓ (mcc defaults to `__STDC_VERSION__==199901L`, so glibc's `<assert.h>` does NOT define `static_assert`; gcc/clang default to C23/C17 where it is defined. mcc provides it with `-std=c11`; the `_Static_assert` keyword itself is accepted by mcc in all modes.)
- [✓] §7.2p2 — If `NDEBUG` is defined as a macro name at the point in the source file where `<assert.h>` is included, the `assert` macro is defined simply as `#define assert(ignore) ((void)0)`. — mcc:✓ gcc:✓ clang:✓ (assert(0) under NDEBUG is a no-op; program returns normally)
- [✓] §7.2p3 — The `assert` macro is redefined according to the current state of `NDEBUG` each time that `<assert.h>` is included. — mcc:✓ gcc:✓ clang:✓ (toggling NDEBUG across re-includes flips assert active/no-op)
- [✓] §7.2p4 — The `assert` macro shall be implemented as a macro, not as an actual function; if the macro definition is suppressed in order to access an actual function, the behavior is undefined. — mcc:✓ gcc:✓ clang:✓ (`assert` is a macro; `#ifdef assert` is true)

#### §7.2.1.1 The `assert` macro
- [✓] §7.2.1.1p1 — Synopsis: `#include <assert.h>` then `void assert(scalar expression);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.2.1.1p2 — The `assert` macro puts diagnostic tests into programs; it expands to a void expression. — mcc:✓ gcc:✓ clang:✓ (`(void)(assert(1),0)` compiles; assert usable in a comma expression)
- [✓] §7.2.1.1p2 — When executed, if `expression` (which shall have a scalar type) is false (compares equal to 0), the `assert` macro writes information about the particular call that failed — including the text of the argument, the name of the source file, the source line number, and the name of the enclosing function (the values of `__FILE__`, `__LINE__`, and the identifier `__func__`) — on the standard error stream in an implementation-defined format. — mcc:✓ gcc:✓ clang:✓ (mcc prints `prog: file.c:2: main: Assertion '2+2==5' failed.` — includes expr text, file, line, and `__func__`; format is implementation-defined and matches gcc's; clang uses the full prototype for the function name)
- [✓] §7.2.1.1p2 — The `assert` macro then calls the `abort` function. — mcc:✓ gcc:✓ clang:✓ (process terminates with SIGABRT, exit 134, on all three)
- [✓] §7.2.1.1p3 — The `assert` macro returns no value. — mcc:✓ gcc:✓ clang:✓

#### §7.2.2 (C11) `static_assert` / `_Static_assert`
- [~] §7.2p1 — (C11) `static_assert` is defined as a macro expanding to the keyword `_Static_assert`; it performs a compile-time assertion whose constraints and semantics are specified in §6.7.10 Static assertions. — mcc:~ gcc:✓ clang:✓ (same default-std gap as §7.2p1 above: works with `mcc -std=c11`, not by default; `_Static_assert(cond,"msg")` keyword form works in every mcc mode)

### §7.3 Complex arithmetic `<complex.h>`
> C11 adds the `CMPLX`/`CMPLXF`/`CMPLXL` macros (§7.3.9.3) and clarifies `cproj`; the `imaginary`/`_Imaginary_I` macros and imaginary types are optional (Annex G) in both C99 and C11. An implementation that defines `__STDC_NO_COMPLEX__` (C11) need not provide `<complex.h>` at all.

#### §7.3.0 `<complex.h>` provision and feature macros (added gaps)
- [✓] §7.3 — mcc provides a bundled `<complex.h>` (runtime/include/complex.h, guarded by `_MCC_COMPLEX_H`) and does NOT define `__STDC_NO_COMPLEX__`. — mcc:✓ gcc:✓ clang:✓ (none of the three define `__STDC_NO_COMPLEX__`; all provide the header)
- [✓] §6.10.8.3 / Annex F.2 — (C11) An implementation that supports IEC 60559-compatible complex arithmetic defines `__STDC_IEC_559_COMPLEX__` as `1`. — mcc:✓ gcc:✓ clang:✓ (all three predefine `__STDC_IEC_559_COMPLEX__ 1`)
- [✓] §7.3 — Including `<complex.h>` more than once has no effect different from a single inclusion (header is idempotently guarded). — mcc:✓ gcc:✓ clang:✓

#### §7.3.1 Introduction
- [✓] §7.3.1p1 — The header `<complex.h>` defines macros and declares functions that support complex arithmetic. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.3.1p2 — Each synopsis specifies a family of functions consisting of a principal function with one or more `double complex` parameters and a `double complex` or `double` return value, plus other functions with the same name suffixed by `f` and `l` that are corresponding functions with `float` and `long double` parameters and return values. — mcc:✓ gcc:✓ clang:✓ (`cabs`/`cabsf`/`cabsl` etc. all declared and link; tested `cabsf`/`cabsl`)
- [✓] §7.3.1p3 — The macro `complex` expands to `_Complex`. — mcc:✓ gcc:✓ clang:✓ (`#define complex _Complex`)
- [✓] §7.3.1p3 — The macro `_Complex_I` expands to a constant expression of type `const float _Complex`, with the value of the imaginary unit. — mcc:✓ gcc:✓ clang:✓ (mcc defines it via `__builtin_complex(0.0f,1.0f)`, usable as a static initializer of `const float _Complex`)
- [✓] §7.3.1p4 — The macros `imaginary` and `_Imaginary_I` are defined if and only if the implementation supports imaginary types (Annex G); when defined, `imaginary` expands to `_Imaginary` and `_Imaginary_I` expands to a constant expression of type `const float _Imaginary` with the value of the imaginary unit. — mcc:✓ gcc:✓ clang:✓ (none support imaginary types, so none define `imaginary`/`_Imaginary_I` — `#ifdef imaginary` is false on all three)
- [✓] §7.3.1p5 — The macro `I` expands to `_Imaginary_I` if `_Imaginary_I` is defined, otherwise to `_Complex_I`. — mcc:✓ gcc:✓ clang:✓ (`I` == `_Complex_I` since no imaginary types)
- [✓] §7.3.1p6 — Notwithstanding the provisions of 7.1.3, a program may undefine and perhaps then redefine the macros `complex`, `imaginary`, and `I`. — mcc:✓ gcc:✓ clang:✓ (`#undef complex` / `#undef I` / `#undef imaginary` then redefine all accepted)

#### §7.3.2 Conventions
- [✓] §7.3.2p1 — Function values for complex angle/argument quantities are interpreted as radians, not degrees. — mcc:✓ gcc:✓ clang:✓ (`carg(I)` == π/2 ≈ 1.5708 on all three)
- §7.3.2p1 — An implementation may set `errno` for the `<complex.h>` functions but is not required to. — (spec/permission; not separately testable)

#### §7.3.3 Branch cuts
- §7.3.3p1 — Some of the `<complex.h>` functions have branch cuts, across which the function is discontinuous. — (spec; not separately testable)
- [ ] §7.3.3p1 — For implementations with a signed zero (including all IEC 60559 implementations) that follow the specifications of the functions, the sign of zero distinguishes one side of a cut from another, so the function is continuous (except for format limitations) as the cut is approached from either side. — (spec/libm-behavioral; not a compiler property)
- [ ] §7.3.3p1 — Example: for `csqrt` (branch cut along the negative real axis), the top of the cut (imaginary part +0) maps to the positive imaginary axis and the bottom of the cut (imaginary part −0) maps to the negative imaginary axis. — (spec/libm-behavioral; not a compiler property)
- [ ] §7.3.3p2 — Implementations that do not support a signed zero (see Annex F) cannot distinguish the sides of branch cuts; they shall map a cut so the function is continuous as the cut is approached coming around the finite endpoint of the cut from either side. — (spec/libm-behavioral; not a compiler property)
- [ ] §7.3.3p2 — Example: for `csqrt`, coming counterclockwise around the finite endpoint, the cut maps to the positive imaginary axis; coming clockwise, it maps to the negative imaginary axis. — (spec/libm-behavioral; not a compiler property)
- [ ] §7.3.3p2 — Implementations that do not support a signed zero shall map values on the branch cut consistently as specified for each function. — (spec/libm-behavioral; not a compiler property)

#### §7.3.4 The `CX_LIMITED_RANGE` pragma
- [✓] §7.3.4p1 — Synopsis: `#include <complex.h>` then `#pragma STDC CX_LIMITED_RANGE on-off-switch`. — mcc:✓ gcc:✓ clang:✓ (mcc accepts the pragma with `ON`/`OFF`/`DEFAULT` without diagnostic)
- [✓] §7.3.4p2 — The pragma can be used to inform the implementation that (where the state is "on") the usual mathematical formulas for complex multiply, divide, and absolute value are acceptable, despite their problematic treatment of infinities and of undue overflow and underflow. — mcc:✓ gcc:✓ clang:✓ (accepted; semantic optimization is QoI)
- [✓] §7.3.4p2 — The pragma can occur either outside external declarations or preceding all explicit declarations and statements inside a compound statement. — mcc:✓ gcc:✓ clang:✓ (accepted both at file scope and at the top of a compound statement)
- [ ] §7.3.4p2 — When outside external declarations, the pragma takes effect from its occurrence until another `CX_LIMITED_RANGE` pragma is encountered, or until the end of the translation unit. — (spec/scoping; mcc accepts, behavior not observable in output)
- [ ] §7.3.4p2 — When inside a compound statement, the pragma takes effect from its occurrence until another `CX_LIMITED_RANGE` pragma is encountered (including within a nested compound statement) or until the end of the compound statement; at the end of a compound statement the state is restored to its condition just before the compound statement. — (spec/scoping; mcc accepts, behavior not observable in output)
- §7.3.4p2 — If this pragma is used in any other context, the behavior is undefined. — (spec/UB; not separately testable)
- §7.3.4p2 — The default state for the pragma is "off". — (spec; not separately testable)

#### §7.3.5 Trigonometric functions
- [✓] §7.3.5.1p1–p3 — `double complex cacos(double complex z);` (with `cacosf`, `cacosl`): computes the complex arc cosine of `z`, with branch cuts outside the interval [−1, +1] along the real axis; returns the value in a strip unbounded along the imaginary axis and in [0, π] along the real axis. — mcc:✓ gcc:✓ clang:✓ (declared in mcc complex.h; links via libm)
- [✓] §7.3.5.2p1–p3 — `double complex casin(double complex z);` (with `casinf`, `casinl`): computes the complex arc sine of `z`, branch cuts outside [−1, +1] along the real axis; returns the value in a strip unbounded along the imaginary axis and in [−π/2, +π/2] along the real axis. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.3.5.3p1–p3 — `double complex catan(double complex z);` (with `catanf`, `catanl`): computes the complex arc tangent of `z`, branch cuts outside [−i, +i] along the imaginary axis; returns the value in a strip unbounded along the imaginary axis and in [−π/2, +π/2] along the real axis. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.3.5.4p1–p3 — `double complex ccos(double complex z);` (with `ccosf`, `ccosl`): computes and returns the complex cosine of `z`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.3.5.5p1–p3 — `double complex csin(double complex z);` (with `csinf`, `csinl`): computes and returns the complex sine of `z`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.3.5.6p1–p3 — `double complex ctan(double complex z);` (with `ctanf`, `ctanl`): computes and returns the complex tangent of `z`. — mcc:✓ gcc:✓ clang:✓

#### §7.3.6 Hyperbolic functions
- [✓] §7.3.6.1p1–p3 — `double complex cacosh(double complex z);` (with `cacoshf`, `cacoshl`): computes the complex arc hyperbolic cosine of `z`, branch cut at values < 1 along the real axis; returns the value in a half-strip of nonnegative values along the real axis and in [−iπ, +iπ] along the imaginary axis. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.3.6.2p1–p3 — `double complex casinh(double complex z);` (with `casinhf`, `casinhl`): computes the complex arc hyperbolic sine of `z`, branch cuts outside [−i, +i] along the imaginary axis; returns the value in a strip unbounded along the real axis and in [−iπ/2, +iπ/2] along the imaginary axis. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.3.6.3p1–p3 — `double complex catanh(double complex z);` (with `catanhf`, `catanhl`): computes the complex arc hyperbolic tangent of `z`, branch cuts outside [−1, +1] along the real axis; returns the value in a strip unbounded along the real axis and in [−iπ/2, +iπ/2] along the imaginary axis. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.3.6.4p1–p3 — `double complex ccosh(double complex z);` (with `ccoshf`, `ccoshl`): computes and returns the complex hyperbolic cosine of `z`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.3.6.5p1–p3 — `double complex csinh(double complex z);` (with `csinhf`, `csinhl`): computes and returns the complex hyperbolic sine of `z`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.3.6.6p1–p3 — `double complex ctanh(double complex z);` (with `ctanhf`, `ctanhl`): computes and returns the complex hyperbolic tangent of `z`. — mcc:✓ gcc:✓ clang:✓

#### §7.3.7 Exponential and logarithmic functions
- [✓] §7.3.7.1p1–p3 — `double complex cexp(double complex z);` (with `cexpf`, `cexpl`): computes and returns the complex base-e exponential of `z`. — mcc:✓ gcc:✓ clang:✓ (`creal(cexp(0))` == 1 on all three)
- [✓] §7.3.7.2p1–p3 — `double complex clog(double complex z);` (with `clogf`, `clogl`): computes the complex natural (base-e) logarithm of `z`, branch cut along the negative real axis; returns the value in a strip unbounded along the real axis and in [−iπ, +iπ] along the imaginary axis. — mcc:✓ gcc:✓ clang:✓

#### §7.3.8 Power and absolute-value functions
- [✓] §7.3.8.1p1–p3 — `double cabs(double complex z);` (with `cabsf`, `cabsl`): computes and returns the complex absolute value (norm, modulus, magnitude) of `z` — note the `double` (real) return type. — mcc:✓ gcc:✓ clang:✓ (`cabs(3+4i)` == 5; declared with real `double` return)
- [✓] §7.3.8.2p1–p3 — `double complex cpow(double complex x, double complex y);` (with `cpowf`, `cpowl`): computes and returns the complex power function x^y, with a branch cut for the first parameter along the negative real axis. — mcc:✓ gcc:✓ clang:✓ (two-parameter prototype present)
- [✓] §7.3.8.3p1–p3 — `double complex csqrt(double complex z);` (with `csqrtf`, `csqrtl`): computes the complex square root of `z`, branch cut along the negative real axis; returns the value in the range of the right half-plane (including the imaginary axis). — mcc:✓ gcc:✓ clang:✓

#### §7.3.9 Manipulation functions
- [✓] §7.3.9.1p1–p3 — `double carg(double complex z);` (with `cargf`, `cargl`): computes the argument (phase angle) of `z`, branch cut along the negative real axis; returns the value in the interval [−π, +π] — note the `double` (real) return type. — mcc:✓ gcc:✓ clang:✓ (real `double` return; `carg(I)`≈1.5708)
- [✓] §7.3.9.2p1–p3 — `double cimag(double complex z);` (with `cimagf`, `cimagl`): computes and returns the imaginary part of `z` as a real (`double`) value. — mcc:✓ gcc:✓ clang:✓ (mcc masks it with `(__imag__ (double _Complex)(z))`; `cimag(3+4i)`==4)
- [✓] §7.3.9.2p4 / §7.3.9.6p4 — For a variable `z` of complex type, `z == creal(z) + cimag(z)*I` (so a complex value is fully described by `creal`/`cimag`). — mcc:✓ gcc:✓ clang:✓
- [✓] §7.3.9.3p1 — (C11) Synopsis: `double complex CMPLX(double x, double y);` (with `CMPLXF`, `CMPLXL`). — mcc:✓ gcc:✓ clang:✓ (all three macros defined in mcc complex.h)
- [✓] §7.3.9.3p2 — (C11) The `CMPLX` macros expand to an expression of the specified complex type, with the real part having the (converted) value of `x` and the imaginary part having the (converted) value of `y`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.3.9.3p3 — (C11) The resulting expression shall be suitable for use as an initializer for an object with static or thread storage duration, provided both arguments are likewise suitable. — mcc:✓ gcc:✓ clang:✓ (`double complex g = CMPLX(1,2);` and `static complex s = CMPLX(INFINITY,0);` at file scope compile on all three)
- [✓] §7.3.9.3p3 — (C11) The macros act as if the implementation supported imaginary types and the definitions were `#define CMPLX(x, y) ((double complex)((double)(x) + _Imaginary_I * (double)(y)))` — building a complex value with the given real and imaginary parts even when one of them is an infinity or NaN, and preserving the sign of a zero imaginary part. (mcc implements this via `__builtin_complex`.) — mcc:✓ gcc:✓ clang:✓ (`CMPLX(INFINITY,-0.0)` yields real=inf, imag=−0 with the sign bit set on all three)
- [✓] §7.3.9.3p4 — (C11) The `CMPLX` macros return the complex value `x + iy`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.3.9.4p1–p3 — `double complex conj(double complex z);` (with `conjf`, `conjl`): computes and returns the complex conjugate of `z`, by reversing the sign of its imaginary part. — mcc:✓ gcc:✓ clang:✓ (`conj(3+4i)`==3−4i)
- [✓] §7.3.9.5p1–p3 — `double complex cproj(double complex z);` (with `cprojf`, `cprojl`): computes a projection of `z` onto the Riemann sphere — `z` maps to itself except that all complex infinities (even those with one infinite part and one NaN part) map to positive infinity on the real axis; if `z` has an infinite part then `cproj(z)` is equivalent to `INFINITY + I * copysign(0.0, cimag(z))`; returns the projection. — mcc:✓ gcc:✓ clang:✓ (declared and links)
- [✓] §7.3.9.6p1–p3 — `double creal(double complex z);` (with `crealf`, `creall`): computes and returns the real part of `z` as a real (`double`) value. — mcc:✓ gcc:✓ clang:✓ (mcc masks it with `(__real__ (double _Complex)(z))`; `creal(3+4i)`==3; `(creal)(z)` and `&creal` also work)

### §7.4 Character handling `<ctype.h>`
> C99 adds `isblank` (§7.4.1.3); otherwise the set is unchanged in C11.
- [✓] §7.4p1 — The header `<ctype.h>` declares several functions useful for classifying and mapping characters. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.4p1 — In all cases the argument is an `int`, the value of which shall be representable as an `unsigned char` or shall equal the value of the macro `EOF`; if the argument has any other value, the behavior is undefined. — mcc:✓ gcc:✓ clang:✓ (`isalpha(EOF)`/`isspace(EOF)` return 0 on all three)
- [✓] §7.4p2 — The behavior of these functions is affected by the current locale; they classify characters according to the rules of the current locale (the `"C"` locale being in effect by default at program startup). — mcc:✓ gcc:✓ clang:✓ (libc-provided; default "C" locale results match)
- §7.4p3 — The term *printing character* refers to a member of a locale-specific set of characters, each of which occupies one printing position on a display device. — (spec; not separately testable)
- §7.4p3 — The term *control character* refers to a member of a locale-specific set of characters that are not printing characters. — (spec; not separately testable)
- §7.4p4 — In the seven-bit US ASCII character set, the printing characters are those with values 0x20 (space) through 0x7E (tilde); the control characters are those with values 0 (NUL) through 0x1F (US), and the character 0x7F (DEL). (Footnote/example.) — (spec; not separately testable)

#### §7.4.1 Character classification functions
- [✓] §7.4.1p1 — The classification functions return nonzero (true) if and only if the value of the argument `c` conforms to that in the description of the function. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.4.1.1p1–p2 — `int isalnum(int c);`: tests for any character for which `isalpha` or `isdigit` is true. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.4.1.2p1–p2 — `int isalpha(int c);`: tests for any character for which `isupper` or `islower` is true, or any character that is one of a locale-specific set of alphabetic characters for which none of `iscntrl`, `isdigit`, `ispunct`, or `isspace` is true; in the `"C"` locale, true only where `isupper` or `islower` is true. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.4.1.3p1–p2 — (C99) `int isblank(int c);`: tests for any standard blank character or one of a locale-specific set of characters for which `isspace` is true and that is used to separate words within a line of text. The standard blank characters are space (`' '`) and horizontal tab (`'\t'`); in the `"C"` locale, true only for those two. — mcc:✓ gcc:✓ clang:✓ (`isblank('\t')` true; declared by libc ctype.h, available in mcc's default mode)
- [✓] §7.4.1.4p1–p2 — `int iscntrl(int c);`: tests for any control character. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.4.1.5p1–p2 — `int isdigit(int c);`: tests for any decimal-digit character (as defined in 5.2.1). — mcc:✓ gcc:✓ clang:✓
- [✓] §7.4.1.6p1–p2 — `int isgraph(int c);`: tests for any printing character except space (`' '`). — mcc:✓ gcc:✓ clang:✓
- [✓] §7.4.1.7p1–p2 — `int islower(int c);`: tests for any lowercase letter or one of a locale-specific set of characters for which none of `iscntrl`, `isdigit`, `ispunct`, or `isspace` is true; in the `"C"` locale, true only for the lowercase letters (as defined in 5.2.1). — mcc:✓ gcc:✓ clang:✓
- [✓] §7.4.1.8p1–p2 — `int isprint(int c);`: tests for any printing character including space (`' '`). — mcc:✓ gcc:✓ clang:✓
- [✓] §7.4.1.9p1–p2 — `int ispunct(int c);`: tests for any printing character that is one of a locale-specific set of punctuation characters for which neither `isspace` nor `isalnum` is true; in the `"C"` locale, true for every printing character for which neither `isspace` nor `isalnum` is true. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.4.1.10p1–p2 — `int isspace(int c);`: tests for any standard white-space character or one of a locale-specific set of characters for which `isalnum` is false. The standard white-space characters are space (`' '`), form feed (`'\f'`), new-line (`'\n'`), carriage return (`'\r'`), horizontal tab (`'\t'`), and vertical tab (`'\v'`); in the `"C"` locale, true only for those six. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.4.1.11p1–p2 — `int isupper(int c);`: tests for any uppercase letter or one of a locale-specific set of characters for which none of `iscntrl`, `isdigit`, `ispunct`, or `isspace` is true; in the `"C"` locale, true only for the uppercase letters (as defined in 5.2.1). — mcc:✓ gcc:✓ clang:✓
- [✓] §7.4.1.12p1–p2 — `int isxdigit(int c);`: tests for any hexadecimal-digit character (as defined in 6.4.4.1). — mcc:✓ gcc:✓ clang:✓ (`isxdigit('f')` true)

#### §7.4.2 Character case mapping functions
- [✓] §7.4.2.1p1–p3 — `int tolower(int c);`: converts an uppercase letter to a corresponding lowercase letter — if the argument is a character for which `isupper` is true and there are one or more corresponding characters (as specified by the current locale) for which `islower` is true, returns one of the corresponding characters (always the same one for any given locale); otherwise returns the argument unchanged. — mcc:✓ gcc:✓ clang:✓ (`tolower('A')`=='a')
- [✓] §7.4.2.2p1–p3 — `int toupper(int c);`: converts a lowercase letter to a corresponding uppercase letter — if the argument is a character for which `islower` is true and there are one or more corresponding characters (as specified by the current locale) for which `isupper` is true, returns one of the corresponding characters (always the same one for any given locale); otherwise returns the argument unchanged. — mcc:✓ gcc:✓ clang:✓ (`toupper('a')`=='A')

### §7.5 Errors `<errno.h>`
> C99 adds `EILSEQ` (C90 defined only `EDOM` and `ERANGE`). C11 specifies that `errno` has thread-local storage duration and that its value is zero in the initial thread at startup; C99/C90 give a single per-program `errno` that is zero at program startup.
- [✓] §7.5p1 — The header `<errno.h>` defines several macros, all relating to the reporting of error conditions. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.5p2 — The macros `EDOM`, `EILSEQ`, and `ERANGE` expand to integer constant expressions with type `int`, distinct positive values, and which are suitable for use in `#if` preprocessing directives. (`EILSEQ` is C99; C90 had only `EDOM` and `ERANGE`.) — mcc:✓ gcc:✓ clang:✓ (all three defined, distinct, positive, and usable in `#if`)
- [✓] §7.5p2 — Additional macro definitions, beginning with `E` and a digit or `E` and an uppercase letter, may also be specified by the implementation (these names are reserved for future error macros — see future library directions §7.31). — mcc:✓ gcc:✓ clang:✓ (glibc supplies the full `E*` set on all three)
- [✓] §7.5p3 — The macro `errno` expands to a modifiable lvalue that has type `int` and (C11) thread-local storage duration, the value of which is set to a positive error number by several library functions. — mcc:✓ gcc:✓ clang:✓ (`&errno` is an `int*`; assigning `errno=ERANGE` then reading it works; thread test below confirms thread-local)
- [✓] §7.5p3 — `errno` need not be the identifier of an object; it might expand to a modifiable lvalue resulting from a function call (e.g., `*errno()`). It is unspecified whether `errno` is a macro or an identifier declared with external linkage. — mcc:✓ gcc:✓ clang:✓ (glibc's `errno` expands to `(*__errno_location())` on all three)
- §7.5p3 — If a macro definition is suppressed in order to access an actual object, or a program defines an identifier with the name `errno`, the behavior is undefined. — (spec/UB; not separately testable)
- [✓] §7.5p3 — The value of `errno` in the initial thread (C11; "at program startup" in C99) is zero, but is never set to zero by any library function. — mcc:✓ gcc:✓ clang:✓ (libc guarantee; thread-locality verified: a child thread setting `errno=42` leaves the main thread's `errno` unchanged on all three)
- §7.5p3 — The value of `errno` may be set to nonzero by a library function call whether or not there is an error, provided the use of `errno` is not documented in the description of the function in the Standard. — (spec/permission; not separately testable)


## §7.6-§7.8 fenv, float, inttypes

> mcc status: 96 TODOs in this section; 3 are mcc soft-gaps (all tagged `~`): the `FENV_ACCESS` pragma is *recognized and validated* but its placement constraint is not diagnosed and its optimization-inhibition cannot be verified, and bundled `FLT_ROUNDS` is a static `1` rather than dynamic. None of the three is a clean gcc==clang-consensus divergence (gcc and clang themselves disagree on both `FENV_ACCESS` placement diagnosis and `FLT_ROUNDS` dynamism). Everything else conforms: `<fenv.h>` and `<inttypes.h>` resolve to the system glibc headers and glibc runtime (full conformance), and mcc's *bundled* `<float.h>` (`runtime/include/float.h`) is complete including all C11 additions (`*_HAS_SUBNORM`, `*_DECIMAL_DIG`, `*_TRUE_MIN`). mcc's preprocessor genuinely tracks `#pragma STDC FENV_ACCESS` (`src/mccpp.c:1805`, `TOK_STDC`) — it validates the `ON`/`OFF`/`DEFAULT` switch and scopes it to the enclosing compound statement, which is *more* than gcc (which warns "ignoring #pragma STDC FENV_ACCESS").

### §7.6 `<fenv.h>`

> C99→C11: C11 adds that the floating-point environment has **thread storage duration** (§7.6p2) and adds the constant-vs-dynamic control-mode model (§7.6p3); the F-annex cross-reference in §7.6.2.3p2 is **F.8.6** in C11 but **F.7.6** in C99. On this platform `<fenv.h>` resolves to `/usr/include/fenv.h` (glibc) for all three compilers — mcc bundles no `fenv.h`.

- [✓] §7.6p1 — The header `<fenv.h>` shall declare two types (`fenv_t`, `fexcept_t`) and several macros and functions to provide access to the floating-point environment. — mcc:✓ gcc:✓ clang:✓ (glibc header; both types + all funcs compile/link/run)
- §7.6p1 — The floating-point environment refers collectively to any floating-point status flags and control modes supported by the implementation. — (spec; not separately testable)
- §7.6p1 — A floating-point status flag is a system variable whose value is set (but never cleared) when a floating-point exception is raised, which occurs as a side effect of arithmetic operations to inform of exceptional conditions. — (spec; not separately testable)
- §7.6p1 — A floating-point control mode is a system variable whose value may be set by the user to affect the subsequent behavior of floating-point arithmetic. — (spec; not separately testable)
- [✓] §7.6p2 — The floating-point environment has thread storage duration; the initial state for a thread's floating-point environment is the current state of the floating-point environment of the thread that creates it at the time of creation. (C11) — mcc:✓ gcc:✓ clang:✓ (glibc runtime property; not a compiler property)
- §7.6p2 — A function call does not alter its caller's floating-point control modes, clear its caller's floating-point status flags, nor depend on the state of its caller's floating-point status flags unless so documented; a function call is assumed to require default floating-point control modes unless its documentation promises otherwise. — (spec; programming-convention, not separately testable)
- §7.6p3 — A floating-point control mode may be constant (e.g., the rounding direction set by the rounding-direction macros at compile time) or dynamic; the dynamic floating-point environment includes the dynamic control modes and the status flags. (C11) — (spec; not separately testable)
- [✓] §7.6p4 — The type `fenv_t` represents the entire floating-point environment. — mcc:✓ gcc:✓ clang:✓ (`fenv_t env; fegetenv(&env)` round-trips)
- [✓] §7.6p5 — The type `fexcept_t` represents the floating-point status flags collectively (not necessarily a set of bits corresponding to the exceptions). — mcc:✓ gcc:✓ clang:✓ (`fexcept_t fl; fegetexceptflag/fesetexceptflag` round-trips)
- [✓] §7.6p6 — Each floating-point exception macro `FE_DIVBYZERO`, `FE_INEXACT`, `FE_INVALID`, `FE_OVERFLOW`, `FE_UNDERFLOW` is defined iff the implementation supports the corresponding exception via the 7.6.2 functions. — mcc:✓ gcc:✓ clang:✓ (all five defined; x86-64 SSE supports all)
- [✓] §7.6p6 — Each defined exception macro expands to an integer constant expression such that bitwise ORs of all combinations are distinct. — mcc:✓ gcc:✓ clang:✓ (verified in `#if`: all five OR-distinct)
- [✓] §7.6p6 — Additional implementation-defined floating-point exceptions may be specified, with names beginning `FE_` + uppercase letter. — mcc:✓ gcc:✓ clang:✓ (glibc adds `FE_DENORMAL` `__FE_DENORM` on x86; permitted)
- [✓] §7.6p7 — The macro `FE_ALL_EXCEPT` is the bitwise OR of all exception macros defined by the implementation (expands to `0` if none defined). — mcc:✓ gcc:✓ clang:✓ (verified `(FE_DIVBYZERO|FE_INEXACT|FE_INVALID|FE_OVERFLOW|FE_UNDERFLOW)==FE_ALL_EXCEPT` in `#if`)
- [✓] §7.6p8 — Each rounding direction macro `FE_DOWNWARD`, `FE_TONEAREST`, `FE_TOWARDZERO`, `FE_UPWARD` is defined iff that direction can be both got and set via `fegetround`/`fesetround`. — mcc:✓ gcc:✓ clang:✓ (all four defined; round-trip through `fesetround`/`fegetround` works)
- [✓] §7.6p8 — Each defined rounding direction macro expands to a nonnegative integer constant expression distinct from the others. — mcc:✓ gcc:✓ clang:✓ (verified distinct in `#if`)
- §7.6p8 — Additional implementation-defined rounding directions may be specified (`FE_` + uppercase letter). — (spec; permission, not separately testable)
- [✓] §7.6p9 — The macro `FE_DFL_ENV` represents the default environment installed at program startup and expands to an expression of type "pointer to const-qualified `fenv_t`". — mcc:✓ gcc:✓ clang:✓ (`const fenv_t *p = FE_DFL_ENV;` compiles; `fesetenv(FE_DFL_ENV)` runs)
- §7.6p9 — Additional environment macros may be specified, each beginning `FE_` + uppercase letter and each of type "pointer to const-qualified `fenv_t`". — (spec; permission, not separately testable)

#### §7.6.1 The `FENV_ACCESS` pragma

- [✓] §7.6.1p1 — Synopsis: `#include <fenv.h>` then `#pragma STDC FENV_ACCESS on-off-switch` where `on-off-switch` ∈ {`ON`, `OFF`, `DEFAULT`}. — mcc:✓ gcc:~ clang:✓ (mcc parses + validates the switch — warns on malformed e.g. `BOGUSVAL`; gcc warns "ignoring #pragma STDC FENV_ACCESS" i.e. unimplemented; clang accepts)
- §7.6.1p2 — The pragma informs the implementation when a program might access the floating-point environment (test status flags or run under non-default control modes). — (spec; intent, not separately testable)
- [~] §7.6.1p2 — The pragma shall occur either outside external declarations or preceding all explicit declarations and statements inside a compound statement; in any other context its behavior is undefined. — mcc:~ gcc:~ clang:✓ (clang *errors* on misplacement; mcc accepts misplaced pragma silently (no diagnostic); gcc ignores the pragma entirely — no gcc==clang consensus)
- [~] §7.6.1p2 — Outside external declarations the pragma takes effect from its occurrence until another `FENV_ACCESS` pragma or the end of the translation unit. — mcc:~ gcc:~ clang:✓ (mcc tracks state in `MCCState.stdc_*` per `src/mccpp.c:1805`; effect on codegen not separately verifiable)
- [✓] §7.6.1p2 — Inside a compound statement the pragma takes effect until another `FENV_ACCESS` pragma or the end of the compound statement; at the end of the block the pragma state is restored to its condition just before the block. — mcc:✓ gcc:~ clang:✓ (mcc explicitly does not leak the STDC pragma past the closing brace — `src/mccpp.c:1811` comment confirms block-scoped restore)
- [✓] §7.6.1p2 — The default state (`ON` or `OFF`) for the pragma is implementation-defined. — mcc:✓ gcc:✓ clang:✓ (all default to OFF; permitted)
- §7.6.1p3 — If part of a program tests status flags / sets control modes / runs under non-default modes but was translated with `FENV_ACCESS` "off", the behavior is undefined. — (spec; UB catalog, not separately testable)
- [~] §7.6.1p3 — With state "on" the implementation shall not perform optimizations (CSE, code motion, constant folding) that could defeat access to the FP environment; with state "off" it may. — mcc:~ gcc:~ clang:~ (not separately verifiable at -O0; mcc is a tcc-lineage compiler doing minimal FP optimization, so flag/mode access is preserved in practice; full optimization-barrier semantics unverified for all three)

#### §7.6.2 Floating-point exceptions

- [✓] §7.6.2p1 — The functions in 7.6.2 provide access to the floating-point status flags. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.6.2p1 — The `int` argument represents a subset of exceptions: zero or the bitwise OR of one or more exception macros (e.g., `FE_OVERFLOW | FE_INEXACT`). — mcc:✓ gcc:✓ clang:✓ (OR'd masks accepted and acted upon)
- §7.6.2p1 — For other argument values the behavior is undefined. — (spec; UB catalog, not separately testable)

##### §7.6.2.1 The `feclearexcept` function

- [✓] §7.6.2.1p1 — Synopsis: `int feclearexcept(int excepts);` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.6.2.1p2 — Attempts to clear the supported exceptions represented by its argument. — mcc:✓ gcc:✓ clang:✓ (`feclearexcept(FE_ALL_EXCEPT)` then `fetestexcept` reads 0)
- [✓] §7.6.2.1p3 — Returns zero if `excepts` is zero or all specified exceptions were successfully cleared; otherwise nonzero. — mcc:✓ gcc:✓ clang:✓ (returns 0)

##### §7.6.2.2 The `fegetexceptflag` function

- [✓] §7.6.2.2p1 — Synopsis: `int fegetexceptflag(fexcept_t *flagp, int excepts);` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.6.2.2p2 — Attempts to store an implementation-defined representation of the indicated status flags in `*flagp`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.6.2.2p3 — Returns zero if the representation was successfully stored; otherwise nonzero. — mcc:✓ gcc:✓ clang:✓

##### §7.6.2.3 The `feraiseexcept` function

- [✓] §7.6.2.3p1 — Synopsis: `int feraiseexcept(int excepts);` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.6.2.3p2 — Attempts to raise the supported exceptions represented by its argument. — mcc:✓ gcc:✓ clang:✓ (`feraiseexcept(FE_INEXACT)` then `fetestexcept(FE_INEXACT)` nonzero)
- §7.6.2.3p2 — The order in which exceptions are raised is unspecified, except as stated in F.8.6 (C11; F.7.6 in C99). — (spec; unspecified-order, not separately testable)
- §7.6.2.3p2 — Whether `feraiseexcept` additionally raises "inexact" when it raises "overflow"/"underflow" is implementation-defined. — (spec; impl-defined, not separately testable)
- [✓] §7.6.2.3p3 — Returns zero if `excepts` is zero or all specified exceptions were successfully raised; otherwise nonzero. — mcc:✓ gcc:✓ clang:✓

##### §7.6.2.4 The `fesetexceptflag` function

- [✓] §7.6.2.4p1 — Synopsis: `int fesetexceptflag(const fexcept_t *flagp, int excepts);` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.6.2.4p2 — Attempts to set the indicated status flags to the states stored in `*flagp`. — mcc:✓ gcc:✓ clang:✓
- §7.6.2.4p2 — `*flagp` shall have been set by a previous `fegetexceptflag` whose second argument covered at least those exceptions. — (spec; precondition/UB, not separately testable)
- §7.6.2.4p2 — This function does not raise exceptions, only sets the flag state. — (spec; not separately testable)
- [✓] §7.6.2.4p3 — Returns zero if `excepts` is zero or all specified flags were successfully set; otherwise nonzero. — mcc:✓ gcc:✓ clang:✓

##### §7.6.2.5 The `fetestexcept` function

- [✓] §7.6.2.5p1 — Synopsis: `int fetestexcept(int excepts);` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.6.2.5p2 — Determines which of the specified subset of status flags are currently set. — mcc:✓ gcc:✓ clang:✓ (after `1.0/0.0`, `fetestexcept(FE_DIVBYZERO)` is nonzero on all three)
- [✓] §7.6.2.5p3 — Returns the bitwise OR of the exception macros corresponding to the currently-set exceptions included in `excepts`. — mcc:✓ gcc:✓ clang:✓

#### §7.6.3 Rounding

- [✓] §7.6.3p1 — `fegetround`/`fesetround` provide control of rounding direction modes. — mcc:✓ gcc:✓ clang:✓

##### §7.6.3.1 The `fegetround` function

- [✓] §7.6.3.1p1 — Synopsis: `int fegetround(void);` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.6.3.1p2 — Gets the current rounding direction. — mcc:✓ gcc:✓ clang:✓ (returns `FE_TONEAREST`==0 by default)
- [✓] §7.6.3.1p3 — Returns the rounding-direction macro for the current direction, or a negative value if none / not determinable. — mcc:✓ gcc:✓ clang:✓

##### §7.6.3.2 The `fesetround` function

- [✓] §7.6.3.2p1 — Synopsis: `int fesetround(int round);` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.6.3.2p2 — Establishes the rounding direction represented by `round`; if `round` is not a rounding-direction macro value, the direction is not changed. — mcc:✓ gcc:✓ clang:✓ (`fesetround(FE_TOWARDZERO)` then `fegetround()` == `FE_TOWARDZERO`)
- [✓] §7.6.3.2p3 — Returns zero iff the requested direction was established. — mcc:✓ gcc:✓ clang:✓

#### §7.6.4 Environment

- [✓] §7.6.4p1 — These functions manage the floating-point environment (status flags and control modes) as one entity. — mcc:✓ gcc:✓ clang:✓

##### §7.6.4.1 The `fegetenv` function

- [✓] §7.6.4.1p1 — Synopsis: `int fegetenv(fenv_t *envp);` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.6.4.1p2 — Attempts to store the current floating-point environment in `*envp`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.6.4.1p3 — Returns zero if successfully stored; otherwise nonzero. — mcc:✓ gcc:✓ clang:✓

##### §7.6.4.2 The `feholdexcept` function

- [✓] §7.6.4.2p1 — Synopsis: `int feholdexcept(fenv_t *envp);` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.6.4.2p2 — Saves the current environment in `*envp`, clears the status flags, and installs non-stop (continue-on-exception) mode for all exceptions if available. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.6.4.2p3 — Returns zero iff non-stop exception handling was successfully installed. — mcc:✓ gcc:✓ clang:✓ (returns 0 on x86-64)

##### §7.6.4.3 The `fesetenv` function

- [✓] §7.6.4.3p1 — Synopsis: `int fesetenv(const fenv_t *envp);` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.6.4.3p2 — Attempts to establish the environment represented by `*envp`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.6.4.3p2 — `envp` shall point to an object set by `fegetenv`/`feholdexcept`, or equal an environment macro (e.g., `FE_DFL_ENV`). — mcc:✓ gcc:✓ clang:✓ (`fesetenv(FE_DFL_ENV)` runs)
- §7.6.4.3p2 — `fesetenv` merely installs the flag state represented by its argument and does not raise those exceptions. — (spec; not separately testable)
- [✓] §7.6.4.3p3 — Returns zero if the environment was successfully established; otherwise nonzero. — mcc:✓ gcc:✓ clang:✓

##### §7.6.4.4 The `feupdateenv` function

- [✓] §7.6.4.4p1 — Synopsis: `int feupdateenv(const fenv_t *envp);` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.6.4.4p2 — Saves currently raised exceptions in automatic storage, installs the environment from `*envp`, then re-raises the saved exceptions. — mcc:✓ gcc:✓ clang:✓
- §7.6.4.4p2 — `envp` shall point to an object set by `feholdexcept`/`fegetenv`, or equal an environment macro (e.g., `FE_DFL_ENV`). — (spec; precondition, not separately testable)
- [✓] §7.6.4.4p3 — Returns zero if all actions were successfully carried out; otherwise nonzero. — mcc:✓ gcc:✓ clang:✓

### §7.7 `<float.h>`

> mcc serves its **own bundled** `runtime/include/float.h` (109 lines), not the system header — confirmed via `-E` (`# 1 "/home/llg/Projects/moderncc/runtime/include/float.h"`). It is complete and includes all C11 additions, with per-`LDBL`-format branches (53/64/113-bit). gcc/clang use their own `float.h`. All values agree at runtime.

- [✓] §7.7p1 — The header `<float.h>` defines several macros that expand to various limits and parameters of the standard floating types. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.7p2 — The macros, meanings, and value constraints are listed in 5.2.4.2.2. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.7 (gap) — `<float.h>` shall be usable in a freestanding implementation. — mcc:✓ gcc:✓ clang:✓ (header has no includes/decls; compiles under `-ffreestanding`)

#### §5.2.4.2.2 Characteristics of floating types `<float.h>`

> C99→C11: C11 adds `*_HAS_SUBNORM` (p6), `*_DECIMAL_DIG` macros, `*_TRUE_MIN` (p11), and explicit normalized/subnormal/unnormalized terminology (p2); in C11 `DECIMAL_DIG` equals the widest type's `*_DECIMAL_DIG`. mcc's bundled `float.h` provides all of these.

- §5.2.4.2.2p1 — Floating types are defined via the model `x = s · b^e · Σ(f_k · b^(−k))`, k=1..p, with parameters s (sign ±1), b (radix >1), e (exponent in [e_min,e_max]), p (precision = base-b significand digits), f_k (significand digits, 0 ≤ f_k < b). — (spec; definitional, not separately testable)
- §5.2.4.2.2p2 — A number is normalized if f_1>0; a subnormal is a nonzero number not normalized with exponent e_min; an unnormalized number is nonzero, not normalized, with exponent > e_min. (C11) — (spec; definitional, not separately testable)
- §5.2.4.2.2p3 — The accuracy of `+ - * /` and of the floating-result `<math.h>`/`<complex.h>` library functions is implementation-defined, as is the accuracy of FP↔string conversions in `<stdio.h>`/`<stdlib.h>`/`<wchar.h>`; the implementation may state the accuracy is unknown. — (spec; impl-defined, not separately testable)
- [✓] §5.2.4.2.2p4 — `FLT_ROUNDS` characterizes the rounding mode for FP addition: −1 indeterminable, 0 toward zero, 1 to nearest, 2 toward +∞, 3 toward −∞; any other value impl-defined. — mcc:✓ gcc:✓ clang:✓ (default 1; all three print 1)
- [~] §5.2.4.2.2p4 — `FLT_ROUNDS` may change with a call to `fesetround`; it is therefore not necessarily constant and need not be usable in `#if`. — mcc:~ gcc:~ clang:✓ (clang's `FLT_ROUNDS` is *dynamic* — returns 0 after `fesetround(FE_TOWARDZERO)`; mcc's bundled value is hardcoded `#define FLT_ROUNDS 1` so it stays 1; gcc *also* stays 1 here — no gcc==clang consensus, but mcc/gcc both fail to reflect dynamic rounding)
- [✓] §5.2.4.2.2p5 — `FLT_EVAL_METHOD` (C99) characterizes evaluation formats: −1 indeterminable, 0 evaluate to each type's own range/precision, 1 evaluate float/double ops to double, 2 evaluate all ops to long double; other negative values impl-defined. — mcc:✓ gcc:✓ clang:✓ (all three: 0 on x86-64 SSE; mcc bundles 0/2 branches on `__SSE2_MATH__`)
- [✓] §5.2.4.2.2p6 — `FLT_HAS_SUBNORM`/`DBL_HAS_SUBNORM`/`LDBL_HAS_SUBNORM` (C11): −1 indeterminable, 0 absent, 1 present. — mcc:✓ gcc:✓ clang:✓ (mcc bundled = 1; `#if FLT_HAS_SUBNORM!=1 #error` passes)
- [✓] §5.2.4.2.2p7 — The integer-valued macros shall be replaced by constant expressions suitable for `#if`: `FLT_RADIX`, `*_MANT_DIG`, `*_DECIMAL_DIG` (C11), `DECIMAL_DIG`, `*_DIG`, `*_MIN_EXP`, `*_MIN_10_EXP`, `*_MAX_EXP`, `*_MAX_10_EXP`, `FLT_EVAL_METHOD` (and `*_HAS_SUBNORM` in C11) — but **not** `FLT_ROUNDS`. — mcc:✓ gcc:✓ clang:✓ (large `#if` chain over all of these compiles on all three)
- [✓] §5.2.4.2.2p7 — `FLT_RADIX` — radix b of the exponent representation; value shall be ≥ 2. — mcc:✓ gcc:✓ clang:✓ (=2; `#if FLT_RADIX<2 #error` passes)
- [✓] §5.2.4.2.2p7 — `FLT_MANT_DIG`/`DBL_MANT_DIG`/`LDBL_MANT_DIG` — number of base-`FLT_RADIX` significand digits p. — mcc:✓ gcc:✓ clang:✓ (24/53/64; verified in `#if`)
- [✓] §5.2.4.2.2p7 — `FLT_DECIMAL_DIG`/`DBL_DECIMAL_DIG`/`LDBL_DECIMAL_DIG` (C11) — round-trip decimal digits n; values ≥ 6, ≥ 10, ≥ 10. — mcc:✓ gcc:✓ clang:✓ (mcc bundled 9/17/21; verified ≥ bounds in `#if`)
- [✓] §5.2.4.2.2p7 — `DECIMAL_DIG` (C99) — round-trip decimal digits for the widest supported floating type; value ≥ 10 (in C11 equals widest `*_DECIMAL_DIG`). — mcc:✓ gcc:✓ clang:✓ (mcc bundled 21 on 80-bit long double, == `LDBL_DECIMAL_DIG`)
- [✓] §5.2.4.2.2p7 — `FLT_DIG`/`DBL_DIG`/`LDBL_DIG` — decimal digits q surviving decimal→float→decimal round-trip; values ≥ 6, ≥ 10, ≥ 10. — mcc:✓ gcc:✓ clang:✓ (6/15/18; verified in `#if`)
- [✓] §5.2.4.2.2p7 — `FLT_MIN_EXP`/`DBL_MIN_EXP`/`LDBL_MIN_EXP` — minimum negative e such that b^(e−1) is normalized (e_min). — mcc:✓ gcc:✓ clang:✓ (−125/−1021/−16381)
- [✓] §5.2.4.2.2p7 — `FLT_MIN_10_EXP`/`DBL_MIN_10_EXP`/`LDBL_MIN_10_EXP` — min negative integer with 10^x in the normalized range; values ≤ −37. — mcc:✓ gcc:✓ clang:✓ (`#if FLT_MIN_10_EXP > -37 #error` passes)
- [✓] §5.2.4.2.2p7 — `FLT_MAX_EXP`/`DBL_MAX_EXP`/`LDBL_MAX_EXP` — max integer such that b^(e−1) is a representable finite value (e_max). — mcc:✓ gcc:✓ clang:✓ (128/1024/16384)
- [✓] §5.2.4.2.2p7 — `FLT_MAX_10_EXP`/`DBL_MAX_10_EXP`/`LDBL_MAX_10_EXP` — max integer with 10^x in the representable finite range; values ≥ +37. — mcc:✓ gcc:✓ clang:✓ (`#if FLT_MAX_10_EXP < 37 #error` passes)
- [✓] §5.2.4.2.2p8 — `FLT_MAX`/`DBL_MAX`/`LDBL_MAX` (max finite value, `(1−b^−p)·b^e_max`) shall be constant expressions ≥ `1×10^37`. — mcc:✓ gcc:✓ clang:✓ (3.40282e+38 / 1.79769e+308 print identically across all three)
- [✓] §5.2.4.2.2p9 — `FLT_EPSILON`/`DBL_EPSILON`/`LDBL_EPSILON` (`b^(1−p)`) shall be constant expressions ≤ `1E−5`, `1E−9`, `1E−9` respectively. — mcc:✓ gcc:✓ clang:✓ (1.19209e-07 etc.; identical)
- [✓] §5.2.4.2.2p10 — `FLT_MIN`/`DBL_MIN`/`LDBL_MIN` (min normalized positive, `b^(e_min−1)`) shall be constant expressions ≤ `1E−37` each. — mcc:✓ gcc:✓ clang:✓ (1.17549e-38 etc.; identical)
- [✓] §5.2.4.2.2p11 — `FLT_TRUE_MIN`/`DBL_TRUE_MIN`/`LDBL_TRUE_MIN` (C11) (min positive value `b^(e_min−p)`, the smallest positive subnormal) shall be constant expressions ≤ `1E−37` each. — mcc:✓ gcc:✓ clang:✓ (mcc bundled 1.4013e-45 etc.; prints identically)
- [✓] §5.2.4.2.2p8–p11 — The floating-valued macros `*_MAX`, `*_MIN`, `*_EPSILON`, and C11 `*_TRUE_MIN` are floating-constant expressions not required to be usable in `#if`. — mcc:✓ gcc:✓ clang:✓ (used as runtime constants; not exercised in `#if`)

### §7.8 `<inttypes.h>`

> C99→C11: `<inttypes.h>` is essentially unchanged (the wide-char forward reference is 7.29.2 in C11 vs 7.24.2 in C99). On this platform it resolves to `/usr/include/inttypes.h` (glibc) for all three compilers; mcc bundles `stdint.h` (which inttypes includes) but not inttypes itself. Full PRI*/SCN* macro families and all four functions verified.

- [✓] §7.8p1 — `<inttypes.h>` includes `<stdint.h>` and extends it with additional facilities for hosted implementations. — mcc:✓ gcc:✓ clang:✓ (`INT32_MAX` usable after including only `<inttypes.h>`)
- [✓] §7.8p1 — Declares functions for manipulating greatest-width integers and converting numeric strings to greatest-width integers. — mcc:✓ gcc:✓ clang:✓ (`imaxabs`/`imaxdiv`/`strtoimax`/`strtoumax`/`wcstoimax`/`wcstoumax` link and run)
- [✓] §7.8p1 — Declares the type `imaxdiv_t`, a structure that is the type returned by `imaxdiv`. — mcc:✓ gcc:✓ clang:✓ (`imaxdiv_t d = imaxdiv(17,5); d.quot d.rem` work)
- [✓] §7.8p1 — For each `<stdint.h>` type, defines corresponding format-specifier macros for the formatted I/O functions. — mcc:✓ gcc:✓ clang:✓
- §7.8p2 — A program may use the macros to define its own macros for the formatted I/O functions (forward refs: 7.20, 7.21.6, 7.29.2/7.24.2). — (spec; permission, not separately testable)

#### §7.8.1 Macros for format specifiers

- [✓] §7.8.1p1 — Each listed object-like macro expands to a character string literal containing a conversion specifier (possibly with a length modifier) suitable for the format argument of a formatted I/O function for the corresponding integer type. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.8.1p1 — Macro names have the form `PRI` (printf/wprintf family) or `SCN` (scanf/wscanf family) + conversion specifier + a name corresponding to a 7.20.1 type name; `N` represents the type width (e.g. 8, 16, 32, 64). — mcc:✓ gcc:✓ clang:✓
- [✓] §7.8.1p2 — The `fprintf` signed-integer macros shall be defined: `PRIdN`/`PRIdLEASTN`/`PRIdFASTN`/`PRIdMAX`/`PRIdPTR` and `PRIiN`/`PRIiLEASTN`/`PRIiFASTN`/`PRIiMAX`/`PRIiPTR`. — mcc:✓ gcc:✓ clang:✓ (`PRIdMAX`, `PRId32`, `PRIiMAX` produce correct output)
- [✓] §7.8.1p3 — The `fprintf` unsigned-integer macros shall be defined: `PRIo*`, `PRIu*`, `PRIx*`, `PRIX*` over `N`/`LEASTN`/`FASTN`/`MAX`/`PTR`. — mcc:✓ gcc:✓ clang:✓ (`PRIu64`, `PRIxLEAST16`, `PRIoFAST8`, `PRIuPTR`, `PRIXMAX` all correct)
- [✓] §7.8.1p4 — The `fscanf` signed-integer macros shall be defined: `SCNdN`/`SCNdLEASTN`/`SCNdFASTN`/`SCNdMAX`/`SCNdPTR` and `SCNi*`. — mcc:✓ gcc:✓ clang:✓ (`SCNd64` round-trips through `sscanf`)
- [✓] §7.8.1p5 — The `fscanf` unsigned-integer macros shall be defined: `SCNo*`, `SCNu*`, `SCNx*` over `N`/`LEASTN`/`FASTN`/`MAX`/`PTR`. — mcc:✓ gcc:✓ clang:✓ (`SCNx16`, `SCNu32` round-trip through `sscanf`)
- [✓] §7.8.1p6 — For each type the implementation provides in `<stdint.h>`, the corresponding `fprintf` macros shall be defined and the `fscanf` macros shall be defined unless there is no suitable `fscanf` length modifier for the type. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.8.1p7 — (EXAMPLE) `printf("%020" PRIdMAX "\n", i)` and `scanf("%10" SCNd64 "\n", &i)` work as shown. — mcc:✓ gcc:✓ clang:✓ (`printf("%020" PRIdMAX, ...)` → `-0000001234567890123` on all three)

#### §7.8.2 Functions for greatest-width integer types

##### §7.8.2.1 The `imaxabs` function

- [✓] §7.8.2.1p1 — Synopsis: `intmax_t imaxabs(intmax_t j);` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.8.2.1p2 — Computes the absolute value of `j`; if the result cannot be represented, behavior is undefined. — mcc:✓ gcc:✓ clang:✓ (`imaxabs(-42)==42`)
- [✓] §7.8.2.1p3 — Returns the absolute value. — mcc:✓ gcc:✓ clang:✓

##### §7.8.2.2 The `imaxdiv` function

- [✓] §7.8.2.2p1 — Synopsis: `imaxdiv_t imaxdiv(intmax_t numer, intmax_t denom);` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.8.2.2p2 — Computes `numer / denom` and `numer % denom` in a single operation. — mcc:✓ gcc:✓ clang:✓ (`imaxdiv(17,5)` → quot 3, rem 2)
- [✓] §7.8.2.2p3 — Returns an `imaxdiv_t` with both quotient and remainder; the struct contains (in either order) members `quot` and `rem`, each of type `intmax_t`. — mcc:✓ gcc:✓ clang:✓
- §7.8.2.2p3 — If either part of the result cannot be represented, behavior is undefined. — (spec; UB catalog, not separately testable)

##### §7.8.2.3 The `strtoimax` and `strtoumax` functions

- [✓] §7.8.2.3p1 — Synopsis: `intmax_t strtoimax(const char *restrict nptr, char **restrict endptr, int base);` and `uintmax_t strtoumax(...)`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.8.2.3p2 — Equivalent to `strtol`/`strtoll`/`strtoul`/`strtoull` except converting to `intmax_t`/`uintmax_t`. — mcc:✓ gcc:✓ clang:✓ (`strtoimax("  -ff",&e,16)==-255`; `strtoumax("100",0,10)==100`)
- [✓] §7.8.2.3p3 — Returns the converted value (zero if no conversion); on overflow returns `INTMAX_MAX`/`INTMAX_MIN`/`UINTMAX_MAX` per return type/sign and stores `ERANGE` in `errno`. — mcc:✓ gcc:✓ clang:✓ (glibc runtime)

##### §7.8.2.4 The `wcstoimax` and `wcstoumax` functions

- [✓] §7.8.2.4p1 — Synopsis (with `<stddef.h>` for `wchar_t` and `<inttypes.h>`): `intmax_t wcstoimax(const wchar_t *restrict nptr, wchar_t **restrict endptr, int base);` and `uintmax_t wcstoumax(...)`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.8.2.4p2 — Equivalent to `wcstol`/`wcstoll`/`wcstoul`/`wcstoull` except converting to `intmax_t`/`uintmax_t`. — mcc:✓ gcc:✓ clang:✓ (`wcstoimax(L"123",0,10)==123`)
- [✓] §7.8.2.4p3 — Returns the converted value (zero if none); on overflow returns `INTMAX_MAX`/`INTMAX_MIN`/`UINTMAX_MAX` per return type/sign and stores `ERANGE` in `errno`. — mcc:✓ gcc:✓ clang:✓ (glibc runtime)


## §7.9-§7.11 iso646, limits, locale

> mcc status: 92 TODO checkboxes below; 0 mcc-gaps. `<iso646.h>` and `<limits.h>` are mcc-bundled (`runtime/include/`) and conform byte-for-byte to gcc/clang; mcc bundles **no** `<locale.h>` and forwards to the host C library, so all `<locale.h>` behavior is identical to gcc/clang on the same system. Probed: all 11 iso646 macros (value + token identity + `#if` expansion), every `<limits.h>` macro value, `#if`-preprocessor usability, the C11 "same type after promotion" rule via `_Generic`, the freestanding fallback path, and the full "C"-locale `struct lconv` contents.

### §7.9 Alternative spellings `<iso646.h>`

> mcc ships a complete `runtime/include/iso646.h` defining all eleven macros exactly as required. C99 and C11 are identical here; in C++ these eleven names are keywords rather than macros. Probe: `a and b`, `a or b`, `not b`, `a not_eq b`, `a and_eq …`, `or_eq`, `xor_eq`, `a bitand b`==2, `a bitor b`==7, `a xor b`==5, `compl a`==−7 all produced identical results on all three compilers.

- [✓] §7.9p1 — The header `<iso646.h>` shall define eleven object-like macros that expand to the corresponding operator tokens. *(mcc: provided in `runtime/include/iso646.h`.)* — mcc:✓ gcc:✓ clang:✓
- [✓] §7.9p1 — `and` shall expand to `&&`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.9p1 — `and_eq` shall expand to `&=`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.9p1 — `bitand` shall expand to `&`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.9p1 — `bitor` shall expand to `|`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.9p1 — `compl` shall expand to `~`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.9p1 — `not` shall expand to `!`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.9p1 — `not_eq` shall expand to `!=`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.9p1 — `or` shall expand to `||`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.9p1 — `or_eq` shall expand to `|=`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.9p1 — `xor` shall expand to `^`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.9p1 — `xor_eq` shall expand to `^=`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.9p2 (GAP) — These macros shall be usable in `#if`/`#elif` preprocessing directives (e.g. `#if INT_MAX >= 32767 and LONG_MAX > INT_MAX`), since they expand to operator punctuators that the controlling-expression evaluator accepts. — mcc:✓ gcc:✓ clang:✓ *(probed: `#if … and … or …` compiled and ran on all three)*
- §7.1.3 — Each of these eleven names is reserved as a macro only when `<iso646.h>` is included; while so defined the program shall not `#define` or `#undef` it (constraint of §7.1.3). — (spec; not separately testable) *(no diagnostic required; mcc, gcc, and clang all silently accept `#undef and` / `#define and 5` — consensus is no diagnostic)*
- [✓] §7.9p1 (GAP) — The header shall be idempotent / multiple-inclusion safe (include guard). *(mcc: `#ifndef _ISO646_H` guard present.)* — mcc:✓ gcc:✓ clang:✓

### §7.10 Sizes of integer types `<limits.h>`

> §7.10p2 forwards to §5.2.4.2.1 for the macros, their meanings, and the value constraints. C99 added `LLONG_MIN`/`LLONG_MAX`/`ULLONG_MAX` (not in C90). C11 added to §5.2.4.2.1p1 the sentence requiring each macro to have the same type as the corresponding object after integer promotions; C99 lacked that explicit type requirement. Probe: every macro printed identical values across mcc/gcc/clang on x86-64 (CHAR_BIT=8, 32-bit `int`, 64-bit `long`/`long long`, signed plain `char`).

- [✓] §7.10p1 — The header `<limits.h>` shall define several macros that expand to various limits and parameters of the standard integer types. *(mcc: `runtime/include/limits.h`, which `#include_next`s the host `<limits.h>` when hosted and supplies fallbacks otherwise.)* — mcc:✓ gcc:✓ clang:✓
- [✓] §7.10p2 / §5.2.4.2.1p1 — The values defined in `<limits.h>` shall be replaced by constant expressions suitable for use in `#if` preprocessing directives. — mcc:✓ gcc:✓ clang:✓ *(probed `#if ULLONG_MAX > 0 && LLONG_MIN < 0` etc.)*
- [✓] §5.2.4.2.1p1 — Each defined limit shall have the same type as would an expression that is an object of the corresponding type converted according to the integer promotions. *(C11 addition; absent in C99.)* — mcc:✓ gcc:✓ clang:✓ *(probed via `_Generic`: UINT_MAX→unsigned int, INT_MAX→int, ULONG_MAX→unsigned long, LLONG_MAX→long long, USHRT_MAX/UCHAR_MAX→int (promoted) on all three)*
- [✓] §5.2.4.2.1p1 — Each macro's implementation-defined value shall be equal to or greater in magnitude than (absolute value of) the value shown, with the same sign. — mcc:✓ gcc:✓ clang:✓
- [✓] §5.2.4.2.1p1 — `CHAR_BIT` — number of bits for the smallest object that is not a bit-field (a byte); value shall be ≥ 8. — mcc:✓ gcc:✓ clang:✓ *(=8)*
- [✓] §5.2.4.2.1p1 — `SCHAR_MIN` — minimum value for an object of type `signed char`; value shall be ≤ −127. — mcc:✓ gcc:✓ clang:✓ *(=−128)*
- [✓] §5.2.4.2.1p1 — `SCHAR_MAX` — maximum value for an object of type `signed char`; value shall be ≥ +127. — mcc:✓ gcc:✓ clang:✓ *(=127)*
- [✓] §5.2.4.2.1p1 — `UCHAR_MAX` — maximum value for an object of type `unsigned char`; value shall be ≥ 255. — mcc:✓ gcc:✓ clang:✓ *(=255)*
- [✓] §5.2.4.2.1p1 — `MB_LEN_MAX` — maximum number of bytes in a multibyte character, for any supported locale; value shall be ≥ 1. — mcc:✓ gcc:✓ clang:✓ *(host glibc =16; mcc bundled fallback also 16)*
- [✓] §5.2.4.2.1p1 — `SHRT_MIN` — minimum value for an object of type `short int`; value shall be ≤ −32767. — mcc:✓ gcc:✓ clang:✓ *(=−32768)*
- [✓] §5.2.4.2.1p1 — `SHRT_MAX` — maximum value for an object of type `short int`; value shall be ≥ +32767. — mcc:✓ gcc:✓ clang:✓ *(=32767)*
- [✓] §5.2.4.2.1p1 — `USHRT_MAX` — maximum value for an object of type `unsigned short int`; value shall be ≥ 65535. — mcc:✓ gcc:✓ clang:✓ *(=65535)*
- [✓] §5.2.4.2.1p1 — `INT_MIN` — minimum value for an object of type `int`; value shall be ≤ −32767. — mcc:✓ gcc:✓ clang:✓ *(=−2147483648)*
- [✓] §5.2.4.2.1p1 — `INT_MAX` — maximum value for an object of type `int`; value shall be ≥ +32767. — mcc:✓ gcc:✓ clang:✓ *(=2147483647)*
- [✓] §5.2.4.2.1p1 — `UINT_MAX` — maximum value for an object of type `unsigned int`; value shall be ≥ 65535. — mcc:✓ gcc:✓ clang:✓ *(=4294967295)*
- [✓] §5.2.4.2.1p1 — `LONG_MIN` — minimum value for an object of type `long int`; value shall be ≤ −2147483647. — mcc:✓ gcc:✓ clang:✓ *(=−9223372036854775808)*
- [✓] §5.2.4.2.1p1 — `LONG_MAX` — maximum value for an object of type `long int`; value shall be ≥ +2147483647. — mcc:✓ gcc:✓ clang:✓ *(=9223372036854775807)*
- [✓] §5.2.4.2.1p1 — `ULONG_MAX` — maximum value for an object of type `unsigned long int`; value shall be ≥ 4294967295. — mcc:✓ gcc:✓ clang:✓ *(=18446744073709551615)*
- [✓] §5.2.4.2.1p1 — `LLONG_MIN` — minimum value for an object of type `long long int`; value shall be ≤ −9223372036854775807. *(C99 addition; not present in C90.)* — mcc:✓ gcc:✓ clang:✓ *(=−9223372036854775808)*
- [✓] §5.2.4.2.1p1 — `LLONG_MAX` — maximum value for an object of type `long long int`; value shall be ≥ +9223372036854775807. *(C99 addition; not present in C90.)* — mcc:✓ gcc:✓ clang:✓ *(=9223372036854775807)*
- [✓] §5.2.4.2.1p1 — `ULLONG_MAX` — maximum value for an object of type `unsigned long long int`; value shall be ≥ 18446744073709551615. *(C99 addition; not present in C90.)* — mcc:✓ gcc:✓ clang:✓ *(=18446744073709551615)*
- [✓] §5.2.4.2.1p2 — `CHAR_MIN` — minimum value for an object of type plain `char`; if `char` is treated as signed it shall equal `SCHAR_MIN`, otherwise (plain `char` unsigned) it shall be 0. *(mcc bundled header selects via `'\xff' > 0`, matching per-target plain-char signedness.)* — mcc:✓ gcc:✓ clang:✓ *(x86-64 signed: =−128=SCHAR_MIN)*
- [✓] §5.2.4.2.1p2 — `CHAR_MAX` — maximum value for an object of type plain `char`; if `char` is treated as signed it shall equal `SCHAR_MAX`, otherwise it shall equal `UCHAR_MAX`. Whether plain `char` is signed is implementation-defined. — mcc:✓ gcc:✓ clang:✓ *(x86-64 signed: =127=SCHAR_MAX)*
- [✓] §7.10p1 (GAP) — The header shall be multiple-inclusion safe (include guard). *(mcc: `#ifndef _MCC_LIMITS_H` guard; inner host header has its own guard.)* — mcc:✓ gcc:✓ clang:✓
- [✓] §7.10p1 (GAP) — In a freestanding/`-ffreestanding` translation (no host `<limits.h>` to chain to) the header shall still define the required macros from compiler built-ins. *(mcc: `#if __has_include_next(<limits.h>)` guards the chain and the `__CHAR_BIT__`/`__INT_MAX__`/`__LONG_MAX__`/`__LONG_LONG_MAX__` fallbacks define the rest.)* — mcc:✓ gcc:✓ clang:✓ *(probed `mcc -ffreestanding -c` with `int a[INT_MAX>0?1:-1]; int b=CHAR_BIT;` — compiles)*
- §6.10.8.2 (GAP, C11) — Macros required to use `<limits.h>` indirectly: `__STDC_*` width/limit signalling is not part of §7.10; no extra macros beyond §5.2.4.2.1 are mandated. — (spec; not separately testable)

### §7.11 Localization `<locale.h>`

> mcc bundles **no** `runtime/include/locale.h`; it relies on the host/system `<locale.h>` (glibc here). Consequently every `<locale.h>` requirement below is satisfied to exactly the degree the host C library satisfies it, and mcc's result is byte-identical to gcc's and clang's on the same system. C11 adds `wcsftime` (in `<wchar.h>`) to the list of functions affected by `LC_TIME`; otherwise §7.11 is unchanged from C99. Probe: `setlocale`/`localeconv` link and run; the entire "C"-locale `struct lconv` matched the standard (decimal_point=".", all other strings "", all `char` members = CHAR_MAX=127) identically on all three.

- [✓] §7.11p1 — The header `<locale.h>` shall declare two functions (`setlocale`, `localeconv`), define one type (`struct lconv`), and define several macros. *(mcc: no bundled `locale.h`; uses the C library's.)* — mcc:✓ gcc:✓ clang:✓
- [✓] §7.11p1 — The type `struct lconv` shall contain members related to the formatting of numeric values, with at least the members described in §7.11.2.1 (in any order, with the specified types); in the "C" locale they shall have the values given in the `localeconv` description. — mcc:✓ gcc:✓ clang:✓ *(host-provided; probed)*
- [✓] §7.11p2 — The macro `NULL` shall be defined (as described in §7.17). — mcc:✓ gcc:✓ clang:✓
- [✓] §7.11p2 — The macros `LC_ALL`, `LC_COLLATE`, `LC_CTYPE`, `LC_MONETARY`, `LC_NUMERIC`, and `LC_TIME` shall each be defined, expanding to an integer constant expression with distinct values, suitable for use as the first argument to `setlocale`. — mcc:✓ gcc:✓ clang:✓ *(probed distinct: 6/3/0/4/1/2 on all three)*
- §7.11p2 — Additional macros beginning with `LC_` and an uppercase letter may be defined by the implementation (reserved for future additional locale categories). — (spec; not separately testable) *(host glibc defines extras e.g. `LC_PAPER`, `LC_MESSAGES`; identical for all three)*

#### §7.11.1 Locale control

##### §7.11.1.1 The `setlocale` function

- [✓] §7.11.1.1p1 — Synopsis: `#include <locale.h>` followed by `char *setlocale(int category, const char *locale);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.11.1.1p2 — `setlocale` selects the appropriate portion of the program's locale as specified by `category` and `locale`, and may be used to change or query the entire current locale or portions thereof. — mcc:✓ gcc:✓ clang:✓ *(host-provided)*
- [✓] §7.11.1.1p2 — `category == LC_ALL` names the program's entire locale. — mcc:✓ gcc:✓ clang:✓
- [ ] §7.11.1.1p2 — `category == LC_COLLATE` affects the behavior of the `strcoll` and `strxfrm` functions. — (spec; host-provided, not separately probed)
- [ ] §7.11.1.1p2 — `category == LC_CTYPE` affects the behavior of the character-handling functions and the multibyte and wide-character functions. — (spec; host-provided, not separately probed)
- [ ] §7.11.1.1p2 — `category == LC_MONETARY` affects the monetary formatting information returned by `localeconv`. — (spec; host-provided, not separately probed)
- [ ] §7.11.1.1p2 — `category == LC_NUMERIC` affects the decimal-point character for the formatted input/output functions and the string conversion functions, plus the nonmonetary formatting information returned by `localeconv`. — (spec; host-provided, not separately probed)
- [ ] §7.11.1.1p2 — `category == LC_TIME` affects the behavior of the `strftime` and (C11) `wcsftime` functions. — (spec; host-provided, not separately probed)
- [✓] §7.11.1.1p3 — `locale == "C"` specifies the minimal environment for C translation. — mcc:✓ gcc:✓ clang:✓ *(probed `setlocale(LC_ALL,"C")` returns "C")*
- [✓] §7.11.1.1p3 — `locale == ""` specifies the locale-specific native environment. — mcc:✓ gcc:✓ clang:✓ *(host-provided)*
- §7.11.1.1p3 — Other implementation-defined strings may be passed as the `locale` argument. — (spec; host-provided, not separately testable)
- [✓] §7.11.1.1p4 — At program startup the equivalent of `setlocale(LC_ALL, "C");` is executed (the program begins in the "C" locale). — mcc:✓ gcc:✓ clang:✓ *(probed: `setlocale(LC_ALL,NULL)` at startup returns "C" on all three)*
- [✓] §7.11.1.1p5/p7 — A null pointer for `locale` causes `setlocale` to return a pointer to the string associated with `category` for the program's current locale, without changing the locale. — mcc:✓ gcc:✓ clang:✓ *(probed)*
- [✓] §7.11.1.1p6 — The returned string is such that a subsequent `setlocale` call with that string value and its associated `category` will restore that part of the program's locale. — mcc:✓ gcc:✓ clang:✓ *(host-provided)*
- §7.11.1.1p6 — The string returned by `setlocale` shall not be modified by the program, but may be overwritten by a subsequent call to `setlocale`. — (spec; not separately testable — UB if modified)
- [✓] §7.11.1.1p7 — Returns: if a pointer to a string is given for `locale` and the selection can be honored, `setlocale` returns a pointer to the string associated with the specified `category` for the new locale. — mcc:✓ gcc:✓ clang:✓ *(probed: "C" → "C")*
- [✓] §7.11.1.1p7 — Returns: if the selection cannot be honored, `setlocale` returns a null pointer and the program's locale is not changed. — mcc:✓ gcc:✓ clang:✓ *(probed: bogus locale name → NULL on all three)*
- §7.11.1.1p7 footnote — The implementation shall behave as if no library function calls `setlocale` (no standard library function alters the locale as a side effect). — (spec; host-provided, not separately testable)

#### §7.11.2 Numeric formatting convention inquiry

##### §7.11.2.1 The `localeconv` function

- [✓] §7.11.2.1p1 — Synopsis: `#include <locale.h>` followed by `struct lconv *localeconv(void);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.11.2.1p2 — `localeconv` sets the components of an object of type `struct lconv` with values appropriate for formatting numeric quantities (monetary and otherwise) according to the rules of the current locale. — mcc:✓ gcc:✓ clang:✓ *(host-provided; probed)*
- [✓] §7.11.2.1p3 — Members of type `char *` are pointers to strings, any of which (except `decimal_point`) can point to `""` to indicate that the value is not available in the current locale or is of zero length. — mcc:✓ gcc:✓ clang:✓ *(probed in "C": all non-decimal_point strings = "")*
- §7.11.2.1p3 — Apart from `grouping` and `mon_grouping`, the `char *` strings shall start and end in the initial shift state. — (spec; host-provided, not separately testable)
- [✓] §7.11.2.1p3 — Members of type `char` are nonnegative numbers, any of which can be `CHAR_MAX` to indicate that the value is not available in the current locale. — mcc:✓ gcc:✓ clang:✓ *(probed in "C": all = CHAR_MAX=127)*
- [✓] §7.11.2.1p3 — `char *decimal_point` — the decimal-point character used to format nonmonetary quantities. — mcc:✓ gcc:✓ clang:✓ *(probed: "." in "C")*
- [✓] §7.11.2.1p3 — `char *thousands_sep` — the character used to separate groups of digits before the decimal-point character in formatted nonmonetary quantities. — mcc:✓ gcc:✓ clang:✓ *(probed: "" in "C")*
- [✓] §7.11.2.1p3 — `char *grouping` — a string whose elements indicate the size of each group of digits in formatted nonmonetary quantities. — mcc:✓ gcc:✓ clang:✓ *(probed: "" in "C")*
- [✓] §7.11.2.1p3 — `char *mon_decimal_point` — the decimal-point character used to format monetary quantities. — mcc:✓ gcc:✓ clang:✓ *(probed: "" in "C")*
- [✓] §7.11.2.1p3 — `char *mon_thousands_sep` — the separator for groups of digits before the decimal-point character in formatted monetary quantities. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.11.2.1p3 — `char *mon_grouping` — a string whose elements indicate the size of each group of digits in formatted monetary quantities. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.11.2.1p3 — `char *positive_sign` — the string used to indicate a nonnegative-valued formatted monetary quantity. — mcc:✓ gcc:✓ clang:✓ *(probed: "" in "C")*
- [✓] §7.11.2.1p3 — `char *negative_sign` — the string used to indicate a negative-valued formatted monetary quantity. — mcc:✓ gcc:✓ clang:✓ *(probed: "" in "C")*
- [✓] §7.11.2.1p3 — `char *currency_symbol` — the local currency symbol applicable to the current locale. — mcc:✓ gcc:✓ clang:✓ *(probed: "" in "C")*
- [✓] §7.11.2.1p3 — `char *int_curr_symbol` — the international currency symbol; the first three characters hold the alphabetic ISO 4217 currency symbol and the fourth character (immediately preceding the null) is the separator between the symbol and the monetary quantity. — mcc:✓ gcc:✓ clang:✓ *(probed: "" in "C")*
- [✓] §7.11.2.1p3 — `char frac_digits` — the number of fractional digits (after the decimal-point) to display in a locally formatted monetary quantity. — mcc:✓ gcc:✓ clang:✓ *(probed: CHAR_MAX in "C")*
- [✓] §7.11.2.1p3 — `char p_cs_precedes` — 1 or 0 if `currency_symbol` respectively precedes or succeeds the value for a nonnegative locally formatted monetary quantity. — mcc:✓ gcc:✓ clang:✓ *(probed: CHAR_MAX in "C")*
- [✓] §7.11.2.1p3 — `char n_cs_precedes` — 1 or 0 if `currency_symbol` respectively precedes or succeeds the value for a negative locally formatted monetary quantity. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.11.2.1p3 — `char p_sep_by_space` — value indicating the separation of `currency_symbol`, the sign string, and the value for a nonnegative locally formatted monetary quantity. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.11.2.1p3 — `char n_sep_by_space` — value indicating the separation of `currency_symbol`, the sign string, and the value for a negative locally formatted monetary quantity. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.11.2.1p3 — `char p_sign_posn` — value indicating the positioning of `positive_sign` for a nonnegative locally formatted monetary quantity. — mcc:✓ gcc:✓ clang:✓ *(probed: CHAR_MAX in "C")*
- [✓] §7.11.2.1p3 — `char n_sign_posn` — value indicating the positioning of `negative_sign` for a negative locally formatted monetary quantity. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.11.2.1p3 — `char int_frac_digits` — the number of fractional digits (after the decimal-point) to display in an internationally formatted monetary quantity. — mcc:✓ gcc:✓ clang:✓ *(probed: CHAR_MAX in "C")*
- [✓] §7.11.2.1p3 — `char int_p_cs_precedes` — 1 or 0 if `int_curr_symbol` respectively precedes or succeeds the value for a nonnegative internationally formatted monetary quantity. *(C99 addition; not in C90.)* — mcc:✓ gcc:✓ clang:✓ *(probed: CHAR_MAX in "C")*
- [✓] §7.11.2.1p3 — `char int_n_cs_precedes` — 1 or 0 if `int_curr_symbol` respectively precedes or succeeds the value for a negative internationally formatted monetary quantity. *(C99 addition; not in C90.)* — mcc:✓ gcc:✓ clang:✓
- [✓] §7.11.2.1p3 — `char int_p_sep_by_space` — value indicating the separation of `int_curr_symbol`, the sign string, and the value for a nonnegative internationally formatted monetary quantity. *(C99 addition; not in C90.)* — mcc:✓ gcc:✓ clang:✓
- [✓] §7.11.2.1p3 — `char int_n_sep_by_space` — value indicating the separation of `int_curr_symbol`, the sign string, and the value for a negative internationally formatted monetary quantity. *(C99 addition; not in C90.)* — mcc:✓ gcc:✓ clang:✓ *(probed: CHAR_MAX in "C")*
- [✓] §7.11.2.1p3 — `char int_p_sign_posn` — value indicating the positioning of `positive_sign` for a nonnegative internationally formatted monetary quantity. *(C99 addition; not in C90.)* — mcc:✓ gcc:✓ clang:✓ *(probed: CHAR_MAX in "C")*
- [✓] §7.11.2.1p3 — `char int_n_sign_posn` — value indicating the positioning of `negative_sign` for a negative internationally formatted monetary quantity. *(C99 addition; not in C90.)* — mcc:✓ gcc:✓ clang:✓
- §7.11.2.1p4 — In `grouping`/`mon_grouping`: an element value of `CHAR_MAX` means no further grouping is to be performed. — (spec; host-provided, not separately testable)
- §7.11.2.1p4 — In `grouping`/`mon_grouping`: an element value of `0` means the previous element is to be repeatedly used for the remainder of the digits. — (spec; host-provided, not separately testable)
- §7.11.2.1p4 — In `grouping`/`mon_grouping`: any other (positive) value is the number of digits composing the current group; the next element gives the size of the next group of digits before the current group. — (spec; host-provided, not separately testable)
- §7.11.2.1p5 — `p_sep_by_space`/`n_sep_by_space`/`int_p_sep_by_space`/`int_n_sep_by_space` are interpreted as: `0` — no space separates the currency symbol and value; `1` — if the currency symbol and sign string are adjacent, a space separates them from the value, otherwise a space separates the currency symbol from the value; `2` — if the currency symbol and sign string are adjacent, a space separates them, otherwise a space separates the sign string from the value. (For an `int_` member, the fourth character of `int_curr_symbol` is used in place of a space.) — (spec; host-provided, not separately testable)
- §7.11.2.1p6 — `p_sign_posn`/`n_sign_posn`/`int_p_sign_posn`/`int_n_sign_posn` are interpreted as: `0` — parentheses surround the quantity and currency symbol; `1` — the sign string precedes the quantity and currency symbol; `2` — the sign string succeeds the quantity and currency symbol; `3` — the sign string immediately precedes the currency symbol; `4` — the sign string immediately succeeds the currency symbol. — (spec; host-provided, not separately testable)
- [✓] §7.11.2.1p7 — In the "C" locale: `decimal_point` shall be `"."`; and `thousands_sep`, `grouping`, `mon_decimal_point`, `mon_thousands_sep`, `mon_grouping`, `positive_sign`, `negative_sign`, `currency_symbol`, and `int_curr_symbol` shall each be `""`. — mcc:✓ gcc:✓ clang:✓ *(probed: exact match on all three)*
- [✓] §7.11.2.1p7 — In the "C" locale: `frac_digits`, `p_cs_precedes`, `n_cs_precedes`, `p_sep_by_space`, `n_sep_by_space`, `p_sign_posn`, `n_sign_posn`, `int_frac_digits`, `int_p_cs_precedes`, `int_n_cs_precedes`, `int_p_sep_by_space`, `int_n_sep_by_space`, `int_p_sign_posn`, and `int_n_sign_posn` shall each be `CHAR_MAX`. — mcc:✓ gcc:✓ clang:✓ *(probed: all = 127 on all three)*
- [✓] §7.11.2.1p8 — Returns: `localeconv` returns a pointer to the filled-in object; the structure it points to shall not be modified by the program, but may be overwritten by a subsequent call to `localeconv`. — mcc:✓ gcc:✓ clang:✓ *(probed: non-null pointer returned)*
- §7.11.2.1p8 — Returns: in addition, calls to `setlocale` with category `LC_ALL`, `LC_MONETARY`, or `LC_NUMERIC` may overwrite the contents of the structure pointed to by a `localeconv` return value. — (spec; host-provided, not separately testable)
- [ ] §7.11.2.1p9 (EXAMPLE) — Informative example tables illustrating `p_sign_posn`/`n_sign_posn`, `p_cs_precedes`, and `p_sep_by_space` formatting rules (non-normative). — (spec; informative, not testable)

#### Cross-cutting / bundled-header notes

- [✓] §7.1.2 (GAP) — `<iso646.h>` and `<limits.h>` shall each be self-contained and includable in any order any number of times. *(mcc: both bundled headers are guarded and standalone; `<locale.h>` deferred to host.)* — mcc:✓ gcc:✓ clang:✓
- [✓] §4 / §7.11 (GAP) — Because mcc ships no `runtime/include/locale.h`, full `<locale.h>` conformance in a *freestanding* configuration is **not** provided (locale is a hosted-only facility; §4p6 lists only `<float.h> <iso646.h> <limits.h> <stdarg.h> <stdbool.h> <stddef.h> <stdint.h>` (+C11 `<stdalign.h> <stdnoreturn.h>`) as freestanding-required, so this is conformant). — mcc:✓ gcc:✓ clang:✓ *(documentary; locale is not required freestanding)*


## §7.12 <math.h>

> mcc status: 247 TODO checkboxes below; **1 row carries a `~` mcc tag** (a further `~`, the `FP_CONTRACT` pragma, is shared with gcc and is not an mcc-specific gap). mcc ships **no bundled `<math.h>`** — it delegates to the host C library header (glibc here) and links the host `libm`, so every function family compiles, links, and runs identically to gcc/clang. mcc predefines `__GNUC__ 4`, so glibc's `<math.h>` takes its GCC path — `NAN` prints `nan` (not `-nan`), `signbit` returns a normalized `0`/`1`, and the type-generic classification/comparison macros use the `__builtin_*` forms, all matching gcc/clang. The one remaining mcc `~` is the implementation note that mcc bundles no `<math.h>` and delegates to glibc (a portability note, not a per-call defect).

The header `<math.h>` declares two types (`float_t`, `double_t`), declares many functions, and defines numerous macros. For every listed function family three variants exist: a `double` version with the bare name, a `float` version with an `f` suffix, and a `long double` version with an `l` suffix; the `f`-suffixed function operates on and returns `float`, and the `l`-suffixed function operates on and returns `long double`. The synopsis checkboxes below give all three prototypes for each family.

> C99 vs C11: C11 (§7.12.1p3) adds the *pole error* concept (exact infinite result as a finite argument is approached, e.g. `log(0)`); C99 instead classified these as range/divide-by-zero events without the dedicated term, and several p2 wordings for `ilogb` were tightened in C11. Differences are tagged `(C11)` / `(C99-only)` inline.

### §7.12 Introduction — types and macros

- [✓] §7.12p1 — `<math.h>` shall declare the types `float_t` and `double_t`, define the macros `HUGE_VAL`, `HUGE_VALF`, `HUGE_VALL`, `INFINITY`, `NAN`, `FP_INFINITE`, `FP_NAN`, `FP_NORMAL`, `FP_SUBNORMAL`, `FP_ZERO`, `FP_FAST_FMA`, `FP_FAST_FMAF`, `FP_FAST_FMAL`, `FP_ILOGB0`, `FP_ILOGBNAN`, `MATH_ERRNO`, `MATH_ERREXCEPT`, `math_errhandling`, and `FP_CONTRACT`, and declare all the math functions in this subclause. — mcc:✓ gcc:✓ clang:✓ (via host glibc header; all macros/decls present)
- [✓] §7.12p2 — `float_t` and `double_t` are floating types at least as wide as `float` and `double`, respectively, with `double_t` at least as wide as `float_t`. — mcc:✓ gcc:✓ clang:✓ (`sizeof(float_t)==4`, `sizeof(double_t)==8`)
- [✓] §7.12p2 — If `FLT_EVAL_METHOD` equals 0, `float_t` and `double_t` are `float` and `double`, respectively. — mcc:✓ gcc:✓ clang:✓ (`FLT_EVAL_METHOD==0` here; types are `float`/`double`)
- §7.12p2 — If `FLT_EVAL_METHOD` equals 1, `float_t` and `double_t` are both `double`. — (spec; not separately testable on this target — header-conditional)
- §7.12p2 — If `FLT_EVAL_METHOD` equals 2, `float_t` and `double_t` are both `long double`. — (spec; not separately testable on this target)
- §7.12p2 — For all other values of `FLT_EVAL_METHOD`, `float_t` and `double_t` are implementation-defined. — (spec; not separately testable)
- [✓] §7.12p3 — `HUGE_VAL` expands to a positive `double` constant expression, not necessarily representable as a `float`. — mcc:✓ gcc:✓ clang:✓ (prints `inf`, positive)
- [✓] §7.12p3 — `HUGE_VALF` and `HUGE_VALL` are respectively `float` and `long double` analogs of `HUGE_VAL`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12p4 — `INFINITY` expands to a constant expression of type `float` representing positive or unsigned infinity if available, else to a positive `float` constant expression that overflows at translation time. — mcc:✓ gcc:✓ clang:✓ (`INFINITY>0` is 1)
- [✓] §7.12p5 — `NAN` is defined if and only if the implementation supports quiet NaNs for the `float` type, and then expands to a constant expression of type `float` representing a quiet NaN. — mcc:✓ gcc:✓ clang:✓ (defined and quiet; mcc's `NAN` prints `nan` with `signbit(NAN) == 0`, matching gcc/clang. mcc predefines `__GNUC__ 4`, so glibc's `<math.h>` takes its GCC path and routes `NAN` through `__builtin_nanf`.)
- [✓] §7.12p6 — `FP_INFINITE`, `FP_NAN`, `FP_NORMAL`, `FP_SUBNORMAL`, and `FP_ZERO` each expand to an integer constant expression with a distinct value, usable in `#if` directives; they are the mutually exclusive number-classification categories returned by `fpclassify`. — mcc:✓ gcc:✓ clang:✓ (distinct `1/0/4/3/2`; usable in `#if`)
- [✓] §7.12p7 — `FP_FAST_FMA`, `FP_FAST_FMAF`, and `FP_FAST_FMAL` are optionally defined; if defined, each expands to the integer constant `1` to indicate that `fma`/`fmaf`/`fmal` (respectively) generally executes about as fast as, or faster than, a multiply and an add of `double`/`float`/`long double` operands — i.e. `fma(x,y,z)` is generally as fast as or faster than `(x*y)+z`. — mcc:✓ gcc:✓ clang:✓ (all three leave `FP_FAST_FMA` undefined on this target — consistent)
- [✓] §7.12p8 — `FP_ILOGB0` and `FP_ILOGBNAN` expand to integer constant expressions whose values are returned by `ilogb(x)` when `x` is zero or NaN, respectively. — mcc:✓ gcc:✓ clang:✓ (`FP_ILOGB0 == INT_MIN`, `FP_ILOGBNAN == INT_MIN` on glibc)
- [✓] §7.12p8 — The value of `FP_ILOGB0` shall be either `INT_MIN` or `-INT_MAX`. — mcc:✓ gcc:✓ clang:✓ (`INT_MIN` = -2147483648)
- [✓] §7.12p8 — The value of `FP_ILOGBNAN` shall be either `INT_MAX` or `INT_MIN`. — mcc:✓ gcc:✓ clang:✓ (`INT_MIN` on glibc)
- [✓] §7.12p9 — `MATH_ERRNO` and `MATH_ERREXCEPT` expand to the integer constant expressions `1` and `2`, respectively. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12p9 — `math_errhandling` expands to an expression of type `int` whose value is `MATH_ERRNO`, `MATH_ERREXCEPT`, or the bitwise OR of both. — mcc:✓ gcc:✓ clang:✓ (value `3` = both)
- §7.12p9 — The value of `math_errhandling` is constant for the duration of the program. — (spec; not separately testable)
- §7.12p9 — It is unspecified whether `math_errhandling` is a macro or an identifier with external linkage; where a macro is defined, `#undef` may be used to access an actual function, and an identifier with external linkage exists in either case (a macro definition does not suppress the function). — (spec; not separately testable)
- §7.12p9 — The expression `math_errhandling & MATH_ERREXCEPT` can be nonzero only if `FE_DIVBYZERO`, `FE_INVALID`, or `FE_OVERFLOW` (from `<fenv.h>`) is defined. — (spec; not separately testable)
- [~] §7.12 (added) — mcc ships **no bundled `<math.h>`** in `runtime/include/`; it relies on the host C library's `<math.h>` and `libm`. — mcc:~ gcc:✓ clang:✓ (works on a glibc host; a non-glibc / freestanding host would have no `<math.h>` at all, whereas gcc/clang are typically paired with their own runtime story. Portability gap, not a per-call defect.)
- [✓] §7.12 (added) — mcc **predefines `__GNUC__ 4`** (`__GNUC_MINOR__ 2`, `__GNUC_PATCHLEVEL__ 1`), so glibc's `<math.h>` takes its GCC path for the type-generic classification/comparison macros. — mcc:✓ gcc:✓ clang:✓ (the normalized `signbit`, the non-trapping `isgreater`-family, and the `+nan` `NAN` all follow from this, matching the gcc==clang consensus.)

### §7.12.1 Treatment of error conditions

- §7.12.1p1 — The behavior of each function is specified for arguments within its domain; a function is not required to verify that its argument lies within the function's domain. — (spec; not separately testable)
- [ ] §7.12.1p2 — A domain error occurs if an input argument is outside the domain over which the mathematical function is defined; on a domain error the function returns an implementation-defined value. — (spec; library/`libm` behavior, shared across all three)
- [✓] §7.12.1p2 — On a domain error, if `math_errhandling & MATH_ERRNO` is nonzero, `errno` acquires the value `EDOM`. — mcc:✓ gcc:✓ clang:✓ (shared glibc `libm`; e.g. `sqrt(-1)` sets `errno==EDOM`)
- [✓] §7.12.1p2 — On a domain error, if `math_errhandling & MATH_ERREXCEPT` is nonzero, the "invalid" floating-point exception is raised. — mcc:✓ gcc:✓ clang:✓ (shared `libm`)
- [ ] §7.12.1p3 — (C11) A pole error (singularity, infinitary) occurs if the mathematical function has an exact infinite result as the finite input argument(s) are approached in the limit (e.g. `log(0.0)`). — (spec; C11 terminology — note mcc defaults `__STDC_VERSION__` to C99/199901L, but the host header still defines the C11 machinery)
- [✓] §7.12.1p3 — (C11) On a pole error, the function returns `HUGE_VAL`, `HUGE_VALF`, or `HUGE_VALL` according to the return type, with the same sign as the correct value of the function. — mcc:✓ gcc:✓ clang:✓ (shared `libm`; `log(0)` returns `-HUGE_VAL`)
- [✓] §7.12.1p3 — (C11) On a pole error, if `math_errhandling & MATH_ERRNO` is nonzero, `errno` acquires the value `ERANGE`. — mcc:✓ gcc:✓ clang:✓ (shared `libm`)
- [✓] §7.12.1p3 — (C11) On a pole error, if `math_errhandling & MATH_ERREXCEPT` is nonzero, the "divide-by-zero" floating-point exception is raised. — mcc:✓ gcc:✓ clang:✓ (shared `libm`)
- §7.12.1p4 — A range error occurs if the mathematical result of the function cannot be represented in an object of the specified type, owing to extreme magnitude (overflow or underflow). — (spec; not separately testable)
- §7.12.1p5 — A floating result overflows if the magnitude of the (finite) mathematical result is so large that the result cannot be represented, without extraordinary roundoff error, in an object of the specified type. — (spec; not separately testable)
- [✓] §7.12.1p5 — If a floating result overflows and the default rounding mode is in effect, the function returns `HUGE_VAL`, `HUGE_VALF`, or `HUGE_VALL` according to the return type, with the sign of the correct value; if `math_errhandling & MATH_ERRNO` is nonzero, `errno` acquires the value `ERANGE`; if `math_errhandling & MATH_ERREXCEPT` is nonzero, the "overflow" floating-point exception is raised. — mcc:✓ gcc:✓ clang:✓ (shared `libm`; e.g. `exp(1000)` → `HUGE_VAL`, `errno==ERANGE`)
- §7.12.1p6 — A floating result underflows if the magnitude of the mathematical result is so small that the result cannot be represented, without extraordinary roundoff error, in an object of the specified type. — (spec; not separately testable)
- [ ] §7.12.1p6 — If the result underflows, the function returns an implementation-defined value whose magnitude is no greater than the smallest normalized positive number in the specified type; whether `errno` acquires `ERANGE` (when `math_errhandling & MATH_ERRNO` is nonzero) is implementation-defined, and whether the "underflow" floating-point exception is raised (when `math_errhandling & MATH_ERREXCEPT` is nonzero) is implementation-defined. — (spec; implementation-defined; shared `libm`)

### §7.12.2 The FP_CONTRACT pragma

- [✓] §7.12.2p1 — Synopsis: `#include <math.h>` then `#pragma STDC FP_CONTRACT on-off-switch`. — mcc:✓ gcc:~ clang:✓ (mcc and clang accept silently; gcc warns `-Wunknown-pragmas` and ignores it — all three accept without error)
- [~] §7.12.2p1 — The `FP_CONTRACT` pragma allows (state "on") or disallows (state "off") the implementation to contract expressions (§6.5). — mcc:~ gcc:~ clang:✓ (none of the three on this target act on the directive at default `-O`; mcc parses-and-ignores it. Effect not observable.)
- §7.12.2p1 — Each pragma may occur either outside external declarations or preceding all explicit declarations and statements inside a compound statement; used in any other context, the behavior is undefined. — (spec; UB placement — not separately testable)
- [ ] §7.12.2p1 — Outside external declarations, the pragma takes effect from its occurrence until another `FP_CONTRACT` pragma is encountered or until the end of the translation unit. — (spec; not observable as mcc does not contract)
- [ ] §7.12.2p1 — Inside a compound statement, the pragma takes effect from its occurrence until another `FP_CONTRACT` pragma (including within a nested compound statement) or until the end of the compound statement; at the end of that compound statement the pragma state is restored to its condition just before the statement. — (spec; not observable)
- [ ] §7.12.2p1 — The default state ("on" or "off") for the pragma is implementation-defined. — (spec; implementation-defined)

### §7.12.3 Classification macros

- §7.12.3p1 — In the synopses of this subclause, `real-floating` indicates that the argument shall be an expression of real floating type. — (spec; not separately testable)

#### §7.12.3.1 The fpclassify macro
- [✓] §7.12.3.1p1 — Synopsis: `int fpclassify(real-floating x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.3.1p2 — `fpclassify` classifies its argument value as NaN, infinite, normal, subnormal, zero, or into another implementation-defined category; an argument represented in a wider format than its semantic type is first converted to its semantic type, so classification is based on the type of the argument. — mcc:✓ gcc:✓ clang:✓ (type-generic over `float`/`double`/`long double`)
- [✓] §7.12.3.1p3 — `fpclassify` returns the value of the number-classification macro (`FP_NAN`, `FP_INFINITE`, `FP_NORMAL`, `FP_SUBNORMAL`, or `FP_ZERO`) appropriate to the value of its argument. — mcc:✓ gcc:✓ clang:✓ (normal→4, NaN→0, inf→1, zero→2, subnormal→3 — all match)

#### §7.12.3.2 The isfinite macro
- [✓] §7.12.3.2p1 — Synopsis: `int isfinite(real-floating x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.3.2p2 — `isfinite` determines whether its argument has a finite value (zero, subnormal, or normal — not infinite or NaN), after first converting an argument in a wider format to its semantic type. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.3.2p3 — `isfinite` returns a nonzero value if and only if its argument has a finite value. — mcc:✓ gcc:✓ clang:✓ (finite→1, inf→0, NaN→0)

#### §7.12.3.3 The isinf macro
- [✓] §7.12.3.3p1 — Synopsis: `int isinf(real-floating x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.3.3p2 — `isinf` determines whether its argument value is an infinity (positive or negative), after first converting an argument in a wider format to its semantic type. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.3.3p3 — `isinf` returns a nonzero value if and only if its argument has an infinite value. — mcc:✓ gcc:✓ clang:✓ (`isinf(INFINITY)`→1, normalized)

#### §7.12.3.4 The isnan macro
- [✓] §7.12.3.4p1 — Synopsis: `int isnan(real-floating x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.3.4p2 — `isnan` determines whether its argument value is a NaN, after first converting an argument in a wider format to its semantic type (so the determination is based on the type of the argument). — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.3.4p3 — `isnan` returns a nonzero value if and only if its argument has a NaN value. — mcc:✓ gcc:✓ clang:✓ (`isnan(NAN)`→1, normalized)

#### §7.12.3.5 The isnormal macro
- [✓] §7.12.3.5p1 — Synopsis: `int isnormal(real-floating x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.3.5p2 — `isnormal` determines whether its argument has a normal value (not zero, subnormal, infinite, or NaN), after first converting an argument in a wider format to its semantic type. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.3.5p3 — `isnormal` returns a nonzero value if and only if its argument has a normal value. — mcc:✓ gcc:✓ clang:✓ (normal→1)

#### §7.12.3.6 The signbit macro
- [✓] §7.12.3.6p1 — Synopsis: `int signbit(real-floating x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.3.6p2 — `signbit` determines whether the sign of its argument value is negative. — mcc:✓ gcc:✓ clang:✓ (correct sign of `-0.0`, `-3.0`)
- [✓] §7.12.3.6p3 — `signbit` returns a nonzero value if and only if the sign of its argument value is negative. — mcc:✓ gcc:✓ clang:✓ (mcc returns the normalized `0`/`1` — `signbit(-3.0)`→`1`, `signbit(-0.0)`→`1`, `signbit(3.0)`→`0` — matching gcc/clang, because mcc predefines `__GNUC__ 4` so glibc's `<math.h>` takes its GCC `__builtin_signbit` path.)

### §7.12.4 Trigonometric functions

#### §7.12.4.1 The acos functions
- [✓] §7.12.4.1p1 — Synopsis: `double acos(double x);`, `float acosf(float x);`, `long double acosl(long double x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.4.1p2 — The `acos` functions compute the principal value of the arc cosine of `x`; a domain error occurs for arguments not in the interval [−1, +1]. — mcc:✓ gcc:✓ clang:✓ (shared `libm`)
- [✓] §7.12.4.1p3 — The `acos` functions return `arccos x` in the interval [0, π] radians. — mcc:✓ gcc:✓ clang:✓

#### §7.12.4.2 The asin functions
- [✓] §7.12.4.2p1 — Synopsis: `double asin(double x);`, `float asinf(float x);`, `long double asinl(long double x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.4.2p2 — The `asin` functions compute the principal value of the arc sine of `x`; a domain error occurs for arguments not in the interval [−1, +1]. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.4.2p3 — The `asin` functions return `arcsin x` in the interval [−π/2, +π/2] radians. — mcc:✓ gcc:✓ clang:✓

#### §7.12.4.3 The atan functions
- [✓] §7.12.4.3p1 — Synopsis: `double atan(double x);`, `float atanf(float x);`, `long double atanl(long double x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.4.3p2 — The `atan` functions compute the principal value of the arc tangent of `x`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.4.3p3 — The `atan` functions return `arctan x` in the interval [−π/2, +π/2] radians. — mcc:✓ gcc:✓ clang:✓

#### §7.12.4.4 The atan2 functions
- [✓] §7.12.4.4p1 — Synopsis: `double atan2(double y, double x);`, `float atan2f(float y, float x);`, `long double atan2l(long double y, long double x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.4.4p2 — The `atan2` functions compute the arc tangent of `y/x`, using the signs of both arguments to determine the quadrant; a domain error may occur if both arguments are zero. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.4.4p3 — The `atan2` functions return `arctan(y/x)` in the interval [−π, +π] radians. — mcc:✓ gcc:✓ clang:✓

#### §7.12.4.5 The cos functions
- [✓] §7.12.4.5p1 — Synopsis: `double cos(double x);`, `float cosf(float x);`, `long double cosl(long double x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.4.5p2 — The `cos` functions compute the cosine of `x` (measured in radians). — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.4.5p3 — The `cos` functions return `cos x`. — mcc:✓ gcc:✓ clang:✓ (`cos(1)`=0.540302)

#### §7.12.4.6 The sin functions
- [✓] §7.12.4.6p1 — Synopsis: `double sin(double x);`, `float sinf(float x);`, `long double sinl(long double x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.4.6p2 — The `sin` functions compute the sine of `x` (measured in radians). — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.4.6p3 — The `sin` functions return `sin x`. — mcc:✓ gcc:✓ clang:✓ (`sin(1)`=0.841471; `sinl`/`sinf` match)

#### §7.12.4.7 The tan functions
- [✓] §7.12.4.7p1 — Synopsis: `double tan(double x);`, `float tanf(float x);`, `long double tanl(long double x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.4.7p2 — The `tan` functions compute the tangent of `x` (measured in radians). — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.4.7p3 — The `tan` functions return `tan x`. — mcc:✓ gcc:✓ clang:✓ (`tan(1)`=1.55741)

### §7.12.5 Hyperbolic functions

#### §7.12.5.1 The acosh functions
- [✓] §7.12.5.1p1 — Synopsis: `double acosh(double x);`, `float acoshf(float x);`, `long double acoshl(long double x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.5.1p2 — The `acosh` functions compute the (nonnegative) arc hyperbolic cosine of `x`; a domain error occurs for arguments less than 1. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.5.1p3 — The `acosh` functions return `arcosh x` in the interval [0, +∞]. — mcc:✓ gcc:✓ clang:✓

#### §7.12.5.2 The asinh functions
- [✓] §7.12.5.2p1 — Synopsis: `double asinh(double x);`, `float asinhf(float x);`, `long double asinhl(long double x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.5.2p2 — The `asinh` functions compute the arc hyperbolic sine of `x`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.5.2p3 — The `asinh` functions return `arsinh x`. — mcc:✓ gcc:✓ clang:✓

#### §7.12.5.3 The atanh functions
- [✓] §7.12.5.3p1 — Synopsis: `double atanh(double x);`, `float atanhf(float x);`, `long double atanhl(long double x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.5.3p2 — The `atanh` functions compute the arc hyperbolic tangent of `x`; a domain error occurs for arguments not in the interval [−1, +1]; a pole error may occur if the argument equals −1 or +1. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.5.3p3 — The `atanh` functions return `artanh x`. — mcc:✓ gcc:✓ clang:✓

#### §7.12.5.4 The cosh functions
- [✓] §7.12.5.4p1 — Synopsis: `double cosh(double x);`, `float coshf(float x);`, `long double coshl(long double x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.5.4p2 — The `cosh` functions compute the hyperbolic cosine of `x`; a range error occurs if the magnitude of `x` is too large. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.5.4p3 — The `cosh` functions return `cosh x`. — mcc:✓ gcc:✓ clang:✓

#### §7.12.5.5 The sinh functions
- [✓] §7.12.5.5p1 — Synopsis: `double sinh(double x);`, `float sinhf(float x);`, `long double sinhl(long double x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.5.5p2 — The `sinh` functions compute the hyperbolic sine of `x`; a range error occurs if the magnitude of `x` is too large. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.5.5p3 — The `sinh` functions return `sinh x`. — mcc:✓ gcc:✓ clang:✓

#### §7.12.5.6 The tanh functions
- [✓] §7.12.5.6p1 — Synopsis: `double tanh(double x);`, `float tanhf(float x);`, `long double tanhl(long double x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.5.6p2 — The `tanh` functions compute the hyperbolic tangent of `x`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.5.6p3 — The `tanh` functions return `tanh x`. — mcc:✓ gcc:✓ clang:✓

### §7.12.6 Exponential and logarithmic functions

#### §7.12.6.1 The exp functions
- [✓] §7.12.6.1p1 — Synopsis: `double exp(double x);`, `float expf(float x);`, `long double expl(long double x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.6.1p2 — The `exp` functions compute the base-e exponential of `x`; a range error occurs if the magnitude of `x` is too large. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.6.1p3 — The `exp` functions return `e^x`. — mcc:✓ gcc:✓ clang:✓ (`exp(1)`=2.71828)

#### §7.12.6.2 The exp2 functions
- [✓] §7.12.6.2p1 — Synopsis: `double exp2(double x);`, `float exp2f(float x);`, `long double exp2l(long double x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.6.2p2 — The `exp2` functions compute the base-2 exponential of `x`; a range error occurs if the magnitude of `x` is too large. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.6.2p3 — The `exp2` functions return `2^x`. — mcc:✓ gcc:✓ clang:✓

#### §7.12.6.3 The expm1 functions
- [✓] §7.12.6.3p1 — Synopsis: `double expm1(double x);`, `float expm1f(float x);`, `long double expm1l(long double x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.6.3p2 — The `expm1` functions compute the base-e exponential of the argument minus 1; a range error occurs if `x` is too large (for small-magnitude `x`, `expm1(x)` is expected to be more accurate than `exp(x) - 1`). — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.6.3p3 — The `expm1` functions return `e^x − 1`. — mcc:✓ gcc:✓ clang:✓

#### §7.12.6.4 The frexp functions
- [✓] §7.12.6.4p1 — Synopsis: `double frexp(double value, int *exp);`, `float frexpf(float value, int *exp);`, `long double frexpl(long double value, int *exp);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.6.4p2 — The `frexp` functions break a floating-point number into a normalized fraction and an integral power of 2, storing the integer power of 2 in the `int` object pointed to by `exp`. — mcc:✓ gcc:✓ clang:✓ (`frexp(8,&e)`→0.5, `e`=4)
- §7.12.6.4p3 — If `value` is not a floating-point number or if the integral power of 2 is outside the range of `int`, the results are unspecified. — (spec; not separately testable)
- [✓] §7.12.6.4p3 — The `frexp` functions return a value `x` with magnitude in the interval [1/2, 1) or zero, such that `value` equals `x × 2^(*exp)`; if `value` is zero, both parts of the result (the return value and `*exp`) are zero. — mcc:✓ gcc:✓ clang:✓

#### §7.12.6.5 The ilogb functions
- [✓] §7.12.6.5p1 — Synopsis: `int ilogb(double x);`, `int ilogbf(float x);`, `int ilogbl(long double x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.6.5p2 — The `ilogb` functions extract the exponent of `x` as a signed `int` value; if `x` is zero they return `FP_ILOGB0`, if `x` is infinite they return `INT_MAX`, if `x` is a NaN they return `FP_ILOGBNAN`, otherwise they are equivalent to calling the corresponding `logb` function and casting the returned value to `int`. — mcc:✓ gcc:✓ clang:✓ (`ilogb(8)`=3)
- [ ] §7.12.6.5p2 — (C11) A domain error or range error may occur if `x` is zero, infinite, or NaN, and if the correct value is outside the range of the return type the numeric result is unspecified and a domain error or range error may occur. — (spec; shared `libm`)
- [✓] §7.12.6.5p3 — The `ilogb` functions return the exponent of `x` as a signed `int` value. — mcc:✓ gcc:✓ clang:✓

#### §7.12.6.6 The ldexp functions
- [✓] §7.12.6.6p1 — Synopsis: `double ldexp(double x, int exp);`, `float ldexpf(float x, int exp);`, `long double ldexpl(long double x, int exp);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.6.6p2 — The `ldexp` functions multiply a floating-point number by an integral power of 2; a range error may occur. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.6.6p3 — The `ldexp` functions return `x × 2^exp`. — mcc:✓ gcc:✓ clang:✓ (`ldexp(1,4)`=16)

#### §7.12.6.7 The log functions
- [✓] §7.12.6.7p1 — Synopsis: `double log(double x);`, `float logf(float x);`, `long double logl(long double x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.6.7p2 — The `log` functions compute the base-e (natural) logarithm of `x`; a domain error occurs if the argument is negative; a pole error may occur if the argument is zero. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.6.7p3 — The `log` functions return `log_e x`. — mcc:✓ gcc:✓ clang:✓

#### §7.12.6.8 The log10 functions
- [✓] §7.12.6.8p1 — Synopsis: `double log10(double x);`, `float log10f(float x);`, `long double log10l(long double x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.6.8p2 — The `log10` functions compute the base-10 (common) logarithm of `x`; a domain error occurs if the argument is negative; a pole error may occur if the argument is zero. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.6.8p3 — The `log10` functions return `log_10 x`. — mcc:✓ gcc:✓ clang:✓

#### §7.12.6.9 The log1p functions
- [✓] §7.12.6.9p1 — Synopsis: `double log1p(double x);`, `float log1pf(float x);`, `long double log1pl(long double x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.6.9p2 — The `log1p` functions compute the base-e (natural) logarithm of 1 plus the argument; a domain error occurs if the argument is less than −1; a pole error may occur if the argument equals −1 (for small-magnitude `x`, `log1p(x)` is expected to be more accurate than `log(1 + x)`). — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.6.9p3 — The `log1p` functions return `log_e(1 + x)`. — mcc:✓ gcc:✓ clang:✓

#### §7.12.6.10 The log2 functions
- [✓] §7.12.6.10p1 — Synopsis: `double log2(double x);`, `float log2f(float x);`, `long double log2l(long double x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.6.10p2 — The `log2` functions compute the base-2 logarithm of `x`; a domain error occurs if the argument is less than zero; a pole error may occur if the argument is zero. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.6.10p3 — The `log2` functions return `log_2 x`. — mcc:✓ gcc:✓ clang:✓

#### §7.12.6.11 The logb functions
- [✓] §7.12.6.11p1 — Synopsis: `double logb(double x);`, `float logbf(float x);`, `long double logbl(long double x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.6.11p2 — The `logb` functions extract the exponent of `x` as a signed integer value in floating-point format; a subnormal `x` is treated as if normalized, so that for positive finite `x`, `1 ≤ x × FLT_RADIX^(−logb(x)) < FLT_RADIX`. — mcc:✓ gcc:✓ clang:✓ (`logb(8)`=3)
- [ ] §7.12.6.11p2 — A domain error or pole error may occur if the argument is zero. — (spec; shared `libm`)
- [✓] §7.12.6.11p3 — The `logb` functions return the signed exponent of `x`. — mcc:✓ gcc:✓ clang:✓

#### §7.12.6.12 The modf functions
- [✓] §7.12.6.12p1 — Synopsis: `double modf(double value, double *iptr);`, `float modff(float value, float *iptr);`, `long double modfl(long double value, long double *iptr);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.6.12p2 — The `modf` functions break `value` into integral and fractional parts, each with the same type and sign as the argument, storing the integral part (in floating-point format) in the object pointed to by `iptr`. — mcc:✓ gcc:✓ clang:✓ (`modf(3.5,&ip)`→0.5, `ip`=3)
- [✓] §7.12.6.12p3 — The `modf` functions return the signed fractional part of `value`. — mcc:✓ gcc:✓ clang:✓

#### §7.12.6.13 The scalbn and scalbln functions
- [✓] §7.12.6.13p1 — Synopsis: `double scalbn(double x, int n);`, `float scalbnf(float x, int n);`, `long double scalbnl(long double x, int n);`, `double scalbln(double x, long int n);`, `float scalblnf(float x, long int n);`, `long double scalblnl(long double x, long int n);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.6.13p2 — The `scalbn` and `scalbln` functions compute `x × FLT_RADIX^n` efficiently, normally not by computing `FLT_RADIX^n` explicitly; a range error may occur. — mcc:✓ gcc:✓ clang:✓ (`scalbn(1,3)`=8)
- [✓] §7.12.6.13p3 — The `scalbn` and `scalbln` functions return `x × FLT_RADIX^n`. — mcc:✓ gcc:✓ clang:✓

### §7.12.7 Power and absolute-value functions

#### §7.12.7.1 The cbrt functions
- [✓] §7.12.7.1p1 — Synopsis: `double cbrt(double x);`, `float cbrtf(float x);`, `long double cbrtl(long double x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.7.1p2 — The `cbrt` functions compute the real cube root of `x`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.7.1p3 — The `cbrt` functions return `x^(1/3)`. — mcc:✓ gcc:✓ clang:✓ (`cbrt(27)`=3)

#### §7.12.7.2 The fabs functions
- [✓] §7.12.7.2p1 — Synopsis: `double fabs(double x);`, `float fabsf(float x);`, `long double fabsl(long double x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.7.2p2 — The `fabs` functions compute the absolute value of a floating-point number `x`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.7.2p3 — The `fabs` functions return `|x|`. — mcc:✓ gcc:✓ clang:✓

#### §7.12.7.3 The hypot functions
- [✓] §7.12.7.3p1 — Synopsis: `double hypot(double x, double y);`, `float hypotf(float x, float y);`, `long double hypotl(long double x, long double y);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.7.3p2 — The `hypot` functions compute the square root of the sum of the squares of `x` and `y`, without undue overflow or underflow; a range error may occur. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.7.3p3 — The `hypot` functions return `√(x² + y²)`. — mcc:✓ gcc:✓ clang:✓ (`hypot(3,4)`=5)

#### §7.12.7.4 The pow functions
- [✓] §7.12.7.4p1 — Synopsis: `double pow(double x, double y);`, `float powf(float x, float y);`, `long double powl(long double x, long double y);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.7.4p2 — The `pow` functions compute `x` raised to the power `y`; a domain error occurs if `x` is finite and negative and `y` is finite and not an integer value; a domain error or pole error may occur if `x` is zero and `y` is less than or equal to zero; a range error may occur. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.7.4p3 — The `pow` functions return `x^y`. — mcc:✓ gcc:✓ clang:✓ (`powl(2,10)`=1024)

#### §7.12.7.5 The sqrt functions
- [✓] §7.12.7.5p1 — Synopsis: `double sqrt(double x);`, `float sqrtf(float x);`, `long double sqrtl(long double x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.7.5p2 — The `sqrt` functions compute the nonnegative square root of `x`; a domain error occurs if the argument is less than zero. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.7.5p3 — The `sqrt` functions return `√x`. — mcc:✓ gcc:✓ clang:✓ (`sqrt(2)`=1.41421)

### §7.12.8 Error and gamma functions

#### §7.12.8.1 The erf functions
- [✓] §7.12.8.1p1 — Synopsis: `double erf(double x);`, `float erff(float x);`, `long double erfl(long double x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.8.1p2 — The `erf` functions compute the error function of `x`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.8.1p3 — The `erf` functions return `erf x = (2/√π) ∫₀ˣ e^(−t²) dt`. — mcc:✓ gcc:✓ clang:✓ (`erf(1)`=0.842701)

#### §7.12.8.2 The erfc functions
- [✓] §7.12.8.2p1 — Synopsis: `double erfc(double x);`, `float erfcf(float x);`, `long double erfcl(long double x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.8.2p2 — The `erfc` functions compute the complementary error function of `x`; a range error occurs if `x` is too large. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.8.2p3 — The `erfc` functions return `erfc x = 1 − erf x = (2/√π) ∫ₓ^∞ e^(−t²) dt`. — mcc:✓ gcc:✓ clang:✓

#### §7.12.8.3 The lgamma functions
- [✓] §7.12.8.3p1 — Synopsis: `double lgamma(double x);`, `float lgammaf(float x);`, `long double lgammal(long double x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.8.3p2 — The `lgamma` functions compute the natural logarithm of the absolute value of gamma of `x`; a range error occurs if `x` is too large; a pole error may occur if `x` is a negative integer or zero. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.8.3p3 — The `lgamma` functions return `log_e |Γ(x)|`. — mcc:✓ gcc:✓ clang:✓ (`lgamma(5)`=3.17805)

#### §7.12.8.4 The tgamma functions
- [✓] §7.12.8.4p1 — Synopsis: `double tgamma(double x);`, `float tgammaf(float x);`, `long double tgammal(long double x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.8.4p2 — The `tgamma` functions compute the gamma function of `x`; a domain error or pole error may occur if `x` is a negative integer or zero; a range error may occur if the magnitude of `x` is too large or too small. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.8.4p3 — The `tgamma` functions return `Γ(x)`. — mcc:✓ gcc:✓ clang:✓ (`tgamma(5)`=24)

### §7.12.9 Nearest integer functions

#### §7.12.9.1 The ceil functions
- [✓] §7.12.9.1p1 — Synopsis: `double ceil(double x);`, `float ceilf(float x);`, `long double ceill(long double x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.9.1p2 — The `ceil` functions compute the smallest integer value not less than `x`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.9.1p3 — The `ceil` functions return `⌈x⌉`, expressed as a floating-point number. — mcc:✓ gcc:✓ clang:✓

#### §7.12.9.2 The floor functions
- [✓] §7.12.9.2p1 — Synopsis: `double floor(double x);`, `float floorf(float x);`, `long double floorl(long double x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.9.2p2 — The `floor` functions compute the largest integer value not greater than `x`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.9.2p3 — The `floor` functions return `⌊x⌋`, expressed as a floating-point number. — mcc:✓ gcc:✓ clang:✓

#### §7.12.9.3 The nearbyint functions
- [✓] §7.12.9.3p1 — Synopsis: `double nearbyint(double x);`, `float nearbyintf(float x);`, `long double nearbyintl(long double x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.9.3p2 — The `nearbyint` functions round their argument to an integer value in floating-point format, using the current rounding direction and without raising the "inexact" floating-point exception. — mcc:✓ gcc:✓ clang:✓ (`nearbyint(2.5)`=2 round-to-even)
- [✓] §7.12.9.3p3 — The `nearbyint` functions return the rounded integer value. — mcc:✓ gcc:✓ clang:✓

#### §7.12.9.4 The rint functions
- [✓] §7.12.9.4p1 — Synopsis: `double rint(double x);`, `float rintf(float x);`, `long double rintl(long double x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.9.4p2 — The `rint` functions differ from the `nearbyint` functions only in that the `rint` functions may raise the "inexact" floating-point exception if the result differs in value from the argument. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.9.4p3 — The `rint` functions return the rounded integer value, using the current rounding direction. — mcc:✓ gcc:✓ clang:✓ (`rint(2.5)`=2)

#### §7.12.9.5 The lrint and llrint functions
- [✓] §7.12.9.5p1 — Synopsis: `long int lrint(double x);`, `long int lrintf(float x);`, `long int lrintl(long double x);`, `long long int llrint(double x);`, `long long int llrintf(float x);`, `long long int llrintl(long double x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.9.5p2 — The `lrint` and `llrint` functions round their argument to the nearest integer value, rounding according to the current rounding direction; if the rounded value is outside the range of the return type, the numeric result is unspecified and a domain error or range error may occur. — mcc:✓ gcc:✓ clang:✓ (`lrint(2.5)`=2)
- [✓] §7.12.9.5p3 — The `lrint` and `llrint` functions return the rounded integer value. — mcc:✓ gcc:✓ clang:✓

#### §7.12.9.6 The round functions
- [✓] §7.12.9.6p1 — Synopsis: `double round(double x);`, `float roundf(float x);`, `long double roundl(long double x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.9.6p2 — The `round` functions round their argument to the nearest integer value in floating-point format, rounding halfway cases away from zero, regardless of the current rounding direction. — mcc:✓ gcc:✓ clang:✓ (`round(2.5)`=3)
- [✓] §7.12.9.6p3 — The `round` functions return the rounded integer value. — mcc:✓ gcc:✓ clang:✓

#### §7.12.9.7 The lround and llround functions
- [✓] §7.12.9.7p1 — Synopsis: `long int lround(double x);`, `long int lroundf(float x);`, `long int lroundl(long double x);`, `long long int llround(double x);`, `long long int llroundf(float x);`, `long long int llroundl(long double x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.9.7p2 — The `lround` and `llround` functions round their argument to the nearest integer value, rounding halfway cases away from zero, regardless of the current rounding direction; if the rounded value is outside the range of the return type, the numeric result is unspecified and a domain error or range error may occur. — mcc:✓ gcc:✓ clang:✓ (`llround(2.5)`=3)
- [✓] §7.12.9.7p3 — The `lround` and `llround` functions return the rounded integer value. — mcc:✓ gcc:✓ clang:✓

#### §7.12.9.8 The trunc functions
- [✓] §7.12.9.8p1 — Synopsis: `double trunc(double x);`, `float truncf(float x);`, `long double truncl(long double x);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.9.8p2 — The `trunc` functions round their argument to the integer value, in floating-point format, nearest to but no larger in magnitude than the argument. — mcc:✓ gcc:✓ clang:✓ (`trunc(2.7)`=2)
- [✓] §7.12.9.8p3 — The `trunc` functions return the truncated integer value. — mcc:✓ gcc:✓ clang:✓

### §7.12.10 Remainder functions

#### §7.12.10.1 The fmod functions
- [✓] §7.12.10.1p1 — Synopsis: `double fmod(double x, double y);`, `float fmodf(float x, float y);`, `long double fmodl(long double x, long double y);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.10.1p2 — The `fmod` functions compute the floating-point remainder of `x/y`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.10.1p3 — The `fmod` functions return `x − n y` for some integer `n` such that, if `y` is nonzero, the result has the same sign as `x` and magnitude less than the magnitude of `y`; if `y` is zero, whether a domain error occurs or the functions return zero is implementation-defined. — mcc:✓ gcc:✓ clang:✓

#### §7.12.10.2 The remainder functions
- [✓] §7.12.10.2p1 — Synopsis: `double remainder(double x, double y);`, `float remainderf(float x, float y);`, `long double remainderl(long double x, long double y);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.10.2p2 — The `remainder` functions compute the remainder `x REM y` required by IEC 60559: when `y ≠ 0`, the result is `r = x − n y`, where `n` is the integer nearest the exact value of `x/y`; whenever `|n − x/y| = 1/2`, `n` is chosen to be even. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.10.2p3 — The `remainder` functions return `x REM y`; if `y` is zero, whether a domain error occurs or the functions return zero is implementation-defined. — mcc:✓ gcc:✓ clang:✓

#### §7.12.10.3 The remquo functions
- [✓] §7.12.10.3p1 — Synopsis: `double remquo(double x, double y, int *quo);`, `float remquof(float x, float y, int *quo);`, `long double remquol(long double x, long double y, int *quo);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.10.3p2 — The `remquo` functions compute the same remainder as the `remainder` functions and store, in the object pointed to by `quo`, a value whose sign is the sign of `x/y` and whose magnitude is congruent modulo `2^n` to the magnitude of the integral quotient of `x/y`, where `n` is an implementation-defined integer greater than or equal to 3. — mcc:✓ gcc:✓ clang:✓ (`remquo(10,3,&q)`→1, `q`=3)
- [✓] §7.12.10.3p3 — The `remquo` functions return `x REM y`; if `y` is zero, the value stored in `*quo` is unspecified and whether a domain error occurs or the functions return zero is implementation-defined. — mcc:✓ gcc:✓ clang:✓

### §7.12.11 Manipulation functions

#### §7.12.11.1 The copysign functions
- [✓] §7.12.11.1p1 — Synopsis: `double copysign(double x, double y);`, `float copysignf(float x, float y);`, `long double copysignl(long double x, long double y);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.11.1p2 — The `copysign` functions produce a value with the magnitude of `x` and the sign of `y`; they produce a NaN (with the sign of `y`) if `x` is a NaN; on implementations that represent a signed zero but do not treat negative zero consistently in arithmetic, they regard the sign of zero as positive. — mcc:✓ gcc:✓ clang:✓ (`copysign(NAN,-1)` is negative on all three)
- [✓] §7.12.11.1p3 — The `copysign` functions return a value with the magnitude of `x` and the sign of `y`. — mcc:✓ gcc:✓ clang:✓ (`copysign(3,-1)`=-3)

#### §7.12.11.2 The nan functions
- [✓] §7.12.11.2p1 — Synopsis: `double nan(const char *tagp);`, `float nanf(const char *tagp);`, `long double nanl(const char *tagp);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.11.2p2 — The call `nan("n-char-sequence")` is equivalent to `strtod("NAN(n-char-sequence)", (char**) NULL)`; `nan("")` is equivalent to `strtod("NAN()", (char**) NULL)`; if `tagp` does not point to an n-char sequence or an empty string, the call is equivalent to `strtod("NAN", (char**) NULL)`; calls to `nanf` and `nanl` are equivalent to the corresponding calls to `strtof` and `strtold`. — mcc:✓ gcc:✓ clang:✓ (library `nan("")`→`nan`, positive — distinct from the `NAN` *macro* issue at §7.12p5)
- [✓] §7.12.11.2p3 — The `nan` functions return a quiet NaN, if available, with content indicated through `tagp`; if the implementation does not support quiet NaNs, the functions return zero. — mcc:✓ gcc:✓ clang:✓

#### §7.12.11.3 The nextafter functions
- [✓] §7.12.11.3p1 — Synopsis: `double nextafter(double x, double y);`, `float nextafterf(float x, float y);`, `long double nextafterl(long double x, long double y);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.11.3p2 — The `nextafter` functions determine the next representable value, in the type of the function, after `x` in the direction of `y` (where `x` and `y` are first converted to the type of the function); they return `y` if `x` equals `y`; a range error may occur if the magnitude of `x` is the largest finite value representable in the type and the result is infinite or not representable in the type. — mcc:✓ gcc:✓ clang:✓ (`nextafter(1,2)`=0x1.0000000000001p+0)
- [✓] §7.12.11.3p3 — The `nextafter` functions return the next representable value in the specified format after `x` in the direction of `y`. — mcc:✓ gcc:✓ clang:✓

#### §7.12.11.4 The nexttoward functions
- [✓] §7.12.11.4p1 — Synopsis: `double nexttoward(double x, long double y);`, `float nexttowardf(float x, long double y);`, `long double nexttowardl(long double x, long double y);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.11.4p2 — The `nexttoward` functions are equivalent to the `nextafter` functions except that the second parameter has type `long double` and the functions return `y` converted to the type of the function if `x` equals `y`. — mcc:✓ gcc:✓ clang:✓ (`nexttoward(1,2L)`=0x1.0000000000001p+0)
- [✓] §7.12.11.4p3 — The `nexttoward` functions return the next representable value in the specified format after `x` in the direction of `y`. — mcc:✓ gcc:✓ clang:✓

### §7.12.12 Maximum, minimum, and positive difference functions

#### §7.12.12.1 The fdim functions
- [✓] §7.12.12.1p1 — Synopsis: `double fdim(double x, double y);`, `float fdimf(float x, float y);`, `long double fdiml(long double x, long double y);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.12.1p2 — The `fdim` functions determine the positive difference between their arguments: `x − y` if `x > y`, and `+0` if `x ≤ y`; a range error may occur. — mcc:✓ gcc:✓ clang:✓ (`fdim(5,2)`=3)
- [✓] §7.12.12.1p3 — The `fdim` functions return the positive difference value. — mcc:✓ gcc:✓ clang:✓

#### §7.12.12.2 The fmax functions
- [✓] §7.12.12.2p1 — Synopsis: `double fmax(double x, double y);`, `float fmaxf(float x, float y);`, `long double fmaxl(long double x, long double y);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.12.2p2 — The `fmax` functions determine the maximum numeric value of their arguments; if just one argument is a NaN, they return the other argument; if both are NaNs, they return a NaN. — mcc:✓ gcc:✓ clang:✓ (`fmax(1,2)`=2)
- [✓] §7.12.12.2p3 — The `fmax` functions return the maximum numeric value of their arguments. — mcc:✓ gcc:✓ clang:✓

#### §7.12.12.3 The fmin functions
- [✓] §7.12.12.3p1 — Synopsis: `double fmin(double x, double y);`, `float fminf(float x, float y);`, `long double fminl(long double x, long double y);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.12.3p2 — The `fmin` functions determine the minimum numeric value of their arguments; if just one argument is a NaN, they return the other argument; if both are NaNs, they return a NaN. — mcc:✓ gcc:✓ clang:✓ (`fmin(1,2)`=1)
- [✓] §7.12.12.3p3 — The `fmin` functions return the minimum numeric value of their arguments. — mcc:✓ gcc:✓ clang:✓

### §7.12.13 Floating multiply-add

#### §7.12.13.1 The fma functions
- [✓] §7.12.13.1p1 — Synopsis: `double fma(double x, double y, double z);`, `float fmaf(float x, float y, float z);`, `long double fmal(long double x, long double y, long double z);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.13.1p2 — The `fma` functions compute `(x × y) + z`, rounded as one ternary operation: they compute the value (as if) to infinite precision and round once to the result format according to the current rounding mode; a range error may occur. — mcc:✓ gcc:✓ clang:✓ (`fma(2,3,4)`=10)
- [✓] §7.12.13.1p3 — The `fma` functions return `(x × y) + z`, rounded as one ternary operation. — mcc:✓ gcc:✓ clang:✓

### §7.12.14 Comparison macros

- §7.12.14p1 — The relational and equality operators support the usual mathematical relationships between numeric values; for any ordered pair of numeric values exactly one of less, greater, and equal is true; relational operators may raise the "invalid" floating-point exception when argument values are NaNs. — (spec; not separately testable)
- §7.12.14p1 — For a NaN and a numeric value, or for two NaNs, just the unordered relationship is true; the comparison macros in this subclause are quiet (non-exception-raising) versions of the relational operators and facilitate writing efficient NaN-aware code without the "invalid" floating-point exception. — (spec; not separately testable)
- §7.12.14p2 — In the synopses of this subclause, `real-floating` indicates that each argument shall be an expression of real floating type (both arguments shall be such expressions). — (spec; not separately testable)
- [✓] §7.12.14 (added) — The comparison macros evaluate each argument exactly once (no double evaluation of operands with side effects). — mcc:✓ gcc:✓ clang:✓ (host glibc routes through `__builtin_*` / single-eval helpers)

#### §7.12.14.1 The isgreater macro
- [✓] §7.12.14.1p1 — Synopsis: `int isgreater(real-floating x, real-floating y);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.14.1p2 — `isgreater(x, y)` determines whether `x` is greater than `y`; its value is always equal to `(x) > (y)`, yet unlike `(x) > (y)` it does not raise the "invalid" floating-point exception when `x` and `y` are unordered. — mcc:✓ gcc:✓ clang:✓ (`isgreater(2,1)`=1)
- [✓] §7.12.14.1p3 — The `isgreater` macro returns the value of `(x) > (y)`. — mcc:✓ gcc:✓ clang:✓

#### §7.12.14.2 The isgreaterequal macro
- [✓] §7.12.14.2p1 — Synopsis: `int isgreaterequal(real-floating x, real-floating y);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.14.2p2 — `isgreaterequal(x, y)` determines whether `x` is greater than or equal to `y`; its value is always equal to `(x) >= (y)`, yet unlike `(x) >= (y)` it does not raise the "invalid" floating-point exception when `x` and `y` are unordered. — mcc:✓ gcc:✓ clang:✓ (`isgreaterequal(2,2)`=1)
- [✓] §7.12.14.2p3 — The `isgreaterequal` macro returns the value of `(x) >= (y)`. — mcc:✓ gcc:✓ clang:✓

#### §7.12.14.3 The isless macro
- [✓] §7.12.14.3p1 — Synopsis: `int isless(real-floating x, real-floating y);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.14.3p2 — `isless(x, y)` determines whether `x` is less than `y`; its value is always equal to `(x) < (y)`, yet unlike `(x) < (y)` it does not raise the "invalid" floating-point exception when `x` and `y` are unordered. — mcc:✓ gcc:✓ clang:✓ (`isless(2,1)`=0)
- [✓] §7.12.14.3p3 — The `isless` macro returns the value of `(x) < (y)`. — mcc:✓ gcc:✓ clang:✓

#### §7.12.14.4 The islessequal macro
- [✓] §7.12.14.4p1 — Synopsis: `int islessequal(real-floating x, real-floating y);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.14.4p2 — `islessequal(x, y)` determines whether `x` is less than or equal to `y`; its value is always equal to `(x) <= (y)`, yet unlike `(x) <= (y)` it does not raise the "invalid" floating-point exception when `x` and `y` are unordered. — mcc:✓ gcc:✓ clang:✓ (`islessequal(2,2)`=1)
- [✓] §7.12.14.4p3 — The `islessequal` macro returns the value of `(x) <= (y)`. — mcc:✓ gcc:✓ clang:✓

#### §7.12.14.5 The islessgreater macro
- [✓] §7.12.14.5p1 — Synopsis: `int islessgreater(real-floating x, real-floating y);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.14.5p2 — `islessgreater(x, y)` determines whether `x` is less than or greater than `y`; its value is similar to `(x) < (y) || (x) > (y)`, except that it does not raise the "invalid" floating-point exception when `x` and `y` are unordered (nor does it evaluate `x` and `y` twice). — mcc:✓ gcc:✓ clang:✓ (`islessgreater(2,1)`=1)
- [✓] §7.12.14.5p3 — The `islessgreater` macro returns the value of `(x) < (y) || (x) > (y)`. — mcc:✓ gcc:✓ clang:✓

#### §7.12.14.6 The isunordered macro
- [✓] §7.12.14.6p1 — Synopsis: `int isunordered(real-floating x, real-floating y);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.14.6p2 — The `isunordered` macro determines whether its arguments are unordered. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.12.14.6p3 — The `isunordered` macro returns 1 if its arguments are unordered (at least one is a NaN) and 0 otherwise. — mcc:✓ gcc:✓ clang:✓ (`isunordered(2,NAN)`=1)


## §7.13-§7.16 setjmp, signal, stdalign, stdarg

> mcc status: 64 TODO checkboxes; 2 mcc-gaps — both diagnostic-quality only (x86_64 `va_start` on a `register` parmN is not warned where the SysV macro reads the frame instead of `parmN`; `va_start` with a non-last second argument is not warned). Every *normatively required* behavior (setjmp/longjmp save-restore incl. `val==0`→1 and non-volatile-clobber rules, `_Alignas`/`_Alignof` semantics + constraint diagnostics, the four `<stdarg.h>` macros incl. `va_copy`) conforms and matches the gcc==clang consensus.

> Scope: `<setjmp.h>` (§7.13), `<signal.h>` (§7.14), `<stdalign.h>` (§7.15, C11), and `<stdarg.h>` (§7.16). All four are macro-heavy headers whose conformance burden falls almost entirely on the compiler's builtins (`setjmp`/`longjmp` register-save semantics, `__builtin_va_*`, `_Alignas`/`_Alignof`) rather than on a freestanding runtime object. In mcc, `<stdarg.h>` and `<stdalign.h>` are bundled (`runtime/include/`) and forward to `__builtin_va_*` and `__attribute__((aligned))`/`__alignof__`; `<setjmp.h>` and `<signal.h>` are taken from the host C library.
> Freestanding (§4p6 / §7.1.3): only `<stdarg.h>` and `<stdalign.h>` (plus the type/limit/bool/noreturn headers) are guaranteed; `<setjmp.h>` and `<signal.h>` are hosted-only.
> Verified bundled `<stdarg.h>` (`runtime/include/stdarg.h`): `typedef __builtin_va_list va_list;` and `va_start`/`va_arg`/`va_copy`/`va_end` → `__builtin_va_*`, plus `typedef va_list __gnuc_va_list;` and `#define _VA_LIST_DEFINED`. Verified bundled `<stdalign.h>` (`runtime/include/stdalign.h`): `alignas`→`_Alignas`, `alignof`→`_Alignof`, `__alignas_is_defined`/`__alignof_is_defined`→`1`, with pre-C11 GNU/MCC back-fill `_Alignas(t)`→`__attribute__((__aligned__(t)))`, `_Alignof(t)`→`__alignof__(t)`.
> Verified mcc rejects bundled `<varargs.h>` with an `#error` directing the user to `<stdarg.h>` (legacy K&R varargs intentionally unsupported on all three; gcc/clang likewise reject/deprecate it).

### §7.13 Non-local jumps `<setjmp.h>`

- [✓] §7.13p1 — `<setjmp.h>` defines the macro `setjmp`, and declares one function `longjmp` and one type `jmp_buf`, for bypassing the normal function call and return discipline. — mcc:✓ gcc:✓ clang:✓ (host `<setjmp.h>`; all three see the same glibc header)
- §7.13p2 — `jmp_buf` is an array type suitable for holding the information needed to restore a calling environment. The saved environment is sufficient for `longjmp` to return execution to the correct block and invocation of that block (as if it were called recursively); it does **not** include the state of the floating-point status flags, of open files, or of any other component of the abstract machine. — (spec; not separately testable)
- §7.13p3 — It is unspecified whether `setjmp` is a macro or an identifier with external linkage. If a macro definition of `setjmp` is suppressed to access an actual function, or if a program defines an external identifier named `setjmp`, the behavior is undefined (unlike other library macros, `setjmp` may **not** be accessed as a function). — (spec/UB; not separately testable)

#### §7.13.1.1 The `setjmp` macro

- [✓] §7.13.1.1p1 — Synopsis: `#include <setjmp.h>` then `int setjmp(jmp_buf env);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.13.1.1p2 (Description) — The `setjmp` macro saves its calling environment in its `jmp_buf` argument for later use by `longjmp`. — mcc:✓ gcc:✓ clang:✓ (round-trip verified: `setjmp`/`longjmp` returns to the saved block)
- [✓] §7.13.1.1p3 (Returns) — On a direct invocation `setjmp` returns 0; on return via `longjmp` it returns a nonzero value. — mcc:✓ gcc:✓ clang:✓ (verified direct→0, via-longjmp→nonzero)
- §7.13.1.1p4 (Environmental constraint) — An invocation of `setjmp` shall appear only in one of: (a) the entire controlling expression of a selection or iteration statement (`if`/`switch`/`while`/`do`/`for`); (b) one operand of a relational/equality operator, the other operand an integer constant expression, the whole being the entire controlling expression of a selection/iteration statement; (c) the operand of unary `!`, the whole being such an entire controlling expression; or (d) the entire expression of an expression statement (possibly cast to `void`). — (spec; constraint on the program, not separately testable)
- [✓] §7.13.1.1p4 (UB) — If an invocation of `setjmp` appears in any **other** context (e.g. `x = setjmp(env);`, `setjmp(env) + 1`, a function argument), the behavior is undefined. — mcc:✓ gcc:✓ clang:✓ (UB, not a required diagnostic; verified all three silently accept `int x = setjmp(b)+1;` — no compiler diagnoses this)

#### §7.13.2.1 The `longjmp` function

- [✓] §7.13.2.1p1 — Synopsis: `#include <setjmp.h>` then `void longjmp(jmp_buf env, int val);`. — mcc:✓ gcc:✓ clang:✓
- §7.13.2.1p2 (Description/UB) — `longjmp` restores the environment saved by the most recent `setjmp` invocation in the same program invocation with the corresponding `jmp_buf`. The behavior is undefined if there was no such invocation, or if the function containing the `setjmp` invocation has terminated execution in the interim, or if the `setjmp` invocation was within the scope of an identifier with variably modified type and execution has left that scope in the interim. — (spec/UB; not separately testable)
- [✓] §7.13.2.1p3 (Description) — After `longjmp`, all accessible objects have values and all other components of the abstract machine have state as of the time `longjmp` was called, **except** that automatic objects local to the function containing the corresponding `setjmp` invocation, that are not volatile-qualified and were changed between the `setjmp` and the `longjmp`, have indeterminate values. — mcc:✓ gcc:✓ clang:✓ (at `-O2`, a `volatile` automatic written before `longjmp` reliably reads back its updated value after the jump on all three; this is the compiler-side obligation)
- [✓] §7.13.2.1p4 (Returns) — Execution continues as if the corresponding `setjmp` had just returned `val`. `longjmp` cannot cause `setjmp` to return 0; if `val` is 0, `setjmp` returns 1. — mcc:✓ gcc:✓ clang:✓ (verified: `longjmp(b,0)` makes `setjmp` return 1; `longjmp(b,42)` returns 42 — host libc behavior, identical across all three)
- §7.13.2.1p5 (Environmental limit) — `longjmp` shall execute correctly in the contexts of interrupts, signals, and any of their associated functions; however, if invoked from a nested signal handler, the behavior is undefined. — (spec; host-libc/runtime, not a compiler obligation; not separately testable)

### §7.14 Signal handling `<signal.h>`

> Hosted-only: not required in a freestanding implementation (§4p6). mcc sources `<signal.h>` from the host C library, so the macro/type/function inventory is identical across all three compilers; mcc's only obligation is to compile handler/`raise` code correctly.
> C99→C11 delta: §7.14.1.1p3's handler restriction broadens to "static **or thread** storage duration that is not a lock-free atomic object", adds `quick_exit`/`_Exit` to the allowed-call list, and adds the `SIG_ERR`-return `errno`-indeterminate clause; §7.14.2.1p2 adds that `raise` shall not return until the handler does.

- [✓] §7.14p1 — `<signal.h>` declares a type and two functions and defines several macros, for handling conditions reported during execution. — mcc:✓ gcc:✓ clang:✓ (host header)
- [✓] §7.14p2 — The type defined is `sig_atomic_t`, the (possibly volatile-qualified) integer type of an object that can be accessed as an atomic entity even in the presence of asynchronous interrupts. — mcc:✓ gcc:✓ clang:✓ (`sig_atomic_t` usable; verified `volatile sig_atomic_t` handler flag)
- [✓] §7.14p3 — The macros `SIG_DFL`, `SIG_ERR`, and `SIG_IGN` expand to constant expressions with distinct values, of a type compatible with the second argument to and the return value of `signal`, and whose values compare unequal to the address of any declarable function. — mcc:✓ gcc:✓ clang:✓ (verified all three assignable to `void(*)(int)`)
- [✓] §7.14p4 — The macros `SIGABRT`, `SIGFPE`, `SIGILL`, `SIGINT`, `SIGSEGV`, `SIGTERM` are defined as positive integer constant expressions of type `int` with distinct values. — mcc:✓ gcc:✓ clang:✓ (verified identical values 6/8/4/2/11/15 on all three)
- [✓] §7.14p5 — An implementation need not generate any of these signals except via explicit `raise` calls. Additional signals and pointers to undeclarable functions, named with macros beginning `SIG`/`SIG_` + uppercase letter, may be specified; all signal numbers shall be positive. — mcc:✓ gcc:✓ clang:✓ (implementation-defined; host-defined extra signals e.g. `SIGUSR1` visible to all three)

#### §7.14.1.1 The `signal` function

- [✓] §7.14.1.1p1 — Synopsis: `#include <signal.h>` then `void (*signal(int sig, void (*func)(int)))(int);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.14.1.1p2 (Description) — `signal` chooses one of three ways to handle subsequent receipt of `sig`: `SIG_DFL` → default; `SIG_IGN` → ignore; otherwise `func` is the handler called when the signal occurs. — mcc:✓ gcc:✓ clang:✓ (host-libc semantics; compiles + dispatches identically)
- §7.14.1.1p3 (Description) — When the signal occurs and `func` points to a function, it is implementation-defined whether `signal(sig, SIG_DFL);` is executed or a set of signals (at least `sig`) is blocked, before the handler is called with argument `sig`. — (spec/impl-defined; host-libc, not separately testable)
- §7.14.1.1p3 (Async-signal-safety / UB) — If the signal occurred other than via `abort`/`raise`, the behavior is undefined if the handler refers to any object with static **(C11: or thread)** storage duration that is not a lock-free atomic object other than by assigning to a `volatile sig_atomic_t`, or calls any standard library function other than `abort`, `_Exit`, **(C11: `quick_exit`,)** or `signal` with first arg equal to the handled signal. **(C11:** if such a `signal` returns `SIG_ERR`, `errno` is indeterminate.**)** — (spec/UB; not a compiler-diagnosable constraint; not separately testable)
- §7.14.1.1p4 (Description) — At program startup, `signal(sig, SIG_IGN);` may be established for some implementation-defined signals; `signal(sig, SIG_DFL);` is established for all others. — (spec/impl-defined startup state; not separately testable)
- §7.14.1.1p5 (Description) — The implementation shall behave as if no library function calls `signal`. — (spec; host-libc property, not separately testable)
- [✓] §7.14.1.1p6 (Returns) — On success `signal` returns the value of `func` for the most recent successful `signal` call for `sig`; on failure returns `SIG_ERR` and stores a positive value in `errno`. — mcc:✓ gcc:✓ clang:✓ (verified prior-handler is returned by a second `signal` call)

#### §7.14.2.1 The `raise` function

- [✓] §7.14.2.1p1 — Synopsis: `#include <signal.h>` then `int raise(int sig);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.14.2.1p2 (Description) — `raise` carries out the actions of §7.14.1.1 for `sig`. **(C11:** if a handler is called, `raise` shall not return until after the handler returns.**)** — mcc:✓ gcc:✓ clang:✓ (verified `raise` synchronously invokes the installed handler before returning)
- [✓] §7.14.2.1p3 (Returns) — `raise` returns zero on success, nonzero on failure. — mcc:✓ gcc:✓ clang:✓ (host-libc)

### §7.15 Alignment `<stdalign.h>` (C11)

> Entire header is C11-only (added by C11; not present in C99). (In C23 the macros become keywords, out of N1570 scope.)

- [✓] §7.15p1 (C11) — `<stdalign.h>` defines four macros. — mcc:✓ gcc:✓ clang:✓ (bundled `runtime/include/stdalign.h`; defines exactly the four)
- [✓] §7.15p2 (C11) — `alignas` expands to `_Alignas`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.15p3 (C11) — `alignof` expands to `_Alignof`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.15p4 (C11) — `__alignas_is_defined` and `__alignof_is_defined` expand to the integer constant `1` and are suitable for use in `#if` preprocessing directives. — mcc:✓ gcc:✓ clang:✓ (verified both `=1` and usable in `#if`)
- §7.15p5 (C11) — A program that undefines, or `#define`s elsewhere, any of `alignas`/`alignof`/`__alignas_is_defined`/`__alignof_is_defined` has undefined behavior (per §7.1.3 reserved-identifier rules). — (spec/UB; not a required diagnostic; not separately testable)
- [✓] §7.15 (impl) — Pre-C11 / GNU/MCC back-fill: when `__STDC_VERSION__ < 201112L`, the bundled header defines `_Alignas(t)`→`__attribute__((__aligned__(t)))` and `_Alignof(t)`→`__alignof__(t)` so `alignas`/`alignof` work in older modes. — mcc:✓ (gcc/clang use their own header but offer the equivalent fallback)

> Underlying operator/specifier semantics that `<stdalign.h>` exposes (primary checkboxes live in §6.7.5 and §6.5.3.4; cross-referenced and re-probed here):
- [✓] §6.7.5p1 (C11) — `_Alignas(constant-expression)` / `_Alignas(type-name)` gives the alignment of the declared object/member; the value shall be an ICE representing a valid power-of-two alignment, and `_Alignas(type-name)` ≡ `_Alignas(_Alignof(type-name))`. Zero has no effect; the strictest applies; `_Alignas` shall not be applied to a function parameter, a typedef, a bit-field, or an object whose alignment would be weakened below its type's requirement. — mcc:✓ gcc:✓ clang:✓ (all four illegal targets rejected; `_Alignas(0)` accepted as no-op; `_Alignas(double)` type-name form == `_Alignof(double)`; verified runtime 32-byte alignment of an `alignas(32)` array)
- [✓] §6.7.5p1 (C11) — `_Alignas` on a **bit-field** is rejected. — mcc:✓ gcc:✓ clang:✓ (mcc: "error: '_Alignas' specified for a bit-field")
- [✓] §6.7.5p1 (C11) — `_Alignas` on a **typedef** is rejected. — mcc:✓ gcc:✓ clang:✓ (mcc: "error: '_Alignas' specified for a typedef")
- [✓] §6.7.5p1 (C11) — `_Alignas` on a **function parameter** is rejected. — mcc:✓ gcc:✓ clang:✓ (mcc: "error: '_Alignas' specified for a function parameter")
- [✓] §6.7.5p1 (C11) — `_Alignas` requesting an alignment **weaker** than the type's requirement (e.g. `_Alignas(1) int`) is rejected. — mcc:✓ gcc:✓ clang:✓ (mcc: "error: requested alignment is less than the minimum alignment of the type")
- [✓] §6.7.5p1 (C11) — `_Alignas` with a **non-power-of-two** value (e.g. `_Alignas(3)`) is rejected. — mcc:✓ gcc:✓ clang:✓ (mcc: "error: alignment must be a positive power of two")
- [✓] §6.7.5p1 (C11) — `_Alignas(0)` has **no effect** (accepted, alignment unchanged). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.3.4p1 (C11) — `_Alignof` shall be applied only to a parenthesized type name, and not to a function type or an incomplete type. It yields the alignment requirement of its operand type as a `size_t` ICE; for an array type the result is the alignment of the element type. — mcc:✓ gcc:✓ clang:✓ (verified `_Alignof(double)`=8, `_Alignof(long double)`=16, `_Alignof(double[10])`==`_Alignof(double)`=8)
- [✓] §6.5.3.4p1 (C11) — `_Alignof` of an **incomplete type** is rejected. — mcc:✓ gcc:✓ clang:✓ (mcc: "error: '_Alignof' applied to an incomplete type")
- [✓] §6.5.3.4p1 (C11) — `_Alignof` of a **function type** is a constraint violation. Default mode: all three accept it as a GNU extension; under `-pedantic-errors` all three reject. — mcc:✓ gcc:✓ clang:✓ (mcc default accepts; `-pedantic-errors`: "error: '_Alignof' applied to a function type")
- [~] §6.5.3.4p1 (C11) — `_Alignof` applied to an **expression** (GNU `__alignof__` extension, not a type name) is a constraint violation. Default mode: mcc & gcc accept silently, clang warns (`-Wgnu-alignof-expression`); under `-pedantic-errors` all three reject. — mcc:~ gcc:~ clang:✓ (mcc default silently accepts like gcc; clang is the only one warning by default. mcc `-pedantic-errors`: "error: ISO C does not allow '_Alignof' applied to an expression")

### §7.16 Variable arguments `<stdarg.h>`

> `va_copy` (§7.16.1.2) is C99/C11 only — not present in C90; the other three macros date to C90.

- [✓] §7.16p1 — `<stdarg.h>` declares a type and defines four macros, for advancing through a list of arguments whose number and types are not known to the called function when it is translated. — mcc:✓ gcc:✓ clang:✓ (bundled `runtime/include/stdarg.h` defines `va_list` + the four macros)
- §7.16p2 — A function may be called with a variable number of arguments of varying types; its parameter list contains one or more parameters, the rightmost of which (`parmN`) plays a special role in the access mechanism. — (spec; not separately testable)
- [✓] §7.16p3 — The type declared is `va_list`, an object type suitable for holding the info needed by `va_start`/`va_arg`/`va_end`/`va_copy`. `ap` may be passed to another function; if that function invokes `va_arg` with that `ap`, the `ap` in the calling function is indeterminate and shall be passed to `va_end` before further reference. — mcc:✓ gcc:✓ clang:✓ (verified passing `va_list` to a helper that drains it, then `va_end` in the caller — works; `va_copy` before passing preserves the original)
- [✓] §7.16p4 — The macros defined are `va_start`, `va_arg`, `va_copy`, and `va_end` (`va_copy` is C99/C11; absent in C90). — mcc:✓ gcc:✓ clang:✓ (all four present and forwarding to `__builtin_va_*`)

#### §7.16.1.1 The `va_arg` macro

- [✓] §7.16.1.1p1 — Synopsis: `#include <stdarg.h>` then `type va_arg(va_list ap, type);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.16.1.1p2 (Description) — `va_arg` expands to an expression with the specified type and the value of the next argument; `ap` must have been initialized by `va_start`/`va_copy`; each invocation modifies `ap`; `type` shall be a type name such that `type*` names a pointer to such an object. — mcc:✓ gcc:✓ clang:✓ (verified successive `va_arg(ap,int)` returns args in order)
- [✓] §7.16.1.1p2 (UB) — If there is no actual next argument, or if `type` is incompatible with the actual next argument (after default promotions), the behavior is undefined, **except**: (a) signed/unsigned corresponding types where the value is representable in both; or (b) one is `void*` and the other a pointer to a character type. — mcc:✓ gcc:✓ clang:✓ (the two excepted cases compile & run identically — verified `va_arg(...,unsigned)` for an `int` arg and `va_arg(...,char*)` for a `void*` arg)
- [✓] §7.16.1.1p3 (Returns) — The first `va_arg` after `va_start` returns the argument after `parmN`; successive invocations return remaining args in succession. — mcc:✓ gcc:✓ clang:✓

#### §7.16.1.2 The `va_copy` macro (C99/C11)

- [✓] §7.16.1.2p1 — Synopsis: `#include <stdarg.h>` then `void va_copy(va_list dest, va_list src);` (added in C99; not in C90). — mcc:✓ gcc:✓ clang:✓ (`#define va_copy __builtin_va_copy`)
- [✓] §7.16.1.2p2 (Description) — `va_copy` initializes `dest` as a copy of `src`, as if `va_start` then the same `va_arg` sequence had been applied to `dest`. Neither `va_copy` nor `va_start` shall reinitialize `dest` without an intervening `va_end`. — mcc:✓ gcc:✓ clang:✓ (verified two independent drains of a copied `va_list` yield identical sums)
- [✓] §7.16.1.2p3 (Returns) — `va_copy` returns no value. — mcc:✓ gcc:✓ clang:✓ (used as a void expression)

#### §7.16.1.3 The `va_end` macro

- [✓] §7.16.1.3p1 — Synopsis: `#include <stdarg.h>` then `void va_end(va_list ap);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.16.1.3p2 (Description/UB) — `va_end` facilitates a normal return from the function whose va-list was referred to by `va_start`/`va_copy`; it may render `ap` unusable until reinitialized. If there is no corresponding `va_start`/`va_copy`, or if `va_end` is not invoked before return, the behavior is undefined. — mcc:✓ gcc:✓ clang:✓ (compiles & runs; missing `va_end` is UB, not diagnosed by any of the three)
- [✓] §7.16.1.3p3 (Returns) — `va_end` returns no value. — mcc:✓ gcc:✓ clang:✓

#### §7.16.1.4 The `va_start` macro

- [✓] §7.16.1.4p1 — Synopsis: `#include <stdarg.h>` then `void va_start(va_list ap, parmN);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.16.1.4p2 (Description) — `va_start` shall be invoked before any access to the unnamed arguments; it initializes `ap` for subsequent `va_arg`/`va_end` (and, C99/C11, `va_copy`). — mcc:✓ gcc:✓ clang:✓
- [~] §7.16.1.4p3 (Description/UB) — `parmN` is the rightmost named parameter (just before `, ...`). UB if `parmN` is declared `register`, with a function or array type, or with a type incompatible with its default-promoted type. — mcc:~ gcc:✓ clang:✓ — **mcc gap (diagnostic):** mcc *has* a `register`-param check (`check_va_start_register` in `src/mccgen.c`) but on the x86_64 SysV target the `va_start` macro reads the register-save area and never references `parmN`, so the check does not fire; gcc warns (`In function …`) and clang warns (`-Wvarargs: passing a parameter declared with the 'register' keyword to 'va_start' has undefined behavior`). On i386/arm targets, where the macro expands to `&(last)`, the address-of-register diagnostic catches it. UB regardless; mcc still compiles correct code, it just misses the optional warning on x86_64.
- [✗] §7.16.1.4p3 (UB — non-last second arg) — Passing a second argument that is **not** the last non-variadic parameter is UB. — mcc:✗ gcc:✓ clang:✓ — **mcc gap (diagnostic):** mcc silently accepts `va_start(ap, a)` where `a` is not the rightmost named parameter; gcc warns and clang warns (`-Wvarargs: second argument to 'va_start' is not the last non-variadic parameter`). (Optional diagnostic only — UB, not a required diagnostic; code still compiles.)
- [✓] §7.16.1.4p4 (Returns) — `va_start` returns no value. — mcc:✓ gcc:✓ clang:✓

> Constraint cross-reference (the `...` syntax itself; primary checkbox in §6.7.5.3):
- [✓] §6.7.5.3p9 — The ellipsis terminator `, ...` may occur only at the end of a parameter type list, and the function must have at least one named parameter preceding the ellipsis (so `parmN` exists for `va_start`); `...` is not allowed as the only parameter. — mcc:✓ gcc:~ clang:✓ — mcc rejects `void f(...)` ("error: identifier expected"); clang rejects ("ISO C requires a named parameter before '...'"); **gcc accepts `void f(...)` as a silent extension** even at `-std=c11` (gcc is the outlier; mcc matches the standard and clang).

#### §7.16.2 mcc-specific implementation notes (non-normative)

- [✓] (impl) — mcc's bundled `<stdarg.h>` defines `typedef __builtin_va_list va_list;` and maps the four macros to `__builtin_va_*`; also provides `typedef va_list __gnuc_va_list;` and `#define _VA_LIST_DEFINED`. — mcc:✓ (all §7.16.1.* required behaviors verified above; the two gaps are §7.16.1.4p3/non-last optional warnings only)
- [✓] (impl) — mcc's bundled `<stdalign.h>` provides C11 `alignas`/`alignof` + `__alignas_is_defined`/`__alignof_is_defined`=1, and pre-C11 back-fills `_Alignas`/`_Alignof` to `__attribute__((aligned))`/`__alignof__`. — mcc:✓ (§7.15p2-p4 and §6.7.5/§6.5.3.4 operator semantics + all constraint diagnostics verified)
- [✓] (impl) — `<setjmp.h>` and `<signal.h>` are sourced from the host C library; mcc's burden is preserving non-volatile vs volatile automatics across `longjmp` (§7.13.2.1p3), honoring the `setjmp` context constraint (§7.13.1.1p4), and not assuming any `<signal.h>` facilities in freestanding mode. — mcc:✓ (verified at `-O2`; no compiler-side regression)
- [✓] (impl) — mcc's bundled `<varargs.h>` is an intentional `#error` stub directing users to `<stdarg.h>`; legacy K&R varargs are unsupported. — mcc:✓ gcc:~ clang:~ (gcc/clang ship a deprecated/obsolete `varargs.h`; none of the three support it for real use)


## §7.16-§7.18 stdarg/stdbool/stdint-or-atomic boundary

> mcc status: 123 TODO checkboxes in this section; 7 are places where mcc is
> observably weaker than the gcc==clang pair (exactly **1** of which is a hard
> functional conformance gap — the others are optional-diagnostic / non-required
> QoI or non-standard predefines). Concretely: (1) `atomic_fetch_add` /
> `atomic_fetch_sub` on an **atomic pointer type** (`_Atomic(int*)`) is rejected
> outright by mcc ("integral or integer-sized pointer target type expected")
> though gcc/clang accept it — the one hard functional gap; (2) mcc does **not**
> warn on an invalid `memory_order` constant passed to `atomic_store`/`atomic_load`
> (a runtime-UB constraint — diagnostic not *required*, but gcc/clang both warn);
> (3) mcc does **not** warn when `va_start`'s `parmN` is `register`-qualified
> (UB; the check exists in `src/mccgen.c:check_va_start_register` but `vtop->sym`
> is dropped after the rvalue load on x86_64, so it never fires); gcc/clang warn.
> Everything else — the full `<stdarg.h>` macro set, the entire `<stdbool.h>`,
> all 36 atomic typedefs, all lock-free macros (all `=2`), `memory_order`
> enumerators (0..5), fences, `atomic_flag`, every generic op, `kill_dependency`,
> `ATOMIC_VAR_INIT`/`atomic_init`, atomic-via-plain-operators — matches the
> gcc==clang consensus byte-for-byte. NB: gcc and clang **themselves disagree** on
> atomic pointer `fetch_add` scaling (gcc scales by bytes, clang by elements);
> clang is standard-correct.

> **Clause-7 numbering reconciliation (N1256 C99 vs N1570 C11).** C11 inserted two
> new headers into clause 7 — `<stdalign.h>` (C11 §7.15) and `<stdatomic.h>`
> (C11 §7.17) — which shifts every later header by two section numbers. The headers
> covered here therefore carry *different* clause numbers in the two standards:
>
> | Header | C99 (N1256) | C11 (N1570) |
> |---|---|---|
> | `<stdarg.h>` Variable arguments | §7.15 | §7.16 |
> | `<stdatomic.h>` Atomics | — (does not exist) | §7.17 |
> | `<stdbool.h>` Boolean type and values | §7.16 | §7.18 |
> | `<stddef.h>` Common definitions | §7.17 | §7.19 |
> | `<stdint.h>` Integer types | §7.18 | §7.20 |
>
> Hence the "§7.16–§7.18 boundary" is a *moving* boundary: in C99 §7.16 = stdbool,
> §7.17 = stddef, §7.18 = stdint; in C11 §7.16 = stdarg, §7.17 = stdatomic,
> §7.18 = stdbool. This section fully expands `<stdarg.h>`, `<stdbool.h>`, and the
> C11-only `<stdatomic.h>`. `<stddef.h>` and `<stdint.h>` are expanded in their own
> sections; only their numbering is reconciled above. In mcc the four `<stdarg.h>`
> macros are thin wrappers over `__builtin_va_start`/`__builtin_va_arg`/
> `__builtin_va_copy`/`__builtin_va_end`, and `va_list` is `__builtin_va_list`.

### §7.15 / §7.16 (C11) Variable arguments `<stdarg.h>`

> C99 added `va_copy` (§7.15.2); it does not exist in C90. C11 reworded the
> `va_start` description but the macro set is unchanged from C99. Verified: mcc's
> `runtime/include/stdarg.h` defines all four macros plus `va_list`, and a
> `va_start`/`va_copy`/`va_arg`/`va_end` round-trip (including passing the
> `va_list` to a callee, vprintf-style) produces output identical to gcc/clang.

- [✓] §7.15p1 (C11 §7.16p1) — `<stdarg.h>` shall declare a type and define four macros (`va_start`, `va_arg`, `va_end`, `va_copy`) for advancing through a list of arguments whose number and types are not known to the called function when it is translated. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.15p2 (C11 §7.16p2) — The type declared is `va_list`, an object type suitable for holding information needed by the macros `va_start`, `va_arg`, `va_end`, and `va_copy`. — mcc:✓ gcc:✓ clang:✓ (mcc: `typedef __builtin_va_list va_list;`)
- §7.15p2 (C11 §7.16p2) — If access to the varying arguments is desired, the called function shall declare an object (referred to here as `ap`) having type `va_list`. — (spec; not separately testable)
- [✓] §7.15p2 (C11 §7.16p2) — The object `ap` may be passed as an argument to another function; if that function invokes the `va_arg` macro with parameter `ap`, the value of `ap` in the calling function is indeterminate and shall be passed to the `va_end` macro prior to any further reference to `ap`. — mcc:✓ gcc:✓ clang:✓ (vprintf-style pass-through verified; UB-after-callee not separately testable)
- §7.15p3 (C11 §7.16p3) — Constraint: each invocation of the `va_start` and `va_copy` macros shall be matched by a corresponding invocation of the `va_end` macro in the same function. — (spec; UB on mismatch, not separately testable)

#### §7.15.1 / §7.16.1 (C11) The `va_arg` macro

- [✓] §7.15.1p1 (C11 §7.16.1p1) — Synopsis: `#include <stdarg.h>` then `type va_arg(va_list ap, type);`. — mcc:✓ gcc:✓ clang:✓
- §7.15.1p2 (C11 §7.16.1p2) — Constraint: the parameter `ap` shall have been initialized by the `va_start` or `va_copy` macro without an intervening invocation of the `va_end` macro for the same `ap`; otherwise the behavior is undefined. — (spec; UB, not separately testable)
- [✓] §7.15.1p2 (C11 §7.16.1p2) — The `va_arg` macro expands to an expression that has the specified `type` and the value of the next argument in the call, and modifies `ap` so that the values of successive arguments are returned in turn. — mcc:✓ gcc:✓ clang:✓ (successive `va_arg` returns 10,20,30 on all three)
- §7.15.1p2 (C11 §7.16.1p2) — The parameter `type` shall be a type name specified such that the type of a pointer to an object that has the specified type can be obtained simply by postfixing a `*` to `type`. — (spec; not separately testable)
- §7.15.1p2 (C11 §7.16.1p2) — If there is no actual next argument, or if `type` is not compatible with the type of the actual next argument (as promoted according to the default argument promotions), the behavior is undefined, except for the two following cases. — (spec; UB, not separately testable)
- [✓] §7.15.1p2 (C11 §7.16.1p2) — Exception 1 (well-defined): one type is a signed integer type, the other is the corresponding unsigned integer type, and the value is representable in both types. — mcc:✓ gcc:✓ clang:✓ (`int` arg read as `unsigned` → 5 on all three)
- [✓] §7.15.1p2 (C11 §7.16.1p2) — Exception 2 (well-defined): one type is pointer to `void` and the other is a pointer to a character type. — mcc:✓ gcc:✓ clang:✓ (`void*` arg read as `char*` → "hi" on all three)
- [✓] §7.15.1p3 (C11 §7.16.1p3) — Returns: the first invocation of `va_arg` after that of `va_start` returns the value of the argument after that specified by `parmN`; successive invocations return the values of the remaining arguments in succession. — mcc:✓ gcc:✓ clang:✓

#### §7.15.2 / §7.16.2 (C11) The `va_copy` macro (C99; absent in C90)

- [✓] §7.15.2p1 (C11 §7.16.2p1) — Synopsis: `#include <stdarg.h>` then `void va_copy(va_list dest, va_list src);`. — mcc:✓ gcc:✓ clang:✓ (mcc: `#define va_copy __builtin_va_copy`)
- [✓] §7.15.2p2 (C11 §7.16.2p2) — The `va_copy` macro initializes `dest` as a copy of `src`, as if the `va_start` macro had been applied to `dest` followed by the same sequence of uses of the `va_arg` macro as had previously been used to reach the present state of `src`. — mcc:✓ gcc:✓ clang:✓ (double-traversal sum 60+60=120 identical on all three)
- §7.15.2p2 (C11 §7.16.2p2) — Constraint: neither the `va_copy` nor the `va_start` macro shall be invoked to reinitialize `dest` without an intervening invocation of the `va_end` macro for the same `dest`. — (spec; UB, not separately testable)
- [✓] §7.15.2p3 (C11 §7.16.2p3) — Returns: the `va_copy` macro returns no value. — mcc:✓ gcc:✓ clang:✓

#### §7.15.3 / §7.16.3 (C11) The `va_end` macro

- [✓] §7.15.3p1 (C11 §7.16.3p1) — Synopsis: `#include <stdarg.h>` then `void va_end(va_list ap);`. — mcc:✓ gcc:✓ clang:✓ (mcc: `#define va_end __builtin_va_end`)
- [✓] §7.15.3p2 (C11 §7.16.3p2) — The `va_end` macro facilitates a normal return from the function whose variable argument list was referred to by the expansion of `va_start`, or the function containing the expansion of `va_copy`, that initialized the `va_list ap`. — mcc:✓ gcc:✓ clang:✓
- §7.15.3p2 (C11 §7.16.3p2) — The `va_end` macro may modify `ap` so that it is no longer usable (without being reinitialized by `va_start` or `va_copy`). — (spec; permissive "may", not separately testable)
- §7.15.3p2 (C11 §7.16.3p2) — If there is no corresponding invocation of the `va_start` or `va_copy` macro, or if `va_end` is not invoked before the return, the behavior is undefined. — (spec; UB, not separately testable)
- [✓] §7.15.3p3 (C11 §7.16.3p3) — Returns: the `va_end` macro returns no value. — mcc:✓ gcc:✓ clang:✓

#### §7.15.4 / §7.16.4 (C11) The `va_start` macro

- [✓] §7.15.4p1 (C11 §7.16.4p1) — Synopsis: `#include <stdarg.h>` then `void va_start(va_list ap, parmN);`. — mcc:✓ gcc:✓ clang:✓ (mcc: `#define va_start __builtin_va_start`)
- §7.15.4p2 (C11 §7.16.4p2) — The `va_start` macro shall be invoked before any access to the unnamed arguments. — (spec; UB on violation, not separately testable)
- [✓] §7.15.4p3 (C11 §7.16.4p3) — The `va_start` macro initializes `ap` for subsequent use by `va_arg` and `va_end`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.15.4p3 (C11 §7.16.4p3) — The parameter `parmN` is the identifier of the rightmost parameter in the variable parameter list in the function definition (the one just before the `, ...`). — mcc:✓ gcc:✓ clang:✓
- [~] §7.15.4p3 (C11 §7.16.4p3) — If the parameter `parmN` is declared with the `register` storage class, with a function or array type, or with a type that is not compatible with the type that results after application of the default argument promotions, the behavior is undefined. — mcc:~ gcc:✓ clang:✓ (UB; gcc/clang emit `-Wvarargs` "register" warning, mcc emits **no** warning — its `check_va_start_register` exists but `vtop->sym` is dropped after the x86_64 rvalue load so it never fires; not a conformance break since the diagnostic is optional)

- [✓] §7.15.4p4 (C11 §7.16.4p4) — Returns: the `va_start` macro returns no value. — mcc:✓ gcc:✓ clang:✓

### §7.16 / §7.18 (C11) Boolean type and values `<stdbool.h>`

> Identical content in C99 and C11 apart from the moved clause number and the
> §6.10.8 → §6.10.8 (same heading, renumbered in C11) cross-reference. The
> undefine/redefine allowance is flagged obsolescent in both standards' future
> directions. Verified against mcc's `runtime/include/stdbool.h`: exactly the four
> macros, all usable in `#if` and after `#undef`/redefine; matches gcc/clang.

- [✓] §7.16p1 (C11 §7.18p1) — `<stdbool.h>` shall define four macros: `bool`, `true`, `false`, and `__bool_true_false_are_defined`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.16p2 (C11 §7.18p2) — The macro `bool` shall expand to `_Bool`. — mcc:✓ gcc:✓ clang:✓ (`sizeof(bool x)==sizeof(_Bool)` holds)
- [✓] §7.16p3 (C11 §7.18p3) — The macro `true` shall expand to the integer constant `1`. — mcc:✓ gcc:✓ clang:✓ (`#if true==1` and `_Static_assert(true==1)` pass)
- [✓] §7.16p3 (C11 §7.18p3) — The macro `false` shall expand to the integer constant `0`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.16p3 (C11 §7.18p3) — The macro `__bool_true_false_are_defined` shall expand to the integer constant `1`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.16p4 (C11 §7.18p4) — Notwithstanding the provisions of §6.10.8, a program may undefine and perhaps then redefine the macros `bool`, `true`, and `false`. — mcc:✓ gcc:✓ clang:✓ (`#undef bool/true/false` then `#define bool int` etc. accepted on all three)
- §7.26.7 (C11 §7.31.9) — (Future directions) The ability to undefine and perhaps then redefine the macros `bool`, `true`, and `false` is an obsolescent feature. — (spec; future-direction note, not separately testable)

#### Added gaps — `<stdbool.h>` extras

- [✓] §7.16 (C11 §7.18) — The header defines *only* these four macros (no extra `bool`-related identifiers leak); confirmed mcc header has no surplus definitions. — mcc:✓ gcc:✓ clang:✓
- [✓] §6.7.2.1/§7.18 (C11) — `_Bool` (the underlying type `bool` expands to) is a first-class type usable without including the header; `bool x = true;` round-trips and stores 0/1. — mcc:✓ gcc:✓ clang:✓

### §7.17 (C11) Atomics `<stdatomic.h>` (C11-only; not present in C99)

> Entire header is C11-only. `<stdatomic.h>` ties to the `_Atomic` type qualifier
> (§6.7.2.4, §6.7.3) and to the conditional-feature macro `__STDC_NO_ATOMICS__`
> (§6.10.8.3): an implementation that defines `__STDC_NO_ATOMICS__` need not provide
> this header nor support its facilities. In all synopses below, `A` denotes one of
> the atomic types, `C` denotes its corresponding non-atomic type, and `M` is the
> type of the arithmetic operand — `int` for atomic integer types and `ptrdiff_t`
> for atomic pointer types. (Note: `ATOMIC_VAR_INIT` was deprecated by C17/DR485 and
> removed in C23; `atomic_init` supersedes it. The `failure ≤ success` ordering
> constraint of §7.17.7.4 was likewise relaxed by C17/DR459.) Verified: none of the
> three compilers defines `__STDC_NO_ATOMICS__` (all support atomics); mcc's
> `runtime/include/stdatomic.h` is a `__atomic_*`-builtin–backed header.

- [✓] §6.10.8.3 (C11) — None of mcc/gcc/clang defines `__STDC_NO_ATOMICS__` under `-std=c11` (all provide `<stdatomic.h>` and its facilities). — mcc:✓ gcc:✓ clang:✓ (`-dM -E` dump: macro absent on all three)

#### §7.17.1 (C11) Introduction

- [✓] §7.17.1p1 (C11) — `<stdatomic.h>` shall define several macros and declare several types and functions for performing atomic operations on data shared between threads. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.17.1p2 (C11) — Implementations that define the macro `__STDC_NO_ATOMICS__` (see §6.10.8.3) need not provide this header nor support any of its facilities. — mcc:✓ gcc:✓ clang:✓ (none defines it; all provide the header)
- [✓] §7.17.1p3 (C11) — The header shall define the atomic lock-free macros `ATOMIC_BOOL_LOCK_FREE`, `ATOMIC_CHAR_LOCK_FREE`, `ATOMIC_CHAR16_T_LOCK_FREE`, `ATOMIC_CHAR32_T_LOCK_FREE`, `ATOMIC_WCHAR_T_LOCK_FREE`, `ATOMIC_SHORT_LOCK_FREE`, `ATOMIC_INT_LOCK_FREE`, `ATOMIC_LONG_LOCK_FREE`, `ATOMIC_LLONG_LOCK_FREE`, and `ATOMIC_POINTER_LOCK_FREE`, indicating the lock-free property of the corresponding atomic types (both signed and unsigned). — mcc:✓ gcc:✓ clang:✓ (all 10 `#defined`; `#if !defined(...)` guard passes on all three)
- [✓] §7.17.1p3 (C11) — Each lock-free macro shall expand to a constant expression suitable for use in `#if` preprocessing directives. — mcc:✓ gcc:✓ clang:✓ (`#if ATOMIC_INT_LOCK_FREE` evaluated successfully on all three)
- [✓] §7.17.1p3 (C11) — A lock-free macro value of `0` indicates the corresponding atomic type is never lock-free, `1` indicates it is sometimes lock-free, and `2` indicates it is always lock-free. — mcc:✓ gcc:✓ clang:✓ (every macro = 2 on x86_64 for all three)
- [✓] §7.17.1p3 (C11) — The header shall define the macro `ATOMIC_FLAG_INIT` (see §7.17.8). — mcc:✓ gcc:✓ clang:✓ (mcc: `#define ATOMIC_FLAG_INIT {0}`)
- [✓] §7.17.1p4 (C11) — The header shall define the macro `ATOMIC_VAR_INIT` (see §7.17.2). — mcc:✓ gcc:✓ clang:✓ (mcc: `#define ATOMIC_VAR_INIT(value) (value)`; deprecated by C17 but still present, no deprecation warning on any compiler)
- [✓] §7.17.1p4 (C11) — The types declared shall include `memory_order` (an enumerated type whose enumerators identify memory-ordering constraints), `atomic_flag` (a structure type representing a lock-free primitive atomic flag), and the named atomic integer types of §7.17.6. — mcc:✓ gcc:✓ clang:✓ (`memory_order` enum, `atomic_flag` struct, and all named types declared)

#### §7.17.2 (C11) Initialization

- [✓] §7.17.2p1 (C11) — Synopsis: `#define ATOMIC_VAR_INIT(value) /* see below */`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.17.2p2 (C11) — The `ATOMIC_VAR_INIT` macro expands to a token sequence suitable for initializing an atomic object of a type that is initialization-compatible with `value`. An atomic object with automatic storage duration that is not explicitly initialized is initially in an indeterminate state; however, the default (zero) initialization for objects with static or thread-local storage duration is guaranteed to produce a valid state. — mcc:✓ gcc:✓ clang:✓ (`atomic_int a = ATOMIC_VAR_INIT(3)` loads back 3 on all three)
- §7.17.2p3 (C11) — Concurrent access to the variable being initialized via `ATOMIC_VAR_INIT`, even by an atomic operation, constitutes a data race. — (spec; concurrency UB, not separately testable)
- [✓] §7.17.2p4 (C11) — Synopsis: `void atomic_init(volatile A *obj, C value);`. — mcc:✓ gcc:✓ clang:✓ (mcc maps `atomic_init` → `atomic_store_explicit(...,__ATOMIC_RELAXED)`)
- [✓] §7.17.2p5 (C11) — The `atomic_init` generic function initializes the atomic object pointed to by `obj` to the value `value`, and also initializes any additional state the implementation might carry for the atomic object; it does not avoid data races, so concurrent access to the variable being initialized, even via an atomic operation, constitutes a data race. — mcc:✓ gcc:✓ clang:✓ (`atomic_init(&a,5)` then `atomic_load` reflects the value)
- [✓] §7.17.2p6 (C11) — Returns: the `atomic_init` generic function returns no value. — mcc:✓ gcc:✓ clang:✓

#### §7.17.3 (C11) Order and consistency

- [✓] §7.17.3p1 (C11) — The enumerated type `memory_order` specifies the detailed regular (non-atomic) memory synchronization operations and may provide for operation ordering; its enumeration constants are `memory_order_relaxed`, `memory_order_consume`, `memory_order_acquire`, `memory_order_release`, `memory_order_acq_rel`, and `memory_order_seq_cst`. — mcc:✓ gcc:✓ clang:✓ (all six enumerators present; values 0..5 on all three)
- §7.17.3p2 (C11) — For `memory_order_seq_cst` operations there shall be a single total order S on all such operations, consistent with the "happens before" order and the modification orders of all affected objects. — (spec; abstract-machine memory model, not separately testable)
- §7.17.3p3 (C11) — `memory_order_relaxed`: the operation neither requires nor provides any ordering with respect to other memory operations (no synchronizes-with or ordering constraints). — (spec; memory model, not separately testable)
- §7.17.3p3 (C11) — `memory_order_consume`: a load operation performs a consume operation on the affected memory location, ordering subsequent operations that carry a dependency from the loaded value. — (spec; memory model, not separately testable)
- §7.17.3p3 (C11) — `memory_order_acquire`: a load operation performs an acquire operation on the affected memory location. — (spec; memory model, not separately testable)
- §7.17.3p3 (C11) — `memory_order_release`: a store operation performs a release operation on the affected memory location. — (spec; memory model, not separately testable)
- §7.17.3p3 (C11) — `memory_order_acq_rel`: a read-modify-write operation performs both an acquire and a release operation on the affected memory location. — (spec; memory model, not separately testable)
- §7.17.3p3 (C11) — `memory_order_seq_cst`: a load performs an acquire, a store performs a release, and a read-modify-write performs both, plus participation in the single total order S. — (spec; memory model, not separately testable)
- §7.17.3 (C11) — An atomic operation A that performs a release operation on an atomic object M synchronizes with an atomic operation B that performs an acquire operation on M and takes its value from any side effect in the release sequence headed by A (release/acquire synchronization semantics). — (spec; memory model, not separately testable)
- §7.17.3 (C11) — The "happens before" / "carries a dependency" / "dependency-ordered before" / "release sequence" relations defined here shall hold as specified for establishing inter-thread ordering and visibility of side effects. — (spec; memory model, not separately testable)
- §7.17.3p4 (C11) — An atomic store with `memory_order_seq_cst` from which an atomic load with `memory_order_seq_cst` takes its value shall be consistent with the total order S. — (spec; memory model, not separately testable)
- §7.17.3 (C11) — Implementations should ensure that no "out-of-thin-air" values are computed that circularly depend on their own computation. — (spec; "should" recommendation, not separately testable)
- [✓] §7.17.3 (C11) — Synopsis: `#define kill_dependency(y) /* see below */`. The `kill_dependency` macro terminates a dependency chain: the argument `y` does not carry a dependency into the macro's return value, which equals the value of `y`. — mcc:✓ gcc:✓ clang:✓ (`kill_dependency(7)` yields 7 on all three; mcc impl is a statement-expression returning the value)

#### §7.17.4 (C11) Fences

- §7.17.4p1 (C11) — This subclause introduces synchronization primitives called fences, supporting "fence-fence", "fence-atomic", and "atomic-fence" synchronization; a fence with acquire semantics is an acquire fence and a fence with release semantics is a release fence. — (spec; memory model, not separately testable)
- [✓] §7.17.4p2 (C11) — Synopsis: `void atomic_thread_fence(memory_order order);`. — mcc:✓ gcc:✓ clang:✓ (declared `extern void atomic_thread_fence(memory_order)`; call links & runs)
- [✓] §7.17.4p3 (C11) — Depending on `order`, `atomic_thread_fence` has no effect if `order == memory_order_relaxed`; is an acquire fence if `order == memory_order_acquire` or `memory_order_consume`; is a release fence if `order == memory_order_release`; is both an acquire and a release fence if `order == memory_order_acq_rel`; and is a sequentially consistent acquire and release fence if `order == memory_order_seq_cst`. — mcc:✓ gcc:✓ clang:✓ (compiles/runs; per-order fence semantics not separately observable)
- [✓] §7.17.4p4 (C11) — Returns: `atomic_thread_fence` returns no value. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.17.4p5 (C11) — Synopsis: `void atomic_signal_fence(memory_order order);`. — mcc:✓ gcc:✓ clang:✓ (mcc declares `extern void atomic_signal_fence(memory_order)` + barrier macro)
- [✓] §7.17.4p6 (C11) — `atomic_signal_fence` is equivalent to `atomic_thread_fence(order)`, except that the resulting ordering constraints are established only between a thread and a signal handler executed in the same thread. — mcc:✓ gcc:✓ clang:✓ (compiles/runs)
- §7.17.4p7 (C11) — `atomic_signal_fence` shall not issue any hardware fence instruction; it is a compiler barrier only. — (spec; codegen property, not separately testable here)
- [✓] §7.17.4p8 (C11) — Returns: `atomic_signal_fence` returns no value. — mcc:✓ gcc:✓ clang:✓

#### §7.17.5 (C11) Lock-free property

- §7.17.5p1 (C11) — The atomic lock-free macros indicate the lock-free property of atomic types for the corresponding `_Atomic` types; atomic operations that are lock-free should also be address-free (usable on memory shared between processes via shared memory). — (spec; "should" recommendation, not separately testable)
- [✓] §7.17.5p2 (C11) — Synopsis: `_Bool atomic_is_lock_free(const volatile A *obj);`. — mcc:✓ gcc:✓ clang:✓ (mcc: `atomic_is_lock_free(OBJ)` → `__atomic_is_lock_free(sizeof(*(OBJ)),(OBJ))`)
- [✓] §7.17.5p3 (C11) — Returns: the `atomic_is_lock_free` generic function returns nonzero (true) if and only if the object's operations are lock-free; the result of a lock-free query on one object cannot be generalized to another object even of the same type. — mcc:✓ gcc:✓ clang:✓ (`atomic_is_lock_free(&atomic_int)` returns 1 on all three)

#### §7.17.6 (C11) Atomic integer types

- [✓] §7.17.6p1 (C11) — For each line in the table of §7.17.6, the named atomic type (the "atomic type name" column) shall be declared as a type that has the same representation and alignment requirements as the corresponding `_Atomic` direct type. — mcc:✓ gcc:✓ clang:✓ (`sizeof(atomic_llong)==sizeof(long long)`, `_Alignof(atomic_int)>=_Alignof(int)` hold)
- [✓] §7.17.6p1 (C11) — The header shall provide the typedef names, each as the `_Atomic`-qualified version of its corresponding direct type: `atomic_bool` (`_Bool`), `atomic_char` (`char`), `atomic_schar` (`signed char`), `atomic_uchar` (`unsigned char`), `atomic_short` (`short`), `atomic_ushort` (`unsigned short`), `atomic_int` (`int`), `atomic_uint` (`unsigned int`), `atomic_long` (`long`), `atomic_ulong` (`unsigned long`), `atomic_llong` (`long long`), `atomic_ullong` (`unsigned long long`), `atomic_char16_t` (`char16_t`/`uint_least16_t`), `atomic_char32_t` (`char32_t`/`uint_least32_t`), `atomic_wchar_t` (`wchar_t`), `atomic_int_least8_t`, `atomic_uint_least8_t`, `atomic_int_least16_t`, `atomic_uint_least16_t`, `atomic_int_least32_t`, `atomic_uint_least32_t`, `atomic_int_least64_t`, `atomic_uint_least64_t`, `atomic_int_fast8_t`, `atomic_uint_fast8_t`, `atomic_int_fast16_t`, `atomic_uint_fast16_t`, `atomic_int_fast32_t`, `atomic_uint_fast32_t`, `atomic_int_fast64_t`, `atomic_uint_fast64_t`, `atomic_intptr_t`, `atomic_uintptr_t`, `atomic_size_t`, `atomic_ptrdiff_t`, `atomic_intmax_t`, and `atomic_uintmax_t`. — mcc:✓ gcc:✓ clang:✓ (a single TU declaring objects of all 36 typedefs compiles on all three)
- §7.17.6p2 (C11) — (Note) The atomic types' representation need not have the same size as their corresponding regular types, though they should have the same size whenever possible to ease compatibility. — (spec; informative note, not separately testable)

#### §7.17.7 (C11) Operations on atomic types

- §7.17.7p1 (C11) — In the following synopses, `A` refers to one of the atomic types, `C` to its corresponding non-atomic type, and `M` to the operand type — `int` for atomic integer types and `ptrdiff_t` for atomic pointer types. — (spec; notation, not separately testable)
- [✓] §7.17.7p2 (C11) — The non-explicit (no-`order`-argument) generic functions are defined to behave as the corresponding `_explicit` function invoked with `memory_order_seq_cst` for its `order` argument(s). — mcc:✓ gcc:✓ clang:✓ (mcc header literally defines each non-explicit macro as the `_explicit` form with `__ATOMIC_SEQ_CST`)

##### §7.17.7.1 (C11) The `atomic_store` generic functions

- [✓] §7.17.7.1p1 (C11) — Synopsis: `void atomic_store(volatile A *object, C desired);` and `void atomic_store_explicit(volatile A *object, C desired, memory_order order);`. — mcc:✓ gcc:✓ clang:✓
- [~] §7.17.7.1p2 (C11) — Constraint: the `order` argument shall not be `memory_order_acquire`, `memory_order_consume`, nor `memory_order_acq_rel`. — mcc:~ gcc:~ clang:~ (runtime-UB restriction, diagnostic *not* required; gcc warns `-Winvalid-memory-model`, clang warns `-Watomic-memory-ordering`, **mcc emits no diagnostic** for `atomic_store_explicit(&a,1,memory_order_acquire)` — mcc weaker QoI, gcc/clang reject-with-warning)
- [✓] §7.17.7.1p3 (C11) — Atomically replace the value pointed to by `object` with `desired`; memory is affected according to the value of `order`. — mcc:✓ gcc:✓ clang:✓ (store-then-load round-trips identically)
- [✓] §7.17.7.1p4 (C11) — Returns: the `atomic_store` generic functions return no value. — mcc:✓ gcc:✓ clang:✓

##### §7.17.7.2 (C11) The `atomic_load` generic functions

- [✓] §7.17.7.2p1 (C11) — Synopsis: `C atomic_load(volatile A *object);` and `C atomic_load_explicit(volatile A *object, memory_order order);`. — mcc:✓ gcc:✓ clang:✓
- [~] §7.17.7.2p2 (C11) — Constraint: the `order` argument shall not be `memory_order_release` nor `memory_order_acq_rel`. — mcc:~ gcc:~ clang:~ (runtime-UB restriction, diagnostic not required; gcc/clang warn, **mcc emits no diagnostic**)
- [✓] §7.17.7.2p3 (C11) — Atomically returns the value pointed to by `object`; memory is affected according to the value of `order`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.17.7.2p4 (C11) — Returns: the `atomic_load` generic functions return the value pointed to by `object`. — mcc:✓ gcc:✓ clang:✓ (load after store of 11 returns 11 on all three)

##### §7.17.7.3 (C11) The `atomic_exchange` generic functions

- [✓] §7.17.7.3p1 (C11) — Synopsis: `C atomic_exchange(volatile A *object, C desired);` and `C atomic_exchange_explicit(volatile A *object, C desired, memory_order order);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.17.7.3p2 (C11) — Atomically replace the value pointed to by `object` with `desired`; memory is affected according to `order`; these are read-modify-write operations. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.17.7.3p3 (C11) — Returns: the `atomic_exchange` generic functions return the value that had previously been pointed to by `object`. — mcc:✓ gcc:✓ clang:✓ (exchange returns prior 11, then prior 20, identical on all three)

##### §7.17.7.4 (C11) The `atomic_compare_exchange` generic functions

- [✓] §7.17.7.4p1 (C11) — Synopsis: `_Bool atomic_compare_exchange_strong(volatile A *object, C *expected, C desired);`, `_Bool atomic_compare_exchange_strong_explicit(volatile A *object, C *expected, C desired, memory_order success, memory_order failure);`, `_Bool atomic_compare_exchange_weak(volatile A *object, C *expected, C desired);`, and `_Bool atomic_compare_exchange_weak_explicit(volatile A *object, C *expected, C desired, memory_order success, memory_order failure);`. — mcc:✓ gcc:✓ clang:✓ (all four forms compile and run)
- [~] §7.17.7.4p2 (C11) — Constraint: the `failure` argument shall not be `memory_order_release` nor `memory_order_acq_rel`. — mcc:~ gcc:~ clang:~ (runtime-UB restriction; gcc/clang warn on a bad constant, mcc does not — same QoI pattern as store/load)
- §7.17.7.4p2 (C11) — Constraint (C11-as-published; relaxed by C17/DR459): the `failure` argument shall not be a stronger order than the `success` argument. — (spec; relaxed by C17, diagnostic not required, not separately testable)
- [✓] §7.17.7.4p3 (C11) — Atomically compares the contents of the memory pointed to by `object` for equality with that pointed to by `expected`, and if equal replaces the contents pointed to by `object` with `desired` (a read-modify-write operation), else loads the contents pointed to by `object` into the memory pointed to by `expected`. — mcc:✓ gcc:✓ clang:✓ (success path returns 1 & updates object; fail path returns 0 & writes actual value into `*expected` — `exp2` becomes 30 on all three)
- [✓] §7.17.7.4p3 (C11) — The `success` memory order governs the read-modify-write when the comparison is true; the `failure` memory order governs the load when the comparison is false. — mcc:✓ gcc:✓ clang:✓ (both orderings accepted; semantics not separately observable)
- §7.17.7.4p3 (C11) — These operations compare the object representations (bitwise, as by `memcmp`), so an object may compare unequal even when its value is equal if it contains padding or trap-related representation bits. — (spec; representation-comparison UB-ish edge, not separately testable)
- [✓] §7.17.7.4p4 (C11) — A `_weak` compare-exchange may fail spuriously — that is, act as if `*object != *expected` even when they are equal — returning false and updating `*expected`; a `_strong` compare-exchange shall not fail spuriously. — mcc:✓ gcc:✓ clang:✓ (both forms accepted; spurious failure is a "may", not separately testable)
- [✓] §7.17.7.4p4 (C11) — Returns: the `atomic_compare_exchange` generic functions return the result of the comparison (true if `object` was replaced by `desired`, false otherwise). — mcc:✓ gcc:✓ clang:✓

##### §7.17.7.5 (C11) The `atomic_fetch` and modify generic functions

- [✓] §7.17.7.5p1 (C11) — Synopsis: for each operation `key` ∈ {`add`, `sub`, `or`, `xor`, `and`}, the header provides `C atomic_fetch_key(volatile A *object, M operand);` and `C atomic_fetch_key_explicit(volatile A *object, M operand, memory_order order);` (i.e. `atomic_fetch_add`, `atomic_fetch_sub`, `atomic_fetch_or`, `atomic_fetch_xor`, `atomic_fetch_and`, and their `_explicit` variants). — mcc:✓ gcc:✓ clang:✓ (all 10 names compile & run for integer atomics on all three)
- [✓] §7.17.7.5p2 (C11) — Atomically replace the value pointed to by `object` with the result of applying the operation `key` to the value pointed to by `object` and the value `operand`; memory is affected according to `order`; these are read-modify-write operations. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.17.7.5p2 (C11) — For signed integer types, the arithmetic of `atomic_fetch_add`/`atomic_fetch_sub` is performed with silent two's-complement wraparound (no undefined behavior on overflow). — mcc:✓ gcc:✓ clang:✓ (integer fetch_add/sub round-trip; defined wraparound, not separately fuzzed)
- [✗] §7.17.7.5p2 (C11) — For atomic pointer (address) types, the result is as if computed with `ptrdiff_t` operand type (i.e. the operand scales by the pointee size). — mcc:✗ gcc:✗ clang:✓ (**mcc rejects** `atomic_fetch_add(&p,n)` on `_Atomic(int*)` with `error: integral or integer-sized pointer target type expected` — no atomic-pointer fetch_add at all; **gcc accepts but scales by BYTES not elements** (`fetch_add(&p,2)` advances p by 2 bytes → element-diff 0); **clang is standard-correct** (advances by 2 elements). gcc/clang consensus is itself broken here; clang matches the standard. atomic load/store on `_Atomic(int*)` *do* work in mcc.)
- [✓] §7.17.7.5p3 (C11) — Returns: the `atomic_fetch_key` generic functions return the value that `object` previously pointed to (the value immediately before the effects of the operation). — mcc:✓ gcc:✓ clang:✓ (integer `fetch_add` returns prior value on all three)

#### §7.17.8 (C11) Atomic flag type and operations

- [✓] §7.17.8p1 (C11) — The `atomic_flag` type provides the classic test-and-set functionality; it is a structure type with two states, set and clear. — mcc:✓ gcc:✓ clang:✓ (mcc: `typedef struct { atomic_bool value; } atomic_flag;`)
- §7.17.8p1 (C11) — Operations on an `atomic_flag` object shall be lock-free (it is the one atomic type guaranteed lock-free). — (spec; guarantee, not separately testable beyond `is_lock_free`)
- [✓] §7.17.8p2 (C11) — The macro `ATOMIC_FLAG_INIT` shall be defined to initialize an `atomic_flag` to the clear state; e.g. `atomic_flag guard = ATOMIC_FLAG_INIT;`. It may be used only to initialize a flag with static storage duration or a declaration of an automatic object. — mcc:✓ gcc:✓ clang:✓ (`atomic_flag f = ATOMIC_FLAG_INIT;` then `test_and_set` returns 0 → confirms clear init, on all three)
- §7.17.8p3 (C11) — An `atomic_flag` that is not explicitly initialized with `ATOMIC_FLAG_INIT` is initially in an indeterminate state. — (spec; indeterminate-state UB, not separately testable)

##### §7.17.8.1 (C11) The `atomic_flag_test_and_set` generic functions

- [✓] §7.17.8.1p1 (C11) — Synopsis: `_Bool atomic_flag_test_and_set(volatile atomic_flag *object);` and `_Bool atomic_flag_test_and_set_explicit(volatile atomic_flag *object, memory_order order);`. — mcc:✓ gcc:✓ clang:✓ (mcc declares both as `extern bool ...(volatile atomic_flag *, ...)`)
- [✓] §7.17.8.1p2 (C11) — Atomically set the value pointed to by `object` to true; memory is affected according to `order`; these are read-modify-write operations. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.17.8.1p3 (C11) — Returns: the `atomic_flag_test_and_set` generic functions return the value of the `atomic_flag` pointed to by `object` immediately before the effects (the previous state). — mcc:✓ gcc:✓ clang:✓ (first `test_and_set` → 0 (was clear), second → 1 (was set), identical on all three)
- [~] §7.17.8.1 (C11) — Passing a non-`atomic_flag` argument is diagnosed (mcc declares the real `volatile atomic_flag *` signature rather than a `void*` builtin). — mcc:~ gcc:✗ clang:✓ (mcc **warns** "assignment from incompatible pointer type"; clang **errors**; **gcc accepts silently** — mcc is between the two, gcc/clang split)

##### §7.17.8.2 (C11) The `atomic_flag_clear` generic functions

- [✓] §7.17.8.2p1 (C11) — Synopsis: `void atomic_flag_clear(volatile atomic_flag *object);` and `void atomic_flag_clear_explicit(volatile atomic_flag *object, memory_order order);`. — mcc:✓ gcc:✓ clang:✓
- [~] §7.17.8.2p2 (C11) — Constraint: the `order` argument shall not be `memory_order_acquire`, `memory_order_consume`, nor `memory_order_acq_rel`. — mcc:~ gcc:~ clang:~ (runtime-UB restriction, diagnostic not required; gcc/clang warn, mcc does not — same QoI pattern as store/load)
- [✓] §7.17.8.2p3 (C11) — Atomically set the value pointed to by `object` to false; memory is affected according to `order`. — mcc:✓ gcc:✓ clang:✓ (`test_and_set` → `clear` → `test_and_set` returns 0 again, confirming clear, on all three)
- [✓] §7.17.8.2p4 (C11) — Returns: the `atomic_flag_clear` generic functions return no value. — mcc:✓ gcc:✓ clang:✓

#### Added gaps — `<stdatomic.h>` / `_Atomic` extras

- [✓] §6.7.2.4/§6.7.3 (C11) — The `_Atomic` type qualifier and the `_Atomic(type-name)` specifier form are both accepted (`_Atomic int`, `int *_Atomic`, `_Atomic(int*)` all declare cleanly). — mcc:✓ gcc:✓ clang:✓
- [✓] §6.5.2.4/§6.5.16.2 (C11) — An `_Atomic`-qualified object used with plain operators (`a=5; a++; a+=10;`) performs implicit atomic read-modify-write; result 16 identical on all three. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.17.1p3 (C11) — Both signed and unsigned variants share the lock-free macro for their common rank (e.g. `atomic_int`/`atomic_uint` both governed by `ATOMIC_INT_LOCK_FREE`); confirmed via the per-rank macro set. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.17.6/§7.17.1 (C11) — `atomic_char16_t`/`atomic_char32_t` are typedef'd over `uint_least16_t`/`uint_least32_t` (mcc) — compatible with the `char16_t`/`char32_t` mapping; objects of both declare cleanly. — mcc:✓ gcc:✓ clang:✓
- [~] §6.10.8.3 (C11) — `__GCC_ATOMIC_*_LOCK_FREE` predefined macros: gcc and clang predefine the full `__GCC_ATOMIC_*` / `__CLANG_ATOMIC_*` set; **mcc predefines only the `__ATOMIC_*` memory-order constants** (0..5), not the `__GCC_ATOMIC_*_LOCK_FREE` family. — mcc:~ gcc:✓ clang:✓ (the public `ATOMIC_*_LOCK_FREE` macros are still provided by mcc's header, so portable code is unaffected; only the gcc/clang-internal `__GCC_ATOMIC_*` predefines are absent — not a C11 requirement)
- [✓] §7.17.7 (C11) — Each `_explicit` generic accepts a runtime (non-constant) `memory_order` argument, not only a compile-time constant. — mcc:✓ gcc:✓ clang:✓ (verified via `memory_order` enum object passed through)


## <stddef.h> & <stdint.h>

> mcc status: 51 TODO items; 0 mcc-gaps. Every mechanically testable requirement was probed against mcc, gcc 15.3, and clang 22 on both the hosted (`#include_next` over glibc) and the freestanding (`-ffreestanding -nostdinc`) paths; mcc matches the gcc==clang consensus on all of them.

> Clause numbers differ between revisions: `<stddef.h>` is §7.17 in C99 (N1256) and §7.19 in C11 (N1570); `<stdint.h>` is §7.18 in C99 and §7.20 in C11. Citations give the C99 number with the C11 number in brackets. mcc ships both headers in `runtime/include/` (`stddef.h`, `stdint.h`). On a hosted target `<stdint.h>` does `#include_next <stdint.h>` (guarded by `__has_include_next`) and only back-fills the C11 contents when the system header was absent (detected via `#ifndef INT8_MAX`); on `-ffreestanding`/`-nostdinc` it defines the full contents itself. `<stddef.h>` is fully self-defined on every path. All requirements below were verified to compile under `mcc -std=c11` on both the hosted and freestanding paths.

### §7.17 [§7.19] Common definitions <stddef.h>

> C11 difference: C11 adds the type `max_align_t` to `<stddef.h>`; it is absent in C99. mcc gates it on `__STDC_VERSION__ >= 201112L`.

- [✓] §7.17p1 [§7.19p1] — `<stddef.h>` shall define the types `ptrdiff_t`, `size_t`, `wchar_t` and ((C11) `max_align_t`), and the macros `NULL` and `offsetof(type, member-designator)`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.17p2 [§7.19p2] — `ptrdiff_t` shall be the signed integer type of the result of subtracting two pointers. (mcc: `typedef __PTRDIFF_TYPE__ ptrdiff_t;`.) — mcc:✓ gcc:✓ clang:✓
- [✓] §7.17p2 [§7.19p2] — `size_t` shall be the unsigned integer type of the result of the `sizeof` operator. (mcc: `typedef __SIZE_TYPE__ size_t;`.) — mcc:✓ gcc:✓ clang:✓
- §7.17p2 [§7.19p2] — `wchar_t` shall be an integer type whose range of values can represent distinct codes for all members of the largest extended character set among the supported locales; the null character shall have code value zero, and a member of the basic character set shall have a code value equal to its value as the lone character of an integer character constant. (mcc: `typedef __WCHAR_TYPE__ wchar_t;`.) — (spec; not separately testable)
- [✓] §7.17p2 [§7.19p2] — (C11) `max_align_t` shall be an object type whose alignment is as great as is supported by the implementation in all contexts. (mcc: `typedef union { long long __ll; long double __ld; } max_align_t;`, defined only when `__STDC_VERSION__ >= 201112L`.) — mcc:✓ gcc:✓ clang:✓
- [✓] §7.17p2 [§7.19p2] — (C11) `max_align_t` is absent under C99; including `<stddef.h>` with `-std=c99` must not define it. (mcc: gated by `__STDC_VERSION__ >= 201112L`.) — mcc:✓ gcc:✓ clang:✓ (all three reject `max_align_t` under `-std=c99`)
- [✓] §7.17p3 [§7.19p3] — `NULL` shall expand to an implementation-defined null pointer constant. (mcc: `#define NULL ((void*)0)`, guarded by `#ifndef NULL`.) — mcc:✓ gcc:✓ clang:✓
- [✓] §7.17p3 [§7.19p3] — `NULL`'s `#ifndef` guard must not warn-redefine when another header (e.g. `<stdio.h>`) already defined `NULL`. (mcc: `#ifndef NULL`.) — mcc:✓ gcc:✓ clang:✓ (`-Wall` clean with `<stdio.h>` included first)
- [✓] §7.17p3 [§7.19p3] — `offsetof(type, member-designator)` shall expand to an integer constant expression of type `size_t` whose value is the offset in bytes, to the structure member designated by `member-designator`, from the beginning of its structure designated by `type`. (mcc: `#define offsetof(type, field) __builtin_offsetof(type, field)`, preceded by `#undef offsetof`.) — mcc:✓ gcc:✓ clang:✓ (`_Generic` confirms result type is exactly `size_t`)
- [✓] §7.17p3 [§7.19p3] — `member-designator` may include array subscripting and member access (e.g. `offsetof(struct S, arr[3])`). (mcc: forwarded to `__builtin_offsetof`.) — mcc:✓ gcc:✓ clang:✓
- §7.17p3 [§7.19p3] — `type` and `member-designator` shall be such that, given `static type t;`, the expression `&(t.member-designator)` evaluates to an address constant. (Constraint on the program; mcc enforces it via `__builtin_offsetof`.) — (spec; not separately testable)
- [✓] §7.17p3 footnote [§7.19p3] — If `member-designator` designates a bit-field, the behavior is undefined. (mcc/gcc/clang: rejected as required.) — mcc:✓ gcc:✓ clang:✓ (all three reject `offsetof` of a bit-field member with a diagnostic)
- [✓] (namespace) §7.1.3 — A strictly conforming `<stddef.h>` defines only `ptrdiff_t`, `size_t`, `max_align_t`, `wchar_t`, `NULL`, and `offsetof`; no other identifiers are introduced. (mcc: keeps `ssize_t`/`intptr_t`/`uintptr_t` out of `<stddef.h>` on the ELF path, matching gcc's own `<stddef.h>`; supplies `ssize_t`/`intptr_t`/`uintptr_t` here only on the `_WIN32`/PE target to match MinGW's `<stddef.h>`.) — mcc:✓ gcc:✓ clang:✓ (re-declaring `intptr_t`/`ssize_t`/`uintptr_t` as plain `int` after `<stddef.h>` compiles cleanly on all three ⇒ no leak)
- (extension) — On `__need_wint_t`, `<stddef.h>` additionally defines `wint_t` (`typedef __WINT_TYPE__ wint_t;`, guarded by `_WINT_T`) and then `#undef`s `__need_wint_t`; this glibc-compatibility hook does not introduce identifiers in the default include. (mcc-specific.) — (spec; not separately testable)

### §7.18 [§7.20] Integer types <stdint.h>

- [✓] §7.18p1 [§7.20p1] — `<stdint.h>` declares sets of integer types having specified widths and defines corresponding sets of macros, and also defines macros specifying limits of integer types defined in other standard headers. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.18p1 [§7.20p1] — Types are defined in the following categories: exact-width (§7.18.1.1), minimum-width (§7.18.1.2), fastest minimum-width (§7.18.1.3), integer types wide enough to hold object pointers (§7.18.1.4), and greatest-width integer types (§7.18.1.5). (Cross-reference to subclauses below; mcc provides all five categories.) — mcc:✓ gcc:✓ clang:✓
- [✓] §7.18p2 [§7.20p2] — For each type described herein, if the implementation provides integer types meeting the stated requirements, it shall declare the typedef name and define the associated macros; required types/macros are present unconditionally and optional ones only when provided. (mcc: provides all required types plus the common optional exact-width 8/16/32/64 set on every supported target.) — mcc:✓ gcc:✓ clang:✓
- [✓] §7.18p3 [§7.20p3] — A program shall not define the macros named in this header (e.g. `INTN_MAX`, `INTN_C`); the header may be included more than once with consistent effect. (mcc: idempotent via the `_MCC_STDINT_H` include guard.) — mcc:✓ gcc:✓ clang:✓ (double-inclusion `-Wall` clean)
- [✓] §7.18p4/p5 [§7.20p4/p5] — Each macro name corresponding to a type, and each limit macro, shall expand to a constant expression suitable for use in `#if` preprocessing directives. (mcc: `INTn_MIN/MAX`, `UINTn_MAX`, and the `*_C` results are all integer constant expressions usable in `#if`.) — mcc:✓ gcc:✓ clang:✓ (battery of `#if` tests over INT32_MAX/UINT64_MAX/INTMAX_C/PTRDIFF_MAX/SIZE_MAX/WCHAR_MAX/WINT_MAX/SIG_ATOMIC_MAX/INTPTR_MAX all pass)

#### §7.18.1 [§7.20.1] Integer types

##### §7.18.1.1 [§7.20.1.1] Exact-width integer types

- [✓] §7.18.1.1p1 [§7.20.1.1p1] — `intN_t` shall designate a signed integer type with width exactly `N`, no padding bits, and a two's complement representation (e.g. `int8_t` is exactly 8 bits). (mcc: `int8_t`=`signed char`, `int16_t`=`short`, `int32_t`=`int`, `int64_t`=`__INT64_TYPE__`.) — mcc:✓ gcc:✓ clang:✓ (`sizeof` of int8/16/32/64_t = 1/2/4/8 confirmed)
- [✓] §7.18.1.1p1 [§7.20.1.1p1] — `uintN_t` shall designate an unsigned integer type with width exactly `N` and no padding bits. (mcc: `uint8_t`=`unsigned char`, `uint16_t`=`unsigned short`, `uint32_t`=`unsigned int`, `uint64_t`=`unsigned __INT64_TYPE__`.) — mcc:✓ gcc:✓ clang:✓
- [✓] §7.18.1.1p2/p3 [§7.20.1.1p2/p3] — These typedef names are optional; however, if the implementation provides integer types with widths of 8, 16, 32, or 64 bits, no padding bits, and (for the signed types) a two's complement representation, it shall define the corresponding `intN_t`/`uintN_t`. (mcc: defines the full 8/16/32/64 exact-width set.) — mcc:✓ gcc:✓ clang:✓

##### §7.18.1.2 [§7.20.1.2] Minimum-width integer types

- [✓] §7.18.1.2p1 [§7.20.1.2p1] — `int_leastN_t` shall designate a signed integer type with width at least `N` such that no signed integer type with lesser size has at least the specified width; `uint_leastN_t` shall designate the analogous smallest unsigned type (e.g. `int_least8_t`, `uint_least16_t`). (mcc: each `int_leastN_t`/`uint_leastN_t` aliases the corresponding exact-width type.) — mcc:✓ gcc:✓ clang:✓
- [✓] §7.18.1.2p2 [§7.20.1.2p2] — The required types are `int_least8_t`, `int_least16_t`, `int_least32_t`, `int_least64_t`, `uint_least8_t`, `uint_least16_t`, `uint_least32_t`, `uint_least64_t`. (mcc: all eight present.) — mcc:✓ gcc:✓ clang:✓
- §7.18.1.2p3 [§7.20.1.2p3] — All other types of this form are optional. (mcc: defines no others.) — (spec; not separately testable)

##### §7.18.1.3 [§7.20.1.3] Fastest minimum-width integer types

- [✓] §7.18.1.3p1 [§7.20.1.3p1] — `int_fastN_t` shall designate the fastest signed integer type with width at least `N`; `uint_fastN_t` the fastest unsigned integer type with width at least `N`. (mcc: each `int_fastN_t`/`uint_fastN_t` aliases the corresponding exact-width type.) — mcc:✓ gcc:✓ clang:✓
- §7.18.1.3p2 [§7.20.1.3p2] — "Fastest" means usually fastest to operate with among types having at least the specified width; if no clear winner, any qualifying type may be chosen. (Implementation-defined; mcc chooses the exact-width type.) — (spec; not separately testable)
- [✓] §7.18.1.3p3 [§7.20.1.3p3] — The required types are `int_fast8_t`, `int_fast16_t`, `int_fast32_t`, `int_fast64_t`, `uint_fast8_t`, `uint_fast16_t`, `uint_fast32_t`, `uint_fast64_t`. (mcc: all eight present.) — mcc:✓ gcc:✓ clang:✓
- §7.18.1.3p4 [§7.20.1.3p4] — All other types of this form are optional. (mcc: defines no others.) — (spec; not separately testable)

##### §7.18.1.4 [§7.20.1.4] Integer types capable of holding object pointers

- [✓] §7.18.1.4p1 [§7.20.1.4p1] — `intptr_t` (optional) shall designate a signed integer type such that any valid `void*` can be converted to it and back, comparing equal to the original. (mcc: `typedef __INTPTR_TYPE__ intptr_t;`; provided on all targets.) — mcc:✓ gcc:✓ clang:✓
- [✓] §7.18.1.4p1 [§7.20.1.4p1] — `uintptr_t` (optional) shall designate an unsigned integer type with the same `void*` round-trip property. (mcc: `typedef __UINTPTR_TYPE__ uintptr_t;`; provided on all targets.) — mcc:✓ gcc:✓ clang:✓

##### §7.18.1.5 [§7.20.1.5] Greatest-width integer types

- [✓] §7.18.1.5p1 [§7.20.1.5p1] — `intmax_t` (required) shall designate a signed integer type capable of representing any value of any signed integer type. (mcc: `typedef long long intmax_t;`.) — mcc:✓ gcc:✓ clang:✓
- [✓] §7.18.1.5p1 [§7.20.1.5p1] — `uintmax_t` (required) shall designate an unsigned integer type capable of representing any value of any unsigned integer type. (mcc: `typedef unsigned long long uintmax_t;`.) — mcc:✓ gcc:✓ clang:✓

#### §7.18.2 [§7.20.2] Limits of specified-width integer types

- [✓] §7.18.2p1 [§7.20.2p1] — Each of the following object-like macros specifying a min/max limit of a `<stdint.h>` type shall expand to a constant expression usable in `#if`, having the same type as an object of the corresponding type converted per the integer promotions. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.18.2p1 [§7.20.2p1] — Each implementation-defined value shall be equal to or greater in magnitude than the corresponding value listed, with the same sign, except where the limit is stated to be exactly the given value (exact-width limits). (mcc: exact-width limits match exactly.) — mcc:✓ gcc:✓ clang:✓

##### §7.18.2.1 [§7.20.2.1] Limits of exact-width integer types

- [✓] §7.18.2.1p1 [§7.20.2.1p1] — `INTN_MIN` shall equal exactly −(2^(N−1)): `INT8_MIN`=−128, `INT16_MIN`=−32768, `INT32_MIN`=−2147483648, `INT64_MIN`=−9223372036854775808. (mcc: `INT8_MIN (-128)`, `INT16_MIN (-32768)`, `INT32_MIN (-2147483647 - 1)`, `INT64_MIN (-__LONG_LONG_MAX__ - 1)`.) — mcc:✓ gcc:✓ clang:✓
- [✓] §7.18.2.1p1 [§7.20.2.1p1] — `INTN_MAX` shall equal exactly 2^(N−1)−1: `INT8_MAX`=127, `INT16_MAX`=32767, `INT32_MAX`=2147483647, `INT64_MAX`=9223372036854775807. (mcc: `INT8_MAX 127`, `INT16_MAX 32767`, `INT32_MAX 2147483647`, `INT64_MAX __LONG_LONG_MAX__`.) — mcc:✓ gcc:✓ clang:✓
- [✓] §7.18.2.1p1 [§7.20.2.1p1] — `UINTN_MAX` shall equal exactly 2^N−1: `UINT8_MAX`=255, `UINT16_MAX`=65535, `UINT32_MAX`=4294967295, `UINT64_MAX`=18446744073709551615. (mcc: `UINT8_MAX 255`, `UINT16_MAX 65535`, `UINT32_MAX 4294967295U`, `UINT64_MAX (__LONG_LONG_MAX__ * 2ULL + 1ULL)`.) — mcc:✓ gcc:✓ clang:✓ (`_Static_assert` on every value passes)

##### §7.18.2.2 [§7.20.2.2] Limits of minimum-width integer types

- [✓] §7.18.2.2p1 [§7.20.2.2p1] — `INT_LEASTN_MIN` ≤ {−127, −32767, −2147483647, −9223372036854775807}; `INT_LEASTN_MAX` ≥ {127, 32767, 2147483647, 9223372036854775807}; `UINT_LEASTN_MAX` ≥ {255, 65535, 4294967295, 18446744073709551615} for N = 8/16/32/64. (mcc: each `INT_LEASTN_MIN/MAX` and `UINT_LEASTN_MAX` is `#define`d to the exact-width limit, satisfying the bounds.) — mcc:✓ gcc:✓ clang:✓

##### §7.18.2.3 [§7.20.2.3] Limits of fastest minimum-width integer types

- [✓] §7.18.2.3p1 [§7.20.2.3p1] — `INT_FASTN_MIN` ≤ {−127, −32767, −2147483647, −9223372036854775807}; `INT_FASTN_MAX` ≥ {127, 32767, 2147483647, 9223372036854775807}; `UINT_FASTN_MAX` ≥ {255, 65535, 4294967295, 18446744073709551615} for N = 8/16/32/64. (mcc: each `INT_FASTN_MIN/MAX` and `UINT_FASTN_MAX` is `#define`d to the exact-width limit.) — mcc:✓ gcc:✓ clang:✓

##### §7.18.2.4 [§7.20.2.4] Limits of integer types capable of holding object pointers

- [✓] §7.18.2.4p1 [§7.20.2.4p1] — If `intptr_t` is defined, `INTPTR_MIN` ≤ −32767 and `INTPTR_MAX` ≥ 32767. (mcc: keyed on `__SIZEOF_POINTER__` — `INT64_MIN`/`INT64_MAX` when pointer is 8 bytes, else `INT32_MIN`/`INT32_MAX`.) — mcc:✓ gcc:✓ clang:✓
- [✓] §7.18.2.4p1 [§7.20.2.4p1] — If `uintptr_t` is defined, `UINTPTR_MAX` ≥ 65535. (mcc: `UINT64_MAX` when pointer is 8 bytes, else `UINT32_MAX`.) — mcc:✓ gcc:✓ clang:✓

##### §7.18.2.5 [§7.20.2.5] Limits of greatest-width integer types

- [✓] §7.18.2.5p1 [§7.20.2.5p1] — `INTMAX_MIN` ≤ −9223372036854775807 and `INTMAX_MAX` ≥ 9223372036854775807. (mcc: `INTMAX_MIN INT64_MIN`, `INTMAX_MAX INT64_MAX`.) — mcc:✓ gcc:✓ clang:✓
- [✓] §7.18.2.5p1 [§7.20.2.5p1] — `UINTMAX_MAX` ≥ 18446744073709551615. (mcc: `UINTMAX_MAX UINT64_MAX`.) — mcc:✓ gcc:✓ clang:✓

#### §7.18.3 [§7.20.3] Limits of other integer types

- [✓] §7.18.3p1 [§7.20.3p1] — Each of the following object-like macros, specifying a min/max limit of an integer type defined in another standard header, shall expand to a constant expression usable in `#if`, has the same type as the corresponding type promoted per the integer promotions, and its value is equal to or greater in magnitude than the value shown, with the same sign. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.18.3p2 [§7.20.3p2] — Limits of `ptrdiff_t`: `PTRDIFF_MIN` ≤ −65535 and `PTRDIFF_MAX` ≥ 65535. (mcc: `PTRDIFF_MIN INTPTR_MIN`, `PTRDIFF_MAX INTPTR_MAX`.) — mcc:✓ gcc:✓ clang:✓ (runtime print: `PTRDIFF_MAX=9223372036854775807`)
- [✓] §7.18.3p3 [§7.20.3p3] — Limits of `sig_atomic_t` (`SIG_ATOMIC_MIN`/`SIG_ATOMIC_MAX`): if signed, `SIG_ATOMIC_MIN` ≤ −127 and `SIG_ATOMIC_MAX` ≥ 127; if unsigned, `SIG_ATOMIC_MIN`=0 and `SIG_ATOMIC_MAX` ≥ 255. (mcc: treats `sig_atomic_t` as signed — `SIG_ATOMIC_MIN INT32_MIN`, `SIG_ATOMIC_MAX INT32_MAX`.) — mcc:✓ gcc:✓ clang:✓ (all three: MIN=−2147483648, MAX=2147483647)
- [✓] §7.18.3p4 [§7.20.3p4] — Limit of `size_t`: `SIZE_MAX` ≥ 65535. (mcc: `SIZE_MAX UINTPTR_MAX`.) — mcc:✓ gcc:✓ clang:✓ (runtime print: `SIZE_MAX=18446744073709551615`)
- [✓] §7.18.3p5 [§7.20.3p5] — Limits of `wchar_t` (`WCHAR_MIN`/`WCHAR_MAX`): if signed, `WCHAR_MIN` ≤ −127 and `WCHAR_MAX` ≥ 127; if unsigned, `WCHAR_MIN`=0 and `WCHAR_MAX` ≥ 255. (mcc: `WCHAR_MIN INT32_MIN`, `WCHAR_MAX INT32_MAX`, each `#ifndef`-guarded against a prior system/SDK definition.) — mcc:✓ gcc:✓ clang:✓ (all three: MIN=−2147483648, MAX=2147483647 on this target)
- [✓] §7.18.3p6 [§7.20.3p6] — Limits of `wint_t` (`WINT_MIN`/`WINT_MAX`): if signed, `WINT_MIN` ≤ −32767 and `WINT_MAX` ≥ 32767; if unsigned, `WINT_MIN`=0 and `WINT_MAX` ≥ 65535. (mcc: treats `wint_t` as unsigned — `WINT_MIN 0`, `WINT_MAX UINT32_MAX`, each `#ifndef`-guarded.) — mcc:✓ gcc:✓ clang:✓ (all three: WINT_MIN=0, WINT_MAX=4294967295)

#### §7.18.4 [§7.20.4] Macros for integer constants

- [✓] §7.18.4p1 [§7.20.4p1] — The following function-like macros expand to integer constants suitable for initializing objects of integer types corresponding to types defined in `<stdint.h>`; each macro name corresponds to a similarly named type in §7.18.1.2/§7.18.1.5. — mcc:✓ gcc:✓ clang:✓
- §7.18.4p2 [§7.20.4p2] — The argument shall be an unsuffixed integer constant (per §6.4.4.1) whose value does not exceed the limits of the corresponding type. (Constraint on the program.) — (spec; not separately testable)
- [✓] §7.18.4p3 [§7.20.4p3] — Each invocation shall expand to an integer constant expression suitable for use in `#if`, whose value and type are obtained by suffixing/casting the unsuffixed argument to the corresponding type. (mcc/gcc/clang: OK.) — mcc:✓ gcc:✓ clang:✓ (`#if INTMAX_C(5)!=5` passes)

##### §7.18.4.1 [§7.20.4.1] Macros for minimum-width integer constants

- [✓] §7.18.4.1p1 [§7.20.4.1p1] — `INTN_C(value)` shall expand to an integer constant expression with type `int_leastN_t`, and `UINTN_C(value)` with type `uint_leastN_t` (e.g. if `uint_least64_t` is `unsigned long long int`, `UINT64_C(0x123)` may expand to `0x123ULL`). (mcc: `INT8_C/INT16_C/INT32_C(x)` and `UINT8_C/UINT16_C(x)` are identity; `UINT32_C(x)` is `x ## U`; `INT64_C`/`UINT64_C` pick `L`/`UL` when `__SIZEOF_LONG__==8` else `LL`/`ULL`, so the result has type `int_least64_t`/`uint_least64_t` exactly even on LP64.) — mcc:✓ gcc:✓ clang:✓ (`_Static_assert(sizeof(INT64_C(1))==sizeof(int_least64_t))` and the `UINT64_C`/`UINT32_C` analogues pass on both hosted and freestanding LP64 paths)

##### §7.18.4.2 [§7.20.4.2] Macros for greatest-width integer constants

- [✓] §7.18.4.2p1 [§7.20.4.2p1] — `INTMAX_C(value)` shall expand to an integer constant expression with type `intmax_t`, and `UINTMAX_C(value)` with type `uintmax_t`. (mcc: `INTMAX_C(x)` is `x ## LL`, `UINTMAX_C(x)` is `x ## ULL`, matching `intmax_t`/`uintmax_t` = `long long`/`unsigned long long`.) — mcc:✓ gcc:✓ clang:✓ (`_Static_assert(sizeof(INTMAX_C(1))==sizeof(intmax_t))` and the unsigned analogue pass)

#### Additional `<stdint.h>` items (gaps the draft omitted)

- [✓] §7.18.1.1 [§7.20.1.1] (optional) — `RSIZE_MAX` and the bounds-checking interfaces of Annex K are NOT mandated by the base standard and are only present when an implementation provides Annex K; absence is conforming. (mcc: `RSIZE_MAX` undefined.) — mcc:✓(absent) gcc:✓(absent) clang:✓(absent) — none of the three define `RSIZE_MAX` by default
- [✓] §6.10.1/§7.18 [§7.20] — On a hosted target mcc obtains all `<stdint.h>` types and macros by `#include_next` over the system `<stdint.h>` (here glibc); the back-fill block under `#ifndef INT8_MAX` is skipped, so glibc's exact typedefs and `*_C` definitions are used. (mcc-specific layering.) — mcc:✓ (hosted values match gcc==clang exactly)
- [✓] §7.18 [§7.20] — On `-ffreestanding -nostdinc` (no reachable system header) mcc self-defines the entire C11 `<stdint.h>` content; all types, limit macros, and `*_C` type-correctness hold on this path too. (mcc-specific.) — mcc:✓ gcc:✓ clang:✓ (freestanding probe with INT64 limits, `INT64_C` type check, and `offsetof` all pass)

### Cross-cutting: freestanding & namespace

- [✓] §4p6 / §6.10.8 — `<stddef.h>` and `<stdint.h>` (along with `<float.h>`, `<limits.h>`, `<iso646.h>`, (C11) `<stdalign.h>`, `<stdarg.h>`, `<stdbool.h>`, `<stdnoreturn.h>`) shall be available in a freestanding environment. (mcc: both headers are self-contained on `-ffreestanding -nostdinc -I runtime/include`; `<stddef.h>` always self-defines and `<stdint.h>` back-fills the full C11 contents when no system header is reachable.) — mcc:✓ gcc:✓ clang:✓ (compiled and ran under `-ffreestanding`)
- [✓] §7.1.3 — All identifiers defined in these headers (the typedefs, the limit/`*_C` macros, `NULL`, `offsetof`) are reserved, and the `__`-prefixed helpers used to implement them are in the implementation's reserved namespace. (mcc: implements the typedefs via reserved predefined macros `__SIZE_TYPE__`/`__PTRDIFF_TYPE__`/`__WCHAR_TYPE__`/`__WINT_TYPE__`/`__INT64_TYPE__`/`__INTPTR_TYPE__`/`__UINTPTR_TYPE__` and the include guards `_STDDEF_H`/`_MCC_STDINT_H`.) — mcc:✓ gcc:✓ clang:✓
- §7.20p2 [§7.8] (informative) — `<stdint.h>` integer types/macros must remain consistent with `<inttypes.h>`, which itself includes `<stdint.h>` and adds the `PRI*`/`SCN*` format-specifier macros; the limit and `*_C` macros defined here are the ones `<inttypes.h>` relies upon. (mcc: `<inttypes.h>` is layered atop this header.) — (spec; not separately testable)


## §7.21 <stdio.h>

> mcc status: 280 TODO checkboxes below; 1 mcc-gap. mcc ships **no** `<stdio.h>` of its own — it consumes the platform (glibc) header and links the platform libc, exactly as gcc and clang do here, so every macro/type/function/runtime-conversion requirement is satisfied identically across all three (verified: BUFSIZ=8192, FOPEN_MAX=16, TMP_MAX=238328, EOF=-1, all conversions, file ops, and the whole `v*` family produce byte-identical results). mcc now performs `-Wformat` checking of `printf`/`scanf` conversion-specifier vs argument types, matching gcc/clang. The remaining mcc-specific gap is **diagnostic only**: mcc silently accepts a call to an **undeclared** stdio function (e.g. the C11-removed `gets`) as an implicit declaration, where gcc/clang error — not a conformance violation (implicit-decl is a constraint mcc declines to diagnose), but a divergence from the gcc==clang consensus.

> Numbering follows C11 (N1570); the corresponding C99 (N1256) clause is §7.19 with identical paragraph structure unless a line is tagged otherwise. `mcc` ships no `<stdio.h>` of its own — it consumes the platform libc header — so every requirement below is "does the compiler+headers+runtime triple accept/implement it", checked against the system libc that mcc, gcc, and clang each use.
> C11 vs C99 material deltas in this clause: C11 added the `"x"` (exclusive-create) `fopen`/`freopen` mode forms; C11 **removed** the `gets` function (§7.21.7.7 in C99 only); C99 (relative to C90) had earlier added `snprintf`, the `v*scanf`/`vsnprintf` family, the `hh`/`ll`/`j`/`z`/`t` length modifiers, the `a`/`A`/`F` conversions, and the inf/nan output styles.

### §7.21.1 Introduction

- [✓] §7.21.1p1 — `<stdio.h>` shall declare three types, several macros, and many functions for performing input and output. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.21.1p2 — `<stdio.h>` shall define the type `size_t` (the unsigned integer type of the result of `sizeof`, as in §7.19). — mcc:✓ gcc:✓ clang:✓
- [✓] §7.21.1p2 — `<stdio.h>` shall define the type `FILE`, an object type capable of recording all the information needed to control a stream: its file position indicator, a pointer to its associated buffer (if any), an error indicator that records whether a read/write error has occurred, and an end-of-file indicator that records whether end-of-file has been reached. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.21.1p2 — `<stdio.h>` shall define the type `fpos_t`, an object type other than an array type capable of recording all the information needed to specify uniquely every position within a file. — mcc:✓ gcc:✓ clang:✓ (declared `fpos_t p;` compiles and `fgetpos`/`fsetpos` accept it on all three)
- [✓] §7.21.1p3 — `<stdio.h>` shall define `NULL` (the null-pointer-constant macro, as in §7.19). — mcc:✓ gcc:✓ clang:✓
- [✓] §7.21.1p3 — `<stdio.h>` shall define `_IOFBF`, `_IOLBF`, and `_IONBF`, each expanding to an integer constant expression with distinct value, suitable for use as the third argument to `setvbuf`. — mcc:✓ gcc:✓ clang:✓ (constraint `_IOFBF!=_IOLBF!=_IONBF` checked in a constant-expression array bound)
- [✓] §7.21.1p3 — `<stdio.h>` shall define `BUFSIZ`, an integer constant expression that is the size of the buffer used by the `setbuf` function. — mcc:✓ gcc:✓ clang:✓ (BUFSIZ=8192)
- [✓] §7.21.1p3 — `<stdio.h>` shall define `EOF`, which expands to an integer constant expression, with type `int` and a negative value, that is returned by several functions to indicate end-of-file. — mcc:✓ gcc:✓ clang:✓ (EOF=-1, `EOF<0` in ICE)
- [✓] §7.21.1p3 — `<stdio.h>` shall define `FOPEN_MAX`, an integer constant expression that is the minimum number of files that can be open simultaneously. — mcc:✓ gcc:✓ clang:✓ (FOPEN_MAX=16)
- [✓] §7.21.1p3 — `<stdio.h>` shall define `FILENAME_MAX`, an integer constant expression that is the size needed for an array of `char` large enough to hold the longest file name string that the implementation guarantees can be opened. — mcc:✓ gcc:✓ clang:✓ (FILENAME_MAX=4096)
- [✓] §7.21.1p3 — `<stdio.h>` shall define `L_tmpnam`, an integer constant expression that is the size needed for an array of `char` large enough to hold a temporary file name string generated by the `tmpnam` function. — mcc:✓ gcc:✓ clang:✓ (L_tmpnam=20)
- [✓] §7.21.1p3 — `<stdio.h>` shall define `SEEK_CUR`, `SEEK_END`, and `SEEK_SET`, each expanding to an integer constant expression with distinct value, suitable for use as the third argument to `fseek`. — mcc:✓ gcc:✓ clang:✓ (distinctness checked in ICE)
- [✓] §7.21.1p3 — `<stdio.h>` shall define `TMP_MAX`, an integer constant expression that is the minimum number of unique file names that can be generated by the `tmpnam` function. — mcc:✓ gcc:✓ clang:✓ (TMP_MAX=238328)
- [✓] §7.21.1p3 — `<stdio.h>` shall define `stderr`, `stdin`, and `stdout`, each an expression of type "pointer to `FILE`" that points to the `FILE` object associated, respectively, with the standard error, input, and output streams. — mcc:✓ gcc:✓ clang:✓ (all three usable as `FILE*`)
- [✓] §7.21.1p4 — `FOPEN_MAX` shall be at least 8, including the three standard text streams. — mcc:✓ gcc:✓ clang:✓ (16 ≥ 8, checked in ICE)

### §7.21.2 Streams

- §7.21.2p1 — Input and output shall be mapped into logical data streams (text and binary) whose properties are more uniform than their various inputs and outputs. — (spec; not separately testable — shared runtime model)
- §7.21.2p2 — A text stream shall be an ordered sequence of characters composed into lines, each terminated by a new-line; whether the last line requires a terminating new-line is implementation-defined. — (spec; not separately testable — shared runtime)
- §7.21.2p2 — Characters may be added/altered/deleted on input/output to conform to host text conventions; no one-to-one correspondence is required. — (spec; not separately testable — shared runtime)
- §7.21.2p2 — Text data read in shall compare equal to data written only if it is printing chars + HT/NL, no NL is preceded by spaces, and the last char is a new-line. — (spec; not separately testable — shared runtime)
- §7.21.2p2 — Whether trailing space characters before a new-line appear when read back is implementation-defined. — (spec; not separately testable — shared runtime)
- §7.21.2p3 — A binary stream shall transparently record internal data; data read back shall compare equal (under the same implementation), except an implementation may append null characters to fill a medium. — (spec; not separately testable — shared runtime)
- [✓] §7.21.2p4 — Environmental limits: support text files with lines of at least 254 characters (incl. NL); `BUFSIZ` ≥ 256. — mcc:✓ gcc:✓ clang:✓ (BUFSIZ=8192 ≥ 256, checked in ICE)
- §7.21.2p5 — The characteristics of the stream interfaces and file environment are as described in the stream model. — (spec; not separately testable)

### §7.21.3 Files

- [ ] §7.21.3p1 — A stream shall be associated with an external file by opening it (possibly creating it); creating an existing file discards its former contents if necessary. — (spec; runtime — shared libc)
- [ ] §7.21.3p1 — For positionable files, the file position indicator starts at character zero unless opened in append mode, where its initial position is implementation-defined. — (spec; runtime — shared libc)
- [ ] §7.21.3p1 — The file position indicator shall be maintained by reads/writes/positioning; all input is as if by `fgetc`, all output as if by `fputc`. — (spec; runtime — shared libc)
- [ ] §7.21.3p2 — Binary files are not truncated except per §7.21.5.3; whether a write on a text stream truncates beyond that point is implementation-defined. — (spec; runtime — shared libc)
- [✓] §7.21.3p3 — Unbuffered streams transmit chars as soon as possible; fully buffered accumulate into a block; line buffered transmit a block on new-line. — mcc:✓ gcc:✓ clang:✓ (`setvbuf`/`setbuf` with `_IOFBF`/`_IOLBF`/`_IONBF` accepted and effective)
- [ ] §7.21.3p3 — Chars are transmitted as a block when a buffer fills, on unbuffered input, or on line-buffered input requiring host transmission; affected by `setbuf`/`setvbuf`. — (spec; runtime — shared libc)
- [ ] §7.21.3p4 — Closing disassociates a file; output streams are flushed before disassociation; a `FILE*` value is indeterminate after the file is closed. — (spec; runtime — shared libc)
- §7.21.3p4 — Whether a special file is actually created is unspecified; a file may have other implementation-defined restrictions. — (spec; not separately testable)
- §7.21.3p5 — Whether a zero-length file on which nothing was written actually exists is implementation-defined. — (spec; not separately testable)
- [ ] §7.21.3p6 — The file may be reopened and its contents reclaimed/modified if it can be repositioned at its start. — (spec; runtime — shared libc)
- [ ] §7.21.3p6 — On return from `main` or call to `exit`, all open files are closed (all output flushed); abnormal paths such as `abort` need not close files properly. — (spec; runtime — shared libc)
- [ ] §7.21.3p7 — The address of the controlling `FILE` object may be significant; a copy of a `FILE` need not serve in place of the original. — (spec; runtime — shared libc)
- [✓] §7.21.3p8 — At startup three text streams are predefined: standard input, standard output, standard error. — mcc:✓ gcc:✓ clang:✓ (`stdin`/`stdout`/`stderr` usable without `fopen`)
- [ ] §7.21.3p8 — As initially opened, stderr is not fully buffered; stdin/stdout are fully buffered iff the stream can be determined not to refer to an interactive device. — (spec; runtime — shared libc)
- §7.21.3p9 — Opening nontemporary files requires a file-name string; valid-name rules are implementation-defined, as is whether the same file may be open multiple times. — (spec; not separately testable)
- §7.21.3p10 — The library does not directly support wide-oriented text/binary streams; orientation is in §7.29.3. — (spec; not separately testable)
- [✓] §7.21.3p11 — Environmental limit: `FOPEN_MAX` shall be at least 8, including the three standard text streams. — mcc:✓ gcc:✓ clang:✓ (16 ≥ 8)

### §7.21.4 Operations on files

#### §7.21.4.1 The remove function

- [✓] §7.21.4.1p1 — Synopsis: `int remove(const char *filename);` — mcc:✓ gcc:✓ clang:✓ (declared & callable; used in x-mode probe)
- [✓] §7.21.4.1p2 — `remove` shall make the named file no longer accessible by that name; a later open by that name fails unless re-created. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- §7.21.4.1p2 — If the file is open, the behavior of `remove` is implementation-defined. — (spec; not separately testable)
- [✓] §7.21.4.1p3 — Returns: zero on success, nonzero on failure. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)

#### §7.21.4.2 The rename function

- [✓] §7.21.4.2p1 — Synopsis: `int rename(const char *old, const char *new);` — mcc:✓ gcc:✓ clang:✓ (declared & callable)
- [✓] §7.21.4.2p2 — `rename` shall make `old` henceforth known by `new`; `old` is no longer accessible. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- §7.21.4.2p2 — If a file named `new` exists prior to the call, the behavior is implementation-defined. — (spec; not separately testable)
- [✓] §7.21.4.2p3 — Returns: zero on success, nonzero on failure (in which case, if the file existed, it keeps its original name). — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)

#### §7.21.4.3 The tmpfile function

- [✓] §7.21.4.3p1 — Synopsis: `FILE *tmpfile(void);` — mcc:✓ gcc:✓ clang:✓ (declared & callable; used in probe)
- [✓] §7.21.4.3p2 — `tmpfile` shall create a unique temporary binary file auto-removed on close/termination, opened with `"wb+"` mode. — mcc:✓ gcc:✓ clang:✓ (probe wrote/seeked/read a tmpfile successfully on all three)
- §7.21.4.3p3 — Recommended practice: at least `TMP_MAX` temporary files openable over the program lifetime (shareable with `tmpnam`); no simultaneous-open limit beyond `FOPEN_MAX`. (C99 note.) — (spec; recommended practice; not separately testable)
- [✓] §7.21.4.3p4 — Returns: pointer to the created stream, or a null pointer if it cannot be created. — mcc:✓ gcc:✓ clang:✓ (probe got non-null)

#### §7.21.4.4 The tmpnam function

- [✓] §7.21.4.4p1 — Synopsis: `char *tmpnam(char *s);` — mcc:✓ gcc:✓ clang:✓ (declared & callable)
- [✓] §7.21.4.4p2 — `tmpnam` shall generate a valid file name not the same as an existing file; capable of up to `TMP_MAX` distinct strings. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [ ] §7.21.4.4p3 — `tmpnam` shall generate a different string each call; may modify the same static object each time. — (spec; runtime — shared libc)
- [✓] §7.21.4.4p4 — If `s` is null, the result is left in an internal static object and a pointer to it is returned; otherwise `s` (≥ `L_tmpnam` chars) receives the result and `s` is returned. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.4.4p4 — Returns: null pointer if no suitable string can be generated; otherwise the internal-object pointer (null arg) or `s`. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.4.4p5 — Environmental limit: `TMP_MAX` shall be at least 25. — mcc:✓ gcc:✓ clang:✓ (238328 ≥ 25, checked in ICE)

### §7.21.5 File access functions

#### §7.21.5.1 The fclose function

- [✓] §7.21.5.1p1 — Synopsis: `int fclose(FILE *stream);` — mcc:✓ gcc:✓ clang:✓ (declared & callable; used in probe)
- [✓] §7.21.5.1p2 — A successful `fclose` shall flush the stream and close the file; unwritten buffered data is delivered, unread buffered data discarded. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [ ] §7.21.5.1p2 — Whether or not the call succeeds, the stream is disassociated and any `setbuf`/`setvbuf` buffer is disassociated (and deallocated if auto-allocated). — (spec; runtime — shared libc)
- [✓] §7.21.5.1p3 — Returns: zero on success, `EOF` if any errors were detected. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)

#### §7.21.5.2 The fflush function

- [✓] §7.21.5.2p1 — Synopsis: `int fflush(FILE *stream);` — mcc:✓ gcc:✓ clang:✓ (declared & callable)
- [✓] §7.21.5.2p2 — For an output/most-recent-not-input update stream, `fflush` delivers unwritten data; otherwise behavior is undefined. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.5.2p3 — If `stream` is null, `fflush` flushes all streams for which the behavior is defined. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.5.2p4 — Returns: sets the error indicator and returns `EOF` on write error, otherwise zero. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)

#### §7.21.5.3 The fopen function

- [✓] §7.21.5.3p1 — Synopsis: `FILE *fopen(const char * restrict filename, const char * restrict mode);` — mcc:✓ gcc:✓ clang:✓ (declared with `restrict`, accepted & callable)
- [✓] §7.21.5.3p2 — `fopen` shall open the named file and associate a stream with it. — mcc:✓ gcc:✓ clang:✓ (probe opened a file)
- [✓] §7.21.5.3p3 — The `mode` string shall accept `"r"`,`"w"`,`"a"`,`"rb"`,`"wb"`,`"ab"`,`"r+"`,`"w+"`,`"a+"` and the binary-update forms `"r+b"`/`"rb+"`,`"w+b"`/`"wb+"`,`"a+b"`/`"ab+"`; otherwise undefined. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.5.3p3 — (C11) The mode string may take exclusive forms `"wx"`,`"wbx"`,`"w+x"`,`"w+bx"`,`"wb+x"`: exclusive create fails if the file already exists. (C99-only: `x` forms do not exist.) — mcc:✓ gcc:✓ clang:✓ (probe: second `fopen("...","wx")` returned null = failed-exclusive on all three)
- [✓] §7.21.5.3p4 — Opening with `r` first character shall fail if the file does not exist or cannot be read. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [ ] §7.21.5.3p5 — Opening with `a` first character forces all writes to the then-current end of file regardless of `fseek`; a binary append may initially position beyond last data due to null padding. — (spec; runtime — shared libc)
- [ ] §7.21.5.3p6 — In update mode (`+`), both input and output may be performed, but output→input requires an intervening `fflush`/positioning, and input→output requires intervening positioning unless input hit EOF. — (spec; runtime — shared libc)
- §7.21.5.3p6 — Opening a text file with update mode may instead open a binary stream in some implementations. — (spec; not separately testable)
- [ ] §7.21.5.3p7 — When opened, a stream is fully buffered iff it can be determined not to refer to an interactive device; the error and EOF indicators are cleared. — (spec; runtime — shared libc)
- [✓] §7.21.5.3p8 — Returns: a pointer to the controlling object, or a null pointer if the open fails. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)

#### §7.21.5.4 The freopen function

- [✓] §7.21.5.4p1 — Synopsis: `FILE *freopen(const char * restrict filename, const char * restrict mode, FILE * restrict stream);` — mcc:✓ gcc:✓ clang:✓ (declared & callable)
- [✓] §7.21.5.4p2 — `freopen` shall open the named file and associate `stream` with it; `mode` is used as in `fopen` (incl. C11 `x` forms). — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- §7.21.5.4p2 — If `filename` is null, `freopen` attempts to change the stream's mode to `mode`; which mode changes are permitted is implementation-defined. — (spec; not separately testable)
- [ ] §7.21.5.4p3 — `freopen` shall first attempt to close any associated file (failure ignored); the error and EOF indicators are cleared. — (spec; runtime — shared libc)
- [✓] §7.21.5.4p4 — Returns: null pointer if the open fails, otherwise the value of `stream`. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)

#### §7.21.5.5 The setbuf function

- [✓] §7.21.5.5p1 — Synopsis: `void setbuf(FILE * restrict stream, char * restrict buf);` — mcc:✓ gcc:✓ clang:✓ (declared & callable; `setbuf(stderr,NULL)` in probe)
- [✓] §7.21.5.5p2 — `setbuf` shall be equivalent to `setvbuf` with `_IOFBF`/`BUFSIZ`, or (if `buf` is null) with `_IONBF`. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.5.5p3 — Returns: no value. — mcc:✓ gcc:✓ clang:✓ (void return)

#### §7.21.5.6 The setvbuf function

- [✓] §7.21.5.6p1 — Synopsis: `int setvbuf(FILE * restrict stream, char * restrict buf, int mode, size_t size);` — mcc:✓ gcc:✓ clang:✓ (declared & callable; used in probe)
- [ ] §7.21.5.6p2 — `setvbuf` may be used only after association with an open file and before any other operation (other than an unsuccessful `setvbuf`). — (spec; runtime — shared libc)
- [✓] §7.21.5.6p2 — `mode` `_IOFBF`/`_IOLBF`/`_IONBF` selects fully/line/unbuffered. — mcc:✓ gcc:✓ clang:✓ (accepted; line-buffering effective in probe)
- [ ] §7.21.5.6p2 — If `buf` is non-null, the array it points to may be used as the buffer; `size` specifies its size; the array contents are indeterminate at any time. — (spec; runtime — shared libc)
- [✓] §7.21.5.6p3 — Returns: zero on success, nonzero on invalid `mode` or unhonorable request. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)

### §7.21.6 Formatted input/output functions

> mcc `-Wformat` (diagnostic): mcc emits a `-Wformat` warning for a conversion-specifier/argument type mismatch (e.g. `printf("%d","str")` → "format '%d' expects an integer argument"), matching gcc (`format '%d' expects ... 'int' but argument has type 'char *'`) and clang. The runtime behavior (UB per p9) is identical because all three call the same glibc `printf`. See the items under §7.21.6.1 and §7.21.6.2.

#### §7.21.6.1 The fprintf function

- [✓] §7.21.6.1p1 — Synopsis: `int fprintf(FILE * restrict stream, const char * restrict format, ...);` — mcc:✓ gcc:✓ clang:✓ (declared & callable)
- [✓] §7.21.6.1p2 — `fprintf` shall write output to `stream` under control of `format`, converting subsequent arguments. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [ ] §7.21.6.1p2 — Insufficient arguments → undefined; excess arguments are evaluated but ignored; `fprintf` returns at end of format. — (spec; runtime — shared libc)
- [✓] §7.21.6.1p3 — The format is a multibyte sequence of ordinary characters (copied) and conversion specifications. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.1p4 — Each conversion spec is `%` then, in sequence: flags, optional minimum field width, optional precision, optional length modifier, conversion specifier. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.1p5 — Field width/precision may be `*` (taken from an `int` arg, appearing before the value); negative width = `-` flag + positive width; negative precision = omitted precision. — mcc:✓ gcc:✓ clang:✓ (`%*d`/`%.*f` produced identical output)
- [✓] §7.21.6.1p6 — Flag `-`: left-justify within the field. — mcc:✓ gcc:✓ clang:✓ (`%-5d` → `42   `)
- [✓] §7.21.6.1p6 — Flag `+`: signed conversion always begins with a sign. — mcc:✓ gcc:✓ clang:✓ (`%+d` → `+3`)
- [✓] §7.21.6.1p6 — Flag space: prefix a space if no sign would appear; ignored if `+` also present. — mcc:✓ gcc:✓ clang:✓ (`% d` → ` 3`)
- [✓] §7.21.6.1p6 — Flag `#`: alternative form (force `o` leading 0; `0x`/`0X` prefix on nonzero `x`/`X`; always a decimal point for `a/A/e/E/f/F/g/G`; keep trailing zeros for `g/G`); else undefined. — mcc:✓ gcc:✓ clang:✓ (`%#x` → `0xff`)
- [✓] §7.21.6.1p6 — Flag `0`: zero-pad to field width for numeric conversions (except inf/NaN); ignored with `-`; for `d/i/o/u/x/X` ignored when a precision is given; else undefined. — mcc:✓ gcc:✓ clang:✓ (`%05d` → `00042`)
- [✓] §7.21.6.1p7 — Length modifier `hh`: `d/i/o/u/x/X` apply to `signed/unsigned char`; `n` to pointer-to-`signed char`. (C99 added `hh`.) — mcc:✓ gcc:✓ clang:✓ (`%hhd` → `-3`)
- [✓] §7.21.6.1p7 — Length modifier `h`: applies to `short`/`unsigned short`; `n` to pointer-to-`short`. — mcc:✓ gcc:✓ clang:✓ (`%hd` → `-4`)
- [✓] §7.21.6.1p7 — Length modifier `l` (ell): applies to `long`/`unsigned long`; `n` to pointer-to-`long`; `c`→`wint_t`; `s`→pointer-to-`wchar_t`; no effect on `a/A/e/E/f/F/g/G`. — mcc:✓ gcc:✓ clang:✓ (`%ld` → `6`)
- [✓] §7.21.6.1p7 — Length modifier `ll`: applies to `long long`/`unsigned long long`; `n` to pointer-to-`long long`. (C99 added `ll`.) — mcc:✓ gcc:✓ clang:✓ (`%lld` → `7`)
- [✓] §7.21.6.1p7 — Length modifier `j`: applies to `intmax_t`/`uintmax_t`; `n` to pointer-to-`intmax_t`. (C99 added `j`.) — mcc:✓ gcc:✓ clang:✓ (`%ju` → `8`)
- [✓] §7.21.6.1p7 — Length modifier `z`: applies to `size_t`/its signed counterpart; `n` to pointer thereto. (C99 added `z`.) — mcc:✓ gcc:✓ clang:✓ (`%zu` → `9`)
- [✓] §7.21.6.1p7 — Length modifier `t`: applies to `ptrdiff_t`/its unsigned counterpart; `n` to pointer thereto. (C99 added `t`.) — mcc:✓ gcc:✓ clang:✓ (`%td` → `10`)
- [✓] §7.21.6.1p7 — Length modifier `L`: applies to `long double` for `a/A/e/E/f/F/g/G`. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- §7.21.6.1p7 — A length modifier with any other conversion specifier is undefined. — (spec; UB; not separately testable)
- [✓] §7.21.6.1p8 — `d`,`i`: signed decimal `[-]dddd`; precision = min digits; default 1; zero value with precision 0 → no characters. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.1p8 — `o`,`u`,`x`,`X`: unsigned octal/decimal/hex; `abcdef` for `x`, `ABCDEF` for `X`; precision = min digits; default 1; zero/precision-0 → no characters. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.1p8 — `f`,`F`: decimal `[-]ddd.ddd`; precision digits after point (default 6); no point if precision 0 and no `#`; rounded. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.1p8 — `f`,`F` (non-finite): infinity → `[-]inf`/`[-]infinity`; NaN → `[-]nan`/`[-]nan(...)`; `F` yields `INF`/`INFINITY`/`NAN`; `#`/`0` have no effect. (C99 added `F` and inf/nan styles.) — mcc:✓ gcc:✓ clang:✓ (`%f`→`inf`, `%F`→`-INF`, `%e`→`-nan` at runtime identical)
- [✓] §7.21.6.1p8 — `e`,`E`: `[-]d.ddde±dd`; precision digits after point (default 6); exponent ≥ 2 digits; `E` uppercase; inf/NaN as `f`/`F`. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.1p8 — `g`,`G`: style `f`/`e` (or `F`/`E`) with precision = significant digits (0→1); `e` used iff exponent < −4 or ≥ precision; trailing zeros removed unless `#`; inf/NaN as `f`/`F`. — mcc:✓ gcc:✓ clang:✓ (`%g` of 123456 → `123456`)
- [✓] §7.21.6.1p8 — `a`,`A`: `[-]0xh.hhhhp±d`; precision controls hex digits; `abcdef`/`ABCDEF`; `A` uses `X`/`P`. (C99 added `a`/`A`.) — mcc:✓ gcc:✓ clang:✓ (`%a`→`0x1.8p+0`, `%A`→`0X1.8P+0`)
- [✓] §7.21.6.1p8 — `a`,`A` (rounding): exact if representable in the precision, otherwise correctly rounded per the current rounding direction. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.1p8 — `c`: without `l`, `int` arg → `unsigned char` written; with `l`, `wint_t` as if `ls`. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.1p8 — `s` (no `l`): pointer to a char array, written up to (not incl.) the null; precision limits bytes; without precision the array must contain a null. — mcc:✓ gcc:✓ clang:✓ (`%.3s` of "hello" → `hel`)
- [✓] §7.21.6.1p8 — `s` (with `l`): pointer to `wchar_t` array converted as if `wcrtomb`; written up to (not incl.) the null; precision limits bytes; no partial multibyte char written. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.1p8 — `p`: pointer to `void` converted to printing characters in an implementation-defined manner. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.1p8 — `n`: pointer to a signed integer receiving the count written so far; no argument converted (one consumed); flags/width/precision → undefined. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.1p8 — `%`: a `%` is written; complete spec is `%%`. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- §7.21.6.1p9 — An invalid conversion spec, or an argument of the wrong type, is undefined. — (spec; UB; not separately testable)
- [✓] §7.21.6.1p10 — A nonexistent/small field width never truncates; the field expands to contain the result. — mcc:✓ gcc:✓ clang:✓ (`%*d` width 4 with "7" still prints `   7`)
- [ ] §7.21.6.1p11 — Recommended practice: for `a`/`A` with `FLT_RADIX` a power of 2, correctly round to a hex float of the given precision. — (spec; recommended practice; runtime — shared libc)
- [ ] §7.21.6.1p12 — Recommended practice: for `e/E/f/F/g/G`, correct rounding within `DECIMAL_DIG` bounds. (C99.) — (spec; recommended practice; runtime — shared libc)
- [✓] §7.21.6.1p13 — The result is rounded per the current rounding direction; the precision indicates the rounding precision. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.1p14 — Returns: number of characters transmitted, or a negative value on output/encoding error. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.1p15 — Environmental limit: at least 4095 characters producible by a single conversion. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] **GAP** §7.21.6.1p4/p7/p9 — A compiler *may* (and gcc/clang do under `-Wall`/`-Wformat`) diagnose `printf`/`fprintf` conversion-specifier vs argument-type mismatches and invalid length-modifier/specifier pairings. mcc emits such warnings too (verified: `printf("%d\n","x")` warns "format '%d' expects an integer argument" under mcc, as under gcc and clang). — mcc:✓ gcc:✓ clang:✓ (mcc checks `printf` formats like gcc/clang — diagnostic-only, not a conformance defect)

#### §7.21.6.2 The fscanf function

- [✓] §7.21.6.2p1 — Synopsis: `int fscanf(FILE * restrict stream, const char * restrict format, ...);` — mcc:✓ gcc:✓ clang:✓ (declared & callable)
- [✓] §7.21.6.2p2 — `fscanf` shall read input under control of `format`, using subsequent arguments as pointers to receiving objects. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [ ] §7.21.6.2p2 — Insufficient arguments → undefined; excess arguments evaluated but ignored. — (spec; runtime — shared libc)
- [✓] §7.21.6.2p3 — The format is a multibyte sequence of white-space directives, ordinary characters, and conversion specifications. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.2p4 — Each conversion spec is `%` then: optional `*`, optional max field width (> 0), optional length modifier, conversion specifier. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.2p5 — Directives are executed in turn; on completion or a directive failure the function returns; failures are input failures or matching failures. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.2p6 — A white-space directive reads up to the first non-white-space char (left unread) or until no more chars; it never fails. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.2p7 — An ordinary-character directive reads matching characters; a mismatch (or EOF/encoding/read error) fails the directive, leaving differing/subsequent chars unread. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [ ] §7.21.6.2p8 — A conversion specification defines a set of matching input sequences and is executed in the following steps. — (spec; runtime — shared libc)
- [✓] §7.21.6.2p9 — Input white-space is skipped unless the specifier is `[`, `c`, or `n`; skipped white-space is not counted against the field width. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.2p10 — An input item (longest matching prefix within the field width) is read; zero length → matching failure (or input failure on EOF/encoding/read error). — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.2p11 — The item is converted per the specifier; on mismatch the directive fails; unless `*`, the result is stored in the next non-received argument; wrong type/unrepresentable → undefined. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.2p11 — Length modifier `hh`: `d/i/o/u/x/X/n` → pointer to `signed/unsigned char`. (C99 added `hh`.) — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.2p11 — Length modifier `h`: → pointer to `short`/`unsigned short`. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.2p11 — Length modifier `l`: integer specs → pointer to `long`/`unsigned long`; float specs → pointer to `double`; `c/s/[` → pointer to `wchar_t`. — mcc:✓ gcc:✓ clang:✓ (probe `%lf` read `45.6` correctly)
- [✓] §7.21.6.2p11 — Length modifier `ll`: → pointer to `long long`/`unsigned long long`. (C99 added `ll`.) — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.2p11 — Length modifier `j`: → pointer to `intmax_t`/`uintmax_t`. (C99 added `j`.) — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.2p11 — Length modifier `z`: → pointer to `size_t`/its signed counterpart. (C99 added `z`.) — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.2p11 — Length modifier `t`: → pointer to `ptrdiff_t`/its unsigned counterpart. (C99 added `t`.) — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.2p11 — Length modifier `L`: float specs → pointer to `long double`. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- §7.21.6.2p11 — A length modifier with any other specifier is undefined. — (spec; UB; not separately testable)
- [✓] §7.21.6.2p12 — `d`: optionally-signed decimal as `strtol` base 10; arg pointer to signed integer. — mcc:✓ gcc:✓ clang:✓ (probe `%d` read `123`)
- [✓] §7.21.6.2p12 — `i`: optionally-signed integer as `strtol` base 0; arg pointer to signed integer. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.2p12 — `o`: octal as `strtoul` base 8; arg pointer to unsigned integer. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.2p12 — `u`: decimal as `strtoul` base 10; arg pointer to unsigned integer. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.2p12 — `x`: hex as `strtoul` base 16; arg pointer to unsigned integer. — mcc:✓ gcc:✓ clang:✓ (probe `%x` read `ff`)
- [✓] §7.21.6.2p12 — `a`,`e`,`f`,`g` (and `A`,`E`,`F`,`G`): floating/inf/NaN as `strtod`; arg pointer to floating. (C99 made `a/A/F` and uppercase aliases valid.) — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.2p12 — `c`: exactly field-width chars (1 default); arg pointer to char array (no null added); with `l`, multibyte→`wchar_t` via `mbrtowc`. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.2p12 — `s`: non-white-space sequence; arg pointer to char array (null added); with `l`, multibyte→`wchar_t` array (null wide char added). — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.2p12 — `[`: nonempty sequence from the scanset; arg pointer to char array (null added); with `l`, → `wchar_t` array. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.2p12 — `[` (scanset syntax): chars up to the matching `]`; leading `^` negates; leading `]`/`^]` includes `]`; significance of `-` is implementation-defined. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.2p12 — `p`: implementation-defined sequence (matching `%p` output); arg pointer to pointer-to-`void`; round-trip pointers compare equal. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.2p12 — `n`: no input consumed; arg pointer to signed integer receiving the count read so far; does not increment the assignment count; `*`/field-width → undefined. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.2p12 — `%`: matches a single `%`; no conversion/assignment; complete spec `%%`. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- §7.21.6.2p13 — An invalid conversion specification is undefined. — (spec; UB; not separately testable)
- [✓] §7.21.6.2p14 — `A`,`E`,`F`,`G`,`X` are valid and behave as `a`,`e`,`f`,`g`,`x`. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.2p15 — Trailing white-space is left unread unless matched; literal-match/suppressed-assignment success is observable only via `%n`. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.2p16 — Returns: `EOF` on input failure before the first conversion completes; otherwise the number of items assigned (possibly fewer or zero). — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] **GAP** §7.21.6.2p4/p11/p13 — gcc/clang diagnose `scanf`/`fscanf` specifier vs pointer-argument-type mismatches under `-Wformat`; mcc does too (verified: `scanf("%d",&d)` with `double d` warns "format '%d' expects a pointer to int argument", same root cause as the `fprintf` check). — mcc:✓ gcc:✓ clang:✓ (diagnostic-only)

#### §7.21.6.3 The printf function

- [✓] §7.21.6.3p1 — Synopsis: `int printf(const char * restrict format, ...);` — mcc:✓ gcc:✓ clang:✓ (declared & callable)
- [✓] §7.21.6.3p2 — `printf` shall be equivalent to `fprintf` with `stdout` interposed. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.3p3 — Returns: number of characters transmitted, or negative on output/encoding error. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)

#### §7.21.6.4 The scanf function

- [✓] §7.21.6.4p1 — Synopsis: `int scanf(const char * restrict format, ...);` — mcc:✓ gcc:✓ clang:✓ (declared & callable)
- [✓] §7.21.6.4p2 — `scanf` shall be equivalent to `fscanf` with `stdin` interposed. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.4p3 — Returns: `EOF` on early input failure; otherwise the number of items assigned. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)

#### §7.21.6.5 The snprintf function

> C99-only addition: `snprintf` did not exist in C90.

- [✓] §7.21.6.5p1 — Synopsis: `int snprintf(char * restrict s, size_t n, const char * restrict format, ...);` — mcc:✓ gcc:✓ clang:✓ (declared & callable)
- [✓] §7.21.6.5p2 — `snprintf` shall be like `fprintf` but write into array `s`; if `n` is zero nothing is written and `s` may be null; otherwise chars beyond `n-1` are discarded and a null terminator is written; overlap is undefined. — mcc:✓ gcc:✓ clang:✓ (probe: `snprintf(b,5,...)` of "abcdefgh" → `abcd`)
- [✓] §7.21.6.5p3 — Returns: the number of characters that would have been written (excl. null), or negative on encoding error. — mcc:✓ gcc:✓ clang:✓ (probe returned 8 from a 5-byte buffer on all three)

#### §7.21.6.6 The sprintf function

- [✓] §7.21.6.6p1 — Synopsis: `int sprintf(char * restrict s, const char * restrict format, ...);` — mcc:✓ gcc:✓ clang:✓ (declared & callable)
- [✓] §7.21.6.6p2 — `sprintf` shall be like `fprintf` but write into array `s` with a null terminator; overlap is undefined. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.6p3 — Returns: the number of characters written (excl. null), or negative on encoding error. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)

#### §7.21.6.7 The sscanf function

- [✓] §7.21.6.7p1 — Synopsis: `int sscanf(const char * restrict s, const char * restrict format, ...);` — mcc:✓ gcc:✓ clang:✓ (declared & callable; used in probe)
- [✓] §7.21.6.7p2 — `sscanf` shall be like `fscanf` but read from string `s`; reaching the end of the string is equivalent to EOF; overlap is undefined. — mcc:✓ gcc:✓ clang:✓ (probe parsed `"123 45.6 ff"` correctly)
- [✓] §7.21.6.7p3 — Returns: `EOF` on early input failure; otherwise the number of items assigned. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)

#### §7.21.6.8 The vfprintf function

- [✓] §7.21.6.8p1 — Synopsis: `#include <stdarg.h>` … `int vfprintf(FILE * restrict stream, const char * restrict format, va_list arg);` — mcc:✓ gcc:✓ clang:✓ (declared & callable; wrapped in probe)
- [✓] §7.21.6.8p2 — `vfprintf` shall be like `fprintf` with the varargs replaced by `arg` (initialized by `va_start`); it does not invoke `va_end`. — mcc:✓ gcc:✓ clang:✓ (probe `vf(stdout,"a%d\n",1)` → `a1`)
- [✓] §7.21.6.8p3 — Returns: number of characters transmitted, or negative on error. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)

#### §7.21.6.9 The vfscanf function

> C99-only addition: `vfscanf` did not exist in C90.

- [✓] §7.21.6.9p1 — Synopsis: `#include <stdarg.h>` … `int vfscanf(FILE * restrict stream, const char * restrict format, va_list arg);` — mcc:✓ gcc:✓ clang:✓ (declared & callable)
- [✓] §7.21.6.9p2 — `vfscanf` shall be like `fscanf` with varargs replaced by `arg`; it does not invoke `va_end`. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.9p3 — Returns: `EOF` on early input failure; otherwise the number of items assigned. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)

#### §7.21.6.10 The vprintf function

- [✓] §7.21.6.10p1 — Synopsis: `#include <stdarg.h>` … `int vprintf(const char * restrict format, va_list arg);` — mcc:✓ gcc:✓ clang:✓ (declared & callable; wrapped in probe)
- [✓] §7.21.6.10p2 — `vprintf` shall be like `printf` with varargs replaced by `arg`; it does not invoke `va_end`. — mcc:✓ gcc:✓ clang:✓ (probe `vp("vp %d\n",99)` → `vp 99`)
- [✓] §7.21.6.10p3 — Returns: number of characters transmitted, or negative on error. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)

#### §7.21.6.11 The vscanf function

> C99-only addition: `vscanf` did not exist in C90.

- [✓] §7.21.6.11p1 — Synopsis: `#include <stdarg.h>` … `int vscanf(const char * restrict format, va_list arg);` — mcc:✓ gcc:✓ clang:✓ (declared & callable)
- [✓] §7.21.6.11p2 — `vscanf` shall be like `scanf` with varargs replaced by `arg`; it does not invoke `va_end`. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.11p3 — Returns: `EOF` on early input failure; otherwise the number of items assigned. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)

#### §7.21.6.12 The vsnprintf function

> C99-only addition: `vsnprintf` did not exist in C90.

- [✓] §7.21.6.12p1 — Synopsis: `#include <stdarg.h>` … `int vsnprintf(char * restrict s, size_t n, const char * restrict format, va_list arg);` — mcc:✓ gcc:✓ clang:✓ (declared & callable; wrapped in probe)
- [✓] §7.21.6.12p2 — `vsnprintf` shall be like `snprintf` with varargs replaced by `arg`; it does not invoke `va_end`; overlap is undefined. — mcc:✓ gcc:✓ clang:✓ (probe `vsn(b,64,"b%d",2)` → `b2`)
- [✓] §7.21.6.12p3 — Returns: the number that would have been written (excl. null), or negative on encoding error. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)

#### §7.21.6.13 The vsprintf function

- [✓] §7.21.6.13p1 — Synopsis: `#include <stdarg.h>` … `int vsprintf(char * restrict s, const char * restrict format, va_list arg);` — mcc:✓ gcc:✓ clang:✓ (declared & callable)
- [✓] §7.21.6.13p2 — `vsprintf` shall be like `sprintf` with varargs replaced by `arg`; it does not invoke `va_end`; overlap is undefined. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.6.13p3 — Returns: the number written (excl. null), or negative on encoding error. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)

#### §7.21.6.14 The vsscanf function

> C99-only addition: `vsscanf` did not exist in C90.

- [✓] §7.21.6.14p1 — Synopsis: `#include <stdarg.h>` … `int vsscanf(const char * restrict s, const char * restrict format, va_list arg);` — mcc:✓ gcc:✓ clang:✓ (declared & callable; wrapped in probe)
- [✓] §7.21.6.14p2 — `vsscanf` shall be like `sscanf` with varargs replaced by `arg`; it does not invoke `va_end`. — mcc:✓ gcc:✓ clang:✓ (probe `vsf("7","%d",&x)` set x=7)
- [✓] §7.21.6.14p3 — Returns: `EOF` on early input failure; otherwise the number of items assigned. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)

### §7.21.7 Character input/output functions

#### §7.21.7.1 The fgetc function

- [✓] §7.21.7.1p1 — Synopsis: `int fgetc(FILE *stream);` — mcc:✓ gcc:✓ clang:✓ (declared & callable; used in probe)
- [✓] §7.21.7.1p2 — If not at EOF and a next char is present, `fgetc` shall obtain it as an `unsigned char` converted to `int` and advance the position indicator. — mcc:✓ gcc:✓ clang:✓ (probe read `h`)
- [✓] §7.21.7.1p3 — Returns: `EOF` (and sets the EOF indicator) at end-of-file; otherwise the next char; on read error sets the error indicator and returns `EOF`. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)

#### §7.21.7.2 The fgets function

- [✓] §7.21.7.2p1 — Synopsis: `char *fgets(char * restrict s, int n, FILE * restrict stream);` — mcc:✓ gcc:✓ clang:✓ (declared & callable)
- [✓] §7.21.7.2p2 — `fgets` shall read at most `n-1` chars; stops after a new-line (retained) or EOF; writes a null after the last char. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.7.2p3 — Returns: `s` on success; null at EOF-with-no-chars (array unchanged); null on read error (array contents indeterminate). — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)

#### §7.21.7.3 The fputc function

- [✓] §7.21.7.3p1 — Synopsis: `int fputc(int c, FILE *stream);` — mcc:✓ gcc:✓ clang:✓ (declared & callable)
- [✓] §7.21.7.3p2 — `fputc` shall write `c` (as `unsigned char`) at the position indicator and advance it; for non-positionable or append streams the char is appended. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.7.3p3 — Returns: the char written; on write error sets the error indicator and returns `EOF`. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)

#### §7.21.7.4 The fputs function

- [✓] §7.21.7.4p1 — Synopsis: `int fputs(const char * restrict s, FILE * restrict stream);` — mcc:✓ gcc:✓ clang:✓ (declared & callable; used in probe)
- [✓] §7.21.7.4p2 — `fputs` shall write the string `s`; the terminating null is not written. — mcc:✓ gcc:✓ clang:✓ (probe wrote "hello" to a tmpfile)
- [✓] §7.21.7.4p3 — Returns: `EOF` on write error; otherwise a nonnegative value. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)

#### §7.21.7.5 The getc function

- [✓] §7.21.7.5p1 — Synopsis: `int getc(FILE *stream);` — mcc:✓ gcc:✓ clang:✓ (declared & callable; glibc may implement as a macro)
- [✓] §7.21.7.5p2 — `getc` shall be like `fgetc`, except a macro implementation may evaluate `stream` more than once (no side effects in the argument). — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.7.5p3 — Returns: the next char; `EOF` (with indicator set) at EOF or on read error. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)

#### §7.21.7.6 The getchar function

- [✓] §7.21.7.6p1 — Synopsis: `int getchar(void);` — mcc:✓ gcc:✓ clang:✓ (declared & callable)
- [✓] §7.21.7.6p2 — `getchar` shall be equivalent to `getc(stdin)`. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.7.6p3 — Returns: the next char from `stdin`; `EOF` (with indicator set) at EOF or on read error. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)

#### §7.21.7.7 The gets function (C99-only — removed in C11)

> Present in C99 §7.19.7.7; removed from the C11 library. A conforming C11 `<stdio.h>` shall not declare `gets`. Verified: none of the three's glibc header declares `gets`. gcc and clang therefore **reject** a call to `gets` (error: implicit/undeclared function in C99+ default mode); **mcc silently accepts** the implicit declaration — an mcc divergence from the gcc==clang consensus (see GAP below).

- [✓] §7.21.7.7p1 — (C99-only) Synopsis: `char *gets(char *s);` — mcc:✓ gcc:✓ clang:✓ (header correctly omits `gets` on all three, per C11 removal)
- [ ] §7.21.7.7p2 — (C99-only) `gets` shall read chars from `stdin` until EOF or a new-line (discarded), writing a null after the last char. — (spec; C99-only; function removed — runtime n/a)
- [ ] §7.21.7.7p3 — (C99-only) Returns: `s` on success; null at EOF-with-no-chars (array unchanged); null on read error (array indeterminate). — (spec; C99-only; function removed — runtime n/a)
- [✗] **GAP** §7.21.7.7 / §6.5.2.2 — Calling the (C11-removed, undeclared) `gets` should be a constraint violation requiring a diagnostic in C99/C11 (implicit function declaration). gcc errors (`-Wimplicit-function-declaration`), clang errors (`call to undeclared function`); **mcc compiles it silently** — mcc accepts implicit function declarations by default. — mcc:✗ gcc:✓ clang:✓ (mcc accepts; gcc/clang reject — affects any undeclared stdio function, not just `gets`)

#### §7.21.7.8 The putc function

- [✓] §7.21.7.8p1 — Synopsis: `int putc(int c, FILE *stream);` — mcc:✓ gcc:✓ clang:✓ (declared & callable)
- [✓] §7.21.7.8p2 — `putc` shall be like `fputc`, except a macro implementation may evaluate `stream` more than once. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.7.8p3 — Returns: the char written; `EOF` (with error indicator set) on write error. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)

#### §7.21.7.9 The putchar function

- [✓] §7.21.7.9p1 — Synopsis: `int putchar(int c);` — mcc:✓ gcc:✓ clang:✓ (declared & callable; used in probe)
- [✓] §7.21.7.9p2 — `putchar` shall be equivalent to `putc(c, stdout)`. — mcc:✓ gcc:✓ clang:✓ (probe `putchar('Z')` → `Z`)
- [✓] §7.21.7.9p3 — Returns: the char written; `EOF` (with error indicator set) on write error. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)

#### §7.21.7.10 The puts function

- [✓] §7.21.7.10p1 — Synopsis: `int puts(const char *s);` — mcc:✓ gcc:✓ clang:✓ (declared & callable; used in probe)
- [✓] §7.21.7.10p2 — `puts` shall write the string `s` to `stdout` and append a new-line; the terminating null is not written. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.7.10p3 — Returns: `EOF` on write error; otherwise a nonnegative value. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)

#### §7.21.7.11 The ungetc function

- [✓] §7.21.7.11p1 — Synopsis: `int ungetc(int c, FILE *stream);` — mcc:✓ gcc:✓ clang:✓ (declared & callable; used in probe)
- [✓] §7.21.7.11p2 — `ungetc` shall push `c` (as `unsigned char`) back; pushed chars are returned in reverse order; an intervening positioning call discards pushback; external storage is unchanged. — mcc:✓ gcc:✓ clang:✓ (probe pushed back `h`)
- [✓] §7.21.7.11p3 — At least one character of pushback is guaranteed; too many calls without an intervening read/positioning may fail. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.7.11p4 — If `c` equals `EOF`, the operation fails and the input stream is unchanged. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [ ] §7.21.7.11p5 — A successful `ungetc` clears the EOF indicator; position-indicator semantics differ for text (unspecified until pushback consumed) vs binary (decremented; indeterminate if it was zero). — (spec; runtime — shared libc)
- [✓] §7.21.7.11p6 — Returns: the char pushed back (after conversion), or `EOF` if the operation fails. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)

### §7.21.8 Direct input/output functions

#### §7.21.8.1 The fread function

- [✓] §7.21.8.1p1 — Synopsis: `size_t fread(void * restrict ptr, size_t size, size_t nmemb, FILE * restrict stream);` — mcc:✓ gcc:✓ clang:✓ (declared & callable)
- [✓] §7.21.8.1p2 — `fread` shall read up to `nmemb` elements of `size` bytes via `fgetc`-equivalent reads, advancing the position indicator; error → indeterminate position; partial element → indeterminate value. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.8.1p3 — Returns: the number of elements read (may be < `nmemb` on error/EOF); if `size` or `nmemb` is zero, returns zero with array/stream unchanged. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)

#### §7.21.8.2 The fwrite function

- [✓] §7.21.8.2p1 — Synopsis: `size_t fwrite(const void * restrict ptr, size_t size, size_t nmemb, FILE * restrict stream);` — mcc:✓ gcc:✓ clang:✓ (declared & callable)
- [✓] §7.21.8.2p2 — `fwrite` shall write up to `nmemb` elements of `size` bytes via `fputc`-equivalent writes, advancing the position indicator; error → indeterminate position. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.8.2p3 — Returns: the number of elements written (< `nmemb` only on write error); if `size` or `nmemb` is zero, returns zero with stream unchanged. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)

### §7.21.9 File positioning functions

#### §7.21.9.1 The fgetpos function

- [✓] §7.21.9.1p1 — Synopsis: `int fgetpos(FILE * restrict stream, fpos_t * restrict pos);` — mcc:✓ gcc:✓ clang:✓ (declared & callable; used in probe)
- [✓] §7.21.9.1p2 — `fgetpos` shall store the current parse state and position into `*pos` for later use by `fsetpos`. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.9.1p3 — Returns: zero on success; nonzero on failure with an implementation-defined positive value in `errno`. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)

#### §7.21.9.2 The fseek function

- [✓] §7.21.9.2p1 — Synopsis: `int fseek(FILE *stream, long int offset, int whence);` — mcc:✓ gcc:✓ clang:✓ (declared & callable; used in probe)
- [✓] §7.21.9.2p2 — `fseek` shall set the position indicator; a read/write error sets the error indicator and the call fails. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.9.2p3 — For a binary stream, the new position is `offset` added to `SEEK_SET`/`SEEK_CUR`/`SEEK_END`; a binary stream need not meaningfully support `SEEK_END`. — mcc:✓ gcc:✓ clang:✓ (probe `fseek(tf,0,SEEK_SET)`)
- [ ] §7.21.9.2p4 — For a text stream, `offset` shall be zero, or a value from an earlier `ftell` with `whence` = `SEEK_SET`. — (spec; runtime — shared libc)
- [ ] §7.21.9.2p5 — A successful `fseek` undoes `ungetc` effects, clears the EOF indicator, and establishes the new position; the next update-stream operation may be input or output. — (spec; runtime — shared libc)
- [✓] §7.21.9.2p6 — Returns: nonzero only for a request that cannot be satisfied. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)

#### §7.21.9.3 The fsetpos function

- [✓] §7.21.9.3p1 — Synopsis: `int fsetpos(FILE *stream, const fpos_t *pos);` — mcc:✓ gcc:✓ clang:✓ (declared & callable; used in probe)
- [✓] §7.21.9.3p2 — `fsetpos` shall set the `mbstate_t` (if any) and position from `*pos` (from an earlier `fgetpos` on the same file); a read/write error sets the error indicator and the call fails. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [ ] §7.21.9.3p3 — A successful `fsetpos` undoes `ungetc` effects, clears the EOF indicator, and establishes the new parse state and position; the next update-stream operation may be input or output. — (spec; runtime — shared libc)
- [✓] §7.21.9.3p4 — Returns: zero on success; nonzero on failure with an implementation-defined positive value in `errno`. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)

#### §7.21.9.4 The ftell function

- [✓] §7.21.9.4p1 — Synopsis: `long int ftell(FILE *stream);` — mcc:✓ gcc:✓ clang:✓ (declared & callable; used in probe)
- [✓] §7.21.9.4p2 — `ftell` shall obtain the current position; for a binary stream this is the byte count from the start; for a text stream it is unspecified information usable by `fseek`. — mcc:✓ gcc:✓ clang:✓ (probe: `ftell` after writing "hello" returned 5)
- [✓] §7.21.9.4p3 — Returns: the current position on success; −1L on failure with an implementation-defined positive value in `errno`. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)

#### §7.21.9.5 The rewind function

- [✓] §7.21.9.5p1 — Synopsis: `void rewind(FILE *stream);` — mcc:✓ gcc:✓ clang:✓ (declared & callable; used in probe)
- [✓] §7.21.9.5p2 — `rewind` shall set the position indicator to the start; equivalent to `(void)fseek(stream,0L,SEEK_SET)` plus clearing the error indicator. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.9.5p3 — Returns: no value. — mcc:✓ gcc:✓ clang:✓ (void return)

### §7.21.10 Error-handling functions

#### §7.21.10.1 The clearerr function

- [✓] §7.21.10.1p1 — Synopsis: `void clearerr(FILE *stream);` — mcc:✓ gcc:✓ clang:✓ (declared & callable; used in probe)
- [✓] §7.21.10.1p2 — `clearerr` shall clear the EOF and error indicators for the stream. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.10.1p3 — Returns: no value. — mcc:✓ gcc:✓ clang:✓ (void return)

#### §7.21.10.2 The feof function

- [✓] §7.21.10.2p1 — Synopsis: `int feof(FILE *stream);` — mcc:✓ gcc:✓ clang:✓ (declared & callable; used in probe)
- [✓] §7.21.10.2p2 — `feof` shall test the EOF indicator for the stream. — mcc:✓ gcc:✓ clang:✓ (probe reported feof=0 before EOF)
- [✓] §7.21.10.2p3 — Returns: nonzero iff the EOF indicator is set. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)

#### §7.21.10.3 The ferror function

- [✓] §7.21.10.3p1 — Synopsis: `int ferror(FILE *stream);` — mcc:✓ gcc:✓ clang:✓ (declared & callable; used in probe)
- [✓] §7.21.10.3p2 — `ferror` shall test the error indicator for the stream. — mcc:✓ gcc:✓ clang:✓ (probe reported ferror=0)
- [✓] §7.21.10.3p3 — Returns: nonzero iff the error indicator is set. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)

#### §7.21.10.4 The perror function

- [✓] §7.21.10.4p1 — Synopsis: `void perror(const char *s);` — mcc:✓ gcc:✓ clang:✓ (declared & callable; used in probe)
- [✓] §7.21.10.4p2 — `perror` shall map `errno` to a message and write to stderr: optional `s:` prefix, the error message, and a new-line; the message text matches `strerror(errno)` and is implementation-defined. — mcc:✓ gcc:✓ clang:✓ (runtime — shared libc)
- [✓] §7.21.10.4p3 — Returns: no value. — mcc:✓ gcc:✓ clang:✓ (void return)


## §7.22 <stdlib.h>

> mcc status: 240 TODO items below; 1 mcc divergence from the gcc==clang consensus — and it is by-design, not a defect (mcc defaults to C99, hiding the C11-only `aligned_alloc`/`at_quick_exit`/`quick_exit` until `-std=c11`). mcc has **no bundled `<stdlib.h>`** — it resolves to the host glibc `/usr/include/stdlib.h`, the same header gcc/clang use, so types, macros, and runtime behavior are byte-for-byte identical across all three. mcc's only relevant difference is that its **default language mode is C99 (`__STDC_VERSION__==199901L`)** whereas gcc 15 defaults to C23 and clang 22 to C17; the C11-only functions (`aligned_alloc`, `at_quick_exit`, `quick_exit`) are gated by glibc behind `__USE_ISOC11`, so mcc must be invoked with `-std=c11` (or newer / `-D_ISOC11_SOURCE`) to see them — which it supports and then conforms. mcc honors `_Noreturn`, `restrict`, and glibc's `__attribute__((__noreturn__))` on the library prototypes.

> Clause numbering follows C11 (N1570) §7.22; the corresponding C99 (N1256) clause is §7.20 with the same subsection suffixes unless noted. Items added by C11 are tagged **(C11)** (`aligned_alloc`, `at_quick_exit`, `quick_exit`, the `_Noreturn` qualification on `exit`/`_Exit`, the data-race wording); items added by C99 over C90 are tagged **(C99)** (`lldiv_t`, `atoll`, `strtof`, `strtold`, `strtoll`, `strtoull`, `_Exit`, `llabs`, `lldiv`). Nothing in this header was removed or changed by C11 relative to C99.

### §7.22 General

- [✓] §7.22p1 — The header `<stdlib.h>` shall declare types and functions in five categories — numeric conversion, pseudo-random sequence generation, memory management, communication with the environment, searching/sorting/integer arithmetic, and multibyte/wide conversion — and shall define several macros. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22p2 — The header shall define the type `size_t` (the unsigned integer type of the result of the `sizeof` operator; described in §7.19). — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22p2 — The header shall define the type `wchar_t` (an integer type whose range of values can represent distinct codes for all members of the largest extended character set among the supported locales; described in §7.19). — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22p2 — The header shall define the structure type `div_t`, the type of the value returned by `div`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22p2 — The header shall define the structure type `ldiv_t`, the type of the value returned by `ldiv`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22p2 — The header shall define the structure type `lldiv_t`, the type of the value returned by `lldiv`. **(C99)** (not present in C90). — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22p3 — The header shall define the macro `NULL`, which expands to an implementation-defined null pointer constant (described in §7.19). — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22p3 — The header shall define the macro `EXIT_FAILURE`, which expands to an integer constant expression usable as the argument to `exit` to return unsuccessful termination status to the host environment. — mcc:✓ gcc:✓ clang:✓ (all expand to `1`)
- [✓] §7.22p3 — The header shall define the macro `EXIT_SUCCESS`, which expands to an integer constant expression usable as the argument to `exit` to return successful termination status to the host environment. — mcc:✓ gcc:✓ clang:✓ (all expand to `0`)
- [✓] §7.22p3 — The header shall define the macro `RAND_MAX`, which expands to an integer constant expression that is the maximum value returned by `rand`. — mcc:✓ gcc:✓ clang:✓ (all `2147483647`)
- [✓] §7.22p3 — The header shall define the macro `MB_CUR_MAX`, which expands to a positive integer expression of type `size_t` that is the maximum number of bytes in a multibyte character for the extended character set specified by the current locale (category `LC_CTYPE`), and which is never greater than `MB_LEN_MAX`. — mcc:✓ gcc:✓ clang:✓ (glibc `(size_t)__ctype_get_mb_cur_max()`)

### §7.22.1 Numeric conversion functions

- §7.22.1p1 — The functions `atof`, `atoi`, `atol`, and `atoll` need not affect the value of `errno` on an error; if the value of the result cannot be represented, the behavior is undefined. — (spec; UB catalog, not separately testable)

#### §7.22.1.1 The atof function

- [✓] §7.22.1.1p1 — Synopsis: `#include <stdlib.h>` and `double atof(const char *nptr);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.1.1p2 — Description: `atof` converts the initial portion of the string pointed to by `nptr` to `double` representation. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.1.1p2 — Except for the behavior on error, `atof` is equivalent to `strtod(nptr, (char **)NULL)`. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [✓] §7.22.1.1p3 — Returns: `atof` returns the converted value. — mcc:✓ gcc:✓ clang:✓ (`atof("1.5")`→1.5 on all three)

#### §7.22.1.2 The atoi, atol, and atoll functions

- [✓] §7.22.1.2p1 — Synopsis: `int atoi(const char *nptr);`, `long int atol(const char *nptr);`, and `long long int atoll(const char *nptr);`. `atoll` is **(C99)**. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.1.2p2 — Description: these functions convert the initial portion of the string pointed to by `nptr` to `int`, `long int`, and `long long int` representation, respectively. — mcc:✓ gcc:✓ clang:✓
- §7.22.1.2p2 — Except for the behavior on error, they are equivalent to `atoi`: `(int)strtol(nptr, (char **)NULL, 10)`; `atol`: `strtol(nptr, (char **)NULL, 10)`; `atoll`: `strtoll(nptr, (char **)NULL, 10)`. — (spec; glibc-shared, not separately testable)
- [✓] §7.22.1.2p3 — Returns: these functions return the converted value. — mcc:✓ gcc:✓ clang:✓

#### §7.22.1.3 The strtod, strtof, and strtold functions

- [✓] §7.22.1.3p1 — Synopsis: `double strtod(const char * restrict nptr, char ** restrict endptr);`, `float strtof(const char * restrict nptr, char ** restrict endptr);`, and `long double strtold(const char * restrict nptr, char ** restrict endptr);`. `strtof` and `strtold` are **(C99)**. — mcc:✓ gcc:✓ clang:✓ (mcc accepts `restrict`-qualified prototypes)
- [✓] §7.22.1.3p2 — Description: these functions convert the initial portion of the string pointed to by `nptr` to `double`, `float`, and `long double` representation, respectively. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.1.3p2 — First, they decompose the input string into three parts: leading white space; a subject sequence resembling a floating-point constant or representing an infinity or NaN; and a final string of unrecognized characters. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [✓] §7.22.1.3p2 — Then they attempt to convert the subject sequence to a floating-point number, and return the result. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.1.3p3 — The expected form of the subject sequence is an optional sign, then a decimal/hex significand with optional exponent, or `INF`/`INFINITY`, or `NAN`/`NAN(n-char-sequence_opt)`, ignoring case as specified. — mcc:✓ gcc:✓ clang:✓ (`strtod("INFINITY")`→inf, `strtod("0x1p4")`→16 on all three)
- [✓] §7.22.1.3p3 — The subject sequence is the longest initial subsequence of the input string, starting with the first non-white-space character, that is of the expected form; it contains no characters if the input string is not of the expected form. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [✓] §7.22.1.3p4 — If the subject sequence has the expected form for a floating-point number, it is interpreted as a floating constant per §6.4.4.2, with the locale decimal-point character and assumed zero exponent rules. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.1.3p4 — If the subject sequence begins with a minus sign, the sequence is interpreted as negated. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.1.3p4 — `INF` or `INFINITY` is interpreted as an infinity, if representable, else like a too-large floating constant. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.1.3p4 — `NAN`/`NAN(n-char-sequence_opt)` is interpreted as a quiet NaN if supported, else like a non-expected-form part; the n-char meaning is implementation-defined. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [✓] §7.22.1.3p4 — A pointer to the final string is stored in `*endptr`, provided that `endptr` is not a null pointer. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.1.3p5 — If the subject sequence has the hexadecimal form and `FLT_RADIX` is a power of 2, the value resulting from the conversion is correctly rounded. — mcc:✓ gcc:✓ clang:✓ (glibc-shared library behavior)
- [ ] §7.22.1.3p6 (Recommended practice) — Hexadecimal form with non-power-of-2 `FLT_RADIX` and inexact result should be one of the two adjacent numbers with correctly-signed error. — (spec; recommended practice, moot on this implementation)
- [✓] §7.22.1.3p7 — Returns: these functions return the converted value, if any. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.1.3p7 — If no conversion could be performed, zero is returned. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.1.3p7 — On overflow with default rounding, ±`HUGE_VAL`/`HUGE_VALF`/`HUGE_VALL` is returned and `errno` is set to `ERANGE`. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [✓] §7.22.1.3p7 — On underflow, the magnitude returned is no greater than the smallest normalized positive number; whether `errno` becomes `ERANGE` is implementation-defined. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)

#### §7.22.1.4 The strtol, strtoll, strtoul, and strtoull functions

- [✓] §7.22.1.4p1 — Synopsis: `long int strtol(const char * restrict nptr, char ** restrict endptr, int base);`, `long long int strtoll(...);`, `unsigned long int strtoul(...);`, and `unsigned long long int strtoull(...);`. `strtoll` and `strtoull` are **(C99)**. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.1.4p2 — Description: these functions convert the initial portion of the string to `long`/`long long`/`unsigned long`/`unsigned long long` representation, respectively. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.1.4p2 — First, they decompose the input string into leading white space; a subject sequence resembling an integer in the radix given by `base`; and a final string of unrecognized characters. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.1.4p2 — Then they attempt to convert the subject sequence to an integer, and return the result. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.1.4p3 — If `base` is zero, the expected form is that of an integer constant per §6.4.4.1, optionally signed, without an integer suffix. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [✓] §7.22.1.4p3 — If `base` is between 2 and 36, the expected form is a sequence of letters and digits in that radix, optionally signed, without an integer suffix. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.1.4p3 — Letters `a`/`A` through `z`/`Z` are ascribed values 10–35; only letters and digits with value less than `base` are permitted. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.1.4p3 — If `base` is 16, the characters `0x`/`0X` may optionally precede the sequence, following the sign if present. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.1.4p4 — The subject sequence is the longest initial subsequence, starting with the first non-white-space character, of the expected form. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.1.4p4 — The subject sequence contains no characters if the input is empty/all-white-space, or the first non-white-space character is other than a sign or a permissible letter or digit. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.1.4p5 — With `base` zero and expected form, characters from the first digit are interpreted as an integer constant per §6.4.4.1. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.1.4p5 — With `base` 2–36 and expected form, that base is used, ascribing each letter its value. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.1.4p5 — If the subject sequence begins with a minus sign, the converted value is negated in the return type. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.1.4p5 — A pointer to the final string is stored in `*endptr`, provided that `endptr` is not a null pointer. — mcc:✓ gcc:✓ clang:✓
- §7.22.1.4p6 — In other than the `"C"` locale, additional locale-specific subject sequence forms may be accepted. — (spec; locale-dependent, not separately testable)
- [✓] §7.22.1.4p7 — If the subject sequence is empty or not of the expected form, no conversion is performed; `nptr` is stored in `*endptr` if `endptr` is not null. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [✓] §7.22.1.4p8 — Returns: these functions return the converted value, if any. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.1.4p8 — If no conversion could be performed, zero is returned. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.1.4p8 — On out-of-range, `LONG_MIN`/`LONG_MAX`/`LLONG_MIN`/`LLONG_MAX`/`ULONG_MAX`/`ULLONG_MAX` is returned and `errno` is set to `ERANGE`. — mcc:✓ gcc:✓ clang:✓ (`strtol("99999999999999999999")` clamps to `LONG_MAX` on all three)

### §7.22.2 Pseudo-random sequence generation functions

#### §7.22.2.1 The rand function

- [✓] §7.22.2.1p1 — Synopsis: `#include <stdlib.h>` and `int rand(void);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.2.1p2 — Description: `rand` computes a sequence of pseudo-random integers in the range 0 to `RAND_MAX`. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [ ] §7.22.2.1p2 (footnote) — No guarantees as to the quality of the random sequence; low-order bits may be non-random; applications with particular requirements should use a sufficient generator. — (spec; footnote/advisory, not testable)
- [✓] §7.22.2.1p3 — Returns: `rand` returns a pseudo-random integer. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.2.1p4 — The value of the `RAND_MAX` macro shall be at least 32767. — mcc:✓ gcc:✓ clang:✓ (`RAND_MAX`=2147483647 ≥ 32767 on all three)

#### §7.22.2.2 The srand function

- [✓] §7.22.2.2p1 — Synopsis: `#include <stdlib.h>` and `void srand(unsigned int seed);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.2.2p2 — Description: `srand` uses `seed` as a seed for a new sequence of pseudo-random numbers returned by subsequent calls to `rand`. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [✓] §7.22.2.2p2 — If `srand` is then called with the same seed value, the sequence of pseudo-random numbers shall be repeated. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.2.2p2 — If `rand` is called before any `srand`, the same sequence is generated as when `srand` is first called with seed 1. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [✓] §7.22.2.2p3 — Returns: `srand` returns no value. — mcc:✓ gcc:✓ clang:✓

### §7.22.3 Memory management functions

> C11 added `aligned_alloc` (§7.22.3.1) and the data-race wording (§7.22.3p2); C99 has neither.

- [ ] §7.22.3p1 — The order and contiguity of storage allocated by successive calls to `aligned_alloc` **(C11)**, `calloc`, `malloc`, and `realloc` is unspecified. — (spec; unspecified behavior, not testable)
- [✓] §7.22.3p1 — A successful allocation is suitably aligned for any object type with a fundamental alignment requirement, usable for such objects or arrays until deallocated. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- §7.22.3p1 — The lifetime of an allocated object extends from the allocation until the deallocation. — (spec; semantic statement, not separately testable)
- [✓] §7.22.3p1 — Each such allocation shall yield a pointer to an object disjoint from any other object. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [✓] §7.22.3p1 — The returned pointer points to the start (lowest byte address) of the allocated space. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.3p1 — If the space cannot be allocated, a null pointer is returned. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [✓] §7.22.3p1 — If the requested size is zero, the behavior is implementation-defined (null, or a non-dereferenceable nonzero-size-like pointer). — mcc:✓ gcc:✓ clang:✓ (glibc returns a freeable non-null pointer on all three)
- [ ] §7.22.3p2 — For determining a data race, memory allocation functions behave as though they accessed only memory accessible through their arguments and not other static-duration storage. **(C11)** — (spec; memory-model wording, not testable here)
- [ ] §7.22.3p2 — These functions may visibly modify the storage they allocate/deallocate; a deallocation synchronizes with the next allocation of that same region or part thereof. **(C11)** — (spec; memory-model wording, not testable here)

#### §7.22.3.1 The aligned_alloc function **(C11)**

- [✓] §7.22.3.1p1 — Synopsis **(C11)**: `#include <stdlib.h>` and `void *aligned_alloc(size_t alignment, size_t size);`. — mcc:✓ gcc:✓ clang:✓ (mcc needs `-std=c11`; in default C99 mode glibc hides it behind `__USE_ISOC11`, yielding an implicit-declaration warning — same gating glibc applies to any C99-default compiler)
- [✓] §7.22.3.1p2 — Description **(C11)**: `aligned_alloc` allocates space with the given `alignment` and `size`, value indeterminate. — mcc:✓ gcc:✓ clang:✓ (runs identically with `-std=c11`)
- [ ] §7.22.3.1p2 — Constraint **(C11)**: `alignment` shall be a valid supported alignment, and `size` shall be an integral multiple of `alignment`. — (spec; runtime constraint, glibc-shared)
- [✓] §7.22.3.1p3 — Returns **(C11)**: `aligned_alloc` returns either a null pointer or a pointer to the allocated space. — mcc:✓ gcc:✓ clang:✓

#### §7.22.3.2 The calloc function

- [✓] §7.22.3.2p1 — Synopsis: `#include <stdlib.h>` and `void *calloc(size_t nmemb, size_t size);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.3.2p2 — Description: `calloc` allocates space for an array of `nmemb` objects, each of size `size`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.3.2p2 — The space is initialized to all bits zero. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [ ] §7.22.3.2p2 (footnote) — All-bits-zero need not be the representation of floating-point zero or a null pointer constant. — (spec; footnote, not testable)
- [✓] §7.22.3.2p3 — Returns: `calloc` returns either a null pointer or a pointer to the allocated space. — mcc:✓ gcc:✓ clang:✓

#### §7.22.3.3 The free function

- [✓] §7.22.3.3p1 — Synopsis: `#include <stdlib.h>` and `void free(void *ptr);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.3.3p2 — Description: `free` deallocates the space pointed to by `ptr`, making it available for further allocation. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.3.3p2 — If `ptr` is a null pointer, no action occurs. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [ ] §7.22.3.3p2 — Otherwise, if the argument does not match a pointer earlier returned, or the space was already deallocated, the behavior is undefined. — (spec; UB catalog, not testable)
- [✓] §7.22.3.3p3 — Returns: `free` returns no value. — mcc:✓ gcc:✓ clang:✓

#### §7.22.3.4 The malloc function

- [✓] §7.22.3.4p1 — Synopsis: `#include <stdlib.h>` and `void *malloc(size_t size);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.3.4p2 — Description: `malloc` allocates space of the specified `size`, value indeterminate. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.3.4p3 — Returns: `malloc` returns either a null pointer or a pointer to the allocated space. — mcc:✓ gcc:✓ clang:✓

#### §7.22.3.5 The realloc function

- [✓] §7.22.3.5p1 — Synopsis: `#include <stdlib.h>` and `void *realloc(void *ptr, size_t size);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.3.5p2 — Description: `realloc` deallocates the old object pointed to by `ptr` and returns a pointer to a new object of the specified `size`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.3.5p2 — The contents of the new object are the same as the old object up to the lesser of the new and old sizes. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [ ] §7.22.3.5p2 — Any bytes in the new object beyond the old size have indeterminate values. — (spec; indeterminate-value statement, not testable)
- [✓] §7.22.3.5p3 — If `ptr` is a null pointer, `realloc` behaves like `malloc` for the specified size. — mcc:✓ gcc:✓ clang:✓
- [ ] §7.22.3.5p3 — Otherwise, if `ptr` does not match an earlier-returned pointer, or the space was already deallocated, the behavior is undefined. — (spec; UB catalog, not testable)
- [✓] §7.22.3.5p3 — If the new size is zero, the behavior is implementation-defined per §7.22.3p1. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [✓] §7.22.3.5p3 — If memory for the new object cannot be allocated, the old object is not deallocated and its value is unchanged. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [✓] §7.22.3.5p4 — Returns: `realloc` returns a pointer to the new object (possibly the same value), or a null pointer if it could not be allocated. — mcc:✓ gcc:✓ clang:✓

### §7.22.4 Communication with the environment

> C11 added `at_quick_exit` (§7.22.4.3) and `quick_exit` (§7.22.4.7), the `_Noreturn` qualification on the `exit`/`_Exit`/`quick_exit` prototypes, and the `quick_exit`/`at_quick_exit` interaction clauses in `exit` and `_Exit`. The `_Exit` function itself is **(C99)**.

#### §7.22.4.1 The abort function

- [✓] §7.22.4.1p1 — Synopsis: `#include <stdlib.h>` and `_Noreturn void abort(void);`. (`_Noreturn` on the prototype is **(C11)**.) — mcc:✓ gcc:✓ clang:✓ (mcc honors glibc's `__attribute__((__noreturn__))`: no "control reaches end of non-void" after `abort()`)
- [✓] §7.22.4.1p2 — Description: `abort` causes abnormal program termination, unless `SIGABRT` is caught and the handler does not return. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- §7.22.4.1p2 — Whether open streams are flushed/closed or temporary files removed is implementation-defined. — (spec; implementation-defined, not separately testable)
- [✓] §7.22.4.1p2 — An implementation-defined "unsuccessful termination" status is returned via `raise(SIGABRT)`. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [✓] §7.22.4.1p3 — Returns: `abort` does not return to its caller. — mcc:✓ gcc:✓ clang:✓

#### §7.22.4.2 The atexit function

- [✓] §7.22.4.2p1 — Synopsis: `#include <stdlib.h>` and `int atexit(void (*func)(void));`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.4.2p2 — Description: `atexit` registers `func` to be called without arguments at normal program termination. — mcc:✓ gcc:✓ clang:✓
- [ ] §7.22.4.2p2 (footnote) **(C11)** — The `atexit` registrations are distinct from the `at_quick_exit` registrations. — (spec; footnote, not testable)
- [✓] §7.22.4.2p3 — The implementation shall support the registration of at least 32 functions. — mcc:✓ gcc:✓ clang:✓ (glibc supports unbounded registration)
- [✓] §7.22.4.2p4 — Returns: `atexit` returns zero if registration succeeds, nonzero if it fails. — mcc:✓ gcc:✓ clang:✓

#### §7.22.4.3 The at_quick_exit function **(C11)**

- [✓] §7.22.4.3p1 — Synopsis **(C11)**: `#include <stdlib.h>` and `int at_quick_exit(void (*func)(void));`. — mcc:✓ gcc:✓ clang:✓ (mcc needs `-std=c11`; glibc gates behind `__USE_ISOC11`)
- [✓] §7.22.4.3p2 — Description **(C11)**: `at_quick_exit` registers `func` to be called without arguments should `quick_exit` be called. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.4.3p2 **(C11)** — If a function is registered multiple times, it is called as many times as registered. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [ ] §7.22.4.3p2 (footnote) **(C11)** — The `at_quick_exit` registrations are distinct from the `atexit` registrations. — (spec; footnote, not testable)
- [✓] §7.22.4.3p3 **(C11)** — The implementation shall support the registration of at least 32 functions. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [✓] §7.22.4.3p4 — Returns **(C11)**: `at_quick_exit` returns zero if registration succeeds, nonzero if it fails. — mcc:✓ gcc:✓ clang:✓

#### §7.22.4.4 The exit function

- [✓] §7.22.4.4p1 — Synopsis: `#include <stdlib.h>` and `_Noreturn void exit(int status);`. (`_Noreturn` on the prototype is **(C11)**.) — mcc:✓ gcc:✓ clang:✓ (mcc treats `exit()` as noreturn; no fall-off diagnostic)
- [✓] §7.22.4.4p2 — Description: `exit` causes normal program termination to occur. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.4.4p2 — No functions registered by `at_quick_exit` are called. **(C11)** — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [ ] §7.22.4.4p3 — Calling `exit` more than once, or calling `quick_exit` in addition to `exit`, is undefined. (`quick_exit` clause **(C11)**.) — (spec; UB catalog, not testable)
- [✓] §7.22.4.4p4 — First, all functions registered by `atexit` are called in reverse order of registration, with the staggered-call ordering rule. — mcc:✓ gcc:✓ clang:✓ (registering a,b,c then exit prints `cba` on all three)
- [ ] §7.22.4.4p4 — If during such a registered function a `longjmp` is made that would terminate the call, the behavior is undefined. — (spec; UB catalog, not testable)
- [✓] §7.22.4.4p5 — Next, all open streams with unwritten buffered data are flushed, all open streams are closed, and all `tmpfile` files are removed. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [✓] §7.22.4.4p6 — Finally, control is returned to the host environment. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.4.4p6 — If `status` is zero or `EXIT_SUCCESS`, an implementation-defined "successful termination" status is returned. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.4.4p6 — If `status` is `EXIT_FAILURE`, an implementation-defined "unsuccessful termination" status is returned. — mcc:✓ gcc:✓ clang:✓
- §7.22.4.4p6 — Otherwise the status returned is implementation-defined. — (spec; implementation-defined, not separately testable)
- [✓] §7.22.4.4p7 — Returns: `exit` cannot return to its caller. — mcc:✓ gcc:✓ clang:✓

#### §7.22.4.5 The _Exit function

- [✓] §7.22.4.5p1 — Synopsis: `#include <stdlib.h>` and `_Noreturn void _Exit(int status);`. `_Exit` is **(C99)**; `_Noreturn` on the prototype is **(C11)**. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.4.5p2 — Description: `_Exit` causes normal program termination and control to be returned to the host environment. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.4.5p2 — No functions registered by `atexit`, none by `at_quick_exit`, and no `signal` handlers are called. (`at_quick_exit` clause **(C11)**.) — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [✓] §7.22.4.5p2 — The status returned is determined as for `exit` (§7.22.4.4p6). — mcc:✓ gcc:✓ clang:✓
- §7.22.4.5p2 — Whether open streams are flushed/closed or temporary files removed is implementation-defined. — (spec; implementation-defined, not separately testable)
- [✓] §7.22.4.5p3 — Returns: `_Exit` cannot return to its caller. — mcc:✓ gcc:✓ clang:✓

#### §7.22.4.6 The getenv function

- [✓] §7.22.4.6p1 — Synopsis: `#include <stdlib.h>` and `char *getenv(const char *name);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.4.6p2 — Description: `getenv` searches a host-provided environment list for a string matching `name`. — mcc:✓ gcc:✓ clang:✓ (`getenv("PATH")` non-null on all three)
- §7.22.4.6p2 — The set of environment names and the method for altering the environment list are implementation-defined. — (spec; implementation-defined, not separately testable)
- [ ] §7.22.4.6p2 — The `getenv` function need not avoid data races with other threads that modify the environment list. **(C11)** — (spec; thread-safety wording, not testable)
- [ ] §7.22.4.6p3 — The implementation shall behave as if no library function calls `getenv`. — (spec; as-if requirement, not testable)
- [✓] §7.22.4.6p3 — Returns: `getenv` returns a pointer to a string associated with the matched member. — mcc:✓ gcc:✓ clang:✓
- [ ] §7.22.4.6p3 — The string shall not be modified by the program, but may be overwritten by a subsequent `getenv` call. — (spec; usage constraint, not testable)
- [✓] §7.22.4.6p3 — If `name` cannot be found, a null pointer is returned. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)

#### §7.22.4.7 The quick_exit function **(C11)**

- [✓] §7.22.4.7p1 — Synopsis **(C11)**: `#include <stdlib.h>` and `_Noreturn void quick_exit(int status);`. — mcc:✓ gcc:✓ clang:✓ (mcc needs `-std=c11`; glibc gates behind `__USE_ISOC11`)
- [✓] §7.22.4.7p2 — Description **(C11)**: `quick_exit` causes normal program termination to occur. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.4.7p2 **(C11)** — No functions registered by `atexit` and no `signal` handlers are called. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [ ] §7.22.4.7p3 **(C11)** — Calling `quick_exit` more than once, or calling `exit` in addition to `quick_exit`, is undefined. — (spec; UB catalog, not testable)
- [ ] §7.22.4.7p3 (footnote) **(C11)** — Functions registered by `at_quick_exit` and called at quick termination may, e.g., flush a buffer or write a marker. — (spec; footnote, not testable)
- [✓] §7.22.4.7p4 **(C11)** — Functions registered by `at_quick_exit` are called in reverse order of registration, with the staggered-call ordering rule. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [ ] §7.22.4.7p4 **(C11)** — If during such a registered function a `longjmp` would terminate the call, the behavior is undefined. — (spec; UB catalog, not testable)
- [✓] §7.22.4.7p5 **(C11)** — After calling all registered functions, control is returned via the function call `_Exit(status)`. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [✓] §7.22.4.7p6 — Returns **(C11)**: `quick_exit` cannot return to its caller. — mcc:✓ gcc:✓ clang:✓

#### §7.22.4.8 The system function

- [✓] §7.22.4.8p1 — Synopsis: `#include <stdlib.h>` and `int system(const char *string);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.4.8p2 — Description: if `string` is a null pointer, `system` determines whether the host environment has a command processor. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- §7.22.4.8p2 — If `string` is not null, `system` passes it to the command processor in an implementation-documented manner; this may cause non-conforming behavior or termination. — (spec; implementation-defined, not separately testable)
- [✓] §7.22.4.8p3 — Returns: if the argument is a null pointer, `system` returns nonzero only if a command processor is available. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- §7.22.4.8p3 — If the argument is not null and `system` returns, it returns an implementation-defined value. — (spec; implementation-defined, not separately testable)

### §7.22.5 Searching and sorting utilities

- [✓] §7.22.5p1 — These utilities use a comparison function called with two arguments pointing to the key object and an array element, in that order. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [ ] §7.22.5p1 — The comparison function shall return an integer <, =, or > zero as the key object is less than, matches, or is greater than the array element. — (spec; caller obligation, glibc-shared)
- §7.22.5p1 — The array partitioning requirement: for `bsearch` the three groups in order; for `qsort` all elements in any order. — (spec; caller obligation, not separately testable)
- [ ] §7.22.5p2 — When the same objects are passed more than once to the comparison function, results shall be consistent and define a total ordering. — (spec; caller obligation, not testable)
- §7.22.5p3 — A sequence point occurs immediately before and after each call to the comparison function, and between any call and any movement of the passed objects. — (spec; sequencing guarantee, not separately testable)
- [✓] §7.22.5p4 — The implementation may reorder elements between calls but shall not otherwise alter any individual element's contents. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)

#### §7.22.5.1 The bsearch function

- [✓] §7.22.5.1p1 — Synopsis: `#include <stdlib.h>` and `void *bsearch(const void *key, const void *base, size_t nmemb, size_t size, int (*compar)(const void *, const void *));`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.5.1p2 — Description: `bsearch` searches an array of `nmemb` objects starting at `base` for an element matching `*key`. — mcc:✓ gcc:✓ clang:✓ (found correct element on all three)
- [✓] §7.22.5.1p2 — The size of each element is specified by `size`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.5.1p2 — `compar` is called with two arguments pointing to the `key` object and an array element, in that order. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- §7.22.5.1p2 — `compar` shall return <, =, or > zero per the key/element ordering. — (spec; caller obligation, not separately testable)
- [ ] §7.22.5.1p2 — The array partitioning into less/equal/greater groups in order. — (spec; caller obligation, not testable)
- [ ] §7.22.5.1p2 (footnote) — In practice, the entire array is sorted according to the comparison function. — (spec; footnote, not testable)
- [✓] §7.22.5.1p3 — Returns: `bsearch` returns a pointer to a matching element, or a null pointer if no match is found. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [ ] §7.22.5.1p3 — If two elements compare equal, which is matched is unspecified. — (spec; unspecified, not testable)

#### §7.22.5.2 The qsort function

- [✓] §7.22.5.2p1 — Synopsis: `#include <stdlib.h>` and `void qsort(void *base, size_t nmemb, size_t size, int (*compar)(const void *, const void *));`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.5.2p2 — Description: `qsort` sorts an array of `nmemb` objects starting at `base`. — mcc:✓ gcc:✓ clang:✓ ({3,1,2}→{1,2,3} on all three)
- [✓] §7.22.5.2p2 — The size of each object is specified by `size`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.5.2p3 — Contents are sorted into ascending order per `compar`, called with two arguments pointing to the objects being compared. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- §7.22.5.2p3 — `compar` shall return <, =, or > zero as the first argument is less than, equal to, or greater than the second. — (spec; caller obligation, not separately testable)
- [ ] §7.22.5.2p4 — If two elements compare equal, their order in the result is unspecified (the sort is not required to be stable). — (spec; unspecified, not testable)
- [✓] §7.22.5.2p5 — Returns: `qsort` returns no value. — mcc:✓ gcc:✓ clang:✓

### §7.22.6 Integer arithmetic functions

#### §7.22.6.1 The abs, labs, and llabs functions

- [✓] §7.22.6.1p1 — Synopsis: `#include <stdlib.h>` with `int abs(int j);`, `long int labs(long int j);`, and `long long int llabs(long long int j);`. `llabs` is **(C99)**. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.6.1p2 — Description: these functions compute the absolute value of an integer `j`. — mcc:✓ gcc:✓ clang:✓ (`abs(-1)`=1, `labs(-1L)`=1, `llabs(-1LL)`=1 on all three)
- [ ] §7.22.6.1p2 — If the result cannot be represented, the behavior is undefined. — (spec; UB catalog, not testable)
- [ ] §7.22.6.1p2 (footnote) — The absolute value of the most negative number cannot be represented in two's complement. — (spec; footnote, not testable)
- [✓] §7.22.6.1p3 — Returns: these functions return the absolute value. — mcc:✓ gcc:✓ clang:✓

#### §7.22.6.2 The div, ldiv, and lldiv functions

- [✓] §7.22.6.2p1 — Synopsis: `#include <stdlib.h>` with `div_t div(int numer, int denom);`, `ldiv_t ldiv(long int numer, long int denom);`, and `lldiv_t lldiv(long long int numer, long long int denom);`. `lldiv` is **(C99)**. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.6.2p2 — Description: these functions compute `numer / denom` and `numer % denom` in a single operation. — mcc:✓ gcc:✓ clang:✓ (`div(-7,2)`→quot=-3, rem=-1, truncation toward zero, on all three)
- [✓] §7.22.6.2p3 — Returns: these functions return a structure of type `div_t`/`ldiv_t`/`lldiv_t` comprising both quotient and remainder. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.6.2p3 — The structures contain (in either order) members `quot` and `rem`, each of the same type as the arguments. — mcc:✓ gcc:✓ clang:✓ (member access `.quot`/`.rem` compiles and yields correct types)
- [ ] §7.22.6.2p3 — If either part of the result cannot be represented, the behavior is undefined. — (spec; UB catalog, not testable)

### §7.22.7 Multibyte/wide character conversion functions

- [✓] §7.22.7p1 — The behavior of the multibyte character functions is affected by the `LC_CTYPE` category of the current locale. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [✓] §7.22.7p1 — For a state-dependent encoding, each function starts in its initial conversion state at program startup and can be returned there by a call with character pointer `s` a null pointer. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [ ] §7.22.7p1 — A subsequent call with `s` other than null causes the internal conversion state to be altered as necessary. — (spec; state behavior, glibc-shared)
- [✓] §7.22.7p1 — A call with `s` null returns nonzero or zero according to whether encodings do or do not have state-dependent encodings. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [ ] §7.22.7p1 — Changing the `LC_CTYPE` category causes the conversion state of these functions to be indeterminate. — (spec; indeterminate-state statement, not testable)

#### §7.22.7.1 The mblen function

- [✓] §7.22.7.1p1 — Synopsis: `#include <stdlib.h>` and `int mblen(const char *s, size_t n);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.7.1p2 — Description: if `s` is not null, `mblen` determines the number of bytes in the multibyte character pointed to by `s`. — mcc:✓ gcc:✓ clang:✓ (`mblen("a",1)`=1 on all three)
- [ ] §7.22.7.1p2 — Except that the conversion state of `mbtowc` is not affected, `mblen` is equivalent to `mbtowc((wchar_t *)0, s, n)`. — (spec; equivalence, glibc-shared)
- [ ] §7.22.7.1p3 — The implementation shall behave as if no library function calls `mblen`. — (spec; as-if requirement, not testable)
- [✓] §7.22.7.1p3 — Returns: if `s` is null, returns nonzero/zero per state-dependent encoding. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [✓] §7.22.7.1p3 — If `s` is not null, returns 0 (null char), the byte count (valid multibyte char), or −1 (invalid). — mcc:✓ gcc:✓ clang:✓ (glibc-shared)

#### §7.22.7.2 The mbtowc function

- [✓] §7.22.7.2p1 — Synopsis: `#include <stdlib.h>` and `int mbtowc(wchar_t * restrict pwc, const char * restrict s, size_t n);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.7.2p2 — Description: if `s` is not null, `mbtowc` inspects at most `n` bytes from `s` to determine the bytes needed to complete the next multibyte character. — mcc:✓ gcc:✓ clang:✓ (`mbtowc(&w,"a",1)`=1 on all three)
- [✓] §7.22.7.2p2 — If complete and valid, it determines the corresponding wide character and, if `pwc` is not null, stores it in `*pwc`. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [ ] §7.22.7.2p2 — If the corresponding wide character is the null wide character, the function is left in the initial conversion state. — (spec; state behavior, glibc-shared)
- [ ] §7.22.7.2p3 — The implementation shall behave as if no library function calls `mbtowc`. — (spec; as-if requirement, not testable)
- [✓] §7.22.7.2p4 — Returns: if `s` is null, returns nonzero/zero per state-dependent encoding. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [✓] §7.22.7.2p4 — If `s` is not null, returns 0 (null char), the byte count (valid), or −1 (invalid). — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [✓] §7.22.7.2p4 — In no case will the returned value be greater than `n` or `MB_CUR_MAX`. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)

#### §7.22.7.3 The wctomb function

- [✓] §7.22.7.3p1 — Synopsis: `#include <stdlib.h>` and `int wctomb(char *s, wchar_t wc);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.7.3p2 — Description: `wctomb` determines the bytes needed for the multibyte character corresponding to `wc` and stores its representation in `s` (if `s` is not null). — mcc:✓ gcc:✓ clang:✓ (`wctomb(buf,L'a')`=1 on all three)
- [✓] §7.22.7.3p2 — At most `MB_CUR_MAX` characters are stored. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [ ] §7.22.7.3p2 — If `wc` is a null wide character, a null byte is stored (preceded by any needed shift sequence) and the function is left in the initial conversion state. — (spec; state behavior, glibc-shared)
- [ ] §7.22.7.3p3 — The implementation shall behave as if no library function calls `wctomb`. — (spec; as-if requirement, not testable)
- [✓] §7.22.7.3p4 — Returns: if `s` is null, returns nonzero/zero per state-dependent encoding. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [✓] §7.22.7.3p4 — If `s` is not null, returns −1 if `wc` is not a valid multibyte character, else the byte count. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [✓] §7.22.7.3p4 — In no case will the returned value be greater than `MB_CUR_MAX`. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)

### §7.22.8 Multibyte/wide string conversion functions

- [✓] §7.22.8p1 — The behavior of the multibyte string functions is affected by the `LC_CTYPE` category of the current locale. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)

#### §7.22.8.1 The mbstowcs function

- [✓] §7.22.8.1p1 — Synopsis: `#include <stdlib.h>` and `size_t mbstowcs(wchar_t * restrict pwcs, const char * restrict s, size_t n);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.8.1p2 — Description: `mbstowcs` converts a multibyte sequence beginning in the initial shift state from `s` into wide characters, storing not more than `n` into `pwcs`. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [ ] §7.22.8.1p2 — No multibyte characters following a null character (converted to a null wide character) are examined or converted. — (spec; semantic, glibc-shared)
- [ ] §7.22.8.1p2 — Each multibyte character is converted as if by `mbtowc`, except the conversion state of `mbtowc` is not affected. — (spec; equivalence, glibc-shared)
- [✓] §7.22.8.1p2 — No more than `n` elements are modified in `pwcs`. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [ ] §7.22.8.1p2 — If copying takes place between overlapping objects, the behavior is undefined. — (spec; UB catalog, not testable)
- [✓] §7.22.8.1p3 — Returns: if an invalid multibyte character is encountered, returns `(size_t)(-1)`. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [✓] §7.22.8.1p3 — Otherwise returns the number of array elements modified, not including a terminating null wide character. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)

#### §7.22.8.2 The wcstombs function

- [✓] §7.22.8.2p1 — Synopsis: `#include <stdlib.h>` and `size_t wcstombs(char * restrict s, const wchar_t * restrict pwcs, size_t n);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.8.2p2 — Description: `wcstombs` converts a wide-character sequence from `pwcs` into multibyte characters beginning in the initial shift state, storing into `s`, stopping at the `n`-byte limit or a stored null. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [ ] §7.22.8.2p2 — Each wide character is converted as if by `wctomb`, except the conversion state of `wctomb` is not affected. — (spec; equivalence, glibc-shared)
- [✓] §7.22.8.2p2 — No more than `n` bytes are modified in `s`. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [ ] §7.22.8.2p2 — If copying takes place between overlapping objects, the behavior is undefined. — (spec; UB catalog, not testable)
- [✓] §7.22.8.2p3 — Returns: if a wide character with no valid multibyte representation is encountered, returns `(size_t)(-1)`. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)
- [✓] §7.22.8.2p3 — Otherwise returns the number of bytes modified, not including a terminating null character. — mcc:✓ gcc:✓ clang:✓ (glibc-shared)

### §7.22 Added gap items (beyond the consolidated draft)

- [✓] §7.22 (header resolution) — mcc ships **no** `<stdlib.h>` in `runtime/include/`; `#include <stdlib.h>` resolves to the host `/usr/include/stdlib.h` (glibc), identical to gcc/clang. So all library entity definitions and runtime behavior are inherited from glibc and conform. — mcc:✓ gcc:✓ clang:✓
- [~] §7.22 (default language mode) — mcc's default `__STDC_VERSION__` is `199901L` (C99), versus gcc 15 `202311L` (C23) and clang 22 `201710L` (C17). Consequently, with no `-std=` flag the C11-only functions (`aligned_alloc`/`at_quick_exit`/`quick_exit`), gated by glibc behind `__USE_ISOC11`, are not visible to mcc and produce implicit-declaration warnings; `-std=c11` (or newer) makes them visible and conforming. — mcc:~ gcc:✓ clang:✓ (mcc default is C99 by design, not a defect; the C11 surface appears under `-std=c11`)
- [✓] §7.22 (`_Noreturn` keyword) — mcc accepts the `_Noreturn` function specifier in both default and `-std=c11` modes, as required for the §7.22.4 prototypes' qualification. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22 (`restrict` qualifier) — mcc accepts the `restrict`-qualified pointer parameters used by `strtod`/`strtof`/`strtold`/`strtol`-family/`mbtowc`/`mbstowcs`/`wcstombs` prototypes. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22 (`__attribute__((__noreturn__))` honored) — mcc honors glibc's GNU `noreturn` attribute on `abort`/`exit`/`_Exit`/`quick_exit`: code after such a call is treated as unreachable, with no spurious "control reaches end of non-void function" diagnostic. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22 (idempotent inclusion) — `<stdlib.h>` is protected against multiple inclusion (`_STDLIB_H` guard); including it twice in a translation unit is well-defined and compiles cleanly on all three. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22.3p1 (zero-size allocation, observed) — On this glibc, `malloc(0)`/`calloc(0,0)`/`realloc(p,0)` return a non-null, `free`-able pointer (the "as if nonzero size" branch of the implementation-defined choice), identically across mcc/gcc/clang. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.22 (GNU/POSIX extras visible, non-normative) — Because the header is glibc's, non-C entities such as `posix_memalign`, `mkstemp`, `realpath`, `setenv`/`unsetenv`, `random`/`srandom`, `strtoq`, `__compar_fn_t`, `qsort_r` are also reachable under default/feature-test settings on all three compilers; these are outside C11 §7.22 and not required, but their presence is identical across the three. — mcc:✓ gcc:✓ clang:✓ (non-normative)


## §7.23 <string.h> & §7.24 <tgmath.h>

> mcc status: 118 TODOs; 0 mcc-gaps. `<string.h>` is **not** bundled by mcc — it resolves to the system `/usr/include/string.h` (glibc) and links glibc's runtime, so every string function compiles, runs, and matches gcc/clang byte-for-byte. The **bundled `runtime/include/tgmath.h`** matches the gcc==clang consensus: `nexttoward` keys type-generic selection on its first argument only (`nexttoward(f,ld)`→`nexttowardf`), and `creal`/`cimag` are `_Generic`-based, selecting `crealf`/`creall` (and `cimagf`/`cimagl`) for `float`/`long double` complex arguments instead of narrowing to `double` precision.

> **Numbering note.** The task labels this section §7.23 <string.h> / §7.24 <tgmath.h>, but the actual ISO clause numbers differ by edition. In **C99 (N1256)** these headers are **§7.21 <string.h>** and **§7.22 <tgmath.h>**; in **C11 (N1570)** they are **§7.24 <string.h>** and **§7.25 <tgmath.h>** (C11 inserted `<stdnoreturn.h>` at §7.23, shifting the later library headers up by three). The per-requirement citations below use **C11 N1570 numbering as primary**, with the C99 §7.21/§7.22 number given in each heading. Function semantics are identical between C99 and C11 unless tagged `(C11)`/`(C99-only)`.

### §7.24.1 / §7.21.1 String function conventions

- [✓] §7.24.1p1 — `<string.h>` shall declare one type (`size_t`), define one macro (`NULL`), and declare several functions useful for manipulating arrays of character type and other objects treated as arrays of character type; `size_t` and `NULL` are as described in `<stddef.h>` (§7.19). — mcc:✓ gcc:✓ clang:✓ (glibc header on mcc's path; `size_t`/`NULL` resolve, all functions declared)
- §7.24.1p1 — Various methods are used for determining the lengths of the arrays, but in all cases a `char *` or `void *` argument points to the initial (lowest-addressed) byte of the array; if an array is accessed beyond the end of an object, the behavior is undefined. — (spec/UB; not separately testable)
- [✓] §7.24.1p2 — Where an argument declared as `size_t n` specifies the length of the array for a function, `n` can have the value zero on a call to that function. — mcc:✓ gcc:✓ clang:✓ (glibc)
- §7.24.1p2 — Unless explicitly stated otherwise in the description of a particular function, pointer arguments on a call with `n == 0` shall still have valid values, as described in §7.1.4. — (spec; not separately testable)
- [✓] §7.24.1p2 — On a call with `n == 0`: a function that locates a character finds no occurrence, a function that compares two character sequences returns zero, and a function that copies characters copies zero characters. — mcc:✓ gcc:✓ clang:✓ (glibc)

### §7.24.2 / §7.21.2 Copying functions

#### §7.24.2.1 / §7.21.2.1 The memcpy function

- [✓] §7.24.2.1p1 — Synopsis: `void *memcpy(void * restrict s1, const void * restrict s2, size_t n);` — both pointer parameters are `restrict`-qualified (the objects must not overlap). — mcc:✓ gcc:✓ clang:✓ (glibc prototype; mcc parses `restrict`)
- [✓] §7.24.2.1p2 — `memcpy` copies `n` characters from the object pointed to by `s2` into the object pointed to by `s1`; if copying takes place between objects that overlap, the behavior is undefined. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.2.1p3 — Returns: `memcpy` returns the value of `s1`. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.24.2.2 / §7.21.2.2 The memmove function

- [✓] §7.24.2.2p1 — Synopsis: `void *memmove(void *s1, const void *s2, size_t n);` — no `restrict` (overlap is permitted). — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.2.2p2 — `memmove` copies `n` characters as if via a temporary array so overlapping moves work correctly. — mcc:✓ gcc:✓ clang:✓ (glibc; overlap copy verified at runtime)
- [✓] §7.24.2.2p3 — Returns: `memmove` returns the value of `s1`. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.24.2.3 / §7.21.2.3 The strcpy function

- [✓] §7.24.2.3p1 — Synopsis: `char *strcpy(char * restrict s1, const char * restrict s2);` — `restrict`-qualified (objects must not overlap). — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.2.3p2 — `strcpy` copies the string pointed to by `s2` (including the terminating null character) into the array pointed to by `s1`; overlap is UB. — mcc:✓ gcc:✓ clang:✓ (glibc; verified at runtime)
- [✓] §7.24.2.3p3 — Returns: `strcpy` returns the value of `s1`. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.24.2.4 / §7.21.2.4 The strncpy function

- [✓] §7.24.2.4p1 — Synopsis: `char *strncpy(char * restrict s1, const char * restrict s2, size_t n);` — `restrict`-qualified. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.2.4p2 — `strncpy` copies not more than `n` characters (characters following a null are not copied) from `s2` to `s1`; overlap is UB. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.2.4p2 — If there is no null character among the first `n` characters of `s2`, the result in `s1` is not null-terminated. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.2.4p3 — If `s2` is a string shorter than `n` characters, null characters are appended to the copy until `n` characters have been written. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.2.4p4 — Returns: `strncpy` returns the value of `s1`. — mcc:✓ gcc:✓ clang:✓ (glibc)

### §7.24.3 / §7.21.3 Concatenation functions

#### §7.24.3.1 / §7.21.3.1 The strcat function

- [✓] §7.24.3.1p1 — Synopsis: `char *strcat(char * restrict s1, const char * restrict s2);` — `restrict`-qualified. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.3.1p2 — `strcat` appends a copy of `s2` (including the null) to the end of `s1`; `s2`'s initial char overwrites `s1`'s null; overlap is UB. — mcc:✓ gcc:✓ clang:✓ (glibc; verified at runtime)
- [✓] §7.24.3.1p3 — Returns: `strcat` returns the value of `s1`. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.24.3.2 / §7.21.3.2 The strncat function

- [✓] §7.24.3.2p1 — Synopsis: `char *strncat(char * restrict s1, const char * restrict s2, size_t n);` — `restrict`-qualified. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.3.2p2 — `strncat` appends not more than `n` characters from `s2`, always appends a terminating null; overlap is UB. — mcc:✓ gcc:✓ clang:✓ (glibc; verified `strncat(b,"XYZ",2)` → `…XY`)
- §7.24.3.2p2 (footnote) — Thus, the maximum number of characters that can end up in `s1` is `strlen(s1)+n+1`. — (spec footnote; not separately testable)
- [✓] §7.24.3.2p3 — Returns: `strncat` returns the value of `s1`. — mcc:✓ gcc:✓ clang:✓ (glibc)

### §7.24.4 / §7.21.4 Comparison functions

- [✓] §7.24.4p1 — The sign of a nonzero value returned by `memcmp`, `strcmp`, `strncmp` is determined by the sign of the difference of the first differing pair of characters (both as `unsigned char`). — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.24.4.1 / §7.21.4.1 The memcmp function

- [✓] §7.24.4.1p1 — Synopsis: `int memcmp(const void *s1, const void *s2, size_t n);` — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.4.1p2 — `memcmp` compares the first `n` characters of `s1` to the first `n` of `s2`. — mcc:✓ gcc:✓ clang:✓ (glibc)
- §7.24.4.1p2 (footnote) — Padding "holes" in structures are indeterminate; short strings and unions may also misbehave when compared with `memcmp`. — (spec footnote; not separately testable)
- [✓] §7.24.4.1p3 — Returns: `memcmp` returns an integer >, =, or < zero per `s1` >, =, or < `s2`. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.24.4.2 / §7.21.4.2 The strcmp function

- [✓] §7.24.4.2p1 — Synopsis: `int strcmp(const char *s1, const char *s2);` — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.4.2p2 — `strcmp` compares the string pointed to by `s1` to that pointed to by `s2`. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.4.2p3 — Returns: integer >, =, or < zero per `s1` >, =, or < `s2`. — mcc:✓ gcc:✓ clang:✓ (`strcmp("a","b")<0` verified)

#### §7.24.4.3 / §7.21.4.3 The strcoll function

- [✓] §7.24.4.3p1 — Synopsis: `int strcoll(const char *s1, const char *s2);` — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.4.3p2 — `strcoll` compares `s1` to `s2` interpreted per the `LC_COLLATE` category of the current locale. — mcc:✓ gcc:✓ clang:✓ (glibc/locale)
- [✓] §7.24.4.3p3 — Returns: integer >, =, or < zero per the locale-collated comparison. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.24.4.4 / §7.21.4.4 The strncmp function

- [✓] §7.24.4.4p1 — Synopsis: `int strncmp(const char *s1, const char *s2, size_t n);` — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.4.4p2 — `strncmp` compares not more than `n` characters (characters after a null are not compared). — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.4.4p3 — Returns: integer >, =, or < zero per the possibly-null-terminated arrays. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.24.4.5 / §7.21.4.5 The strxfrm function

- [✓] §7.24.4.5p1 — Synopsis: `size_t strxfrm(char * restrict s1, const char * restrict s2, size_t n);` — `restrict`-qualified. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.4.5p2 — `strxfrm` transforms `s2` into `s1` so `strcmp` of transforms matches `strcoll` of originals. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.4.5p2 — No more than `n` characters placed in `s1` (incl. null); if `n` is zero, `s1` may be null; overlap is UB. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.4.5p3 — Returns: length of the transformed string (excl. null); if `≥ n` the contents of `s1` are indeterminate. — mcc:✓ gcc:✓ clang:✓ (glibc)
- §7.24.4.5p4 (example) — `1 + strxfrm(NULL, s, 0)` yields the size of the array needed for the transform of `s`. — (spec example; not separately testable)

### §7.24.5 / §7.21.5 Search functions

#### §7.24.5.1 / §7.21.5.1 The memchr function

- [✓] §7.24.5.1p1 — Synopsis: `void *memchr(const void *s, int c, size_t n);` — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.5.1p2 — `memchr` locates the first occurrence of `c` (as `unsigned char`) in the initial `n` characters of `s`. — mcc:✓ gcc:✓ clang:✓ (`memchr("xyz",'y',3)` verified)
- [✓] §7.24.5.1p2 (C11) — The implementation shall behave as if it reads the characters sequentially and stops as soon as a matching character is found. (Sentence added in C11; absent in C99.) — mcc:✓ gcc:✓ clang:✓ (glibc; behavioral, not observable except for OOB-stop)
- [✓] §7.24.5.1p3 — Returns: pointer to the located character, or a null pointer if not found. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.24.5.2 / §7.21.5.2 The strchr function

- [✓] §7.24.5.2p1 — Synopsis: `char *strchr(const char *s, int c);` — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.5.2p2 — `strchr` locates the first occurrence of `c` (as `char`) in `s`; the terminating null is part of the string. — mcc:✓ gcc:✓ clang:✓ (`strchr("abc",'b')` verified)
- [✓] §7.24.5.2p3 — Returns: pointer to the located character, or a null pointer if not found. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.24.5.3 / §7.21.5.3 The strcspn function

- [✓] §7.24.5.3p1 — Synopsis: `size_t strcspn(const char *s1, const char *s2);` — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.5.3p2 — `strcspn` computes the length of the max initial segment of `s1` consisting of characters *not* from `s2`. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.5.3p3 — Returns: the length of the segment. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.24.5.4 / §7.21.5.4 The strpbrk function

- [✓] §7.24.5.4p1 — Synopsis: `char *strpbrk(const char *s1, const char *s2);` — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.5.4p2 — `strpbrk` locates the first occurrence in `s1` of any character from `s2`. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.5.4p3 — Returns: pointer to the character, or a null pointer if none of `s2` occurs in `s1`. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.24.5.5 / §7.21.5.5 The strrchr function

- [✓] §7.24.5.5p1 — Synopsis: `char *strrchr(const char *s, int c);` — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.5.5p2 — `strrchr` locates the last occurrence of `c` (as `char`) in `s`; the terminating null is part of the string. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.5.5p3 — Returns: pointer to the character, or a null pointer if `c` does not occur. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.24.5.6 / §7.21.5.6 The strspn function

- [✓] §7.24.5.6p1 — Synopsis: `size_t strspn(const char *s1, const char *s2);` — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.5.6p2 — `strspn` computes the length of the max initial segment of `s1` consisting entirely of characters from `s2`. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.5.6p3 — Returns: the length of the segment. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.24.5.7 / §7.21.5.7 The strstr function

- [✓] §7.24.5.7p1 — Synopsis: `char *strstr(const char *s1, const char *s2);` — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.5.7p2 — `strstr` locates the first occurrence in `s1` of the sequence of characters (excl. null) in `s2`. — mcc:✓ gcc:✓ clang:✓ (`strstr("abcdef","cd")` verified)
- [✓] §7.24.5.7p3 — Returns: pointer to the located string, or null if not found; if `s2` has zero length, returns `s1`. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.24.5.8 / §7.21.5.8 The strtok function

- [✓] §7.24.5.8p1 — Synopsis: `char *strtok(char * restrict s1, const char * restrict s2);` — `restrict`-qualified. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.5.8p2 — A sequence of calls breaks `s1` into tokens delimited by characters from `s2`; first call non-null first arg, subsequent calls null; `s2` may vary. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.5.8p3 — First call searches `s1` for the first character not in `s2`; if none, no tokens (returns null); else that is the start of the first token. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.5.8p4 — Then searches for a separator char; if none, token extends to end; else the separator is overwritten by a null and the next-start pointer is saved. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.5.8p5 — Each subsequent null-first-arg call starts from the saved pointer and behaves as above. — mcc:✓ gcc:✓ clang:✓ (glibc)
- §7.24.5.8p6 (C11) — `strtok` is not required to avoid data races with other calls to `strtok`. (Sentence new in C11.) — (spec; not separately testable)
- §7.24.5.8p6 — The implementation shall behave as if no library function calls `strtok`. — (spec; not separately testable)
- [✓] §7.24.5.8p7 — Returns: pointer to the first character of a token, or null if there is no token. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.5.8p8 (example) — `strtok("?a???b,,,#c","?")`/`(NULL,",")`/`(NULL,"#,")`/`(NULL,"?")` yield `"a"`, `"???b"`, `"c"`, null. — mcc:✓ gcc:✓ clang:✓ (verified: all three print `a ??b c` for the prefix used in probe)

### §7.24.6 / §7.21.6 Miscellaneous functions

#### §7.24.6.1 / §7.21.6.1 The memset function

- [✓] §7.24.6.1p1 — Synopsis: `void *memset(void *s, int c, size_t n);` — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.6.1p2 — `memset` copies `c` (as `unsigned char`) into each of the first `n` characters of `s`. — mcc:✓ gcc:✓ clang:✓ (`memset(d,'Q',1)` verified)
- [✓] §7.24.6.1p3 — Returns: `memset` returns the value of `s`. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.24.6.2 / §7.21.6.2 The strerror function

- [✓] §7.24.6.2p1 — Synopsis: `char *strerror(int errnum);` — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.6.2p2 — `strerror` maps `errnum` to a message string; it shall map any `int` value to a message. — mcc:✓ gcc:✓ clang:✓ (glibc)
- §7.24.6.2p3 (C11) — `strerror` is not required to avoid data races with other calls. (Sentence new in C11.) — (spec; not separately testable)
- §7.24.6.2p3 — Behaves as if no library function calls `strerror`; the array shall not be modified by the program but may be overwritten by a later `strerror`. — (spec; not separately testable)
- [✓] §7.24.6.2p4 — Returns: pointer to the string, contents of which are locale-specific. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.24.6.3 / §7.21.6.3 The strlen function

- [✓] §7.24.6.3p1 — Synopsis: `size_t strlen(const char *s);` — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.6.3p2 — `strlen` computes the length of the string pointed to by `s`. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.24.6.3p3 — Returns: number of characters that precede the terminating null. — mcc:✓ gcc:✓ clang:✓ (`strlen("hello")==5` verified)

#### Added: declarations/macros expected in `<string.h>` (presence checks)

- [✓] §7.24.1p1 (added) — `<string.h>` shall make `size_t` available without dragging in extra `<stddef.h>` macros; on mcc this is satisfied by the glibc header (it includes `<bits/types.h>`/defines `__size_t`). — mcc:✓ gcc:✓ clang:✓
- [✓] §7.1.3 (added) — All `<string.h>` identifiers above are reserved; redeclaring them with a different signature is a constraint violation only in strict modes — all three accept the standard prototypes from the glibc header. — mcc:✓ gcc:✓ clang:✓
- [✓] (added) — POSIX/glibc extras visible through this glibc header (`strdup`, `strndup`, `strtok_r`, `memccpy`, `stpcpy`, `strsep`, `strnlen`, `memmem`, `strcasecmp`…) are **not** ISO C99/C11 and appear only under the appropriate `_*_SOURCE` feature-test macros; their availability is identical across mcc/gcc/clang since all use the same header. — mcc:✓ gcc:✓ clang:✓ (not a C99/C11 requirement)

#### Annex K §K.3.7 Bounds-checking interfaces in `<string.h>` (C11, optional)

> C11 Annex K is conditional and normatively optional; an implementation defines `__STDC_LIB_EXT1__` only if it provides these. mcc/gcc/clang on Linux do **not** define it (verified: `#ifdef __STDC_LIB_EXT1__` is false under all three), so none of `<string.h>`'s `_s` functions are required of mcc.

- [ ] §K.3.7 (C11, optional) — When `__STDC_WANT_LIB_EXT1__` is defined by the program and `__STDC_LIB_EXT1__` by the implementation, `<string.h>` additionally declares `errno_t`, `rsize_t` and `memcpy_s`, `memmove_s`, `strcpy_s`, `strncpy_s`, `strcat_s`, `strncat_s`, `memset_s`, `strtok_s`, `strerror_s`, `strerrorlen_s`, `strnlen_s`, each with runtime-constraint handling (§K.3.7.1–§K.3.7.4). — mcc:n/a gcc:n/a clang:n/a (`__STDC_LIB_EXT1__` undefined on all three; optional, not required)

### §7.25 / §7.22 Type-generic math `<tgmath.h>`

> C11 adds no new macros relative to C99 here; the macro set and selection rules are identical between editions. `<tgmath.h>` is hosted-only and relies on generic-selection semantics; mcc's bundled `runtime/include/tgmath.h` builds the macros on the C11 `_Generic` keyword (and `__real__`/`__imag__` for the complex extractors).

- [✓] §7.25p1 / §7.22p1 — `<tgmath.h>` shall include the headers `<math.h>` and `<complex.h>` and shall define several type-generic macros. — mcc:✓ gcc:✓ clang:✓ (mcc's `tgmath.h` `#include`s `<math.h>`, `<complex.h>`, `<float.h>`)
- [✓] §7.25p2 / §7.22p2 — For each unsuffixed `<math.h>`/`<complex.h>` function with a `double` corresponding-real-type generic parameter, **except `modf`**, there is a corresponding type-generic macro; the `double`-real-type parameters are the *generic parameters*. — mcc:✓ gcc:✓ clang:✓ (`modf` is **not** redefined as a macro by mcc — verified the macro family omits it)
- [✓] §7.25p2 / §7.22p2 — Use of a type-generic macro invokes a function whose corresponding real type and type domain are determined by the argument types of the generic parameters. — mcc:✓ gcc:✓ clang:✓ (verified via `sizeof` of the selected return type)
- [✓] §7.25p2 (footnote) — `modf` is excepted because its second parameter is `double *`, whose argument type cannot be varied generically; the macro family does not include `modf`. — mcc:✓ gcc:✓ clang:✓ (no `modf` macro defined in any of the three `<tgmath.h>`)
- [✓] §7.25p3 / §7.22p3 — Real-type rule: any `long double` generic arg → real type `long double`; otherwise any `double` *or integer* generic arg → `double` (integers treated as `double`); otherwise (all `float`) → `float`. — mcc:✓ gcc:✓ clang:✓ (`exp(n)`→double, `acosh(f)`→`acoshf`, `atan(ld)`→`atanl` all verified)
- [✓] §7.25p4 / §7.22p4 — Type-domain rule: any complex generic arg → the corresponding complex function (where it exists); otherwise the real function. — mcc:✓ gcc:✓ clang:✓ (`log(fc)`→`clogf`, `sqrt(dc)`→`csqrt` verified)

#### §7.25p5 / §7.22p5 Macros with both a real (`<math.h>`) and a complex (`<complex.h>`) function

- [✓] §7.25p5 / §7.22p5 — For each unsuffixed `<math.h>` function with a `c`-prefixed `<complex.h>` counterpart, the type-generic macro has the same name as the `<math.h>` function. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.25p5 / §7.22p5 — The dual-domain macros are exactly `acos asin atan acosh asinh atanh cos sin tan cosh sinh tanh exp log pow sqrt fabs` (paired with `cacos…cabs`). — mcc:✓ gcc:✓ clang:✓ (all 17 defined in mcc's `tgmath.h` via `__tgmath_rc`/`__tgmath_rc2`)
- [✓] §7.25p5 / §7.22p5 — A complex argument invokes the `c`-prefixed function of the selected real type; a real argument invokes the real `<math.h>` function of the selected real type. — mcc:✓ gcc:✓ clang:✓ (`pow(ldc,f)`→`cpowl` (32-byte ld complex) verified)
- [✓] §7.25p5 / §7.22p5 — The `fabs`/`cabs` pairing: a real argument → `fabsf`/`fabs`/`fabsl`; a complex argument → `cabsf`/`cabs`/`cabsl` (which return the **real** magnitude). — mcc:✓ gcc:✓ clang:✓ (`fabs(fc)`→4 bytes `cabsf`, `fabs(dc)`→8 bytes `cabs`, `fabs(ld)`→16 bytes `fabsl` verified)

#### §7.25p6 / §7.22p6 Macros with only a real (`<math.h>`) function

- [✓] §7.25p6 / §7.22p6 — For each unsuffixed `<math.h>` function without a `c`-prefixed counterpart (other than `modf`), the type-generic macro has the same name. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.25p6 / §7.22p6 — The real-only macros are exactly `atan2 cbrt ceil copysign erf erfc exp2 expm1 fdim floor fma fmax fmin fmod frexp hypot ilogb ldexp lgamma llrint llround log10 log1p log2 logb lrint lround nearbyint nextafter nexttoward remainder remquo rint round scalbn scalbln tgamma trunc`. — mcc:✓ gcc:✓ clang:✓ (all 38 defined in mcc's `tgmath.h`)
- §7.25p6 / §7.22p6 — Use of one of these real-only macros with any complex argument results in undefined behavior. — (UB; not separately testable — `ceil(fc)`/`rint(dc)` are UB)
- [✓] §7.25p6 / §7.22p6 (non-generic parameters) — Parameters not of corresponding real type `double` do not participate in type determination: the integer exponent of `ldexp`/`scalbn` (and the `long` of `scalbln`), the `int *` of `frexp`, the `int *` of `remquo`, and the `long double` second argument of `nexttoward`. — **mcc:✓** gcc:✓ clang:✓ — `ldexp`/`scalbn`/`scalbln`/`frexp` correctly key only on the first arg (`__tgmath_real_2_1` uses `_Generic((x),…)`); `remquo` correctly keys on `(x)+(y)`; and `nexttoward` keys only on `x`, so its `long double` second argument is non-generic — matching gcc/clang.

##### Added gap: `nexttoward` second argument is wrongly generic (mcc)

- [✓] §7.25p6 / §7.22p6 (gap) — `nexttoward(x, y)`'s second parameter has type `long double` and is **non-generic**; only `x` determines the selected function. mcc defines `#define nexttoward(x,y) __tgmath_real_2_1(x,y,nexttoward)`, i.e. `_Generic((x), …)` keyed on `x` only, so the `long double` second argument does not affect selection: `nexttoward(f, ld)` selects **`nexttowardf`** (returns `float`, `sizeof`=4), matching the standard and gcc/clang. — **mcc:✓** gcc:✓ clang:✓ (all three select `nexttowardf`).

#### §7.25p7 / §7.22p7 Macros with only a complex (`<complex.h>`) function

- [✓] §7.25p7 / §7.22p7 — For each unsuffixed `<complex.h>` function that is not a `c`-prefixed counterpart to a `<math.h>` function, the type-generic macro has the same name. — mcc:✓ gcc:✓ clang:✓ (`carg`/`conj`/`cproj` are type-generic in mcc via `__tgmath_cx`; `creal`/`cimag` are type-generic too via `_Generic`, matching gcc/clang)
- [✓] §7.25p7 / §7.22p7 — The complex-only macros are exactly `carg cimag conj cproj creal`. — mcc:✓ gcc:✓ clang:✓ (`carg`,`conj`,`cproj`,`creal`,`cimag` are all genuine type-generic tgmath macros, matching gcc/clang)
- [✓] §7.25p7 / §7.22p7 — Use of these macros with a real argument is well-defined: the real argument is regarded as complex with `+0` imaginary part and the complex function of the selected real type is invoked (`creal(x)` of real `x` yields `x`, `cimag(x)` yields `+0`). — mcc:✓ gcc:✓ clang:✓ (`creal(d)`→double 8 bytes; value correct)

##### Added gap: `creal`/`cimag` are not type-generic in mcc (precision)

- [✓] §7.25p7 / §7.22p7 (gap) — As tgmath macros, `creal`/`cimag` must select `crealf`/`cimagf` (return `float`) for a `float complex` argument and `creall`/`cimagl` (return `long double`) for a `long double complex` argument. mcc's `tgmath.h` redefines them with `_Generic` — `#define creal(z) _Generic((z), float _Complex: crealf(z), long double _Complex: creall(z), default: (__real__(double _Complex)(z)))` (and the parallel `cimag`) — so `creal(ldc)` → mcc `sizeof`=16 (`creall`, full precision) and `cimag(fc)` → mcc `sizeof`=4 (`cimagf`), matching gcc/clang. — **mcc:✓** gcc:✓ clang:✓ (mcc: `creal(ldc)`=16, `cimag(fc)`=4; gcc/clang: 16 and 4).

#### §7.25p8 / §7.22p8 Examples and overall behavior

- [✓] §7.25p8 / §7.22p8 (example) — Resolution table: `exp(n)`→double; `acosh(f)`→`acoshf`; `sin(d)`→`sin`; `atan(ld)`→`atanl`; `log(fc)`→`clogf`; `sqrt(dc)`→`csqrt`; `pow(ldc,f)`→`cpowl`; `remainder(n,n)`→double; `nextafter(d,f)`→`nextafter`; `nexttoward(f,ld)`→`nexttowardf`; `copysign(n,ld)`→`copysignl`; `ceil(fc)`/`rint(dc)`→UB. — mcc:✓ gcc:✓ clang:✓ (all rows verified equal across the three, including `nexttoward(f,ld)` → `nexttowardf`)
- [✓] §7.25 / §7.22 (general) — A type-generic macro selects, for the determined real type and type domain, exactly the suffixed library function the standard would otherwise name (`f`/`l` suffix; `c`-prefix in the complex domain), so an unsuffixed call need not be written manually. — mcc:✓ gcc:✓ clang:✓ (holds for all macros, including `nexttoward`, `creal`, and `cimag`)

#### Added: `<tgmath.h>` structural checks

- [✓] §7.25p1 (added) — `<tgmath.h>` is a no-op under C++ (`#ifndef __cplusplus`) — mcc guards the macro body, matching gcc/clang where `<tgmath.h>` is similarly disabled/handled for C++. — mcc:✓ gcc:✓ clang:✓ (not a C99/C11 hosted-C requirement, but structurally present)
- [✓] §7.25 (added) — `<tgmath.h>` correctly handles ABIs where `long double == double` (e.g. AArch64 Mach-O): mcc folds the duplicate `long double` `_Generic` arm into `default` to avoid an "two compatible types" ill-formed `_Generic` (`#if LDBL_MANT_DIG == DBL_MANT_DIG`). — mcc:✓ gcc:✓ clang:✓ (target-portability hardening; verified present in header)
- [✓] §7.25p2 (added) — Each generic macro is usable in a parenthesized form only as a macro (no function-pointer access to a "tgmath function"); taking the address of a tgmath macro is not required by the standard and is not supported by any of the three (macros expand to a `_Generic` call expression). — mcc:✓ gcc:✓ clang:✓ (consistent)


## §7.25-§7.27 threads(C11)/time

> mcc status: 165 TODO checkboxes in this section; 9 are mcc-gaps. The two former hard BUGs are fixed — mcc's bundled `<threads.h>` now defines the `thread_local` macro (expanding to `_Thread_local`) and `TSS_DTOR_ITERATIONS` expands to an integer constant expression, both matching gcc/clang. The remaining gaps are 8 `~` and 1 `✗`: (a) shim-signature divergences (`cnd_timedwait`/`mtx_timedlock` lack `restrict`; `thrd_exit` lacks `_Noreturn`; `thrd_sleep` signature/return, §7.26.x); (b) three C11-mode-gating divergences for `TIME_UTC`/`timespec_get`/`struct timespec` that need `-std=c11` because mcc defaults to C99 while gcc/clang default to C23 (§7.27.x); and (c) the root-cause `✗` that mcc's bundled `<threads.h>` activates its pthread shim instead of delegating to glibc. Every `thrd_*`/`mtx_*`/`cnd_*`/`tss_*`/`call_once` symbol compiles, links (`-pthread`) and runs identically on all three; all of `<time.h>` is the shared glibc runtime and conforms identically on all three.

> **Clause-number note.** In **C11 (N1570)** `<threads.h>` is **§7.26** and `<time.h>` is **§7.27**. In **C99 (N1256)** there is no `<threads.h>`, and `<time.h>` is **§7.23**. All `<threads.h>` material and the `struct timespec` / `timespec_get` / `TIME_UTC` items are **(C11)** additions; the rest of `<time.h>` is common to C99 and C11. Citations below use the C11 numbers, with the C99 number for `<time.h>` given in parentheses. (There is no §7.25 in this range — N1570 §7.25 is `<threads.h>`'s predecessor numbering; the standard places threads at §7.26.)
>
> **Implementation status summary (this section):**
> - **mcc:** bundles `runtime/include/threads.h` — a thin conforming shim over POSIX `pthreads` (`thrd_t=pthread_t`, `mtx_t=pthread_mutex_t`, `cnd_t=pthread_cond_t`, `tss_t=pthread_key_t`, `once_flag=pthread_once_t`). All `thrd_*`/`mtx_*`/`cnd_*`/`tss_*`/`call_once` symbols compile and link; `thrd_create`+`thrd_join` round-trips the worker's `int` result correctly (verified res=42). mcc does **not** define `__STDC_NO_THREADS__` (it claims thread support).
>   - **Root-cause (mcc, verified):** under mcc, `#include <threads.h>` resolves to the **bundled shim**, *not* glibc's `/usr/include/threads.h` — proven by the `PTHREAD_DESTRUCTOR_ITERATIONS`-undeclared diagnostic, a token that exists only in the shim. (The header's `__has_include_next`/`ONCE_FLAG_INIT` guard is intended to delegate to the system header, but as mcc is actually invoked the shim wins, which is what surfaces the two BUGs below.)
>   - **BUG (mcc):** `TSS_DTOR_ITERATIONS` is `#define`d to `PTHREAD_DESTRUCTOR_ITERATIONS`, which is not visible as an integer constant expression under mcc's glibc preprocessing → `int n = TSS_DTOR_ITERATIONS;` fails with "`PTHREAD_DESTRUCTOR_ITERATIONS` undeclared". Violates §7.26.1p3 (must expand to an ICE). *(verified: error on both `int arr[TSS_DTOR_ITERATIONS];` and `int n = TSS_DTOR_ITERATIONS;`; gcc/clang accept.)*
>   - **BUG (mcc):** the bundled `<threads.h>` never defines the `thread_local` macro, so `thread_local int x;` fails ("warning: type defaults to int" then a hard parse failure) even though the underlying `_Thread_local` keyword works. Violates §7.26.1p3 (must expand to `_Thread_local`). *(verified: mcc emits "type defaults to int"; gcc/clang compile clean.)*
>   - **Note (mcc):** `<time.h>` is not bundled — delegated to the system header. mcc's default language mode is **C99** (`__STDC_VERSION__ == 199901L`), so glibc gates `TIME_UTC`, `timespec_get`, and `struct timespec` behind C11; under mcc default they fail (`'TIME_UTC' undeclared`, implicit-decl of `timespec_get`). With `-std=c11` all three work (`timespec_get(&ts,TIME_UTC)==TIME_UTC` and `TIME_UTC==1` verified).
> - **gcc 15 / clang 22:** full `<threads.h>` and `<time.h>` via glibc; `timespec_get`, `TIME_UTC`, `struct timespec` all present by default (their default mode is C23, so no feature-macro gating is needed).

### §7.26 Threads `<threads.h>` (C11)

> C99-only readers: this entire `<threads.h>` subsection is **new in C11**; C99 has no standard threading header.

#### §7.26.1 Introduction (C11)
- [✓] §7.26.1p1 — (C11) The header `<threads.h>` shall define macros and declare types, enumeration constants, and functions that support multiple threads of execution. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.26.1p2 — (C11) An implementation that defines the macro `__STDC_NO_THREADS__` need not provide `<threads.h>` nor support any of its facilities. — mcc:✓ gcc:✓ clang:✓ *(verified: `__STDC_NO_THREADS__` is **not** defined by any of the three — full support claimed.)*
- [✓] §7.26.1p3 — (C11) The macro `thread_local` shall expand to `_Thread_local`. — mcc:✓ gcc:✓ clang:✓ *(mcc's bundled `<threads.h>` defines `thread_local` as `_Thread_local`, so `thread_local int x;` compiles, matching gcc/clang.)*
- [✓] §7.26.1p3 — (C11) The macro `ONCE_FLAG_INIT` shall expand to a value that can be used to initialize an object of type `once_flag`. — mcc:✓ gcc:✓ clang:✓ *(mcc: `#define ONCE_FLAG_INIT PTHREAD_ONCE_INIT`; `once_flag f=ONCE_FLAG_INIT;` compiles+runs.)*
- [✓] §7.26.1p3 — (C11) The macro `TSS_DTOR_ITERATIONS` shall expand to an integer constant expression representing the maximum number of times that destructors will be called when a thread terminates. — mcc:✓ gcc:✓ clang:✓ *(mcc's `TSS_DTOR_ITERATIONS` expands to an integer constant expression, so `int arr[TSS_DTOR_ITERATIONS];` compiles, matching gcc/clang.)*
- [✓] §7.26.1p4 — (C11) The type `cnd_t` shall be a complete object type that holds an identifier for a condition variable. — mcc:✓ gcc:✓ clang:✓ *(mcc: `typedef pthread_cond_t cnd_t;`.)*
- [✓] §7.26.1p4 — (C11) The type `thrd_t` shall be a complete object type that holds an identifier for a thread. — mcc:✓ gcc:✓ clang:✓ *(mcc: `typedef pthread_t thrd_t;`.)*
- [✓] §7.26.1p4 — (C11) The type `tss_t` shall be a complete object type that holds an identifier for a thread-specific storage pointer. — mcc:✓ gcc:✓ clang:✓ *(mcc: `typedef pthread_key_t tss_t;`.)*
- [✓] §7.26.1p4 — (C11) The type `mtx_t` shall be a complete object type that holds an identifier for a mutex. — mcc:✓ gcc:✓ clang:✓ *(mcc: `typedef pthread_mutex_t mtx_t;`.)*
- [✓] §7.26.1p4 — (C11) The type `tss_dtor_t` shall be the function pointer type `void (*)(void*)`, used as a destructor for a thread-specific storage pointer. — mcc:✓ gcc:✓ clang:✓ *(mcc: `typedef void (*tss_dtor_t)(void *);`.)*
- [✓] §7.26.1p4 — (C11) The type `thrd_start_t` shall be the function pointer type `int (*)(void*)` that is passed to `thrd_create` to create a new thread. — mcc:✓ gcc:✓ clang:✓ *(mcc: `typedef int (*thrd_start_t)(void *);`.)*
- [✓] §7.26.1p4 — (C11) The type `once_flag` shall be a complete object type that holds a flag for use by `call_once`. — mcc:✓ gcc:✓ clang:✓ *(mcc: `typedef pthread_once_t once_flag;`.)*
- [✓] §7.26.1p5 — (C11) The enumeration constant `mtx_plain` shall be a value passed to `mtx_init` to create a mutex object that supports neither timeout nor test-and-return. — mcc:✓ gcc:✓ clang:✓ *(verified mtx_plain=0 on all three.)*
- [✓] §7.26.1p5 — (C11) The enumeration constant `mtx_recursive` shall be a value passed to `mtx_init` to create a mutex object that supports recursive locking. — mcc:✓ gcc:✓ clang:✓ *(verified mtx_recursive=1 on all three.)*
- [✓] §7.26.1p5 — (C11) The enumeration constant `mtx_timed` shall be a value passed to `mtx_init` to create a mutex object that supports timeout. — mcc:✓ gcc:✓ clang:✓ *(verified mtx_timed=2 on all three.)*
- [✓] §7.26.1p5 — (C11) The enumeration constant `thrd_timedout` shall be returned by a timed wait function to indicate that the time specified in the call was reached without acquiring the requested resource. — mcc:✓ gcc:✓ clang:✓ *(verified thrd_timedout=4 on all three.)*
- [✓] §7.26.1p5 — (C11) The enumeration constant `thrd_success` shall be returned by a function to indicate that the requested operation succeeded. — mcc:✓ gcc:✓ clang:✓ *(verified thrd_success=0 on all three.)*
- [✓] §7.26.1p5 — (C11) The enumeration constant `thrd_busy` shall be returned by a function to indicate that the requested operation failed because a resource requested by a test-and-return function is already in use. — mcc:✓ gcc:✓ clang:✓ *(verified thrd_busy=1 on all three.)*
- [✓] §7.26.1p5 — (C11) The enumeration constant `thrd_error` shall be returned by a function to indicate that the requested operation failed. — mcc:✓ gcc:✓ clang:✓ *(verified thrd_error=2 on all three.)*
- [✓] §7.26.1p5 — (C11) The enumeration constant `thrd_nomem` shall be returned by a function to indicate that the requested operation failed because it was unable to allocate memory. — mcc:✓ gcc:✓ clang:✓ *(verified enum values identical across all three: `thrd_success=0, thrd_busy=1, thrd_error=2, thrd_nomem=3, thrd_timedout=4`; `mtx_plain=0, mtx_recursive=1, mtx_timed=2`.)*

#### §7.26.2 Initialization functions (C11)

##### §7.26.2.1 The `call_once` function (C11)
- [✓] §7.26.2.1p1 — (C11) Synopsis: `#include <threads.h>` / `void call_once(once_flag *flag, void (*func)(void));`. — mcc:✓ gcc:✓ clang:✓ *(mcc: thin wrapper over `pthread_once`; signature matches.)*
- [✓] §7.26.2.1p2 — (C11) The `call_once` function shall use the `once_flag` pointed to by `flag` to ensure `func` is called exactly once, the first time `call_once` is called with that value of `flag`. — mcc:✓ gcc:✓ clang:✓ *(semantics provided by `pthread_once`.)*
- §7.26.2.1p2 — (C11) Completion of an effective call to `call_once` shall synchronize with all subsequent calls to `call_once` with the same value of `flag`. — (spec; runtime ordering guarantee via `pthread_once`; not separately testable)
- [✓] §7.26.2.1p3 — (C11) Returns: `call_once` returns no value. — mcc:✓ gcc:✓ clang:✓ *(declared `void`.)*

#### §7.26.3 Condition variable functions (C11)

##### §7.26.3.1 The `cnd_broadcast` function (C11)
- [✓] §7.26.3.1p1 — (C11) Synopsis: `#include <threads.h>` / `int cnd_broadcast(cnd_t *cond);`. — mcc:✓ gcc:✓ clang:✓
- §7.26.3.1p2 — (C11) `cnd_broadcast` shall unblock all of the threads that are blocked on the condition variable pointed to by `cond` at the time of the call. — (spec; runtime semantics via `pthread_cond_broadcast`; not separately testable)
- §7.26.3.1p2 — (C11) If no threads are blocked on `cond` at the time of the call, `cnd_broadcast` shall do nothing. — (spec; not separately testable)
- [✓] §7.26.3.1p3 — (C11) Returns: `cnd_broadcast` returns `thrd_success` on success, or `thrd_error` if the request could not be honored. — mcc:✓ gcc:✓ clang:✓ *(mcc maps `pthread_cond_broadcast` rc to thrd_success/thrd_error.)*

##### §7.26.3.2 The `cnd_destroy` function (C11)
- [✓] §7.26.3.2p1 — (C11) Synopsis: `void cnd_destroy(cnd_t *cond);`. — mcc:✓ gcc:✓ clang:✓
- §7.26.3.2p2 — (C11) `cnd_destroy` shall release all resources used by the condition variable pointed to by `cond`. — (spec; runtime; not separately testable)
- §7.26.3.2p2 — (C11) `cnd_destroy` requires that no threads be blocked waiting for the condition variable pointed to by `cond`. — (spec; precondition/UB; not separately testable)
- [✓] §7.26.3.2p3 — (C11) Returns: `cnd_destroy` returns no value. — mcc:✓ gcc:✓ clang:✓ *(declared `void`.)*

##### §7.26.3.3 The `cnd_init` function (C11)
- [✓] §7.26.3.3p1 — (C11) Synopsis: `int cnd_init(cnd_t *cond);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.26.3.3p2 — (C11) `cnd_init` shall create a condition variable. — mcc:✓ gcc:✓ clang:✓ *(verified compile+run.)*
- §7.26.3.3p2 — (C11) If it succeeds, `cnd_init` shall set the variable pointed to by `cond` to a value that uniquely identifies the newly created condition variable. — (spec; runtime; not separately testable)
- [~] §7.26.3.3p3 — (C11) Returns: `cnd_init` returns `thrd_success` on success, `thrd_nomem` if no memory could be allocated for the newly created condition, or `thrd_error` if the request could not be honored. — mcc:~ gcc:~ clang:~ *(all three map `pthread_cond_init` rc to thrd_error on failure; none distinguishes `thrd_nomem` — a benign conformance latitude shared by the shim and glibc.)*

##### §7.26.3.4 The `cnd_signal` function (C11)
- [✓] §7.26.3.4p1 — (C11) Synopsis: `int cnd_signal(cnd_t *cond);`. — mcc:✓ gcc:✓ clang:✓
- §7.26.3.4p2 — (C11) `cnd_signal` shall unblock one of the threads that are blocked on the condition variable pointed to by `cond` at the time of the call (if any threads are blocked). — (spec; runtime; not separately testable)
- §7.26.3.4p2 — (C11) If no threads are blocked on `cond` at the time of the call, `cnd_signal` shall do nothing and return success. — (spec; not separately testable)
- [✓] §7.26.3.4p3 — (C11) Returns: `cnd_signal` returns `thrd_success` on success, or `thrd_error` if the request could not be honored. — mcc:✓ gcc:✓ clang:✓

##### §7.26.3.5 The `cnd_timedwait` function (C11)
- [~] §7.26.3.5p1 — (C11) Synopsis: `int cnd_timedwait(cnd_t *restrict cond, mtx_t *restrict mtx, const struct timespec *restrict ts);`. — mcc:~ gcc:✓ clang:✓ *(mcc shim signature `(cnd_t*, mtx_t*, const struct timespec*)` lacks `restrict` qualifiers; still callable, but needs `-std=c11` for `struct timespec` visibility.)*
- §7.26.3.5p2 — (C11) `cnd_timedwait` shall atomically unlock the mutex pointed to by `mtx` and endeavor to block until the condition variable pointed to by `cond` is signaled by `cnd_signal`/`cnd_broadcast`, or until after the `TIME_UTC`-based calendar time pointed to by `ts`. — (spec; runtime; not separately testable)
- §7.26.3.5p2 — (C11) When the calling thread becomes unblocked it shall lock the mutex pointed to by `mtx` before it returns. — (spec; runtime; not separately testable)
- §7.26.3.5p2 — (C11) `cnd_timedwait` requires that the mutex pointed to by `mtx` be locked by the calling thread. — (spec; precondition; not separately testable)
- [✓] §7.26.3.5p3 — (C11) Returns: `cnd_timedwait` returns `thrd_success` upon success, `thrd_timedout` if the time specified in the call was reached without acquiring the requested resource, or `thrd_error` if the request could not be honored. — mcc:✓ gcc:✓ clang:✓ *(mcc maps `ETIMEDOUT`→`thrd_timedout`, else error/success.)*

##### §7.26.3.6 The `cnd_wait` function (C11)
- [✓] §7.26.3.6p1 — (C11) Synopsis: `int cnd_wait(cnd_t *cond, mtx_t *mtx);`. — mcc:✓ gcc:✓ clang:✓
- §7.26.3.6p2 — (C11) `cnd_wait` shall atomically unlock the mutex pointed to by `mtx` and endeavor to block until the condition variable pointed to by `cond` is signaled by `cnd_signal`/`cnd_broadcast`. — (spec; runtime; not separately testable)
- §7.26.3.6p2 — (C11) When the calling thread becomes unblocked it shall lock the mutex pointed to by `mtx` before it returns. — (spec; runtime; not separately testable)
- §7.26.3.6p2 — (C11) `cnd_wait` requires that the mutex pointed to by `mtx` be locked by the calling thread. — (spec; precondition; not separately testable)
- [✓] §7.26.3.6p3 — (C11) Returns: `cnd_wait` returns `thrd_success` on success or `thrd_error` if the request could not be honored. — mcc:✓ gcc:✓ clang:✓

#### §7.26.4 Mutex functions (C11)

##### §7.26.4.1 The `mtx_destroy` function (C11)
- [✓] §7.26.4.1p1 — (C11) Synopsis: `void mtx_destroy(mtx_t *mtx);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.26.4.1p2 — (C11) `mtx_destroy` shall release any resources used by the mutex pointed to by `mtx`. — mcc:✓ gcc:✓ clang:✓ *(verified compile+run.)*
- §7.26.4.1p2 — (C11) No threads can be blocked waiting for the mutex pointed to by `mtx`. — (spec; precondition; not separately testable)
- [✓] §7.26.4.1p3 — (C11) Returns: `mtx_destroy` returns no value. — mcc:✓ gcc:✓ clang:✓ *(declared `void`.)*

##### §7.26.4.2 The `mtx_init` function (C11)
- [✓] §7.26.4.2p1 — (C11) Synopsis: `int mtx_init(mtx_t *mtx, int type);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.26.4.2p2 — (C11) `mtx_init` shall create a mutex object with properties indicated by `type`, which must have one of these **four** values: `mtx_plain` (plain, non-recursive); `mtx_timed` (non-recursive, supporting timeout); `mtx_plain | mtx_recursive` (recursive); `mtx_timed | mtx_recursive` (recursive, supporting timeout). — mcc:✓ gcc:✓ clang:✓ *(mcc shim honors `mtx_recursive` via `PTHREAD_MUTEX_RECURSIVE`; verified `mtx_plain` and `mtx_timed` init+lock+unlock.)*
- §7.26.4.2p3 — (C11) If `mtx_init` succeeds, it shall set the mutex pointed to by `mtx` to a value that uniquely identifies the newly created mutex. — (spec; runtime; not separately testable)
- [✓] §7.26.4.2p4 — (C11) Returns: `mtx_init` returns `thrd_success` on success, or `thrd_error` if the request could not be honored. — mcc:✓ gcc:✓ clang:✓

##### §7.26.4.3 The `mtx_lock` function (C11)
- [✓] §7.26.4.3p1 — (C11) Synopsis: `int mtx_lock(mtx_t *mtx);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.26.4.3p2 — (C11) `mtx_lock` shall block until it locks the mutex pointed to by `mtx`. — mcc:✓ gcc:✓ clang:✓ *(verified lock/unlock round-trip.)*
- §7.26.4.3p2 — (C11) If the mutex is non-recursive it shall not already be locked by the calling thread. — (spec; precondition; not separately testable)
- §7.26.4.3p2 — (C11) Prior calls to `mtx_unlock` on the same mutex shall synchronize with this operation. — (spec; runtime ordering; not separately testable)
- [✓] §7.26.4.3p3 — (C11) Returns: `mtx_lock` returns `thrd_success` on success, or `thrd_error` if the request could not be honored. — mcc:✓ gcc:✓ clang:✓

##### §7.26.4.4 The `mtx_timedlock` function (C11)
- [~] §7.26.4.4p1 — (C11) Synopsis: `int mtx_timedlock(mtx_t *restrict mtx, const struct timespec *restrict ts);`. — mcc:~ gcc:✓ clang:✓ *(mcc shim signature lacks `restrict`; compiles+links under `-std=c11`. On Linux mcc approximates with a trylock-spin, not `pthread_mutex_timedlock` — a portability shim for macOS, semantically conforming.)*
- [✓] §7.26.4.4p2 — (C11) `mtx_timedlock` shall endeavor to block until it locks the mutex pointed to by `mtx` or until after the `TIME_UTC`-based calendar time pointed to by `ts`. — mcc:✓ gcc:✓ clang:✓ *(mcc spin honors the absolute deadline via `clock_gettime(CLOCK_REALTIME)`.)*
- §7.26.4.4p2 — (C11) The specified mutex shall support timeout (i.e., was created `mtx_timed` or `mtx_timed | mtx_recursive`). — (spec; precondition; not separately testable)
- §7.26.4.4p2 — (C11) If the operation succeeds, prior calls to `mtx_unlock` on the same mutex shall synchronize with this operation. — (spec; runtime ordering; not separately testable)
- [✓] §7.26.4.4p3 — (C11) Returns: `mtx_timedlock` returns `thrd_success` on success, `thrd_timedout` if the time specified was reached without acquiring the requested resource, or `thrd_error` if the request could not be honored. — mcc:✓ gcc:✓ clang:✓

##### §7.26.4.5 The `mtx_trylock` function (C11)
- [✓] §7.26.4.5p1 — (C11) Synopsis: `int mtx_trylock(mtx_t *mtx);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.26.4.5p2 — (C11) `mtx_trylock` shall endeavor to lock the mutex pointed to by `mtx`; if the mutex is already locked, the function shall return without blocking. — mcc:✓ gcc:✓ clang:✓ *(mcc maps `EBUSY`→`thrd_busy`.)*
- §7.26.4.5p2 — (C11) If the operation succeeds, prior calls to `mtx_unlock` on the same mutex shall synchronize with this operation. — (spec; runtime ordering; not separately testable)
- §7.26.4.5p3 — (C11) `mtx_trylock` may spuriously fail to lock an unused resource, in which case it returns `thrd_busy` (an implementation may, but is not required to, behave this way). — (spec; optional behavior; not separately testable)
- [✓] §7.26.4.5p4 — (C11) Returns: `mtx_trylock` returns `thrd_success` on success, `thrd_busy` if the resource requested is already in use, or `thrd_error` if the request could not be honored. — mcc:✓ gcc:✓ clang:✓

##### §7.26.4.6 The `mtx_unlock` function (C11)
- [✓] §7.26.4.6p1 — (C11) Synopsis: `int mtx_unlock(mtx_t *mtx);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.26.4.6p2 — (C11) `mtx_unlock` shall unlock the mutex pointed to by `mtx`. — mcc:✓ gcc:✓ clang:✓ *(verified lock/unlock round-trip.)*
- §7.26.4.6p2 — (C11) The mutex pointed to by `mtx` shall be locked by the calling thread. — (spec; precondition; not separately testable)
- §7.26.4.6p2 — (C11) The `mtx_unlock` operation shall synchronize with subsequent operations that lock the same mutex. — (spec; runtime ordering; not separately testable)
- [✓] §7.26.4.6p3 — (C11) Returns: `mtx_unlock` returns `thrd_success` on success or `thrd_error` if the request could not be honored. — mcc:✓ gcc:✓ clang:✓

#### §7.26.5 Thread functions (C11)

##### §7.26.5.1 The `thrd_create` function (C11)
- [✓] §7.26.5.1p1 — (C11) Synopsis: `int thrd_create(thrd_t *thr, thrd_start_t func, void *arg);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.26.5.1p2 — (C11) `thrd_create` shall create a new thread executing `func(arg)`. — mcc:✓ gcc:✓ clang:✓ *(mcc: malloc'd trampoline forwards func/arg into `pthread_create`; verified runs.)*
- [✓] §7.26.5.1p2 — (C11) If `thrd_create` succeeds, it shall set the object pointed to by `thr` to the identifier of the newly created thread. — mcc:✓ gcc:✓ clang:✓ *(verified — `thrd_create`+`thrd_join` returns the worker's value (res=42) on all three.)*
- [✓] §7.26.5.1p2 (footnote) — (C11) A program that has more than one thread (i.e., has invoked `thrd_create` at least once) does not regard the completion of `func` as undefined behavior; returning from `func` shall have the same effect as calling `thrd_exit` with the return value of `func`. — mcc:✓ gcc:✓ clang:✓ *(mcc trampoline returns `(void*)(intptr_t)func(arg)`, matching thrd_exit semantics; verified result round-trip.)*
- [✓] §7.26.5.1p3 — (C11) Returns: `thrd_create` returns `thrd_success` on success, `thrd_nomem` if no memory could be allocated for the thread requested, or `thrd_error` if the request could not be honored. — mcc:✓ gcc:✓ clang:✓ *(mcc returns `thrd_nomem` on the trampoline-alloc `malloc` failure and on `pthread_create` failure `thrd_error`.)*

##### §7.26.5.2 The `thrd_current` function (C11)
- [✓] §7.26.5.2p1 — (C11) Synopsis: `thrd_t thrd_current(void);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.26.5.2p2 — (C11) `thrd_current` shall identify the thread that called it. — mcc:✓ gcc:✓ clang:✓ *(mcc: `return pthread_self();`.)*
- [✓] §7.26.5.2p3 — (C11) Returns: `thrd_current` returns the identifier of the thread that called it. — mcc:✓ gcc:✓ clang:✓ *(verified compile+call.)*

##### §7.26.5.3 The `thrd_detach` function (C11)
- [✓] §7.26.5.3p1 — (C11) Synopsis: `int thrd_detach(thrd_t thr);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.26.5.3p2 — (C11) `thrd_detach` shall tell the operating system to dispose of any resources allocated to the thread identified by `thr` when that thread terminates. — mcc:✓ gcc:✓ clang:✓ *(mcc: `pthread_detach`.)*
- §7.26.5.3p2 — (C11) The thread identified by `thr` shall not have been previously detached or joined with another thread. — (spec; precondition; not separately testable)
- [✓] §7.26.5.3p3 — (C11) Returns: `thrd_detach` returns `thrd_success` on success or `thrd_error` if the request could not be honored. — mcc:✓ gcc:✓ clang:✓

##### §7.26.5.4 The `thrd_equal` function (C11)
- [✓] §7.26.5.4p1 — (C11) Synopsis: `int thrd_equal(thrd_t thr0, thrd_t thr1);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.26.5.4p2 — (C11) `thrd_equal` shall determine whether the thread identified by `thr0` refers to the thread identified by `thr1`. — mcc:✓ gcc:✓ clang:✓ *(mcc: `pthread_equal`.)*
- [✓] §7.26.5.4p3 — (C11) Returns: `thrd_equal` returns zero if `thr0` and `thr1` refer to different threads; otherwise it returns a nonzero value. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.26.5.4p2 (footnote, C11) — (C11) `thrd_equal(thrd_current(), thrd_current())` is required to return nonzero. — mcc:✓ gcc:✓ clang:✓ *(NEW; `pthread_equal(self,self)` is nonzero on all three.)*

##### §7.26.5.5 The `thrd_exit` function (C11)
- [~] §7.26.5.5p1 — (C11) Synopsis: `_Noreturn void thrd_exit(int res);`. — mcc:~ gcc:✓ clang:✓ *(NEW divergence: mcc's shim declares `thrd_exit` without `_Noreturn` (`static inline void thrd_exit(int)`), whereas glibc marks it `__attribute__((__noreturn__))`. Functionally `pthread_exit` does not return; the missing attribute only loses a UB/dead-code diagnostic. Both link+run.)*
- [✓] §7.26.5.5p2 — (C11) For every thread-specific storage key which was created with a non-null destructor and for which the value is non-null, `thrd_exit` shall set the value associated with the key to a null pointer value and then invoke the destructor with the previous value of the key. — mcc:✓ gcc:✓ clang:✓ *(provided by `pthread_exit` TSD-destructor machinery.)*
- §7.26.5.5p2 — (C11) The order in which destructors are invoked is unspecified. — (spec; not separately testable)
- §7.26.5.5p3 — (C11) If, after this, there remain keys with both non-null destructors and non-null values, the implementation shall repeat this process up to `TSS_DTOR_ITERATIONS` times. — (spec; runtime; not separately testable)
- [✓] §7.26.5.5p4 — (C11) Following this, `thrd_exit` shall terminate execution of the calling thread and set its result code to `res`. — mcc:✓ gcc:✓ clang:✓ *(verified via join result round-trip.)*
- §7.26.5.5p5 — (C11) The program shall terminate normally after the last thread has been terminated; the behavior shall be as if the program called `exit` with the status `EXIT_SUCCESS` at thread-termination time. — (spec; runtime; not separately testable)
- [✓] §7.26.5.5p6 — (C11) Returns: `thrd_exit` returns no value. — mcc:✓ gcc:✓ clang:✓ *(declared `void`, does not return.)*

##### §7.26.5.6 The `thrd_join` function (C11)
- [✓] §7.26.5.6p1 — (C11) Synopsis: `int thrd_join(thrd_t thr, int *res);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.26.5.6p2 — (C11) `thrd_join` shall join the thread identified by `thr` with the current thread by blocking until the other thread has terminated. — mcc:✓ gcc:✓ clang:✓ *(mcc: `pthread_join`.)*
- [✓] §7.26.5.6p2 — (C11) If `res` is not a null pointer, `thrd_join` shall store the thread's result code in the integer pointed to by `res`. — mcc:✓ gcc:✓ clang:✓ *(verified res=42 stored.)*
- §7.26.5.6p2 — (C11) The termination of the other thread shall synchronize with the completion of `thrd_join`. — (spec; runtime ordering; not separately testable)
- §7.26.5.6p2 — (C11) The thread identified by `thr` shall not have been previously detached or joined with another thread. — (spec; precondition; not separately testable)
- [✓] §7.26.5.6p3 — (C11) Returns: `thrd_join` returns `thrd_success` on success or `thrd_error` if the request could not be honored. — mcc:✓ gcc:✓ clang:✓

##### §7.26.5.7 The `thrd_sleep` function (C11)
- [~] §7.26.5.7p1 — (C11) Synopsis: `int thrd_sleep(const struct timespec *duration, struct timespec *remaining);`. — mcc:~ gcc:✓ clang:✓ *(mcc: `return nanosleep(dur,rem);` — needs `-std=c11` for `struct timespec`; signature matches.)*
- [✓] §7.26.5.7p2 — (C11) `thrd_sleep` shall suspend execution of the calling thread until either the interval specified by `duration` has elapsed or a signal which is not being ignored is received. — mcc:✓ gcc:✓ clang:✓ *(via `nanosleep`.)*
- [✓] §7.26.5.7p3 — (C11) If interrupted by a signal and `remaining` is non-null, the amount of time remaining (the requested interval minus the time actually slept) shall be stored in the interval it points to. — mcc:✓ gcc:✓ clang:✓ *(`nanosleep` populates `rem`.)*
- §7.26.5.7p3 — (C11) The `duration` and `remaining` arguments may point to the same object. — (spec; allowed aliasing; not separately testable)
- §7.26.5.7p4 — (C11) The suspension time may be longer than requested ... but, except for the case of being interrupted by a signal, the suspension time shall not be less than that specified, as measured by the system clock `TIME_UTC`. — (spec; runtime timing; not separately testable)
- [~] §7.26.5.7p5 — (C11) Returns: `thrd_sleep` returns zero if the requested time has elapsed, `-1` if it has been interrupted by a signal, or a negative value if it fails. — mcc:~ gcc:✓ clang:✓ *(NEW divergence: mcc returns `nanosleep`'s raw value — `0` on success, `-1` with `errno` on interrupt/failure — which conflates the "interrupted (-1)" and "other failure (some negative)" distinction the way glibc's own thrd_sleep does; both behave the same here, conforming for the `TIME_UTC` interrupt path.)*

##### §7.26.5.8 The `thrd_yield` function (C11)
- [✓] §7.26.5.8p1 — (C11) Synopsis: `void thrd_yield(void);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.26.5.8p2 — (C11) `thrd_yield` shall endeavor to permit other threads to run, even if the current thread would ordinarily continue to run. — mcc:✓ gcc:✓ clang:✓ *(mcc: `sched_yield`.)*
- [✓] §7.26.5.8p3 — (C11) Returns: `thrd_yield` returns no value. — mcc:✓ gcc:✓ clang:✓ *(declared `void`.)*

#### §7.26.6 Thread-specific storage functions (C11)

##### §7.26.6.1 The `tss_create` function (C11)
- [✓] §7.26.6.1p1 — (C11) Synopsis: `int tss_create(tss_t *key, tss_dtor_t dtor);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.26.6.1p2 — (C11) `tss_create` shall create a thread-specific storage pointer with destructor `dtor`, which may be null. — mcc:✓ gcc:✓ clang:✓ *(mcc: `pthread_key_create`; verified create+set+get+delete.)*
- [✓] §7.26.6.1p3 — (C11) If `tss_create` is successful, it shall set the thread-specific storage pointed to by `key` to a value that uniquely identifies the newly created pointer and return `thrd_success`; otherwise `thrd_error` shall be returned and the thread-specific storage pointed to by `key` shall be set to an undefined value. — mcc:✓ gcc:✓ clang:✓
- §7.26.6.1p3 (footnote) — (C11) A call to `tss_create` from within a thread-specific storage destructor results in undefined behavior. — (spec; UB; not separately testable)
- [✓] §7.26.6.1p4 — (C11) Returns: `tss_create` returns `thrd_success` on success or `thrd_error` if the request could not be honored. — mcc:✓ gcc:✓ clang:✓

##### §7.26.6.2 The `tss_delete` function (C11)
- [✓] §7.26.6.2p1 — (C11) Synopsis: `void tss_delete(tss_t key);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.26.6.2p2 — (C11) `tss_delete` shall release any resources used by the thread-specific storage identified by `key`. — mcc:✓ gcc:✓ clang:✓ *(mcc: `pthread_key_delete`.)*
- §7.26.6.2p2 — (C11) Calling `tss_delete` will not result in the invocation of any destructors, even for thread-specific data created prior to the call. — (spec; runtime; not separately testable)
- [✓] §7.26.6.2p3 — (C11) Returns: `tss_delete` returns no value. — mcc:✓ gcc:✓ clang:✓ *(declared `void`.)*

##### §7.26.6.3 The `tss_get` function (C11)
- [✓] §7.26.6.3p1 — (C11) Synopsis: `void *tss_get(tss_t key);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.26.6.3p2 — (C11) `tss_get` shall return the value for the current thread held in the thread-specific storage identified by `key`. — mcc:✓ gcc:✓ clang:✓ *(mcc: `pthread_getspecific`.)*
- §7.26.6.3p2 (footnote) — (C11) A call to `tss_get` from within a thread-specific storage destructor results in undefined behavior. — (spec; UB; not separately testable)
- [✓] §7.26.6.3p3 — (C11) Returns: `tss_get` returns the value for the current thread if successful, or zero (null) if unsuccessful. — mcc:✓ gcc:✓ clang:✓

##### §7.26.6.4 The `tss_set` function (C11)
- [✓] §7.26.6.4p1 — (C11) Synopsis: `int tss_set(tss_t key, void *val);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.26.6.4p2 — (C11) `tss_set` shall set the value for the current thread held in the thread-specific storage identified by `key` to `val`. — mcc:✓ gcc:✓ clang:✓ *(mcc: `pthread_setspecific`.)*
- §7.26.6.4p2 (footnote) — (C11) A call to `tss_set` from within a thread-specific storage destructor results in undefined behavior. — (spec; UB; not separately testable)
- §7.26.6.4p3 — (C11) `tss_set` shall not invoke the destructor associated with the key on the value being replaced. — (spec; runtime; not separately testable)
- [✓] §7.26.6.4p4 — (C11) Returns: `tss_set` returns `thrd_success` on success or `thrd_error` if the request could not be honored. — mcc:✓ gcc:✓ clang:✓

#### §7.26 — additional/structural items (NEW)
- [✓] §7.26 (link requirement) — All `thrd_*`/`mtx_*`/`cnd_*`/`tss_*`/`call_once` facilities are backed by POSIX threads; programs must link with `-pthread` (or `-lpthread`). — mcc:✓ gcc:✓ clang:✓ *(NEW; identical requirement on all three on this glibc; verified link with `-pthread`.)*
- [✗] §7.26.1p3 (mcc root-cause) — Under mcc, `#include <threads.h>` resolves to the **bundled pthread shim**, not glibc's `<threads.h>`. — mcc:✗ gcc:n/a clang:n/a *(NEW; this is the direct cause of the `thread_local` and `TSS_DTOR_ITERATIONS` failures — gcc/clang always use glibc's header. Confirmed by the `PTHREAD_DESTRUCTOR_ITERATIONS` diagnostic, a shim-only token.)*

### §7.27 Date and time `<time.h>` (C11 §7.27 / C99 §7.23)

> **C99 vs C11.** The body of `<time.h>` is shared. C11 **adds** the `TIME_UTC` macro (§7.27.1p2), the `struct timespec` type (§7.27.1p3), the `timespec_get` function (§7.27.2.5), the data-race wording in §7.27.3p1, and the out-of-range/year UB sentences in `asctime` (§7.27.3.1p2). The `strftime` conversion specifiers `%C %D %e %F %g %G %h %r %R %t %T %u %V %z` and the `E`/`O` modifiers (§7.27.3.5p3–p4) were added in **C99** relative to C90. mcc does not bundle `<time.h>`; all three compilers use the same glibc `<time.h>`, so the only mcc-specific divergence is the default-language-mode gating of the three C11 additions (mcc default = C99; gcc/clang default = C23).

#### §7.27.1 (C99 §7.23.1) Components of time
- §7.27.1p1 (C99 §7.23.1p1) — The header `<time.h>` shall define two macros and declare several types and functions for manipulating time ... *calendar time* / *local time* / *Daylight Saving Time*. — (spec; descriptive; not separately testable)
- [✓] §7.27.1p2 (C99 §7.23.1p2) — The macro `NULL` shall be defined as described in §7.19 (C99 §7.17). — mcc:✓ gcc:✓ clang:✓ *(glibc `<time.h>` pulls `NULL`.)*
- [✓] §7.27.1p2 (C99 §7.23.1p2) — The macro `CLOCKS_PER_SEC` shall expand to an expression with type `clock_t` that is the number per second of the value returned by the `clock` function. — mcc:✓ gcc:✓ clang:✓ *(verified usable in `clock()/CLOCKS_PER_SEC`.)*
- [~] §7.27.1p2 — (C11) The macro `TIME_UTC` shall expand to an integer constant greater than 0 that designates the UTC time base. — mcc:~ gcc:✓ clang:✓ *(verified `TIME_UTC==1` under `-std=c11`. mcc default C99 mode: `'TIME_UTC' undeclared` — gated off by glibc behind `__STDC_VERSION__ >= 201112L`. gcc/clang default C23 → present unconditionally. mcc-gap only at default `-std`.)*
- [✓] §7.27.1p3 (C99 §7.23.1p3) — The type `size_t` shall be the unsigned integer type of the result of `sizeof` (declared as in §7.19). — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.1p3 (C99 §7.23.1p3) — The types `clock_t` and `time_t` shall be real types capable of representing times. — mcc:✓ gcc:✓ clang:✓ *(verified usable as arithmetic types.)*
- [~] §7.27.1p3 — (C11) The type `struct timespec` shall hold an interval specified in seconds and nanoseconds. — mcc:~ gcc:✓ clang:✓ *(present via system `<time.h>`; mcc requires `-std=c11`+. NB inside `<threads.h>` it is visible even in mcc default mode because `<pthread.h>` defines it under its own POSIX feature gate.)*
- [✓] §7.27.1p3 — (C11) `struct timespec` shall contain at least the members `time_t tv_sec;` and `long tv_nsec;` (`[0, 999999999]`), in that order. — mcc:✓ gcc:✓ clang:✓ *(glibc layout; verified `.tv_sec/.tv_nsec` usable under `-std=c11`.)*
- [✓] §7.27.1p3 (C99 §7.23.1p3) — The type `struct tm` shall hold the components of a calendar time, called the broken-down time. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.1p4 (C99 §7.23.1p4) — `struct tm` shall contain at least the members, in any order: `tm_sec [0,60]`, `tm_min [0,59]`, `tm_hour [0,23]`, `tm_mday [1,31]`, `tm_mon [0,11]`, `tm_year` (since 1900), `tm_wday [0,6]`, `tm_yday [0,365]`, `tm_isdst`. — mcc:✓ gcc:✓ clang:✓ *(verified all members assignable + `asctime` round-trip "Wed Jan  1 13:02:03 1997".)*
- §7.27.1p4 (C99 §7.23.1p4) — The value of `tm_isdst` shall be positive if DST is in effect, zero if not, negative if unavailable. — (spec; runtime; not separately testable)
- §7.27.1p4 (C99 §7.23.1p4) — Range note: `tm_sec` allows `[0, 60]` to accommodate a positive leap second (C99/C11; C90 used `[0,61]`). — (spec; descriptive; not separately testable)

#### §7.27.2 (C99 §7.23.2) Time manipulation functions

##### §7.27.2.1 (C99 §7.23.2.1) The `clock` function
- [✓] §7.27.2.1p1 (C99 §7.23.2.1p1) — Synopsis: `#include <time.h>` / `clock_t clock(void);`. — mcc:✓ gcc:✓ clang:✓
- §7.27.2.1p2 (C99 §7.23.2.1p2) — The `clock` function shall determine the processor time used. — (spec; runtime; not separately testable)
- [✓] §7.27.2.1p3 (C99 §7.23.2.1p3) — Returns: best approximation to processor time since an implementation-defined era related only to program invocation. — mcc:✓ gcc:✓ clang:✓ *(verified `clock()` callable.)*
- §7.27.2.1p3 (C99 §7.23.2.1p3) — To determine the time in seconds, divide by `CLOCKS_PER_SEC`. — (spec; descriptive; not separately testable)
- §7.27.2.1p3 (C99 §7.23.2.1p3) — If the processor time used is not available, `clock` returns `(clock_t)(-1)`. — (spec; runtime; not separately testable)
- §7.27.2.1p3 (C99 §7.23.2.1p3) — If the value cannot be represented, the function returns an unspecified value. — (spec; not separately testable)

##### §7.27.2.2 (C99 §7.23.2.2) The `difftime` function
- [✓] §7.27.2.2p1 (C99 §7.23.2.2p1) — Synopsis: `double difftime(time_t time1, time_t time0);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.2.2p2 (C99 §7.23.2.2p2) — Computes `time1 - time0`. — mcc:✓ gcc:✓ clang:✓ *(verified callable.)*
- [✓] §7.27.2.2p3 (C99 §7.23.2.2p3) — Returns: the difference expressed in seconds as a `double`. — mcc:✓ gcc:✓ clang:✓

##### §7.27.2.3 (C99 §7.23.2.3) The `mktime` function
- [✓] §7.27.2.3p1 (C99 §7.23.2.3p1) — Synopsis: `time_t mktime(struct tm *timeptr);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.2.3p2 (C99 §7.23.2.3p2) — Converts the broken-down local time in `*timeptr` into a calendar time with the same encoding as `time`. — mcc:✓ gcc:✓ clang:✓
- §7.27.2.3p2 (C99 §7.23.2.3p2) — The original values of `tm_wday` and `tm_yday` shall be ignored. — (spec; runtime; not separately testable)
- §7.27.2.3p2 (C99 §7.23.2.3p2) — The original values of the other components are not restricted to the ranges in §7.27.1p4. — (spec; runtime; not separately testable)
- §7.27.2.3p2 (C99 §7.23.2.3p2) — On success, `tm_wday`/`tm_yday` set appropriately, other components forced to range; `tm_mday` not set until `tm_mon`/`tm_year` determined. — (spec; runtime; not separately testable)
- [✓] §7.27.2.3p3 (C99 §7.23.2.3p3) — Returns: the calendar time as `time_t`, or `(time_t)(-1)` if it cannot be represented. — mcc:✓ gcc:✓ clang:✓

##### §7.27.2.4 (C99 §7.23.2.4) The `time` function
- [✓] §7.27.2.4p1 (C99 §7.23.2.4p1) — Synopsis: `time_t time(time_t *timer);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.2.4p2 (C99 §7.23.2.4p2) — Determines the current calendar time; encoding unspecified. — mcc:✓ gcc:✓ clang:✓ *(verified callable.)*
- §7.27.2.4p3 (C99 §7.23.2.4p3) — Returns: best approximation to current calendar time; `(time_t)(-1)` if not available. — (spec; runtime; not separately testable)
- [✓] §7.27.2.4p3 (C99 §7.23.2.4p3) — If `timer` is not null, the return value is also assigned to the object it points to. — mcc:✓ gcc:✓ clang:✓

##### §7.27.2.5 The `timespec_get` function (C11)
- [~] §7.27.2.5p1 — (C11) Synopsis: `int timespec_get(struct timespec *ts, int base);`. — mcc:~ gcc:✓ clang:✓ *(under `-std=c11` present+functional — verified `timespec_get(&ts,TIME_UTC)==TIME_UTC`. mcc default C99 mode: implicit-declaration warning for `timespec_get` and `TIME_UTC` undeclared. gcc/clang default C23 → present unconditionally.)*
- [✓] §7.27.2.5p2 — (C11) Sets `*ts` to the current calendar time for the specified time base. — mcc:✓ gcc:✓ clang:✓ *(verified under `-std=c11`.)*
- §7.27.2.5p3 — (C11) If `base` is `TIME_UTC`, `tv_sec` = seconds since an implementation-defined epoch (truncated), `tv_nsec` = nanoseconds rounded to clock resolution. — (spec; runtime; not separately testable)
- §7.27.2.5p3 (footnote) — (C11) Available resolution is system dependent and may even be greater than 1 second. — (spec; not separately testable)
- [✓] §7.27.2.5p4 — (C11) Returns: nonzero value `base` on success, zero otherwise. — mcc:✓ gcc:✓ clang:✓ *(verified returns `TIME_UTC`.)*

#### §7.27.3 (C99 §7.23.3) Time conversion functions
- §7.27.3p1 (C99 §7.23.3p1) — Except `strftime`, these functions return a pointer to one of two static objects: a broken-down time structure or an array of `char`. — (spec; descriptive; not separately testable)
- §7.27.3p1 (C99 §7.23.3p1) — Execution of any of these may overwrite the object of the same type from a previous call. — (spec; runtime; not separately testable)
- §7.27.3p1 — (C11) The implementation shall behave as if no other library functions call these functions. — (spec; C11 data-race wording; not separately testable)

##### §7.27.3.1 (C99 §7.23.3.1) The `asctime` function
- [✓] §7.27.3.1p1 (C99 §7.23.3.1p1) — Synopsis: `char *asctime(const struct tm *timeptr);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.3.1p2 (C99 §7.23.3.1p2) — Converts to the form `Sun Sep 16 01:03:52 1973\n\0` via the specified algorithm. — mcc:✓ gcc:✓ clang:✓ *(verified identical "Wed Jan  1 13:02:03 1997" on all three.)*
- §7.27.3.1p2 — (C11) If any member is outside its normal range, the behavior is undefined. — (spec; UB; not separately testable)
- §7.27.3.1p2 — (C11) If the calculated year exceeds four digits or is less than year 1000, the behavior is undefined. — (spec; UB; not separately testable)
- [✓] §7.27.3.1p3 (C99 §7.23.3.1p3) — Returns: a pointer to the string. — mcc:✓ gcc:✓ clang:✓

##### §7.27.3.2 (C99 §7.23.3.2) The `ctime` function
- [✓] §7.27.3.2p1 (C99 §7.23.3.2p1) — Synopsis: `char *ctime(const time_t *timer);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.3.2p2 (C99 §7.23.3.2p2) — Equivalent to `asctime(localtime(timer))`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.3.2p3 (C99 §7.23.3.2p3) — Returns: the pointer returned by `asctime`. — mcc:✓ gcc:✓ clang:✓

##### §7.27.3.3 (C99 §7.23.3.3) The `gmtime` function
- [✓] §7.27.3.3p1 (C99 §7.23.3.3p1) — Synopsis: `struct tm *gmtime(const time_t *timer);`. — mcc:✓ gcc:✓ clang:✓ *(verified callable.)*
- [✓] §7.27.3.3p2 (C99 §7.23.3.3p2) — Converts calendar time to broken-down time expressed as UTC. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.3.3p3 (C99 §7.23.3.3p3) — Returns: a pointer to the broken-down time, or null if it cannot be converted to UTC. — mcc:✓ gcc:✓ clang:✓

##### §7.27.3.4 (C99 §7.23.3.4) The `localtime` function
- [✓] §7.27.3.4p1 (C99 §7.23.3.4p1) — Synopsis: `struct tm *localtime(const time_t *timer);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.3.4p2 (C99 §7.23.3.4p2) — Converts calendar time to broken-down time expressed as local time. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.3.4p3 (C99 §7.23.3.4p3) — Returns: a pointer to the broken-down time, or null if it cannot be converted to local time. — mcc:✓ gcc:✓ clang:✓

##### §7.27.3.5 (C99 §7.23.3.5) The `strftime` function
- [✓] §7.27.3.5p1 (C99 §7.23.3.5p1) — Synopsis: `size_t strftime(char *restrict s, size_t maxsize, const char *restrict format, const struct tm *restrict timeptr);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.3.5p2 (C99 §7.23.3.5p2) — Places characters into `s` as controlled by `format`. — mcc:✓ gcc:✓ clang:✓
- §7.27.3.5p2 (C99 §7.23.3.5p2) — The format shall be a multibyte character sequence, beginning and ending in its initial shift state. — (spec; descriptive; not separately testable)
- §7.27.3.5p2 (C99 §7.23.3.5p2) — The format string shall consist of zero or more conversion specifiers and ordinary multibyte characters. — (spec; descriptive; not separately testable)
- §7.27.3.5p2 (C99 §7.23.3.5p2) — A conversion specifier: `%`, optionally `E`/`O`, then a behavior character. — (spec; descriptive; not separately testable)
- [✓] §7.27.3.5p2 (C99 §7.23.3.5p2) — All ordinary multibyte characters (including the terminating null) shall be copied unchanged. — mcc:✓ gcc:✓ clang:✓ *(verified literal text + `%%` round-trip.)*
- §7.27.3.5p2 (C99 §7.23.3.5p2) — If copying takes place between objects that overlap, the behavior is undefined. — (spec; UB; not separately testable)
- [✓] §7.27.3.5p2 (C99 §7.23.3.5p2) — No more than `maxsize` characters shall be placed into the array. — mcc:✓ gcc:✓ clang:✓
- §7.27.3.5p3 (C99 §7.23.3.5p3) — Each conversion specifier replaced using `LC_TIME` of the current locale and the bracketed `tm` members. — (spec; descriptive; not separately testable)
- §7.27.3.5p3 (C99 §7.23.3.5p3) — If any specified value is outside the normal range, the characters stored are unspecified. — (spec; unspecified; not separately testable)
- [✓] §7.27.3.5p3 — Conversion specifier `%a` — abbreviated weekday name. `[tm_wday]` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.3.5p3 — Conversion specifier `%A` — full weekday name. `[tm_wday]` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.3.5p3 — Conversion specifier `%b` — abbreviated month name. `[tm_mon]` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.3.5p3 — Conversion specifier `%B` — full month name. `[tm_mon]` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.3.5p3 — Conversion specifier `%c` — locale's date and time representation. `[all]` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.3.5p3 — (C99) Conversion specifier `%C` — year/100 truncated (00–99). `[tm_year]` — mcc:✓ gcc:✓ clang:✓ *(verified `%C`→`19`.)*
- [✓] §7.27.3.5p3 — Conversion specifier `%d` — day of month (01–31). `[tm_mday]` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.3.5p3 — (C99) Conversion specifier `%D` — `"%m/%d/%y"`. `[tm_mon, tm_mday, tm_year]` — mcc:✓ gcc:✓ clang:✓ *(verified `%D`→`01/01/97`.)*
- [✓] §7.27.3.5p3 — (C99) Conversion specifier `%e` — day of month (1–31), single digit preceded by space. `[tm_mday]` — mcc:✓ gcc:✓ clang:✓ *(verified `%e`→`" 1"`.)*
- [✓] §7.27.3.5p3 — (C99) Conversion specifier `%F` — `"%Y-%m-%d"` (ISO 8601 date). `[tm_year, tm_mon, tm_mday]` — mcc:✓ gcc:✓ clang:✓ *(verified `%F`→`1997-01-01`.)*
- [✓] §7.27.3.5p3 — (C99) Conversion specifier `%g` — last 2 digits of week-based year (00–99). `[tm_year, tm_wday, tm_yday]` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.3.5p3 — (C99) Conversion specifier `%G` — week-based year (e.g., 1997). `[tm_year, tm_wday, tm_yday]` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.3.5p3 — (C99) Conversion specifier `%h` — `"%b"`. `[tm_mon]` — mcc:✓ gcc:✓ clang:✓ *(verified `%h`→`Jan`.)*
- [✓] §7.27.3.5p3 — Conversion specifier `%H` — hour, 24-hour (00–23). `[tm_hour]` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.3.5p3 — Conversion specifier `%I` — hour, 12-hour (01–12). `[tm_hour]` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.3.5p3 — Conversion specifier `%j` — day of year (001–366). `[tm_yday]` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.3.5p3 — Conversion specifier `%m` — month (01–12). `[tm_mon]` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.3.5p3 — Conversion specifier `%M` — minute (00–59). `[tm_min]` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.3.5p3 — Conversion specifier `%n` — a new-line character. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.3.5p3 — Conversion specifier `%p` — locale's AM/PM designation. `[tm_hour]` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.3.5p3 — (C99) Conversion specifier `%r` — locale's 12-hour clock time. `[tm_hour, tm_min, tm_sec]` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.3.5p3 — (C99) Conversion specifier `%R` — `"%H:%M"`. `[tm_hour, tm_min]` — mcc:✓ gcc:✓ clang:✓ *(verified `%R`→`13:02`.)*
- [✓] §7.27.3.5p3 — Conversion specifier `%S` — second (00–60). `[tm_sec]` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.3.5p3 — (C99) Conversion specifier `%t` — a horizontal-tab character. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.3.5p3 — (C99) Conversion specifier `%T` — `"%H:%M:%S"` (ISO 8601 time). `[tm_hour, tm_min, tm_sec]` — mcc:✓ gcc:✓ clang:✓ *(verified `%T`→`13:02:03`.)*
- [✓] §7.27.3.5p3 — (C99) Conversion specifier `%u` — ISO 8601 weekday (1–7), Monday=1. `[tm_wday]` — mcc:✓ gcc:✓ clang:✓ *(verified `%u`→`3`.)*
- [✓] §7.27.3.5p3 — Conversion specifier `%U` — week number, first Sunday = week 1 (00–53). `[tm_year, tm_wday, tm_yday]` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.3.5p3 — (C99) Conversion specifier `%V` — ISO 8601 week number (01–53). `[tm_year, tm_wday, tm_yday]` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.3.5p3 — Conversion specifier `%w` — weekday (0–6), Sunday=0. `[tm_wday]` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.3.5p3 — Conversion specifier `%W` — week number, first Monday = week 1 (00–53). `[tm_year, tm_wday, tm_yday]` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.3.5p3 — Conversion specifier `%x` — locale's date representation. `[all]` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.3.5p3 — Conversion specifier `%X` — locale's time representation. `[all]` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.3.5p3 — Conversion specifier `%y` — last 2 digits of year (00–99). `[tm_year]` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.3.5p3 — Conversion specifier `%Y` — the year as a decimal number. `[tm_year]` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.3.5p3 — (C99) Conversion specifier `%z` — offset from UTC in ISO 8601 (`-0430`), or none if undeterminable. `[tm_isdst]` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.3.5p3 — Conversion specifier `%Z` — locale's time zone name/abbreviation, or none. `[tm_isdst]` — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.3.5p3 — Conversion specifier `%%` — a `%` character. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.27.3.5p4 (C99 §7.23.3.5p4) — (C99) Some specifiers can be modified by `E`/`O` to indicate an alternative format. — mcc:✓ gcc:✓ clang:✓ *(runtime libc; identical.)*
- §7.27.3.5p4 (C99 §7.23.3.5p4) — (C99) If the alternative format does not exist for the current locale, the modifier shall be ignored. — (spec; locale-dependent; not separately testable)
- [✓] §7.27.3.5p4 (C99 §7.23.3.5p4) — (C99) `%Ec %EC %Ex %EX %Ey %EY` → locale's alternative representation. — mcc:✓ gcc:✓ clang:✓ *(runtime libc; identical.)*
- [✓] §7.27.3.5p4 (C99 §7.23.3.5p4) — (C99) `%Od %Oe %OH %OI %Om %OM %OS %Ou %OU %OV %Ow %OW %Oy` → locale's alternative numeric symbols. — mcc:✓ gcc:✓ clang:✓ *(runtime libc; identical.)*
- §7.27.3.5p5 (C99 §7.23.3.5p5) — If a conversion specifier is not one of the above, the behavior is undefined. — (spec; UB; not separately testable)
- [✓] §7.27.3.5p6 (C99 §7.23.3.5p6) — Returns: number of characters placed (excluding the null) if total ≤ `maxsize`; otherwise zero and the array contents are indeterminate. — mcc:✓ gcc:✓ clang:✓ *(verified return count + zero-on-overflow behavior identical on all three.)*


## §7.28-§7.31 uchar(C11)/wchar/wctype

> mcc status: 232 TODO checkboxes in this section; **0 mcc-gaps** (mcc rejects the C11 `u`/`U` character-constant and string-literal prefixes under `-std=c99 -pedantic-errors`, matching gcc and clang). Everything else conforms: mcc supplies the compiler-level pieces — the `wchar_t`/`wint_t`/`char16_t`/`char32_t` types, the `u`/`U`/`L` literal prefixes, and the `__STDC_UTF_16__`/`__STDC_UTF_32__` (C11-gated, value `1`), `__STDC_ISO_10646__`, `__CHAR16_TYPE__`/`__CHAR32_TYPE__` predefined macros — and delegates the entire `<wchar.h>`/`<wctype.h>`/`<uchar.h>` *runtime* to the host glibc (the bundled `runtime/include/uchar.h` `include_next`-wraps the system header with a C11 freestanding fallback; `<wchar.h>`/`<wctype.h>` are taken verbatim from glibc). Every function below was compiled, linked, **and run** against glibc through all three compilers with byte-identical output (incl. UTF-16 surrogate-pair `mbrtoc16` returning `(size_t)(-3)`, `c16rtomb`/`c32rtomb`, `wctype`/`iswctype`, `wctrans`/`towctrans`).

> Numbering note: clause numbers below follow **C11 (N1570)**. In C99 (N1256) the same material is numbered **§7.24 `<wchar.h>`** and **§7.25 `<wctype.h>`** (future directions §7.26.12 / §7.26.13). `<uchar.h>` (§7.28) and `char16_t`/`char32_t` are **C11-only** (added by N1570; the functions came via DTR 19769). `<wchar.h>`/`<wctype.h>` were originally added in **C95 (NA1, Amendment 1)**; `iswblank`, `vfwscanf`, `vswscanf`, `vwscanf`, the `%a`/`%A` conversions, the `hh`/`ll`/`j`/`z`/`t` length modifiers, and `WCHAR_MIN`/`WCHAR_MAX` (via `<stdint.h>`) were added in **C99**. Each checkbox is a single normative requirement to verify against mcc / gcc 15.3 / clang 22.

### §7.28.1 `<uchar.h>` — Introduction (C11)

> Entire header, both types, and all four functions are **C11-only**; there is no `<uchar.h>` in C99/C95. mcc bundles `runtime/include/uchar.h`, which `#include_next`-pulls the system `<uchar.h>` when present and otherwise falls back to C11-conforming contents guarded by glibc's `_UCHAR_H` sentinel.

- [✓] §7.28.1p1 — `<uchar.h>` shall declare types and functions for manipulating Unicode characters. (C11) — mcc:✓ gcc:✓ clang:✓
- [✓] §7.28.1p2 — The header shall declare the type `mbstate_t` (described in §7.29.1) and the type `size_t` (described in §7.19). (C11) — mcc:✓ gcc:✓ clang:✓ (bundled header pulls `mbstate_t` from system header, or defines the C11 fallback; `size_t` via `<stddef.h>`)
- [✓] §7.28.1p2 — `char16_t` shall be an unsigned integer type used for 16-bit characters and shall be the same type as `uint_least16_t` (described in §7.20.1.2). (C11) — mcc:✓ gcc:✓ clang:✓ (`__CHAR16_TYPE__`=`unsigned short`, `sizeof==2`; `uint_least16_t*` ↔ `char16_t*` accepted without diagnostic)
- [✓] §7.28.1p2 — `char32_t` shall be an unsigned integer type used for 32-bit characters and shall be the same type as `uint_least32_t` (described in §7.20.1.2). (C11) — mcc:✓ gcc:✓ clang:✓ (`__CHAR32_TYPE__`=`unsigned int`, `sizeof==4`)
- [✓] §6.10.8.2 / §7.28 — If `__STDC_UTF_16__` is defined (the implementation shall define it with value `1` when so), values of type `char16_t` are UTF-16 encoded and `mbrtoc16`/`c16rtomb` perform conversion to/from UTF-16; otherwise the encoding is implementation-defined. (C11) — mcc:✓ gcc:✓ clang:✓ (`__STDC_UTF_16__`==`1`, C11-gated in `mccdefs.h`; surrogate-pair conversion verified at runtime)
- [✓] §6.10.8.2 / §7.28 — If `__STDC_UTF_32__` is defined (the implementation shall define it with value `1` when so), values of type `char32_t` are UTF-32 encoded and `mbrtoc32`/`c32rtomb` perform conversion to/from UTF-32; otherwise the encoding is implementation-defined. (C11) — mcc:✓ gcc:✓ clang:✓ (`__STDC_UTF_32__`==`1`, C11-gated)

#### Added gaps — `<uchar.h>` compiler-level support

- [✓] §6.10.8.2 — `__STDC_UTF_16__`/`__STDC_UTF_32__` shall **not** be predefined outside C11+ mode (no `<uchar.h>` exists in C99). — mcc:✓ gcc:✓ clang:✓ (mcc gates both behind `__STDC_VERSION__ >= 201112L`; absent in mcc's default `-std=c99`, present under `-std=c11`. gcc/clang default to a C11+ dialect so define them by default — same gating, different default dialect)
- [✓] §6.4.4.4/§6.4.5 (under §7.28) — The C11 `u`/`U` prefixes on character constants (`u'A'`, `U'A'`) and string literals (`u"…"`, `U"…"`) — required to make `char16_t`/`char32_t` usable — are a **C11 feature** and a conforming compiler must diagnose them in a strict C99 mode. — mcc:✓ gcc:✓ clang:✓ (under `-std=c99 -pedantic-errors` mcc rejects `u'A'`/`U'A'`/`u"hi"`/`U"hi"` — "the 'u'/'U' character/string prefix is a C11 feature"; gcc and clang reject them as undeclared identifiers / C11-only.) Default and `-std=c11`: all three accept; runtime values byte-identical.
- [✓] §7.28 — `<uchar.h>` shall be includable and its four functions link against the host runtime; the bundled freestanding fallback shall compile under `-nostdinc`. — mcc:✓ gcc:✓ clang:✓

### §7.28.2 Restartable multibyte/wide character conversion functions (C11)

- [✓] §7.28.2p1 — Each function has a parameter `ps` of type pointer to `mbstate_t` pointing to an object that can completely describe the current conversion state of the associated multibyte character sequence; this object is altered as needed to track the shift state and to maintain partial conversion progress between calls. (C11) — mcc:✓ gcc:✓ clang:✓ (glibc runtime; surrogate-pair progress in `*ps` verified)
- [✓] §7.28.2p1 — If `ps` is a null pointer, each function shall use its own internal `mbstate_t` object instead, which is initialized at program startup to the initial conversion state; no other function in the library calls these functions with a null pointer for `ps`. (C11) — mcc:✓ gcc:✓ clang:✓ (glibc)
- §7.28.2p2 — A subsequent call to one of these functions with a `ps` value different from that used in a previous call may produce results different from any consistent sequencing of calls. (C11) — (spec; not separately testable)
- §7.28.2p2 — Changing the `LC_CTYPE` category causes the conversion state of these functions to become indeterminate. (C11) — (spec; not separately testable)
- [✓] §7.28.2p2 — These functions are restartable: a subsequent call takes up the conversion where the prior call left off (using the state stored in `*ps`). (C11) — mcc:✓ gcc:✓ clang:✓ (glibc; verified via surrogate split)

#### §7.28.2.1 The `mbrtoc16` function (C11)

- [✓] §7.28.2.1p1 — Synopsis: `#include <uchar.h>` `size_t mbrtoc16(char16_t * restrict pc16, const char * restrict s, size_t n, mbstate_t * restrict ps);`. (C11) — mcc:✓ gcc:✓ clang:✓ (declared, links, runs)
- [✓] §7.28.2.1p2 — If `s` is a null pointer, the call shall be equivalent to `mbrtoc16(NULL, "", 1, ps)`; in this case the values of `pc16` and `n` are ignored. (C11) — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.28.2.1p3 — If `s` is not a null pointer, `mbrtoc16` shall inspect at most `n` bytes beginning with the byte pointed to by `s` to determine the number of bytes needed to complete the next multibyte character (including any shift sequences). (C11) — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.28.2.1p3 — If the next multibyte character is complete and valid, the function determines the values of the corresponding `char16_t` characters and, if `pc16` is not null, stores the value of the first (or only) such character in `*pc16`. (C11) — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.28.2.1p3 — Subsequent calls shall store successive `char16_t` characters (e.g. the second unit of a UTF-16 surrogate pair) without consuming any additional input, until all the characters corresponding to that multibyte character have been stored. (C11) — mcc:✓ gcc:✓ clang:✓ (U+1F600 → first call `4`/`0xd83d`, second call `(size_t)(-3)`/`0xde00` on all three)
- [✓] §7.28.2.1p3 — If the corresponding wide character is the null character, the resulting conversion state is the initial conversion state. (C11) — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.28.2.1p4 — Returns `0` if the next `n` or fewer bytes complete the multibyte character corresponding to the null wide character (which is the value stored). (C11) — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.28.2.1p4 — Returns a value between `1` and `n` inclusive — the number of bytes that complete a valid multibyte character (whose first/only `char16_t` is the value stored). (C11) — mcc:✓ gcc:✓ clang:✓
- [✓] §7.28.2.1p4 — Returns `(size_t)(-3)` if the next `char16_t` resulting from a previous call has been stored (no bytes from the input have been consumed by this call). (C11) — mcc:✓ gcc:✓ clang:✓ (verified)
- [✓] §7.28.2.1p4 — Returns `(size_t)(-2)` if the next `n` bytes contribute to an incomplete (but potentially valid) multibyte character and all `n` bytes have been processed (no value is stored). (C11) — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.28.2.1p4 — Returns `(size_t)(-1)` if an encoding error occurs (the next `n` or fewer bytes do not contribute to a complete and valid multibyte character); stores `EILSEQ` in `errno`, no value is stored, and the conversion state is unspecified. (C11) — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.28.2.2 The `c16rtomb` function (C11)

- [✓] §7.28.2.2p1 — Synopsis: `size_t c16rtomb(char * restrict s, char16_t c16, mbstate_t * restrict ps);`. (C11) — mcc:✓ gcc:✓ clang:✓ (declared, links, runs)
- [✓] §7.28.2.2p2 — If `s` is a null pointer, the call shall be equivalent to `c16rtomb(buf, L'\0', ps)` where `buf` is an internal buffer. (C11) — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.28.2.2p3 — If `s` is not a null pointer, the function determines the number of bytes needed to represent the multibyte character that corresponds to the wide character given by `c16` (including any shift sequences) and stores the multibyte character representation in the array whose first element is pointed to by `s`; at most `MB_CUR_MAX` bytes are stored. (C11) — mcc:✓ gcc:✓ clang:✓ (`c16rtomb(out,0x42,..)`→`1`/`"B"`)
- [✓] §7.28.2.2p3 — If `c16` is a null wide character, a null byte is stored, preceded by any shift sequence needed to restore the initial shift state, and the resulting conversion state is the initial conversion state. (C11) — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.28.2.2p3 — If `c16` is a high surrogate, the function does not produce output and remembers the value in `*ps`; the following call (with the matching low surrogate) emits the corresponding multibyte character. (C11) — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.28.2.2p4 — Returns the number of bytes stored in the array (including any shift sequences); when `c16` does not correspond to a valid wide character an encoding error occurs: the function stores `EILSEQ` in `errno`, returns `(size_t)(-1)`, and the conversion state is unspecified. (C11) — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.28.2.3 The `mbrtoc32` function (C11)

- [✓] §7.28.2.3p1 — Synopsis: `size_t mbrtoc32(char32_t * restrict pc32, const char * restrict s, size_t n, mbstate_t * restrict ps);`. (C11) — mcc:✓ gcc:✓ clang:✓ (declared, links, runs)
- [✓] §7.28.2.3p2 — If `s` is a null pointer, the call shall be equivalent to `mbrtoc32(NULL, "", 1, ps)`; in this case `pc32` and `n` are ignored. (C11) — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.28.2.3p3 — If `s` is not null, `mbrtoc32` inspects at most `n` bytes from `s` to determine the bytes needed for the next multibyte character; if complete and valid it stores the corresponding `char32_t` value(s) in `*pc32` (first/only one), with subsequent calls storing successive characters without consuming additional input; if the corresponding wide character is the null character, the resulting state is the initial conversion state. (C11) — mcc:✓ gcc:✓ clang:✓ (`mbrtoc32(&c,"A",1,..)`→`1`/`65`)
- [✓] §7.28.2.3p4 — Returns `0`, `1..n`, `(size_t)(-3)`, `(size_t)(-2)`, or `(size_t)(-1)` with `errno=EILSEQ` — same meanings as for `mbrtoc16` (note: for UTF-32, `-3` essentially never occurs since one code point = one `char32_t`). (C11) — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.28.2.4 The `c32rtomb` function (C11)

- [✓] §7.28.2.4p1 — Synopsis: `size_t c32rtomb(char * restrict s, char32_t c32, mbstate_t * restrict ps);`. (C11) — mcc:✓ gcc:✓ clang:✓ (declared, links, runs)
- [✓] §7.28.2.4p2 — If `s` is null, the call shall be equivalent to `c32rtomb(buf, L'\0', ps)` with `buf` an internal buffer. (C11) — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.28.2.4p3 — If `s` is not null, the function stores the multibyte representation of the wide character `c32` (at most `MB_CUR_MAX` bytes, including any shift sequences) into the array pointed to by `s`; a null `c32` stores a null byte preceded by any needed shift sequence and restores the initial conversion state. (C11) — mcc:✓ gcc:✓ clang:✓ (`c32rtomb(out,0x41,..)`→`1`/`"A"`)
- [✓] §7.28.2.4p4 — Returns the number of bytes stored (including shift sequences); on an encoding error (`c32` not corresponding to a valid wide character) it stores `EILSEQ` in `errno`, returns `(size_t)(-1)`, and leaves the conversion state unspecified. (C11) — mcc:✓ gcc:✓ clang:✓ (glibc)

---

### §7.29.1 `<wchar.h>` — Introduction

> C11 §7.29 = C99 §7.24 = original C95 (Amendment 1) material. mcc takes `<wchar.h>` verbatim from the host glibc; it supplies the underlying `__WCHAR_TYPE__`=`int`, `__WINT_TYPE__`=`unsigned int`, and (for `WCHAR_MIN`/`WCHAR_MAX`) the `<stdint.h>` machinery. C99 additions: `vfwscanf`/`vswscanf`/`vwscanf`, `%a`/`%A`, and the `hh`/`ll`/`j`/`z`/`t` length modifiers (tagged inline).

- [✓] §7.29.1p1 — `<wchar.h>` shall declare four types, one tag, several macros, and many functions for extended multibyte and wide character utilities. — mcc:✓ gcc:✓ clang:✓ (header includes & links)
- [✓] §7.29.1p2 — The types declared are `wchar_t` and `size_t` (both described in §7.19). — mcc:✓ gcc:✓ clang:✓ (`sizeof(wchar_t)==4`)
- [✓] §7.29.1p2 — `mbstate_t` shall be an object type other than an array type that can hold the conversion state information necessary to convert between sequences of multibyte characters and wide characters. — mcc:✓ gcc:✓ clang:✓ (`sizeof==8`; assignable `b=a` accepted → not an array type)
- [✓] §7.29.1p2 — `wint_t` shall be an integer type unchanged by the default argument promotions that can hold any value corresponding to members of the extended character set, as well as at least one value (`WEOF`) that does not correspond to any member of the extended character set. — mcc:✓ gcc:✓ clang:✓ (`unsigned int`, 32-bit, unchanged by promotions)
- [✓] §7.29.1p2 — The tag `struct tm` shall be declared as an incomplete type (its contents are described in §7.27.1). — mcc:✓ gcc:✓ clang:✓ (`struct tm *` usable from `<wchar.h>` alone)
- [✓] §7.29.1p3 — The macros defined are `NULL` (described in §7.19); `WCHAR_MIN` and `WCHAR_MAX` (described in §7.20.3); and `WEOF` (described in §7.29.1p4). — mcc:✓ gcc:✓ clang:✓ (`WCHAR_MIN`=`-2147483648`, `WCHAR_MAX`=`2147483647` on all three)
- [✓] §7.29.1p4 — `WEOF` shall expand to a constant expression of type `wint_t` whose value does not correspond to any member of the extended character set; it is accepted (and returned) by several functions in this subclause to indicate end-of-file. — mcc:✓ gcc:✓ clang:✓ (`_Generic(WEOF,…)`→`unsigned int`; value `0xffffffff`)
- [✓] §7.29.1p4 — `WEOF` may have the same value as `EOF` and need not be negative. — mcc:✓ gcc:✓ clang:✓ (here `WEOF`=`0xffffffff`, non-negative `wint_t`)
- §7.29.1p5 — The functions declared are grouped as: formatted wide I/O, wide I/O, general wide string utilities, wide time conversion, and extended multibyte/wide conversion utilities. — (spec; not separately testable)

### §7.29.2 Formatted wide character input/output functions

- [✓] §7.29.2p1 — Whenever a `*` is used in place of a field width or precision, an `int` argument supplies it; a negative field width is treated as a `-` flag followed by a positive field width, and a negative precision is treated as though omitted. — mcc:✓ gcc:✓ clang:✓ (glibc runtime; `swprintf(L"%*d",…)` verified)
- [✓] §7.29.2p2 — The formatted wide-character I/O functions behave as their byte counterparts except that they read/write wide characters and the formats are wide strings; `%c`/`%s` operate per function with the `l` length modifier. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.29.2.1 The `fwprintf` function

- [✓] §7.29.2.1p1 — Synopsis: `int fwprintf(FILE * restrict stream, const wchar_t * restrict format, ...);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.2.1p2 — `fwprintf` writes output under control of `format`; insufficient arguments → UB; excess arguments are evaluated but ignored; returns at end of format. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.2.1p3 — `format` is zero or more directives: ordinary wide characters copied unchanged; and conversion specifications. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.2.1p4 — Each conversion specification is introduced by `%`, then in sequence: flags; optional minimum field width; optional precision; optional length modifier; conversion specifier. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.2.1p5 — The flags `-`, `+`, space, `#`, `0` have the same meanings as for `fprintf`. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.2.1p6 — A nonnegative decimal minimum field width pads the value as for `fprintf`; `*` denotes an `int` argument. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.2.1p7 — An optional precision `.`+integer (or `*`) has the same per-conversion meaning as for `fprintf`. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.2.1p8 — The length modifiers `hh`, `h`, `l`, `ll`, `j`, `z`, `t`, `L` have the same meanings as for `fprintf` (`hh`,`ll`,`j`,`z`,`t` added in C99). — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.2.1p9 — The conversion specifiers `d i o u x X f F e E g G a A p n %` behave as for `fprintf` (`a`/`A` added in C99). — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.2.1p9 — For `c` without `l`, the `int` argument is converted to a wide character as if by `btowc` and written; with `l`, the `wint_t` argument is converted to `wchar_t` and written. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.2.1p9 — For `s` without `l`, the argument is a multibyte-character array, each converted (as if by repeated `mbrtowc`) to a wide character and written up to the null, honoring precision. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.2.1p9 — For `s` with `l`, the argument is a `wchar_t` array, written up to the null, honoring precision. — mcc:✓ gcc:✓ clang:✓ (glibc; `swprintf(L"%ls",L"x")` verified)
- §7.29.2.1p10 — Invalid conversion specification → UB; wrong-type argument → UB. — (spec; not separately testable)
- [✓] §7.29.2.1p11 — A nonexistent or small field width never causes truncation; the field is expanded to contain the result. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.2.1p12 — Returns the number of wide characters transmitted, or a negative value on output/encoding error. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.29.2.2 The `fwscanf` function

- [✓] §7.29.2.2p1 — Synopsis: `int fwscanf(FILE * restrict stream, const wchar_t * restrict format, ...);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.2.2p2 — `fwscanf` reads under control of `format`, using subsequent arguments as pointers; insufficient args → UB; excess args evaluated but ignored. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.2.2p3 — `format` is zero or more directives: white-space wide chars; an ordinary wide char; or a conversion specification. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.2.2p4 — Each conversion specification: `%`, optional `*`, optional nonzero field width, optional length modifier, conversion specifier. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.2.2p5 — A white-space directive reads up to the first non-white-space wide character (left unread), or until no more input. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.2.2p6 — An ordinary-wide-character directive reads the next character; mismatch fails the directive leaving the char unread. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.2.2p7 — A conversion specification skips leading white space (except `[`,`c`,`n`), reads an input item (except `n`), and converts it. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.2.2p8 — The length modifiers `hh h l ll j z t L` have the same meaning as for `fscanf` (`hh`,`ll`,`j`,`z`,`t` added in C99). — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.2.2p9 — The conversion specifiers `d i o u x X a/e/E/f/F/g/G c s [ p n %` behave as for `fscanf` on wide characters (`a`/`A` added in C99). — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.2.2p10 — For `c` without `l`, matched wide chars are converted as if by `wcrtomb` into a `char` array; with `l`, stored as `wchar_t` (no null for `c`). — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.2.2p10 — For `s`/`[` without `l`, converted (as if by `wcrtomb`) into a `char` array + null; with `l`, into a `wchar_t` array + null wide char. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.2.2p11 — Conversion uses `wcrtomb` with an `mbstate_t` reset to the initial state before each converting directive. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.2.2p12 — Trailing white space is left unread unless matched; literal-match/suppressed-assignment success is determinable only via `%n`. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.2.2p13 — Returns `EOF` on input failure before any conversion; otherwise the number of items assigned (possibly fewer/zero on early matching failure). — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.29.2.3 The `swprintf` function

- [✓] §7.29.2.3p1 — Synopsis: `int swprintf(wchar_t * restrict s, size_t n, const wchar_t * restrict format, ...);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.2.3p2 — Equivalent to `fwprintf` but writes into a `wchar_t` array; at most `n` wide chars including the always-appended terminating null (unless `n` is zero). — mcc:✓ gcc:✓ clang:✓ (`swprintf(buf,64,L"%d-%ls",7,L"x")`→`3`/`"7-x"`)
- [✓] §7.29.2.3p3 — Returns the number of wide characters written (excl. null), or a negative value on encoding error or if `n` or more wide chars were requested. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.29.2.4 The `swscanf` function

- [✓] §7.29.2.4p1 — Synopsis: `int swscanf(const wchar_t * restrict s, const wchar_t * restrict format, ...);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.2.4p2 — Equivalent to `fwscanf` but reads from a wide string; reaching the end is equivalent to EOF. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.2.4p3 — Returns the same as `fwscanf`. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.29.2.5 The `vfwprintf` function

- [✓] §7.29.2.5p1 — Synopsis: `#include <stdarg.h>`/`<wchar.h>` `int vfwprintf(FILE * restrict stream, const wchar_t * restrict format, va_list arg);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.2.5p2 — Equivalent to `fwprintf` with the variable arguments replaced by `arg` (initialized by `va_start`); does not invoke `va_end`. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.2.5p3 — Returns the number of wide characters transmitted, or negative on output/encoding error. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.29.2.6 The `vfwscanf` function (C99)

- [✓] §7.29.2.6p1 — Synopsis: `int vfwscanf(FILE * restrict stream, const wchar_t * restrict format, va_list arg);`. (C99) — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.2.6p2 — Equivalent to `fwscanf` with the variable arguments replaced by `arg`; does not invoke `va_end`. (C99) — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.2.6p3 — Returns the same as `fwscanf`. (C99) — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.29.2.7 The `vswprintf` function

- [✓] §7.29.2.7p1 — Synopsis: `int vswprintf(wchar_t * restrict s, size_t n, const wchar_t * restrict format, va_list arg);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.2.7p2 — Equivalent to `swprintf` with variable arguments replaced by `arg`; does not invoke `va_end`. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.2.7p3 — Returns the number of wide characters written (excl. null), or negative on encoding error or if `n` or more requested. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.29.2.8 The `vswscanf` function (C99)

- [✓] §7.29.2.8p1 — Synopsis: `int vswscanf(const wchar_t * restrict s, const wchar_t * restrict format, va_list arg);`. (C99) — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.2.8p2 — Equivalent to `swscanf` with variable arguments replaced by `arg`; does not invoke `va_end`. (C99) — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.2.8p3 — Returns the same as `fwscanf`. (C99) — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.29.2.9 The `vwprintf` function

- [✓] §7.29.2.9p1 — Synopsis: `int vwprintf(const wchar_t * restrict format, va_list arg);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.2.9p2 — Equivalent to `wprintf` with variable arguments replaced by `arg`; does not invoke `va_end`. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.2.9p3 — Returns the number of wide characters transmitted, or negative on output/encoding error. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.29.2.10 The `vwscanf` function (C99)

- [✓] §7.29.2.10p1 — Synopsis: `int vwscanf(const wchar_t * restrict format, va_list arg);`. (C99) — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.2.10p2 — Equivalent to `wscanf` with variable arguments replaced by `arg`; does not invoke `va_end`. (C99) — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.2.10p3 — Returns the same as `fwscanf`. (C99) — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.29.2.11 The `wprintf` function

- [✓] §7.29.2.11p1 — Synopsis: `int wprintf(const wchar_t * restrict format, ...);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.2.11p2 — Equivalent to `fwprintf` with `stdout` interposed. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.2.11p3 — Returns the number of wide characters transmitted, or negative on output/encoding error. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.29.2.12 The `wscanf` function

- [✓] §7.29.2.12p1 — Synopsis: `int wscanf(const wchar_t * restrict format, ...);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.2.12p2 — Equivalent to `fwscanf` with `stdin` interposed. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.2.12p3 — Returns the same as `fwscanf`. — mcc:✓ gcc:✓ clang:✓ (glibc)

### §7.29.3 Wide character input/output functions

#### §7.29.3.1 The `fgetwc` function

- [✓] §7.29.3.1p1 — Synopsis: `wint_t fgetwc(FILE *stream);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.3.1p2 — If not at EOF and a next wide character is present, obtains it (as `wchar_t`→`wint_t`) and advances the file position indicator. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.3.1p3 — Returns the next wide character; on EOF sets the EOF indicator and returns `WEOF`; on read error sets the error indicator and returns `WEOF`; on encoding error sets `errno=EILSEQ` and returns `WEOF`. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.29.3.2 The `fgetws` function

- [✓] §7.29.3.2p1 — Synopsis: `wchar_t *fgetws(wchar_t * restrict s, int n, FILE * restrict stream);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.3.2p2 — Reads at most `n-1` wide chars; stops after a retained newline or at EOF; writes a null wide char after the last read. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.3.2p3 — Returns `s` on success; null pointer if EOF before any read (array unchanged); null pointer on read/encoding error (array indeterminate). — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.29.3.3 The `fputwc` function

- [✓] §7.29.3.3p1 — Synopsis: `wint_t fputwc(wchar_t c, FILE *stream);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.3.3p2 — Writes `c` at the position indicated by the file position indicator (or appends in append mode), advancing the indicator. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.3.3p3 — Returns the wide character written; on write error sets the error indicator and returns `WEOF`; on encoding error sets `errno=EILSEQ` and returns `WEOF`. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.29.3.4 The `fputws` function

- [✓] §7.29.3.4p1 — Synopsis: `int fputws(const wchar_t * restrict s, FILE * restrict stream);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.3.4p2 — Writes the wide string `s`; the terminating null wide character is not written. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.3.4p3 — Returns `EOF` on write/encoding error, otherwise a nonnegative value. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.29.3.5 The `fwide` function

- [✓] §7.29.3.5p1 — Synopsis: `int fwide(FILE *stream, int mode);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.3.5p2 — Determines/sets the orientation: `mode>0` attempts wide, `mode<0` attempts byte, `mode==0` does not alter. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.3.5p2 — In all cases does not change the orientation of a stream that already has one. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.3.5p3 — Returns `>0` if wide-oriented after the call, `<0` if byte-oriented, `0` if no orientation. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.29.3.6 The `getwc` function

- [✓] §7.29.3.6p1 — Synopsis: `wint_t getwc(FILE *stream);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.3.6p2 — Equivalent to `fgetwc`, except it may evaluate `stream` more than once if a macro. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.3.6p3 — Returns the next wide character, or `WEOF`. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.29.3.7 The `getwchar` function

- [✓] §7.29.3.7p1 — Synopsis: `wint_t getwchar(void);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.3.7p2 — Equivalent to `getwc(stdin)`. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.3.7p3 — Returns the next wide character from `stdin`, or `WEOF`. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.29.3.8 The `putwc` function

- [✓] §7.29.3.8p1 — Synopsis: `wint_t putwc(wchar_t c, FILE *stream);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.3.8p2 — Equivalent to `fputwc`, except it may evaluate `stream` more than once if a macro. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.3.8p3 — Returns the wide character written, or `WEOF`. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.29.3.9 The `putwchar` function

- [✓] §7.29.3.9p1 — Synopsis: `wint_t putwchar(wchar_t c);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.3.9p2 — Equivalent to `putwc(c, stdout)`. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.3.9p3 — Returns the wide character written, or `WEOF`. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.29.3.10 The `ungetwc` function

- [✓] §7.29.3.10p1 — Synopsis: `wint_t ungetwc(wint_t c, FILE *stream);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.3.10p2 — Pushes `c` (converted to `wchar_t`) back; subsequent reads return pushed-back chars in reverse; a successful intervening file-positioning call discards them. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.3.10p2 — One wide character of pushback is guaranteed; too many calls without an intervening read/position may fail. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.3.10p3 — If `c` equals `WEOF`, the operation fails and the stream is unchanged. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.3.10p4 — A successful call clears the EOF indicator; the file position after reading/discarding pushed-back chars equals its prior value; for text/binary streams the indicator is decremented per successful call (indeterminate if it was zero). — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.3.10p5 — Returns the wide character pushed back (after conversion), or `WEOF` on failure. — mcc:✓ gcc:✓ clang:✓ (glibc)

### §7.29.4 General wide string utilities

- §7.29.4p1 — `<wchar.h>` declares wide-string functions; a `wchar_t *`/`const wchar_t *` argument points to the initial element of an array; accessing beyond the end is UB. — (spec; not separately testable)

#### §7.29.4.1 Wide string numeric conversion functions

##### §7.29.4.1.1 The `wcstod`, `wcstof`, `wcstold` functions

- [✓] §7.29.4.1.1p1 — Synopsis: `double wcstod(...);` `float wcstof(...);` `long double wcstold(...);` (`wcstof`/`wcstold` added in C99). — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.4.1.1p2 — Convert the initial portion of `nptr` to `double`/`float`/`long double`; decompose into white space, subject sequence, and final string. — mcc:✓ gcc:✓ clang:✓ (`wcstod(L"3.14xyz",0)`→`3.14`)
- [✓] §7.29.4.1.1p3 — The expected subject form is the same as for `strtod`, using wide characters (sign; decimal/hex significand with optional radix and exponent; or `INF`/`INFINITY`/`NAN`). — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.4.1.1p4 — Convert the longest matching initial subsequence; store the final-string pointer in `*endptr`; on no conversion store `nptr` in `*endptr`. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.4.1.1p5 — Hex digits and the radix character behave as for `strtod`, rounded per the current rounding direction. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.4.1.1p6 — Returns the converted value; `0` if none; `±HUGE_VAL*` with `errno=ERANGE` on overflow; an underflow value with implementation-defined `ERANGE`. — mcc:✓ gcc:✓ clang:✓ (glibc)

##### §7.29.4.1.2 The `wcstol`, `wcstoll`, `wcstoul`, `wcstoull` functions

- [✓] §7.29.4.1.2p1 — Synopsis: `long int wcstol(...);` `long long int wcstoll(...);` `unsigned long int wcstoul(...);` `unsigned long long int wcstoull(...);` (`wcstoll`/`wcstoull` added in C99). — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.4.1.2p2 — Convert the initial portion of `nptr` to the respective integer type; decompose into white space, subject sequence in radix `base`, and final string. — mcc:✓ gcc:✓ clang:✓ (`wcstol(L"  -42",0,10)`→`-42`)
- [✓] §7.29.4.1.2p3 — The expected subject form is the same as for `strtol`/`strtoul`; `base` shall be 0 or 2..36. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.4.1.2p4 — If `base` is zero, the form is an integer constant (decimal/octal/hex); the subject sequence is the longest matching initial subsequence. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.4.1.2p5 — Store the final-string pointer in `*endptr`; on no conversion store `nptr`. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.4.1.2p6 — Returns the converted value (unsigned functions negate a signed sequence); `0` if none; range limit (`LONG_MIN`…`ULLONG_MAX`) with `errno=ERANGE` on out-of-range. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.29.4.2 Wide string copying functions

##### §7.29.4.2.1 The `wcscpy` function

- [✓] §7.29.4.2.1p1 — Synopsis: `wchar_t *wcscpy(wchar_t * restrict s1, const wchar_t * restrict s2);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.4.2.1p2 — Copies `s2` (including the null) into `s1`. — mcc:✓ gcc:✓ clang:✓ (`wcscpy(buf,L"Hello")` verified)
- [✓] §7.29.4.2.1p3 — Returns `s1`. — mcc:✓ gcc:✓ clang:✓ (glibc)

##### §7.29.4.2.2 The `wcsncpy` function

- [✓] §7.29.4.2.2p1 — Synopsis: `wchar_t *wcsncpy(wchar_t * restrict s1, const wchar_t * restrict s2, size_t n);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.4.2.2p2 — Copies at most `n` wide chars (none after a null); overlapping objects → UB. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.4.2.2p3 — If `s2` is shorter than `n`, null wide chars are appended until `n` are written. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.4.2.2p4 — Returns `s1` (result need not be null-terminated). — mcc:✓ gcc:✓ clang:✓ (glibc)

##### §7.29.4.2.3 The `wmemcpy` function

- [✓] §7.29.4.2.3p1 — Synopsis: `wchar_t *wmemcpy(wchar_t * restrict s1, const wchar_t * restrict s2, size_t n);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.4.2.3p2 — Copies `n` wide chars; overlapping objects → UB. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.4.2.3p3 — Returns `s1`. — mcc:✓ gcc:✓ clang:✓ (glibc)

##### §7.29.4.2.4 The `wmemmove` function

- [✓] §7.29.4.2.4p1 — Synopsis: `wchar_t *wmemmove(wchar_t *s1, const wchar_t *s2, size_t n);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.4.2.4p2 — Copies `n` wide chars as if via a non-overlapping temporary (overlap handled correctly). — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.4.2.4p3 — Returns `s1`. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.29.4.3 Wide string concatenation functions

##### §7.29.4.3.1 The `wcscat` function

- [✓] §7.29.4.3.1p1 — Synopsis: `wchar_t *wcscat(wchar_t * restrict s1, const wchar_t * restrict s2);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.4.3.1p2 — Appends a copy of `s2` (including the null) to the end of `s1`; overlap → UB. — mcc:✓ gcc:✓ clang:✓ (`wcscat(buf,L" World")`→`"Hello World"`)
- [✓] §7.29.4.3.1p3 — Returns `s1`. — mcc:✓ gcc:✓ clang:✓ (glibc)

##### §7.29.4.3.2 The `wcsncat` function

- [✓] §7.29.4.3.2p1 — Synopsis: `wchar_t *wcsncat(wchar_t * restrict s1, const wchar_t * restrict s2, size_t n);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.4.3.2p2 — Appends at most `n` wide chars (stopping at a null); always appends a terminating null; overlap → UB. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.4.3.2p3 — Returns `s1`. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.29.4.4 Wide string comparison functions

- §7.29.4.4p1 — Unless stated otherwise, these functions compare wide characters as values of type `wchar_t`. — (spec; not separately testable)

##### §7.29.4.4.1 The `wcscmp` function

- [✓] §7.29.4.4.1p1 — Synopsis: `int wcscmp(const wchar_t *s1, const wchar_t *s2);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.4.4.1p2 — Compares `s1` to `s2`. — mcc:✓ gcc:✓ clang:✓ (`wcscmp(L"Hello World",L"Hello World")`→`0`)
- [✓] §7.29.4.4.1p3 — Returns `>0`/`0`/`<0` as `s1` is greater/equal/less. — mcc:✓ gcc:✓ clang:✓ (glibc)

##### §7.29.4.4.2 The `wcscoll` function

- [✓] §7.29.4.4.2p1 — Synopsis: `int wcscoll(const wchar_t *s1, const wchar_t *s2);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.4.4.2p2 — Compares `s1` to `s2`, both interpreted per the `LC_COLLATE` category. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.4.4.2p3 — Returns `>0`/`0`/`<0` per the locale interpretation. — mcc:✓ gcc:✓ clang:✓ (glibc)

##### §7.29.4.4.3 The `wcsncmp` function

- [✓] §7.29.4.4.3p1 — Synopsis: `int wcsncmp(const wchar_t *s1, const wchar_t *s2, size_t n);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.4.4.3p2 — Compares at most `n` wide chars (none after a null). — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.4.4.3p3 — Returns `>0`/`0`/`<0` accordingly. — mcc:✓ gcc:✓ clang:✓ (glibc)

##### §7.29.4.4.4 The `wcsxfrm` function

- [✓] §7.29.4.4.4p1 — Synopsis: `size_t wcsxfrm(wchar_t * restrict s1, const wchar_t * restrict s2, size_t n);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.4.4.4p2 — Transforms `s2` into `s1` so that `wcscmp` of two transformed strings matches `wcscoll` of the originals; at most `n` wide chars (incl. null) are placed. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.4.4.4p3 — Returns the transformed length (excl. null); if `≥ n`, `s1` contents are indeterminate. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.4.4.4p3 — If `n` is zero, `s1` may be a null pointer. — mcc:✓ gcc:✓ clang:✓ (glibc)

##### §7.29.4.4.5 The `wmemcmp` function

- [✓] §7.29.4.4.5p1 — Synopsis: `int wmemcmp(const wchar_t *s1, const wchar_t *s2, size_t n);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.4.4.5p2 — Compares the first `n` wide chars of `s1` and `s2`. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.4.4.5p3 — Returns `>0`/`0`/`<0` accordingly. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.29.4.5 Wide string search functions

##### §7.29.4.5.1 The `wcschr` function

- [✓] §7.29.4.5.1p1 — Synopsis: `wchar_t *wcschr(const wchar_t *s, wchar_t c);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.4.5.1p2 — Locates the first occurrence of `c` in `s`; the terminating null is part of the string. — mcc:✓ gcc:✓ clang:✓ (`wcschr(L"abcd",L'c')`→`L"cd"`)
- [✓] §7.29.4.5.1p3 — Returns a pointer to the located wide char, or null. — mcc:✓ gcc:✓ clang:✓ (glibc)

##### §7.29.4.5.2 The `wcscspn` function

- [✓] §7.29.4.5.2p1 — Synopsis: `size_t wcscspn(const wchar_t *s1, const wchar_t *s2);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.4.5.2p2 — Computes the length of the max initial segment of `s1` with no wide char from `s2`. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.4.5.2p3 — Returns the segment length. — mcc:✓ gcc:✓ clang:✓ (glibc)

##### §7.29.4.5.3 The `wcspbrk` function

- [✓] §7.29.4.5.3p1 — Synopsis: `wchar_t *wcspbrk(const wchar_t *s1, const wchar_t *s2);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.4.5.3p2 — Locates the first occurrence in `s1` of any wide char from `s2`. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.4.5.3p3 — Returns a pointer to the wide char, or null. — mcc:✓ gcc:✓ clang:✓ (glibc)

##### §7.29.4.5.4 The `wcsrchr` function

- [✓] §7.29.4.5.4p1 — Synopsis: `wchar_t *wcsrchr(const wchar_t *s, wchar_t c);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.4.5.4p2 — Locates the last occurrence of `c` in `s`; the null is part of the string. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.4.5.4p3 — Returns a pointer to the wide char, or null. — mcc:✓ gcc:✓ clang:✓ (glibc)

##### §7.29.4.5.5 The `wcsspn` function

- [✓] §7.29.4.5.5p1 — Synopsis: `size_t wcsspn(const wchar_t *s1, const wchar_t *s2);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.4.5.5p2 — Computes the length of the max initial segment of `s1` consisting entirely of wide chars from `s2`. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.4.5.5p3 — Returns the segment length. — mcc:✓ gcc:✓ clang:✓ (glibc)

##### §7.29.4.5.6 The `wcsstr` function

- [✓] §7.29.4.5.6p1 — Synopsis: `wchar_t *wcsstr(const wchar_t *s1, const wchar_t *s2);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.4.5.6p2 — Locates the first occurrence in `s1` of the wide-char sequence (excl. null) in `s2`. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.4.5.6p3 — Returns a pointer to the located substring, or null; a zero-length `s2` returns `s1`. — mcc:✓ gcc:✓ clang:✓ (glibc)

##### §7.29.4.5.7 The `wcstok` function

- [✓] §7.29.4.5.7p1 — Synopsis: `wchar_t *wcstok(wchar_t * restrict s1, const wchar_t * restrict s2, wchar_t ** restrict ptr);`. — mcc:✓ gcc:✓ clang:✓ (note: the C `wcstok` is 3-argument/reentrant, unlike 2-argument `strtok`)
- [✓] §7.29.4.5.7p2 — Breaks `s1` into tokens delimited by chars from `s2`; `ptr` points to a caller-provided `wchar_t*` for continuation state. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.4.5.7p3 — The first call has non-null `s1` and seeds `*ptr`; subsequent calls pass null `s1` and the prior `*ptr`; `s2` may differ per call. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.4.5.7p4 — The first call searches for the first non-separator char; if none, returns null (and writes null through `ptr`). — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.4.5.7p4 — Otherwise marks the token start, searches for a separator; if none the token runs to the end; if found, overwrites it with a null and saves the next-char pointer in `*ptr`. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.4.5.7p5 — Returns a pointer to the first wide char of a token, or null. — mcc:✓ gcc:✓ clang:✓ (glibc)

##### §7.29.4.5.8 The `wmemchr` function

- [✓] §7.29.4.5.8p1 — Synopsis: `wchar_t *wmemchr(const wchar_t *s, wchar_t c, size_t n);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.4.5.8p2 — Locates the first occurrence of `c` in the initial `n` wide chars of `s`. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.4.5.8p3 — Returns a pointer to the located wide char, or null. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.29.4.6 Miscellaneous functions

##### §7.29.4.6.1 The `wcslen` function

- [✓] §7.29.4.6.1p1 — Synopsis: `size_t wcslen(const wchar_t *s);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.4.6.1p2 — Computes the length of `s`. — mcc:✓ gcc:✓ clang:✓ (`wcslen(L"Hello World")`→`11`)
- [✓] §7.29.4.6.1p3 — Returns the number of wide chars before the null. — mcc:✓ gcc:✓ clang:✓ (glibc)

##### §7.29.4.6.2 The `wmemset` function

- [✓] §7.29.4.6.2p1 — Synopsis: `wchar_t *wmemset(wchar_t *s, wchar_t c, size_t n);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.4.6.2p2 — Copies `c` into each of the first `n` wide chars of `s`. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.4.6.2p3 — Returns `s`. — mcc:✓ gcc:✓ clang:✓ (glibc)

### §7.29.5 Wide character time conversion functions

#### §7.29.5.1 The `wcsftime` function

- [✓] §7.29.5.1p1 — Synopsis: `size_t wcsftime(wchar_t * restrict s, size_t maxsize, const wchar_t * restrict format, const struct tm * restrict timeptr);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.5.1p2 — Equivalent to `strftime` but writes into a `wchar_t` array, with `maxsize` wide chars, a wide `format`, and `LC_TIME`-appropriate wide sequences. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.5.1p3 — Returns the count of wide chars (excl. null) if `≤ maxsize`; otherwise `0` and the array is indeterminate. — mcc:✓ gcc:✓ clang:✓ (glibc)

### §7.29.6 Extended multibyte/wide character conversion utilities

- §7.29.6p1 — `<wchar.h>` declares extended conversion functions whose behavior is affected by `LC_CTYPE`. — (spec; not separately testable)
- [✓] §7.29.6p2 — For the restartable functions of §7.29.6.3/§7.29.6.4, a `ps` of null uses an internal `mbstate_t`; no library function calls these with a null `ps`. — mcc:✓ gcc:✓ clang:✓ (glibc)
- §7.29.6p3 — Changing `LC_CTYPE` makes the restartable functions' conversion state indeterminate. — (spec; not separately testable)

#### §7.29.6.1 Single-byte/wide character conversion functions

##### §7.29.6.1.1 The `btowc` function

- [✓] §7.29.6.1.1p1 — Synopsis: `wint_t btowc(int c);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.6.1.1p2 — Determines whether `c` is a valid single-byte character in the initial shift state. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.6.1.1p3 — Returns `WEOF` for `EOF` or an invalid single-byte char; otherwise the wide-character representation. — mcc:✓ gcc:✓ clang:✓ (`btowc('A')`→`65`)

##### §7.29.6.1.2 The `wctob` function

- [✓] §7.29.6.1.2p1 — Synopsis: `int wctob(wint_t c);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.6.1.2p2 — Determines whether `c` maps to a single-byte multibyte character in the initial shift state. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.6.1.2p3 — Returns `EOF` if not single-byte; otherwise the `unsigned char`→`int` single-byte representation. — mcc:✓ gcc:✓ clang:✓ (`wctob(L'A')`→`65`)

#### §7.29.6.2 Conversion state functions

##### §7.29.6.2.1 The `mbsinit` function

- [✓] §7.29.6.2.1p1 — Synopsis: `int mbsinit(const mbstate_t *ps);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.6.2.1p2 — If `ps` is not null, determines whether `*ps` describes an initial conversion state. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.6.2.1p3 — Returns nonzero if `ps` is null or `*ps` is the initial state; otherwise zero. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.29.6.3 Restartable multibyte/wide character conversion functions

- §7.29.6.3p1 — These differ from `mbtowc`/`wctomb`/`mblen` by an extra `ps` parameter and not assuming null-terminated input; on entry `*ps` describes the current state. — (spec; not separately testable)

##### §7.29.6.3.1 The `mbrlen` function

- [✓] §7.29.6.3.1p1 — Synopsis: `size_t mbrlen(const char * restrict s, size_t n, mbstate_t * restrict ps);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.6.3.1p2 — Equivalent to `mbrtowc(NULL, s, n, ps?:&internal)` except `ps` is evaluated once. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.6.3.1p3 — Returns `0..n`, `(size_t)(-1)`, or `(size_t)(-2)` per `mbrtowc` semantics. — mcc:✓ gcc:✓ clang:✓ (glibc)

##### §7.29.6.3.2 The `mbrtowc` function

- [✓] §7.29.6.3.2p1 — Synopsis: `size_t mbrtowc(wchar_t * restrict pwc, const char * restrict s, size_t n, mbstate_t * restrict ps);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.6.3.2p2 — If `s` is null, equivalent to `mbrtowc(NULL, "", 1, ps)`; `pwc`/`n` ignored. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.6.3.2p3 — If `s` is not null, inspect at most `n` bytes; on a complete valid char store the wide char in `*pwc` (if non-null); a null wide char yields the initial state. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.6.3.2p4 — Returns `0` when the bytes complete the null wide char. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.6.3.2p4 — Returns `1..n` — the byte count completing a valid char (whose value is stored). — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.6.3.2p4 — Returns `(size_t)(-2)` for an incomplete char with all `n` bytes processed (no value stored). — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.6.3.2p4 — Returns `(size_t)(-1)` on encoding error; `errno=EILSEQ`, state unspecified, no value stored. — mcc:✓ gcc:✓ clang:✓ (glibc)

##### §7.29.6.3.3 The `wcrtomb` function

- [✓] §7.29.6.3.3p1 — Synopsis: `size_t wcrtomb(char * restrict s, wchar_t wc, mbstate_t * restrict ps);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.6.3.3p2 — If `s` is null, equivalent to `wcrtomb(buf, L'\0', ps)`. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.6.3.3p3 — If `s` is not null, store the multibyte representation of `wc` (≤ `MB_CUR_MAX` bytes); a null `wc` stores a null byte preceded by any shift sequence and yields the initial state. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.6.3.3p4 — Returns the byte count stored; on an invalid `wc` stores `EILSEQ` and returns `(size_t)(-1)`, state unspecified. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.29.6.4 Restartable multibyte/wide string conversion functions

- §7.29.6.4p1 — These differ from `mbstowcs`/`wcstombs` by an extra `ps`, storing a result pointer back through the source pointer-to-pointer, and handling encoding-error termination. — (spec; not separately testable)

##### §7.29.6.4.1 The `mbsrtowcs` function

- [✓] §7.29.6.4.1p1 — Synopsis: `size_t mbsrtowcs(wchar_t * restrict dst, const char ** restrict src, size_t len, mbstate_t * restrict ps);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.6.4.1p2 — Converts a multibyte sequence from `*src` (state `*ps`) to wide chars in `dst` (if non-null), up to and including a terminating null. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.6.4.1p2 — Stops early on an invalid sequence, or (if `dst` non-null) after `len` wide chars; each conversion is as if by `mbrtowc`. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.6.4.1p3 — If `dst` is non-null, `*src` is set to null (terminating null reached) or just past the last char converted; reaching the null yields the initial state. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.6.4.1p4 — Returns the count of multibyte chars converted (excl. null); on an invalid char returns `(size_t)(-1)`, `errno=EILSEQ`, state unspecified. — mcc:✓ gcc:✓ clang:✓ (glibc)

##### §7.29.6.4.2 The `wcsrtombs` function

- [✓] §7.29.6.4.2p1 — Synopsis: `size_t wcsrtombs(char * restrict dst, const wchar_t ** restrict src, size_t len, mbstate_t * restrict ps);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.29.6.4.2p2 — Converts a wide sequence from `*src` (state `*ps`) to multibyte chars in `dst` (if non-null), up to and including a terminating null (after any shift sequence). — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.6.4.2p2 — Stops early on a wide char with no valid multibyte char, or (if `dst` non-null) when the next char would exceed `len` bytes; each conversion is as if by `wcrtomb`. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.6.4.2p3 — If `dst` is non-null, `*src` is set to null (terminating null reached) or just past the last char converted; reaching the null yields the initial state. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.29.6.4.2p4 — Returns the resulting byte count (excl. null); on an invalid wide char returns `(size_t)(-1)`, `errno=EILSEQ`, state unspecified. — mcc:✓ gcc:✓ clang:✓ (glibc)

---

### §7.30.1 `<wctype.h>` — Introduction

> C11 §7.30 = C99 §7.25 = original C95 (Amendment 1) material. mcc takes `<wctype.h>` verbatim from the host glibc. The only C99 addition to this header is `iswblank` (§7.30.2.1.3).

- [✓] §7.30.1p1 — `<wctype.h>` shall declare three data types, one macro, and many wide-char classification/mapping functions. — mcc:✓ gcc:✓ clang:✓ (header includes & links)
- [✓] §7.30.1p2 — The three types are `wint_t`; `wctrans_t` (a scalar type holding locale-specific mappings); and `wctype_t` (a scalar type holding locale-specific classifications). — mcc:✓ gcc:✓ clang:✓ (`wctype_t`/`wctrans_t` used as scalars)
- [✓] §7.30.1p3 — The macro defined is `WEOF`. — mcc:✓ gcc:✓ clang:✓ (also visible via `<wctype.h>` alone)
- §7.30.1p4 — Wide-char classification/mapping behavior is affected by `LC_CTYPE`. — (spec; not separately testable)
- §7.30.1p4 — The functions accept any value representable as `wchar_t` or equal to `WEOF`; any other value → UB. — (spec; not separately testable)

### §7.30.2 Wide character classification utilities

#### §7.30.2.1 Wide character classification functions

- [✓] §7.30.2.1p1 — The functions test for membership in overlapping classes defined for the `"C"` locale, paralleling the narrow `<ctype.h>` classes. — mcc:✓ gcc:✓ clang:✓ (glibc; consistent with §7.4)
- [✓] §7.30.2.1p2 — In the `"C"` locale, `iswprint` is true exactly for the chars where `iswalnum`/`iswpunct`/the wide space hold, with membership relationships paralleling §7.4. — mcc:✓ gcc:✓ clang:✓ (glibc)

##### §7.30.2.1.1 The `iswalnum` function

- [✓] §7.30.2.1.1p1 — Synopsis: `int iswalnum(wint_t wc);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.30.2.1.1p2 — Tests for any wide char where `iswalpha` or `iswdigit` is true. — mcc:✓ gcc:✓ clang:✓ (glibc)

##### §7.30.2.1.2 The `iswalpha` function

- [✓] §7.30.2.1.2p1 — Synopsis: `int iswalpha(wint_t wc);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.30.2.1.2p2 — Tests for `iswupper`/`iswlower` chars or an implementation-defined set with none of `iswcntrl`/`iswdigit`/`iswpunct`/`iswspace`. — mcc:✓ gcc:✓ clang:✓ (`iswalpha(L'a')` true on all three)
- [✓] §7.30.2.1.2p2 — In the `"C"` locale, true only for `iswupper`/`iswlower` chars. — mcc:✓ gcc:✓ clang:✓ (glibc)

##### §7.30.2.1.3 The `iswblank` function (C99)

- [✓] §7.30.2.1.3p1 — Synopsis: `int iswblank(wint_t wc);`. (C99) — mcc:✓ gcc:✓ clang:✓
- [✓] §7.30.2.1.3p2 — Tests for a standard blank wide char (or an implementation-defined `iswspace` word-separator); in `"C"`, true only for space and tab. (C99) — mcc:✓ gcc:✓ clang:✓ (`iswblank(L'\t')`→true)

##### §7.30.2.1.4 The `iswcntrl` function

- [✓] §7.30.2.1.4p1 — Synopsis: `int iswcntrl(wint_t wc);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.30.2.1.4p2 — Tests for any control wide character. — mcc:✓ gcc:✓ clang:✓ (glibc)

##### §7.30.2.1.5 The `iswdigit` function

- [✓] §7.30.2.1.5p1 — Synopsis: `int iswdigit(wint_t wc);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.30.2.1.5p2 — Tests for a decimal-digit character (per §5.2.1). — mcc:✓ gcc:✓ clang:✓ (`iswdigit(L'1')`→true)

##### §7.30.2.1.6 The `iswgraph` function

- [✓] §7.30.2.1.6p1 — Synopsis: `int iswgraph(wint_t wc);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.30.2.1.6p2 — Tests for any printing wide char except space (`iswprint` true, `iswspace` false). — mcc:✓ gcc:✓ clang:✓ (glibc)

##### §7.30.2.1.7 The `iswlower` function

- [✓] §7.30.2.1.7p1 — Synopsis: `int iswlower(wint_t wc);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.30.2.1.7p2 — Tests for a lowercase letter or an implementation-defined set with none of `iswcntrl`/`iswdigit`/`iswpunct`/`iswspace`. — mcc:✓ gcc:✓ clang:✓ (glibc)

##### §7.30.2.1.8 The `iswprint` function

- [✓] §7.30.2.1.8p1 — Synopsis: `int iswprint(wint_t wc);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.30.2.1.8p2 — Tests for any printing wide char (including space). — mcc:✓ gcc:✓ clang:✓ (glibc)

##### §7.30.2.1.9 The `iswpunct` function

- [✓] §7.30.2.1.9p1 — Synopsis: `int iswpunct(wint_t wc);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.30.2.1.9p2 — Tests for a printing wide char in an implementation-defined set with neither `iswspace` nor `iswalnum`. — mcc:✓ gcc:✓ clang:✓ (glibc)

##### §7.30.2.1.10 The `iswspace` function

- [✓] §7.30.2.1.10p1 — Synopsis: `int iswspace(wint_t wc);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.30.2.1.10p2 — Tests for a locale-specific white-space wide char with none of `iswalnum`/`iswgraph`/`iswpunct`. — mcc:✓ gcc:✓ clang:✓ (`iswspace(L' ')`→true)

##### §7.30.2.1.11 The `iswupper` function

- [✓] §7.30.2.1.11p1 — Synopsis: `int iswupper(wint_t wc);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.30.2.1.11p2 — Tests for an uppercase letter or an implementation-defined set with none of `iswcntrl`/`iswdigit`/`iswpunct`/`iswspace`. — mcc:✓ gcc:✓ clang:✓ (`iswupper(L'A')`→true)

##### §7.30.2.1.12 The `iswxdigit` function

- [✓] §7.30.2.1.12p1 — Synopsis: `int iswxdigit(wint_t wc);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.30.2.1.12p2 — Tests for a hexadecimal-digit character (per §6.4.4.1). — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.30.2.2 Extensible wide character classification functions

- [✓] §7.30.2.2p1 — `wctype`/`iswctype` provide extensible classification and a common interface to the §7.30.2.1 functions. — mcc:✓ gcc:✓ clang:✓ (glibc)

##### §7.30.2.2.1 The `wctype` function

- [✓] §7.30.2.2.1p1 — Synopsis: `wctype_t wctype(const char *property);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.30.2.2.1p2 — Constructs a `wctype_t` for the class named by `property`; the §7.30.2.2.2 strings are valid in all locales. — mcc:✓ gcc:✓ clang:✓ (`wctype("alpha")` verified)
- [✓] §7.30.2.2.1p3 — Returns a nonzero `wctype_t` valid for `iswctype` if `property` names a valid class; otherwise zero. — mcc:✓ gcc:✓ clang:✓ (glibc)

##### §7.30.2.2.2 The `iswctype` function

- [✓] §7.30.2.2.2p1 — Synopsis: `int iswctype(wint_t wc, wctype_t desc);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.30.2.2.2p2 — Determines whether `wc` has the property `desc`; `LC_CTYPE` must match the `wctype` call that produced `desc`. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.30.2.2.2p3 — `iswctype(wc, wctype("X"))` is equivalent to the corresponding §7.30.2.1 call for each of `alnum alpha blank cntrl digit graph lower print punct space upper xdigit`. — mcc:✓ gcc:✓ clang:✓ (`iswctype(L'q',wctype("alpha"))`→`1`)
- [✓] §7.30.2.2.2p4 — Returns nonzero iff `wc` has the property `desc`; `desc==0` → UB. — mcc:✓ gcc:✓ clang:✓ (glibc)

### §7.30.3 Wide character case mapping utilities

#### §7.30.3.1 Wide character case mapping functions

##### §7.30.3.1.1 The `towlower` function

- [✓] §7.30.3.1.1p1 — Synopsis: `wint_t towlower(wint_t wc);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.30.3.1.1p2 — Converts an uppercase letter to the corresponding lowercase letter. — mcc:✓ gcc:✓ clang:✓ (`towlower(L'Z')`→`z`)
- [✓] §7.30.3.1.1p3 — If `iswupper(wc)` and a corresponding `iswlower` char exists, returns that (locale-consistent) char; otherwise returns the argument unchanged. — mcc:✓ gcc:✓ clang:✓ (glibc)

##### §7.30.3.1.2 The `towupper` function

- [✓] §7.30.3.1.2p1 — Synopsis: `wint_t towupper(wint_t wc);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.30.3.1.2p2 — Converts a lowercase letter to the corresponding uppercase letter. — mcc:✓ gcc:✓ clang:✓ (`towupper(L'a')`→`A`)
- [✓] §7.30.3.1.2p3 — If `iswlower(wc)` and a corresponding `iswupper` char exists, returns that (locale-consistent) char; otherwise returns the argument unchanged. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §7.30.3.2 Extensible wide character case mapping functions

- [✓] §7.30.3.2p1 — `wctrans`/`towctrans` provide extensible mapping and a common interface to the §7.30.3.1 functions. — mcc:✓ gcc:✓ clang:✓ (glibc)

##### §7.30.3.2.1 The `wctrans` function

- [✓] §7.30.3.2.1p1 — Synopsis: `wctrans_t wctrans(const char *property);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.30.3.2.1p2 — Constructs a `wctrans_t` for the mapping named by `property`; the §7.30.3.2.2 strings are valid in all locales. — mcc:✓ gcc:✓ clang:✓ (`wctrans("toupper")` verified)
- [✓] §7.30.3.2.1p3 — Returns a nonzero `wctrans_t` valid for `towctrans` if `property` names a valid mapping; otherwise zero. — mcc:✓ gcc:✓ clang:✓ (glibc)

##### §7.30.3.2.2 The `towctrans` function

- [✓] §7.30.3.2.2p1 — Synopsis: `wint_t towctrans(wint_t wc, wctrans_t desc);`. — mcc:✓ gcc:✓ clang:✓
- [✓] §7.30.3.2.2p2 — Maps `wc` using `desc`; `LC_CTYPE` must match the `wctrans` call that produced `desc`. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §7.30.3.2.2p3 — `towctrans(wc, wctrans("tolower"))` ≡ `towlower(wc)`, and `towctrans(wc, wctrans("toupper"))` ≡ `towupper(wc)`. — mcc:✓ gcc:✓ clang:✓ (`towctrans(L'm',wctrans("toupper"))`→`M`)
- [✓] §7.30.3.2.2p4 — Returns the mapped value of `wc`; `desc==0` → UB. — mcc:✓ gcc:✓ clang:✓ (glibc)

---

### §7.31.15 Future library directions — `<wchar.h>` (C11 numbering; §7.26.12 in C99)

- §7.31.15p1 — Function names beginning with `wcs` + a lowercase letter may be added to `<wchar.h>`; the `wcs` prefix is reserved in this context. (C11; §7.26.12 in C99) — (spec; reservation/not separately testable — mcc neither defines extra `wcs*` names nor diagnoses program use, matching gcc/clang)

### §7.31.16 Future library directions — `<wctype.h>` (C11 numbering; §7.26.13 in C99)

- §7.31.16p1 — Function names beginning with `is` or `to` + a lowercase letter may be added to `<wctype.h>`; these prefixes are reserved in this context. (C11; §7.26.13 in C99) — (spec; reservation/not separately testable)

> Note: `<uchar.h>` (§7.28) has no entry under §7.31 (Future library directions) in C11.


## Annexes F, G, K (normative)

> mcc status: 188 TODO items in this section; **no mcc-specific gap remains** (no `mcc:✗`/`~` where mcc diverges from a gcc==clang consensus). The `isgreater`/`isless`-family quiet-NaN FE item (§F.10.11p1) in the conditional-conformant numeric annexes that mcc *opts into* (by defining `__STDC_IEC_559__` and `__STDC_IEC_559_COMPLEX__`) conforms: mcc predefines `__GNUC__ 4` and expands those comparison macros to the non-trapping `__builtin_isgreater`/`__builtin_isless`/…, so `isgreater(NaN,1.0)` raises no FE_INVALID and matches gcc/clang. mcc's complex `*` and `/` implement the §G.5.1 infinity-recovery tables (§G.5.1p3/p4, umbrella §G.5p2), matching gcc/clang. The `_Imaginary`-type rows (§G.2–§G.7) are consensus `✗` — none of the three implement `_Imaginary`, which is itself conforming. Annex K is **not provided by any of the three** on glibc (no `__STDC_LIB_EXT1__`) — that omission is itself conforming, so no Annex-K item is an mcc-specific gap. All `<math.h>`/`<complex.h>`/`<fenv.h>` *runtime* special-value behavior is supplied by glibc libm and is therefore byte-identical across mcc/gcc/clang.

> Numbering note: C11 (N1570) inserted **§F.6 The return statement** and renumbered everything after it, so the old C99 (N1256) §F.6–§F.9 became C11 §F.7–§F.10. Headings below use the **C11** numbers with the C99 number noted in parentheses.
> Conditional-conformance note: Annexes F and G are normative but *conditional* — their requirements bind only when the implementation defines `__STDC_IEC_559__` (Annex F) / `__STDC_IEC_559_COMPLEX__` (Annex G). **mcc defines BOTH** (probed: `__STDC_IEC_559__=1`, `__STDC_IEC_559_COMPLEX__=1`, same as gcc/clang), so it has opted into the full Annex F/G binding. Annex K is **C11-only and entirely optional**: its identifiers exist only when the implementation defines `__STDC_LIB_EXT1__` *and* the program defines `__STDC_WANT_LIB_EXT1__` to the integer constant `1` before first including the relevant header. **None of mcc/gcc/clang define `__STDC_LIB_EXT1__`** (probed: all `undef`).

### Conditional-conformance macros (gap-probe summary)
- [✓] §F/§G/§K — `__STDC_IEC_559__`, `__STDC_IEC_559_COMPLEX__`, `__STDC_LIB_EXT1__` predefined-macro presence. — mcc:✓ gcc:✓ clang:✓ (all three: IEC_559=1, IEC_559_COMPLEX=1, LIB_EXT1 undefined — identical)
- [✓] §6.10.8 — `__STDC_NO_COMPLEX__` shall be **undefined** when complex types are supported. — mcc:✓ gcc:✓ clang:✓ (undefined in all three; `double _Complex` works)
- [✓] §F.2.2 / §7.12 — `<math.h>` shall define `math_errhandling` and `MATH_ERREXCEPT`; when Annex F binds, `MATH_ERREXCEPT` shall be set. — mcc:✓ gcc:✓ clang:✓ (all three: `math_errhandling==3`, `MATH_ERREXCEPT` bit set — glibc)

### §F.1 Introduction
- [✓] §F.1p1 — An implementation that defines `__STDC_IEC_559__` shall conform to the specifications in this annex (the IEC 60559:1989 / IEEE 754 binding); an implementation that does not define `__STDC_IEC_559__` is not required to conform to it. — mcc:✓ gcc:✓ clang:✓ (all three define it and meet the format requirements below)
- §F.1p1 — Where a binding between the C language and IEC 60559 is indicated, the IEC 60559-specified behavior is adopted by reference unless stated otherwise. — (spec; not separately testable)
- §F.1p2 (footnote) — IEC 60559 (originally IEC 559:1989) floating-point arithmetic is also the IEEE 754 / IEEE 854 standard; the terms are used interchangeably in this annex. — (spec; not separately testable)

### §F.2 Types
- [✓] §F.2p1 — The C `float` type shall match the IEC 60559 single format; the `double` type shall match the IEC 60559 double format. — mcc:✓ gcc:✓ clang:✓ (FLT_RADIX=2, FLT/DBL mantissa/exp values identical)
- [✓] §F.2p1 — `long double` shall match an IEC 60559 extended format, else a non-IEC-60559 extended format, else the IEC 60559 double format; any non-IEC-60559 extended `long double` shall have more precision than `double` and at least the range of `double`. — mcc:✓ gcc:✓ clang:✓ (all three: `LDBL_MANT_DIG=64`, `LDBL_MAX_EXP=16384` — the x86 80-bit double-extended format)
- [✓] §F.2p1 (footnote) — Permitted `long double` choices include the IEC 60559 double-extended format (e.g. the 80-bit format with p ≥ 64, e_max ≥ 16383) or plain IEC 60559 double; the recommended extended-format parameters are documented for reference. — mcc:✓ gcc:✓ clang:✓ (80-bit, p=64, e_max=16383 — satisfies the recommended parameters)
- [✓] §F.2p2 — The values of the `FLT_*`, `DBL_*`, and `LDBL_*` characteristics in `<float.h>` shall be consistent with the IEC 60559 formats actually in use. — mcc:✓ gcc:✓ clang:✓ (probed `<float.h>` values identical across all three)

#### §F.2.1 Infinities, signed zeros, and NaNs
- §F.2.1p1 — This annex uses *NaN* to denote a quiet NaN; signaling NaNs (where supported) are denoted by *signaling NaN* explicitly. — (spec; terminology — not separately testable)
- [✓] §F.2.1p1 — The `INFINITY` macro in `<math.h>` shall expand to a constant expression of type `float` representing positive (or unsigned) infinity. — mcc:✓ gcc:✓ clang:✓ (`sizeof(INFINITY)==sizeof(float)`; `isinf(INFINITY)` true — glibc `<math.h>`)
- [✓] §F.2.1p1 — The `NAN` macro in `<math.h>` shall be defined **if and only if** the implementation supports quiet NaNs for `float`, expanding to a quiet-NaN `float` constant. — mcc:✓ gcc:✓ clang:✓ (`NAN` defined, `isnan(NAN)` true — glibc)
- [✓] §F.2.1p1 — Operations and functions of this annex distinguish `+0` from `−0` and shall observe the sign of zero wherever IEC 60559 specifies it. — mcc:✓ gcc:✓ clang:✓ (probed `signbit(-0.0-0.0)` etc. — sign of zero preserved by all three)

### §F.3 Operators and functions
- [✓] §F.3p1 — The C unary `−` and binary `+`, `−`, `*`, `/` operators provide the IEC 60559 negate/add/subtract/multiply/divide — correctly rounded in the current rounding direction and signaling the IEC 60559 exceptions. — mcc:✓ gcc:✓ clang:✓ (probed: `1.0/0.0` raises FE_DIVBYZERO under all three)
- [✓] §F.3p1 — The `sqrtf`/`sqrt`/`sqrtl` functions provide the IEC 60559 (correctly rounded) square root. — mcc:✓ gcc:✓ clang:✓ (glibc libm)
- [✓] §F.3p1 — The `remainder` family → IEC 60559 *remainder*; `rint` → round-to-integer in the current mode; `nearbyint` → same but no "inexact". — mcc:✓ gcc:✓ clang:✓ (glibc libm)
- [✓] §F.3p1 — `<fenv.h>` (`fegetround`/`fesetround`, the exception-flag functions, the `FE_*` macros) provides the IEC 60559 rounding modes (`FE_TONEAREST`/`FE_TOWARDZERO`/`FE_UPWARD`/`FE_DOWNWARD`) and the five status flags (`FE_INVALID`/`FE_DIVBYZERO`/`FE_OVERFLOW`/`FE_UNDERFLOW`/`FE_INEXACT`). — mcc:✓ gcc:✓ clang:✓ (probed: `fesetround(FE_UPWARD)`+`fegetround` round-trips; `feclearexcept`/`fetestexcept` work — system `<fenv.h>`)
- [✓] §F.3p1 — `copysign`→copysign; `scalbn`/`scalbln`→scalb; `logb`→logb; `nextafter`/`nexttoward`→nextafter; `fmod`→mod; `ceil`/`floor`/`trunc`/`round`→round-to-integral; binary↔decimal in `<stdlib.h>`/`<stdio.h>`/`<wchar.h>`→IEC 60559 conversions. — mcc:✓ gcc:✓ clang:✓ (glibc libm/libc)
- [✓] §F.3p1 — The `fma` family provides the IEC 60559 fused multiply-add (single final rounding). — mcc:✓ gcc:✓ clang:✓ (glibc libm `fma`)
- [✓] §F.3p2 — `feraiseexcept`/`feclearexcept`/`fetestexcept` and `fegetexceptflag`/`fesetexceptflag` provide status-flag manipulation; `feholdexcept`/`feupdateenv` and `fegetenv`/`fesetenv` save/restore the environment. — mcc:✓ gcc:✓ clang:✓ (system `<fenv.h>`)
- [✓] §F.3p3 — Conversion between integer and floating types, and between floating types, shall round per the current rounding direction and signal the IEC 60559 exceptions (detailed in §F.4). — mcc:✓ gcc:✓ clang:✓ (hardware FP conversions emitted by all three)

### §F.4 Floating to integer conversion
- [✓] §F.4p1 — Finite in-range-but-inexact float→integer truncates toward zero; whether "inexact" is raised is unspecified. — mcc:✓ gcc:✓ clang:✓ (truncation toward zero; cvttsd2si on x86)
- [~] §F.4p1 — float→integer of ∞/NaN, or a truncated value out of the destination range, raises "invalid" and the result is unspecified. — mcc:~ gcc:~ clang:~ (raising of the flag is hardware-dependent; mcc emits the same cvtt instruction as gcc/clang — no observable divergence)
- [✓] §F.4p2 (C11) — Integer→floating that cannot be represented exactly rounds per the current rounding direction and raises "inexact". — mcc:✓ gcc:✓ clang:✓ (hardware cvtsi2sd)

### §F.5 Binary-decimal conversion
- [✓] §F.5p1 — Conversion from the widest IEC 60559 format to a `DECIMAL_DIG`-digit decimal string and back is the identity (exact round-trip). — mcc:✓ gcc:✓ clang:✓ (`DECIMAL_DIG=21` in all three; round-trip via glibc strtold/printf)
- [✓] §F.5p2 — Conversions in `strtod`/`scanf`/`printf` shall be correctly rounded as far as required and honor the current rounding direction. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.5p3 — A binary↔decimal conversion that overflows raises "overflow", underflows raises "underflow", is inexact raises "inexact". — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.5 (added) — `<float.h>` shall define `DECIMAL_DIG` (C99) — the number of decimal digits for the widest supported format. — mcc:✓ gcc:✓ clang:✓ (probed `DECIMAL_DIG=21`)

### §F.6 The return statement (C11) *(new in C11; no C99 counterpart)*
> C99 had no §F.6; the subclause was added in C11 and pushed C99 §F.6–§F.9 to C11 §F.7–§F.10.
- [✓] §F.6p1 (C11) — If the return expression is evaluated in a format wider than the return type (per `FLT_EVAL_METHOD`), it is converted as if by assignment to the return type before returning. — mcc:✓ gcc:✓ clang:✓ (NA in practice: `FLT_EVAL_METHOD==0` in all three — no excess precision, so no narrowing needed)

### §F.7 Contracted expressions *(C99 §F.6)*
- [✓] §F.7p1 — A contracted expression is correctly rounded once for the whole expression and treats infinities/NaNs/signed-zeros/subnormals/rounding consistently. — mcc:✓ gcc:✓ clang:✓ (at -O0 none contract; default `FP_CONTRACT` honored)
- [ ] §F.7p2 — A contracted expression raises exceptions consistently with non-contracted evaluation, except "inexact"/"underflow"/"overflow" only for the final single-rounded result. — (spec; not separately observable without contraction)
- [✓] §F.7p3 — `<math.h>` defines the `FP_CONTRACT` pragma; its default state is implementation-defined and shall be documented. — mcc:✓ gcc:~ clang:✓ (mcc parses `#pragma STDC FP_CONTRACT` silently — handled in `src/mccpp.c:1817`; gcc warns "ignoring #pragma STDC FP_CONTRACT" under -Wall; clang accepts silently)

### §F.8 Floating-point environment *(C99 §F.7)*
- [✓] §F.8p1 — Governed by the `FENV_ACCESS` pragma; `<fenv.h>` gives access to the IEC 60559 status flags and control modes. — mcc:✓ gcc:~ clang:✓ (mcc recognizes `#pragma STDC FENV_ACCESS` — `src/mccpp.c:1819`, and acts on it in codegen `src/mccgen.c:2607`; gcc warns "ignoring #pragma STDC FENV_ACCESS"; clang accepts)

#### §F.8.1 Environment management
- [~] §F.8.1p1 — Code that tests flags or changes modes must execute as if `FENV_ACCESS` were ON so implicit flag/mode effects are observed. — mcc:~ gcc:~ clang:~ (all three give correct flag/mode behavior at -O0; full `FENV_ACCESS ON` value-fencing is not guaranteed under optimization by any)

#### §F.8.2 Translation
- [✓] §F.8.2p1 — During translation the IEC 60559 default modes are in effect: round to nearest; all exceptions cleared; no traps. — mcc:✓ gcc:✓ clang:✓ (constant folding uses round-to-nearest in all three)
- [✓] §F.8.2p2 — Translation-time FP evaluation uses round-to-nearest and cannot depend on a runtime-set rounding mode. — mcc:✓ gcc:✓ clang:✓ (compile-time constant folding is round-to-nearest)

#### §F.8.3 Execution
- [✓] §F.8.3p1 — At program startup the FP environment is the default: round-to-nearest, all flags clear, no traps. — mcc:✓ gcc:✓ clang:✓ (C runtime/`crt` sets default FP env; probed `fegetround`==FE_TONEAREST at start)

#### §F.8.4 Constant expressions
- [✓] §F.8.4p1 — A required constant expression is evaluated in the default environment; otherwise it may be evaluated during execution and observe the runtime rounding mode under `FENV_ACCESS ON`. — mcc:✓ gcc:✓ clang:✓ (mcc folds required constants at translation; see §F.8.2)
- [~] §F.8.4p2 — Rounding a constant with the runtime mode rather than the default is permitted only where the value need not be a constant expression and `FENV_ACCESS` is ON. — mcc:~ gcc:~ clang:~ (no observable divergence; mcc honors FENV_ACCESS in codegen)

#### §F.8.5 Initialization
- [✓] §F.8.5p1 — Non-constant FP initializers are evaluated (and raise exceptions) at the point of initialization under the runtime environment; constant-expression initializers are evaluated in the default environment at translation time. — mcc:✓ gcc:✓ clang:✓ (runtime initializers emit runtime FP code in all three)

#### §F.8.6 Changing the environment
- §F.8.6p1 — Code that changes modes or tests/clears flags must execute under `FENV_ACCESS ON`; a function that changes the environment shall restore it on return unless documented otherwise. — (spec/library convention; not a compiler property — not separately testable)
- [~] §F.8.6p2 — Operations under `FENV_ACCESS` OFF may assume the default environment and be optimized; under ON the visible flag/mode semantics shall be preserved. — mcc:~ gcc:~ clang:~ (mcc fences runtime-rounding ops under FENV_ACCESS ON per `src/mccgen.c:2607`; no observed divergence)

### §F.9 Optimization *(C99 §F.8)*
- [ ] §F.9p1 — Identifies transformations that may/may not be applied while preserving IEC 60559 semantics. — (spec; rules tested individually below)

#### §F.9.1 Floating-point arithmetic and value-changing optimizations
- [✓] §F.9.1p1 — The implementation shall not apply optimizations that change the value (including the sign of a zero) of an FP expression except as permitted below. — mcc:✓ gcc:✓ clang:✓ (probed at -O0 and -O2; none apply value-changing FP transforms by default — `-ffast-math`/`-funsafe-math-optimizations` would be the opt-in)

#### §F.9.2 Expression transformations
- [✓] §F.9.2 — `x/2` ↔ `x*0.5` is **allowed**. — mcc:✓ gcc:✓ clang:✓ (exact; no divergence)
- [✓] §F.9.2 — `1*x` → `x` and `x/1` → `x` are **allowed**. — mcc:✓ gcc:✓ clang:✓
- [✓] §F.9.2 — `x/x` → `1.0` is **NOT allowed**. — mcc:✓ gcc:✓ clang:✓ (none fold `x/x`; would be wrong for 0/∞/NaN)
- [✓] §F.9.2 — `x*0.0` → `0.0` is **NOT allowed**. — mcc:✓ gcc:✓ clang:✓ (probed `INFINITY*0.0` → NaN in all three at -O0 and -O2)
- [✓] §F.9.2 — `x+0.0` → `x` is **NOT allowed** (fails for x=−0.0). — mcc:✓ gcc:✓ clang:✓ (probed: `f(-0.0)=x+0.0` yields `+0.0` (signbit 0) in all three — the forbidden fold is NOT applied)
- [✓] §F.9.2 — `x−0.0` → `x` is **allowed**. — mcc:✓ gcc:✓ clang:✓ (probed: `x-0.0` of −0.0 stays −0.0 (signbit set) in all three)
- [✓] §F.9.2 — `0.0−x` → `−x` is **NOT allowed** in general; `−x` vs `0.0−x` must be distinguished. — mcc:✓ gcc:✓ clang:✓ (none mis-fold this by default)
- [✓] §F.9.2 — `x−y` ↔ `−(y−x)` is **NOT allowed** (sign-of-zero). — mcc:✓ gcc:✓ clang:✓
- [✓] §F.9.2 — Re-association of `+`/`*` (changing roundings) is **NOT allowed** unless provably identical. — mcc:✓ gcc:✓ clang:✓ (no FP reassociation without `-fassociative-math`/`-ffast-math`)

#### §F.9.3 Relational operators
- [✓] §F.9.3p1 — `x != x` → `false` and `x == x` → `true` are **NOT allowed** (wrong for NaN). — mcc:✓ gcc:✓ clang:✓ (probed: `NAN==NAN` is false, `NAN!=NAN` is true at runtime in all three)
- [✓] §F.9.3p2 — `x < y` and `!(x >= y)` are not equivalent when an operand is NaN; the implementation shall not interconvert them. — mcc:✓ gcc:✓ clang:✓ (none perform the De-Morgan FP rewrite by default)

### §F.10 Mathematics `<math.h>` *(C99 §F.9)*
> All §F.10.x function special-value behavior below is supplied by **glibc libm**, which mcc links exactly as gcc/clang do (mcc bundles no `<math.h>`/libm — it uses the system ones). Consequently every special-value result is byte-identical across the three. Tags reflect that shared-runtime reality; the lone mcc-specific divergence in this clause is the comparison macros of §F.10.11.
- [✓] §F.10p1 — Each function produces a correctly rounded result honoring the current rounding direction where IEC 60559 specifies it, and raises exceptions consistently. — mcc:✓ gcc:✓ clang:✓ (glibc libm)
- [✓] §F.10p2 — "invalid" for domain error, "divide-by-zero" for pole, "overflow"/"underflow" for range; prescribed values for ±0/±∞/NaN. — mcc:✓ gcc:✓ clang:✓ (glibc libm)
- [✓] §F.10p3 — A NaN argument (except where fully specified) is returned as a NaN with no exception raised solely for a quiet NaN. — mcc:✓ gcc:✓ clang:✓ (glibc libm)
- [✓] §F.10p4 — A function with >1 floating argument computes as if infinitely precise and rounds once. — mcc:✓ gcc:✓ clang:✓ (glibc libm)
- [✓] §F.10p5 — "inexact"/subnormal-"underflow" behavior follows IEC 60559; no spurious exceptions for in-domain arguments. — mcc:✓ gcc:✓ clang:✓ (glibc libm)

#### §F.10.1 Trigonometric functions
- [✓] §F.10.1.1 — `acos(1)`→+0; `acos(|x|>1)`→"invalid",NaN. — mcc:✓ gcc:✓ clang:✓ (glibc; probed `acos(1.0)==0`)
- [✓] §F.10.1.2 — `asin(±0)`→±0; `asin(|x|>1)`→"invalid",NaN. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.1.3 — `atan(±0)`→±0; `atan(±∞)`→±π/2. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.1.4 — `atan2` full IEC 60559 quadrant table. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.1.5 — `cos(±0)`→1; `cos(±∞)`→"invalid",NaN. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.1.6 — `sin(±0)`→±0; `sin(±∞)`→"invalid",NaN. — mcc:✓ gcc:✓ clang:✓ (glibc; probed `sin(0.0)==0`)
- [✓] §F.10.1.7 — `tan(±0)`→±0; `tan(±∞)`→"invalid",NaN. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §F.10.2 Hyperbolic functions
- [✓] §F.10.2.1 — `acosh(1)`→+0; `acosh(x<1)`→"invalid",NaN; `acosh(+∞)`→+∞. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.2.2 — `asinh(±0)`→±0; `asinh(±∞)`→±∞. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.2.3 — `atanh(±0)`→±0; `atanh(±1)`→"divide-by-zero",±∞; `atanh(|x|>1)`→"invalid",NaN. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.2.4 — `cosh(±0)`→1; `cosh(±∞)`→+∞. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.2.5 — `sinh(±0)`→±0; `sinh(±∞)`→±∞. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.2.6 — `tanh(±0)`→±0; `tanh(±∞)`→±1. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §F.10.3 Exponential and logarithmic functions
- [✓] §F.10.3.1 — `exp(±0)`→1; `exp(−∞)`→+0; `exp(+∞)`→+∞. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.3.2 — `exp2(±0)`→1; `exp2(−∞)`→+0; `exp2(+∞)`→+∞. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.3.3 — `expm1(±0)`→±0; `expm1(−∞)`→−1; `expm1(+∞)`→+∞. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.3.4 — `frexp` special values; stored exponent unspecified for ±0/±∞/NaN. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.3.5 — `ilogb(0)`→"invalid",`FP_ILOGB0`; `ilogb(±∞)`→"invalid",`INT_MAX`; `ilogb(NaN)`→"invalid",`FP_ILOGBNAN`. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.3.6 — `ldexp(x,0)`→x; `ldexp(±0/±∞,n)`→arg; overflow/underflow raise. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.3.7 — `log(±0)`→"divide-by-zero",−∞; `log(1)`→+0; `log(x<0)`→"invalid",NaN; `log(+∞)`→+∞. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.3.8 — `log10` same special values as `log`. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.3.9 — `log1p(±0)`→±0; `log1p(−1)`→"divide-by-zero",−∞; `log1p(x<−1)`→"invalid",NaN; `log1p(+∞)`→+∞. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.3.10 — `log2(±0)`→"divide-by-zero",−∞; `log2(1)`→+0; `log2(x<0)`→"invalid",NaN; `log2(+∞)`→+∞. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.3.11 — `logb(±0)`→"divide-by-zero",−∞; `logb(±∞)`→+∞; `logb(±1)`→+0. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.3.12 — `modf(±0,iptr)`→±0,stores±0; `modf(±∞,iptr)`→±0,stores±∞; `modf(NaN,iptr)`→NaN,storesNaN. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.3.13 — `scalbn`/`scalbln`: ±0/±∞→arg; else x·FLT_RADIX^n, correctly rounded with overflow/underflow. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §F.10.4 Power and absolute-value functions
- [✓] §F.10.4.1 — `cbrt(±0)`→±0; `cbrt(±∞)`→±∞. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.4.2 — `fabs(±0)`→+0; `fabs(±∞)`→+∞; `fabs(NaN)`→NaN; never raises. — mcc:✓ gcc:✓ clang:✓ (glibc / `__builtin_fabs`)
- [✓] §F.10.4.3 — `hypot(x,±∞)`=`hypot(±∞,y)`→+∞ for any other operand incl. NaN; `hypot` symmetric in sign/order. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.4.4 — `pow` full special-value table. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.4.5 — `sqrt(±0)`→±0; `sqrt(+∞)`→+∞; `sqrt(x<0)`→"invalid",NaN; correctly rounded. — mcc:✓ gcc:✓ clang:✓ (glibc / hardware `sqrtsd`)

#### §F.10.5 Error and gamma functions
- [✓] §F.10.5.1 — `erf(±0)`→±0; `erf(±∞)`→±1. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.5.2 — `erfc(−∞)`→2; `erfc(+∞)`→+0; may raise "underflow" for large +x. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.5.3 — `lgamma(1)`/`lgamma(2)`→+0; non-positive integer→"divide-by-zero",+∞; `lgamma(±∞)`→+∞. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.5.4 — `tgamma(±0)`→"divide-by-zero",±∞; negative integer→"invalid",NaN; `tgamma(−∞)`→"invalid",NaN; `tgamma(+∞)`→+∞. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §F.10.6 Nearest integer functions
- [✓] §F.10.6.1 — `ceil(±0)`→±0; `ceil(±∞)`→±∞; whether "inexact" is raised is impl-defined. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.6.2 — `floor(±0)`→±0; `floor(±∞)`→±∞; "inexact" as `ceil`. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.6.3 — `nearbyint(±0)`→±0; `nearbyint(±∞)`→±∞; shall **not** raise "inexact". — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.6.4 — `rint(±0)`→±0; `rint(±∞)`→±∞; may raise "inexact". — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.6.5 — `lrint`/`llrint`: out-of-range→"invalid",unspecified; else round per current mode. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.6.6 — `round(±0)`→±0; `round(±∞)`→±∞; halfway away from zero regardless of mode. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.6.7 — `lround`/`llround`: out-of-range→"invalid",unspecified; halfway away from zero. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.6.8 — `trunc(±0)`→±0; `trunc(±∞)`→±∞; toward zero. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §F.10.7 Remainder functions
- [✓] §F.10.7.1 — `fmod` special values (±0, finite/±∞, ±∞→"invalid", ±0 divisor→"invalid"). — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.7.2 — `remainder` special values (mirrors `fmod` invalid cases). — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.7.3 — `remquo` returns `remainder`'s value for special cases and stores low-order quotient bits with sign of x/y. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §F.10.8 Manipulation functions
- [✓] §F.10.8.1 — `copysign(x,y)`→x with sign of y (even NaN); never raises. — mcc:✓ gcc:✓ clang:✓ (glibc / `__builtin_copysign`)
- [✓] §F.10.8.2 — `nan("…")`→quiet NaN per tag; never raises. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.8.3 — `nextafter(x,y)` next representable; `nextafter(x,x)`→x; overflow→"overflow"+"inexact"; subnormal/underflow→"underflow"+"inexact". — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.8.4 — `nexttoward` like `nextafter` with `long double` second arg; `nexttoward(x,x)`→x converted to type of x. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §F.10.9 Maximum, minimum, and positive difference functions
- [✓] §F.10.9.1 — `fdim(x,y)`→x−y if x>y else +0; range error→"overflow". — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.9.2 — `fmax` returns larger; NaN ignored vs a number; `fmax(NaN,NaN)`→NaN; no exception for NaN arg. — mcc:✓ gcc:✓ clang:✓ (glibc)
- [✓] §F.10.9.3 — `fmin` returns smaller; NaN ignored vs a number; `fmin(NaN,NaN)`→NaN; no exception for NaN arg. — mcc:✓ gcc:✓ clang:✓ (glibc)

#### §F.10.10 Floating multiply-add
- [✓] §F.10.10.1 — `fma(x,y,z)` correctly rounded fused; 0·∞/∞·0→"invalid",NaN even if z NaN; ∞ + oppositely-signed ∞→"invalid",NaN; else single-rounded with overflow/underflow/inexact only for the final result. — mcc:✓ gcc:✓ clang:✓ (glibc `fma`)

#### §F.10.11 Comparison macros (C11)
> C99 placed the `<math.h>` comparison-macro quiet-NaN guarantee in §F.9 prose; C11 gives it its own subclause §F.10.11.
- [✓] §F.10.11p1 (C11) — `isgreater`, `isgreaterequal`, `isless`, `islessequal`, `islessgreater`, `isunordered` shall **not** raise "invalid" when an argument is a quiet NaN (unlike the bare `<`/`>`/`<=`/`>=`). — mcc:✓ gcc:✓ clang:✓ (probed `isgreater(NaN,1.0)` — mcc raises no `FE_INVALID` and returns `0`, identical to gcc/clang, while the bare `NaN > 1.0` still raises `FE_INVALID` on all three. mcc predefines `__GNUC__ 4`, so glibc's `<math.h>` expands these to the non-trapping `__builtin_isgreater`/`__builtin_isless`/… forms.)

### §G.1 Introduction
- [✓] §G.1p1 — An implementation that defines `__STDC_IEC_559_COMPLEX__` shall conform to this annex; it should also define `__STDC_IEC_559__`. — mcc:✓ gcc:✓ clang:✓ (mcc defines both — probed)
- §G.1p2 (note) — This annex augments Annex F with complex types; its complex-operation specs take precedence over the general `<complex.h>` semantics. — (spec; not separately testable)

### §G.2 Types
- [✓] §G.2p1 — One complex type per real floating type (`float`/`double`/`long double _Complex`), same representation/alignment as an array of two reals, real part first. — mcc:✓ gcc:✓ clang:✓ (all three: `double _Complex` is two contiguous doubles, real first; probed `cimag`/`creal`)
- [✗] §G.2p1 — One imaginary type per real floating type (`float`/`double`/`long double _Imaginary`), required as a conforming extension whenever this annex applies. — **mcc:✗ gcc:✗ clang:✗** (consensus gap — NOT mcc-specific: all three reject `_Imaginary` — mcc "imaginary types are not supported", clang same, gcc "expected identifier". Strictly nonconforming for an Annex-G implementation, but identical across the three.)
- [✗] §G.2p2 — `<complex.h>` shall define `_Imaginary_I`; when imaginary types are supported, `I` expands to `_Imaginary_I` (not `_Complex_I`). — mcc:✗(consensus) gcc:✗ clang:✗ (none define `_Imaginary_I` — probed `#ifdef` false in all three; `I` expands to `_Complex_I` everywhere)

### §G.3 Conventions
- [ ] §G.3p1 — A complex/imaginary value with ≥1 infinite part is an *infinity*, even if its other part is NaN. — (spec/convention; exercised by §G.5/§G.6 behavior)
- §G.3p1 — A complex/imaginary value is *finite* iff both parts are finite. — (spec; not separately testable)
- §G.3p1 — A complex/imaginary value is *zero* if each part is a (possibly signed) zero. — (spec; not separately testable)

### §G.4 Conversions
#### §G.4.1 Imaginary types
- [✗] §G.4.1p1 — Conversions among imaginary types follow the real-floating rules. — mcc:✗(consensus) gcc:✗ clang:✗ (no imaginary types in any — see §G.2p1)

#### §G.4.2 Real and imaginary
- [✗] §G.4.2p1 — Imaginary→real (other than `_Bool`) yields +0. — mcc:✗(consensus) gcc:✗ clang:✗ (no imaginary types)
- [✗] §G.4.2p2 — Real→imaginary yields +imaginary-zero. — mcc:✗(consensus) gcc:✗ clang:✗ (no imaginary types)

#### §G.4.3 Imaginary and complex
- [✗] §G.4.3p1 — Imaginary→complex yields +0 real part, the value as imaginary part. — mcc:✗(consensus) gcc:✗ clang:✗ (no imaginary types)
- [✗] §G.4.3p2 — Complex→imaginary yields the imaginary part (real part discarded). — mcc:✗(consensus) gcc:✗ clang:✗ (no imaginary types)

### §G.5 Binary operators
- [✓] §G.5p1 — `*`/`/`/`+`/`−` on complex/imaginary/real yield the type per the usual arithmetic conversions extended to imaginary types and follow §G.5.1–§G.5.2. — mcc:✓ gcc:✓ clang:✓ (real↔complex `+`/`−` and `*`/`/` work in mcc; the imaginary-type axis is absent in all three; mcc's complex `*`/`/` value handling follows the §G.5.1p3/p4 infinity-recovery tables, matching gcc/clang)
- [✓] §G.5p2 — Every binary-operator table's result-type AND special-value handling shall be exactly as specified (not the naïve arithmetic-conversion implementation). — **mcc:✓** gcc:✓ clang:✓ (mcc's complex `*` and `/` implement the §G.5.1 infinity-recovery tables, matching gcc/clang — see the two items below)

#### §G.5.1 Multiplicative operators
- [✗] §G.5.1p1 — real·imaginary→imaginary; imaginary·imaginary→real; imaginary/imaginary→real. — mcc:✗(consensus) gcc:✗ clang:✗ (no imaginary types, so the imaginary-result typing cannot be exercised in any)
- [✗] §G.5.1p2 — `imaginary*imaginary` is the negated product of the imaginary parts (real); `imaginary/imaginary` is the real quotient; the per-operand tables shall be honored. — mcc:✗(consensus) gcc:✗ clang:✗ (no imaginary types)
- [✓] §G.5.1p3 — Complex `*` uses `(ac−bd)+(ad+bc)i`, but a naïve NaN+iNaN result with an infinite operand shall be recovered to the proper signed infinity via the §G.5.1p3 algorithm. — **mcc:✓** gcc:✓ clang:✓ (probed `(INFINITY+0i)*(1+1i)` → mcc gives `inf+inf·i`, matching gcc/clang. mcc applies `__muldc3`-style infinity recovery to the complex multiply.)
- [✓] §G.5.1p4 — Complex `/` uses `(a+bi)/(c+di)` with recovery: infinite dividend÷finite divisor→infinity; infinite divisor→NaN-or-zero, per the §G.5.1p4 algorithm. — **mcc:✓** gcc:✓ clang:✓ (probed `(1+1i)/(INFINITY+INFINITY·i)` → mcc gives `0−0i` (divisor is infinite, so result is `0`), matching gcc/clang. mcc applies `__divdc3`-style recovery/scaling to the complex divide.)
- [~] §G.5.1p5 — The `CX_LIMITED_RANGE` pragma (§7.3.4) permits the naïve formulas (no recovery, no scaling); default "off". — mcc:~ gcc:~ clang:~ (mcc *recognizes* `#pragma STDC CX_LIMITED_RANGE` — `src/mccpp.c:1821`, `src/mccgen.c:5172` — and gcc warns "ignoring" it; but because mcc's default-off path is *already* the naïve formula (the §G.5.1p3/p4 gap), the pragma has no observable effect in mcc. gcc/clang recover by default and the pragma is a no-op at -O0 in practice.)

#### §G.5.2 Additive operators
- [✗] §G.5.2p1 — real±imaginary→complex with parts from the respective operands, no cross-cancellation, signs of zeros preserved. — mcc:✗(consensus) gcc:✗ clang:✗ (no imaginary types; real±complex works)
- [~] §G.5.2p2 — imaginary±imaginary→imaginary; complex±complex→componentwise; signed zeros/infinities handled componentwise. — mcc:~ gcc:~ clang:~ (complex±complex componentwise add works identically in all three; the imaginary±imaginary half is unreachable — no imaginary types)

### §G.6 Complex arithmetic `<complex.h>`
> All §G.6.x special-value behavior is supplied by **glibc libm** (mcc bundles `runtime/include/complex.h` declaring the functions, but the implementations are glibc's), so results are identical across the three. `cpow` and any function that internally multiplies/divides complex values goes through glibc's own `__muldc3`/careful code; mcc's own inline complex `*`/`/` now applies the same §G.5.1 infinity recovery, so the two paths agree.
- [✓] §G.6p1 — Each listed function returns the specified result for the special arguments and raises/does-not-raise exceptions as specified; "NaN+iNaN" signals "invalid" only where indicated. — mcc:✓ gcc:✓ clang:✓ (glibc libm)
- [✓] §G.6p2 — Functions are continuous onto both sides of their branch cuts, taking the sign of zero into account. — mcc:✓ gcc:✓ clang:✓ (glibc libm)
- [✓] §G.6p3 — The non-tabulated functions (`cacos`/`casin`/`catan`, `ccos`/`csin`/`ctan`, the `*sinh`/`*tanh`/`*cosh` companions, `csqrt`) are defined via the §G.6 identities and honor them for special values. — mcc:✓ gcc:✓ clang:✓ (glibc libm)
- [✓] §G.6p4 — Each function maps `conj(z)` to `conj(f(z))` except where the branch cut requires otherwise. — mcc:✓ gcc:✓ clang:✓ (glibc libm)

#### §G.6.1 Trigonometric functions
##### §G.6.1.1 The `cacos` functions
- [✓] §G.6.1.1p1 — `cacos` full special-value table with §G.6p4 conjugate symmetry. — mcc:✓ gcc:✓ clang:✓ (glibc libm)

#### §G.6.2 Hyperbolic functions
##### §G.6.2.1 The `cacosh` functions
- [✓] §G.6.2.1p1 — `cacosh` full special-value table. — mcc:✓ gcc:✓ clang:✓ (glibc libm)
##### §G.6.2.2 The `casinh` functions
- [✓] §G.6.2.2p1 — `casinh` full special-value table. — mcc:✓ gcc:✓ clang:✓ (glibc libm)
##### §G.6.2.3 The `catanh` functions
- [✓] §G.6.2.3p1 — `catanh` full special-value table. — mcc:✓ gcc:✓ clang:✓ (glibc libm)
##### §G.6.2.4 The `ccosh` functions
- [✓] §G.6.2.4p1 — `ccosh` full special-value table. — mcc:✓ gcc:✓ clang:✓ (glibc libm)
##### §G.6.2.5 The `csinh` functions
- [✓] §G.6.2.5p1 — `csinh` full special-value table. — mcc:✓ gcc:✓ clang:✓ (glibc libm)
##### §G.6.2.6 The `ctanh` functions
- [✓] §G.6.2.6p1 — `ctanh` full special-value table. — mcc:✓ gcc:✓ clang:✓ (glibc libm)

#### §G.6.3 Exponential and logarithmic functions
##### §G.6.3.1 The `cexp` functions
- [✓] §G.6.3.1p1 — `cexp` full special-value table. — mcc:✓ gcc:✓ clang:✓ (glibc libm)
##### §G.6.3.2 The `clog` functions
- [✓] §G.6.3.2p1 — `clog` full special-value table. — mcc:✓ gcc:✓ clang:✓ (glibc libm)

#### §G.6.4 Power and absolute-value functions
##### §G.6.4.1 The `cpow` functions
- [✓] §G.6.4.1p1 — `cpow(z,w)`=`cexp(w·clog(z))`; branch-cut/continuity of §G.6p2 shall hold. — mcc:✓ gcc:✓ clang:✓ (glibc libm `cpow`; the internal complex multiply is glibc's, not mcc's inline operator)
##### §G.6.4.2 The `csqrt` functions
- [✓] §G.6.4.2p1 — `csqrt` full special-value table. — mcc:✓ gcc:✓ clang:✓ (glibc libm)
##### §G.6.4.3 The `cabs` functions
- [✓] §G.6.4.3 — `cabs(x+iy)`→+∞ if either part infinite (even if other NaN); NaN if no infinity and ≥1 NaN; else `hypot(x,y)`. — mcc:✓ gcc:✓ clang:✓ (glibc libm `cabs`)

### §G.7 Type-generic math `<tgmath.h>`
- [~] §G.7p1 — A type-generic macro for a `<complex.h>` function shall, for a complex argument, invoke the corresponding complex function with §G.6 special-value behavior; for an imaginary argument it determines the function/result type per §G.4. — mcc:~ gcc:~ clang:~ (mcc bundles `runtime/include/tgmath.h`; complex-argument dispatch works in all three; the imaginary-argument axis is unreachable — no imaginary types)
- [✗] §G.7p2 — Where an imaginary argument can be handled by a real function on the imaginary part, the implementation may do so with the appropriate type/sign conversion. — mcc:✗(consensus) gcc:✗ clang:✗ (no imaginary types in any)

### §K.1 Background *(C11; the entire Annex K is optional and conditional on `__STDC_LIB_EXT1__`)*
> Annex K does not exist in C99; it is C11-only. **None of mcc/gcc/clang define `__STDC_LIB_EXT1__`** (probed: all `undef`), so none provide Annex K. Because Annex K is optional, this omission is **conforming** — therefore no Annex-K item below is an mcc-specific gap. The `_s` identifiers are simply absent in all three (glibc does not implement Annex K). Tags reflect "not provided; omission conforming".
- [✓] §K.1p1 (C11) — Annex K specifies optional bounds-checking interfaces; an implementation that defines `__STDC_LIB_EXT1__` shall conform. — mcc:✓ gcc:✓ clang:✓ (none define `__STDC_LIB_EXT1__`; omission is conforming)
- [ ] §K.1p2 (C11) — Annex K identifiers are not declared/defined unless `__STDC_WANT_LIB_EXT1__` is defined as the integer constant `1` before the header is first included. — (spec; not provided by any — untestable when absent)

### §K.2 Scope
- [✓] §K.2p1 (C11) — Annex K provides replacement/new functions and types supporting runtime-constraint checking; providing it is optional. — mcc:✓ gcc:✓ clang:✓ (optional; none provide it)
- [ ] §K.2p2 (C11) — If defined, `__STDC_LIB_EXT1__` shall expand to `201112L`. — mcc:n/a gcc:n/a clang:n/a (undefined in all three — vacuously satisfied)

### §K.3 Library
> Every item in §K.3 is **not provided by mcc/gcc/clang** (no `__STDC_LIB_EXT1__`; glibc ships no `_s` functions, no `errno_t`/`rsize_t`/`RSIZE_MAX`/`constraint_handler_t`). All are tagged `— (C11 Annex K optional; not provided by mcc/gcc/clang — omission conforming)`; none is an mcc-specific gap.
#### §K.3.1 Introduction
- [ ] §K.3.1.1p1 (C11) — Header identifiers visible only when `__STDC_WANT_LIB_EXT1__`==1 at inclusion; re-inclusion with a different definition has impl-defined effect. — (C11 Annex K optional; not provided by mcc/gcc/clang — omission conforming)
- [ ] §K.3.1.2p1 (C11) — Identifiers ending in `_s` plus the Annex K macros/types are reserved when an Annex K header is included with the extension enabled. — (not provided by any)
- [ ] §K.3.1.3p1 (C11) — Whether an Annex K function sets `errno` is unspecified unless documented. — (not provided by any)
- [ ] §K.3.1.4p1 (C11) — Each function has runtime-constraints; a violation calls the registered constraint handler. — (not provided by any)
- [ ] §K.3.1.4p2 (C11) — A function checks runtime-constraints before any other operation except those needed to detect the violation. — (not provided by any)
- [ ] §K.3.1.4p3 (C11) — After the handler returns, the function returns the indicated error value and does not perform its nominal operation. — (not provided by any)
- [ ] §K.3.1.4p4 (C11) — If a call violates ≥2 constraints, any one may be reported (which is unspecified). — (not provided by any)

#### §K.3.2 Errors `<errno.h>`
- [ ] §K.3.2.1p1 (C11) — `<errno.h>` with the extension shall define `errno_t` as `int`. — (not provided by any; probed `errno_t` undeclared)

#### §K.3.3 Common definitions `<stddef.h>`
- [ ] §K.3.3.1p1 (C11) — `<stddef.h>` with the extension shall define `rsize_t` = `size_t`. — (not provided by any; mcc's bundled `runtime/include/stddef.h` has no `rsize_t`)

#### §K.3.4 Integer types `<stdint.h>`
- [ ] §K.3.4.1p1 (C11) — `<stdint.h>` with the extension shall define `RSIZE_MAX` (recommended ≤ `SIZE_MAX >> 1`). — (not provided by any; mcc's bundled `runtime/include/stdint.h` has no `RSIZE_MAX`)
- [ ] §K.3.4.1p2 (C11) — Sizes > `RSIZE_MAX` are treated as runtime-constraint violations. — (not provided by any)

#### §K.3.5 Input/output `<stdio.h>`
- [ ] §K.3.5p1 (C11) — `<stdio.h>` with the extension shall define `errno_t`, `rsize_t`, `L_tmpnam_s`, `TMP_MAX_S`, and the `_s` I/O functions. — (not provided by any)
- [ ] §K.3.5.1.1p1 (C11) — `tmpfile_s` — runtime-constraint `streamptr` non-null; creates/opens a temp binary file. — (not provided by any)
- [ ] §K.3.5.1.2p1 (C11) — `tmpnam_s(char*s, rsize_t maxsize)` — writes a unique name; `TMP_MAX_S` bounds the guarantee. — (not provided by any)
- [ ] §K.3.5.2.1p1 (C11) — `fopen_s` — non-null streamptr/filename/mode; opens with default deny-other-access. — (not provided by any)
- [ ] §K.3.5.2.2p1 (C11) — `freopen_s` — non-null newstreamptr/mode/stream (filename may be null). — (not provided by any)
- [ ] §K.3.5.3.1p1 (C11) — `fprintf_s` — non-null stream/format, no `%n`, no null `%s`. — (not provided by any)
- [ ] §K.3.5.3.2p1 (C11) — `fscanf_s` — `%c`/`%s`/`%[` each take pointer + `rsize_t` count. — (not provided by any)
- [ ] §K.3.5.3.3p1 (C11) — `printf_s` ≡ `fprintf_s(stdout,…)`. — (not provided by any)
- [ ] §K.3.5.3.4p1 (C11) — `scanf_s` ≡ `fscanf_s` from `stdin`. — (not provided by any)
- [ ] §K.3.5.3.5p1 (C11) — `snprintf_s` — `n`≠0, ≤`RSIZE_MAX`; always null-terminates when `n>0`. — (not provided by any)
- [ ] §K.3.5.3.6p1 (C11) — `sprintf_s` — full result incl. null must fit in `n` (truncation is a violation). — (not provided by any)
- [ ] §K.3.5.3.7p1 (C11) — `sscanf_s` — reads from a string; `%c`/`%s`/`%[` take pointer + `rsize_t`. — (not provided by any)
- [ ] §K.3.5.3.8p1 (C11) — `vfprintf_s` — va_list form of `fprintf_s`. — (not provided by any)
- [ ] §K.3.5.3.9p1 (C11) — `vfscanf_s` — va_list form of `fscanf_s`. — (not provided by any)
- [ ] §K.3.5.3.10p1 (C11) — `vprintf_s` — va_list form of `printf_s`. — (not provided by any)
- [ ] §K.3.5.3.11p1 (C11) — `vscanf_s` — va_list form of `scanf_s`. — (not provided by any)
- [ ] §K.3.5.3.12p1 (C11) — `vsnprintf_s` — va_list form of `snprintf_s`. — (not provided by any)
- [ ] §K.3.5.3.13p1 (C11) — `vsprintf_s` — va_list form of `sprintf_s`. — (not provided by any)
- [ ] §K.3.5.3.14p1 (C11) — `vsscanf_s` — va_list form of `sscanf_s`. — (not provided by any)
- [ ] §K.3.5.4.1p1 (C11) — `gets_s(char*s, rsize_t n)` — reads ≤`n−1` chars from `stdin`, discards newline, null-terminates. — (not provided by any)

#### §K.3.6 General utilities `<stdlib.h>`
- [ ] §K.3.6p1 (C11) — `<stdlib.h>` with the extension shall define `errno_t`, `rsize_t`, `constraint_handler_t`, and the functions below. — (not provided by any)
- [ ] §K.3.6.1.1p1 (C11) — `set_constraint_handler_s` — installs the handler, returns the previous; null installs the impl-defined default. — (not provided by any)
- [ ] §K.3.6.1.1p2 (C11) — Handler type `void(*)(const char*restrict, void*restrict, errno_t)`; only the most-recent handler is active. — (not provided by any)
- [ ] §K.3.6.1.2p1 (C11) — `abort_handler_s` — writes a message to `stderr` and calls `abort()`; does not return. — (not provided by any)
- [ ] §K.3.6.1.3p1 (C11) — `ignore_handler_s` — takes no action and returns. — (not provided by any)
- [ ] §K.3.6.2.1p1 (C11) — `getenv_s` — looks up `name`, copies into `value` if it fits, stores length in `*len`. — (not provided by any)
- [ ] §K.3.6.3.1p1 (C11) — `bsearch_s` — like `bsearch` with a `context` third compar arg. — (not provided by any)
- [ ] §K.3.6.3.2p1 (C11) — `qsort_s` — like `qsort` with a `context`; returns `errno_t`. — (not provided by any)
- [ ] §K.3.6.4.1p1 (C11) — `wctomb_s` — converts `wc` to multibyte in `s` (or queries shift-state when `s` null). — (not provided by any)
- [ ] §K.3.6.5.1p1 (C11) — `mbstowcs_s` — multibyte→wide, stores count in `*retval`. — (not provided by any)
- [ ] §K.3.6.5.2p1 (C11) — `wcstombs_s` — wide→multibyte, stores byte count in `*retval`. — (not provided by any)

#### §K.3.7 String handling `<string.h>`
- [ ] §K.3.7.1.1p1 (C11) — `memcpy_s` — `n`≤`s1max`, non-overlapping; clears `s1` on violation. — (not provided by any)
- [ ] §K.3.7.1.2p1 (C11) — `memmove_s` — `n`≤`s1max`, overlap permitted. — (not provided by any)
- [ ] §K.3.7.1.3p1 (C11) — `strcpy_s` — `s1max`>`strlen(s2)`, no overlap. — (not provided by any)
- [ ] §K.3.7.1.4p1 (C11) — `strncpy_s` — copies ≤`n`, always null-terminates. — (not provided by any)
- [ ] §K.3.7.2.1p1 (C11) — `strcat_s` — combined length incl. null ≤`s1max`. — (not provided by any)
- [ ] §K.3.7.2.2p1 (C11) — `strncat_s` — appends ≤`n` chars then a null. — (not provided by any)
- [ ] §K.3.7.3.1p1 (C11) — `strtok_s` — re-entrant tokenizer with caller state `ptr`/`s1max`. — (not provided by any)
- [ ] §K.3.7.4.1p1 (C11) — `memset_s` — store shall **not** be optimized away; sets min(`smax`,`n`) bytes even on violation. — (not provided by any)
- [ ] §K.3.7.4.2p1 (C11) — `strerror_s` — maps `errnum` to a message in `s`, truncating with a null and a truncation marker. — (not provided by any)
- [ ] §K.3.7.4.3p1 (C11) — `strerrorlen_s` — length of the message `strerror_s` would produce; no runtime-constraints. — (not provided by any)
- [ ] §K.3.7.4.4p1 (C11) — `strnlen_s` — 0 if `s` null, else chars before null but ≤`maxsize`; no runtime-constraints. — (not provided by any)

#### §K.3.8 Date and time `<time.h>`
- [ ] §K.3.8p1 (C11) — `<time.h>` with the extension shall define `errno_t`, `rsize_t`, and the `_s` time functions. — (not provided by any)
- [ ] §K.3.8.2.1p1 (C11) — `asctime_s(char*s, rsize_t maxsize, const struct tm*)` — `maxsize`≥26; writes the 26-char string. — (not provided by any)
- [ ] §K.3.8.2.2p1 (C11) — `ctime_s(char*s, rsize_t maxsize, const time_t*)` ≡ `asctime_s(s,maxsize,localtime_s(...))`. — (not provided by any)
- [ ] §K.3.8.2.3p1 (C11) — `gmtime_s(const time_t*restrict, struct tm*restrict)` — UTC; returns `result` or null. — (not provided by any)
- [ ] §K.3.8.2.4p1 (C11) — `localtime_s(const time_t*restrict, struct tm*restrict)` — local; returns `result` or null. — (not provided by any)

#### §K.3.9 Wide character `<wchar.h>`
- [ ] §K.3.9p1 (C11) — `<wchar.h>` with the extension shall define `errno_t`, `rsize_t`, `wchar_t`, and the wide `_s` functions; semantics parallel the narrow ones. — (not provided by any; mcc's bundled `runtime/include/uchar.h`/`wchar.h` provide no `_s` wide functions)
- [ ] §K.3.9.1.1p1 (C11) — `fwprintf_s` — wide `fprintf_s`. — (not provided by any)
- [ ] §K.3.9.1.2p1 (C11) — `fwscanf_s` — wide `fscanf_s`. — (not provided by any)
- [ ] §K.3.9.1.3p1 (C11) — `snwprintf_s` — wide `snprintf_s`. — (not provided by any)
- [ ] §K.3.9.1.4p1 (C11) — `swprintf_s` — wide `sprintf_s` (truncation is a violation). — (not provided by any)
- [ ] §K.3.9.1.5p1 (C11) — `swscanf_s` — wide `sscanf_s`. — (not provided by any)
- [ ] §K.3.9.1.6p1 (C11) — `vfwprintf_s` — va_list `fwprintf_s`. — (not provided by any)
- [ ] §K.3.9.1.7p1 (C11) — `vfwscanf_s` — va_list `fwscanf_s`. — (not provided by any)
- [ ] §K.3.9.1.8p1 (C11) — `vsnwprintf_s` — va_list `snwprintf_s`. — (not provided by any)
- [ ] §K.3.9.1.9p1 (C11) — `vswprintf_s` — va_list `swprintf_s`. — (not provided by any)
- [ ] §K.3.9.1.10p1 (C11) — `vswscanf_s` — va_list `swscanf_s`. — (not provided by any)
- [ ] §K.3.9.1.11p1 (C11) — `vwprintf_s` — va_list `wprintf_s`. — (not provided by any)
- [ ] §K.3.9.1.12p1 (C11) — `vwscanf_s` — va_list `wscanf_s`. — (not provided by any)
- [ ] §K.3.9.1.13p1 (C11) — `wprintf_s` — wide `printf_s`. — (not provided by any)
- [ ] §K.3.9.1.14p1 (C11) — `wscanf_s` — wide `scanf_s`. — (not provided by any)
- [ ] §K.3.9.2.1.1p1 (C11) — `wcscpy_s` — wide `strcpy_s`. — (not provided by any)
- [ ] §K.3.9.2.1.2p1 (C11) — `wcsncpy_s` — wide `strncpy_s`. — (not provided by any)
- [ ] §K.3.9.2.1.3p1 (C11) — `wmemcpy_s` — wide `memcpy_s` (counts in wide chars; no overlap). — (not provided by any)
- [ ] §K.3.9.2.1.4p1 (C11) — `wmemmove_s` — wide `memmove_s` (overlap permitted). — (not provided by any)
- [ ] §K.3.9.2.2.1p1 (C11) — `wcscat_s` — wide `strcat_s`. — (not provided by any)
- [ ] §K.3.9.2.2.2p1 (C11) — `wcsncat_s` — wide `strncat_s`. — (not provided by any)
- [ ] §K.3.9.2.3.1p1 (C11) — `wcstok_s` — wide `strtok_s` (re-entrant). — (not provided by any)
- [ ] §K.3.9.2.4.1p1 (C11) — `wcsnlen_s` — wide `strnlen_s`. — (not provided by any)
- [ ] §K.3.9.3.1.1p1 (C11) — `wcrtomb_s` — restartable wide→multibyte of one char. — (not provided by any)
- [ ] §K.3.9.3.2.1p1 (C11) — `mbsrtowcs_s` — restartable multibyte→wide string. — (not provided by any)
- [ ] §K.3.9.3.2.2p1 (C11) — `wcsrtombs_s` — restartable wide→multibyte string. — (not provided by any)
- [ ] §K.3.9 (C11, note) — Annex K adds no `_s` wide *date* functions; only the narrow `asctime_s`/`ctime_s` (+`gmtime_s`/`localtime_s`) exist; wide date uses non-`_s` `wcsftime`. — (spec note; not provided by any)


## Annexes C, D, E, H, I, J (sequence points, UCN ranges, limits, portability)

> mcc status: 285 TODO checkboxes in this section; 7 are mcc-gaps vs the gcc==clang consensus
> (7 partial `~`, all in Annex D). There are no *hard* conformance gaps: mcc now
> validates UCNs in identifiers against the §D.1/§D.2 range tables — it rejects a `̀` combining mark as the *initial* character of an
> identifier (D.2) and rejects out-of-range names such as `￿`, matching gcc/clang. The
> residual `~` rows are that mcc accepts the permitted D.1/D.2 blocks but does not enforce
> each as a closed set.
> Annex E (all `<limits.h>`/`<float.h>` minimum magnitudes, including the C11-added subnormal/
> decimal-digit/true-min macros) is fully satisfied. Annex I warnings are *informative/optional*, so
> mcc conforms even though it emits only the unknown-`#pragma` warning of the catalog. Annexes
> C, H, J are sequence-point / portability *catalogs* recapitulating normative text and are mostly
> not separately testable; the language-level sequence points and the testable `register`/`&`
> constraint match gcc==clang.

These annexes are the standard's cross-cutting catalogs. Annexes **C, D, E are normative**; Annexes **H, I, J are informative** (they recapitulate normative text stated elsewhere). Each checkbox below restates one catalog entry as a discrete requirement; the parenthetical `(6.x.y)` is the originating normative clause the annex points to. C11 (N1570) and C99 (N1256) agree on the *content* of these annexes except where tagged. The two main divergences are (a) clause-number drift — the C11 library moved from §7.19/§7.24/§7.20.5 to §7.21/§7.29/§7.22.5, and initializers moved from §6.7.8 to §6.7.9 — and (b) genuine list changes: Annex D's range list was wholly rewritten between C99 and C11, and Annex C/J absorbed the C11 "sequenced before" memory model and the C11-only library facilities (atomics, threads, `_Generic`, bounds-checked Annex K).

### §C Sequence points (normative)

> C11 restates Annex C under the "sequenced before"/"indeterminately sequenced" model of §5.1.2.3 and §6.5p2; the *enumerated* sequence points below are identical to C99, but the underlying relation changes from C99's "between the previous and next sequence point" to C11's partial-order sequencing. The C11-added sequence point "between the evaluation of a full declarator and any initialization" is folded into the full-expression list.

- [✓] §C — A sequence point occurs between the evaluations of the function designator and the actual arguments in a function call and the actual call itself (6.5.2.2). — mcc:✓ gcc:✓ clang:✓
- [✓] §C — A sequence point occurs between the evaluations of the first and second operands of the logical-AND operator `&&` (6.5.13). — mcc:✓ gcc:✓ clang:✓ (short-circuit suppresses RHS side effect, verified at runtime)
- [✓] §C — A sequence point occurs between the evaluations of the first and second operands of the logical-OR operator `||` (6.5.14). — mcc:✓ gcc:✓ clang:✓ (RHS side effect suppressed when LHS true, verified)
- [✓] §C — A sequence point occurs between the evaluations of the first and second operands of the comma operator `,` (6.5.17). — mcc:✓ gcc:✓ clang:✓ (LHS side effect committed before RHS, verified)
- [✓] §C — A sequence point occurs between the evaluation of the first operand of the conditional operator `?:` and whichever of the second and third operands is evaluated (6.5.15). — mcc:✓ gcc:✓ clang:✓ (only the selected arm's side effect occurs, verified)
- [✓] §C — A sequence point occurs between the evaluation of a full expression and the evaluation of the next full expression to be evaluated; the full expressions are those enumerated immediately below (6.8). — mcc:✓ gcc:✓ clang:✓
- §C — A full expression is an initializer that is not part of a compound literal (C99 6.7.8 / C11 6.7.9). — (spec; not separately testable)
- §C — A full expression is the expression in an expression statement (6.8.3). — (spec; not separately testable)
- §C — A full expression is the controlling expression of a selection statement (`if` or `switch`) (6.8.4). — (spec; not separately testable)
- §C — A full expression is the controlling expression of a `while` or `do` statement (6.8.5). — (spec; not separately testable)
- §C — A full expression is each of the three (optional) expressions of a `for` statement (6.8.5.3). — (spec; not separately testable)
- §C — A full expression is the (optional) expression in a `return` statement (6.8.6.4). — (spec; not separately testable)
- [✓] §C — A sequence point occurs immediately before a library function returns (7.1.4). — mcc:✓ gcc:✓ clang:✓ (provided by the shared host libc; not a codegen choice)
- §C — A sequence point occurs after the actions associated with each formatted input/output function conversion specifier (C99 7.19.6, 7.24.2 / C11 7.21.6, 7.29.2). — (spec; library-internal, not separately testable)
- §C — A sequence point occurs immediately before and immediately after each call to a comparison function passed to `qsort`/`bsearch`, and also between any call to the comparison function and any movement of the objects passed as arguments to that call (C99 7.20.5 / C11 7.22.5). — (spec; library-internal, not separately testable)

### §D Universal character names for identifiers — C99 §D / C11 §D.1, §D.2 (normative)

> The range *lists* differ materially between standards. **C99 §D** enumerates an older, per-script catalog (Latin/Greek/Cyrillic/Armenian/Hebrew/Arabic/Devanagari/… ranges) plus separate "special characters" and "digits" sublists. **C11** rewrote the annex around contiguous Unicode blocks and split it into **§D.1 "Ranges of characters allowed"** and **§D.2 "Ranges of characters disallowed initially."** The block ranges below are the **C11 §D.1/§D.2** values; under C99 the same requirement (6.4.2.1) holds but against the older list.
>
> **mcc gap:** mcc does *not* validate UCNs in identifiers against these tables. It accepts the permitted ranges (✓) but also accepts UCNs in *no* permitted range (e.g. `￿`) and accepts a §D.2 combining mark as the *initial* character — both of which gcc and clang reject. mcc does still reject UCNs that name a basic-source-character (e.g. `A` = 'A'), per 6.4.3.

- [✓] §D — A universal character name appearing in an identifier shall designate a character whose short identifier (ISO/IEC 10646) lies within one of the permitted ranges enumerated below (6.4.2.1). — mcc:✓ gcc:✓ clang:✓ (mcc rejects an out-of-range identifier character such as `￿` — "universal character ￿ is not valid in an identifier" — matching gcc/clang; raw-UTF-8 identifiers are validated against Annex D.1/D.2 like the `\u` escape form)
- [~] §D.1 (C11) — Latin block permitted: 00A8, 00AA, 00AD, 00AF, 00B2–00B5, 00B7–00BA, 00BC–00BE, 00C0–00D6, 00D8–00F6, 00F8–00FF. — mcc:~ gcc:✓ clang:✓ (mcc accepts these but does not enforce the block as a closed set)
- [~] §D.1 (C11) — General-extension blocks permitted: 0100–167F, 1681–180D, 180F–1FFF. — mcc:~ gcc:✓ clang:✓ (accepted; not enforced)
- [~] §D.1 (C11) — Format/punctuation-adjacent blocks permitted: 200B–200D, 202A–202E, 203F–2040, 2054, 2060–206F. — mcc:~ gcc:✓ clang:✓ (accepted; not enforced)
- [~] §D.1 (C11) — Symbol/CJK-radical blocks permitted: 2070–218F, 2460–24FF, 2776–2793, 2C00–2DFF, 2E80–2FFF. — mcc:~ gcc:✓ clang:✓ (accepted; not enforced)
- [~] §D.1 (C11) — CJK punctuation blocks permitted: 3004–3007, 3021–302F, 3031–303F. — mcc:~ gcc:✓ clang:✓ (accepted; not enforced)
- [~] §D.1 (C11) — Hangul/CJK ideograph block permitted: 3040–D7FF. — mcc:~ gcc:✓ clang:✓ (accepted; not enforced)
- [~] §D.1 (C11) — Compatibility/presentation blocks permitted: F900–FD3D, FD40–FDCF, FDF0–FE44, FE47–FFFD. — mcc:~ gcc:✓ clang:✓ (accepted; not enforced)
- [✓] §D.1 (C11) — Supplementary-plane blocks permitted: 10000–1FFFD, 20000–2FFFD, 30000–3FFFD, 40000–4FFFD, 50000–5FFFD, 60000–6FFFD, 70000–7FFFD, 80000–8FFFD, 90000–9FFFD, A0000–AFFFD, B0000–BFFFD, C0000–CFFFD, D0000–DFFFD, E0000–EFFFD. — mcc:✓ gcc:✓ clang:✓ (`\U00010000` accepted by all three)
- [✓] §D.2 (C11) — Combining-mark ranges 0300–036F, 1DC0–1DFF, 20D0–20FF, FE20–FE2F shall not appear as the *initial* character of an identifier (allowed only in non-initial position). — mcc:✓ gcc:✓ clang:✓ (mcc rejects `̀` as an identifier's first character — "universal character ̀ is not valid as the first character of an identifier" — matching gcc, which errors "not valid at the start of an identifier", and clang, which errors. Non-initial `à` is accepted by all three; raw-UTF-8 identifiers are validated against Annex D.1/D.2 like the `\u` escape form.)
- [✓] §D (C99-only) — Under C99 the allowed list is the older per-script enumeration (e.g. Latin 00C0–00D6/00D8–00F6/00F8–01F5/…, Greek 0384/0388–03CE/…, Cyrillic 0401–04F9, plus separate "special characters" 00B5/00B7/02D0/02D1/0387/0640/0E46/0EC6/3005/3031–3035/309D–309E/30FC–30FE and "digits" 0660–0669/06F0–06F9/0966–096F/…); an implementation conforming to C99 must validate against that table rather than the C11 blocks (6.4.2.1). — mcc:✓ gcc:✓ clang:✓ (mcc validates identifier characters against the Annex D.1/D.2 (C11) blocks in both C99 and C11 modes, matching gcc/clang, which likewise apply the C11 table regardless of `-std`)
- [✓] §D — *(gap)* A `\uXXXX` escape naming a basic-source-character or a value in the range D800–DFFF or below 00A0 (other than the explicitly named control points) is rejected as a UCN that cannot designate an identifier character (6.4.3p2). — mcc:✓ gcc:✓ clang:✓ (all three reject `A`)

### §E Implementation limits (normative)

> Annex E's *minimum-magnitude* table is unchanged from C99 to C11. C11 adds new `<float.h>` macros (subnormal flags, decimal-digit counts, true-min) but assigns them **no** Annex-E minimum magnitudes. All rows below were verified by static-array-size probes against mcc's bundled `<limits.h>`/`<float.h>`.

- [✓] §E — The values in `<limits.h>` and `<float.h>` shall be replaced by implementation-defined constant expressions whose magnitudes (absolute values) are greater than or equal to those shown, with the same sign (5.2.4.2). — mcc:✓ gcc:✓ clang:✓
- [✓] §E — `CHAR_BIT` shall be ≥ 8 (5.2.4.2.1). — mcc:✓ gcc:✓ clang:✓ (mcc: 8 via `__CHAR_BIT__`)
- [✓] §E — `SCHAR_MIN` shall be ≤ −127, `SCHAR_MAX` ≥ +127, `UCHAR_MAX` ≥ 255 (5.2.4.2.1). — mcc:✓ gcc:✓ clang:✓ (−128/127/255)
- [✓] §E — `CHAR_MIN`/`CHAR_MAX` shall equal the `signed char` limits or the `unsigned char` limits according to plain-`char` signedness (no fixed magnitude of their own) (5.2.4.2.1). — mcc:✓ gcc:✓ clang:✓ (mcc selects via `'\xff' > 0` test)
- [✓] §E — `MB_LEN_MAX` shall be ≥ 1 (5.2.4.2.1). — mcc:✓ gcc:✓ clang:✓ (mcc: 16)
- [✓] §E — `SHRT_MIN` shall be ≤ −32767, `SHRT_MAX` ≥ +32767, `USHRT_MAX` ≥ 65535 (5.2.4.2.1). — mcc:✓ gcc:✓ clang:✓ (−32768/32767/65535)
- [✓] §E — `INT_MIN` shall be ≤ −32767, `INT_MAX` ≥ +32767, `UINT_MAX` ≥ 65535 (5.2.4.2.1). — mcc:✓ gcc:✓ clang:✓
- [✓] §E — `LONG_MIN` shall be ≤ −2147483647, `LONG_MAX` ≥ +2147483647, `ULONG_MAX` ≥ 4294967295 (5.2.4.2.1). — mcc:✓ gcc:✓ clang:✓
- [✓] §E — `LLONG_MIN` shall be ≤ −9223372036854775807, `LLONG_MAX` ≥ +9223372036854775807, `ULLONG_MAX` ≥ 18446744073709551615 (5.2.4.2.1). — mcc:✓ gcc:✓ clang:✓
- [✓] §E — `FLT_RADIX` shall be ≥ 2 (5.2.4.2.2). — mcc:✓ gcc:✓ clang:✓ (mcc: 2)
- [✓] §E — `DECIMAL_DIG` shall be ≥ 10 (C99 addition) (5.2.4.2.2). — mcc:✓ gcc:✓ clang:✓ (mcc: 21 on x86-64)
- [✓] §E — `FLT_DIG` ≥ 6; `DBL_DIG` ≥ 10; `LDBL_DIG` ≥ 10 (5.2.4.2.2). — mcc:✓ gcc:✓ clang:✓ (6/15/18)
- [✓] §E — `FLT_MIN_10_EXP`, `DBL_MIN_10_EXP`, `LDBL_MIN_10_EXP` shall each be ≤ −37 (5.2.4.2.2). — mcc:✓ gcc:✓ clang:✓
- [✓] §E — `FLT_MAX_10_EXP`, `DBL_MAX_10_EXP`, `LDBL_MAX_10_EXP` shall each be ≥ +37 (5.2.4.2.2). — mcc:✓ gcc:✓ clang:✓
- [✓] §E — `FLT_MAX`, `DBL_MAX`, `LDBL_MAX` shall each be ≥ 1E+37 (5.2.4.2.2). — mcc:✓ gcc:✓ clang:✓
- [✓] §E — `FLT_EPSILON` ≤ 1E-5; `DBL_EPSILON` ≤ 1E-9; `LDBL_EPSILON` ≤ 1E-9 (5.2.4.2.2). — mcc:✓ gcc:✓ clang:✓
- [✓] §E — `FLT_MIN`, `DBL_MIN`, `LDBL_MIN` shall each be ≤ 1E-37 (5.2.4.2.2). — mcc:✓ gcc:✓ clang:✓
- [✓] §E — `FLT_MANT_DIG`/`FLT_MIN_EXP`/`FLT_MAX_EXP` (and their `DBL_`/`LDBL_` analogues) appear in Annex E but carry **no** fixed minimum magnitude (5.2.4.2.2). — mcc:✓ gcc:✓ clang:✓ (all defined; 24/-125/128, 53/-1021/1024, 64/-16381/16384)
- [✓] §E (C11) — The C11-added `<float.h>` macros `FLT_HAS_SUBNORM`/`DBL_HAS_SUBNORM`/`LDBL_HAS_SUBNORM`, `FLT_DECIMAL_DIG`/`DBL_DECIMAL_DIG`/`LDBL_DECIMAL_DIG`, and `FLT_TRUE_MIN`/`DBL_TRUE_MIN`/`LDBL_TRUE_MIN` are defined by 5.2.4.2.2 but are **not** assigned Annex-E minimum magnitudes. — mcc:✓ gcc:✓ clang:✓ (all nine present in mcc's `<float.h>`; HAS_SUBNORM=1, DECIMAL_DIG 9/17/21, TRUE_MIN denormals)
- [✓] §E — *(gap)* `FLT_ROUNDS` and `FLT_EVAL_METHOD` are defined in `<float.h>` (5.2.4.2.2); they have no Annex-E magnitude but are required members. — mcc:✓ gcc:✓ clang:✓ (mcc: `FLT_ROUNDS`=1, `FLT_EVAL_METHOD`=0 on x86-64 / 2 on i386)

### §H Language-independent arithmetic — relation to LIA-1 (informative)

> Annex H is *informative*; it binds nothing beyond the normative clauses it references, so every row is "(spec; not separately testable)". mcc's underlying arithmetic (two's-complement integers, IEEE-754 binary float, wraparound unsigned) realizes the LIA-1 mapping the same way gcc/clang do.

- §H.1 — Annex H documents the relationship between this standard and ISO/IEC 10967-1 (LIA-1); it is informative and binds nothing beyond the normative clauses it references (H.1). — (spec; not separately testable)
- §H.2 — The C arithmetic types are mapped to the LIA-1 datatypes and operations (H.2). — (spec; not separately testable)
- §H.2.1 — The C `_Bool`/`bool` type corresponds to the LIA-1 boolean datatype (H.2.1). — (spec; not separately testable)
- §H.2.2 — The C standard and extended integer types model LIA-1 integer datatypes; the LIA-1 `bounded`, `modulo`, `minint`, `maxint` parameters map to the `<limits.h>` values (H.2.2). — (spec; not separately testable)
- §H.2.2 — LIA-1 `integer_overflow` corresponds to C's undefined behavior on signed integer overflow; unsigned types provide LIA-1 `modulo` (non-trapping, wraparound) arithmetic (H.2.2). — (spec; not separately testable)
- §H.2.3 — The C floating types model LIA-1 floating-point datatypes; the LIA-1 parameters `r` (radix), `p` (precision), `emin`, `emax` map to `FLT_RADIX`, `*_MANT_DIG`, `*_MIN_EXP`, `*_MAX_EXP` (H.2.3). — (spec; not separately testable)
- §H.2.3.1 — LIA-1 floating overflow/underflow notifications correspond to C's `<fenv.h>` exception flags `FE_OVERFLOW`/`FE_UNDERFLOW` (H.2.3.1). — (spec; not separately testable)
- §H.2.4 — C's integer↔integer, integer↔floating, and floating↔floating conversions correspond to the LIA-1 conversion operations and their notifications (H.2.4). — (spec; not separately testable)
- §H.3 — Annex H documents the LIA-1 notification mechanisms and how a C implementation realizes them (H.3). — (spec; not separately testable)
- §H.3.1 — LIA-1 "recording of indicators" maps to the `<fenv.h>` floating-point status flags; "alteration of control flow"/trap maps to signals such as `SIGFPE` (H.3.1). — (spec; not separately testable)
- §H.3.2 — Changing the rounding/notification environment is performed via the `<fenv.h>` functions in conjunction with `#pragma STDC FENV_ACCESS` (H.3.2). — (spec; not separately testable)

### §I Common warnings (informative)

> Annex I is purely *informative*: **none** of these warnings is required, and an implementation may also warn in any other situation it chooses. mcc therefore conforms on every row regardless of whether it emits the warning. Probed under `-Wall -Wextra`: of this catalog mcc emits only the unknown-`#pragma` warning; gcc and clang additionally emit the unused-object, `/*`-in-comment, and partly-bracketed-initializer warnings.

- [~] §I — A new struct/union type first appearing in a parameter declaration (whose scope ends at the end of the function declarator) may be diagnosed (6.7.2.3, 6.9.1). — mcc:~ (informative/optional; mcc does not warn, gcc/clang do)
- [~] §I — A block that initializes an object of automatic storage duration is jumped into, bypassing the initializer (6.8). — mcc:~ (informative/optional; mcc does not warn)
- [~] §I — An implicit narrowing conversion is encountered (e.g. `long int`→`int`, `double`→`int`, or `void *`→a pointer to any type other than a character type) (6.3, C99 6.7.8 / C11 6.7.9). — mcc:~ (informative/optional; mcc does not warn without `-Wconversion`-style flags)
- [~] §I — A hexadecimal floating constant cannot be represented exactly in its evaluation format (6.4.4.2). — mcc:~ (informative/optional; not emitted)
- [~] §I — An integer character constant contains more than one character, or a wide character constant contains more than one multibyte character (6.4.4.4). — mcc:~ (informative/optional; gcc/clang warn under `-Wmultichar`, mcc accepts silently)
- [~] §I — The characters `/*` are found within a comment (6.4.9). — mcc:~ gcc:✓(warns) clang:✓(warns) (informative/optional; mcc does not warn)
- [~] §I — An "unordered" binary operator (one not `,`, `&&`, or `||`) has a side effect to an lvalue in one operand and a side effect on, or an access to, the identical lvalue in the other operand (6.5). — mcc:~ (informative/optional; gcc/clang warn `-Wsequence-point`, mcc does not)
- [~] §I — A function is called but no prototype has been supplied (6.5.2.2). — mcc:~ (informative/optional; emitted under `-Wmissing-prototypes`-style flags by gcc/clang; not by mcc)
- [~] §I — The arguments in a function call do not agree in number and type with those of the parameters of a (non-prototype) function definition (6.5.2.2). — mcc:~ (informative/optional)
- [~] §I — An object is defined but not used (6.9). — mcc:~ gcc:✓(warns) clang:✓(warns) (informative/optional; mcc emits no unused-variable warning)
- [~] §I — A value is given to an object of enumerated type other than via an enumeration constant of that type, an object of the same enumerated type, or a function returning that type (6.7.2.2). — mcc:~ (informative/optional)
- [~] §I — An aggregate has a partly bracketed initialization (C99 6.7.8 / C11 6.7.9). — mcc:~ gcc:✓(warns) clang:✓(warns) (informative/optional; mcc accepts `int a[2][2]={1,2,3,4}` with no warning)
- [~] §I — A statement cannot be reached (6.8). — mcc:~ (informative/optional; none of the three warn by default on `return;return;`)
- [~] §I — A statement has no apparent effect (6.8). — mcc:~ (informative/optional)
- [~] §I — A constant expression is used as the controlling expression of a selection statement (6.8.4). — mcc:~ (informative/optional; none of the three warn on `if(1)`)
- [~] §I — An incorrectly formed preprocessing group is encountered while skipping a conditionally excluded preprocessing group (6.10.1). — mcc:~ (informative/optional)
- [✓] §I — An unrecognized `#pragma` directive is encountered (6.10.6). — mcc:✓(warns) gcc:✓(warns) clang:✓(warns) (mcc emits "#pragma … ignored", matching gcc/clang `-Wunknown-pragmas`)

### §J.1 Unspecified behavior (informative)

> Annex J.1 is an *informative* catalog of unspecified-behavior points; the listed behaviors are by definition not constrained, so a conforming implementation may choose any of the allowed alternatives. Not separately testable as conformance — each row is a documentation pointer. mcc behaves within the allowed latitude in every case (verified incidentally: evaluation-order, padding, and zero-size-allocation behaviors all fall inside the permitted set).

- [ ] §J.1 — The manner of, and whether any, static initialization of an object happens before `main` is entered, in cases not otherwise specified (5.1.2). — (spec; unspecified-behavior catalog)
- [ ] §J.1 — The order in which two declarators referring to the same object are processed (where not otherwise specified) (5.1.2). — (spec; unspecified-behavior catalog)
- [ ] §J.1 — The value of bytes corresponding to any padding bits in an object representation (6.2.6.1). — (spec; unspecified-behavior catalog)
- [ ] §J.1 — The values of bytes corresponding to union members other than the one most recently stored into (6.2.6.1). — (spec; unspecified-behavior catalog)
- [ ] §J.1 — The representation actually used when a value has more than one object representation (e.g. a value with multiple representations) (6.2.6.1). — (spec; unspecified-behavior catalog)
- [ ] §J.1 — Whether two string literals (or two compatible-element compound literals) result in distinct or shared/overlapping storage (6.4.5). — (spec; unspecified-behavior catalog) — mcc pools identical string literals like gcc/clang
- [ ] §J.1 — The order in which subexpressions are evaluated and the order in which side effects take place, except as specified for `&&`, `||`, `?:`, `,`, and function-call sequencing (6.5). — (spec; unspecified-behavior catalog)
- [ ] §J.1 — The order in which the function designator, the arguments, and subexpressions within the arguments are evaluated in a function call (6.5.2.2). — (spec; unspecified-behavior catalog)
- [ ] §J.1 — The order of side effects among compound-literal initializer-list expressions (6.5.2.5). — (spec; unspecified-behavior catalog)
- [ ] §J.1 — The order in which the operands of an assignment operator are evaluated (6.5.16). — (spec; unspecified-behavior catalog)
- [ ] §J.1 — The alignment of the addressable storage unit allocated to hold a bit-field (6.7.2.1). — (spec; unspecified-behavior catalog)
- [ ] §J.1 — Whether a call to an inline function uses the inline definition or the external definition of the function (6.7.4). — (spec; unspecified-behavior catalog)
- [ ] §J.1 — Whether, when the operand of `sizeof` is not a VLA, the operand's size expression is evaluated (it does not affect the result) (C99 6.7.6.2 / 6.5.3.4). — (spec; unspecified-behavior catalog)
- [ ] §J.1 — The order in which any side effects occur among the initialization-list expressions in an aggregate initializer (C99 6.7.8 / C11 6.7.9). — (spec; unspecified-behavior catalog)
- [ ] §J.1 — The layout of storage for function parameters (6.9.1). — (spec; unspecified-behavior catalog)
- [ ] §J.1 — Whether and how a function makes use of the register storage suggested by `register` (6.7.1) — *the effectiveness of the hint is implementation-defined per J.3.8; whether the object actually occupies a register is unspecified*. — (spec; unspecified-behavior catalog)
- [ ] §J.1 — When a fully expanded macro replacement contains a function-like macro name, whether it is treated as a macro invocation (the order/extent of re-expansion across nesting) (6.10.3.4). — (spec; unspecified-behavior catalog)
- [ ] §J.1 — The order in which `#` and `##` operations are evaluated during macro replacement (6.10.3.2, 6.10.3.3). — (spec; unspecified-behavior catalog)
- [ ] §J.1 — The state of the floating-point status flags when execution passes from code that ignores them to code that observes them, absent `FENV_ACCESS` (7.6.1). — (spec; unspecified-behavior catalog)
- [ ] §J.1 — The order and contiguity of storage allocated by successive calls to `calloc`/`malloc`/`realloc` (C99 7.20.3 / C11 7.22.3). — (spec; library/host-allocator behavior)
- [ ] §J.1 — The amount of storage allocated by a memory-management request of zero size (a null or a unique non-dereferenceable pointer may be returned) (C99 7.20.3 / 7.22.3). — (spec; library/host-allocator behavior)
- [ ] §J.1 — Which of two elements that compare equal is matched by `bsearch`, and the relative order in the array of two `qsort` elements that compare equal (C99 7.20.5 / 7.22.5). — (spec; library/host behavior)
- [ ] §J.1 — Whether `errno` is a macro or an identifier declared with external linkage (7.5). — (spec; host `<errno.h>` behavior — a macro on glibc)
- [ ] §J.1 — The contents of the array pointed to by `setbuf`/`setvbuf`, the value of `argv`-string contents after `main` returns, and other locale/time-dependent string contents, within the bounds the standard allows (miscellaneous library entries enumerated in J.1). — (spec; library/host behavior)

### §J.2 Undefined behavior (informative)

> Annex J.2 is an *informative* enumeration of behaviors made undefined by the normative clauses it cites; it is not guaranteed exhaustive. Because the behavior is *undefined*, an implementation conforms whatever it does — so these are catalog rows, not pass/fail tests, except where the cited normative clause *also* imposes a constraint (those few are probed below). C11 entries marked **(C11)** cover atomics/threads/`_Generic`/Annex K and the "unsequenced side effect" reformulation of the C99 "modified more than once between sequence points" rule.

- [ ] §J.2 — A "shall"/"shall not" requirement that appears outside a constraint is violated (4p2). — (spec; UB catalog)
- [ ] §J.2 — A nonempty source file does not end in a new-line not immediately preceded by a backslash, or ends with a partial preprocessing token or comment (5.1.1.2). — (spec; UB catalog)
- [ ] §J.2 — Token concatenation by backslash-newline splicing produces a universal character name (5.1.1.2). — (spec; UB catalog)
- [ ] §J.2 — A program in a hosted environment does not define `main` with one of the permitted signatures (`int main(void)`, `int main(int, char *[])`, or an implementation-defined form) (5.1.2.2.1). — (spec; UB catalog)
- [ ] §J.2 — A character not in the basic source character set occurs except in an identifier, comment, string/character constant, header name, or where the standard permits (5.2.1). — (spec; UB catalog)
- [ ] §J.2 — An identifier, comment, string literal, character constant, or header name contains an invalid multibyte character or does not begin and end in the initial shift state (5.2.1.2). — (spec; UB catalog)
- [ ] §J.2 — The same identifier has both internal and external linkage in the same translation unit (6.2.2). — (spec; UB catalog)
- [ ] §J.2 — An object is referred to outside of its lifetime (6.2.4). — (spec; UB catalog)
- [ ] §J.2 — The value of a pointer to an object whose lifetime has ended is used (6.2.4). — (spec; UB catalog)
- [ ] §J.2 — The value of an object with automatic storage duration is used while it is indeterminate (6.2.4, C99 6.7.8/6.8 / C11 6.7.9/6.8). — (spec; UB catalog)
- [ ] §J.2 — A trap representation is read by an lvalue expression that does not have character type (6.2.6.1). — (spec; UB catalog)
- [ ] §J.2 — A trap representation is produced by a side effect that modifies all or part of an object via a non-character-type lvalue (6.2.6.1). — (spec; UB catalog)
- [ ] §J.2 — Two declarations of the same object or function specify types that are not compatible (6.2.7). — (spec; UB catalog) — mcc/gcc/clang all diagnose the in-TU case as a constraint
- [ ] §J.2 — A program attempts to modify a string literal (6.4.5). — (spec; UB catalog) — mcc places literals in read-only storage like gcc/clang
- [ ] §J.2 — The characters `'`, `\`, `"`, `//`, or `/*` occur between the `<` and `>` delimiters or between the `"` delimiters of a header-name preprocessing token (6.4.7). — (spec; UB catalog)
- [ ] §J.2 — A side effect on a scalar object is unsequenced relative to either a different side effect on the same scalar object or a value computation using that object **(C11 6.5p2)**; equivalently in C99, an object is modified more than once, or modified and read, between two adjacent sequence points (C99 6.5p2). — (spec; UB catalog; gcc/clang warn `-Wsequence-point`, mcc does not)
- [ ] §J.2 — An exceptional condition occurs during the evaluation of an expression (e.g. signed integer overflow) (6.5). — (spec; UB catalog)
- [ ] §J.2 — An object's stored value is accessed through an lvalue of a type not in the permitted aliasing list for the object's effective type (6.5). — (spec; UB catalog)
- [ ] §J.2 — An argument to a function has a type incompatible with the corresponding parameter of a function defined with a prototype (6.5.2.2). — (spec; UB catalog)
- [ ] §J.2 — A function is defined with a type incompatible with the type (derived from its declaration) pointed to by the expression that calls it (6.5.2.2). — (spec; UB catalog)
- [ ] §J.2 (C11) — A member of an atomic structure or union is accessed directly (6.5.2.3). — (spec; UB catalog)
- [ ] §J.2 — The operand of `sizeof` or (C11) `_Alignof` is a function type, an incomplete type, the parenthesized name of such a type, or a bit-field member, used where prohibited (6.5.3.4, C11 6.5.3.4). — (spec; constraint, diagnosed by all three)
- [ ] §J.2 — Pointers that do not point into, or just one past the end of, the same array object are compared with relational operators (6.5.8). — (spec; UB catalog)
- [ ] §J.2 — An object is assigned to an inexactly overlapping object, or to an exactly overlapping object of incompatible type (6.5.16.1). — (spec; UB catalog)
- [ ] §J.2 — An expression is shifted by a negative number, or by an amount ≥ the width of the promoted left operand (6.5.7). — (spec; UB catalog; gcc/clang warn on constant out-of-range shift, mcc may not)
- [ ] §J.2 — Integer division or remainder is computed with a second operand of zero (6.5.5). — (spec; UB catalog; gcc/clang warn `-Wdiv-by-zero` on constant 0)
- [ ] §J.2 — Addition/subtraction of an integer and a pointer produces a result that does not point into, or just one past the end of, the same array object (6.5.6). — (spec; UB catalog)
- [ ] §J.2 — Pointer arithmetic is performed on a pointer that does not point into, or just one past, an array object (6.5.6). — (spec; UB catalog)
- [ ] §J.2 — An array subscript is out of range, even where an object is seemingly accessible with the given subscript (6.5.6, 6.5.2.1). — (spec; UB catalog)
- [ ] §J.2 — The unary `*` operator is applied to a pointer with an invalid value (null, misaligned for the referenced type, or pointing past the object) (6.5.3.2). — (spec; UB catalog)
- [ ] §J.2 — Conversion to or from an integer type produces a value outside the range that can be represented (6.3.1.4). — (spec; UB catalog)
- [ ] §J.2 — Conversion between two pointer types produces a result that is incorrectly aligned (6.3.2.3). — (spec; UB catalog)
- [ ] §J.2 — A pointer is converted to other than an integer or pointer type (6.3.2.3). — (spec; constraint, diagnosed by all three)
- [ ] §J.2 — Demotion of a floating value to a narrower floating type produces a value outside the representable range (6.3.1.5). — (spec; UB catalog)
- [ ] §J.2 — An lvalue does not designate an object when evaluated (6.3.2.1). — (spec; UB catalog)
- [ ] §J.2 — A non-array lvalue with incomplete type is used in a context that requires the value of the designated object (6.3.2.1). — (spec; constraint, diagnosed by all three)
- [✓] §J.2 — An lvalue designating an object of automatic storage duration that could have been declared `register` is used in a context that requires the address-of operator `&` (6.3.2.1). — mcc:✓ gcc:✓ clang:✓ (this *is* a constraint for `register`-declared objects: all three error "address of register variable requested"; verified)
- [ ] §J.2 — An attempt is made to modify, through a non-const lvalue, an object defined with a const-qualified type (6.7.3). — (spec; UB catalog)
- [ ] §J.2 — An attempt is made to refer to, through a non-volatile lvalue, an object defined with a volatile-qualified type (6.7.3). — (spec; UB catalog)
- [ ] §J.2 — A `restrict`-qualified pointer's aliasing guarantee is violated (a restricted object is modified via one pointer and accessed via another based pointer) (6.7.3.1). — (spec; UB catalog)
- [ ] §J.2 — A function with external linkage is declared `inline` (without `extern`) but no external definition exists in the translation unit, and the function is used (6.7.4). — (spec; UB catalog)
- [ ] §J.2 — The value of an unnamed member of a structure or union is used (C99 6.7.8 / C11 6.7.9). — (spec; UB catalog)
- [ ] §J.2 — A non-constant initializer is used for an object that requires a constant one (static storage duration) (C99 6.7.8 / C11 6.7.9). — (spec; constraint, diagnosed by all three)
- [ ] §J.2 — An identifier with internal linkage and incomplete type is declared by a tentative definition not completed by the end of the translation unit (6.9.2). — (spec; UB catalog)
- [ ] §J.2 — A `goto`, `switch`, or fall-through jumps into the scope of an identifier having a variably modified type / VLA from outside that scope (6.8.6.1). — (spec; constraint for VM jumps, diagnosed by all three)
- [ ] §J.2 — A function is called with a number of arguments not equal to the number of parameters (no prototype in scope) (6.5.2.2, 6.9.1). — (spec; UB catalog)
- [ ] §J.2 — A `return` statement with an expression executes in a `void` function, or a `return` without an expression executes in a non-`void` function and the caller uses the value (6.8.6.4). — (spec; constraint for the `void`+expr case, diagnosed by all three)
- [ ] §J.2 — Execution reaches the closing `}` of a non-`void` function and the caller uses the (nonexistent) returned value (6.9.1). — (spec; UB catalog; gcc/clang warn `-Wreturn-type`)
- [ ] §J.2 — `va_arg` is invoked with a type incompatible with the type of the actual next argument (after default promotions), or is invoked when there is no actual next argument (C99 7.16.1.1 / C11 7.16). — (spec; UB catalog)
- [ ] §J.2 — A `va_start`/`va_copy` is not matched by a corresponding `va_end` in the same function, or `va_arg` is used on a `va_list` after `va_end` (7.16). — (spec; UB catalog)
- [ ] §J.2 — `va_start` is used with a `parmN` declared `register`, of function or array type, or of a type incompatible with the default argument promotions (7.16.1.4). — (spec; UB catalog; gcc/clang warn `-Wvarargs`, mcc accepts silently)
- [ ] §J.2 — A library-function argument has an invalid value or a type inappropriate for the parameter (7.1.4). — (spec; UB catalog)
- [ ] §J.2 — A library-function pointer argument does not point to a valid object or range (e.g. a null pointer where one is not permitted) (7.1.4). — (spec; UB catalog)
- [ ] §J.2 — The `assert` macro is suppressed (to access an actual function) or is otherwise redefined (7.2). — (spec; UB catalog)
- [ ] §J.2 — A standard-library reserved identifier or macro name is the subject of a `#define` or `#undef`, or a file with the same name as a standard header is supplied (7.1.3, 7.1.4). — (spec; UB catalog)
- [ ] §J.2 — A conversion-state object (`mbstate_t`) is used by a function with a different conversion state or in an indeterminate state (C99 7.24.6 / C11 7.28). — (spec; UB catalog)
- [ ] §J.2 — The array set as a stream buffer by `setvbuf` is accessed while it is in use (C99 7.19.5.6 / C11 7.21.5.6). — (spec; UB catalog)
- [ ] §J.2 — A `FILE` object's pointer is copied/used after the lifetime of the `FILE` object ends (C99 7.19.3 / C11 7.21.3). — (spec; UB catalog)
- [ ] §J.2 — On an update stream, an output, input, or file-positioning operation is performed without the required intervening operation after switching direction (C99 7.19.5.3 / C11 7.21.5.3). — (spec; UB catalog)
- [ ] §J.2 — A `printf`/`scanf` conversion specification is invalid, an argument has the wrong type, or there are insufficient arguments for the format (C99 7.19.6 / C11 7.21.6). — (spec; UB catalog; gcc/clang warn `-Wformat`, mcc does not)
- [ ] §J.2 — A `%n` directive's argument is not a valid pointer to an integer of the matching type (C99 7.19.6 / C11 7.21.6). — (spec; UB catalog)
- [ ] §J.2 — `realloc` or `free` is passed a pointer not previously returned by `malloc`/`calloc`/`realloc`, or already freed (C99 7.20.3 / C11 7.22.3). — (spec; UB catalog)
- [ ] §J.2 — The value of a pointer that refers to space deallocated by `free`/`realloc` is used (C99 7.20.3 / C11 7.22.3). — (spec; UB catalog)
- [ ] §J.2 — `longjmp` is invoked from a nested signal handler, or to an environment whose function has terminated, or in a way that violates the `setjmp`/`longjmp` rules (7.13.2.1). — (spec; UB catalog)
- [ ] §J.2 — A signal handler returns when the signal was `SIGFPE`, `SIGILL`, `SIGSEGV`, or any other implementation-defined value corresponding to a computational exception (7.14.1.1). — (spec; UB catalog)
- [ ] §J.2 — A signal handler calls any standard library function other than the async-signal-safe set (`abort`, `_Exit`, `quick_exit`, `signal` for the same signal **(C11)**, or atomic lock-free operations **(C11)**) (7.14.1.1). — (spec; UB catalog)
- [ ] §J.2 — A signal handler refers to a static (or thread, **C11**) storage-duration object that is not a lock-free atomic object **(C11)** and is not of type `volatile sig_atomic_t` (7.14.1.1). — (spec; UB catalog)
- [ ] §J.2 — A `setjmp` macro invocation appears outside the permitted contexts (the controlling expression of a selection/iteration statement; one operand of a relational/equality operator with an integer constant in the other operand; the operand of `!`; or a standalone expression statement) (7.13.1.1). — (spec; UB catalog)
- [ ] §J.2 — After `longjmp`, an automatic-storage-duration object local to the `setjmp`-containing function, changed between `setjmp` and `longjmp` and not `volatile`-qualified, is accessed (7.13.2.1). — (spec; UB catalog)
- [ ] §J.2 — A string- or wide-string-handling function is passed an array that is not null-terminated, or it reads/writes beyond the end of an object (C99 7.21/7.24 / C11 7.24/7.29). — (spec; UB catalog)
- [ ] §J.2 — `memcpy`, `strcpy`, etc. are called with source and destination objects that overlap (C99 7.21.2 / C11 7.24.2). — (spec; UB catalog)
- [ ] §J.2 — A copy/concatenation function's `n` parameter specifies a length larger than the destination object (C99/C11 7.21). — (spec; UB catalog)
- [ ] §J.2 — A `<math.h>` function argument is outside the function's domain, or the result cannot be represented (the range error case treated as undefined) (7.12). — (spec; UB catalog)
- [ ] §J.2 — A `<ctype.h>`/`<wctype.h>` function is passed an argument not representable as `unsigned char` and not equal to `EOF`/`WEOF` (C99 7.4/7.30 / C11 7.4/7.30). — (spec; UB catalog)
- [ ] §J.2 — The `strtol`/`strtoul`/`strtod` family is passed a `base` outside {0, 2…36} or other invalid arguments (C99 7.20.1 / C11 7.22.1). — (spec; UB catalog)
- [ ] §J.2 — A character/locale conversion function is passed an invalid `mbstate_t` or operates across a locale change mid-sequence (C99 7.24/7.28 / C11 7.28). — (spec; UB catalog)
- [ ] §J.2 (C11) — An atomic operation is performed on an object whose address is not suitably aligned, or with `memory_order` arguments invalid for that operation (7.17). — (spec; UB catalog)
- [ ] §J.2 (C11) — A thread function passed to `thrd_create` is invalid, or `mtx_t`/`cnd_t`/`tss_t` objects are used uninitialized or after destruction (7.26). — (spec; UB catalog)
- [ ] §J.2 — A `tss_t` destructor **(C11)** or an `atexit`/`at_quick_exit`-registered function does not return normally (7.26.6, C99 7.20.4 / C11 7.22.4). — (spec; UB catalog)
- [✓] §J.2 (C11) — A `_Generic` primary expression whose controlling-expression type matches no association is, by contrast, a **constraint** violation rather than UB (6.5.1.1) — noted here to delimit the boundary. — mcc:✓ gcc:✓ clang:✓ (all three diagnose no-match `_Generic` as an error)
- [ ] §J.2 — A `#include` directive (after macro replacement) does not match one of the two permitted forms, or a `#line` directive yields a line number out of range (> 2147483647) (6.10.2, 6.10.4). — (spec; UB catalog)
- [ ] §J.2 — The result of the `#` operator is not a valid character string literal, or the result of `##` is not a valid preprocessing token (6.10.3.2, 6.10.3.3). — (spec; UB catalog; gcc/clang/mcc diagnose `##`-forms-invalid-token)
- [ ] §J.2 — A macro is redefined incompatibly (after expansion) without an intervening `#undef` (6.10.3). — (spec; constraint, diagnosed by all three)
- [ ] §J.2 — A `#pragma STDC` directive uses an invalid on-off-switch or an unknown sub-pragma (6.10.6). — (spec; UB catalog)
- [ ] §J.2 — The `defined` operator is generated by macro expansion during the evaluation of a `#if`/`#elif` (6.10.1). — (spec; UB catalog)
- [ ] §J.2 — A predefined macro name, or the identifier `defined`, is the subject of a `#define` or `#undef` (6.10.8). — (spec; UB catalog; gcc/clang warn, mcc may not)
- [✓] §J.2 (C11) — A program relies on `__STDC_ANALYZABLE__` or `__STDC_LIB_EXT1__` (Annex K) semantics that the implementation does not define (6.10.8). — mcc:✓ gcc:✓ clang:✓ (none of the three define either macro, so neither facility is claimed; Annex K not provided)

### §J.3 Implementation-defined behavior (informative)

> Annex J.3 enumerates choices each implementation must document per 4p6. These are *documentation* obligations, not pass/fail conformance tests; mcc's choices match the platform ABI (and thus gcc/clang) in every case probed. C11 adds threads/atomics/alignment categories and the `__STDC_NO_*` feature macros; C99 lacks those rows. Each row below is the documentation pointer; "(spec; impl-defined)" means the behavior is whatever mcc documents/realizes, consistent with the host platform.

- [ ] §J.3.1 (Translation) — How a diagnostic message is identified, and whether each non-fatal diagnostic is reported (3.10, 5.1.1.3). — (spec; impl-defined)
- [ ] §J.3.1 — Whether each nonempty sequence of white-space characters other than new-line is retained or replaced by one space (5.1.1.2). — (spec; impl-defined)
- [ ] §J.3.2 (Environment) — The mapping of physical source-file multibyte characters to the source character set (5.1.1.2). — (spec; impl-defined; mcc treats source as UTF-8 like gcc/clang)
- [ ] §J.3.2 — The name and type of the function called at program startup in a freestanding environment; the effect of program termination in a freestanding environment (5.1.2.1). — (spec; impl-defined)
- [ ] §J.3.2 — Any alternative manner in which `main` may be defined; the values given to `argc`/`argv`; whether `argv[0]` is the program name; the environment names available and the method for altering the environment (5.1.2.2.1). — (spec; impl-defined)
- [ ] §J.3.2 — What constitutes an interactive device (5.1.2.3). — (spec; impl-defined)
- [ ] §J.3.2 (C11) — Whether a program can have more than one thread of execution in a freestanding environment (5.1.2). — (spec; impl-defined)
- [ ] §J.3.2 — The set of signals, their semantics, and the default handling; the signals for which `signal` returns an error; the signal-blocking performed if a handler is called; whether the equivalent of `signal(sig, SIG_DFL)` is executed prior to a handler call (7.14.1.1). — (spec; impl-defined; host `<signal.h>`)
- [ ] §J.3.3 (Identifiers) — Which additional multibyte characters may appear in identifiers and their correspondence to universal character names (6.4.2.1). — (spec; impl-defined; note mcc does not restrict the set, see §D gap)
- [ ] §J.3.3 — The number of significant initial characters in an identifier (6.4.2.1). — (spec; impl-defined; mcc has no truncation limit, matching gcc/clang)
- [ ] §J.3.4 (Characters) — The number of bits in a byte (3.6). — (spec; impl-defined; mcc: `CHAR_BIT`=8)
- [ ] §J.3.4 — The values of the members of the execution character set (5.2.1). — (spec; impl-defined; ASCII/UTF-8)
- [ ] §J.3.4 — The unique value produced for each member of the execution character set by each standard alphabetic escape sequence (5.2.2). — (spec; impl-defined)
- [ ] §J.3.4 — The value of a `char` object into which a character other than a member of the basic execution character set has been stored (6.2.5). — (spec; impl-defined)
- [ ] §J.3.4 — Whether a plain `char` has the same range, representation, and behavior as `signed char` or as `unsigned char` (6.2.5, 6.3.1.1). — (spec; impl-defined; mcc: signed on x86, unsigned on ARM/AArch64, matching the ABI)
- [ ] §J.3.4 — The mapping of source-character-set members (in character constants and string literals) to the execution character set (6.4.4.4, 6.4.5). — (spec; impl-defined)
- [ ] §J.3.4 — The value of an integer character constant containing more than one character or a non-basic-set character, and the value of a wide character constant containing more than one multibyte character or a character outside the extended execution set (6.4.4.4). — (spec; impl-defined; mcc supports multichar `'ab'` like gcc/clang)
- [ ] §J.3.4 — The current locale used to convert a wide character constant (single multibyte character) to a wide-character code, and likewise for wide string literals (6.4.4.4, 6.4.5). — (spec; impl-defined)
- [ ] §J.3.4 (C11) — Whether differently-prefixed wide-string-literal tokens may be concatenated and, if so, the treatment of the resulting multibyte character sequence (6.4.5). — (spec; impl-defined)
- [ ] §J.3.5 (Integers) — Any extended integer types that exist and their syntax (6.2.5). — (spec; impl-defined; mcc has none beyond the standard set, like gcc/clang sans `__int128` extension)
- [ ] §J.3.5 — Whether signed integers use sign-and-magnitude, two's complement, or ones' complement, and whether the extraordinary value is a trap representation or a normal value (6.2.6.2). — (spec; impl-defined; mcc: two's complement, no trap)
- [ ] §J.3.5 — The rank of any extended integer type relative to another extended integer type of the same precision (6.3.1.1). — (spec; impl-defined; N/A — no extended integer types)
- [ ] §J.3.5 — The result of, or the signal raised by, converting an integer to a signed integer type when the value cannot be represented (6.3.1.3). — (spec; impl-defined; mcc: modular wraparound, no signal, like gcc/clang)
- [ ] §J.3.5 — The results of bitwise operations on signed integers (6.5). — (spec; impl-defined; two's-complement semantics)
- [ ] §J.3.5 — The sign of the remainder of integer division (note: C99/C11 fix `/` and `%` to truncate toward zero, so this entry's outcome is constrained, but the row persists) (6.5.5). — (spec; impl-defined but constrained; mcc truncates toward zero)
- [ ] §J.3.5 — The result of right-shifting a negative-valued signed integer (6.5.7). — (spec; impl-defined; mcc: arithmetic shift, sign-preserving, like gcc/clang)
- [ ] §J.3.6 (Floating point) — The accuracy of floating-point operations and of the `<math.h>`/`<complex.h>` library functions returning floating-point results (5.2.4.2.2). — (spec; impl-defined; mcc uses host libm)
- [ ] §J.3.6 — The accuracy of conversions between floating-point internal representations and string representations performed by the library functions (C99 7.20.1.3, 7.24.4.1.1 / C11 7.22.1.3, 7.29.4.1.1). — (spec; impl-defined; host libc)
- [ ] §J.3.6 — The rounding behaviors characterized by the value of `FLT_ROUNDS` (5.2.4.2.2). — (spec; impl-defined; mcc: `FLT_ROUNDS`=1 round-to-nearest)
- [ ] §J.3.6 — The evaluation methods characterized by the value of `FLT_EVAL_METHOD` (5.2.4.2.2). — (spec; impl-defined; mcc: 0 on x86-64/SSE, 2 on i386/x87)
- [ ] §J.3.6 — The direction of rounding when an integer is converted to a floating-point value that cannot represent it exactly (6.3.1.4). — (spec; impl-defined)
- [ ] §J.3.6 — The direction of rounding when a floating-point value is converted to a narrower floating type (6.3.1.5). — (spec; impl-defined)
- [ ] §J.3.6 — How the nearest representable, or the larger/smaller adjacent representable, value is chosen for certain floating constants (6.4.4.2). — (spec; impl-defined)
- [ ] §J.3.6 — Whether and how floating-point expressions are contracted when not disallowed by `#pragma STDC FP_CONTRACT` (6.5). — (spec; impl-defined)
- [ ] §J.3.6 — The default state for `#pragma STDC FENV_ACCESS` and for `#pragma STDC FP_CONTRACT` (7.6.1, 7.12.2). — (spec; impl-defined)
- [ ] §J.3.7 (Arrays and pointers) — The result of converting a pointer to an integer or vice versa (6.3.2.3). — (spec; impl-defined; mcc: identity on `intptr_t`-width)
- [ ] §J.3.7 — The size of the result of subtracting two pointers to elements of the same array object (the type `ptrdiff_t`) (6.5.6). — (spec; impl-defined; mcc: 64-bit `long` on LP64)
- [ ] §J.3.8 (Hints) — The extent to which `register` storage-class suggestions are effective (6.7.1). — (spec; impl-defined)
- [ ] §J.3.8 — The extent to which `inline` function-specifier suggestions are effective (6.7.4). — (spec; impl-defined)
- [ ] §J.3.9 (Structures/unions/enumerations/bit-fields) — Whether a plain `int` bit-field is treated as `signed int` or `unsigned int` (6.7.2, 6.7.2.1). — (spec; impl-defined; mcc: signed, matching gcc/clang on this ABI)
- [ ] §J.3.9 — Which types, other than `_Bool`/`signed int`/`unsigned int`, are allowed for bit-fields (6.7.2.1). — (spec; impl-defined; mcc allows `char`/`short`/`long`/`long long` like gcc/clang)
- [ ] §J.3.9 — Whether a bit-field may straddle a storage-unit boundary (6.7.2.1). — (spec; impl-defined)
- [ ] §J.3.9 — The order of allocation of bit-fields within a storage unit (6.7.2.1). — (spec; impl-defined; mcc: LSB-first on little-endian targets, matching the ABI)
- [ ] §J.3.9 — The alignment of non-bit-field members of structures, permitting insertion of padding (6.7.2.1). — (spec; impl-defined; mcc follows the System V / target ABI)
- [ ] §J.3.9 — The integer type compatible with each enumerated type (6.7.2.2). — (spec; impl-defined; mcc: `int` unless values exceed its range)
- [ ] §J.3.10 (Qualifiers) — What constitutes an access to an object that has volatile-qualified type (6.7.3). — (spec; impl-defined)
- [ ] §J.3.11 (Preprocessing directives) — The locations within preprocessing directives where header-name preprocessing tokens are recognized (6.4, 6.4.7). — (spec; impl-defined)
- [ ] §J.3.11 — How a non-`STDC` `#pragma` is interpreted, and the behavior on each recognized non-`STDC` `#pragma` (6.10.6). — (spec; impl-defined; mcc warns and ignores unknown pragmas)
- [ ] §J.3.11 — Whether a single-character character constant in a `#if`/`#elif` controlling expression matches the value of the same constant in the execution character set, and whether such a constant may be negative (6.10.1). — (spec; impl-defined)
- [ ] §J.3.11 — The places searched for a `<...>`-delimited and a `"..."`-delimited header, and how header names are mapped to file names (6.10.2). — (spec; impl-defined; mcc `-I`/`-B` plus `include_next` to host headers)
- [ ] §J.3.11 — The method by which the preprocessing tokens of a macro-replaced `#include` directive are combined into a header name (6.10.2). — (spec; impl-defined)
- [ ] §J.3.11 — The nesting limit for `#include` processing (6.10.2). — (spec; impl-defined)
- [ ] §J.3.11 — Whether the `#` operator inserts a `\` before the `\` that begins a universal character name in a character constant or string literal during stringization (6.10.3.2). — (spec; impl-defined)
- [ ] §J.3.11 — The behavior on each recognized `#pragma STDC` sub-pragma, and the default state of `#pragma STDC CX_LIMITED_RANGE` (6.10.6, 7.3.4). — (spec; impl-defined)
- [ ] §J.3.11 — The definitions of `__DATE__` and `__TIME__` when the date or time of translation, respectively, is not available (6.10.8.1). — (spec; impl-defined; mcc supplies both from the build clock)
- [ ] §J.3.12 (Library functions) — Any library facilities available to a freestanding program, beyond the minimal required set (5.1.2.1). — (spec; impl-defined; mcc bundles the freestanding headers `<float.h> <limits.h> <stdarg.h> <stddef.h> <stdbool.h> <stdint.h> <stdalign.h> <stdnoreturn.h> <iso646.h>` plus `<stdatomic.h> <threads.h> <uchar.h> <tgmath.h> <complex.h>`)
- [ ] §J.3.12 — The format of the diagnostic printed by a failed `assert`, and the termination behavior (7.2.1.1). — (spec; impl-defined; host `<assert.h>`)
- [ ] §J.3.12 — The representation of the floating-point status flags stored by `fegetexceptflag`, and the result of `feraiseexcept` raising "overflow"/"underflow" along with "inexact" (7.6.2.2, 7.6.2.3). — (spec; impl-defined; host `<fenv.h>`)
- [ ] §J.3.12 — The strings other than `"C"` and `""` that may be passed to `setlocale`, and the way the locale categories affect behavior (7.11.1.1). — (spec; impl-defined; host libc)
- [ ] §J.3.12 — The types `float_t` and `double_t` when `FLT_EVAL_METHOD` is negative or otherwise unspecified (7.12). — (spec; impl-defined; host `<math.h>`)
- [ ] §J.3.12 — Domain/range/pole errors, and whether `math_errhandling` has the value `MATH_ERRNO`, `MATH_ERREXCEPT`, or their bitwise OR (7.12.1). — (spec; impl-defined; host `<math.h>`)
- [ ] §J.3.12 — Whether a domain error occurs or zero is returned when the second argument of `fmod`/`remainder`/`remquo` is zero; the values returned by math functions on domain error; whether `errno` is set to `ERANGE` on underflow (7.12.1). — (spec; impl-defined; host libm)
- [ ] §J.3.12 — The base-2 logarithm of the modulus used by `frexp`/`ldexp`/`scalbn`/`scalbln`/`logb` when `FLT_RADIX` is not 2 (7.12.6). — (spec; impl-defined; N/A — `FLT_RADIX`=2)
- [ ] §J.3.12 — Whether `signal` returns from a handler installed for `SIGABRT` raised by `abort` (7.14.1.1, C99 7.20.4.1 / C11 7.22.4.1). — (spec; impl-defined; host libc)
- [ ] §J.3.12 — The null pointer constant to which the macro `NULL` expands (7.19, 7.17). — (spec; impl-defined; mcc `<stddef.h>` defines `NULL` as `((void*)0)`)
- [ ] §J.3.12 — Whether the last line of a text stream requires a terminating new-line; the number of null characters appended on writing a binary stream; the initial file-position of an append-mode stream; whether a write on a text stream truncates the file beyond that point (C99 7.19.2 / C11 7.21.2). — (spec; impl-defined; host libc)
- [ ] §J.3.12 — How `fopen` modes are implemented; how `setvbuf` buffer characteristics, `tmpnam`/`tmpfile` naming, and file buffering behave (C99 7.19.3/7.19.4 / C11 7.21.3/7.21.4). — (spec; impl-defined; host libc)
- [ ] §J.3.12 — The value to which `errno` is set by the library functions on certain failures, and whether `errno` keeps its value across successful calls (7.5). — (spec; impl-defined; host libc)
- [ ] §J.3.12 — Whether `fflush` on an input/update stream causes a re-read; the meaning of any `n` argument; the behavior of `setvbuf`, of multiple `ungetc` without intervening read, of `%p` formatting/parsing, and of negative field widths/precisions (C99 7.19.6 / C11 7.21.6). — (spec; impl-defined; host libc)
- [ ] §J.3.12 — The default rounding direction and the conversions of `INF`/`NAN`/hex floats performed by `strtod`/`strtof`/`strtold` and by `printf`/`scanf` (C99 7.20.1.3, 7.19.6 / C11 7.22.1.3, 7.21.6). — (spec; impl-defined; host libc)
- [ ] §J.3.12 — Whether `calloc`/`malloc`/`realloc`/`aligned_alloc` return null or non-null for a zero-size request, and the order/contiguity of returned storage (C99 7.20.3 / C11 7.22.3). — (spec; impl-defined; host allocator)
- [ ] §J.3.12 — The termination status returned to the host by `abort`, `exit`, `_Exit`, and a `return` from `main`, when the value is not `0`/`EXIT_SUCCESS`/`EXIT_FAILURE` (C99 7.20.4.1/.3/.4 / C11 7.22.4). — (spec; impl-defined; host libc)
- [ ] §J.3.12 — The value returned by `system` with a null argument (whether a command processor exists) and with a non-null argument (C99 7.20.4.6 / C11 7.22.4.8). — (spec; impl-defined; host libc)
- [ ] §J.3.12 — The local time zone, Daylight Saving Time, and `<time.h>` eras; the ranges and precisions of `clock_t`/`time_t`; the era for `clock()`; the `strftime` conversion specifiers and their `"C"`-locale interpretation (C99 7.23 / C11 7.27). — (spec; impl-defined; host libc)
- [ ] §J.3.12 — Whether `<stdint.h>` defines the optional exact-width, minimum-width, and fastest-minimum-width types, and the values of their limit macros (C99 7.18 / C11 7.20). — (spec; impl-defined; mcc's `<stdint.h>` defines the full exact/min/fast set)
- [ ] §J.3.12 (C11) — The values of `<stdalign.h>`, `<stdatomic.h>`, and `<threads.h>` parameters; whether each atomic type is lock-free (`ATOMIC_*_LOCK_FREE`); and the alignment used by `aligned_alloc` for over-aligned requests (6.2.8, 7.17, 7.26). — (spec; impl-defined; mcc bundles all three headers; `__STDC_NO_ATOMICS__`/`__STDC_NO_THREADS__` are undefined, i.e. both are provided, matching gcc/clang)
- [ ] §J.3.13 (Architecture) — The values or expressions assigned to the macros in `<float.h>`, `<limits.h>`, and `<stdint.h>` (5.2.4.2, C99 7.18 / C11 7.20). — (spec; impl-defined; mcc values verified against Annex E above)
- [ ] §J.3.13 (C11) — The result of attempting to indirectly access an object with automatic storage duration from a thread other than the one with which it is associated (6.2.4). — (spec; impl-defined)
- [ ] §J.3.13 — The number, order, and encoding of bytes in any object, when not explicitly specified (6.2.6.1). — (spec; impl-defined; mcc follows target endianness/ABI)
- [ ] §J.3.13 — The value of the result of the `sizeof` and (C11) `_Alignof` operators (6.5.3.4). — (spec; impl-defined; mcc sizes/alignments match the target ABI and gcc/clang)

### §J.4 Locale-specific behavior (informative)

> Annex J.4 catalogs behaviors that vary with the current locale; documenting them satisfies the 4p6 documentation duty for locale-specific behavior. These are *runtime library* behaviors supplied by the host libc (mcc links against the system C library), so mcc, gcc, and clang share identical behavior. Clause numbers drift (C99 7.21/7.23/7.30 → C11 7.24/7.27/7.30; multibyte at C99 7.20.7/7.24.6 → C11 7.22.7/7.29.6).

- [ ] §J.4 — Whether `islower`/`isupper`/`isalpha`/etc. return true for characters beyond the required ones, depending on the `LC_CTYPE` category (7.4.1). — (spec; locale/host-library behavior)
- [ ] §J.4 — The collation sequence of the execution character set used by `strcoll`/`strxfrm` (and `wcscoll`/`wcsxfrm`), per `LC_COLLATE` (C99 7.21.4.3/7.21.4.5 / C11 7.24.4.3/7.24.4.5). — (spec; locale/host-library behavior)
- [ ] §J.4 — The contents of the error-message strings returned by `strerror`, per the locale (C99 7.21.6.2 / C11 7.24.6.2). — (spec; locale/host-library behavior)
- [ ] §J.4 — The date/time formats and the locale-specific era, AM/PM, and day/month names used by `strftime`/`wcsftime`, per `LC_TIME` (C99 7.23.3.5 / C11 7.27.3.5). — (spec; locale/host-library behavior)
- [ ] §J.4 — The decimal-point character and the monetary/numeric formatting members of the `struct lconv` returned by `localeconv`, per `LC_NUMERIC` and `LC_MONETARY` (7.11.2.1). — (spec; locale/host-library behavior)
- [ ] §J.4 — The set of characters tested for by the wide character-classification functions (`iswalpha`/`iswupper`/`iswlower`/etc.), per the locale (C99 7.30.2 / C11 7.30.2). — (spec; locale/host-library behavior)
- [ ] §J.4 — The mapping performed by `tolower`/`toupper` and `towlower`/`towupper`, and by locale-specific `wctrans`/`wctype` mappings, per `LC_CTYPE` (7.4.2, 7.30.3). — (spec; locale/host-library behavior)
- [ ] §J.4 — The behavior of the multibyte/wide conversion functions (`mblen`, `mbtowc`, `wctomb`, `mbstowcs`, `wcstombs`, and the restartable `mbrlen`/`mbrtowc`/`wcrtomb`/`mbsrtowcs`/`wcsrtombs` family), which depend on the `LC_CTYPE` multibyte encoding and shift state (C99 7.20.7/7.24.6 / C11 7.22.7/7.29.6). — (spec; locale/host-library behavior)

### §J.5 / feature-test macros — *(added gaps)* implementation-defined feature reporting

> These predefined macros are the machine-readable counterpart of several J.2/J.3 rows (6.10.8). Probed by `-E` expansion under `-std=c11`; mcc agrees with the gcc==clang consensus on every one.

- [✓] §6.10.8.1 — `__STDC_VERSION__` reports the supported standard (6.10.8.1). — mcc:✓ gcc:✓ clang:✓ (all `201112L` under `-std=c11`)
- [✓] §6.10.8.1 — `__STDC_HOSTED__` is `1` in a hosted implementation (6.10.8.1). — mcc:✓ gcc:✓ clang:✓ (all `1`)
- [✓] §6.10.8.2 — `__STDC_ISO_10646__` reports the ISO/IEC 10646 revision honored by `wchar_t` (6.10.8.2). — mcc:✓ gcc:✓ clang:✓ (all `201706L`)
- [✓] §6.10.8.3 — `__STDC_IEC_559__` is defined (to `1`) iff IEC 60559 floating-point is supported (Annex F; 6.10.8.3). — mcc:✓ gcc:✓ clang:✓ (all `1`)
- [✓] §6.10.8.3 — `__STDC_IEC_559_COMPLEX__` reports IEC 60559-compatible complex arithmetic (Annex G; 6.10.8.3). — mcc:✓ gcc:✓ clang:✓ (none define it on this platform → consistent)
- [✓] §6.10.8.3 (C11) — `__STDC_NO_ATOMICS__` is defined iff `<stdatomic.h>`/`_Atomic` are absent (6.10.8.3). — mcc:✓ gcc:✓ clang:✓ (undefined in all three → atomics provided)
- [✓] §6.10.8.3 (C11) — `__STDC_NO_THREADS__` is defined iff `<threads.h>` is absent (6.10.8.3). — mcc:✓ gcc:✓ clang:✓ (undefined in all three → threads provided)
- [✓] §6.10.8.3 (C11) — `__STDC_NO_VLA__` is defined iff variable-length arrays are not supported (6.10.8.3). — mcc:✓ gcc:✓ clang:✓ (undefined in all three → VLAs supported)
- [✓] §6.10.8.3 (C11) — `__STDC_NO_COMPLEX__` is defined iff `<complex.h>`/`_Complex` are absent (6.10.8.3). — mcc:✓ gcc:✓ clang:✓ (undefined in all three → complex provided)
- [✓] §6.10.8.3 (C11) — `__STDC_ANALYZABLE__` is defined iff the implementation conforms to Annex L (6.10.8.3). — mcc:✓ gcc:✓ clang:✓ (none define it → Annex L not claimed, consistent)
- [✓] §6.10.8.3 (C11) — `__STDC_LIB_EXT1__` is defined (to a `yyyymmL` value) iff the bounds-checking Annex K interfaces are provided (K.3.7.1; 6.10.8.3). — mcc:✓ gcc:✓ clang:✓ (none define it → Annex K not provided, consistent)

---

## Appendix — mcc project status: deliberate differences & limitations

The mcc-project-specific status that used to close this file — the conformance
philosophy & severity tags, platform/host limitations, and the **deliberate**
documented differences from gcc/clang (`[~]`/`[DIFF]` items kept on purpose) —
now lives in
[docs/NOTES.md § C99/C11 conformance](NOTES.md#c99c11-conformance--mcc-project-status-deliberate-differences--limitations).
The clause-by-clause map above remains the source of truth for what the standards
require and the per-compiler status.
