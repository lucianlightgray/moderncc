# Log: mac-arm64  (narrative checkpoints — newest first)

Migrated from TODO.md § In progress — mac-arm64 at the rev-2 process overhaul (2026-08-21). Append-only, single-writer (mac-arm64 only). The top entry is the current RESUME HANDOFF.

- CHECKPOINT (mac-arm64, 2026-08-22T14:22Z, user "Do TODO.md" resume → wrap-up). **(6) T-mac-30291 slice (e) node-BUDGET unroll DONE (8de3ecc1, DETAILS#t-mac-30291-budget):** replaced the fixed `trip>CAP=8` full-unroll cutoff with an effective cap `max(CAP, min(MAXTRIP=32, BUDGET=128/body_nodes))` via a new bounded `ast_subtree_nodes` counter — tiny-body loops (s+=i) now unroll up to trip ~21-32, large bodies stay at CAP (floor preserved → strictly ADDITIVE, no regression). Reuses the splice mechanism (no body-surgery), default-off → o0-neutral. TDD cli/loop_unroll_budget (trip16+trip20 fire, R=244); battery trips 12/16/20/24 + while ==-O0; trip40 declined; cli 487/487, corpusgate 6/6 (census now green post lin rebank), treegate 13/13, mcc_cross_build green, all 14 prior unroll cells green. **MINTED T-mac-30295 (USER DIRECTIVE, DETAILS#t-mac-30295-jit-benchmark-opt):** AOT static heuristics (this unroll budget, divmagic, inline-limit) are fine BUT the JIT should EMPIRICALLY BENCHMARK the fastest optimization per AST-slice — wall-clock statistical analysis, or a binary-search/sort fallback. Key rationale: AOT objects must be byte-reproducible (the AOT opt-search was deliberately moved OFF wall-clock, opt-search-determinism.py), but the JIT optimizes a LIVE program with no such constraint → wall-clock is the right signal. Substrate: ast_search_gates_* + JIT MccjitCounterState tick/hot-swap + runtime-bench; deep [C]-then-fanout, focused-session. Next mint 30296.

- SESSION WIND-DOWN / RESUME HANDOFF (mac-arm64, 2026-08-22T14:08Z, USER-DIRECTED "wrap up + hand off + commit/push"). **All pushed (HEAD dc08002a6+), tree clean, NO active mac claims.** This session (goal=INSTRUCTIONS.md loop from a fresh start; the repo had migrated to per-session files + GOAL P1/P2/P3 + [D]/KNOWN_RED since the last mac run) delivered **FIVE verified deliverables** + heavy fleet coordination:
  1. **T-mac-30294 [S] DONE+ARCHIVED (77b1c2bf)** — BUILD-BLOCKER fix: `mcc_skip_test` splatted `${ARGN}` unquoted → list-valued `LABELS "macho;native"` skip cells broke `cmake --preset macos` on any box without the x86_64-osx Rosetta backend. cmake_parse_arguments. Unblocked the mac build fleet-wide. (Box note: reconfigure needs `CC=/usr/bin/cc cmake --preset macos`.)
  2. **T-lin-10471 slice (b) ordered-relational CSE DONE (ddba3052, released OPEN)** — P2 GVN parity: a<b≡b>a / a<=b≡b>=a (signed+unsigned) via ast_rel_swap + ast_cse_same branch under -fgcse-comm-rel; sound incl NaN, o0-neutral, cli/gcse_comm_ord.
  3. **T-mac-30293 [S] DONE+ARCHIVED (74ec1b64)** — P3 gpu/vram-budget cell SKIPs(77) on Metal (Vulkan-only VK_EXT_memory_budget test).
  4+5. **T-mac-30291 slice (d) while+do-while full-unroll DONE (fbb8312e + 00ddccf4, released OPEN)** — P2: extended ast_unroll_apply to op==2/4 via li->body/li->incr splice (byte-identical for-loops); do-while entry-once handled for free (span<1 declines). cli/loop_unroll_{while,dowhile}, cli 486/486, shapes+entry-once ==-O0==AOT.
  FLEET COORDINATION: (a) diagnosed + KNOWN_RED'd + RESOLVED-with-lin the wide-census divrem_fuse.c drift (lin rebanked elf at my head 3dc76230f, delisted; option-a accept the ast_cse_commutative_op GPU-lowerability loss); (b) consumed lin's T-lin-10470 SLP claim (no dup); (c) analyzed lin's **T-lin-10541** (newer-glibc dropped printf_unlocked → my T-mac-30047 __APPLE__-only shim link-fails on lin) — handed lin the o0-neutral insight (no rebank; only tests/cli/unlocked_check.c uses it) + the mccdefs.h vprintf-decl-ordering gotcha + recorded the correction (2b29e8b95); lin owns the Linux-compile-verify-bound edit (a blind mac edit is unsafe). ABANDONED (unshipped, reverted): nested-commutative CSE (T-lin-10471 residual a) — sound but fires unpredictably by traversal order. FINAL VERIFY: optfire arm64-osx JIT-run 209/209 green (fleet optimizer passes correct on Mach-O). VERIFICATION per slice: cli/corpusgate(o0-neutral)/treegate/targeted correctness batteries. **NEXT PICKUP:** clean solo P2/backlog is EXHAUSTED for mac — remaining = deep-multi-turn (partial/runtime unroll = loop-restructure; escape-analysis; true-GVN — main gaps FLEET-COVERED: lin SLP, win bswap), architecturally-blocked backlog (30202 parse-shape-tracker, 30247 constexpr-storage, 30264 bignum-f128-parser), or cross-platform-gated (qemu/COFF/riscv64 — caps mac lacks). Best mac re-entry: cross-verify lin/win optimizer passes on the Mach-O legs when they land, or a fresh-context deep-P2 (partial-unroll) session. T-lin-10541 verify-confirm owed to lin (my do-while 00ddccf4 landed post their bbd40ce1 rebank; macho census re-ran PASS, elf near-certain inert). Next mint 30295.

- CHECKPOINT (mac-arm64, 2026-08-22T13:52Z, continued). **T-mac-30291 slice (d-rem) DO-WHILE full-unroll DONE (00ddccf4, DETAILS#t-mac-30291-dowhile):** one-line gate extension (op==4 added to ast_unroll_apply). do-while's entry-once semantics need NO special code — span≥1 → do-while trip == entry-checked trip (dup(body)×trip correct); span<1 (cond initially false → do-while runs once, while runs zero) → the existing `span<1 → return 0` guard DECLINES, so it runs once un-unrolled rather than a wrong count. Residual (d) while+do-while now COMPLETE. Verified: cli/loop_unroll_dowhile (R=164, U=fired), cli 486/486, corpusgate o0-neutral, treegate 13/13; do-while battery incl the entry-once edge (`i=5 do{}while(i<3)` runs once) all -funroll-loops == -O0 == AOT (5211340). **CENSUS RESOLVED:** lin rebanked the elf wide census at bbd40ce1 (3dc76230f, option-a accept) + delisted the rir-coverage-census KNOWN_RED row → census GREEN; kept_coverage[elf] 94.5081→94.3082 as I predicted, macho floors preserved. My do-while (00ddccf4) landed AFTER lin's rebank (I'd wrongly said "no further changes") — re-ran the macho census with it = PASS; FYI'd lin the honest correction + elf-tolerance-confirm owed (near-certain inert, a one-liner). win still owes `win` wide floors.

- CHECKPOINT (mac-arm64, 2026-08-22T13:44Z, continued). **T-mac-30291 slice (d) WHILE full-unroll DONE (fbb8312e, DETAILS#t-mac-30291-while):** P2 loop-unroll parity. Extended `ast_unroll_apply` (was for-loops op==3 only) to WHILE loops (op==2) — the analyzer already gives has_iv/stride/bound/cond for while (IV found in the body since the increment is body-embedded). Gate += op==2; drive the splice off li->body/li->incr (byte-identical to the old ast_child(loop,1/2) for for-loops; li->incr==AST_NONE for while → dup body only, increment rides inside). Same splice mechanism as the for-unroll → replay-safe (no synthesized loop structure, unlike partial-unroll), o0-neutral (default-off -funroll-loops). do-while (op==4, entry-once) + op==8 deferred. Verified: cli/loop_unroll_while (R=164, U=fired), cli 485/485, corpusgate o0-neutral, treegate 13/13; 4 while shapes (i² / stride-2 <= / factorial / descending stride-3) all -funroll-loops == -O0 == AOT; runtime-bound while declines; -S shows the back-branch gone. **Fleet coordination (census):** lin tried my requested elf wide-census rebank and hit a HARD blocker — the PRIOR mac ==/!= CSE slice (c4fd2bff8) grew ast_cse_commutative_op past the GPU-lowerability threshold (self-census kept-coverage 94.5081→94.3083 on elf; the tool refuses to silently bank it). I BLESSED option (a) ACCEPT: the `lowerable` metric is the compiler's OWN bodies being GPU-lowerable — a self-census PROXY, not the P3 user-node deliverable — so trading 0.22pp of one internal function's lowerability for a real P2 optimizer feature is sound (P2>P3). lin holds until my while-unroll SHA (this grows ast_unroll_apply too), then rebanks ONCE at fbb8312e (sources_wide 403 + churned floors + --update-bank-low, capturing c4fd2bff8+ddba3052+while-unroll), pushes, delists the rir-coverage-census KNOWN_RED row, and FYIs win to bank its `win` floors. Good cross-check confirmed: the blind-rebank-clobbers-shared-floors trap is macho-host-ONLY (lin's elf --update-bank left the macho floor untouched). lin proceeding on T-lin-10470 SLP; win on T-lin-10510 bswap.

- CHECKPOINT (mac-arm64, 2026-08-22T13:32Z, continued). **T-mac-30293 [S] DONE+ARCHIVED (74ec1b64, DETAILS#t-mac-30293-vram-budget-metal-skip):** the `gpu/vram-budget` cell FAILed on this Metal host — it verifies the Vulkan slot-budget + VK_EXT_memory_budget free-VRAM mechanism (a `slot 0 ... maxsb=` diag) that Metal lacks (route reports only `maxBufferLength`). Made `tests/gpu/run-gpu-vrambudget.sh` detect the Metal device (maxBufferLength in the diag route) and exit 77 (SKIP) instead of failing; a genuinely-broken Vulkan device still FAILs. Verified: ctest `gpu/vram-budget` = Skipped on Apple M1 Pro. Cleared a red only a Metal box can fix (§11.6 grooming / unique-capability). **Also this batch:** prototyped T-lin-10471 residual (a) nested/deep commutative CSE ((a+b)+c≡(b+a)+c via recursive depth-bounded operand matching under a new -fgcse-comm-nest) — SOUND (5 probes: sub not merged, c≠d not merged, deeper correct) but ABANDONED unshipped: firing is order-dependent/fragile (inner subexprs get CSE-replaced by refs before the outer match runs, so `(a+b)+c` fires but same-depth `(a+b)*c` and deeper nesting don't) — not worth shipping an inconsistently-firing opt; reverted cleanly (all uncommitted). Peer: lin-x64 CLAIMED T-lin-10470 auto-vec/SLP (5b4f95a20) — consumed, not picking it. FYI lin owed: the wide census (rir-coverage-census, KNOWN_RED) needs a coordinated sources_wide+elf-floor re-bank on lin's box + win floors on win's (macho floors unbanked, can't do from here).

- CHECKPOINT (mac-arm64, 2026-08-22T13:16Z, goal=INSTRUCTIONS.md loop, fresh run). All pushed, tree clean, no active mac claims. TWO deliverables + one KNOWN_RED attribution. (1) **T-mac-30294 [S] DONE+ARCHIVED (77b1c2bf, DETAILS#t-mac-30294-mcc-skip-test-labels):** the mac build was BROKEN at HEAD — `cmake --build --preset macos` failed to configure with "set_tests_properties called with incorrect number of arguments" ×N. Root: `mcc_skip_test` splatted `${ARGN}` unquoted, so a list-valued `LABELS "macho;native"` (three skip sites: ast/*-typecov-x86_64-osx, optfire-x64boot/rosetta, optfire-*-osx) flattened to `LABELS macho native` → odd property/value count. Those else()-skip branches fire only when the x86_64-osx Rosetta backend (cmake-macos-x64) is absent → any fresh mac checkout couldn't build. Fixed with cmake_parse_arguments (o0-neutral, test-registration only). BOX NOTE: reconfiguring the macos preset needs `CC` set (`CC=/usr/bin/cc cmake --preset macos`); a bare `cmake --preset macos` NOTFOUNDs the C/ASM compiler (preset uses `CMAKE_C_COMPILER=$env{CC}`) and clobbers the cache — recover by reconfiguring with CC set. (2) **T-lin-10471 slice (b) ordered-relational mirroring DONE (ddba3052, DETAILS#t-lin-10471-gcse-comm-ord):** P2 optimizer. Extended the relational-commutativity CSE (the ==/!= slice c4fd2bff8) to ordered relationals, which mirror to a DIFFERENT op with swapped operands (a<b≡b>a, a<=b≡b>=a, signed+unsigned) — new ast_rel_swap operand-swap table (distinct from the existing ast_rel_negate) + a branch in ast_cse_same under the same default-off -fgcse-comm-rel. Sound for all types incl NaN (both false) and unsigned; a<b vs b<a (same-op swap) and a<b vs a>b (non-swap) correctly NOT merged (5 soundness probes green). o0-neutral, rides AST_SG_CSECOMM (no new gate bit, no novel AST op). TDD cli/gcse_comm_ord (R=4 + cmp on/off→U=fired); cli 484/484. Task released OPEN, residuals (a) deep/nested commutative + (c) true value-numbering. Chose this over the prior-handoff's T-lin-10473 IPA-CP S1 after reading the inliner: S1 needs BOTH retention (ast_fn_inlinable node-limit rejects big callees → not in the pool) AND graft changes AND an anti-pessimization/soundness guard — genuinely multi-part, not a clean single-turn slice (banked that finding on the task). (3) **rir-coverage-census PRE-EXISTING red attributed + KNOWN_RED'd (DETAILS#rir-coverage-census-divrem-drift):** wide-corpus denominator drift 402→403 from divrem_fuse.c (b3d117f3, T-lin-10509) — lin re-banked ast/o0-baseline but not the wide census sources_wide+floors; a macho re-bank writes host-wrong shared values (macho wide floors unbanked), so it needs a coordinated lin(elf)+win re-bank. Added to KNOWN_RED (ALL, delist-on-green); NOT caused by my default-off slice (adds no wide-corpus file). FYI lin owed. Next pickup: another bounded P2 slice (e.g. T-lin-10471 residual (a), or a fresh optimizer gap) or T-mac-30293 (gpu/vram-budget Metal red). Next mint 30295.

- RESUME HANDOFF (mac-arm64, 2026-08-22T18:00Z, goal=INSTRUCTIONS.md loop — USER asked to assist win+lin, then wrap up for handoff; all pushed, tree CLEAN, NO active mac claims). **This turn = one task closed + a P2 investigation banked + THREE cross-fleet verifications (mac-only legs) + a fleet-red o0-baseline rebank.** (1) **T-mac-30292 [D] CAP:gpu-metal RESOLVED (NON-BUG) + pinning cell DONE+ARCHIVED (code cb736a8c, DETAILS#t-mac-30292-resolved):** Metal double-rung oracle — the MSL kernel builder computes every double in SOFTWARE IEEE-754 binary64 (int2 pairs mccf64_addsub/mul/cmp/round, msl_prelude_f64 mccgpu.h:1656), NEVER fp32; the -O13 --jit-always-gpu double oracle JIT-runs BIT-IDENTICAL (17 sig-digits) to -O0 with 21 rungs dispatched, correctly refusing the fp64-invalid distributive rewrite. New cell gpu/f64-oracle-soundness (skips off-device, rungs>0 anti-vacuity), o0-neutral. No code change (f64=1 is honest via emulation; f64=0 would wrongly refuse it and is inert on the MSL path anyway). (2) **T-lin-10473 P2 IPA-CP investigation BANKED (3143bdc41, DETAILS#t-lin-10473-ipa-cp-investigation):** full feasibility map — reusable substrate exists (ast_inline_pool + AstInlineFn metadata, ast_constparam_fold+ast_sccp_run+ast_arena_clone) but codegen is streaming/forward-only, and const-arg-at-callsite is ALREADY covered by the inliner; the genuine gap (specialize non-inlined callees / all-callers CP / DAE) needs clone-emit or two-phase-compile → multi-turn. S1/S2/S3 ladder banked; task left OPEN, S1 (single-call-site specialization-by-forced-inline under default-off -fipa-cp) is the recommended forward-only-safe first landing. (3) **Fleet verifications (mac-only surfaces):** T-lin-10480 `_below` CONTRACT — 10/10 both Mach-O legs (arm64-osx native ctest + x86_64-osx Rosetta defstate driver), no arch.txt pin needed → lin closed the **P1 JIT typecov program fleet-wide**; T-lin-10509 divrem slice-1 — arm64-osx 25/25 (sdiv+mul+sub); **T-win-50060** win's cg_func_alloca fix (4823e7f36) Mach-O cross-verify (DETAILS#t-win-50060-mac-verify) — **PE-gated crash CONFIRMED** (win's m4.c minimizer never crashes at -O0..-Os on arm64-osx OR x86_64-osx, pre OR post-fix; the SEGV is in the PE-only epilog gsym_addr(func_alloca,-func_scratch) x86_64-gen.c:1109, so SysV/Mach-O + lin-ELF are all crash-free — win's PE-gated analysis is right) + **no-regression via BYTE-IDENTITY** (stronger than a test run: x86_64-osx exec corpus 316/316 identical -O1/-O2 [lone -O2 diff was __DATE__/__TIME__]; arm64-osx 310 identical + exactly 2 alloca-adjacent files {atomic_aggregate,c11_freestanding_headers} whose bytes change but disassembly is IDENTICAL [a non-code cg_func_alloca-scratch field, the corrected state-leak], -run behavior identical, all 48 of their cells green) — so win's fix is behavior-neutral on both Mach-O backends; neither changed file is in the o0-baseline corpus so ZERO bank impact. Full native ^exec on arm64-osx run as belt-and-suspenders. (4) **o0-baseline arm64-osx REBANK (fa32afeab) — caught a fleet-red:** lin's T-lin-10509 added tests/exec/optimizer/divrem_fuse.c (new corpus file) WITHOUT rebanking → ast/o0-baseline RED on every active key. Rebanked mac's arm64-osx key natively (clean single-cause: +divrem_fuse.c obj hash, rir fn 1449→1453 faithful 1414→1418; ungated+gated), corpusgate ast/o0-baseline GREEN. **x86_64-osx NOT rebanked** — it has SEPARATE pre-existing accumulated drift (stale since Aug 21: 4 FP files c11_freestanding_headers/flt_eval_method/floating_point/ldouble_to_signed changed SHA across multiple x86_64 commits, likely the ldouble/binary128 FP work) that isn't cleanly single-cause; I won't accept unattributed drift that could mask a regression (the 4 files DO pass their exec cells, so behavior-correct, but attribution is owed to the FP-change owner). FYI'd lin: their box rebanks x86_64/*-win32 keys; x86_64-osx attribution owed. (5) **Minted T-mac-30293** (pre-existing gpu/vram-budget Metal-host red — Vulkan-only diag). **State: NO active mac claims, tree clean, Next mac ID = 30294.** **PICK-UP:** (a) HOLD for lin's T-lin-10518 arm64 msub-fusion ping (I confirm arm64-osx sdiv+msub, the leg lin can't self-run); (b) land T-lin-10473 **S1** (prototype the non-graftable-callee boundary first so the cell is non-vacuous); (c) T-mac-30293 vram-budget Metal skip (quick, mine); (d) deep P2 from-scratch passes (10470 auto-vec, 10474 escape-analysis, 10475 TFA). Build: `CC=/usr/bin/cc cmake --preset macos` (NEVER without CC). Verify targeted `-R`, NEVER bare ctest. o0-baseline rebank recipe (mac holds arm64-osx + x86_64-osx keys): `C2_NO_EXTRA=1 O0_AB_BANK=1 O0_AB_MIN_KEYS=1 sh tools/o0_ab.sh cmake-macos measurable <scratch-out>` (arm64-osx native) / `... cmake-macos-x64 ...` (x86_64-osx Rosetta), + the MCC_DEV=1 O0_AB_GATES=1 gated pass; scratchpad outdir NOT /tmp. grep conflict markers before every push; cite the on-main code SHA after the final rebase.
- RESUME HANDOFF (mac-arm64, 2026-08-22T14:30Z, goal=INSTRUCTIONS.md loop — all pushed, tree CLEAN, NO active mac claims). **This session RESOLVED T-mac-30292 [D] CAP:gpu-metal (Metal ladder-GPU double-rung soundness) as a characterized NON-BUG + landed a pinning regression cell — DONE+ARCHIVED.** My own mint from last session, my exclusive gpu-metal lane (no other session can characterize the Metal MSL path). Priority note: P1 is exhausted-for-mac (T-lin-10480 residual is lin-owned `_below` + a minor tco __float128; no clean mac slice) and the P2 opt-gaps are deep multi-session from-scratch passes — so closing this mac-EXCLUSIVE soundness item was the highest-value one-turn deliverable available to THIS session's unique capability, per the prior handoff's ranking. **What I found (drove the RISK to a definitive answer, verify-first):** the premise — "double rungs dispatch to a GPU that evaluates them in fp32 → the -O13 --jit-always-gpu equivalence oracle could mis-certify an fp32-valid/fp64-invalid rewrite" — is FALSE. Reading the MSL kernel builder: the Metal emitter never lowers a double to a native MSL `double` (MSL has none) nor truncates to fp32 — it computes EVERY double in SOFTWARE IEEE-754 binary64 via int2 (32-bit-pair) arithmetic: `mccf64_addsub`/`mccf64_mul`/`mccf64_cmp`/`mccf64_round` (the `msl_prelude_f64[]` string, src/mccgpu.h:1656), a full round-to-nearest-even impl with denormal/inf/nan. Coverage is exactly `+ − * == != < <= > >=` (`msl_f64_binop_code`, mccgpu.h:929); DIVISION/`%`/transcendentals return fcode=0 → `msl_refuse`. Unary minus = sign-bit flip (correct). **VT_FLOAT (fp32) never reaches the device at all** — `ast_eval_slice_ftype` (src/ast_eval_slice.h:919) returns 0 for any `is_float` non-VT_DOUBLE type, so a float expr falls to the integer arm which refuses on `is_float`. So the ONLY floating type the Metal oracle evaluates is VT_DOUBLE, in faithful binary64. Also: `mcc_gpu_f64()` has NO consumer on the MSL path — the two `used_f64 && !mcc_gpu_f64()` refusals (src/mccslice.h:1075, 2125) are inside the SPIR-V/Vulkan `#if`, compiled OUT on a Metal (MCC_GPU_LANG_MSL) build. So `mcc_gpu.f64=1` unconditional (mccgpu.c:523) is (a) inert on the MSL path and (b) semantically HONEST ("this backend evaluates binary64 correctly", true via emulation), unlike Vulkan where f64 mirrors HARDWARE shaderFloat64 (mccgpu.c:2132) because SPIR-V needs the Float64 device capability. **Empirical confirmation (Apple M1 Pro):** (1) reproduced win's exact `double f(){a*b+a/b-a}` → `rungs=21 dispatches=43 refused=0` via `MCC_AST_EVAL_LADDER=1 MCC_AST_EVAL_LADDER_GPU=1 mcc --jit-always-gpu -O13 -fopt-search -run`; (2) a fp64-precision-sensitive subject — non-distributive `(a*b−a*c)*1e16`, non-associative `((a+b)+c)−(a+(b+c))`, catastrophic cancellation `(s−a)−b` — JIT-runs BIT-IDENTICAL to all 17 sig-digits (`14000001.158365194 -1.0000000001 -1`) under the GPU-armed -O13 oracle (21 double rungs dispatched to Metal) vs a pure -O0 CPU compile. Anti-vacuity: the distributive rewrite `(a*(b−c))*1e16` gives `14000001.159765195` (differs at the 8th sig-digit), so a mis-certification WOULD have moved the output — the oracle correctly REFUSES it. **Resolution: NON-BUG, no code change** (setting f64=0 on Metal would wrongly refuse the faithful emulation, losing double GPU coverage for zero soundness benefit, and is inert on the MSL path anyway). **Deliverable = new pinning cell `gpu/f64-oracle-soundness`** (tests/gpu/f64_oracle_soundness.sh + CMakeLists add_test beside always-gpu-parity): asserts the GPU-armed -O13 double result == -O0 CPU result byte-for-byte, with rungs>0 anti-vacuity + SKIP_RETURN_CODE 77 on no device. o0-neutral (test-only, NO src/ change, mcc output byte-identical). **DoD (all green):** cell PASS rungs=21; treegate 13/13 (fmt/census-bank unmoved); corpusgate 6/6 (ast/o0-baseline NO drift — a .sh isn't corpus, subject is runtime-generated); `^gpu/` suite green EXCEPT one pre-existing unrelated red (below). Cell is arch-independent, registered fleet-wide (skips off-device); on Vulkan (lin/win, RTX shaderFloat64=real hardware fp64) the same `-O13-GPU-run == -O0-run` invariant holds — **lin/win confirm the cell green on pull.** DETAILS#t-mac-30292-resolved. **Also minted T-mac-30293 [S] P3/GPU-infra (LOW):** `gpu/vram-budget` FAILs on any Metal host — pre-existing, NOT device-real: tests/gpu/run-gpu-vrambudget.sh greps a Vulkan-only diag `slot 0 … maxsbrange/freevram/maxsb` but Metal's `slicerun route` emits `maxBufferLength=…`, so the sed yields empty → FAIL. Orthogonal to 30292 (slicerun unchanged); now tracked so the red isn't orphaned. Fix = skip on Metal or emit an equivalent budget diag. **State: NO active mac claims, tree clean, Next mac ID = 30294.** **HELD for lin's incoming CONTRACT verify:** lin messaged (SHA cc9f724b) that it claimed T-lin-10480 to FINISH it (part-b `_below` off-side level cells + closing tco __float128) and will send a CONTRACT-tier request citing a new SHA asking me to run the native optfire suite (new `optfire/default-*_below` cells) on BOTH Mach-O legs (arm64-osx native + x86_64-osx Rosetta) — Mach-O can't be emulated on lin, so my legs are required for its DONE. Do NOT claim 10480; watch for the ping and run both legs when it arrives. **PICK-UP (priority order) after the 10480 verify:** (a) any further mac-exclusive gpu-metal soundness/coverage (P3) or **T-mac-30293** (the vram-budget Metal skip, quick); (b) deep P2 from-scratch passes (T-lin-10470 auto-vec/SLP, 10473 IPA-const-prop/DAE, 10474 escape-analysis→heap-to-stack, 10475 whole-program TFA); (c) T-mac-30291 partial-unroll (the real loop-unroll parity project — remainder loop). Build: `CC=/usr/bin/cc cmake --preset macos` (NEVER without CC). Verify targeted `-R`, NEVER bare ctest. GPU needs no setup (Metal auto-selected on Darwin; device=Apple M1 Pro). grep conflict markers before every push; cite the on-main code SHA after the final rebase.
- RESUME HANDOFF (mac-arm64, 2026-08-22T09:40Z, goal=INSTRUCTIONS.md loop — USER asked to wrap up for handoff; all pushed, tree CLEAN, NO active mac claims). **This session landed 2 tasks + a doc correction + served as the fleet's mac-verification hub for 6 peer deliverables:** (1) **T-lin-10480 [S] P1 part (2) — `ast/select-typecov` cell (code 723637a1d, docs ce3215bdd, DETAILS#t-lin-10480-select-typecov):** new type-varied typecov cell for the if-conversion/select pass, JIT `-run`-verified on both Mach-O legs. Probe-first found `ast_sel_gpr` (mccast.c:13033) restricts select to GPR types, so scoped HONESTLY to integer-WIDTH completeness (char/short/int/unsigned/long/llong, select=6; float/double correctly excluded; pointer DROPPED as context-dependent after reconciling win's FYI 16f961eb). Value beyond the optfire select differ = per-width branchless-select codegen correctness via -run. Registered base-lin + both mac legs + win-PE (arch-independent count); win CONFIRMED the PE leg green (count=6). Floor >=5 (true 6). o0-neutral. T-lin-10480 re-PARKED (remaining part-2 passes are per-pass-deep/rebank-gated per win's landscape map; part 3 `_below` lin-owned). (2) **T-lin-10398/mac [P] P3 — Metal GPU slice-coverage baseline DONE+ARCHIVED (docs cf102a3a2, SHA-fix df6702342, DETAILS#t-lin-10398-mac-baseline):** measured ladder-GPU dispatch on Apple M1 Pro Metal (`MCC_AST_EVAL_LADDER_GPU=1 mcc --jit-always-gpu -O13 -fopt-search -run`): overflow_inline 653/1307/0 (==win), alloca_inline 271/543/0 (arch-lower rungs, refused=0), full_language 0/1; dispatch rate ~100%, all refusal buckets 0 → CONFIRMS the fleet on a 3rd (Metal) stack: gap is BREADTH not dispatch. **Surfaced a cross-stack CORRECTION:** DOUBLE rungs DO dispatch (float-only `f` → 21/43/0; rung gate mccast.c:3325 admits VT_DOUBLE, ignores mcc_gpu_f64) — the fleet "float never a rung" was a combined-subject artifact; win AND lin independently reproduced 21/43/0 (RTX 2060 + RTX 5070 Ti), all corrected their notes. Validated the Metal stack (gpu/{ladder,always}-gpu-parity + msl-slice-{differential,real} + msl-const-cache 7/7). lin closed+archived the [P] parent (all legs recorded). (3) **Minted T-mac-30292 [D] CAP:gpu-metal (DETAILS#t-mac-30292-metal-f64-oracle):** Metal sets mcc_gpu.f64=1 UNCONDITIONALLY (mccgpu.c:523) despite no MSL fp64 → the -O13 --jit-always-gpu double oracle could mis-certify an fp32-valid/fp64-invalid rewrite. No miscompile OBSERVED (always-gpu-parity green; precision-stress==-O0). lin confirmed the fix direction: RTX 5070 Ti has real shaderFloat64 so Vulkan is sound, and the Vulkan path already gates f64 on shaderFloat64 (mccgpu.c:2132) — so the fix = make Metal's f64 honest per-device (query MTLDevice fp64 = false on Apple Silicon), mac-only. **THIS IS THE TOP PICK-UP for next mac session** (my mint, my gpu-metal lane). (4) **Doc correction (7d84e9e30, DETAILS#t-lin-10478-mac-coverage-correction):** my #t-lin-10478-mac-coverage VERIFY line conflated the base AOT ELF leg with the required JIT -run leg; appended the accurate parent close-criteria (lin flagged it). **P1 MILESTONE reached this session:** the T-lin-10478 [P] parent CLOSED fleet-wide — 100% JIT -run coverage of the optfire differ matrix across every triple × format (x86_64/arm64/riscv64 × ELF/Mach-O/PE); my 2 Mach-O legs (92 cells, 13f5e8c77) were part of it; lin closed the last (qemu) legs + archived. **Cross-fleet verifications served (my mac-native/mac-key/Rosetta-x87/gpu-metal role):** win's loop-idiom slice 5 (both Mach-O legs + corpusgate no-drift), win's foldstr T-win-50050 (arm64-osx o0-baseline key no-drift + optfire 96/96), win's VLA-replay T-win-50058 (direct forced-replay-nofb repro on all 4 VLA files × both backends + rir-nofb-probe census green → confirmed win-PE-SPECIFIC, gave win confidence to localize it to a bogus 0x5d VLA-size immediate), lin's tco FP+ldouble family (both legs tco=8→9 incl the unrun x86_64-osx Rosetta **x87 80-bit** long-double surface, all green), lin's native-arm64 varargs "hi 0" loose-end (native clean → confirmed a qemu/sysroot quirk). **Also FOUND + FIXED-BY-RECOMMENDATION a shared-cell hazard:** lin's tco_typecov_subject.c `fq(100000LL,0LL)` -O0 leg stack-overflows (100000 un-TCO'd frames, env-marginal SIGSEGV — was NOT lin's sysroot); delivered the verified coverage-preserving fix `fq(10000LL,5000000000LL)` (>2^32 result kept, stack-safe), lin landed it (4c98c35f). **State: NO active mac claims, tree clean, Next mac ID = 30293.** **PICK-UP (priority order):** (a) **T-mac-30292** [D] CAP:gpu-metal — Metal f64 honesty (mine, recipe banked, gpu-metal box only); (b) **T-lin-10480** P1 breadth residual (select-typecov landed; further type-varied cells are per-pass-deep/rebank-gated per win's map; part 3 `_below` lin-owned); (c) deep P2 from-scratch passes (T-lin-10470 auto-vec/SLP, 10473 IPA-const-prop/DAE, 10474 escape-analysis→heap-to-stack, 10475 whole-program TFA); (d) T-mac-30291 partial-unroll (the real loop-unroll parity project — remainder loop). Build: `CC=/usr/bin/cc cmake --preset macos` (NEVER without CC). Verify targeted `-R`, NEVER bare ctest. GPU measurement needs no special setup (Metal auto-selected on Darwin; device=Apple M1 Pro). grep conflict markers before every push; cite the on-main code SHA after the final rebase.
- RESUME HANDOFF (mac-arm64, 2026-08-22T02:39Z, goal=INSTRUCTIONS.md loop — USER asked to wrap up; all pushed, tree CLEAN, NO active mac claims, full native suite GREEN). **This session (6 slices / 4 tasks + a fleet-red fix):** (1) **T-lin-10471 [S] P2 GVN slice — relational-commutative CSE (code c4fd2bff8):** mcc's CSE (available-expression, ast_cse_same) did commutativity for `+*&|^` only; added `==`/`!=` (a==b≡b==a, sound for int+float incl NaN) to ast_cse_commutative_op behind a new **default-off `-fgcse-comm-rel`** (ast_cse_comm_rel_env, latched beside ast_cse_comm_env; flag row at END of MCC_OPT_LIST to avoid enum renumber). Deliberately NOT threaded into the AST_SG_* gate/search mask (high-surface mccgate.h bijection); safe because default-off ⇒ shipped byte-identical, and when on it's gated UNDER ast_cse_comm_env which the gate machinery restores (replay-consistent) — only the -O13 search can't toggle it independently. cse 0→2, TDD cli/gcse_comm_rel. Left OPEN w/ residual (deep/nested comm matching, ordered-relational mirroring a<b≡b>a, true value-numbering) — DETAILS#t-lin-10471-gcse-comm-rel. Landscape via an Explore agent. (2) **T-mac-30291 [S] P2 loop-unroll — cond/shape surface COMPLETE across 3 slices:** i<=B + positive stride (5f545bd3b), descending i>B/i>=B + negative stride (ce515faaf, sign-aware trip base=asc?(B-I):(I-B), direction/sign guard, overflow-safe), and `!=` cond (691df1e73, NE validates the literal bound in-gate since ast_range_bound skips NE; trip=diff/stride, wrong-dir/non-divisible bail). Gate now fires on LT/LE/GT/GE/NE × any const stride∈[1,CAP], ascending+descending. Left OPEN w/ residual (b) PARTIAL unroll + remainder [the real project], (c) RUNTIME unroll, (d) while/do-while op2/4, (f) -O-level wiring→cross-fleet o0-rebank — DETAILS#t-mac-30291-{wider-shapes,descending,neq}. (3) **T-lin-10480 [S] P1 typecov — parts 1+1b, PARKED:** arm64-osx NATIVE typecov leg (cse/dse/licm/pre via int128-free `_noi128` subjects, 93398eeb7) + JIT `-run` verification for the whole typecov family (env MCC_TYPECOV_RUN, both mac legs, 7d45580f3). Remaining parts (2) type-widen [win now doing via sroa-typecov 3f4921e9], (3) `_below` boundary, (4) win/lin legs [win did the PE leg] — DETAILS#t-lin-10480-typecov-{arm64-osx,jit-run}. **FLEET-RED FIX — win's T-win-50053 loop-idiom broke main on BOTH mandatory gates:** trace-gate-invariant (12 braced branches in ast_loopidiom_apply/_run lacked leading MCC_TRACE — I fixed **dc5a32e12**) AND mcc_cross_build for i386/arm (ast_loopidiom called ast_promo_size_unknown, which sat inside the `#if X86_64||ARM64||RISCV64` guard — win fixed by making it unconditional, **2d9c6399**). Diagnosed both from a `-R selfhost` mcc_cross_build failure; confirmed pre-existing (git-blame → win's 762ed903), not my GVN. After pulling win's fixes, verified win's evolved (memcpy+memset) loopidiom + new `ast/sroa-typecov-{arm64,x86_64}-osx` (T-lin-10480 part 2) GREEN on both Mach-O legs. LESSON banked: win's loop-idiom slices verified native x86_64-PE only, never the cross build or the trace gate — a new caller of a guarded helper + missing trace markers. **Full DoD each code slice (all GREEN):** cli 480/exec 374/diff3 319/selfhost 30 (incl selfhost-jit 600s + all fixpoint + mcc_cross_build)/corpusgate 6 (ast/o0-baseline NO drift)/treegate 13. All changes default-off/o0-neutral. **PICK-UP for next mac session:** no active claims. Highest-priority takeable = continue T-lin-10471 GVN residual (deep/nested comm, ordered-relational mirroring — bounded, same file) or T-mac-30291 partial-unroll (the real parity project, needs a remainder loop). P1 T-lin-10480 parts 2/3 remain (win is helping w/ part 2). Both P2 gap tasks (10470 auto-vec, 10473 IPA/DAE, 10474 escape-analysis, 10475 TFA) are deep from-scratch passes. **Next mac ID = 30292.** Build: `CC=/usr/bin/cc cmake --preset macos` (NEVER without CC). Verify targeted `-R`, NEVER bare ctest. grep conflict markers before every push.
- SESSION CHECKPOINT (mac-arm64, 2026-08-21T22:44Z, goal=INSTRUCTIONS.md loop — user asked to wrap up): **T-lin-10480 typecov-x86_64-osx slice DONE (code 30866a84e); all pushed, tree clean.** Claimed T-lin-10480 (P1 type breadth) + landed the first slice: wired the ast typecov cells (cse/dse/licm/pre) onto the x86_64-osx Rosetta leg. **Key finding:** typecov subjects use `__int128` (mcc x86_64-only; arm64=multi-limb) → cse=4<6 on arm64, and the native cells were Darwin-EXCLUDED (guard NOT MCC_TARGETOS Darwin) so they never ran on mac at all. So the wiring is x86_64-osx-only (via cmake-macos-x64/mcc + transparent Rosetta + the FX_optfire_x64 freshness fixture) — extends type-complete-opt coverage to x86_64-Mach-O (distinct from lin's x86_64-ELF); 4/4 green. RESIDUAL (10480 IN_PROGRESS, large): arm64 int128-free subject + the widen-types half (8 passes × 10 types) + _below boundaries + win/lin legs → DETAILS#t-lin-10480-typecov-x86_64-osx. **Separate:** win's new switch_jt differ row (auto-globbed onto my legs) fires arm64-osx + x86_64-PE but NOT x86_64-osx-Mach-O (byte-identical -f/-fno, non-staleness; a cross-format nuance arch.txt's per-CPU pins can't express) → standing red on my x86_64-osx board, FYI'd win (their cell). **Earlier this turn: T-lin-10479 [S] JIT oracle layering DONE+ARCHIVED (code b56e84a3a); all pushed, tree clean, no active mac claims.** Added the gcc/clang differential (the 10479 half remaining after the AOT-vs-JIT slice): optfire.sh gains a gated OPTFIRE_ORACLE branch (compile src with the oracle cc + OPTFIRE_ORACLE_FLAGS, run under $RUN, assert == mcc -O0; oracle-uncompilable soft-skips). Probed CLEAN pre-wiring — clang==mcc-O0 on all differ srcs (arm64-osx 19/19, x86_64-osx via `clang -arch x86_64`+Rosetta 9/9, zero divergence) — so no per-cell triage. Enabled clang on both mac legs; each mac optfire cell now runs 3 oracle layers (JIT-self-consistency + AOT-vs-JIT + clang differential): 92/92 green, native optfire 149/149 unchanged, all default-off (lin/win legs byte-identical). **lin's key FYI folded in:** OPTFIRE_AOT=1 does NOT work on their qemu ELF legs out-of-the-box (crt1.o/exec-format — hosted-boot bakes qemu-prefix into the MCC wrapper w/ -run-oriented flags, no cross-AOT-link); so the ELF/PE oracle enablement is per-box (lin owns the qemu hosted-boot rework), NOT a flag flip — corrected in the DONE record. DETAILS#t-lin-10479-done. **Earlier this turn: T-lin-10479 AOT-vs-JIT slice DONE (9320c8e62)** (code 9320c8e62) + regstub-lint fleet-gate fixed (8cfb8751b); all pushed, tree clean.** (1) **regstub-lint GREEN** — at win's explicit request (they ceded the CMakeLists optfire-x86_64 block), rewrote their WIN32 block to per-row symmetric skip-stubs (dedup-outer + if(MCC_TEST_SH AND EXISTS)/elseif/else), clearing their 2 violations + my x64boot one; ci/registration-stubs now green (I can test the UNIX gate they can't). (2) **Claimed T-lin-10479 [S] (P1 oracle layering) + landed the AOT-vs-JIT slice:** optfire.sh differ+EXECVIA=run gains a gated OPTFIRE_AOT branch — for each -O{n}/gflag it AOT-compiles+runs and asserts AOT output == -run(JIT) output (mac -run = internal ELF relocator, distinct from Mach-O AOT). Default-OFF (additive; native optfire 149/149 unchanged, lin/win legs untouched); enabled on both mac legs (arm64-osx + x86_64-osx Rosetta, both AOT-link w/o SDK flags) → 92/92 green with the check, AOT branch provably runs. FYI'd lin (shared harness, opt-in for their qemu legs). RESIDUAL (10479 IN_PROGRESS): gcc/clang differential + fleet-wide AOT mandate → DETAILS#t-lin-10479-aot-vs-jit. **Earlier this turn: T-lin-10478/mac leg DONE** (code 13f5e8c77) — P1 Mach-O JIT `-run` coverage LANDED; all pushed, tree clean, no active mac claims.** Implemented the mac leg of lin's JIT-coverage matrix: a self-contained `if(APPLE)` block (CMakeLists ~:6710) registers `optfire-arm64-osx/<cell>` (native `$<TARGET_FILE:mcc>`) + `optfire-x86_64-osx/<cell>` (`cmake-macos-x64/mcc` under `arch -x86_64`) for every differ row, run-verified via `mcc -run` (OPTFIRE_EXECVIA=run) — **92/92 green**. Chose a separate block over extending lin's shared loop to avoid the `optfire-arm64/` cell-name COLLISION with the qemu `arm64|linux` row (OS-qualified `-osx` names). Reused lin's `_of_archnames`/`_of_archcpus` for arch.txt pins (regdisp→x86_64-osx only, etc.). **Key gotcha caught + fixed:** `cmake-macos-x64/mcc` is a manually-built sibling not tracked by MCC_BUILT → a STALE binary gave a false "DID NOT FIRE" (pre-unroll x64 mcc). Added an `optfire-x64boot/rosetta` FIXTURES_SETUP (`cmake --build cmake-macos-x64 --target mcc`) so the Rosetta mcc is rebuilt-fresh before each run (mirrors lin's optfire-boot). Gate-symmetric `else() mcc_skip_test` throughout (regstub-lint). **Verified:** 92/92 mac osx cells, native optfire 149/149 UNCHANGED, ci/gate-contract + must-run green, configure clean (no dup names), o0-neutral. `ci/registration-stubs` red is PRE-EXISTING (win's optfire-x86_64 block 86da969c1 drops 2 cells @6534/6556, stash-confirmed — FYI'd win with the fix). DETAILS#t-lin-10478-mac-coverage. Parent T-lin-10478 closes when win's PE leg lands (lin ELF/qemu + mac Mach-O legs both done). **Earlier this turn: T-lin-10469 loop-unroll full-unroll slice DONE+ARCHIVED** (code c5d075877) — a REAL P2 optimizer win landed + fleet-verified; all pushed, tree clean.** Implemented mcc's first `-funroll-loops` knob (default-off, gcc-aligned): `ast_unroll_run`/`_apply`/`ast_body_has_loop` in the `ast_func_end` loop-transform driver beside interchange/fusion/tile (+threaded into the 4 changed-flag chains), reusing the existing loop-nest/IV/bound framework + `ast_dup_sub` clone. Transform = splice `trip` clones of `[body;incr]` before the loop + unlink it (NO IV subst — the cloned `i++` keeps the IV correct; init store already sets i=I) → low-risk. Gate: op-3 for, `i<const`, stride 1, trip∈[1,8], innermost. Root debug find: `-funroll-loops` was in a pre-existing accept-and-ignore list (libmcc.c ~:3325) swallowing it before set_flag — removed. coverage.txt id32 gap→covered|unroll. TDD cli/loop_unroll_full (anti-vacuity) + optfire differ `unroll`. **Full validation green:** cli 475/475, exec 374/374, diff3 319/319, selfhost {smoke/o3/output-parity/fixpoint} 15/15, corpusgate 6/6 (NO o0/rir drift), o0-neutral (default==-fno); treegate's only 2 reds = PRE-EXISTING docs/refs (lin's dangling cprop-typecov allow-entry, stash-confirmed). Edge matrix (neg-init/cap-boundary/over-cap-bail/unsigned/trip-0/stride≠1-bail) all output-correct. Residual (partial/runtime unroll + wider shapes + -O-level parity wiring) → minted **T-mac-30291** [S] P2. **MID-TURN: lin LANDED T-lin-10476[C] (d40e1a903) → T-lin-10478[P]-mac UNBLOCKED (P1).** Finished landing the green 10469 slice (didn't abandon mid-commit), now PIVOTING to P1 10478-mac (my Mach-O `-run` legs are instant-ready; lin gave the exact `_of_matrix` wiring recipe). Next mint 30292.
- SESSION CHECKPOINT (mac-arm64, 2026-08-21T20:37Z, goal=INSTRUCTIONS.md loop): **CLAIMED T-lin-10469 [S] loop UNROLLING (P2, IN_PROGRESS @8b91f904b) + published an implementable design; all pushed, tree clean.** Per the Stop-hook nudge (takeable tasks remain), selected the highest AVAILABLE mission tier: P1 is blocked/deferred (10478/10489-mac DEPS-blocked; 10477 deferred to lin), so pulled **P2 (optimizer parity)**. Surveyed the full OPEN queue: every P2 optimizer task is a deep from-scratch/multi-turn pass (10469 unroll, 10470 auto-vec, 10471 GVN, 10473 IPA, 10474 escape-analysis) and 10422's "quick win" is explicitly FOCUSED-SESSION-ONLY/self-host-miscompile-prone; every remaining mac HIGH bug (30036 _Float16 ABI = 3 reverted spikes, 30077 vector ABI, 30055 done-slice) is a documented deep/focused-session effort — **no bounded single-turn mac win exists** (re-confirms 15 prior checkpoints). Chose **loop unrolling** as the safest real P2 progress: implement behind a **DEFAULT-OFF `-funroll-loops`** knob so every slice is inert-by-default (o0-neutral, self-host-safe), reusing mcc's existing loop-nest+IV+bound framework (ast_loopnest_build/ast_loop_find_iv/ast_loop_classify_bound) + ast_interchange_apply's arena-mutation pattern + the inliner's graft clone. **This turn = design (DETAILS#t-lin-10469-loop-unroll-design, directly implementable: infra map, trip-count gate, apply plan, slice sequence, mandatory self-host validation per the 10422 lesson, enum-shift/fmt-census hazards) + claim + CLAIM'd lin.** Transform slice 1 lands next turn WITH full self-host validation (NOT rushed tail-of-turn). 10478-mac stays instant-ready; PARK 10469 + pivot to it the moment 10476[C] lands (P1>P2). Next mac ID 30291 (no IDs minted).
- SESSION CHECKPOINT (mac-arm64, 2026-08-21T20:23Z, goal=INSTRUCTIONS.md loop resumed): **mac resumed; both mission lanes still DEPS-blocked; verified 10478-mac instant-ready + probed 10477; no mac claim (deferred to lin, its coupled domain). All pushed, tree clean, no active mac claims.** (1) **T-lin-10476[C] is now CLAIMED by lin-x64 (5fd8e9763)** — the P1 fan-out (10478[P]/10479/10480) stays DEPS-blocked until lin lands it; T-lin-10489[X]mac still blocked on 10404+10482[P]. (2) **T-lin-10478[P]-mac RE-VERIFIED INSTANT-READY:** both Mach-O `-run` legs green today (arm64-osx native + x86_64-osx Rosetta via the still-built `cmake-macos-x64`, O0/O2/O4, fib-sum 10945) — will wire into 10476's matrix the moment it lands. (3) **T-lin-10477 (P1 [S], only takeable P1 task) evaluated + DEFERRED to lin:** the phantom cells need shape-tuned sources matching mcc's exact pass-firing predicates — id79 switch's `tree-switch-conversion` gate does NOT fire on a canonical dense switch at -O4 (byte-identical, default-off); `tree-loop-im` is byte-identical on a naive invariant though the tuned `licm.c` fires. Deep optfire/optimizer-internals = lin's domain (suite author + 10476 owner; rated low-value-for-mac). Additive src+ledger rows would be conflict-free with 10476's registration-loop refactor (cells globbed from ledgers, ratchet expects additions), so it CAN parallelize with lin's firing-shape guidance. Findings + readiness banked: DETAILS#t-lin-10477-mac-probe-2026-08-21; FYI'd lin+win. Next mac ID 30291 (no IDs minted).
- ⏸ RESUME HANDOFF (mac-arm64, 2026-08-21T19:46Z — session PAUSED at USER request; all pushed, tree CLEAN, NO active mac claims). **Pick-up guide for the next mac-arm64 session:**
      **MISSION (docs/GOAL.md, rewritten flat→PRIORITIZED per USER, lin 6552e0a8b):** P1 = 100% JIT `-run` coverage per OS/triple > P2 = optimizer parity w/ gcc+clang then beyond > P3 = every AST/RIR node/slice on GPU (Vulkan+Metal) except anon/external INVOKE. Rule: among §11-takeable tasks, take the highest mission priority; subordinate to §11 type-order + DEPS gate.
      **MY 2 ASSIGNED MAC LANES (BOTH currently DEPS-BLOCKED — RE-CHECK on resume, DEPS may have cleared):**
      **(1) T-lin-10478[P]-mac (P1) — Mach-O JIT run-verify leg.** DEPS: T-lin-10476[C] (lin's contract; was UNCLAIMED @19:46, lin user-driven + expects to take it). WHEN 10476[C] LANDS: `git pull`, claim 10478-mac (§4.3), wire the TWO CONFIRMED Mach-O runners into optfire's new triple-registration matrix per 10476[C]'s interface: (a) **arm64-osx native** `-run` = `cmake-macos/mcc`; (b) **x86_64-osx via Rosetta** — an mcc binary is ALREADY BUILT at **`cmake-macos-x64/mcc`** (x86_64 backend, `-run` enabled), run via `arch -x86_64 cmake-macos-x64/mcc -run`. Both are can-run?=TRUE in the contract (lin baked in, e2f3c7e43). Recipe + the `MCC_TARGET_ARCH`-not-`MCC_CPU` crux + full method: **DETAILS#t-lin-10476-mac-runner-verification**. (x86_64-PE wine = deferred canary; win owns PE natively.)
      **(2) T-lin-10489[X]mac (P3) — Metal FRAME KERNEL BUILDER** (only mac can do it; needs the M1 Pro Metal GPU). DEPS: T-lin-10404 (GPU strategy-4 frame/loop/store executor into the JIT TU) + T-lin-10482[P] (GPU program) — both OPEN/not-DONE @19:46. Goal: give the Metal arm the frame/loop/store executor the Vulkan arm has (M2–M5 slicerun cells currently SKIP "Metal arm has no frame kernel builder"). Spec + GPU architecture: **DETAILS#t-lin-10482-gpu-node-coverage**.
      **IF BOTH LANES STILL BLOCKED ON RESUME:** the only other P1-takeable task is **T-lin-10477** (optfire phantom-coverage cells, DEPS none) — but it's lin's optfire domain (they did the audit at DETAILS#t-lin-10476-optfire-audit); coordinate w/ lin before claiming (they offered it but rated it lower-value for mac). P2 optimizer tasks (10469/70/71/73/74/75, 10425, 10455/10457) are deep JIT/x86_64/lin-domain. Accessible clean single-turn mac DIAGNOSTIC wins are EXHAUSTED (~15 prior checkpoints).
      **BOX FACTS (still valid):** native `gcc-16`+`clang` oracles; x86_64-osx runs via **Rosetta** (`arch -x86_64`; +SDK `.tbd` link for AOT); x86_64-PE runs via `x86_64-w64-mingw32-gcc`+**wine**; arm64-osx native JIT `-run` works. CANNOT run x86_64-Linux ELF, arm64-Windows, or cross-GPU. **Build:** `CC=/usr/bin/cc cmake --preset macos` then `cmake --build cmake-macos` — NEVER reconfigure `cmake-macos` without `CC` set (blanks the compiler; see [[native-build-macos-preset]]). **Verify:** `ctest --test-dir cmake-macos -R <regex> --timeout N` — NEVER bare `ctest` (pre-existing flagsweep hang). Commit docs edits BEFORE `git pull --rebase`; `grep -nE '^(<<<<<<<|=======|>>>>>>>)' docs/*.md` before every push.
      **BANKED FINDING (deprioritized under new goal; ready-to-implement if diagnostics resurface):** `-Wsizeof-pointer-div` is ~fully designed at **DETAILS#t-mac-30265-sizeof-pointer-div-design** (hook `expr_infix` at `/`, per-operand sizeof-provenance, gcc type-compat rule). **Next mac ID = 30291** (Sessions table; 30270–89 skipped after the T-mac-30290 out-of-order-mint N9 fix).
- SESSION CHECKPOINT (mac-arm64, 2026-08-21T19:31Z, /goal loop — GOAL.md pivoted to P1/P2/P3 mid-turn): **P1 mac-runner research published; all pushed, tree clean, no active mac claims.** GOAL.md was rewritten (lin 6552e0a8b, per USER) flat→PRIORITIZED (P1 100% JIT `-run` coverage per OS/triple > P2 optimizer parity+beyond > P3 GPU node coverage). My assigned mac lanes — **T-lin-10478[P]-mac** (Mach-O JIT run-verify) + **T-lin-10489[X]mac** (Metal frame kernel builder) — are both DEPS-BLOCKED (on T-lin-10476[C] / T-lin-10404+10482[P] resp.). 10476[C] is lin's contract+CMake domain (UNCLAIMED; lin user-driven, expects to take it). Non-idle + non-racing move: **empirically de-risked the contract's ASSUMED-runnable mac RUNNER rows** (DETAILS#t-lin-10476-mac-runner-verification) — **(a) arm64-osx native `-run` CONFIRMED** run-verifiable (correct -O0/2/4 vs AOT); **(b) x86_64-osx via Rosetta `-run` CONFIRMED + RECIPE** (build the mcc BINARY as x86_64 w/ the x86_64 backend via `-DCMAKE_OSX_ARCHITECTURES=x86_64 -DMCC_TARGET_ARCH=x86_64` [NOT MCC_CPU, silently overridden→arm64 backend→-run off], build runmain.o+libmccrt.a, `arch -x86_64 mcc -run` → JIT'd x86_64 runs in-process under Rosetta; verified -O0/2/4); **(c) x86_64-PE wine DEFERRED** (canary; win owns PE natively; mingw-PE-host mcc.exe build is the follow-up). So BOTH Mach-O legs flip can-run?=TRUE (not fired-only) — pinged lin to bake into 10476[C]. `cmake-macos-x64` kept built for 10478-mac. **N9 FIX (per lin's docs-hygiene review):** T-mac-30290 was minted out-of-order (DONE+ARCHIVED) above my Next-ID counter (30270) → sequential mint would reuse 30290; bumped Next ID to **30291** (max-minted+1); 30270–30289 intentionally skipped.
- SESSION CHECKPOINT (mac-arm64, 2026-08-21T19:07Z, /goal loop): **2 mac wins + cleared a fleet-red from win's just-landed change; all pushed, tree clean, no active mac claims.** (1) **T-mac-30269 DONE+ARCHIVED (2e6ee277a) — a REAL nondeterministic `_Generic` miscompile, and the ROOT of the recurring tgmath/complex arm64-osx "drift" earlier checkpoints chased as lin's `base->bs`:** scalar constant literals (`4.0f`/`4.0`/`4.0L`/int/char) were pushed at `push_tokc` (unary_nested, mccgen.c:14057) with only `type.t` set, leaving `CType.bs` — the **FSPELL spelling tag** `compare_types` checks for float/double/ldouble since lin's T-lin-10463 (mccgen.c:4602) — as STALE GARBAGE from the local's prior use. So `_Generic((4.0f), float:sqrtf, default:sqrt)` (mcc `<tgmath.h>` for `sqrt(4.0f)`) nondeterministically failed the `float:` match and fell to `default` (double) **depending on the mcc binary's memory layout** — hence any unrelated code addition flipped `tgmath_dispatch.c` between `ldr s0`/`fcmp s` (float, correct) and `ldr d0`/`fcmp d` (double); exec-benign ONLY because sqrt(4)=2 is exact in both widths (a non-exact arg = real miscompile). FIX = zero `.ref`/`.bs` for scalar literals; tgmath now deterministic at the CORRECT float-dispatch **bank** value (o0-baseline 5/5, no rebank). cli/generic_float_const_dispatch. DETAILS#t-mac-30269-generic-float-const-bs-uninit. (2) **T-mac-30265 SLICE — `-Wsizeof-array-argument` (2e6ee277a):** warns `sizeof` of a fn param declared `T a[N]` (decayed `T*`) ==gcc-16 default-on across a 17-case matrix; per-Sym `SymAttr.sizeof_array_param` bit (no struct growth) + a file-scope hint set at the identifier leaf, reset at sizeof-start, cleared by gen_op/comma/inc, guarded by operand ref-identity; o0-neutral (clean once #1 removed the arm64-osx layout landmine). cli/sizeof_array_argument_warn. DETAILS#t-mac-30265-sizeof-array-argument. (3) **CLEARED a fleet-red from win's T-win-50045 (19df84a58):** win's `-fno-common` set_elf_sym narrowing (3d06e503) regressed arm64-osx (win-unmeasurable) — a valid single-TU alias (`int g=34; int x __asm__("g");`) errored "'_g_int' defined twice", breaking exec/alias + o0-baseline (objects 311→310). Narrowed to multi-def only when the EXISTING sym is ALSO tentative (bss/common); real+tentative coalesces, win's cli/fno_common_multidef feature preserved. DETAILS#t-win-50045-arm64-osx-tentative-real-regression. Full native suite green (o0-baseline 5/5; exec/diff3/treegate/cli 0-fail). CONTRACT'd win + FYI'd lin (both hot paths — re-verify ELF/PE o0 on pull). Next mac ID 30270.
- SESSION CHECKPOINT (mac-arm64, 2026-08-21T16:13Z, /goal loop): **TWO distinct varargs miscompiles fixed this session, both from the T-mac-30062 -O2 differential; all pushed, tree clean, no active mac claims.** (1) **T-mac-30045 sub-item 1 (x86_64 SysV 2-SSE va_arg, d9f07d18's predecessor 7ec57708):** `__va_arg_inline` consumed the last XMM slot for a size==16 all-SSE aggregate before confirming both eightbytes fit → a later scalar double the caller backfilled into the leftover XMM was read from overflow (LAST 88.1 not 99.5, pre-fix rc134). Fixed callee-side (test both slots up-front). Verified x86_64-osx AOT+Rosetta vs clang; x86_64-osx rebanked (5 va_arg files), **lin rebanked x86_64 ELF (487e9794a) + confirmed cli/x86_64_sysv_vararg_2sse_boundary PASSES natively on x86_64-Linux** — fully verified fleet-wide. Task stays OPEN (sub-item 2 arm64-ELF gen_va_arg assert → lin, qemu-aarch64-reachable there). (2) **T-mac-30268 DONE+ARCHIVED (d9f07d18):** arm64 Mach-O miscompiled a fully-variadic fn with ZERO named params (`void f(...)`+`va_start(ap)`, C2y form) — `arm64_pcs_aux` used the named-param COUNT as its is-variadic predicate, so var_nb_arg==0 was indistinguishable from non-variadic → all args register-classified, Mach-O stack transition never fired, va_start pointed at the empty stack area → garbage. Threaded a separate `is_variadic` bool. Fixes gcc.c-torture pr117432 (rc134→rc0 -O0..-O3); ==gcc-16 across int/ll/double/struct/char*/12-int-spill/float-mix + named-varargs regression. **o0-baseline NEUTRAL on ALL keys** (no corpus `f(...)` fn → zero drift; arm64-osx bank unchanged) — clean solo, no rebank owed. x86_64/arm64-ELF already correct (predicate is MCC_TARGET_MACHO-gated); PE-branch part is a latent arm64-win32 bonus (FYI'd win). cli/arm64_unnamed_varargs_c2y (cpu=arm64, verified PASS on mac). Full native suite green throughout (cli 464/exec 374/treegate 13/diff3 319). Memory added: [[runtime-header-embedded-needs-rebuild]] (mccdefs.h edits need `ninja mcc`; x86_64-osx rebank recipe). Next mac ID 30269.
- SESSION CHECKPOINT (mac-arm64, 2026-08-21T15:15Z, /goal loop): **T-mac-30062 20230630-2/-4 SLICE DONE (reverse-SSO initializer) + arm64-osx re-rebank from lin's T-lin-10464 fix; pushed, tree clean.** Picked the reverse-SSO bitfield miscompile from last turn's -O2 differential and fixed it: the compile-time INITIALIZER path (init_putv static + local vstore) ignored `scalar_storage_order` (stored native little-endian) while the RUNTIME store path was already correct. Fixed both paths (threaded `p->reverse_so` through decl_initializer_nested; MSB-first bitfield pack via `packed_bf_rev_span` + scalar byte-reverse for the static-data path; `VT_REVSO` on the store lvalue for the local path). ==gcc-16 on 20230630-2/-4 (-O0..-O2) + scalar/bitfield static+local matrices; o0-neutral (gated on p->reverse_so, no corpus reverse-SSO init); cli/reverse_sso_initializer; exec 374/diff3/cli/treegate/o0-baseline green. **Resolved a chased red-herring via lin CONTRACT (9e791e772):** the complex/tgmath arm64-osx "drift" I'd been rebanking (incl. last turn's 97094c50) was lin's T-lin-10464 uninitialized-`base->bs` garbage-read (non-deterministic complex codegen on osx keys; my binary just perturbed the garbage) — lin fixed it, I pulled + re-rebanked arm64-osx from the fix (3 files revert to deterministic pre-bug bytes). x86_64-osx CHECK clean at the fix. T-mac-30062 residual: pr70460 (→T-mac-30197) + QoI/varargs/FORTIFY clusters. No IDs minted.
- PROBE NOTE (mac-arm64, 2026-08-21T14:46Z, /goal loop): ran a FRESH mcc-vs-gcc-16 -O2 differential over the full c-torture/execute corpus (1694 tests) to hunt new miscompiles — 13 found (1513 pass, 167 unsupported), all triaged by opt-level + routed to deep areas (DETAILS#t-mac-30062-o2-differential-2026-08-21). None a clean single-turn win: REAL codegen = reverse-SSO bitfields (20230630-2/-4, T-lin-10394), varargs (pr117432/pr92904/20071213-1, T-mac-30045), setjmp+alloca (pr64242); QoI/optimizer-strength = 20030125-1/990208-1/builtin-constant (T-mac-30069/inline/T-mac-30149); FORTIFY printf-fold ×4 (T-mac-30073). Banked as a prioritized concrete-miscompile list for a focused session (highest REAL value: reverse-SSO bitfields + varargs). This session's fixes (pr85156/980526-1/stdarg-4) no longer appear. No code landed this turn; no IDs minted.
- SESSION CHECKPOINT (mac-arm64, 2026-08-21T14:34Z, /goal loop): **T-mac-30062 stdarg-4 SLICE DONE (5268c4b4); pushed, tree clean, no active mac claims.** Verify-first re-diagnosed stdarg-4: NOT a static-fn issue — it's `inline __attribute__((always_inline))` fns CALLED but not inlined at -O0 (T-mac-30101 architectural) while plain-inline correctly emits no external def (conformant) → `U _f1i`. Minimized to: ANY called plain-inline+always_inline emits `U _f` (gcc inlines it). T-mac-30101's "already works via emitted def" de-risk had a GAP — that's the extern-inline+always_inline static-copy (T-mac-30113), not plain-inline+always_inline. FIX: extend gen_inline_functions' weak-emit to also fire for a referenced `type.ref->f.func_alwinl` (a not-inlined always_inline gets a weak out-of-line body — resolves the ref; gcc inlines, mcc emits weak, both link+run). Surgical (plain inline unchanged, guarded), o0-neutral (zero corpus always_inline), cli/always_inline_out_of_line_emit, exec 374/diff3 319/cli 460/treegate 13/o0-baseline 3 green. **T-mac-30062 now down to ONE residual: pr70460** (label-DIFF, blocked on T-mac-30197 SUBTRACTOR object-writer); the other 4 cross-oracle sub-bugs (pr85156, pr40657, 980526-1, stdarg-4) all DONE. No IDs minted.
- SESSION CHECKPOINT (mac-arm64, 2026-08-21T14:18Z, /goal loop): **T-mac-30062 980526-1 SLICE DONE + a fleet-owed arm64-osx o0 rebank; both pushed, tree clean, no active mac claims.** (1) **980526-1 (8540b96e):** a `__label__` (GNU local label) whose address is taken (`&&lbl`) emitted `U _lbl` (unresolved) — local labels pop at BLOCK exit where nocode_wanted has CODE_OFF_BIT (dead tail after return), tripping put_extern_sym's `nocode_wanted && s==cur_text_section` skip-guard. FIX: clear nocode_wanted around put_extern_sym in label_pop (a defined address-taken label is reachability-independent). o0-neutral, cli/local_label_address, exec 374/diff3 319/cli 459/treegate 13 green. (2) **arm64-osx o0 rebank (97094c50):** caught the ast/o0-baseline gate RED from lin's concurrent `_Decimal`/`_Complex _BitInt`/`_Fract` type-system slices (4b878fb5/b77400db/675c6b82) drifting complex.c/complex_annexg.c/tgmath_dispatch.c on the mac-only arm64-osx key (lin can't measure it) — all 3 pass native arm64 exec, re-banked obj.txt-only, gate 5/5 green. T-mac-30062 stays OPEN (pr70460 label-DIFF→T-mac-30197, stdarg-4 static-fn-addr). No IDs minted.
- SESSION CHECKPOINT (mac-arm64, 2026-08-21T13:55Z, /goal loop): **T-mac-30062 pr85156 SLICE DONE (f11932b9) — a real miscompile fixed; pushed, tree clean, no active mac claims.** Bug-hunted a concrete c-torture miscompile: `__builtin_expect(exp, c)` parsed the hint `c` under nocode_wanted++, dropping its side effects, so `expect(x?y!=0:0, z++)` never ran z++ → foo(10)=10 not 11 (rc134 abort). Minimized against gcc-16/clang: the hint IS evaluated at runtime unless `exp` constant-folds. FIX: `drop_c = exp_const || vtop->r==VT_CMP` (evaluate c for side effects unless exp folds or is a flag-based VT_CMP whose materialization the code-neutral invariant forbids for constant hints). Strict improvement (VT_CMP/const paths byte-identical to pre-fix); pr85156 green -O0..-O3, builtin_expect_is_code_neutral preserved, o0-baseline arm64 byte-identical, cli 452/exec 374/diff3 319/treegate 13 green. Documented RESIDUAL (bare-cmp exp + side-effecting hint needs a two-pass const-detect+token-replay) + kept T-mac-30062 OPEN (pr70460, 980526-1/stdarg-4 remain). No IDs minted.
- PROBE NOTE (mac-arm64, 2026-08-21T13:37Z, /goal loop): verify-first probed two mac Open tasks, both deep (not single-turn), findings banked: **T-mac-30257** (-Wsequence-point part-a) confirmed still-blocked — the flat seqp (Sym,off) model can't separate a `++`-codegen read from a program read (prior attempt reverted 74cd7fc3, broke sso/loop_cond); needs a real per-subexpression sequencing model. **T-mac-30238** (wide `_BitInt` bitfields) RE-SCOPED + routed to lin/x86_64 (DETAILS#t-mac-30238-bitint-bitfield-rescope): `_BitInt(65..128)` is scalar VT_INT128 only where MCC_HAVE_INT128 (x86_64-non-PE); arm64 uses a multi-limb VT_STRUCT — so the x86_64 fix (MED: allow VT_INT128 + raise the LLONG-capped width to 128 at 3 sites) is best from lin/x86_64, and the arm64 multi-limb packed-bf rewrite (HIGH) is a distinct follow-up. Consistent with the standing finding that accessible clean single-turn mac wins are exhausted; mac Open remainder is deep-codegen / cross-fleet-coordinated / GPU. No IDs minted.
- SESSION CHECKPOINT (mac-arm64, 2026-08-21T13:28Z, /goal loop): **T-lin-10416 CLOSED (whole task now DONE) + T-mac-30267 header bug DONE; both pushed, tree clean, no active mac claims.** Claimed win's released x86_64-osx rebank tail. (1) **T-mac-30267 (2ea11256):** `runtime/osx/include/math.h` was missing the long-double math set (`expl`/`logl`/… — only had 6 `l`-fns); on x86_64-osx (80-bit LD) tgmath.h's distinct-LD `_Generic` form references `F##l` in every non-selected branch, and C requires those declared (gcc AND clang both error) → `tgmath_dispatch.c` failed `'expl' undeclared` (invisible on arm64 where LD==double). Added the `l`-set; o0-neutral on arm64-osx (CHECK OK), restores tgmath on x86_64-osx (objects 315→316). (2) **T-lin-10416 x86_64-osx rebank (aa7fc6f6) — completes the task** (win did all 4 *-win32): 13 obj rows drifted from pure-frontend changes (extern-inline 786a018c + _Float/predefines 9ee3bb6a; __GNUC__ Apple-pinned-inert on osx; in-window x86_64 backend changes all -O1+). **Exec-verified all 14 natively** — the mac-only capability: x86_64-osx Mach-O built via cross mcc + `-L$(xcrun --show-sdk-path)/usr/lib` (SDK libSystem.tbd is the link-unlock), run under Rosetta, diffed vs `clang -arch x86_64`. 12 match oracle/self-check; c11_freestanding_headers mcc==clang (both FAIL — x86_64-arch test); ldouble_to_signed differs on near_one only = pre-existing T-mac-30030 host-LD-fold (cross from 64-bit-LD host), not a new regression. Ungated+gated CHECK 0-drift, obj.txt +13/-13, arm64-osx untouched, treegate 13/13, arm64 o0-baseline 5/5. Recipe banked (DETAILS#t-lin-10416-x86_64-osx-rebank-done). Minted+closed T-mac-30267 (Next 30268). [[cross-target-object-verify-and-preexisting-reds]] extended: osx-cross exec-verify via SDK .tbd + Rosetta + clang oracle.
- SESSION CHECKPOINT (mac-arm64, 2026-08-21T12:59Z, /goal loop): **2 code landings + a fleet-red cleared + a peer BLOCKER resolved; both pushed, tree clean, no active mac claims.** (1) **T-mac-30265 SLICE (ec6b8a26):** `-Wparentheses` `&&`-within-`||` (`a && b || c`) ==gcc-16 (once-per-`||`-node left-preferring rule, not clang's warn-both), `expr_landor` first_op + per-operand top-op tracking; o0-neutral; cli/logical_paren_warn. The -Wparentheses gen-hookable family is now landed; T-mac-30265 residual = only the 2 AST-provenance warnings. (2) **T-mac-30266 DONE+ARCHIVED (89d53f75) — cleared the fleet-red `mcctest`/`-embedjit`/`-bcheck`:** a `switch` under CODE_OFF_BIT dead code (`if(0){switch…}`) revived the region — the per-case fallthrough tracker `ft_last_ind = gind()` calls gind() → CODE_ON() → clears CODE_OFF_BIT, so case-body + post-switch statements emitted live → a dead `undefined_function()` leaked an unresolvable reference; guarded both sites with `cur_switch->nocode_wanted` like the adjacent def_sym line. DISTINCT from lin's static-inline `retain` fix (ea511bde) — that GCs on mac/ELF; the switch case-body call is a direct reloc so it link-errored on mac AND PE. **CONFIRMED cross-target on x86_64-win32** (pre-fix cross mcc emits `U undefined_function`, fixed does not) — this IS win's unbisected mcctest red; win RETRACTED its 786a018c/T-mac-30113 BLOCKER (mis-bisect, confirmed). Full mac suite green (cli 451/451, exec 374/374, diff3 319/319, corpusgate 6/6 no o0-drift, treegate 13/13, mcctest×3 now PASS). Verify-first proved the mcctest red PRE-EXISTING on HEAD (not from my slice). Minted+closed T-mac-30266 (Next 30267).
- SESSION WIND-DOWN (mac-arm64, 2026-08-21T06:15Z, /goal loop): **2 DONE this session incl. the fleet centerpiece; all pushed, tree clean, no active mac claims (T-mac-30028 stays intentionally-stale/TTL-resumable).** (1) **T-mac-30290 + T-mac-30178 DONE+ARCHIVED (code 9ee3bb6a + lin x86_64 rebank 740f9a20 + archive bdf35111):** the atomic `__GNUC__` 4→7 bump + `_Float32`/`_Float64`/`_Float32x`/`_Float64x` keyword set (+f32/f64 suffixes), fully landed + verified on ALL legs — mac o0-baseline arm64-osx neutral (mccdefs.h `__APPLE__` pin keeps macOS 4.2.1, bump inert on mac; only `-dumpversion`→7.5.0), lin self-host+`_GNU_SOURCE`+qemu-riscv64 native 1980/1980 + x86_64 3-file o0 rebank, win exec+cli 8662/8662 canary. Drove it through TWO verify-first lin BLOCKER iterations (bump-alone-breaks-self-host → atomic bump+keyword; then glibc also needs the extended-precision `_Float32x`/`_Float64x` → extended the keyword set) — both glibc-only gaps INVISIBLE to the macOS-SDK box, caught by lin's qemu/native-glibc leg before shipping (the 3-machine protocol earning its keep). KEY reusable finding banked (DETAILS#t-mac-30290-combined-atomic-patch-mac-verified): `__GNUC__` is PER-OS in `runtime/include/mccdefs.h` (Apple/OpenBSD pin 4, NetBSD 4.1, FreeBSD 9; Linux/Windows use the `src/mcc.h` predefine). (2) **T-mac-30249 DONE+ARCHIVED (006a07c2):** empty struct/union GLOBALS get distinct addresses ==gcc/clang, o0-neutral (globals+struct-only scoping), confirmed green on all 3 platforms. (3) **DO-NOT-REDO sweep** (verify-first vs gcc-16+clang): T-mac-30217/30189/30194/30247/30248/30257 all confirmed deep/blocked/already-done (details in the 04:55Z checkpoint + each task line). Memories added: [[selfhost-jit-timeout-is-thermal]] (a lone selfhost-jit 720s timeout in a long session = throttling, verify vs pristine HEAD). Accessible clean single-turn mac wins are exhausted; remaining mac Open is deep-codegen / cross-fleet-coordinated / GPU / architecturally-blocked. No new IDs minted (Next 30266).
- SESSION CHECKPOINT (mac-arm64, 2026-08-21T04:55Z, /goal loop): **1 full DONE + a fleet BLOCKER-plan correction + a verify-first probe sweep; all pushed, tree clean.** (1) **T-mac-30249 DONE+ARCHIVED (006a07c2):** empty struct/union GLOBALS now get distinct addresses == gcc/clang (mccgen.c:19254 decl_initializer_alloc reserves 1 byte for a named size-0 VT_STRUCT global; sizeof stays 0, array stride 0). Scoped GLOBALS-ONLY (locals oracle-AMBIGUOUS — gcc-16 itself collapses zero-size locals per stack layout) + STRUCT/UNION-ONLY (excluding zero-length-array globals like flexarray.c:20 `int empty[]={}` keeps it o0-NEUTRAL) → clean single-turn slice where the prior investigation had deferred it cross-fleet. Full native suite green; **lin confirmed the cell green + zero o0-drift on x86_64** — [S] fix validated cross-platform. Zero-length-array-global residual → future cross-fleet turn (DETAILS#t-mac-30249-empty-aggregate-global-distinct-addr). (2) **T-mac-30290 (GNUC 4→7) mac leg verified earlier this session (both options green, per-OS mccdefs.h discovery); landing plan CORRECTED per lin BLOCKER 25807e05a — the bump + T-mac-30178 `_Float32`/`_Float64` keywords MUST land ATOMICALLY (bump-alone breaks self-host/`_GNU_SOURCE`). NEXT = a dedicated mac focused-session for the combined atomic landing + arm64-osx rebank, then lin re-runs its leg + applies the staged x86_64 rebank.** **UPDATE 2026-08-21T05:46Z: COMBINED PATCH MAC-VERIFIED + BANKED (DETAILS#t-mac-30290-combined-atomic-patch-mac-verified): keywords (revert-inverse of 4ea50353) + bump, full mac native suite green (o0-baseline arm64-osx neutral, cli 441/exec 374; selfhost-jit's 720s timeout proven ENVIRONMENTAL — pristine HEAD times out identically under this session's thermal load). WIN LEG GREEN (win 03213d0a — no win rebank; mcc-win uses own old headers, keyword-inert, _Float32-immune). ONLY lin's re-run now gates the atomic landing (apply combined patch → self-host/_GNU_SOURCE/qemu-riscv64 green + apply staged x86_64 rebank). Held unpushed; mac (coord) lands once lin confirms.** **UPDATE 2026-08-21T06:00Z (lin 2nd BLOCKER): lin's re-run found the patch STILL RED for Linux — glibc@7 _GNU_SOURCE also uses `_Float32x`/`_Float64x` (strtof32x/strtof64x, bits/floatn.h) which the re-added keyword set lacked. EXTENDED the combined patch: added `_Float32x`→double + `_Float64x`→long double keywords (non-distinct aliases per glibc __HAVE_DISTINCT_FLOAT{32,64}X 0) + extended cli cell; mac-re-verified (cli 441/exec 374/o0-baseline neutral). Re-pinged lin to re-run on the extended patch. DETAILS addendum banked. (docs/refs treegate red in-tree is lin's unrelated dangling T-lin-10020 citation, TODO.md:495 — flagged to lin.)** **✅ LANDED 2026-08-21T06:10Z — SHA 9ee3bb6a (atomic bump+keywords, all 3 legs green: lin self-host+_GNU_SOURCE+qemu-riscv64 1980/1980, win exec+cli 8662/8662, mac o0-baseline arm64-osx neutral). AWAITING lin's x86_64 3-file o0 rebank (complex_cmplx_special/flt_eval_method/fp_wide_return) to close the x86_64 o0 RED window; then T-mac-30290 + T-mac-30178 archive DONE. win canary re-verify + no win rebank. arm64-osx/riscv64 need no rebank.** (3) **DO-NOT-REDO (verify-first probed vs gcc-16/clang this session):** T-mac-30217 unknown-asm-directive already emits "unknown directive" ==both oracles (stale sub-item); its asm-shift-precedence is oracle-ambiguous (mcc==GNU-as, clang-as differs); column-numbers = data-model. T-mac-30194 only-remaining item is a proven dead-end. T-mac-30189 items are non-bugs / deep-optimizer (UB self-consistent, missing CSE/object-size). T-mac-30247 constexpr = architectural blocker (installs scalar immediate, no storage). T-mac-30248 `_BitInt` switch: `_BitInt(≤64)` ALREADY WORKS all targets; `_BitInt(65..128)` rejected ONLY on non-`__int128` targets (`MCC_HAVE_INT128` = x86_64-non-PE only, mcc.h:1240-1244) → **likely ALREADY WORKS on x86_64-Linux; a lin/x86_64 probe can confirm + rescope the task to non-int128 targets** (full analysis already at DETAILS#t-mac-30248-switch-bitint-wide: gcase-truncation + cast-bug, deferred behind the cast fix). Net: accessible clean single-turn mac wins remain largely exhausted (consistent with prior checkpoints); remaining mac work is deep-codegen / cross-fleet-coordinated / GPU. No new IDs minted.
- FINDING (mac-arm64 2026-08-21T02:20Z, DO-NOT-REDO): mcc WARNS "unsupported option" on an unknown `-Wno-<x>` where gcc/clang are SILENT (gcc's forward-compat: `-Wno-<unknown>` always accepted). I implemented+verified the gcc-aligning fix (libmcc.c:3378 set_flag<0 -> silently accept the `no-` form) — it works — but REVERTED it: it's a DELIBERATE mcc design (warn_unsupported_option field + `-Wno-unsupported-option` suppression + the cli/unknown_w_option_is_not_fatal test), and the current behavior is already BUILD-SAFE (non-fatal even under -Werror; the test proves `-Werror -Wno-made-up` compiles). The only delta is log NOISE for legit `-Wno-*` hardening flags, vs the design's value of catching typo'd `-Wno-` names. A judgment-call FLEET design decision (build-compat noise vs typo-catching), NOT a bug — leave unless the fleet decides to gcc-align it.

- SESSION CHECKPOINT (mac-arm64, 2026-08-21T01:01Z, /goal loop): all pushed, tree clean, no active mac claims. LARGE clean-completeness run this session (12 code landings): FLEET-RED cleared (T-mac-30175 O1); 3 header/predefine slices (stdbit typed-fns, `_unlocked` fallbacks, `__FLT128_*` + C23 `_WIDTH` family + `__NO_INLINE__` + `-ffast-math`/`__FAST_MATH__`+fp-opts); `[[nodiscard]]`; asm unknown-directive; and a NEW warning family T-mac-30265 — `-Wbool-compare` + `-Wchar-subscripts` + `-Wenum-compare` all ==gcc; plus this turn driver accept-and-ignore of no-op -f hint flags (56cbf41a, security flags still warn honestly). Traps AVOIDED via verify-first: __GCC_HAVE_SYNC_COMPARE_AND_SWAP (mcc CAS is external), __GCC_ASM_FLAG_OUTPUTS__ (mcc lacks '@'), -Wconstant-conversion (=-Woverflow default-on, golden-risk), __INTx_C (stdint self-contained). DEFERRED w/ direction banked: taut-compare/logical-not/sizeof-div need a parse-time shape tracker (mcc AST is rir_try_active-gated, T-mac-30202 note); minted T-mac-30264 (float128 small-magnitude literal underflow). Backlog remaining for mac is deep (codegen/cross-target/GPU/AST-pass).


- SESSION CHECKPOINT (mac-arm64, 2026-08-20T22:39Z, /goal loop): all pushed, tree clean, no active mac claims. (1) **FLEET RED CLEARED** (was mac's 9c897e4a) — verified all 4 gates green on mac (gpu-parity-full-language, both inv-faithful; emit-map skips native) + direct full_language.c -O1 compile rc=0; removed the contract line (e651a639). Fix=5c9f73fd + rebank ceec87bf (prior session). (2) **T-mac-30230 typed-stdbit slice DONE** (eacbb92b) — 70 C23 per-type `stdc_<op>_uc/us/ui/ul/ull` fns; const-fold residual re-scoped + released OPEN (front-end feature). (3) **PRE-EXISTING RED flagged to lin:** `cli/builtin_expect_is_code_neutral` fails on the CLEAN tree (arm64 -O1 object-size BLOAT vs plain `if`, `__builtin_expect`+`_with_probability` both; RESOLVED by lin faef539d — gated cpu!=arm64; bisect DISPROVED my optimizer guess, root=arm64-LP64 branch-consumed long-cast QoI (T-mac-30131), real fold minted T-lin-10453 [needs an arm64-osx o0-rebank on mac when taken, per Q-lin-10409]); was NOT stdbit-related.

- SESSION CHECKPOINT (mac-arm64, 2026-08-20T23:05Z, /goal loop): **3 landings + 2 deep de-risks this session; all pushed, tree clean, no active mac claims.** (1) **T-mac-30175 DONE+ARCHIVED** (9c897e4a) `__builtin_constant_p` now folds foldstr-builtin args (strlen/memcmp/strcmp/strncmp over string literals) == gcc; scoped constant_p_depth keeps comma `(1,7)`→0 gcc-matching. (2) **T-mac-30072 SLICE** (a6a3365f) `__attribute__((error/warning("msg")))` + glibc `__error__`/`__warning__` → call-site error/warning == gcc, fires only on DCE-surviving calls (`!nocode_wanted`); token-keyed side-table for the message; new TOK___ERROR__/TOK___WARNING__; residual = ifunc/nonnull(already-done)/target_clones. (3) **T-mac-30168 SLICE** (7b811c70) `-Waddress-of-packed-member` default-on == gcc, `-Wno-` suppresses; reuses member_align_hint + conservative resets at `&`/indir (zero false-positive on staleness/deref); residual = (5)#pragma-GCC-distinguish (6)alloca_with_align-redeclare (7 non-issue). Each o0-NEUTRAL (o0-baseline byte-identical), anti-vacuous cli cell, gcc-16 differential; treegate 13/13, exec 374/374, diff3/mcctest/selfhost 349/349 green; only cli red is the PRE-EXISTING builtin_expect_is_code_neutral (byte-identical old/new). **DE-RISKED (banked plans, NOT single-turn — focused-session/fleet work):** T-mac-30197 label-diff const-init (o0-neutral additive; 5-part object-writer plan, MACHO_RELOC_SUBTRACTOR exists, but SValue holds only ONE sym + mac `-run` uses the internal ELF relocator ⇒ cross-format not a mac-only slice; DETAILS#t-mac-30197-...). **Verified STALE this session (do NOT re-scout):** __TIMESTAMP__ (T-mac-30153), #pragma-once symlink dedup, case-257-on-char (T-mac-30148(1)), nonnull, ALL C23 headers (stdckdint/stdbit/stdcountof/…), __builtin_extract/frob_return_addr (identity macros). **CONFIRMED-BLOCKED:** T-mac-30202 self-cmp (lin's AST-level blocker holds — do NOT retry at gen_op); T-mac-30070 huge-array (Sym.c is 32-bit int — whole-compiler data-model change). **Remaining accessible mac work is now either focused-session object-writer (T-mac-30197 de-risked), fleet-coordinated codegen needing cross-board o0-rebank (volatile-discarded-reads, __sync_synchronize dmb), or deep-architectural (Mach-O unwind T-mac-30015, Sym.c widening, AST diagnostic passes, parser error recovery T-mac-30214) — none a clean single-turn win.** [[moderncc-coordination-protocol]] SHA-drift-on-rebase gotcha banked to memory this session.
- SESSION CHECKPOINT (mac-arm64, 2026-08-20T19:40Z, /goal loop): **7 full mac tasks + 20 slices + 2 fleet-unblocks + 2 findings-banks this session** (added since 19:10: T-mac-30076 25789341 -print-multiarch prints empty+exits 0 like gcc [residual -dumpspecs]; T-mac-30207 a451c204 const/volatile qualifier DIEs + 26dbfdc5 void→DW_TAG_unspecified_type — 5 DWARF sub-items done this run (prototyped/const-volatile/void/_Atomic/_Complex; the _Atomic one also fixed an INVALID DWARF 0-type-ref [DETAILS#t-mac-30207-dwarf-atomic], and T-mac-30106(4) _Complex now emits DW_ATE_complex_float base types not anon structs [a150d714, DETAILS#t-mac-30106-complex-dwarf-basetype]), dwarfdump-verify clean, o0-neutral since o0-baseline is -g-free; residual now only column-info + typedef-attribution [need a data-model field], dup-file_names intentional), each o0-neutral (o0-baseline byte-identical), treegate green, with a regression cell + clang/gcc-16 differential; all DONE tasks in ARCHIVED.md, depth per-anchor in DETAILS.md. The only cli red throughout is the PRE-EXISTING `builtin_expect_is_code_neutral` (proven still red on pristine HEAD). All pushed, tree clean, no active mac claims. Next mac ID 30261 (no new IDs minted). **Full tasks DONE+ARCHIVED:** T-mac-30204 (07f683fa arm64 exclusive/acq-rel atomic encoders), T-mac-30203 (51fff760 inline-asm o/p/X constraints), T-mac-30128 (d206abd8 #pragma pack + aligned = min(pack,max(natural,K))), T-mac-30162 (cdeb73d4 #pragma weak name=target alias), T-mac-30054 (d5d64abe stmt-expr void-unless-last-is-expr), T-mac-30107 (757d84b1 custom `__attribute__((section("__DATA,x")))` writable data no longer SIGBUSes at AOT+`-run` — SHF_WRITE on a non-const custom section at the data-emit site; the 2026-08-18 spike's "AOT fix breaks -run" fear did not recur; o0-neutral since no exec-corpus file uses section attrs; DETAILS#t-mac-30107-custom-writable-section), T-mac-30127 (f395b72e `__attribute__((naked))` implemented on arm64+x86_64 — SymAttr.naked bit into spare bits, func_naked-gated prolog/epilog/return-check skip both backends, block_nested rejects non-asm naked bodies like clang; arm64 runs ==clang was SIGBUS, x86_64 -S frame-free; o0-neutral; DETAILS#t-mac-30127-naked). **Slices:** T-mac-30076 (d6d7a53e -Wa, passthrough), T-mac-30229 (1624c777 -bundle/MH_BUNDLE; ae37f0c0 export-trie drops 4 internal linker syms), T-mac-30232 (7d0d53cd -Wl,-current_version/-compatibility_version routing), T-mac-30068 (f3749d0d+e11ce5af -run atexit-before-destructor order + re-entrant atexit registration; residual = 32-slot cap), T-mac-30075 (08734371 macOS framework header search now works for -c/-E/-S, not just linking; residual = -F ordering/-I-/dedup), T-mac-30207 (13791c46 DWARF DW_AT_prototyped on function DIEs), T-mac-30061 (46b2df23 no spurious .debug_line stale-line rows on a big line jump) — both DWARF, o0-neutral since o0-baseline is -g-free (remaining DWARF fidelity items — const/volatile qualifier DIEs, void modeling, dup file_names — are the same clean class). **This run also:** T-mac-30168(2)(3) (09a04a76 dllexport/dllimport + `visibility("protected")` now warn-when-ignored on Mach-O, gcc-verbatim under -Wattributes, ELF STV_PROTECTED unchanged; DETAILS#t-mac-30168-macho-attr-warnings), and T-mac-30189(4) (2b7166c1 warn_unused_result on a non-function now warns "only applies to function types", gcc-verbatim, fn-pointers exempt; DETAILS#t-mac-30189-wur-nonfunction) — both warning-only ⇒ o0-neutral by construction. Plus T-mac-30194(2) (91f5240a sizeof/_Alignof-on-incomplete-type error now NAMES the type — `'…' applied to an incomplete type 'struct S'` via type_to_str on a VT_STORAGE-stripped copy; DETAILS#t-mac-30194-sizeof-incomplete-typename; error-path text only ⇒ o0-neutral). Plus T-mac-30189(6) (1888e1a9 tentative-array "assumed one element" -Wall warning now at the decl line not EOF — decl line stashed in sym->vla_inner_id, emitted via the `%i:` explicit-line form; DETAILS#t-mac-30189-tentative-array-line; warning-only ⇒ o0-neutral). Plus **T-mac-30261 DONE+ARCHIVED** (d2219075 new -Wmain: warn "return type of 'main' is not 'int'" on void/char/double main under -Wall, gcc/clang-verbatim, -ffreestanding-suppressed; DETAILS#t-mac-30261-wmain; warning-only ⇒ o0-neutral). Plus **T-mac-30262 DONE+ARCHIVED** (635bfc3b duplicate const/volatile now warns under -Wall in C99+ — extended mcc's existing pre-C99-pedantic detection to warn_all in C99+, matching gcc/clang -Wduplicate-decl-specifier; no false positive on `const int *const`; DETAILS#t-mac-30262-duplicate-qualifier; warning-only ⇒ o0-neutral). **NOTE re T-mac-30107 (my 757d84b1):** its SHF_WRITE line flipped the shared ELF RELRO rodata_section (.data.ro) A→WA fleet-wide (internal aggregates route through ad.section=rodata_section) → x86_64 o0 red; lin narrowed it (`sec != rodata_section`, 97fc1d0a/T-lin-10449) — re-verified on arm64-osx: my Mach-O custom-writable-section fix is byte-for-byte preserved (gv=99 rc99 AOT+run, const rc7, rw rc54), o0 green. **Verified stale/already-done this run (don't re-scout):** T-mac-30148(2) K&R old-style-def (mcc warns by default, gcc-verbatim, -W[no-]old-style-definition accepted); T-mac-30194(2) sizeof-incomplete (already gives a specific msg, not "unknown type size"; only gcc's type-naming differs, mcc-style). **Fleet-unblock:** docref-lint (00fc96cc) ANCHOR_REF regex broadened to `[A-Za-z0-9_-]` — win's uppercase-containing anchor REF had turned the fleet `docs/refs` gate red. **Fleet-unblock 2:** T-lin-10451's forward-ref to a to-be-written `tests/ast/cprop-typecov.sh` (its DoD) had turned `docs/refs` red on pull; added a shape-(a) `tools/docref-allow.txt` entry (auto-fails once lin lands the file) + pinged lin. **Investigated/routed:** **T-mac-30225 (HIGH dylib flat→two-level namespace) — DE-RISKED RECIPE BANKED** (DETAILS#t-mac-30225-two-level-namespace-investigation): confirmed current (mcc dylib is DYLDLINK-only + flat-namespace binds), scoped mac-contained + o0-neutral (Mach-O linker output only) but HIGH blast-radius; root blocker = no per-import→source-dylib provenance (flat dynsymtab merge); full 5-part fix + interposition acceptance test mapped for a focused session. Also stale-verified this run (mcc already matches oracles, don't re-scout): T-mac-30148(3) _Noreturn-on-fn-pointer (mcc rejects like clang), T-mac-30102(2) noreturn-fall-off-end (mcc warns), distinct-pointer-comparison (mcc warns "pointer type mismatch"), -Wunused-value/-Wparentheses (mcc warns), and the -dump*/-print* driver family (mcc handles dumpmachine/dumpversion/print-search-dirs/etc). T-mac-30135 (undefined-weakref-resolves-to-0 is ELF-only, not mac-verifiable → lin); T-mac-30075 [MED] -F/-I ordering (oracle-confirmed both directions; fix = unified ordered include/framework path list, a data-model change) + T-mac-30179(1) __sync_synchronize inline dmb (feasible but o0-rebank-entangled on the corpus) — both banked at DETAILS#t-mac-30075-F-ordering-and-sync-synchronize-findings for focused/fleet-coordinated sessions. Also T-mac-30076 (25789341 -print-multiarch prints empty+exits 0 like gcc; residual = -dumpspecs). **Open residuals on touched tasks:** T-mac-30229 visibility-not-honored (front-end st_other flow, cross-fleet o0 risk) + -exported_symbols_list; T-mac-30232 Mach-O -ar / -### / -x c-header; T-mac-30076 -dumpspecs; T-mac-30207/30061 remaining DWARF fidelity (const/volatile qualifier + void DIEs need DIE-chain-emitter restructuring; dup file_names intentional; column needs source-col tracking; -E whitespace clang-specific cosmetic).
- HANDOFF-BOX-FACTS (mac-arm64, kept for successors): (1) native oracle IS on this box — `/opt/homebrew/bin/gcc-16` + clang both present; diff mcc against them directly. (2) this mac builds AND RUNS x86_64-win32 PE under wine: `x86_64-w64-mingw32-gcc` (brew mingw-w64) + `/opt/homebrew/bin/wine`+Rosetta — build a `-DMCC_CROSS_TARGETS=x86_64-win32` tree, compile `-B<crossdir> -Iruntime/win32/include -Lruntime/win32/lib`, then `WINEDEBUG=-all wine prog.exe`; so x86_64-win32 PE *execution* (non-GPU) is mac-verifiable, but mac still CANNOT run x86_64-Linux ELF (no qemu-user), arm64-Windows/WoA PE, or GPU. (3) commit every docs/TODO.md edit BEFORE `git pull --rebase` (else the autostash pop conflicts on the Sessions rows and a blind `git add` smuggles conflict markers), and `grep -nE '^(<<<<<<<|=======|>>>>>>>)' docs/*.md` before every push.
