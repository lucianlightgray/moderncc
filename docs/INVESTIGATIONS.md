# INVESTIGATIONS

Findings from codebase gap/conflict audits (parallel subagent sweeps). Each entry: a discovered gap, contradiction, or conflicting implementation, with file:line, a `[NEW]`/`[KNOWN]` label (KNOWN = already tracked under a `T-id`), and a `TASK:` pointer when a `TODO.md` exploration task has been minted. This file is the durable evidence log; `TODO.md` carries the one-line actionable state. Append-only in spirit; deepen entries as investigation continues.

Method: `docs/GOAL.md`-style parallel audits — one subagent per subsystem, each grepping for stub/bailout/asymmetry markers, reading to confirm, and cross-referencing `TODO.md`/`DETAILS.md` to separate NEW from KNOWN. Findings here are subagent-reported with file:line; only those marked **VERIFIED** were independently reproduced.

---

## Round 1 (2026-08-17) — core subsystems

### <a id="unsigned-const-fold-sdiv"></a>Unsigned `/`,`%` const-fold uses signed `gen_opic_sdiv` — **VERIFIED, FILED**
`ast_fold_eval` (`src/mccast.c:6988-6999`) folds unsigned division/modulo with signed magnitude division, disagreeing with the authoritative `gen_opic` (`src/mccgen.c:3740`). Deterministic wrong-code reproduced under `-fno-replay-fallback` at `-O1+` (masked by replay-fallback in default builds). Trigger: children-first fold of a unary-minus operand. `[NEW]` → already migrated to a full task. **TASK: T-mac-30014** (filed 2026-08-17, see `DETAILS.md#t-mac-30014-unsigned-const-fold-uses-signed-sdiv`).

### <a id="gpu-multidevice-teardown"></a>GPU multi-device teardown / lifetime cluster
The T-win-50022 multi-device "create-all" loop landed without its per-device teardown half (`DETAILS.md:47175` records it as held). `mcc_gpu_quiesce` (`src/mccgpu.c:2941`) is single-slot yet destroys the process-wide `VkInstance` (`:2961`) under still-live logical devices — the T-lin-10033 lifetime-bug class in multi-device form. `stranded` is a global counter (`:50-56`) with slot-local teardown (`:3077`) → one slot's fence timeout leaks all devices; `mcc_gpu_reopen` (`:2978`) is also slot-local. Fires only when `ndev>1`. `[NEW]`, root cause shared with `[KNOWN]` T-lin-10033/T-win-50022. **TASK: T-mac-30021.**

### <a id="spirv-f64-execmode"></a>SPIR-V f64 never pins SignedZero/Inf/NaN-preserve
Only `LocalSize` execution mode is emitted (`src/mccgpu.h:2496`); add/sub/mul run at hardware default though the RTX device advertises `shaderSignedZeroInfNanPreserveFloat64` (`DETAILS.md:31172`). NaN payload/sign and signed-zero results diverge from the CPU reference. Distinct from `[KNOWN]` unpinnable-denormal (T-lin-10061); this is pinnable-but-unpinned. `[NEW]`. Grouped under GPU-numeric investigation (**TASK: T-mac-30021**, numeric sub-item).

### <a id="gpu-metal-vs-vulkan-f64"></a>Metal (software f64) vs Vulkan (native f64) divergence
Metal uses full software IEEE-754 emulation (`src/mccgpu.h:1656`) and sets `mcc_gpu.f64=1` unconditionally (`src/mccgpu.c:523`) with no capability behind it; Vulkan gates on `shaderFloat64`. Same source → backend-specific f64 (esp. denormals). `[NEW]`.

### <a id="jit-lazy-vs-sync-kgc"></a>Lazy JIT counts "KGC-refused / kept-AOT" as "build failed" — root cause of T-lin-10029
`mccjit_lazy_build_masked` (`src/mccjit_embed.c:1119-1167`): when KGC verification refuses, `entry` stays NULL and the only trampoline fallback (`:1164`) fires only when KGC is *disabled*; lazy callers (`:1615`,`:1693`) then count `failed++` and "give up". The sync path (`:1035`) treats the identical KGC-refusal as normal kept-AOT. Same condition, opposite verdicts. `[NEW root-cause]` for `[KNOWN]` **T-lin-10029** — should be attached there, not a new task. Also: a usable variant is silently discarded when the KGC stub/baseline alloc fails (`:1149`,`:1156`).

### <a id="jit-rwx-mapjit"></a>x86_64 JIT pages RWX-forever; no `MAP_JIT`
x86_64 JIT primitives `mmap(...PROT_EXEC...)` and never re-protect (`src/mccjit_embed.c:937,1711,4212,6041`); arm64 uses W^X via `host_runmem_protect` but neither uses `MAP_JIT`/`pthread_jit_write_protect_np` (`src/mcchost.c:1624`). W^X hole on x86_64; x86_64-macOS embed-JIT under hardened runtime would fail (and is unmeasured — T-lin-10030 mac lane is arm64-only). `[NEW]`. Plus `mccjit_dispatch_entry` has no arm64 arm (`:6039`, returns fallback) and `mccjit_set_always_gpu` is one-way with no GPU-exists check (`:2108`, the T-lin-10082 seam).

### <a id="pp-float16-in-if"></a>Preprocessor: `_Float16` constants not rejected in `#if`
The pp-expr float guard `tok>=TOK_STR && tok<=TOK_CLDOUBLE` (`src/mccpp.c:2204`) misses `TOK_CFLOAT16=0xd5` (`src/mcc.h:1326`), so `#if 1.0f16` flows an illegal float operand into the integer-constant-expression evaluator. `[NEW]`. **TASK: T-mac-30022** (preprocessor `#if`/builtin semantics).

### <a id="pp-target-builtins-zero"></a>Preprocessor: 8 `__has_*`/`__is_target_*` builtins silently evaluate to 0
`pp_builtin_func` recognizes `__has_cpp_attribute`, `__has_warning`, `__is_target_arch/os/vendor/environment`, etc. (`src/mccpp.c:1993`), but only 5 are registered as real macros (`:5765`); the other 8 hit the fallback (`:2249`) returning 0 with no diagnostic. `#if __is_target_arch(x86_64)` is always false. `[NEW]`. **TASK: T-mac-30022.** Also: `#embed limit()/offset()` take only a single integer token, not a constant-expression (`:1708`).

### <a id="rir-vacuous-floors"></a>RIR: vacuous floors / ungated validity signals
`tools/rir-coverage.py`: banked lowerable sub-metric floors default to `0.0` so `measured+tol>=0.0` cannot fail (`:1950,1967`) — inconsistent with the arena floors' `is None`→skip. `control_neutral` is computed/printed but never gated (`:1221,1567`), so a non-neutral reference makes every "benign" verdict meaningless yet passing. Divergent bodies whose baseline `-run` exits nonzero are dropped as "unrunnable" and never miscompile-checked (`:1320`) — but many `tests/exec` use nonzero exit as their pass signal. `[NEW]`, same class as `[KNOWN]` T-lin-10043/10054. Folded into gate-vacuity **TASK: T-mac-30020.**

### <a id="codegen-arm64-vaarg-assert"></a>Codegen: live `assert(0)` in arm64 ELF `va_arg` for 16-byte/16-align non-HFA aggregate
`src/arch/arm64/arm64-gen.c:1721` — a conforming over-aligned small struct through `...` aborts the compiler (debug) or runs two untested trailing instructions (NDEBUG); the sibling stack-cursor block at `:1739` treats the same case as normal. `[NEW]`. (Minor; captured, not yet taskified.)

---

## Round 2 (2026-08-17) — object formats, debug, optimizer, runtime, types, gates

### <a id="macho-no-unwind"></a>Mach-O emits NO unwind/CFI information at all — CRITICAL
`src/objfmt/mccelf.c:90-92` forces `unwind_tables=0` for every non-ELF format; `mcc_eh_frame_start` (`src/mccdbg.c:1402`) then early-returns. PE substitutes `.pdata`/`.xdata`, but `mccmacho.c` has zero `__eh_frame`/`__compact_unwind` path (the `sk_uw_info` slot at `:1420` is `{0}`, never populated). macOS table-based unwinding (C++ EH, `_Unwind_*`, `cleanup`, crash reporters) has no data. Undocumented — the DETAILS parity matrix is ELF-vs-PE only. `[NEW]`. **TASK: T-mac-30015.**

### <a id="atomic-32bit-align"></a>`_Atomic` scalars under-aligned on i386/arm32 — 32-bit ABI break
`_Atomic` applied as a pure qualifier (`src/mccgen.c:9103`) with no alignment bump; `type_size` gives `long long`/`double` 4-byte align on i386 (`!MCC_TARGET_PE`) and arm (`!MCC_ARM_EABI`) (`:5711`). `_Alignof(_Atomic long long)`==4 vs gcc's 8; `struct{char c; _Atomic long long x;}` places `x` at offset 4 not 8 — silent interop miscompile vs any 32-bit system header, plus a tearing hazard. x86_64 unaffected. `[NEW]`, trivially confirmable via `_Alignof`. **TASK: T-mac-30016.**

### <a id="riscv64-backend-gaps"></a>riscv64 backend is second-class — inline-asm + reloc gaps
Inline-asm memory operands emit no base-register addressing (`src/arch/riscv64/riscv64-asm.c:2111`): `asm(::"m"(local))` expands to `lw rd, a0` instead of `lw rd, 0(a0)` — malformed. `constraint_priority` advertises `A`/`S` it can't satisfy (`:2255`, hard-fail mid-alloc). `code_reloc` returns -1 for compressed relocs its own `relocate()` handles (`riscv64-link.c:5`). `[NEW]` cluster. **TASK: T-mac-30017.** (Related: arm `K`/`L` dead in `arm-asm.c:2693`; arm32 no VFP constraint class.)

### <a id="optsearch-determinism"></a>Optimizer search: nondeterminism cluster
pthreads search pool races non-TLS emit globals (`ind/loc/rsym` `src/mccgen.c:245`; counters `mccast.c:18470`) — dev-gated off (`OPT_SEARCH_PTHREADS`). Memo/disk key (`ast_search_key_salt:17978`) omits result-changing flags (emit-size vs cost, walk order, predict) → a cache entry from one config served under another emits different objects. Disk-eviction comparator (`:18170`) returns 0 on ties → non-stable `qsort` → libc-dependent survivors. `[NEW]` mechanisms inside `[KNOWN]` T-lin-10045 (disk-cache determinism). Plus a degenerate slice "search" that can only pick greedy-largest (`:19083`). **TASK: T-mac-30018.**

### <a id="coff-reloc-emit"></a>COFF/reloc emit format-disagreement bugs
COFF object emit collapses `STB_WEAK`→strong `EXTERNAL` (`src/objfmt/mccpe.c:2259`; `WEAK_EXTERNAL` honored on read, never emitted, no COMDAT) → multiple-definition errors, lost overrides. i386 `DIR32NB` (RVA) decoded as absolute `R_386_32` (`:2020`). arm64 COFF `BRANCH26` CALL/JUMP gated on **host** not target (`:2035`) → a `B` cross-read on a non-Win host rewritten as `BL`, clobbering X30. `[NEW]`, adjacent to `[KNOWN]` T-win-50005/50006. **TASK: T-mac-30019.**

### <a id="debug-info-asymmetry"></a>Debug-info asymmetries (DWARF version, CodeView)
DWARF default version differs by platform: Mach-O=2, ELF/PE=5 (`src/mcc.h:2206`) → weaker `-g` on macOS. CodeView describes locals only FP-relative (`src/mccdbg.c:534`), no register defrange — optimized locals get no location. `-gcodeview` emits both CodeView AND DWARF into one object (`src/libmcc.c:2916`). `[NEW]`. (Grouped with Mach-O unwind investigation, **TASK: T-mac-30015** debug-info sub-items.)

### <a id="gate-vacuity"></a>Gate infrastructure vacuity beyond RIR
`tools/bitint-diff.py` — the per-target `_BitInt` ABI differential gate — has no floor on lines-compared (`:105`) AND is wired into nothing (no CMakeLists/ctest/must-run); it would catch the `_Atomic`/`_BitInt` ABI divergences but never runs. `defcheck` `def-verify` passes on an empty `.def` glob (`tools/defcheck.c:90`). `asm_reloc_suffix.cmake:81` jmp-equivalence tooth has no non-empty floor. `opt_determinism_mutate.cmake:21` swallows a `77` skip as a detection. `[NEW]`, same class as `[KNOWN]` T-lin-10043/10054/10048. **TASK: T-mac-30020.**

### <a id="type-c23-gaps"></a>Type-system / C23 gaps
`_BitInt(N>64)` and `__int128` bit-fields rejected (`src/mccgen.c:7521`) — C23 mandates the former, gcc/clang accept the latter. x86_64 SysV forces MEMORY class for any struct with a packed/unaligned field (`x86_64-gen.c:1298`) — may disagree with gcc's eightbyte classification at a boundary. `[NEW]`. (Captured; taskify next iteration or fold into ABI-conformance work.)

### <a id="runtime-intrinsics"></a>Runtime / intrinsics gaps
No `arm_neon.h`/`arm_acle.h` (full x86 SIMD suite present) — portable NEON code fails on arm64 (`runtime/include/`). half↔double conversion always double-rounds through float (`src/mccgen.c:5130`; no `__extendhfdf2`/`__truncdfhf2`). `stdckdint.h` `__int128` accumulator overflows for 128-bit operand types (`:51`). `int512.c` (24 helpers) orphaned on main; dead `__mcc_i256_neg/not/nonzero`. `[NEW]`. (Captured; taskify next iteration.)

---

## Round 3 (2026-08-17) — concurrency, memory/lifetime

### <a id="objreader-bounds"></a>Object-file readers: unchecked bounds on untrusted input — SECURITY
The linker's object/archive/dylib readers trust attacker-controllable counts/offsets:
- COFF reloc offset unchecked → **OOB heap write**: `roff = rl->VirtualAddress + smap[i+1].offset` flows into `coff_map_reloc`'s `memcpy(fld,&v,4)` with no bound vs section size (`src/objfmt/mccpe.c:2616-2621`); the Mach-O reader guards the identical op (`mccmacho.c:2781`), COFF does not.
- Archive symbol count `nsyms` from the `/`/`SYM64/` member walked with no bound vs member size → **OOB read** (`src/objfmt/mccelf.c:3815-3833`); BSD variant validates, GNU/PE does not.
- Mach-O dylib exports: `iextdef`/`nextdef` index `symtab[]`/`strtab` unbounded; `nsyms==0` derefs NULL (`mccmacho.c:3296-3308`).
- DLL export name count DWORD→int truncation under-allocates, then reads `namep[i]` (`mccpe.c:1717-1732`).
- Unbounded Mach-O load-command walk in the dll path (`cmdsize==0`→infinite loop, `mccmacho.c:3290`); silent short-read leaves uninitialized parse buffers (`mccelf.c:3370`).
`[NEW]` cluster; distinct from the reloc-*type* task T-mac-30019. Reachable by feeding a crafted `.o`/`.a`/dylib to the linker. **TASK: T-mac-30023.**

### <a id="tls-runslab-bounds"></a>TLS run-slab bounds guard vs. copy mismatch (`-run`)
`src/mccrun.c:471` guards `total` (Σ section `data_offset`) against `mcc_jit_tls_slab[MCC_JIT_TLS_MAX]`, but the seed copy uses `seed_len = max(sh_addr-base + data_offset)` (`:540-543,613-616`); inter-section alignment padding makes `seed_len > total`, so the guard passes while `memcpy(slab, seed, seed_len)` (`:499,643`, per worker-thread reseed) writes past the slab. Trigger: `-run` on Linux a program with ≥2 non-NOBITS TLS sections + alignment gaps. Latent (usual case is single `.tdata`). `[NEW]`, distinct from KNOWN tls_threads tpoff red. **TASK: T-mac-30024.**

### <a id="libmcc-reentrancy"></a>Multithreaded libmcc / JIT reentrancy
Default opt-search is fork-isolated (safe); the racy opt-search-pthreads path is dev-gated off (`[KNOWN]` DETAILS:19797, "60/60 SIGSEGV"). Under-documented gap: the per-`MccjitCounterState` lock (`src/mccjit_embed.c:1191`) does NOT cover the process-global JIT scratch it relies on (`mccjit_last_*`, `:361-367`) — two user threads promoting different slots via the sync fallback (`:1665-1670`) race them. `MCC_GPU_LOCK` compiles to `((void)0)` on Windows (`src/mccgpu.c:35`, `[KNOWN]` L7/DETAILS:35049) — GPU state unsynchronized there. Diag ring buffer publishes index before filling slot (`:266-268`, dev-gated `MCC_JIT_CRASH_DIAG`, cosmetic). Reentrancy hazard for a multithreaded libmcc consumer. `[NEW]`/`[KNOWN]`-mix. **TASK: T-mac-30025.**

### <a id="foldeval-diag-divergence"></a>`ast_fold_eval` diverges from the gen evaluator on diagnostics too — same root as T-mac-30014
The AST template folder silently folds cases the authoritative gen path diagnoses:
- **Signed integer overflow** in constant `+`/`-`/`*`: `ast_fold_eval` computes raw `l1+l2` etc. (`src/mccast.c:6976-6987`) and rewrites to a `Literal`; gen emits `mcc_pedantic("integer overflow in constant expression")` (`src/mccgen.c:3701`), fatal under `-pedantic-errors` (`:553`). Same source: rejected on gen path, silently wrapped on fold path.
- **Shift count negative or ≥ width**: `ast_fold_eval` just masks `l2 & 31/63` (`mccast.c:7000-7005`); gen warns (`mccgen.c:3669-3671`). Diagnostic lost when the fold fires first; value also diverges under the T-mac-30014 gating (`-fno-replay-fallback`/-O1+).
`[NEW]`, same root function/masking as **T-mac-30014** — the unsigned-div value bug is one instance of a general "ast_fold_eval must match gen on value AND diagnostic" problem; fix together. **TASK: T-mac-30026.**

### <a id="pp-directive-guards"></a>Preprocessor directive operand-guard holes
- `#undef` accepts a non-identifier or empty name with no diagnostic (`src/mccpp.c:2765` guards only `defined`/`__VA_ARGS__`); `#define` (`:2320`) rejects both `< TOK_IDENT` and those names.
- `#ifdef`/`#ifndef`/`#elifdef`/`#elifndef` accept `defined` and `__VA_ARGS__` as operand (`:2826,:2882` guard only `< TOK_IDENT`) — silently evaluated as "not defined".
- `__VA_OPT__` in an object-like/non-variadic macro is only a warning, and that path then skips the structural `(`/`##`/unterminated checks (`:2408-2416`) yet still appends the token → bogus definition; every other `__VA_OPT__` misuse is fatal.
- Token-paste producing >1 pp-token uses `mcc_error_noabort` then proceeds to emit each wrong sub-token (`:4946`), vs fatal for the comment-start case (`:4933`).
`[NEW]` cluster of complementary guard holes / severity inconsistencies. **TASK: T-mac-30026** (diagnostic-consistency; adjacent to T-mac-30022).

### <a id="embed-jit-nobake-notice"></a>`--embed-jit` no-bake notice bypasses the warning machinery — [KNOWN T-lin-10028]
`src/mccjit_embed.c:2189` hand-prints via raw `fprintf`, so it is immune to `-w` AND cannot be promoted by `-Werror`; the engine-less binary still exits 0. `[KNOWN]`.

### Reentry-state — verified ROBUST (negative result)
The error-handling audit confirmed `stk_data_floor`/`error_jmp_buf` are saved/restored around every temporary raise, `error1` unwinds to the floor, and `preprocess_start` re-initializes include/ifdef/pack stacks at the next TU — so a top-level error longjmp does not corrupt the next compilation. The multithreaded JIT-scratch race (`#libmcc-reentrancy`) remains valid; single-threaded reentry is clean.

---

## Round 4 (2026-08-17) — driver/CLI, linker resolution, cross-target/ABI

### <a id="driver-cli-semantics"></a>Driver / CLI option semantics
- `-imacros <file>` is a byte-for-byte alias of `-include` (`src/libmcc.c:3070`) — leaks the file's declarations/code into every TU instead of absorbing only its macros; wrong, silent, undocumented.
- `-fstack-protector` help (`src/mcc.c:150`) understates targets — code enables canaries on arm64-ELF/i386/arm/riscv64 too (`libmcc.c:3143`); also omits `-fstack-protector-strong`.
- Plain `char` is unsigned on ALL targets incl. x86_64/i386 (`libmcc.c:1226`, defines `__CHAR_UNSIGNED__`) — deviates from the SysV/GCC signed-char norm; undocumented ABI/behavior surprise.
- Mach-O linker options `-flat_namespace`/`-two_levelnamespace`/`-undefined <treatment>` accepted and silently ignored (`libmcc.c:3430-3435`) — `-undefined dynamic_lookup` is a no-op.
- Parsed-but-undocumented: `--jit-threads`, `-print-isa`, `-iquote`, `-idirafter`, `-imacros`.
`[NEW]`. **TASK: T-mac-30027.** (KNOWN: `--jit-threads` override + `--jit-conservative` help = T-lin-10393.)

### <a id="linker-resolution"></a>Linker resolution / archive semantics divergences
`mccelf.c` is the shared resolution core; divergences are in the format wrappers:
- Mach-O silently ignores `-e`/`--entry` — hardcodes `main` for LC_MAIN (`mccmacho.c:2551`); ELF/PE honor `elf_entryname`. Custom entry dropped without warning.
- Mach-O undefined-symbol diagnosis stricter than ELF/PE: errors any non-weak undef not in dynsymtab regardless of reloc reference (`mccmacho.c:981`), vs the shared `relocate_syms` erroring only reloc-referenced undefs.
- Library search order deviates from GNU ld: pattern-outer/path-inner (`libmcc.c:1854`) → `libfoo.so` searched in all `-L` dirs before `libfoo.a` in any; a `.a` in an earlier dir loses to a `.so` in a later dir.
- No `SHF_MERGE`/`SHF_STRINGS` dedup in any format (size only).
- `.bss`==`SHN_COMMON` conflation in `set_elf_sym` (`mccelf.c:647`) — a genuine `.bss` global treated as tentative, silently overridable by a later strong def.
`[NEW]`. **TASK: T-mac-30028.** (KNOWN: PE-only alacarte precedence = T-win-50021; COFF weak-external aux/COMDAT selection = DETAILS:7683.)

### <a id="longdouble-cross-target"></a>arm64/PE `long double` ABI + host-vs-target + cross-target predefines
- **arm64 `long double` is 16 bytes on every OS** (`src/arch/arm64/arm64-gen.h:27`, no macho/PE guard) — wrong for Apple & Windows arm64 where it is 8 (`==double`). `type_size`/`__SIZEOF_LONG_DOUBLE__` report 16; ABI-mismatched vs system libc (`%Lf`, `strtold`, struct layout). Self-consistent within mcc code (why the mac-arm64 suite stays green). `[NEW, HIGH]`. Same family as `[KNOWN]` x86_64-PE `long double`=16 (T-lin-10394/T-win-50003).
- Host-vs-target: `ast_sv_hi()` (`mccast.c:2735`) extracts a *target* `VT_LDOUBLE` constant's hi word via *host* `sizeof(long double)`/`LDBL_MANT_DIG` → returns 0 on an 8-byte-ld host targeting 80-bit. Long-double constant repack (`mccgen.c:16833-16863`) has an unhandled host×target `#else` that silently emits an uninitialized constant.
- `__SIZEOF_WINT_T__`==4 (`mccpp.c:5612`) contradicts `__WINT_TYPE__`==`unsigned short` (2) on PE.
- Frozen/missing arch feature predefines: arm hardcodes `__ARM_ARCH_4__`, arm64 emits no `__ARM_*`, i386 gets no SSE macros (`mccpp.c:5530-5563`) → feature-test `#if` silently takes the wrong path.
- Non-Darwin→macho cross build omits macOS SDK search paths (`libmcc.c:1327`), silent.
`[NEW]`. **TASK: T-mac-30029.**

---

## Round 5 (2026-08-17) — self-host determinism, standard headers, numeric lexing

### <a id="longdouble-selfhost-determinism"></a>Long-double self-host determinism hole
mcc const-folds `long double` using HOST long-double arithmetic (`gen_opif`, `src/mccgen.c:4019`), and the self-host determinism design explicitly exempts long double from bit-exactness (DETAILS:34715 row E3: "long double escapes … claim it doesn't affect selfhost-fixpoint is probably false"). Compounding: mcc's OWN preprocessor `parse_number` (`src/mccpp.c:3379,3507`) runs every float literal in any source — incl. mcc's ~100K lines — through 80-bit long-double scaling constants (tightest self-reference loop). `LDOUBLE_WORDS` (`src/mcc.h:230`) derives from host `sizeof(long double)`, so on Apple-arm64 (where T-mac-30029 mis-sizes ld to 16) a self-built mcc drifts payload width stage-0→stage-1. No gate sees a STABLE divergence: `selfhost-fixpoint.py` compares stage2==stage3 (same compiler); `selfhost-output-parity.py` runs only `-O2`/same-host; `selftest.c`/`combo_selftest.c` exercise only integer/pointer/codec code (vacuous w.r.t. float). `[NEW]` self-host framing of `[KNOWN]` T-mac-30029/T-lin-10394. **TASK: T-mac-30030.**

### <a id="header-conformance"></a>Bundled standard-header conformance
`runtime/include` headers hardcode values contradicting the compiler's per-target predefines:
- `stdint.h:100` `WCHAR_MIN`/`WCHAR_MAX` hardcoded signed-32 (`INT32_MIN/MAX`) — wrong on ARM/aarch64 Linux (`wchar_t` unsigned, `__WCHAR_MAX__ 0xffffffffU`) and Windows (16-bit). Fix like GCC: `#define WCHAR_MAX __WCHAR_MAX__`.
- `mccdefs.h:236` `__WCHAR_MAX__`/`MIN__` signed-32 on Windows though Windows `wchar_t` is `unsigned short`; the `#if __linux__ && (arm||aarch64)` guard drops Windows into the wrong else-branch.
- `stddef.h:34` `unreachable()` defined as `((void)0)` instead of `__builtin_unreachable()` (the builtin exists, `mccgen.c:13288`) — silently defeats the C23 contract.
- `limits.h`/`stdint.h` missing ALL C23 `*_WIDTH` macros + `BOOL_MAX` + `__STDC_VERSION_*_H__`; `uchar.h` missing `char8_t`/`mbrtoc8`/`c8rtomb` (though `__CHAR8_TYPE__` is predefined).
- `stdint.h:106` `WINT_MAX` 32-bit vs `__WINT_TYPE__ unsigned short` on PE (KNOWN-adjacent T-mac-30029); `intmax_t` typedef `long long` vs `__INTMAX_TYPE__ long` on LP64; `float.h:28` `FLT_EVAL_METHOD` hardcoded 2 for i386 vs `__FLT_EVAL_METHOD__ 0` under SSE2; `threads.h:10` unconditionally includes `<pthread.h>` (fails on PE); `stdatomic.h:197` fences/flags declared `extern` not mapped to builtins (link-fail risk).
`[NEW]`. **TASK: T-mac-30031.**

### <a id="numeric-literal-lexing"></a>Numeric / float literal lexing
- **`wb`/`uwb` `_BitInt` literal truncated to 128 bits** (`src/mccpp.c:3727-3729`): stores only `tokc.q.lo/hi`, zeroes `q.w2/w3`, never writes `q.w4-w7` — but `__BITINT_MAXWIDTH__`=512 (`:5619`), cap allows N≤512 (`:3722`), `_BitInt(256)` is tested. Any `_BitInt` literal >2^128 gets a silently truncated value; codegen reads stale `q.w4-w7` from the reused global `tokc` for widths >256. The sibling `i256` path (`:3765`) stores all four words. `[NEW, HIGH]` — timely after the wideint-unify merge.
- **Triple width drift**: predefine 512, `wb` accumulator `w[8]`=256-bit (`:3684`), store=128 → a 257–512-bit literal overflows the accumulator and is rejected with a misleading "exceeds 512-bit maximum" (`:3722`); representable 512-bit literals can't be formed. `[NEW]`.
- `__bf16` has no literal suffix and no constant path (`mccgen.c:5337` errors "not a load-time constant") despite `VT_BF16`/`bf16_round` existing — asymmetric with `_Float16`. `[NEW]`.
- Decimal literal in [2^63,2^64) made `unsigned long long` + spurious overflow warning (`:3780`), against C rank rules; value >ULLONG wraps mod 2^64 with only a warning (`:3618`). `[NEW, LOW]`.
- long-double/`_Float16` literals stored/rounded via host `long double`/`strtold` (`:3529,3587`) — `[KNOWN]` T-mac-30029 / self-host family (`#longdouble-selfhost-determinism`).
`[NEW]`. **TASK: T-mac-30032.** (Verified correct: C23 digit separators, `0b`/`0o`, leading-zero octal incl. rejecting `089`, suffix sniffing, hex/oct/bin type assignment.)

---

## Round 6 (2026-08-17) — assembler/inline-asm, disassembler round-trip, debug-info, type/ABI, CST/AST/RIR replay, diagnostics

Six parallel subsystem audits (mac-arm64). Every finding cites `file:line`; all deduped against rounds 1-5 + `DETAILS.md`/`TODO.md`/`ARCHIVED.md`. Tasks minted: **T-mac-30033..30038**.

### <a id="r6-inline-asm-regnum"></a>Assembler / inline-asm
- **[HIGH] arm64 inline-asm register numbering collides with codegen numbering → miscompile.** The asm constraint layer uses PHYSICAL numbering (x0..x30 = 0..30, SP = 31; `src/arch/arm64/arm64-asm.c:29-60,231-232,2191-2196`) but `asm_gen_code` calls `load(op->reg,…)`/`store(op->reg,…)` with NO translation (`arm64-asm.c:2099,2122`), while codegen numbering (`src/arch/arm64/arm64-gen.h:6-10`) has `R30 = 19` and `F(x) = x+20` (v0-v7 = indices 20-27). They coincide only for x0-x18; at 19+ they diverge — physical x19 → codegen LR, physical x20-x27 → codegen v0-v7. `register long v asm("x20")` stores an unrelated FP reg; pushing the `'r'` allocator past x18 targets/corrupts LR. Exactly the latent bug `DETAILS.md:14699` predicted after the riscv64 fix; arm32 is protected by `arm_asm_treg()` (`src/arch/arm/arm-asm.c:2465-2475`), arm64 has no shim. Also `reg_saved[]={19..30}` (`:2056-2058`) is physical but gated on the codegen-indexed `regs_allocated[op->reg]`, spuriously save/restoring x20-x27 on float operands.
- **[MED-HIGH] `.section name,"flags",@type` drops the type and force-sets `SHF_ALLOC`.** `asm_parse_directive` starts `flags=SHF_ALLOC` (`src/mccasm.c:806`), parses only `w`/`x`, and parses-then-discards the `@type`/`%type` operand (`:830-835`); `find_section` always makes `SHT_PROGBITS|SHF_ALLOC`. So `.section .tb,"aw",@nobits` → PROGBITS (skip/zero allocate real file bytes, loses BSS/TLS-BSS), and every `.section` gets `SHF_ALLOC` forced on (non-alloc metadata/debug sections wrongly become loadable). Silent.
- **[MED] arm64 cannot clobber v8-v31; models only 8 SIMD regs for asm.** `ARM64_FREG_BASE/LAST=20/27`, `arm64_parse_regvar` only knows v0-v7 (`src/arch/arm64/arm64-asm.c:62-63,233-242`); `asm(:::"v8")` → hard "invalid clobber register" though v8-v15 are callee-saved; the `'x'`/`'y'` constraint distinction collapses to the same 8-reg file.
- **[MED] x86_64 `'A'` constraint reserves only RAX, not RDX:RAX** (`src/arch/i386/i386-asm.c:1280-1285`, `is_llong=0`); an `"=A"` on a 128-bit value leaves RDX unclaimed. (i386 branch is correct.)
- **TASK: T-mac-30033.**

### <a id="r6-disasm-roundtrip"></a>Disassembler ↔ encoder round-trip
- **[HIGH] x86_64 segment-override prefix (`%fs`/`%gs`) parsed then dropped.** `decode()` records `d->seg` but `modrm()` never uses it (`src/arch/x86_64/x86_64-dis.c:284-289` vs `:94-183`); i386 threads seg correctly (`src/arch/i386/i386-dis.c:87,110,146,152`). Every `__thread` access emits `%fs:`-relative; disasm drops it → the `-S`/reassembled form is an absolute load from address 0 (crash), semantics changed. Repro: `__thread int tv; int gt(void){return tv;}` `-S`.
- **[HIGH] Undecoded opcodes under-consume the ModRM byte → instruction-stream desync.** `bsf`/`bsr` (`0F BC/BD`, emitted for `__builtin_ctz/clz/ffs`) hit the `raw0f` `.byte` fallback that returns a length covering only `0F`+opcode, not ModRM/SIB/disp/imm (`src/arch/x86_64/x86_64-dis.c:1181-1184`; same flaw in the 1-byte default `:814-816` and both i386 fallbacks `789-791,897-899`). The next insn starts mid-stream; the epilogue vanishes and `-S` no longer reassembles. Repro: `int f(unsigned x){return __builtin_ctz(x);}` `-S`.
- **[MED] x86_64 `movd`/`movq` register operand decoded 16-bit under the mandatory `0x66`.** `vsize()` treats the mandatory `0x66` as an operand-size override (`:204-210`) → GPR printed 16-bit (`:1070-1086`): `movd %eax,%xmm0` → `movd %ax,%xmm0`. Latent for C codegen (memory-operand/REX.W forms only), real for the inline-assembler path + disassembling arbitrary objects.
- **[LOW-MED] arm64 HFA-passing `LD1`/`ST1` not decoded.** The `LD1 0x4c40…` fails the mask test at `src/arch/arm64/arm64-dis.c:872` → bare `.long`; `ST1` matches but prints `{…}` losing the reg-list/element size. Round-trip preserved (raw word) but disasm uninformative for any HFA-by-value call.
- **TASK: T-mac-30034.**

### <a id="r6-debug-info"></a>Debug-info emission vs codegen
- **[HIGH] arm64 synthetic FDE hardcodes a fixed prologue shape → wrong CFA for variadic / many-arg functions.** `mcc_debug_frame_end` (`src/mccdbg.c:1591-1606`) assumes the sp-sub lands at insn ~4, but the real prologue inserts up to 9 variable register-save insns (`src/arch/arm64/arm64-gen.c:1564-1614`); unlike x86_64 (which rebases the CFA onto fp via `DW_CFA_def_cfa_register`, `mccdbg.c:1574`) the arm64 CIE keeps the CFA sp-relative (`:1445-1453`) and only adjusts `def_cfa_offset`, so `def_cfa_offset 224+diff` attaches ~28-36 bytes too early → CFA overshoots by `diff` across the save window → corrupted backtraces / broken table unwind for any variadic fn. riscv64 shares the anti-pattern (`:1607-1633`).
- **[MED] Flexible/zero-length array emits a bogus 2^64 `DW_AT_upper_bound`.** `dwarf_uleb128(…, t->type.ref->c - 1)` with `c == 0/-1` → `-1` sign-extends to `0xFFFF…FF` (`src/mccdbg.c:2714`; abbrev `:150-152`). gcc/clang omit the bound; consumers see ~2^64 elements.
- **[MED] enum DIE hardcodes `byte_size=4` and an `int` base**, contradicting `enum E:long long` (8), value-widened enums (8), and `-fshort-enums`/packed (1/2) (`src/mccdbg.c:2608,2619` vs `src/mccgen.c:7398-7437`). Debugger reads the wrong byte count; consumer layouts wrong.
- **[MED] bit-field members always use `DW_AT_data_bit_offset` (a DWARF-4 attribute) even in a DWARF-2 CU** (`src/mccdbg.c:184-190,2574-2579`; CU version from `s1->dwarf` can be 2 — the Mach-O default). Non-conforming; strict v2/v3 consumers can't decode any bit-field. (Adjacent to but distinct from the T-mac-30015 Mach-O-weak-`-g` note.)
- **TASK: T-mac-30035.**

### <a id="r6-type-abi-half"></a>Type-system / ABI
- **[HIGH] `_Float16`/`__bf16` passed/classified in INTEGER registers on x86_64 & arm64, violating both psABIs and internally inconsistent with how they are RETURNED.** `is_float_abi()` excludes the half types (`src/mccgen.c:669-670`) though `is_float()` includes them, so args go via a GPR (x86_64 `classify_x86_64_inner` → integer mode, `src/arch/x86_64/x86_64-gen.c:1146-1148`; arm64 skips the FP-reg rule → x0, `src/arch/arm64/arm64-gen.c:1119,1125`, and half-HFAs not recognized at `:979`), while scalar returns use xmm0/v0 (`is_float()` true, `src/mccgen.c:15036`). Any call to/from a gcc/clang half-taking/returning function miscompiles, and a wrongly-GPR half shifts EVERY following integer arg. Root cause is the `is_float`/`is_float_abi` split (`src/mccgen.c:669-670`) — reconcile the two predicates, don't patch per-arch.
- **[MED] x86_64 over-aligned ≤16B struct over-counts registers (NO_CLASS not modeled).** `classify_x86_64_arg` derives reg_count from mode+size/8 (`src/arch/x86_64/x86_64-gen.c:1304-1330`) and `classify_x86_64_inner` merges fields only, never per-eightbyte NO_CLASS. `struct{_Alignas(16) int x;}` (size 16, 2nd eightbyte all padding) → mcc rdi:rsi / rax:rdx, gcc rdi / rax only → ABI break + shifts following args. `struct{_Alignas(16) double d;}` hits it on the SSE side. arm64 is correct here (ceil(size/8), no NO_CLASS concept).
- **TASK: T-mac-30036.**

### <a id="r6-rir-replay"></a>CST/AST/RIR capture-and-replay
- **[MED-HIGH] Census replay fallback restores `.text`/`.rela.text` but never rewinds `.data`/`.rodata`.** `ast_func_end` rewinds only text+rela (`src/mccast.c:20869-20872`), the fallback restores only those (`:21704-21712`); trial-appended data/rodata constants (e.g. a `double` in the const pool) are neither compared nor rewound — the exact stale-section shape the T-win-50003 Bucket B fix zero-fills in the OTHER trial path (`ast_search_emit_size`, `src/mccast.c:19617-19661`), but here in the DEFAULT `-O1+` census path. Benign free-space on ELF/Mach; PE import-table-corruption class (0xC0000005 pre-main) on PE — an uncovered latent-corruption / cross-platform surface.
- **[MED] `ast_reemit` frame-floor scan counts only `VT_LOCAL`, misses `VT_LLOCAL` → under-sized frame.** `src/mccast.c:21876` gates on VT_LOCAL only; the census scan covers both (`:6931-6932`). A forward-inlined re-emit whose only deep local is `VT_LLOCAL` gets too-high a floor → replay-allocated temporaries overlap it → silent stack corruption. No faithfulness fallback on this path; `-O1+` only.
- **[MED] `ast_fold_rec` result-type promotion diverges from `gen`'s usual-arithmetic-conversions** (new fold-vs-gen family member, distinct from the div-value and diagnostic ones). Sub-int operands are never promoted to `int` (`src/mccast.c:7042-7054` keeps the narrow type, may OR in `VT_UNSIGNED`), and `:7069` stamps x's type-ref even in the branch that switched `tt` to y's type. The value matches (`value64` is 32-bit for non-LLONG) but the node TYPE differs → a signedness-sensitive parent op selects different codegen; ships under `-fno-replay-fallback`. Reachable only after a prior template pass synthesizes a `Binary(lit,lit)` from sub-int operands.
- **TASK: T-mac-30037.**

### <a id="r6-diagnostics-pedantic"></a>Diagnostics — pedantic / implicit-int severity
- **`-pedantic-errors` fires HARD errors inside system headers on most preprocessor pedantic paths** — the warning form is dropped there by `error1`'s system-header suppression (`src/libmcc.c:667-673`), but the error form is not, and the pp sites guard `!pp_in_system_header()` only sporadically. Unguarded: `src/mccpp.c:2979,3053,3190,3228,3510,3553,3751,4152,5295`; guarded siblings `:1441,2454,4299,4613,4618`; the `mcc_pedantic()` helper self-guards (`src/mccgen.c:547-552`). So the same construct in a `<system header>` is silent under `-pedantic` but fatal under `-pedantic-errors` — site-dependent, not rule-dependent; makes `-pedantic-errors` unusable against real SDK/glibc headers.
- **`warn_pedantic`-only diagnostics that never honor `pedantic_errors`** — e.g. tentative-array "assumed one element" (`src/mccgen.c:18803-18806`) stays a warning under `-pedantic-errors` while its siblings promote; the flag is silently partial.
- **`-pedantic-errors -Wno-pedantic` honored on some paths, ignored on others.** `-Wno-pedantic` clears only `warn_pedantic`, not `pedantic_errors` (`src/libmcc.c:2403,2793-2794`); `warn_pedantic`-gated sites go silent (`mcc_pedantic` early-return `src/mccgen.c:545`; `src/mccpp.c:3481,3508,3551,3749`) while `pedantic_errors`-only sites still hard-error a disabled diagnostic (`src/mccpp.c:1441`, `src/mccgen.c:4326,5850`).
- **C99 implicit-int diagnosed 3 different ways, and `-Wno-implicit-int` DOWNGRADES the error instead of suppressing.** `src/mccgen.c:18657` hard-errors (old-style extern param) but `:18443,:18526` stay warnings forever; and `:18657` is gated on `(warn_implicit_int & WARN_ON)` so `-Wno-implicit-int` takes the else branch (`:18660`) and turns the hard error into a warning — the opposite of `-Wno-`.
- **TASK: T-mac-30038.**


## Round 7 (2026-08-17) — parser/declarators, static initializers, macro-expansion engine, control-flow/aggregate codegen, attributes/pragmas, char/string literal encoding

Six parallel subsystem audits (mac-arm64), all fresh relative to rounds 1-6 (which covered pp *directives*, ABI *classification*, and object-format *emission* — not the front-end constructs below). Every finding cites `file:line`; all deduped against rounds 1-5 + `DETAILS.md`/`TODO.md`/`ARCHIVED.md`. Two of the six subsystems (macro-expansion engine, static initializers) came back essentially ROBUST — strong negative results recorded below. Tasks minted: **T-mac-30039..30042**.

### <a id="r7-parser-declarators"></a>Parser / declarators / storage-class / C23
- **[MED] `constexpr` out-of-range unsigned initializer silently accepted with a value change.** `constexpr_ival` (`src/mccgen.c:18257`) collapses the initializer to a signed `long long`, discarding source signedness; the representability check `!in_range(ll, dt.t)` (`:18346`) then tests the sign-collapsed value. An unsigned 64-bit constant in `[2^63,2^64)` whose signed reinterpretation lands in range passes wrongly: `constexpr int i = 0xFFFFFFFFFFFFFFFFULL;` → `i == -1` (also `short`, `long long`); gcc/clang reject per C23 6.7.1 ("not exactly representable"). `0x8000…000ULL` (=LLONG_MIN) *is* caught, so only the small-magnitude-negative band leaks.
- **[MED] Storage-class specifiers accepted in every type-name / abstract-declarator context.** The cast/compound-literal/`sizeof`/`_Generic`/`_Alignof` type-name parse (`src/mccgen.c:12974` + siblings) calls `parse_btype`+`type_decl(TYPE_ABSTRACT)` but never rejects the storage classes `parse_btype` consumes (`:9164-9174,9202-9216`). `(static int)3`, `(register int)x`, `(typedef int)3`, `sizeof(static int)`, `_Generic(1, static int:…)`, `(static int){5}` all accepted; clang errors. Missing-diagnostic (class is ignored, not a miscompile) but a broad silent-accept hole across all abstract-declarator entry points.
- **[LOW-MED] `register`/`auto` accepted on struct/union members.** Member validation (`src/mccgen.c:7500-7504`) rejects members with `type1.t & VT_STORAGE` (catches `static`/`extern`/`typedef`/`inline`/`_Thread_local`/`constexpr`) but not `auto`/`register`, which `parse_btype` records only in `ad1.storage_class` bits 1/2 (`:9173`), never inspected here. `struct S{ register int x; };` / `struct S{ auto int x; };` silently accepted; clang errors. Same root family as the type-name hole above (`storage_class` bits unchecked outside object declarations).
- **[LOW] `_Noreturn` on a non-function object is at most a pedantic warning.** `src/mccgen.c:18582-18583` emits `mcc_pedantic(…)` for `(storage_class & 128) && btype != VT_FUNC`; `_Noreturn int x;` compiles silently under default flags, never an error; gcc/clang make it a hard constraint error.
- **VERIFIED ROBUST (negative):** `_Generic` (controlling expr not evaluated; decay/qualifier/`_Atomic` strip; `_BitInt` width-distinctness); tag/enum/prototype-scope namespace isolation; scope-sensitive typedef-vs-identifier resolution; K&R default-arg promotion + prototype-mismatch diagnosis; the full declarator zoo (`int (*(*p)[3])(void)`, function-returning-function rejection, `[*]` prototype-only, `[static N]`); all C23 `[[attribute]]` positions, `auto`/`typeof`/`typeof_unqual`, `constexpr` narrowing; linkage/tentative-definition rules. `[NEW]` cluster. **TASK: T-mac-30039.**

### <a id="r7-charstr-encoding"></a>Character / string literal encoding — surrogate & escape handling
Reproduced by dumping emitted `.data` bytes from the actual `mcc` binary, not by reading alone.
- **[MED] Hex/octal escapes > 0xFFFF in `u"…"`/char16 strings silently surrogate-pair-encoded (1 escape → 2 code units).** `src/mccpp.c:3400-3420`: `\x`/octal values are stored raw into the `nwchar_t` buffer, then the char16 emission loop treats *any* value ≥ 0x10000 as a code point needing a UTF-16 surrogate pair — it cannot distinguish a `\x` escape from a UCN. `char16_t c[] = u"\x1F600";` emits `0xD83D 0xDE00` (bytes `3d d8 00 de`); the standard requires the escape fit `char16_t` (gcc/clang: "hex escape out of range"). Values > 0x10FFFF yield garbage surrogates.
- **[MED] Hex escapes forming a surrogate pattern silently surrogate-*combined* in `U"…"`/char32 strings and `U'…'` chars (2 escapes → 1).** `src/mccpp.c:3430-3438` (string), `:3369-3384` (char): the char32/`U` emission runs surrogate-*pairing* over the raw escape buffer, so two independent `\x` escapes shaped like high+low surrogate merge into one code point. `char32_t d[] = U"\xD800\xDC00";` → single `0x00010000` instead of two elements; `U'\xD800\xDC00'` → single `0x00010000` with no multi-character warning. Each `\x` must be a distinct `char32_t`.
- **[LOW-MED] `u'…'`/char16 char constant > 0xFFFF silently masked to 16 bits.** `src/mccpp.c:3397` (`if (prefix == 'u') c &= 0xFFFF;`): `u'\x1F600'` → `0x0000F600` with no diagnostic; gcc/clang reject "character too large".
- **[KNOWN, concrete root] UCN > 0x10FFFF and lone surrogates (`\uD800`) accepted silently in wide/`u`/`U`/`L` literals.** `src/mccpp.c:3196-3199` routes `is_long` UCNs to `add_hex_or_ucn` (raw store), bypassing the `unicode_to_utf8` validation (`:301-309`) that rejects surrogates and > 0x10FFFF for narrow/`u8`; the sole wide check (`:3190`) is gated on `warn_pedantic && cversion < 202311` and omits any `> 0x10FFFF` clause. `u"\uD800"` compiles clean under `-std=c23`. This is the concrete root cause of the aggregate census bucket "UCN validation (out-of-UCS-codespace and malformed `\u`)" at `DETAILS.md:33637`. Distinct from the `WCHAR_MAX` header-macro items (T-mac-30031/T-lin-10397), which are predefines, not literal emission.
- **VERIFIED ROBUST (negative):** narrow `\xHH` stays a single raw byte (never hits UTF-8 encoding) with overflow diagnosed; narrow/`u8` UCN correctly UTF-8-encoded and range-validated; mixed-prefix concatenation is a hard error (`merge_str_kind`, `mccgen.c:17038`); `parse_string`'s `buf[1000]` falls back to malloc (no truncation); multi-char `'ab'`→`0x6162` with warning; element sizing/endianness consistent with supported LE targets. `[NEW]` cluster. **TASK: T-mac-30040.**

### <a id="r7-bool-weak-fold"></a>Control-flow / aggregate codegen — `(_Bool)&weak_sym` folds to constant `1`
- **[MED] `(_Bool)&sym` unconditionally folds to `1` with no weak-symbol guard → miscompile for weak symbols.** `gen_cast` (`src/mccgen.c:5490-5493`): casting a symbol-address constant to `_Bool` sets the result to `1` for `(vtop->r & (VT_VALMASK|VT_LVAL|VT_SYM)) == (VT_CONST|VT_SYM)`. A weak symbol that resolves to address 0 must give `(_Bool)&weak_sym == 0`. The path is not gated by `CONST_WANTED` and fires before the `DATA_ONLY_WANTED` check (`:5496`), so it hits both runtime (`_Bool b = &weak_var;`) and static-initializer (`static _Bool b = (_Bool)&weak_var;` → wrong `1` byte in `.data`) contexts. Provably inconsistent with the file's other weak-symbol folds, which *do* guard: `condition_3way` (`:14566`, `!vtop->sym->a.weak`) and the `sym==sym` fold (`:3806`, `!v1->sym->a.weak && !v2->sym->a.weak`). `!&weak_sym` correctly does NOT fold (emits a runtime test).
- **VERIFIED ROBUST (negative):** switch compare-chain (signedness of sort == signedness of compare, `long long`/negative/unsigned cases, duplicate/overlap/range detection, empty switch); switch jump-table bounds + arm64 `R_AARCH64_PREL32` table math; bit-field load/store (unit-spanning sign-extension, `_Bool`, non-zero offset, reverse-SO); alloca/VLA across all 5 arches (over-alignment, scope-restore on break/continue/goto/return, jump-into-VMS diagnosed); computed goto. `[NEW]`. **TASK: T-mac-30042.**

### <a id="r7-attr-pragma"></a>Attribute / pragma semantics — parsed-then-dropped
- **[MED-HIGH] `#pragma weak <symbol>` is unimplemented — symbol stays strong.** `pragma_parse` (`src/mccpp.c:2599-2809`) handles only `push_macro`/`pop_macro`/`once`/`pack`/`comment`/`message`/`STDC`/`options align`; `#pragma weak pthread_create` (and `#pragma weak alias = target`) falls to the default `"#pragma %s ignored"` (`:2805-2808`) and never gives `STB_WEAK` binding (unlike `__attribute__((weak))` at `:6500`→`:1206`). A weak *reference* stays a strong undef → link failure; a weak *definition* stays strong → multiple-definition error. Common libc/pthread idiom.
- **[MED] `constructor`/`destructor` priority parsed then discarded → wrong global init/fini order.** `src/mccgen.c:6378-6382` reads `__attribute__((constructor(101)))`'s priority with `expr_const()` but throws the value away; `struct FuncAttr` (`src/mcc.h:316-317`) has only single-bit `func_ctor`/`func_dtor`. Emission (`:18073` → `add_array` → `mccelf.c:1502`) always appends to a fixed `.init_array`/`.fini_array` with no `.NNNNN` priority suffix, so all priorities collapse into one unordered section — prioritized constructors run in the wrong relative order. gcc/clang emit `.init_array.NNNNN` for the linker to sort.
- **[MED] `used`/`unused`/`maybe_unused` set no flag → `-Wunused -Werror` builds break.** `src/mccgen.c:6508-6520`: `TOK_USED*`/`TOK_UNUSED*`/`TOK_MAYBE_UNUSED*` (plus `fallthrough`/`unsequenced`/`reproducible`) all fall to a bare `break`, so `a.used` is never set — yet `-Wunused-variable` (`:15498`, `!sm->a.used`) and `-Wunused-function` (`:1081`) consult it. `int x __attribute__((unused));` / `[[maybe_unused]]` still warns; a `used` static function still warns "defined but not used". Diagnostic-only (emission unaffected) but breaks conforming `-Werror` builds.
- **[MED] `#pragma GCC diagnostic push/pop/ignored` is a no-op and emits a spurious "unknown pragma" warning.** No `GCC`/`clang` prefix arm in `pragma_parse`, so `#pragma GCC diagnostic ignored "-Wfoo"` (and `GCC poison`/`system_header`/`push_options`) hits the default (`src/mccpp.c:2805`): the intended local suppression is ignored *and* mcc prints `#pragma GCC ignored` under `-Wunknown-pragmas`. Headers that bracket code in push/ignored/pop get no suppression.
- **VERIFIED ROBUST (negative):** `aligned`/`packed` + `#pragma pack` layout (tcc-derived `struct_layout`, per-field override, saturation caught, bit-field container fixup); `#pragma pack` push/pop stack (balanced, per-TU reset, no cross-TU leak); `cleanup` (runs on all exits incl. `goto`-out); `alias` (undefined target diagnosed); `mode`/`vector_size`/`transparent_union`/`visibility`/`section`/`deprecated`/`always_inline`/`noinline`/`scalar_storage_order` all parsed *and* applied. `[NEW]` cluster. **TASK: T-mac-30041.**

### <a id="r7-init-robust"></a>Static / aggregate initializers — VERIFIED ROBUST (negative result) + one latent LOW
Broad correctness matrix (mcc arm64-Darwin vs clang, byte/behavior-identical): const-address relocs (`&arr[3]`, `(char*)&s.c`, address differences, negative addends, function addresses, string-literal+offset, offsetof-style null-member folds); anonymous-symbol reloc-copy path incl. reverse-designated relocs; designated init (sparse, re-init/override with stale-reloc drop, mixed positional, nested, GNU range `[0 ... 3]=` spreading a struct-with-reloc); string init (no-NUL exact fit, too-long, wide); bit-fields/`_Bool`/union/FAM/`_Atomic`/`_Complex`; excess/out-of-range/non-constant diagnostics; local zero-skip preserving `-0.0`. One latent defect only:
- **[LOW] Designator/element offset arithmetic done in `int`, not widened — overflows on multi-GB objects.** `src/mccgen.c:16712` (`c += index * elem_size;`), `:16808` (`c += nb_elems * elem_size;`), and the string-fill offsets `:17200,17211` compute the byte product in 32-bit `int` before adding to `unsigned long c`. A designated index or element offset whose byte product exceeds `INT_MAX` (needs a ≥2 GB statically-sized object) yields a wrong (negative) placement offset / reloc `r_offset`, silently. Inconsistent with the sibling spread path at `:16632`, deliberately written `c + (unsigned long)elem_size * i` (widened) — showing the author knew the cast was needed; these four sites were missed. Captured, not taskified (needs an impractical object size).

### <a id="r7-macro-robust"></a>Preprocessor macro-expansion engine — VERIFIED ROBUST (negative result)
Differential (mcc `-E` vs clang) across ~50 pathological cases: the C standard §6.10.3.4 example, the Fultz `DEFER`/`OBSTRUCT` deferred-expansion idiom, hide-set propagation through `##`, GNU comma-paste, `__VA_OPT__` at paste boundaries, wide/prefixed-string/char stringize, 8000-token expansions, invalid-paste diagnostics — **output token sequence byte-identical to clang after whitespace normalization**; no silent wrong-expansion found. Rescan/blue-paint (`macro_subst_nested:5425`, `nested_list` across the invocation boundary `next_argstream:5132`), `##` ordering + invalid-token detection, `#` escaping of `"`/`\` (`:4893`), `__VA_OPT__` depth-≤2, prescan cache `s->e` all correct; no fixed-buffer truncation. Only residue: a cosmetic `-E` inter-token whitespace divergence around expanded/placemarker boundaries (`src/mccpp.c:5438`) — no effect on compilation (mcc compiles from the token stream; `#`-stringize collapses whitespace independently). KNOWN-adjacent to the already-*fixed* pp-whitespace class at `DETAILS.md:15628`. No task warranted unless byte-exact `-E` parity becomes a gate.


## Round 8 (2026-08-18) — optimizer-transform correctness, varargs/stdarg, `_Complex`, builtins/atomics, int/ptr conversion codegen, linkage/visibility/TLS

Six parallel subsystem audits (mac-arm64), fresh relative to rounds 1-7 (which covered opt-SEARCH nondeterminism and the fold-vs-gen family, ABI *classification*, and linker *resolution* — not the transform-correctness, va_arg, complex, intrinsic, or TLS-model surfaces below). Several findings were reproduced against a rebuilt `mcc` (miscompiles confirmed by `-O0` vs `-O2` runtime divergence and by emitted symbols). Two subsystems (int/ptr conversion codegen; most of atomics) came back ROBUST. Tasks minted: **T-mac-30043..30047**.

### <a id="r8-x86_64-tls-model"></a>Linkage / visibility / TLS-model emission
- **[HIGH] x86_64 ELF has NO general-dynamic / initial-exec TLS — always emits local-exec `R_X86_64_TPOFF32`, ignoring `-fPIC`/`-shared`/visibility.** Every TLS access on x86_64 emits `R_X86_64_TPOFF32` (`src/arch/x86_64/x86_64-gen.c:310,326,417,444,556,576`); unlike i386 (`i386-gen.c:94-108` emits `R_386_TLS_GD`/`LDM` under `pic`) and arm64 (`arm64-gen.c:641-648` TLSDESC under `pic`), x86_64 has **no `gen_tls_addr`** and never emits `TLSGD`/`GOTTPOFF` (those cases exist in `x86_64-link.c:20-24,270,297` only to *consume* external relocs). No guard rejects local-exec in a shared object (contrast riscv64's error). Trigger: `mcc -fPIC -shared` (or a `.o` fed to `gcc -shared`) with any `__thread` var → external `ld` rejects `TPOFF32 can not be used when making a shared object`; mcc's internal DLL link silently computes a bogus TP offset; an `extern __thread` from a DSO further mis-indexes `sections[sym->st_shndx]` (`:334`). Strictly broader/worse than the KNOWN i386-only `R_386_TLS_GOTIE` gap (T-lin-10020) — x86_64 is the sole arch with zero dynamic-TLS support, on the primary target.
- **[MED] Mach-O output ignores `STV_HIDDEN` / `-fvisibility=hidden` — never sets `N_PEXT`.** `convert_symbol` (`src/objfmt/mccmacho.c:995-1038`) reads the ELF visibility (`:966`) only for a debug `dprintf`; it sets `N_EXT` for global and `N_WEAK_*` for weak but never consults `vis`, and `N_PEXT` (0x10, private-extern) is defined/used nowhere. Hidden symbols (correctly honored for ELF via `update_storage`, `mccgen.c:1199-1202`) leak into a dylib's export table where clang emits `.private_extern`→`N_PEXT` — defeats interposition control, cross-object collisions. (Distinct from the R4 Mach-O linker items T-mac-30028: this is the front-end→objfmt visibility translation.)
- **[LOW] `update_storage` tests `VT_INLINE`→`STB_LOCAL` before `a.weak`** (`src/mccgen.c:1204-1209`) — a still-`VT_INLINE` weak def would bind LOCAL; currently unreachable because the C99 emission path strips `VT_INLINE` before setting weak (`:18159,18546`). Latent precedence inversion.
- **VERIFIED ROBUST (negative):** C99/GNU `inline` emission (inline-only emits no external def; `extern inline` C99-vs-gnu89 inverted correctly; `-fc99-inline-body` emits a *weak* out-of-line copy; `drop_gnu_inline_body` retires a body superseded by a real def); ELF binding/visibility; tentative/common→SHN_COMMON, `-fno-common`→.bss, TLS→.tbss/.tdata; arm64/i386 dynamic TLS. `[NEW]`. **TASK: T-mac-30043.**

### <a id="r8-complex-fold"></a>`_Complex` constant-fold path (runtime + main-arch ABI are ROBUST)
- **[MED] `_Complex __float128` / `_Complex _Float128` silently narrowed to `_Complex float`.** `combine_complex_base` (`src/mccgen.c:8410-8416`) recognizes only `VT_LDOUBLE`/`VT_DOUBLE` among floats and maps everything else (incl. `VT_FLOAT128`) to `VT_FLOAT`; `gen_complex_op` (`:8514-8519`) then re-types the whole op to `_Complex float` and dispatches `__mcc_cmulf`/`cdivf`. Every `+ - * /` on `_Complex __float128` computes at 24-bit precision (there is also no `__float128` complex helper). Silent, no diagnostic.
- **[MED] Constant-folded complex division/multiply diverge from the (upgraded) runtime.** The fold block in `gen_complex_op` (`src/mccgen.c:8526,8582-8650`, gated on `CONST_WANTED` only — not `cx_limited_range`) uses textbook `(ac+bd)/(c²+d²)` with the denominator in the *base* type: for `_Complex float` `den` overflows in float where `__mcc_cdivf` widens via double, and for double there's no Smith `|c|<|d|` scaling nor the Annex-G `CDIV_RECOVER`/`GEN_MUL` inf-NaN recovery the runtime (`runtime/lib/complex.c`) now has. A compile-time-constant `a/b` (or `a*b`) and the identical runtime expression give different results (overflow→0, `x/0`→NaN+NaN instead of the Annex-G infinity) — contradicts the predefined `__STDC_IEC_559_COMPLEX__ 1`. Finishes the Q-lin-10012 fix on the fold side (`[KNOWN-adjacent]`); the mul-recovery gap is `[NEW, LOW]`.
- **[LOW] `_Imaginary` unconditionally rejected** (`src/mccgen.c:9083,15716`) while Annex-G conformance is advertised — matches gcc's deviation, defensible, tracked.
- **VERIFIED ROBUST (negative):** runtime helpers (`__mcc_cmul*` full G.5.1 recovery, `__mcc_cdiv*` Smith + `CDIV_RECOVER`, `cdivf` double-widened; integer complex divide fold==runtime); x86_64 SysV ABI (`_Complex float`→1 SSE, `double`→2 SSE, `long double`→memory arg / st0:st1 return), riscv64 (2 FP regs, variadic→GPR), arm64 (2-element HFA); `__real__`/`__imag__` lvalues + `__imag__` of a real→0; complex `==`/`!=` and real↔complex UAC; relational-on-complex diagnosed. `[NEW]`. **TASK: T-mac-30044.**

### <a id="r8-varargs"></a>Varargs / stdarg
- **[MED] x86_64 SysV `__va_arg_inline` prematurely consumes the last XMM slot for a 2-eightbyte SSE aggregate → later FP `va_arg` mis-read.** `runtime/include/mccdefs.h:549-560`: for an all-SSE 2-register aggregate (`struct{double a,b;}`, `_Complex double`, size 16) it does `fp_offset += 16` (`:551`) *before* the capacity test (`:554`); with exactly one XMM slot left (`fp_offset==160`), the struct correctly falls to the overflow area but leaves `fp_offset` stuck at 176. The caller backfills a later single `double`/`float` vararg into the leftover XMM7 *without* bumping `nb_sse_args` (`x86_64-gen.c:1699-1712`), so the callee then reads that arg from the overflow area instead of the register — silent miscompile. Contrast the integer path (`:545`) and mixed path (`:562`) which test capacity up-front. Fix: gate on `fp_offset + 16 <= 128+48` before the first `+= 16`. Trigger: `f(fmt, 7 doubles, struct{double,double}, double)` then `va_arg(ap,double)`.
- **[MED, KNOWN root] arm64-ELF `assert(0)` in `gen_va_arg` still live for a size==16/align==16 non-HFA aggregate** (`src/arch/arm64/arm64-gen.c:1728`) — a conforming `struct __attribute__((aligned(16))){long long a,b;}` through `...` aborts the compiler. This is Round-1's `#codegen-arm64-vaarg-assert` (captured, never taskified): the `size<=16` guard added for `__int256` (`>16`) left the `==16 & align 16` case reaching the assert. Now precisely located; folded into this task.
- **VERIFIED ROBUST (negative):** x86_64 register-save area + sret/varargs seeding (`_Complex long double` x87-return excluded); arm64 gr_offs/vr_offs incl. size>8 spill + HFA count + indirect >16B; riscv64 GP-only save contiguous below overflow; i386/arm char* walkers; `va_copy`/`va_end`/small-int promotion; `check_va_start_*` null-guard already fixed. `[NEW]` + `[KNOWN]`. **TASK: T-mac-30045.**

### <a id="r8-promote-setjmp"></a>Optimizer transform — `-fpromote-locals`/`-finline` miss an indirect `setjmp`
- **[MED] `-fpromote-locals` promotes a live local across a `setjmp` reached through a function pointer → silent miscompile at `-O2`.** `ast_body_has_setjmp` (`src/mccast.c:3013,4598`) matches only a callee whose *name* textually contains `"setjmp"` on a direct `AST_Ref` (`:3019-3024`); an indirect call (`setjmp` via a function pointer) is a name-less `Invoke`, so the scan returns 0, promotion proceeds, and a live local is promoted into a callee-saved register that `longjmp` restores to its setjmp-time value — discarding assignments in between. `-fpromote-locals` is auto-enabled at `-O2` (`:2349,2359`). Reproduced: a program storing `x=20` between `sj(jb)` and `longjmp` prints `x=20` at `-O0`, `x=10` at `-O2`/`-O3`; the direct-call form is correctly caught. The blind spot is self-documented (`:3007-3011`) but untracked.
- **[LOW] Same name-based blind spot in the inliner's setjmp guard** (`ast_fn_inlinable`, `src/mccast.c:3049-3058`) — a callee invoking `setjmp` indirectly can be inlined under `-finline`, dissolving the frame boundary. Same root; same fix (reject address-of-`setjmp` / any indirect call).
- **VERIFIED ROBUST (negative, differential ~230 programs):** inline argument single-evaluation; copy-prop-into-inline gated by `ast_local_is_readonly`/`ast_cprop_escapes` (alias case blocked); promote-locals excludes volatile/address-taken, bails on VLA/`&&`/computed-goto; algebraic identities operate on eager operands and FP folds only both-const + NaN/Inf/fenv-guarded; volatile stores preserved, no store-dropping DCE. `[NEW]`. **TASK: T-mac-30046.**

### <a id="r8-builtins"></a>Builtins / atomics (atomics ROBUST; the gaps are builtin lowering)
- **[MED] glibc-only builtins hard-link-fail on macOS/BSD.** `runtime/include/mccdefs.h:710-717` predefine `__builtin_mempcpy`, `__builtin_stpncpy`, `__builtin_{fprintf,printf,fputs}_unlocked` via `__BUILTIN(...)` (`:657`) with an unconditional `__RENAME` asm-label to `mempcpy` etc. — no platform guard. On macOS libSystem (no `mempcpy`/`*_unlocked`), `__builtin_mempcpy(...)` emits `bl _mempcpy` → `unresolved reference to '_mempcpy'` (also via `__builtin___mempcpy_chk`, `:824`). gcc/clang lower `__builtin_mempcpy` intrinsically and never need the symbol.
- **[MED] Non-constant `__builtin_clz`/`ctz`/`ffs` become a runtime CALL on every non-x86 target.** The inline IR ops are `#ifdef MCC_IR_HAVE_X86_PRIMS` (`src/mccircap.c:438-441`) and dispatch is `#if MCC_TARGET_X86_64` (`src/mccgen.c:13683-13702`); the fall-through is `vpush_helper_func; gfunc_call`. On arm64, `__builtin_clz(x)` → `bl ___builtin_clz` instead of native `clz`/`rbit;clz` — correct results but a large perf regression + a hard runtime-lib dependency, diverging from x86 (which inlines). `popcount`/`parity`/`clrsb` DO inline on all targets, so the gap is specifically clz/ctz/ffs.
- **[LOW, in KNOWN `#runtime-intrinsics`] Overflow builtins are not constant-expressions and mis-handle >64-bit non-`__int128`.** `__builtin_{add,sub,mul}_overflow(_p)` expand to the runtime `__mcc_ov_calc*` (`mccdefs.h:1022-1054`), so they're rejected in constant expressions (gcc/clang accept), and the wide-path selector `__mcc_ov_is_wide` (`:966`) matches only exactly `__int128`, so `_BitInt(N>64)`/`__int256` operands silently take the 64-bit path and truncate → wrong verdict. The 64-bit/`__int128` math itself is correct.
- **VERIFIED ROBUST (negative):** bit/bswap builtins (correct + const-fold via `fold_bit_builtin`); atomics honor memory order (arm64/riscv runtime helpers branch on the order arg; x86 strong-model makes the dropped order correct; CAS updates `*expected` on failure; `weak`→strong is legal); non-lock-free-size atomics have a real generic spinlock-table fallback; `__sync_*`→seq_cst; `alloca`; `choose_expr`/`types_compatible_p`/`constant_p`/`expect` compile the untaken arm under `nocode_wanted`; `memcpy`/`memset`/`memmove`→libc. `[NEW]`. **TASK: T-mac-30047.**

### <a id="r8-intptr-robust"></a>Integer / pointer conversion & arithmetic codegen — VERIFIED ROBUST (negative result)
Broad code-read + arch cross-check found no HIGH/MED miscompile. Pointer subtraction `p - q` uses signed `VT_PTRDIFF_T` byte-diff ÷ a de-`VT_UNSIGNED`'d element size via signed `TOK_PDIV` (correct for non-power-of-two sizes; the SAR shortcut fires only for pow2); `pointer + integer` scales in 64-bit `VT_SIZE_T` (unsigned indices zero-extended, no LP64 truncation); usual-arithmetic-conversions promote sub-`int` to signed and pick `unsigned` only for exact `unsigned int`/`long`/`long long`, so mixed-sign compares select the right `TOK_ULT/UGE` form (`-1 < 1u` unsigned); load extension picks signed/unsigned per exact `(ft & VT_TYPE)` on every arch; `gen_cvt_csti`/cast chains re-extend correctly; int↔pointer casts warn on size mismatch and rely on 32-bit-write zeroing; null-pointer-constant guarded by `VT_CONST`/`!VT_NONCONST`. Two benign LOW observations only: (1) `_Bool` lvalue is sign-extended on load (movsbl vs gcc's movzbl) on all arches — unobservable because every `_Bool` store is normalized to 0/1; (2) ILP32 `pointer + (long long)index` narrows via a signed cast — low-32 result identical mod 2³². No task; recorded as a negative result.


## Round 9 (2026-08-18) — register allocator/spill/frame, FP codegen & conversions, `-run` execution engine, source-location/diagnostics, type-compatibility/qualifiers, section/output layout

Six parallel subsystem audits (mac-arm64), fresh relative to rounds 1-8 (which covered ABI *classification*, opt transforms, object-format *reloc-emit* and *readers*, and diagnostic *severity* — not the allocator, scalar-FP conversion, `-run` loader, source-position, type-compat-predicate, or output-layout surfaces below). Many findings reproduced against a rebuilt `mcc` (diagnostics diffed vs clang; `-run` vs AOT). Three subsystems (FP codegen, register allocator, section layout) came back largely ROBUST with only LOW/secondary items. Tasks minted: **T-mac-30048..30051**.

### <a id="r9-diag-location"></a>Source-location / diagnostic position (parser→diagnostic handoff)
Empirically diffed vs clang across 21 inputs; the preprocessor's line/`#line`/`__LINE__`/include-stack tracking is ROBUST — the defects are all in the parser→`error1` handoff, which reads the *live lexer position* instead of the offending token's captured position.
- **[HIGH] Diagnostics report the LOOKAHEAD token's line, not the offending token's — drifts across newlines onto valid code.** `error1` derives the line and caret from the current `file->line_num`/`buf_ptr` (`src/libmcc.c:702,590,618`); expression parsing peeks one token ahead, so a diagnosed operand at end-of-line is reported on a later line. `return undeclared_at_eol\n + 1;` → mcc says line 3 (the `+ 1;`), clang says 2:12; a blank line in between drifts it 3 rows. The token-start line *is* captured (`tok_line_num`/`tok_line_file`, `mccpp.c:22-23,4128`) but never consulted, and is overwritten by the lookahead lex — a correct fix needs the parser to snapshot the operand location before peeking.
- **[MED] Caret column always tracks the live lexer cursor, never the diagnosed token.** Same root; manifests on single lines too (`append_caret_context` `col` from `buf_ptr`, `src/libmcc.c:590,618`). `return undeclared_var + 1;` → mcc's caret on `+` (col ~26), clang underlines `undeclared_var` at col 12; every "expression expected before ';'" case lands one column past the token.
- **[MED] `'X' expected (got 'Y')` reports the got-token's line — missing separators blamed one line late on valid code.** `skip` (`src/mccpp.c:100-107`) fires while `tok`/`line_num` hold the *following* token; omitting `;` after `int a = 5` (line 2) is reported at line 3 under `b`, clang points at 2:14 — the classic missing-semicolon trap.
- **[LOW] No macro-expansion backtrace notes** (`expanded from macro` / `defined here`) — mcc correctly points at the macro *use* site but omits the definition pointer. Feature gap.
- **VERIFIED ROBUST (negative):** `#line N "file"` (+ macro-expanded line arg) sets line+filename for diagnostics AND `__LINE__`/`__FILE__`; `__LINE__` across line-continuations; multi-line comment / line-continuation line counting; CRLF (no drift); include-stack "In file included from" chain + the `-1` adjustment reporting the `#include` line; tab handling in the caret indent. `[NEW]`. **TASK: T-mac-30048.**

### <a id="r9-run-loader"></a>`-run` in-memory loader / page protection (reloc + TLS + external calls ROBUST)
- **[MED] `.rodata` is never protected read-only under `-run` — observable AOT divergence + const data writable for the run.** The protect loop (`src/mccrun.c:758-763`) only ever protects the exec class (`f==0`); for the rodata class (`k=1`) the guard always `continue`s. Reproduced (mac arm64): a program casting away `const` and storing into a `const` table prints `10 999` rc=0 under `mcc -run` but SIGSEGVs the AOT binary (rc 138) — `-run` silently masks a UB class that crashes real builds.
- **[MED] Linux `-run` maps executable code RWX (W^X hole in the loader path).** On non-hardened Linux `host_runmem_dual()` probes RWX successfully → non-dual path → `host_runmem_alloc` returns plain malloc and the protect loop computes `f=HOST_PROT_RWX` for the exec class (`src/mccrun.c:761-762`, `mcchost.c:1656-1658`); code stays RWX for the whole run. Distinct file/path from the KNOWN embed-JIT `#jit-rwx-mapjit` (`mccjit_embed.c`) — this is the `-run` loader itself. `[KNOWN]`-adjacent hardening class.
- **[LOW→MED latent] Dual-map mode performs no I-cache flush of the executable alias.** The exec-protect step (the only caller of `host_runmem_protect`→`host_icache_flush`) is skipped when dual-mapped (`src/mccrun.c:756-757`), and dual `host_runmem_alloc` never flushes (`mcchost.c:1649-1655`) — arm64 I/D coherency hazard; latent because default Darwin-arm64 takes the non-dual RX-protect+flush path.
- **[LOW] Inter-section alignment padding left uninitialized on the non-dual malloc path** (`src/mccrun.c:749` gap not cleared; malloc memory) — minor info-exposure (BSS itself is correctly memset).
- **VERIFIED ROBUST (negative):** reloc-before-protect ordering (layout/copy/protect phased); external libc calls, cross-section function pointers, common/`.bss` (1 MB), `-lm`, string literals — byte-identical to AOT; no 32-bit PC-rel overflow (GOT/PLT + arm64 veneers keep displacements in range); `_Thread_local` + 4 KB `.tdata` + worker-thread seeding correct on Mach-O. `[NEW]`. **TASK: T-mac-30049.**

### <a id="r9-type-compat"></a>Type equality / compatibility / qualifiers
- **[MED] `_Atomic` is dropped as a top-level qualifier in parameter/unqualified compatibility → silent-accept of conflicting function types.** `VT_QUALIFY` includes `VT_ATOMIC_BIT` (`src/mcc.h:1213`); `compare_types(...,unqualified=1)` strips the whole mask (`src/mccgen.c:4481-4482`), and `is_compatible_param`→`is_compatible_unqualified_types` flows through it. `void f(_Atomic int); void f(int);` is accepted rc=0 (clang: conflicting types). The unqualified compare was deliberately chosen to ignore top-level `const`/`volatile` params (C11 6.7.6.3p15, DETAILS:10328) but `_Atomic` — a distinct type, not merely a qualifier — was swept along. File-scope `_Atomic int x; int x;` is correctly rejected (qualified path), so this is specifically the parameter surface.
- **[LOW, latent] Enum→underlying substitution in `compare_types` discards the enum operand's own qualifiers** (`src/mccgen.c:4452,4454` replace a `const`/`volatile` enum lvalue-type with the bare underlying `int` before comparison). Not observable today (`verify_assign_cast`/redefinition do independent raw qualifier checks), but any future caller trusting `compare_types` alone for a qualified enum vs its integer type would conflate them.
- **VERIFIED ROBUST (negative):** const-discard warning; the `const char**`/`char**` nested-qualifier hazard (deepqual walk); `?:` composite pointer qualifier UNION (C11 6.5.15p6); function compat (arity/varargs/CC/K&R-merge/`void`-params/deep-const-param); tag compat by ref identity; array compat (tentative completion, size mismatch, VLA/fixed); `_BitInt` width distinctness; `restrict` (stored in storage_class, never compared — matches clang). Note: mcc escalates `-Wincompatible-pointer-types` + nested qualifier-discard to hard errors by default (GCC-14-like), applied uniformly — not a defect. `[NEW]`. **TASK: T-mac-30050.**

### <a id="r9-i386-frame-align"></a>Register allocator / spill / frame — i386 call-site alignment + `get_reg` fail-safe (primary targets ROBUST)
Spill/reload, callee-saved save/restore symmetry, frame/`loc` layout, red-zone, and spill-slot allocation are ROBUST on arm64 & x86_64-SysV (differential vs clang at -O0..-O3, incl. sub-int sign/zero-extension, `double`/`long double`/`__int128` spills across calls, sret, `-fpromote-locals` symmetry with xmm-promotion disabled under `has_call`). Two findings:
- **[MED] i386 stack frame + call sites maintain only 4-byte alignment, violating the 16-byte SysV/Darwin i386 call-site ABI.** `gfunc_prolog` rounds the frame `(-loc + 3) & -4` (`src/arch/i386/i386-gen.c:826`) and `gfunc_call` cleans up with `gadd_sp` with no 16- (or 8-) byte alignment of the outgoing frame, unconditionally incl. non-leaf functions — contrast arm32 (8 under EABI, `arm-gen.c:1406`), x86_64/arm64 (16). A modern-Linux/Darwin i386 non-leaf calling into SSE-aligned code can fault. Partly known-class (`MCC_MAX_ALIGN`=8 i386 cap, Q-lin-10005) but the frame/call-site alignment itself is untracked. i386-only, secondary arch (not natively verifiable on mac).
- **[LOW, latent] `get_reg` returns `-1` on register-class exhaustion; all 11 call sites use it unchecked** (`src/mccgen.c:2140` → `load(-1,…)`/`reg_classes[-1]`). Effectively unreachable on primary targets (pinning via `-fpromote-locals` only pins callee-saved regs, never an allocatable `RC_INT`/`RC_FLOAT` class), but a `mcc_error("no register")` would fail safe if a future arch/opt pins an allocatable class. `[NEW]`. **TASK: T-mac-30051.**

### <a id="r9-fp-robust"></a>Floating-point scalar codegen & conversions — VERIFIED ROBUST (negative result)
Differential vs clang on arm64 across every boundary value (2^63, 2^64-1, near-2^64→u64, 3e9→u32, u32→double, NaN relational/equality, -0.0→bool, LLONG_MIN→double): all matched. `unsigned long long → float/double/long double` and `float/double/long double → unsigned long long` route to `__floatundi*`/`__fixuns*` helpers (x86_64/i386/arm32) or native `ucvtf`/`fcvtzu` (arm64) / `fcvt.*.lu` (riscv64) — no signed-conversion-above-2^63 bug on any arch; `float→unsigned int` in [2^31,2^32) via 64-bit-convert+truncate; NaN compare results correct on all five arches; `float→bool` NaN/-0.0 correct; negation via sign-bit flip preserves -0.0; float→int is round-toward-zero, int→float round-nearest. One LOW only: **arm64 uses quiet `FCMP` for the relational compares `< <= > >=`, where C requires the signaling `FCMPE`** (`src/arch/arm64/arm64-gen.c:2674-2693`) — so `FE_INVALID` is not raised on a quiet-NaN relational under `FENV_ACCESS ON` (the boolean *result* is correct; x86_64/i386/riscv64 get the signaling/quiet split right). Captured, not taskified.

### <a id="r9-section-layout"></a>Section / segment output layout — VERIFIED ROBUST (negative result)
Over-aligned `_Alignas(64/128/8192)` globals + `_Alignas(32)` const all landed correctly aligned at runtime (linked Mach-O executable); segment file/vaddr page-congruence (ELF + Mach-O), W^X (`sort_sections` splits `PT_LOAD` on flag change; Mach-O `__TEXT`=RX/`__DATA`=RW; no RWX loadable segment), NOBITS-last-in-segment, deterministic zero gap-padding, `PT_GNU_STACK` non-exec, PE sub-section alignment, chained-fixups page bounds — all correct. Three LOW items only: (1) Mach-O records `sec->align` clamped to 12/4096 for over-aligned sections (`src/objfmt/mccmacho.c:2057`) — cosmetic, the placement loop uses the *unclamped* `sh_addralign` so runtime alignment is honored (would matter only if mcc emitted relinkable Mach-O objects, which it does not today); (2) Mach-O zerofill sections get a stale nonzero file `offset` (`:2063`) — dyld ignores it, diverges from Apple `ld`, can confuse lldb; (3) ELF file-offset/size tracked in 32-bit `int` (`src/objfmt/mccelf.c:2457,2752`) — silent truncation/corruption for a single >2 GB output (extreme, inherited from tcc). Captured, not taskified.


## Round 10 (2026-08-18) — semantic warning analyses, `_Alignof`/VLA evaluation, GNU statement-expressions, security instrumentation (stack-protector + UBSan), preprocessor secondary features, bounds checker

Six parallel subsystem audits (mac-arm64), fresh relative to rounds 1-9. Most findings reproduced against a rebuilt `mcc` and diffed vs clang. Every subsystem yielded at least one task-worthy defect. Tasks minted: **T-mac-30052..30057**.

### <a id="r10-wformat"></a>Semantic warning analyses
- **[HIGH] `__attribute__((format(printf/scanf,...)))` is completely ignored — no `-Wformat` on any user function.** The `format` attribute is parsed then `goto skip_param` and its (fmt-index, first-vararg-index) discarded (`src/mccgen.c:6542-6544`); `-Wformat` is driven ONLY by a hardcoded 8-name `strcmp` table (`format_func_spec`, `:10724-10748`; matched `:14385-14398`). A user `void mylog(const char*, ...) __attribute__((format(printf,1,2)))` gets zero warnings (clang flags the `double`-for-`%d`) — the dominant real-world use (custom logging wrappers) is a silent false-negative; conversely a user function coincidentally named `printf` is spuriously checked, and `vprintf`/`asprintf`/`syslog` never are.
- **[MED] printf length modifiers are stripped, so all integer-width mismatches are missed.** `%d` with a `long`, `%ld` with an `int`, `%zu`, `%lld` never warn (`src/mccgen.c:10827-10828` skips `h l L j z t`; `format_arg_class` `:10770` lumps `VT_BOOL..VT_LLONG` into one class) — exactly the LP64/portability bugs `-Wformat` exists to catch.
- **[MED] Fall-off-end "function might return no value" is not wired to `warn_return_type` — unsuppressable AND un-upgradable.** `check_func_return` uses plain `mcc_warning(...)` (`src/mccgen.c:15062`) not `mcc_warning_c(warn_return_type)` (unlike the return-statement siblings `:16064,16070`), so `-Wno-return-type` can't silence it and `-Werror=return-type` can't promote it. Same ungated pattern at `:16052`. One-line fix.
- **[MED] `-Wparentheses` only covers `if/while (x = y)` — no operator-precedence cases.** All four `warn_parentheses` sites (`src/mccgen.c:15838,15988,16132,16178`) fire off assignment-as-truth; there is no `a & b == c` / `a << b + c` / `a || b && c` precedence-confusion check (clang warns).
- **[LOW-MED] `-Wsign-compare` suppressed by ANY constant operand, even one that doesn't fit** (`src/mccgen.c:4874-4878`, `ac || bc`) — `int i; i < 2147483648u` silent (clang warns). **[LOW] Positional-arg printf `%1$d` produces a spurious "too many arguments"** (`:10787,10858,10891`) — false positive on valid code.
- **VERIFIED ROBUST (negative):** `%%`; the 8 known libc names' class-mismatch + arg-count + scanf pointer-target checks; `-Wuninitialized` (flow-insensitive but not false-positive-prone); shift-count-negative/overflow wired; `-Wunused-value`/`-Wint-conversion` live. `[NEW]`. **TASK: T-mac-30052.**

### <a id="r10-alignof-vla"></a>`_Alignof(VLA)` — wrong value + spurious side effect
- **[MED] `_Alignof(VLA)`/`__alignof__(VLA)` returns wrong alignment (always `MCC_PTR_SIZE`=8).** A VLA type is `VT_PTR | VT_VLA` without `VT_ARRAY` (`src/mccgen.c:9759`), so `type_size` (`:5705-5714`) skips the array arm and returns the plain-pointer `MCC_PTR_SIZE`; `_Alignof` reads it (`:13193`). `_Alignof(char[n])`=8 (should be 1), `_Alignof(int[n])`=8 (should be 4) — differential-confirmed. Should recurse into the element type's alignment.
- **[MED] `_Alignof(int[n++])` evaluates the VLA size expression (spurious side effect).** `_Alignof` parses via `expr_type` (not `expr_type_vm`), which doesn't set `vm_type_probe`, so `post_type`'s bound handler (`:9696-9698`) resets `nocode_wanted` and `gexpr()`s the size — `n` is bumped (clang leaves it). `_Alignof` must never evaluate its operand (contrast `sizeof`, correctly routed through `expr_type_vm`). Cheap to fix with (1) together.
- **VERIFIED ROBUST (negative, differential):** `sizeof` non-VLA doesn't evaluate its operand; `sizeof(VLA)`/`sizeof(VLA-expr)`/`typeof(side-effect)` evaluate the size exactly once and the operand never; `_Static_assert` (false→message+error, non-constant rejected, C23 single-arg); VLA decl/param bound eval order+count; compound-literal lifetime (file-scope static, loop-body reused). `[NEW]`. **TASK: T-mac-30053.**

### <a id="r10-stmt-expr"></a>GNU statement-expressions `({...})` — result not normalized (accept-invalid)
Both findings share one root: `unary`'s `'{'` arm (`src/mccgen.c:13037-13049`) leaves `vtop` exactly as the last statement left it, never normalizing to an rvalue of the right type.
- **[MED] Value/type leaks when the last statement is not an expression (incl. a nested `{}` block).** The result is seeded `VT_VOID` (`:13045`) and only a direct *expression* statement replaces it (`:16399`); non-expression statements (`if`/`for`/decl/compound) never reset it, and `STMT_EXPR` is OR'd into every nested compound (`:16038`), so an earlier expression statement — or the last expression *inside* a nested `{}` — leaks out as the whole `({...})` value+type. `int r = ({ f(100); { int z=5; z++; } });` → mcc `r=5` (leaked); clang errors "incompatible type 'void'". Masks errors and diverges `__auto_type`/`_Generic`/`sizeof`.
- **[MED] The statement-expression result is treated as an lvalue (no lvalue→rvalue conversion).** `({ x; }) = 99;` compiles and stores to `x`; `&({ y; })` takes `x`'s address — clang rejects both ("not assignable" / "cannot take address of an rvalue"). A stmt-expr result is never an lvalue.
- **VERIFIED ROBUST (negative, byte-matched clang):** `a ?: b` common operand evaluated exactly once across all operand kinds; nested functions cleanly refused (not miscompiled); `__label__` `goto` from nested block / stmt-expr; case-range value side (reversed → empty-range warning); comma/`&&`/`||` in constant contexts; VLA/struct/`return`-out/empty stmt-expr. `[NEW]`. **TASK: T-mac-30054.**

### <a id="r10-ubsan-divrem"></a>Security instrumentation — UBSan divrem-overflow gap (canary + overflow traps ROBUST)
- **[HIGH] arm64 `-fsanitize=undefined` does NOT trap signed div/rem overflow (`INT_MIN / -1`) — silently yields a wrong value.** Both `/`,`%`,`TOK_PDIV` emit only `arm64_ubsan_trap_if_zero` (CBZ on divisor), no `(a==INT_MIN && b==-1)` check (`src/arch/arm64/arm64-gen.c:2254-2296`); arm64 `sdiv` doesn't fault, so it returns `INT_MIN` (`%`→0), exit 0, no diagnostic. The `divrem_overflow` runtime handler (`runtime/lib/mccubsan.c:109`) is compiled but unreachable. UB uncaught AND a silently-incorrect result.
- **[MED] x86_64 has the same `INT_MIN/-1` gap; degrades to raw SIGFPE, and `-fsanitize-recover=divrem` can't recover.** Only `test fr,fr; jne` before `idiv` (`src/arch/x86_64/x86_64-gen.c:2470-2473`); `#DE`→SIGFPE instead of `__ubsan_handle_divrem_overflow_minimal`.
- **[LOW] `-fstack-protector`/`-strong`/`-all` all collapse to instrument-every-function** (`src/libmcc.c:3142-3144` sets `stack_protector=1` for all three; no `-strong` char-array heuristic) — over-protection (safe, perf cost), adjacent to the KNOWN help-text T-mac-30027. **[LOW] Shift-count UBSan check misses out-of-range counts whose low bits are in range** (variable `<<0x100000005LL`→32 no trap; `arm64-gen.c:2158`, `x86_64-gen.c:2445` compare only 32-bit) + constant out-of-range shift emits no check (overlaps the KNOWN fold-masking note).
- **VERIFIED ROBUST (negative, empirical):** canary placement correct (stored at `[x29,#-8]` above the `char buf` at `x29-24` and below the saved return address; slot allocated before body locals; checked on all return paths, `arm64-gen.c:1437-1451`); signed add/sub/mul overflow traps correct and gated on signedness (no false trap on unsigned); check ordering (overflow after, div-zero/shift before); div-by-zero + variable-shift≥width + null-deref all fire. No `-fstack-clash-protection` support (absent, not a regression). `[NEW]`. **TASK: T-mac-30055.**

### <a id="r10-pp-secondary"></a>Preprocessor secondary features (`#include_next`/`_Pragma`/`push_macro`/`__COUNTER__` ROBUST)
- **[MED] `#pragma once` deduplicates by normalized *path*, not device+inode — same file via a symlink/hardlink is included twice.** `search_cached_include` (`src/mccpp.c:2562-2597`) keys on a textually-normalized `true_filename` (`host_path_normalize`, `libmcc.c:783`) with no `realpath`/`stat`/inode; a header reached via `a/o.h` and a symlink `link_o.h` emitted its body twice (gcc/clang: once) → redefinition errors. Common with symlinked `/usr/include`/homebrew/monorepo layouts or two overlapping `-I` roots; ordinary `#ifndef` guards are unaffected.
- **[LOW-MED] `__TIMESTAMP__` is entirely unimplemented — passes through as a bare identifier** (no token in `mcctok.h`, no macro registered, no handler) → a hard compile error in any string context; gcc/clang define it.
- **[LOW] `#error`/`#warning` message keeps comment text verbatim** (`src/mccpp.c:3077-3092` copies raw chars, no phase-3 comment elision; >1023 chars truncate silently) + **`#include_next` from the primary source file emits no GNU warning** (`:1489`). Cosmetic/minor.
- **VERIFIED ROBUST (negative, live-tested):** `#include_next` resumes past the found directory; computed `#include MAC` (`<>`/`""`/paste); `push_macro`/`pop_macro` (defined/undefined round-trip, empty/double pop, function-like); `__COUNTER__` (monotonic, not in `#if 0`, `##` non-expansion); `_Pragma` destringize + honored-when-macro-produced + wide; `__INCLUDE_LEVEL__`/`__BASE_FILE__`/`__DATE__`/`__TIME__` (+ `SOURCE_DATE_EPOCH`); `#assert`/`#unassert`; `defined` produced by expansion. `[NEW]`. **TASK: T-mac-30056.**

### <a id="r10-bcheck-coverage"></a>Bounds checker (`-b`) — coverage gap (feature BUILT & WORKING)
The `-b`/`MCC_CONFIG_BCHECK` feature is built, wired, and detects OOB correctly (verified: heap/global/local/cast/union all caught, one-past-end legal, `-O2 -b` keeps checks, region table thread-safe). Two gaps:
- **[MED] libc string/mem writers outside the `__BOTH` redirect list escape bounds checking (false negative).** `mempcpy`/`stpcpy`/`stpncpy`/`bcopy`/`bzero`/`memccpy`/`index`/`rindex`/`strstr`/… are declared via `__BUILTIN` (plain name) not `__BOTH`/`__BOUND` (`runtime/include/mccdefs.h:715-728`), so under `-b` they get no `__bound_*` wrapper (while `memcpy`/`strcpy`/… do). Reproduced: `stpcpy(a4,"0123456789")` ran clean; `bzero(a4,64)` → raw SIGSEGV not a clean region error; the fortify `_chk` variants forward to the unchecked builtins too. Related to but distinct from the T-mac-30047 *link-failure* of the same glibc-only builtins (this is the *bounds-coverage* axis).
- **[LOW] No positive-detection regression test** — `mcctest-bcheck` is only a differential (catches false-positives/miscompiles under `-b`), never asserts an OOB access is *caught*; the `tests/exec/bounds/*` corpus is trivial (no overflow), so a future change silently dropping a check would pass CI.
- **VERIFIED ROBUST (negative):** deref/index detection (heap/global/local/cast/union) with correct region bounds; no false positive on one-past-end pointer formation+iteration; `-O2 -b` parity; coarse-but-correct thread-safe region table; `alloca`/`setjmp` handled. `[NEW]`. **TASK: T-mac-30057.**


## Round 11 (2026-08-18) — integrated-assembler directives, make dependency-files, enum C23 fixed-underlying-type, debug `.debug_line`/variable-locations, printf/scanf runtime, compound-assignment codegen

Six parallel subsystem audits (mac-arm64), fresh relative to rounds 1-10. Most findings reproduced against a rebuilt `mcc` and diffed vs clang/gcc. Two subsystems returned strong negative results (printf/scanf runtime; compound-assignment). Tasks minted: **T-mac-30058..30061**.

### <a id="r11-asm-directives"></a>Integrated assembler — directive coverage (can't consume GCC/clang `-S` output)
Expression eval, string/integer directives, `.set`/`.globl`/`.rept`/`.org`/section directives are ROBUST; the gaps are missing directives common in toolchain-emitted asm.
- **[HIGH] `.comm`/`.lcomm` completely unimplemented** — no token (`src/mcctok.h:570-614`), so they fall through to the instruction encoder → `error: ARM64 instruction '.comm' not implemented` (`src/mccasm.c:1136`). `.comm` is the standard GCC emission for every tentative/uninitialized global (`int x;`), so hand-written or GCC-generated `.s` can't be assembled.
- **[HIGH] `.zero` unimplemented** (only `.skip`/`.space` have tokens) — `clang --target=aarch64 -O2 -S` emits `.zero N` for a BSS array, so clang `-S` output fails to reassemble (mcc's own `-S` uses `.skip`, so self-round-trip survives).
- **[MED-HIGH] `.equ`/`.equiv` unimplemented** (only `.set` has a token; the machinery is reachable via `.set FOO,42` or infix `FOO = 42`, `src/mccasm.c:643,1150`) — purely a missing directive-name alias, but `.equ` is ubiquitous in toolchain asm.
- **[MED-HIGH] `.align`/`.balign`/`.p2align` ignore the max-skip (3rd) operand and abort on GCC's `.p2align N,,M` form** — the align case reads `n` + at most one `,fill` (`src/mccasm.c:477-480`); GCC's `.p2align 4,,15` (empty fill, max-skip 15) hits the second comma with no expression → `error: bad expression syntax [,]`.
- **[MED] `.if`/`.else`/`.endif`/`.ifdef`/`.ifndef` entirely absent** (`.rept`/`.endr` IS implemented). **[MED] `.local` unimplemented** (companion to `.comm` for file-scope statics). **[LOW] every unsupported directive is misreported as a missing *instruction*** (routed to `asm_opcode` `:1153` not the directive `default:` `:948`). **[LOW] `.size` warns "ignoring" but actually applies `st_size`** (`:756` false warning, then `:761-763` honors it).
- Adjacent context (out of scope, Round 6): on macOS builds `MCC_EH_FRAME` is compiled out so `.cfi_*` also fall through — blocks assembling real `-S` output before even reaching `.zero`/`.comm`. `[NEW]`. **TASK: T-mac-30058.**

### <a id="r11-depfile"></a>Make dependency-file generation
- **[HIGH] `$` and `#` in prerequisite paths emitted unescaped → silently corrupts the Makefile fragment.** `escape_target_dep` (`src/mcctools.c:464-474`) only escapes spaces (`\ `), never `$`→`$$` nor `#`→`\#`. A header/build-dir path with `#` makes `make` treat the rest of the prereq line as a comment (truncated deps → missed rebuilds / "No rule to make target"); `$` triggers variable expansion. Same buggy strings reused for `-MP` phony stanzas (`:515`).
- **[MED] `-MG` unrecognized → hard error.** The option table (`src/libmcc.c:2338-2345`) has M/MM/MD/MMD/MF/MP/MT/MQ but no `-MG`; `mcc -MG …` → `invalid option`. gcc/clang accept it (tolerate a not-yet-generated header). (`--help` also omits MT/MQ/MG.)
- **[LOW] Depfile not written when compilation fails** (`src/mcc.c:1842`, `if (!ret && gen_deps)`) — gcc/clang write it during preprocessing regardless of a later codegen error; not a `make` hazard (the `.o` is also absent) but a behavior difference.
- **VERIFIED ROBUST (negative):** sys-vs-user header distinction (`-M` incl / `-MM` excl, `mccpp.c:1581`); default target = dir-stripped basename `.o` (matches gcc), overridden by `-o`/`-MT`/`-MQ`; source first + dedup + stable order; space escaping; line-wrapping; `-MP` phony-per-header (skips primary source); `-MT`/`-MQ` accumulate; `-MD`/`-MMD`→`<obj>.d` (not stdout), respect `-MF`, `-o dir/foo.o`→`dir/foo.d`. `[NEW]`. **TASK: T-mac-30059.**

### <a id="r11-enum-c23"></a>Enum C23 fixed-underlying-type diagnostics (enum core exceptionally ROBUST)
Underlying-type selection, sizeof, signedness, `-fshort-enums`/packed, C23 fixed-type sizing, enum↔int conversions all byte-identical to gcc+clang. Three C23 constraint/diagnostic gaps (no miscompiles):
- **[MED] Rejects a negative enumerator in an unsigned fixed underlying type that gcc AND clang accept.** `enum E : unsigned char { A = -1 };` → mcc hard-errors "out of range" (`src/mccgen.c:7370-7372`); gcc/clang convert `-1`→`255` (even under `-pedantic-errors`). The `in_range(ll, t.t)` gate tests the signed value against the unsigned range instead of representability-after-conversion — rejects-valid / over-strict, breaks real headers.
- **[LOW/MED] Conflicting fixed underlying type on redeclaration silently reconciled** — `enum E : int; enum E : long { A };` → mcc adopts `long` (sizeof 8) silently (`src/mccgen.c:7304-7311` checks only `IS_ENUM`, never compares underlying types); gcc/clang error. Latent cross-TU ABI hazard. **[LOW] Nonfixed-then-fixed redeclaration silently accepted** (`enum E; enum E : int;`), same root.
- Note: the KNOWN DETAILS `c23-enum-1` item (`DETAILS.md:17830`, pre-C23 out-of-int enumerator picking the wrong underlying type) appears RESOLVED (now compiles, pedwarns, sizeof 8, correct value) — recommend refreshing/closing that note. `[NEW]`. **TASK: T-mac-30060.**

### <a id="r11-debug-line"></a>Debug `.debug_line` / variable locations (mechanics + locations ROBUST)
`-O0` line program correct (strictly-increasing addresses↔lines, `prologue_end`/`epilogue_begin`, single `DW_LNE_end_sequence`, per-arch `min_inst_length`, special-opcode constants); `DW_AT_frame_base` + every `DW_OP_fbreg` offset matches the machine code (verified vs disassembly); multi-file `DW_LNS_set_file` correct. Findings:
- **[LOW] Spurious duplicate line-row with a stale line number on any source-line jump >8 combined with a small pc advance.** `mcc_debug_line` (`src/mccdbg.c:2110-2118`): when the combined line+pc special opcode doesn't fit but the pc-only special opcode does, it emits that special opcode to advance PC — but a special opcode *always appends a matrix row*, so it appends a row at the target address carrying the OLD line, then the correct row is appended at the same address. Reproduced: a statement on line 2 followed by one on line 30 → two rows at `0x2c` (`line 2` then `line 30`). Harmless for range consumers (zero-length span) but a naive first-match addr2line resolves `0x2c` to line 2. Should use `DW_LNS_advance_pc` (appends no row), not a special opcode. Extremely common trigger (any statement >8 lines below the previous after comments/blank/multi-line initializers).
- **[LOW] Duplicate primary-file entry in `file_names`** (`src/mccdbg.c:1857-1871`): the primary file is inserted at both index 1 and 2 (rows reference 2) — an intentional tcc hack for DWARF≤4/DWARF-5 index-base compatibility; harmless (identical entries) but leaves a redundant unreferenced entry per CU.
- **[MED, KNOWN] At -O1+ the inliner folds a callee into the caller with no `DW_TAG_inlined_subroutine`, wrong `DW_AT_decl_line`, and attributes the caller's pc range to the callee's source lines** — the already-documented consequence of the arena carrying no per-node line info (`DETAILS.md:6475` "B7" / orphan-body note); line-program mechanics are correct, the gap is upstream (inliner↔debug-info). Not re-tasked. `[NEW]` (the two LOW items). **TASK: T-mac-30061.**

### <a id="r11-printf-runtime"></a>printf/scanf runtime — VERIFIED NEGATIVE (no bundled engine)
mcc ships NO bundled printf/scanf conversion engine — it forwards to the host/platform libc (libSystem on macOS, glibc on Linux, msvcrt via import lib on Windows) on every target, including `-run` (undefined externals resolve via `host_dlsym_process`→`dlsym(RTLD_DEFAULT,…)`, `src/mcchost.c:1457`). `runtime/lib/` has zero printf/scanf *definitions*; `runtime/*/include/stdio.h` are pure prototypes. Empirically byte-identical to a clang host build across rounding/width/precision/`%a`/scanf probes. The only format-output defects are platform-libc artifacts, fully captured by the KNOWN **T-win-50030** (msvcrt 3-digit exponent, `1.#INF`, `long double`==`double`). No mcc-side risk surface; no task. (Distinct from the T-mac-30052 `-Wformat` *checker*.)

### <a id="r11-compound-assign"></a>Compound-assignment & inc/dec codegen — VERIFIED ROBUST (negative result)
Every probe matched clang: single-evaluation of the lvalue (`vdup` copies the already-materialized lvalue `SValue`, `src/mccgen.c:14887-14896` + `:1608`, so `a[i++]+=5`, `*p++ += rhs()`, `arr[ix()].bf += rhs()`, `*pf() += rhs()` all evaluate the address once and feed both load+store); conversion applied once at lvalue width (`unsigned char c=200; c+=100`→44; `i*=2.5`→7; sign/unsigned mixing); bit-field RMW (signed/unsigned wrap, unit-spanning, `>>=`/`/=`); `volatile` (verified one-`ldr`/one-`str` in `-S`); pointer/float/`long double`/`_Float16` `++`; `_Atomic` compound incl. `_Atomic double += 2.5` via CAS. `_Atomic`-too-wide and non-assignable `?:`/comma targets error cleanly (conforming). No task.


## Cross-oracle conformance run (2026-08-18) — self-hosted mcc over gcc.c-torture/execute, judged by clang + gcc-16

User-directed differential run (not a subagent audit): a **self-hosted** mcc (`cmake-stage2/mcc` = mcc compiling mcc; its `-O0` objects are **byte-identical to stage1**, confirming self-host determinism) run over the gcc `gcc.c-torture/execute` corpus (1709 tests) in three configs, each program scored against a *foreign* oracle via `tools/xoracle.py` / `tools/jitconform.py`. Oracles: `clang` (Apple clang 21) and `gcc-16` (Homebrew GCC 16.1.0). **1601/1709 (93.7%)** entered the oracle set (108 excluded as nondeterministic / UB-sensitive / oracle-unbuildable). Task minted: **T-mac-30062**.

- **Config A — static AOT `-O0`: 1587/1601 PASS = 99.13%** vs the clang oracle; 9 DIFF_EXIT, 5 MCC_NOBUILD. Triaging every DIFF against *both* oracles (mcc vs clang vs gcc-16) shows **8 of 9 DIFFs are clang-oracle outliers** — mcc AGREES with gcc-16 (rc 0) while **Apple clang 21 aborts (rc 134)** on `alloca-1`, `bitfld-3`, `bitfld-5`, `pr32244-1`, `pr34971`, and the three `ieee/cdivchk{d,f,ld}` (complex-division) tests. So mcc is *correct* on those (incl. complex division — it matches gcc, contrary to a first read of T-mac-30044); the second oracle (gcc-16) was essential to avoid mis-attributing clang's own aborts to mcc.
- **The one real static miscompile: `pr85156`** — mcc rc=134 (abort) where BOTH gcc-16 and clang pass (rc 0). Involves `__builtin_expect(x ? y != 0 : 0, z++)` under `noipa` + an `asm volatile("":"+m"(x),"+m"(y))` barrier; the plain `__builtin_expect(0, z++)` second-arg side-effect drop is standard (all three discard it), so the trigger is a subtler ternary-condition-inside-`__builtin_expect` interaction — needs minimization. `[NEW]` miscompile.
- **5 MCC_NOBUILD — real front-end gaps** surfaced by real tests: `pr40657` — inline-asm `asm volatile("" : "=m"(x) ::)` (empty clobber after `::`) → "string constant expected" (asm-operand parse gap, adjacent to R6 T-mac-30033); `pr70460` — `&&label` (labels-as-values) in a **static initializer** → "initializer element is not constant" (gcc/clang accept the GNU extension); `980526-1` + `stdarg-4` — "unresolved reference to `_lbl1`/`_f1i`" (labels-as-values / static-function address emission gap); `string-opt-18` — "unresolved reference to `_mempcpy`" = **direct corroboration of KNOWN T-mac-30047** (glibc-only builtin link-fail on macOS) by a real torture test.
- **Config B — embed-JIT `-O0`** (`--embed-jit`, each program run twice MCC_JIT=1/0 to isolate JIT-vs-AOT): 595/1601 (37%) baked+correct, engine routing **212 hot-swaps** + 39 kept-aot + 348 refused, **992 (62%) NOT_BAKED** (JIT declined to engage). Crucially the **DIFFER set is identical to static (the same 9), and MCC_JIT=1 agrees with MCC_JIT=0 on every baked program → the JIT introduced ZERO new miscompiles**; where the embed engine engages, its codegen matches AOT. The 62% not-baked is the known incomplete-baking surface (T-lin-10029 / T-win-50021 family).
- **Config C — run-JIT** (`-run --jit`, in-process): the engine **refuses ~99%** (1596/1601 refused, 1 hot-swap) on the native mac build — the `-run` JIT does not engage here (initially all NOT_BUILD because the self-hosted mcc needed `runmain.o`/runtime via `-B`; after providing it, the programs run but the JIT engine still declines to swap). Where it runs it agrees; no miscompiles. The non-engagement is itself the finding (distinct surface from embed).

Net: the self-hosted static compiler is 99%+ conformant against a real cross-vendor oracle, with **exactly one real miscompile (`pr85156`)** and four front-end NOBUILD gaps (one of which re-confirms T-mac-30047); the JIT surfaces add no miscompiles but the embed engine bakes only ~38% and the run engine ~0% on this mac build. See **T-mac-30062**.


## Round 12 (2026-08-18) — GPU kernel codegen, math/FP builtins, extended-asm operands, wide-int arithmetic, struct/union layout, `-E` output format

Six parallel subsystem audits (mac-arm64), fresh relative to rounds 1-11. Most findings reproduced against a rebuilt `mcc` (differential vs clang / gcc-16 / a Python bignum reference). Two subsystems returned strong negative results (struct/union layout; math/FP builtins). Tasks minted: **T-mac-30063..30066**.

### <a id="r12-bitint-cast"></a>Wide-integer arithmetic — unsigned→signed `_BitInt(N)` cast drops sign extension
- **[HIGH] Runtime `(signed _BitInt(N))` cast of an `unsigned _BitInt(N)` drops sign extension for N in (128,256)∪(256,512), N≠256/512 → wrong sign, wrong signed div/mod/compare.** `gen_bitint_cast` (`src/bitint_slice.h:425-429`) widens the source as *unsigned*-typed then calls `wide_to_bitint(duns=0)` (`:130-136`) which requests `TOK_SAR` to sign-extend bit N-1 — but `gen_wideint_op` picks arithmetic-vs-logical from the *operand's* wide-type signedness, not the opcode: `wideint_shift_helper` (`src/wideint_slice.h:101`) and the const-shift branch (`:506-509`) both use `(uns || op==TOK_SHR) ? shr : sar`, so a requested `TOK_SAR` on the unsigned-typed operand is silently demoted to logical `__mcc_i256/512_shr`. Sign extension never happens (top limbs 0 instead of all-ones); the masked value still *prints* right via a `(U)` cast but every later signed op reads it as a large positive. Reproduced at N=129,130,192,200,255,257,300,320,400: `s<0`→0 (should be 1), `s/small`, `s%small`, `s<t` all wrong. N=256/512 are correct (reducer skips the shift when N==arith-width). Deviates from the documented intent (`DETAILS.md:50553`). Fix: make `op==TOK_SAR` override `uns` in `wideint_shift_helper` + the const-shift branch, or retype the wide value signed before the SAR.
- **VERIFIED ROBUST (negative, differential vs clang ≤128 + Python bignum ≤512):** `_BitInt` `* / % + - & | ^ <<`, logical+arithmetic `>>`, signed/unsigned compare, non-mult-of-64 top-limb masking, signed div truncation + remainder sign + most-negative/−1 wrap, mixed-width `_BitInt(100)+_BitInt(200)`; directly-signed negatives (int→signed, float→signed) sign-extend correctly (the bug is *specifically* unsigned-source→signed-dest); wide→double/float correctly rounded (ties-to-even). Scope-adjacent note (not a value bug): `__int128` is compiled out on arm64/riscv64 (`MCC_HAVE_INT128` is x86_64-only, `src/mcc.h:1162`) despite AAPCS ABI support — arm64 emits "`__int128` not supported on this target". `[NEW]`. **TASK: T-mac-30063.**

### <a id="r12-gpu-metal-index"></a>GPU kernel codegen — Metal 32-bit lane-index overflow (backend divergence)
GPU compute-kernel codegen is exceptionally robust (arg marshalling, dispatch bounds, guarded div/shift op-mapping, f64 NaN semantics all verified; other candidates dedup to KNOWN — pipeline-cache-hash-collision DETAILS:7255, sub-word RMW atomicity DETAILS:22590, f64 exec-mode T-mac-30021). One new defect:
- **[MED] Metal computes the per-lane buffer index in 32-bit, so a single-buffer dispatch >8 GB wraps negative → OOB; Vulkan is structurally immune → same-source backend divergence.** `msl_main_begin`/`msl_main_end` (`src/mccgpu.h:559,566`) compute `base = gi * nlive*IN_SLOTS` with `gi = as_type<int>(gid)` (32-bit, wrapping `mcc_mul`); the Metal size gate is only `inlen > maxBufferLength` (`src/mccgpu.c:703,458`, a 64-bit value tens-of-GB on high-memory Apple Silicon). Once `inlen` exceeds 2^31 words (8 GB) the index wraps negative → `inb[]`/`outb[]` OOB on a dispatch the gate accepted. The SPIR-V index (`mccgpu.h:3327,3345`) is safe because Vulkan caps a single buffer at `maxStorageBufferRange` (uint32 ≤4 GB, `mccgpu.c:2693`). `[NEW]`. **TASK: T-mac-30064.**

### <a id="r12-e-format"></a>`-E` preprocessed output format (line-markers / `-P` / `-dM` / `-dD` / blank-resync ROBUST)
- **[MED] System-header flag `3` (and extern-C `4`) are NEVER emitted on line markers.** `pp_line` (`src/mccpp.c:5950-5955`) emits only ` 1`/` 2`/``; `file->system_header` exists but is never consulted. `mcc -E -isystem sys s.c` on a `<foo.h>` emits `# 1 "sys/foo.h" 1` where clang emits `… 1 3` → a downstream `gcc -x cpp-output` loses system-header status and fires warnings gcc would suppress.
- **[MED] `_Pragma("…")` rendering inserts a spurious blank line → +1 line drift for everything after it.** `pp_pragma_operator` (`src/mccpp.c:6135-6170`) prints `#pragma …\n` out-of-band without keeping `file->line_ref` in sync, so the next real token triggers `pp_line` with `d==1` → an extra blank line with no resync marker (`:5944`); all subsequent lines are one too low until the next `#include`/`#line`, so downstream diagnostics after any `_Pragma` point at the wrong line. An ordinary `#pragma` does NOT drift.
- **[MED] `-C`/`-CC` (keep comments) silently accepted as a no-op** (`src/libmcc.c:2353` → `MCC_OPTION_ignored`; no `keep_comments` flag; phase-3 stripping unconditional) — undocumented silent-ignore, so a build relying on `-C` gets wrong output with no signal.
- **[LOW] `-traditional` silently accepted as no-op but `-traditional-cpp` hard-errors** (inconsistent). **[LOW] `-dD`/`-dM` predefined-macro dump emits duplicates** (`__INT16_TYPE__ short` then `short int`, `__GNUC__ 4` twice) + stray blank lines.
- **VERIFIED ROBUST (negative):** line markers `# 1 "file"` + push/pop flags 1/2, post-include return line, `#line N "v.c"`, `mcc -E | gcc -x cpp-output` accepted; `-P`/`-P1`; `-dM` (builtins + user + function-like); `-dD` interspersed; blank-line resync at ≥8 (matches clang); `#`/`//` in string literals + ordinary `#pragma` passthrough. `[NEW]`. **TASK: T-mac-30065.**

### <a id="r12-asm-multialt"></a>Extended inline-asm operands — multiple-alternative constraints (matching/early-clobber/`asm goto` ROBUST)
The high-risk areas are ROBUST (runtime-verified arm64): matching-tie `"0"`, early-clobber `"=&r"`, `"+"` rw, `"memory"` reload-barrier, and `asm goto` control flow (labels `%l[name]`/`%lN`, GCC-11 outputs-on-goto, auto-volatile, `MCC_ASM_EFF_GOTO` to the optimizer) all correct. One new defect:
- **[MED] Multiple-alternative constraints (`"r,m"`, `"=r,m"`) are unimplemented — hard compile error on riscv64/arm, spurious warning + first-alt-only on arm64/x86.** `skip_constraint_modifiers` (`src/mccasm.c:1255`) strips only `= & + %`; the `,` alternative separator survives into `constraint_priority`. On riscv64 (`riscv64-asm.c:2271`) and arm (`arm-asm.c:2614`) it hits `default: mcc_error("unknown constraint")` — any alternative is a hard build failure even when the first is trivially satisfiable; arm64 (`arm64-asm.c:812`) only warns but `asm_compute_constraints` never consumes past the first alternative (`:2179`), so no arch does real alternative selection. Common in kernel/glibc asm. `[NEW]`. **TASK: T-mac-30066.**

### <a id="r12-struct-layout-robust"></a>Struct/union layout — VERIFIED ROBUST (negative result)
Layout computation (offsets/sizes/alignment) byte-identical to clang across ~40 shapes: zero-width bit-fields (incl. where clang & gcc-16 *disagree* — mcc correctly tracks its platform default clang via the pcc/ms_bitfields path), anonymous struct/union members (flattened offsets, nested 2-deep), `_Alignas`/`aligned` on a member (over-align raises offset + struct align; member `aligned` overrides `#pragma pack(1)`), empty structs (GNU size 0), FAM (`sizeof` excludes, union-with-FAM, two-FAM + not-at-end errored), bit-field straddling/container-selection, trailing padding. Only LOW: `_Alignas` *under*-alignment on a member is silently accepted (`src/mccgen.c:7526`, no ABI impact — the too-small value is ignored, natural align kept; same root family as the R8 storage-class-unchecked holes). Captured, not taskified.

### <a id="r12-math-builtins-robust"></a>Math/FP builtins — VERIFIED ROBUST (values + NaN/-0.0) with two KNOWN header gaps
Differential vs clang all byte-identical: arm64 native `gen_fma`=`FMADD` (fused single-rounding), `gen_fminmax`=`FMAXNM`/`FMINNM` (NaN-quieting; `fmax(NaN,1.0)`→1.0), `gen_fabs`/`gen_sqrt`/`gen_round` (`FRINTA`=ties-away), `gen_copysign` bit-splice preserving NaN/-0.0, `signbit(-0.0)=1`; const-folding bails on NaN / differing-sign zeros / errno-unsafe sqrt. Divergences are the KNOWN header-fidelity gaps (`DETAILS.md:14956-14986`, untasked): (a) FP-classification + `copysign` macros multiply-evaluate their arg (`isnan(*p++)` increments twice); (b) `__builtin_nan("payload")` discards the payload (both need real compiler builtins / file-scope-constant support). One LOW NEW: `__builtin_fma`/`fmin`/`fmax` native lowering is arm64/riscv-only — x86_64 falls back to libc calls (perf/completeness, correctness preserved). Captured, not taskified.


## Round 13 (2026-08-18) — constant-expression evaluator, `#pragma STDC` FP-environment, CST/LSP path, C11 `<threads.h>`, effect/purity analysis, startup/atexit/setjmp runtime

Six parallel subsystem audits (mac-arm64), fresh relative to rounds 1-12. Most findings reproduced against a rebuilt `mcc` (differential vs clang / gcc-16). Two subsystems returned strong negative results (`<threads.h>` runtime shim; effect/purity oracle). Tasks minted: **T-mac-30067..30070**.

### <a id="r13-cst-lsp"></a>CST / LSP path (`--lsp`, `src/mcccst.c`) — flat token-value symbol table, no scope stack
Symbol resolution is a flat table `cst_defoff[v]` keyed by identifier-spelling id with NO scope stack (`cst_hook_def` unconditionally overwrites, `cst_hook_use` reads the most-recent def; `src/mcccst.c:991-1019`). Consequences:
- **[HIGH] A struct-member declaration clobbers a same-named variable → go-to-def lands on the field.** `cst_hook_def` fires for member declarators too (`src/mccgen.c:9861`), sharing the one namespace: `int x; … struct S{int x;}; … return x;` resolves the later `x` to the struct field (verified `use→def[28]` the field, not `def[4]` the global). Extremely common code; silent wrong navigation.
- **[MED] Post-scope shadowing — a use OUTSIDE an inner block binds to the dead inner declaration** (last-write-wins, no pop on block exit): after `{ int x; }` closes, an outer `x` use resolves to the inner def (verified `use[51]→def[30]`).
- **[MED] lvalue identifier refs misattached to the following operator leaf** (`cst_cur_tok_off`/`cst_hook_use`, `src/mcccst.c:987,1006`; `mccgen.c:14119`): `a = b;` records the LHS ref on `=`, `s.x` on `.`; go-to-def on the identifier itself returns nothing. Same lookahead-drift *family* as R9 T-mac-30048 but a distinct locus (CST offset capture, not `error1`).
- **[MED] decl/ref ranges include leading trivia** (whitespace/newline/comment) — `cst_emit_leaf` folds leading trivia into the leaf width (`src/mcccst.c:1288-1322`); no `sym_ref` consumer applies `tok_rel`, so an LSP client highlights preceding whitespace + the identifier.
- **[MED] `--lsp` aborts on >64 nested `#if` in an included header** — `cst_build_sourcefile` opens a `CST_PPConditional` unconditionally but records depth only under `if (sp<64)` (`src/mcccst.c:1077,1089`); past 64 the open/close bookkeeping desyncs → `cst_node_close(root)` assert (`:198`) fires (malformed tree under NDEBUG).
- **[LOW] No CST/symbols emitted for a file with a compile error** (`src/libmcc.c:876-894`, `cst_capture_end` skipped on the error longjmp) — the mid-edit state an editor constantly hits yields zero symbols (no crash). **[LOW] `tests/cst/symref-shadow.cmake` asserts a nonexistent "scope-aware resolver"** — contradictory guidance vs the actual last-write-wins code.
- **VERIFIED ROBUST (negative):** malformed/incomplete input doesn't crash; snapshot save/load round-trip + revalidate; content-addressed include store hash-collision self-check; RHS/plain-read uses resolve correctly. `[NEW]`. **TASK: T-mac-30067.**

### <a id="r13-run-atexit"></a>`-run`-mode crt (`runmain.c`) — atexit ordering / late-registration / cap
AOT startup/`atexit`/`exit`/`setjmp` forward to the host libc (ROBUST, byte-matched clang); `__builtin_setjmp` saves the full callee-saved set per arch (ROBUST). The bundled `-run`/JIT crt (`runtime/lib/runmain.c`, heavily exercised by the suite) has three defects:
- **[MED] `-run` runs `__attribute__((destructor))` / `.fini_array` BEFORE `atexit` handlers — reversed vs standard/glibc.** `exit` (`runmain.c:60-61`) and `_runmain` (`:74-75`) call `run_dtors()` then `__run_on_exit()`; glibc registers the fini runner as the earliest `atexit` so user handlers (LIFO) run first, destructors last. Reproduced: `mcc -run` prints `dtor, atexit3..1`; clang + mcc-AOT print `atexit3..1, dtor`.
- **[MED] `-run` drops an `atexit` handler registered from within another exit handler** (C11 §7.22.4.4 violation). `__run_on_exit` snapshots `int n = __rt_nr_exit` and never revisits handlers a running handler adds (`runmain.c:29-33`). Reproduced: late-registered handler doesn't run.
- **[LOW/MED] `-run` `atexit`/`on_exit` fixed cap of 32; extras silently fail** (`rt_exitfunc[32]`, `runmain.c:25,37`) — meets the C minimum but far below host libc; `-fsanitize=undefined` consumes one slot leaving 31. Reproduced: 40 registrations → 8 dropped.
- **VERIFIED ROBUST:** AOT path (host libc); `__builtin_setjmp`/`longjmp` full register save + `longjmp(b,0)→1` + volatile-local preserved; `-run` `exit`/`_Exit`/`abort`/ctors-before-main/`.init_array` forward + `.fini_array` reverse (base order). `[NEW]`. **TASK: T-mac-30068.**

### <a id="r13-stdc-fp"></a>`#pragma STDC` FP-environment & contraction
- **[MED] AST-level builtin-math folding (`sqrt`/`round`/`floor`/`ceil`/`trunc`/`rint`/`fmin`/`fmax`) ignores `#pragma STDC FENV_ACCESS ON`.** The fold gate (`src/mccast.c:7781-7810`, flags `:2524-2534`) is driven only by `-fbfold-*`, with no `stdc_fenv_access` check — so `#pragma STDC FENV_ACCESS ON; __builtin_sqrt(2.0)` folds to a round-to-nearest constant that diverges by 1 ulp under `fesetround(FE_DOWNWARD)`. Asymmetric: the gen-level `+ - * /` fold (`mccgen.c:4047`) IS correctly fenv-guarded (and `-frounding-math` maps to the same field). The existing `fenv_access_fold.c` test only exercises `/`.
- **[LOW] `-ffp-contract=off|on|fast` and `-funsafe-math-optimizations` rejected as unsupported** (`src/libmcc.c:3226,2446`) — the compiler's own reference harness passes `-ffp-contract=off` (silently warning-ignored). **[LOW] `#pragma STDC FP_CONTRACT` parsed + scope-saved but its accessor `stdc_fp_contract()` (`mcc.h:1097`) has zero call sites** — harmless only because mcc never contracts `a*b+c`→FMA (KNOWN `DETAILS.md:31156`). **[LOW] `-ffast-math` under-implements** — doesn't define `__FAST_MATH__` / leaves `__FINITE_MATH_ONLY__ 0`, so glibc `<math.h>` fast paths never engage.
- **VERIFIED ROBUST:** gen-level `+-*/` fold correctly inhibited under FENV_ACCESS ON + `-frounding-math`; `cx_limited_range` honored at complex *codegen* (`mccgen.c:8700`; the fold path is the KNOWN T-mac-30044); pragma block/function scope save/restore wired. `[NEW]`. **TASK: T-mac-30069.**

### <a id="r13-ice-array-cap"></a>ICE evaluator — 32-bit array-size cap (evaluator otherwise ROBUST)
Folded values, overflow wrap, shift signedness, unsigned-in-ICE, accept/reject discrimination, float-in-ICE pedantic guard, bit-field widths, and dead-ternary-arm all match clang+gcc-16. Two findings:
- **[MED] Array-declaration size is capped at 32 bits — valid large arrays rejected.** `src/mccgen.c:9694` reads a non-VLA dimension via `expr_const()` which hard-errors "constant exceeds 32 bit" (`:14987-14994`), and the result is stored into `int n` (`:9702`). So `char a[5000000000]`/`char a[0x100000001]` are rejected and `char a[2147483648]` (2^31 → negative `int`) errors "invalid array size"; clang + gcc-16 accept all three. Reject-valid, NOT a miscompile (the `(unsigned)c==wc` guard + `n<0` check prevent any wrong-size array). Same 32-bit cap governs designated-init indices (`:16698`). Fix needs `expr_const64` + widening the size plumbing.
- **[LOW] Out-of-range / truncating `case` labels emit no diagnostic** (`case 257:` on `char`, `case (1LL<<40):` on `int`) — silently truncated with no warning even under `-Wall -Wextra`; clang (`-Wswitch`) + gcc-16 warn. Runtime correct (label + condition truncated consistently). `[NEW]`. **TASK: T-mac-30070.**

### <a id="r13-threads-robust"></a>C11 `<threads.h>` runtime shim — VERIFIED ROBUST (negative result)
The pthread-mapped shim (`runtime/include/threads.h`) is conformant on its supported platforms (live macOS run): return-code mapping (`thrd_success`/`busy`/`timedout`), `int`↔`void*` thread-fn adaptation (trampoline, `thrd_join *res` correct), recursive mutex, `mtx_timedlock` returns `thrd_timedout` blocking the full deadline (absolute `CLOCK_REALTIME` — not a relative-timeout bug), `mtx_trylock`→`thrd_busy`, `call_once` once, `tss_get`-before-set→NULL, TSS destructor fires once at thread exit (`TSS_DTOR_ITERATIONS=4`), `cnd_timedwait` abs-time, `thrd_sleep` relative + `-1` on EINTR. KNOWN: no Windows backend (unconditional `<pthread.h>`, T-lin-10397); `mtx_timedlock` 1ms polling spin (T-lin-10001). Two LOW NEW captured (not taskified): `thrd_create` never returns `thrd_nomem` for a kernel thread-limit failure (maps everything to `thrd_error`, `threads.h:70`); the compile-time classifier op-table omits `mtx_timedlock` while listing `cnd_timedwait` (`src/mccthread.h:34`, cosmetic — census only).

### <a id="r13-effect-robust"></a>Effect / purity oracle — VERIFIED ROBUST (negative result)
`src/mcceffect.h` is the RIR record/replay effect-LOG (not the compile-time oracle); the actual purity decisions (`ast_expr_pure`/`ast_ident_pure_compute` in `mccast.c`, `rir_addr_pure`/`rir_effectful` in `mccrir.c`) are conservative and correct — runtime-verified vs clang at -O0..-O3: volatile load/store/bit-field preserved when unused, `asm volatile("":::"memory")` barrier honored, `__attribute__((pure))`/`((const))` functions treated as effectful (mcc ignores them — conservative), side-effecting call in an lvalue address blocks the fast path, dead store through an escaping pointer preserved, atomic RMW with discarded result executed, `memcpy` not pure, `rir_addr_pure` excludes volatile + divide/modulo. Two LOW latent design notes captured (not taskified, no constructible miscompile): `ast_expr_pure` (`mccast.c:7346`) is a blacklist (impure only for `AST_Invoke`/`VT_VOLATILE`) not a whitelist like its sibling — safe today because all 5 consumers have independent structural guards, but a future consumer without one could drop a `Store`/RMW side effect; `rir_effectful`'s recursive atomic detection (`mccrir.c:1420`) is `#ifdef MCC_IR_HAVE_X86_PRIMS` (x86_64-only) so a wrapper-nested atomic RMW relies on arm64's lowering avoiding the value-drop path (empirically safe on arm64, riscv64 unverified).


## Round 14 (2026-08-18) — `-S` assembly output, less-common attributes, `_FORTIFY_SOURCE`/object_size, lexer edge cases, wide-int ABI passing, RIR replay faithfulness

Six parallel subsystem audits (mac-arm64), fresh relative to rounds 1-13. Most findings reproduced against a rebuilt `mcc` (differential vs clang / gcc-16; several via `-S`→reassemble round-trips). Two subsystems returned strong negative results (RIR capture/replay; wide-int ABI passing). Tasks minted: **T-mac-30071..30074**.

### <a id="r14-s-output"></a>`-S` textual assembly emitter (disassembles the emitted object back to GAS text)
`mcc -S` fully assembles to an in-memory ELF object then disassembles it to ELF-flavored GAS text (`src/mccdis.c:559`). It is ELF-only (system Mach-O `as`/clang reject the `@function`/`.type` directives). Findings:
- **[HIGH] `-S`→reassemble downgrades every `__thread` from `STT_TLS` to `STT_NOTYPE` → the resulting binary crashes — a broken mcc-own self-loop.** `emit_sym_decl` (`src/mccdis.c:187-198`) emits only `.type …,@function`/`@object` — no `STT_TLS` case, so a `__thread` var is a plain `.globl`/`:` in `.tdata`; on reassembly it becomes `STT_NOTYPE` while the TLS relocations (275/277) are preserved → the linker resolves a TLS-block reloc against a non-TLS symbol and the access sequence `blr`s into garbage. Reproduced end-to-end: `__thread int tv; int gt(void){return tv;}` → `mcc -S` → `mcc -c` → link → SIGBUS (138); direct compile works. arm64 analog of the KNOWN x86 `%fs`/`%gs` drop but worse (breaks mcc's OWN loop). Fix: emit `.type name,@tls_object`.
- **[HIGH] arm64 intra-section branch target emitted as a bare absolute hex number → silently mis-assembled by GAS/LLVM.** `src/arch/arm64/arm64-dis.c:218-225`: a non-relocated `b`/`bl` emits `b 0x84 // .L84` (raw target address; the label only in a comment). mcc's own assembler reads the bare number as an absolute section offset (self-loop OK), but GNU/LLVM read a bare-number branch operand as a PC-relative displacement → verified `b 0x84` at offset 0x60 re-assembled by `clang --target=aarch64-linux-gnu` encodes 0xe4 → wrong control flow, no diagnostic. x86_64 is correct (emits `jmp .L…`). Fix: emit `b .L<target>` like x86_64.
- **[MED] `.align N` emits a byte count that arm64 GAS/LLVM interpret as `2^N` → 256× over-alignment.** `src/mccdis.c:410` emits `.align sh_addralign` (bytes); mcc's assembler reads `.align` as bytes (self-loop OK) but arm64 GAS reads `2^N` → `clang --target=aarch64` produced addralign=256 for mcc's `.align 8`. Benign for correctness, breaks byte-exact reproduction. Fix: emit `.balign N` or `.p2align log2(N)`.
- **[LOW] arm64 disasm coverage gap floods `-S` with raw `.long` words** for `b.cc`/`cbz`/`cbnz`/`tbz`/`cset` (`arm64-dis.c:227-260`, fallback `:897`) — round-trips (raw word frozen) but unreadable + layout-fragile (branch displacement baked in). `-S`-output face of the R6 disasm-coverage work.
- **VERIFIED ROBUST (mcc self-loop):** `.globl`/`.weak`/`.type @function|@object`/`.size`, section switching, `double`/string/struct/jump-table data emission, GOT (`:got:`/`:got_lo12:`), external `bl`+reloc, `.quad sym` all reassemble+run via `mcc -c`. `[NEW]`. **TASK: T-mac-30071.**

### <a id="r14-attrs-poison"></a>Less-common `__attribute__`s — parse-and-drop (root: T-mac-30041 family)
`parse_one_attribute` (`src/mccgen.c:6347-6689`) is a fixed token switch; `nonnull`/`returns_nonnull`/`warn_unused_result`/`alloc_size`/`malloc`/`sentinel`/`ifunc`/`target`/`target_clones`/`no_sanitize`/`access`/`nothrow`/`may_alias`/`error`/`warning` all hit `default:` → "'%s' attribute ignored" + `skip_param` (and `struct FuncAttr`/`SymAttr` have no fields to hold them). `-Wattributes` is silenced by `-w` (which most builds pass). Findings:
- **[MED-HIGH] `__attribute__((error("…")))`/`warning("…")` function-poison dropped.** The tokens exist (`TOK_ERROR`/`TOK_WARNING`) but only the preprocessor consumes them; as attribute names they're ignored. A call to a function marked `((error("boom")))` compiles clean (mcc rc=0) where clang errors — defeating a deliberate compile-time poison, most importantly glibc's `_FORTIFY` `__error__`-tagged too-small-buffer aliases (a statically-detectable bad `memcpy`/`sprintf` that gcc/clang reject is silently accepted).
- **[MED] `ifunc` unsupported → link failure** (loud, not a silent miscompile — the resolver-aliased symbol stays undefined; `src/objfmt/mccelf.c` never emits `STT_GNU_IFUNC`). **[MED] `warn_unused_result` dropped → no `-Wunused-result`** (no such warning class in mcc; defeats glibc's pervasive `__wur` on `read`/`write`/`realloc`). **[LOW-MED] `nonnull`/`returns_nonnull`/`target_clones` dropped** (missing `-Wnonnull`; single default version). **[LOW] `alloc_size`/`malloc`/`sentinel`/`access`/hint-attrs** conservative/diagnostic-only drops.
- **VERIFIED ROBUST:** `vector_size` genuinely applied (`apply_attr_vector_size`); `__builtin_expect_with_probability`/`__builtin_assume_aligned` value-preserving; `__builtin_object_size` returns the safe conservative sentinel. `[NEW]` cluster (uncovered remainder of the T-mac-30041 root arm). **TASK: T-mac-30072.**

### <a id="r14-fortify"></a>`_FORTIFY_SOURCE` / `__builtin_object_size` — fortify inert by design
- **[MED] The preprocessor unconditionally force-disables `_FORTIFY_SOURCE` → `-D_FORTIFY_SOURCE=2/3` silently becomes 0 with no diagnostic.** `src/mccpp.c:5788` appends `#undef _FORTIFY_SOURCE\n#define _FORTIFY_SOURCE 0` after the user's `-D` defs, so glibc/Apple headers install no `_chk` redirects. Fortification the user explicitly requested is a silent no-op (intended "don't break the build" per `mccdefs.h:1153`, but undocumented + no `-W` note). **[LOW] A source-level `#define _FORTIFY_SOURCE 2` gives a spurious "redefined" warning AND still overflows silently** (re-enables the value, but the object_size stub below makes every check `dstlen==-1` → skipped): `char b[4]; strcpy(b,"toolong")` compiled+ran (`survived`, exit 0).
- **[HIGH, KNOWN] `__builtin_object_size`/`__builtin_dynamic_object_size` are a pure stub** — reads only the type arg, returns `(size_t)-1` (0/1) or `0` (2/3), never inspecting the object (`src/mccgen.c:13392-13400`; DETAILS:15705, stale line ref). Defeats fortify for the common type-0/1 redirects. Not re-tasked (KNOWN); the R14 NEW parts are the silent force-disable + redefine warning.
- **VERIFIED ROBUST:** the mem/str `_chk` wrappers (`runtime/include/mccdefs.h:816-915`) are GENUINE checkers (compare len vs dstlen, `__chk_fail()` on overflow) — materially better than the bounds-checker forward-unchecked (T-mac-30057); they're merely dead code today due to the stub + force-disable. `__chk_fail` links. `[NEW]` (force-disable + redefine). **TASK: T-mac-30073.**

### <a id="r14-lexer"></a>Lexer / translation phases (digraphs/splicing/trigraph-ordering/maximal-munch ROBUST)
- **[MED] A lone `\r` (classic-Mac line ending) is treated as whitespace, not a line terminator → directives break and `//` comments swallow code.** `src/mccpp.c:4143-4145` (`case '\r'`: no `line_num++`/BOL), and `parse_line_comment`/`parse_comment` break only on `\n` (`:925-946`). `int a=1;\r#define N 5\rint b=N;` emits the `#define` as text (N never defined); `// c\rint main(){…}` has the comment eat `main` → `_main not defined`. CRLF is handled correctly (only bare-CR affected).
- **[LOW] No `-Wtrigraphs` warning for an ignored trigraph** (default-off gating matches gcc; the missing diagnostic doesn't). **[LOW] Mid-file UTF-8 BOM absorbed into an identifier** (`mccpp.c:4665`, BOM skipped only at buffer start). **[LOW] Trigraph straddling a read-buffer boundary on a non-seekable stream (pipe) may not translate** (`lseek` deferral fails on a pipe). **[LOW] `\`+bare-`\r` is not a line-continuation** (`handle_stray_noerror` splices only `\`+`\r`+`\n`).
- **VERIFIED ROBUST:** digraphs `<:`/`:>`/`<%`/`%>`/`%:`/`%:%:` → `[]{}# ##` (+ spliced introducer); line splicing inside identifiers/`//`-comments/strings; trigraph→splice phase ordering; `-trigraphs` translation; unterminated `/*`/`"` at EOF (clean error, no hang); CRLF no drift; maximal munch (`a+++++b`, `..`/`...`/`->`). `[NEW]`. **TASK: T-mac-30074.**

### <a id="r14-wideint-abi-robust"></a>Wide-int (`__int128`/`_BitInt(N)`) argument-passing & return ABI — VERIFIED ROBUST (negative result)
Every value-correctness path matches gcc-16/clang: x86_64 SysV `__int128`/`_BitInt(65..128)` in two consecutive int regs with the no-split-at-exhaustion rule enforced atomically (`x86_64-gen.c:1703`; whole value spills to stack), returned rax:rdx; `_BitInt(N>128)` → MEMORY by-value + sret (limb-struct not falsely MEMORY-forced); variadic `__int128` forced to the overflow area (no split); arm64 `_BitInt(65..128)` in consecutive X-regs (no even-pair alignment — correct, limb fields are 8-byte), no split, returned x0:x1, stack 16-aligned; `_BitInt(N>128)` by-reference (pointer marker, caller-copies) matching gcc-16, returned via x8. Findings only: [MED] `__int128` rejected on arm64/riscv64 (`MCC_HAVE_INT128` x86_64-only, `mcc.h:1162`) despite the 128-bit machinery being present+correct — type-availability gap, folds into `#r12-bitint-cast`/T-mac-30063; [LOW] `__int128` also rejected on x86_64-PE (mingw supports it); [LOW/INFO] `_BitInt(N>128)` has no clang counterpart (clang caps at 128) — mcc matches gcc-16, so a future "interop-vs-clang" gate must not flag it. Captured, not separately taskified.

### <a id="r14-rir-robust"></a>RIR capture/replay faithfulness — VERIFIED ROBUST (negative result)
The scariest hypothesis ("does a real `-O1+` build ship an unchecked replay?") is answered NO for text/reloc: at `-O1+` every function body is replayed and byte-gated — `faithful = exact .text memcmp && exact reloc length && reloc-content memcmp` (`mccast.c:20981-20989`); `keep = faithful || (nofb-env …)` with `MCC_OPT_REPLAY_FALLBACK` always-on by default, so any text/reloc divergence forces the fallback to restore the parser's direct-emit bytes (`:21704`). Capture is a total catch-all — any un-hooked codegen is captured as raw text+reloc by `ir_cap_gap` and replayed by `memcpy` (byte-identical); the dangerous cases (backward `ind`/reloc, inline-asm section switch) set `ir_cap_bad=1` → safe bail. Determinism hazards closed (float const-pool position, frame slots, register pinning, `loc`/`anon_sym`/vstack all saved/restored). The one real gap — `.data`/`.rodata` not in the compare or rewind — is the KNOWN **T-mac-30037**, confirmed to govern the production driver but not constructibly exploitable in default builds (a shifted const-pool entry changes the reloc addend, which the exact reloc `memcmp` catches → fallback); residual is the PE latent-corruption surface only. `-fno-replay-fallback` keep-on-unfaithful is opt-in (T-lin-10053/10071 nofb-probe). No new task.


## Round 15 (2026-08-18) — include-path search, driver misc options, GNU vector-extension codegen, C23 feature surface, PIC/PIE code model, `offsetof`/pointer-comparison

Six parallel subsystem audits (mac-arm64), fresh relative to rounds 1-14. Most findings reproduced against a rebuilt `mcc` (differential vs clang / gcc-16). Two subsystems returned strong negative results (PIC/PIE addressing; `offsetof`/pointer-comparison). Tasks minted: **T-mac-30075..30078**.

### <a id="r15-include-search"></a>`#include` search-path resolution (precedence chain ROBUST)
The full precedence chain (`abspath → including-file-dir (quote-only) → -iquote → -I → -isystem → default-system → -idirafter → frameworks`) and the including-file-directory rule for nested quote includes are byte-verified against gcc-16 + clang at every rung (`src/mccpp.c:1457-1591`, options wired `src/libmcc.c:2748/3047/3050/3053/3073/3453`). Findings:
- **[HIGH] The default macOS framework search path is populated only for EXE/DYN/MEMORY output → `mcc -c`/`-E`/`-S` FAIL on any `<Framework/Header.h>` angle-include without an explicit `-F`.** `mcc_add_macos_sdkpath` (adds `<sdk>/System/Library/Frameworks` to `framework_paths`) runs in `mcc_set_output_type` (`libmcc.c:1366-1369`) AFTER the early `return 0` for PREPROCESS (`:1332`) and OBJ/ASM (`:1339-1355`), so for `-E`/`-c`/`-S` `framework_paths` is empty and the framework rung (`mccpp.c:1518`) never fires. Reproduced: `mcc -c` / `mcc -E` on `#include <CoreFoundation/CoreFoundation.h>` fail "not found" (rc 1) while `mcc … -o exe` (link) and `clang -c`/`-E` all succeed; explicit `-F <sdk>/System/Library/Frameworks` fixes `mcc -c`. Breaks ordinary separate compilation (compile `.o` then link) of any macOS TU that includes a framework header.
- **[MED] `-F`/`-iframework` dirs searched dead-last (after `-idirafter`) instead of in command-line order interleaved with `-I` → wrong header when `-F` precedes `-I`.** The framework rung always runs after all `-I`/`-isystem`/`-idirafter` (`mccpp.c:1518`); clang treats `-F` like `-I` positionally. Reproduced with `<Foo/Bar.h>` in both a framework and an `-I` dir: `-F fwtest -I idir` → clang picks the framework copy, mcc picks the `-I` copy (silent wrong-header shadowing).
- **[LOW] `-I-` (deprecated quote/bracket split) silently treated as an ordinary `-I` for a dir named `-`** (`libmcc.c:2748`, split semantics lost, no diagnostic). **[LOW] No duplicate-directory elimination** (`mcc_split_path` `libmcc.c:523` appends unconditionally; gcc/clang dedup) — redundant probes only, no miscompile.
- **VERIFIED ROBUST:** full quote-vs-angle precedence at every rung (angle correctly skips cur-dir + `-iquote`); including-file-dir rule (not cwd); multiple `-I` left-to-right; `-nostdinc` suppresses only default-system + SDK; `-idirafter`/`-isystem` flagged system. (The KNOWN Round-12 T-mac-30065 covers the un-emitted `-E` system-header flag, distinct.) `[NEW]`. **TASK: T-mac-30075.**

### <a id="r15-driver-misc"></a>Driver miscellaneous options & invocation
- **[MED] `-Wa,` assembler passthrough NOT implemented — flags silently dropped.** The option table (`src/libmcc.c:2305-2306`) has `Wl,`/`Wp,` but no `Wa,` entry, so `-Wa,--noexecstack` falls to the generic `-W` handler → `unsupported option` warning, rc 0, and never reaches the assembler (`-Wa,--noexecstack`/`-Wa,-g`/`-Wa,--gdwarf-4` all lost). Help text documents `-Wl,`/`-Wp,` but not `-Wa,`.
- **[MED] `-Xlinker` unsupported → hard error** (no table entry; `mcc -Xlinker -rpath -Xlinker /x` → `invalid option`, aborts). libtool/autotools link lines commonly emit `-Xlinker <arg>` (the canonical single-arg linker passthrough) rather than `-Wl,` (which does work).
- **[MED] `-x assembler` conflated with `-x assembler-with-cpp`.** The `-x` handler matches the first char only (`*optarg=='a'` → `AFF_TYPE_ASMPP`, cpp-on; `src/libmcc.c:3377-3389`), so `-x assembler` also runs cpp and there is NO `-x` value yielding no-cpp assembly — the reverse of gcc (`-x assembler` = no cpp). A `.s` meant to be passed verbatim gets preprocessed.
- **[MED] `-dumpversion` emits a non-standard date-based string** (`MCC_VERSION_MAJOR.MINOR` → `20260707.94756`; `src/libmcc.c:3373`) where gcc/clang print a plain integer (`16`) — CMake compiler-id / autoconf version gates that parse `MAJOR[.MINOR]` are defeated/skewed.
- **[LOW] `-print-multiarch` → hard error; [LOW] `-dumpspecs` unsupported** (warns, continues) — both gcc-specific, low relevance on macOS.
- **VERIFIED ROBUST:** response files `@file` (reads args, mixes flags+filenames), stdin `-` (`-x c -` / `-E -o -`), multiple `-c` + single `-o` (correct error), `-x none` reset, `-Wl,`→linker + `-Wp,`→preprocessor argv, `-dumpmachine` (`aarch64-apple-darwin`), `-print-search-dirs`/`-print-file-name=`/`-print-prog-name=` resolve real paths, `--`/bare-`-`. `[NEW]`. **TASK: T-mac-30076.**

### <a id="r15-vector-abi"></a>GNU vector-extension codegen (operation codegen ROBUST; ABI passing broken on arm64)
The generic GNU-vector implementation is fully scalarized (each vector is a `VT_STRUCT` local, ops lower per-lane through memory; `src/mccgen.c:677,7807-8102`) — correct per-lane, but the calling convention is wrong on non-x86:
- **[HIGH] arm64 (and arm/i386/riscv64) pass/return 16-byte GNU vectors as generic structs, not AAPCS64 short-vectors → cross-module ABI miscompile.** There is NO `is_vector` handling in `arm64-gen.c` (only `x86_64-gen.c` special-cases `f->a.is_vector` at `:1160/1194/1237`); AAPCS64 classifies an 8/16-byte `vector_size` type as a short vector passed in a single SIMD reg (v0-v7), but mcc sees `VT_BTYPE==VT_STRUCT` and routes an int vector through GPRs/stack and a float vector through the HFA path. Reproduced (mcc `main` → clang `lib`): `void f(v4 x,v4 y)` → lib sees garbage; `void f(v4f x)` with `{1.5,2.5,3.5,4.5}` → lib sees `1.5 0 0 0`. Same-module calls are self-consistent (work); the failure is strictly cross-TU with a gcc/clang-compiled peer. 32-byte vectors (>16B, memory on both) round-trip fine — confirming it's the short-vector register-class. Distinct from the landed x86_64 SSEUP fix (DETAILS:15276) and the 32-byte-alignment task T-lin-10012.
- **VERIFIED ROBUST (vs clang, arm64):** element-wise `+ - * / % & | ^ << >>` + unary (incl. per-lane int divide/modulo — scalarized, no SSE-int-divide hazard); comparison masks (correct element width + signedness, int/uint/float/double); scalar broadcast; `__builtin_shuffle` (runtime mask), `__builtin_shufflevector` (const indices, out-of-range → compile error), `__builtin_convertvector` (int↔float per-lane, trunc-toward-zero); subscript read/write (const + variable index); `sizeof`/align=16. LOW: `vector_op_ok` permits `%` on float-element vectors, caught only later at the scalar layer (cosmetic). `[NEW]`. **TASK: T-mac-30077.**

### <a id="r15-c23-surface"></a>C23 & extended-type feature surface (`nullptr`/`bool`/`u8`/`_Float128` ROBUST)
- **[HIGH] `<stdbit.h>` header absent — C23 bit-util code fails at `#include`** (`stdbit.h` not in `runtime/include/`) even though the whole `__builtin_stdc_*` family (15 builtins) already exists (`mccdefs.h:1339-1367`) — so it's PARTIAL: only the ~30-line header mapping the public `stdc_*` names + `__STDC_VERSION_STDBIT_H__` onto the builtins is missing; `stdc_bit_width(5u)` breaks at include time.
- **[HIGH] `_Float32`/`_Float64`/`_Float32x`/`_Float64x` unsupported as types** — no tokens (`mcctok.h:57-60` has only `_Float16`/`__bf16`/`__float128`/`_Float128`); `_Float32 x=1.5f32;` → "undeclared". Contrast `_Float128` IS supported (arm64/riscv64). No `f32`/`f64` suffixes.
- **[HIGH] `<stdckdint.h>` completely unusable on every non-x86_64 target** — `ckd_add`/`sub`/`mul` use `__int128`/`unsigned __int128` accumulators unconditionally (`stdckdint.h:49-83`), but `MCC_HAVE_INT128` is x86_64-only (`mcc.h:1163`), so on arm64/arm/i386/riscv64/PE even `ckd_add(&int,1,2)` → "`__int128` not supported". (The narrower 128-bit-operand-overflow is KNOWN `#runtime-intrinsics`; total non-x86 breakage is the more severe new angle.) Fix: gate the accumulator width or refuse cleanly. Folds with T-mac-30063/`#r14-wideint-abi-robust` (arm64 `__int128` availability).
- **[MED] `_Decimal32`/`_Decimal64`/`_Decimal128` unsupported** (no tokens; clean "undeclared", no crash). **[MED, KNOWN] `[[nodiscard]]`/`((nodiscard))` a silent no-op** (`mccgen.c:6529` → `skip_param`, deliberately deferred `DETAILS.md:18397`; contrast `[[deprecated]]` WORKS). **[LOW] `[[fallthrough]]`/`[[unsequenced]]`/`[[reproducible]]` parse-and-ignore** (`mccgen.c:6515`; no `-Wimplicit-fallthrough` exists to suppress).
- **VERIFIED ROBUST:** `nullptr`/`nullptr_t` (keyword `TOK_NULLPTR`, own `VT_NULLPTR`, conversions/`==`/`sizeof`/ordered-compare-diag all correct); bare C23 `bool`/`true`/`false`; `u8'x'` char (`TOK_U8CHAR`); `__STDC_VERSION__`=202311L default; `[[deprecated]]` diagnostic; `_Float128` on arm64. `[NEW]`. **TASK: T-mac-30078.**

### <a id="r15-pic-robust"></a>PIC/PIE code model + GOT/PLT — VERIFIED ROBUST (negative result)
End-to-end macOS arm64 dylib (`-fPIC -shared`) + PIE + `-fno-pic` exercised: external interposable global read, defined/static global read, external call, `&external_func` canonical-address (GOT, ==1 not a stub), `&defined_func`, `.data`→`.rodata` pointer — all correct, linked with system clang and ran. Non-static globals route through `R_AARCH64_ADR_GOT_PAGE`+`LD64_GOT_LO12_NC` / `R_X86_64_GOTPCREL`, calls through `CALL26`/`PLT32`; no absolute-where-PIC-needed reloc; reloc ranges guarded. Two LOW conformance/perf items (captured, not taskified): `-fPIC`/`-fpic`/`-fPIE`/`-fno-pic` have NO effect — addressing is unconditionally PIC-style (`s->pic` set at `libmcc.c:3136` but x86_64-gen never reads it, arm64-gen reads it only for TLS), which is SAFE (never emits absolute addressing a PIE/dylib loader rejects) but means `-fno-pic` is silently ignored; arm64 local `static` globals go through the GOT by default (`arm64_local_pcrel_on()` defaults off, opt-in via `MCC_ARM64_LOCAL_PCREL`) — a needless GOT entry vs the small-model `ADRP+ADD` (x86_64 uses PC32 directly). Context: `mcc -c` on macOS emits an ELF object system `ld` rejects (KNOWN by-design, `#r9-section-layout`) — blocks the "produce `.o`, link with system toolchain" half of the workflow, but mcc's own `-shared` path is correct.

### <a id="r15-offsetof-robust"></a>`offsetof` / pointer-comparison & decay — VERIFIED ROBUST (negative result)
No HIGH/MED miscompile. Pointer relational compare is UNSIGNED (`combine_types` sets `VT_SIZE_T`→`VT_UNSIGNED`, `std_op` rewrites `TOK_LT→TOK_ULT`, `mccgen.c:4610,4886`) — runtime-verified no high-address (`0x8000…`) miscompile, byte-matching clang. `__builtin_offsetof` (lowered to `&((T*)0)->member`) correct for nested/array-element/deep/array-of-struct/anonymous members vs clang; bit-field-member offsetof errors ("cannot take address of bit-field"); distinct-type pointer compare warns; function-pointer `==`/`!=`, null compares, `?:` with pointer operands + array decay, `p-q` signed `ptrdiff_t`, `sizeof arr` vs `sizeof(a+0)` all correct. One LOW: missing `-Wsizeof-array-decay` warning (value correct, no miscompile). No task.


## Round 16 (2026-08-18) — internal-consistency sweep: cross-implementation contradictions & gaps

Six parallel subagents (mac-arm64), a fresh *internal-consistency* lens distinct from the per-subsystem conformance audits of rounds 7–15: each hunted for places where two parts of the tree encode the same fact but DISAGREE, or where a case is handled in one code path and MISSING in a sibling. Angles: cross-backend ABI/codegen drift; constant-fold vs runtime-gen divergence; ELF/Mach-O/COFF emitter contradictions; drifted duplicated macros/tables; preprocessor/lexer↔parser boundary; optimizer/RIR/JIT semantic drift. Most findings reproduced against the rebuilt `cmake-macos/mcc` (several end-to-end mcc↔clang, mcc-fold↔mcc-runtime). Two areas returned strong negatives (optimizer/RIR/JIT; most of the fold path). Tasks minted: **T-mac-30080..30090**.

### <a id="r16-arm64-darwin-stackpack"></a>arm64-Darwin does not pack sub-8-byte FIXED stack arguments — cross-module miscompile (EMPIRICALLY CONFIRMED)
- **[HIGH] mcc rounds every fixed stack scalar to an 8-byte slot on arm64-Darwin; the Apple platform ABI packs fixed stack args to natural size/alignment → mcc↔clang cross-module miscompile.** `arm64_pcs_aux` (`src/arch/arm64/arm64-gen.c`) has a `MCC_TARGET_MACHO` special-case for the *variadic* cutoff (`:1068-1072`) but NONE for stack-slot sizing: float forced `size=8` (`:1116`), sub-8 integer forced `size=8` (`:1156`), stack offset rounded `ns=(ns+7)&~7` (`:1147-1148`) unconditionally. The same function drives both call site (`gfunc_call`) and callee (`gfunc_prolog` via `arm64_pcs` `:1532`), so mcc↔mcc is self-consistent (suite green) but mcc↔clang/libSystem breaks. Reproduced by building an mcc `main` against a clang-built dylib: `long check(long a0..a7,char c,int j)` called `(…,9,42)` → mcc returns 9000, clang reference 9042 (the `int j` read from the wrong stack offset); `double fchk(double d0..d7,float f0,float f1)` (`…,9.0f,42.0f`) → 9000 vs 9042 (float path `:1116`); `chk2(…,short s,char c,int j)` → 7000000 vs 7009042. CLEAN (matches clang): variadic stack args (Apple rounds those to 8 too), small-struct-on-stack (8-slotted on Apple too), register-passed small args, HFA/2-int struct returns. Fix: under `MCC_TARGET_MACHO`, for NAMED (non-variadic) stack args use natural size/align instead of forcing 8 (`:1108/1112/1116/1147-1148/1156`). `[NEW, HIGH]`. **TASK: T-mac-30080.**

### <a id="r16-zero-array-abi"></a>Zero-length trailing array member (`T x[0]`) mis-classifies FP aggregates — arm64 correct, x86_64/riscv64/arm wrong
- **[HIGH] a `struct{double a,b; char pad[0];}` is an HFA on arm64 but demoted to INTEGER regs on x86_64 and to `a0,a1` on riscv64 → cross-module ABI break vs gcc/clang.** arm64 explicitly skips `[0]` arrays (`if(!type->ref->c) return num;`, `src/arch/arm64/arm64-gen.c:1013`); the siblings recurse into the phantom member: x86_64 `classify_x86_64_inner` unwraps/iterates with no `ref->c==0` guard (`src/arch/x86_64/x86_64-gen.c:1131-1132`, member loop `:1168`, whole-struct merge at `:1301`; `classify_x86_64_merge(sse,integer)→integer` at `:1119-1120`); riscv64 `reg_pass_rec` guards only `c<0||c>2` and for `c==0` recurses (`src/arch/riscv64/riscv64-gen.c:548,552`); arm32 `arm_hfa_members` does `count*=ref->c`→0 then returns −1 (`src/arch/arm/arm-gen.c:759-762`). `struct S{double a,b; char pad[0];}` (16B): arm64→d0,d1 (matches gcc/clang), x86_64→rdi,rsi, riscv64→a0,a1. Fix: skip `ref->c==0` array members before recursing in the three siblings, mirroring arm64-gen.c:1013. `[NEW, HIGH]`. **TASK: T-mac-30081.**

### <a id="r16-int64-float-fold"></a>int64→`float` CONSTANT conversion double-rounds via host `long double` (mismatches runtime `SCVTF`)
- **[HIGH] `(float)<64-bit constant>` folds through the host `long double`; on an arm64 host (where `long double`==`double`, 53-bit) it rounds 64→53→24 (double rounding), while the runtime `SCVTF/UCVTF` (and x86 `cvtsi2ss`) is single correctly-rounded → a constant operand and a variable operand of the identical conversion disagree.** `gen_cast` builds `vtop->c.ld` from the integer (`src/mccgen.c:5431-5442`) then `if(dbt==VT_FLOAT) vtop->c.f=(float)vtop->c.ld` (`:5444-5445`); the runtime path is `gen_cvt_itof1` (`:5512-5519`)→`arm64-gen.c:2710-2734` (`SCVTF/UCVTF Sd,Xn` `:2731-2733`). ISO C 6.3.1.4 requires ONE correctly-rounded conversion, so the fold is wrong. Verified end-to-end at `-O0`: `(float)18014399583223809LL` → fold `0x5A800000` (wrong) vs runtime `long long` `0x5A800001` (matches clang); fires for signed AND unsigned 64-bit sources, in both static-initializer and runtime-constant contexts. Because the bug is in HOST arithmetic, an arm64-hosted mcc miscompiles this for EVERY target. `double` dest is robust (int64→double single-rounded on this host); 32-bit sources robust (≤31 sig-bits exact). Corollary (LOW, same root): `_Float16`/`__bf16` const path (`:5325-5331`) computes `f32_to_f16_bits((float)lv)` where `lv` is the long-double-widened integer → int64→double→float→half triple-round; fixing the `(float)lv` step fixes both. Fix: for `VT_FLOAT` dest convert the integer directly (`(sbt&VT_UNSIGNED)?(float)(uint64_t)c.i:(float)(int64_t)c.i`) instead of via `c.ld`. `[NEW, HIGH]`. **TASK: T-mac-30082.**

### <a id="r16-coff-tls"></a>COFF `-c` emits `__thread` in `.tdata`/`.tbss`, never renamed to `.tls` — x86_64/i386-win32 TLS mis-linked
- **[HIGH] a `__thread` object compiled to a COFF `.o` lands in a plain `.tdata` section; ELF keeps `.tdata`+`SHF_TLS` and Mach-O renames to `__thread_*` (`S_THREAD_LOCAL_*`), but COFF has no `SHF_TLS`→`.tls` rename → the var is placed in ordinary writable data (one instance shared by all threads) and the `R_X86_64_TPOFF32`→`IMAGE_REL_AMD64_SECREL` reloc resolves section-relative to `.tdata`, not the TLS block.** ELF (correct): `src/objfmt/mccelf.c:71-72`. Mach-O (correct): classifies `SHF_TLS` (`src/objfmt/mccmacho.c:1834-1835`) and renames via `skinfo[]` (`:1434-1437`). COFF (broken): `coff_output_obj` emits every section under its verbatim ELF name (`src/objfmt/mccpe.c:2187,2340`); `coff_section_characteristics` (`:2072-2095`) has no TLS case, no `.tdata`→`.tls` rename, and mcc's own COFF reader recognizes TLS only by the name `.tls` (`:2511-2512`), so the flag is lost across the object boundary; SECREL mapping at `:2105`. Since COFF is the default `-c` format on x86_64-w64 (T-lin-10083), `__thread int tv=7; int get(void){return tv;}` mis-links on the primary Windows target. Distinct from arm64/arm-win32 COFF reloc gaps (T-win-50005/6). Fix: map `SHF_TLS` sections to `.tls$`/`.tls` in `coff_output_obj`, emit the `_tls_used`/`_tls_index` directory on final link. `[NEW, HIGH]`. **TASK: T-mac-30083.**

### <a id="r16-macho-common"></a>Mach-O `-c` ignores `-fcommon` — forces tentative defs to strong `.bss` (ELF/COFF preserve COMMON)
- **[MED] `macho_output_file` calls `resolve_common_syms(s1)` unconditionally BEFORE checking `MCC_OUTPUT_OBJ` (`src/objfmt/mccmacho.c:2537`), so every `-fcommon` tentative def becomes a concrete strong `.bss` symbol even in a `.o`.** ELF does NOT resolve commons in the object path (`src/objfmt/mccelf.c:3308-3330`; `resolve_common_syms` runs only for executables `:3157`); COFF encodes `SHN_COMMON` as `SectionNumber=0,Value=st_size` (`src/objfmt/mccpe.c:2231-2233`). With `-fcommon` (opt-in; `nocommon` defaults 1 `src/libmcc.c:1192`), two TUs each with file-scope `int counter;` link as ELF/COFF but fail duplicate-symbol as Mach-O `.o`. Fix: gate the `resolve_common_syms` call on `file_type!=MCC_OUTPUT_OBJ`. `[NEW, MED]`. **TASK: T-mac-30084.**

### <a id="r16-coff-reloc-ovfl"></a>COFF `NumberOfRelocations` truncates at 65535 — no `IMAGE_SCN_LNK_NRELOC_OVFL`
- **[MED] `sh.NumberOfRelocations=(WORD)relcount[k]` casts a 32-bit count to 16-bit (`src/objfmt/mccpe.c:2351`) and `coff_section_characteristics` (`:2072-2095`) never sets `IMAGE_SCN_LNK_NRELOC_OVFL`, so a section with >65535 relocations silently wraps (65536→0) and the linker applies the wrong number of fixups.** ELF/Mach-O use 32-bit counts (e.g. Mach-O `nreloc` `:125`). Rare (large generated TU) but silent corruption. Fix: when `relcount>0xFFFF`, write `NumberOfRelocations=0xFFFF`, OR the section's `NRELOC_OVFL`, and store the true count in a leading synthetic reloc's `VirtualAddress`. `[NEW, MED]`. **TASK: T-mac-30085.**

### <a id="r16-ldbl-macros"></a>`__LDBL_*` predefines report `double` precision on quad-`long double` targets (Linux-arm64/riscv64) — contradicts `<float.h>` + codegen, and are self-contradictory
- **[MED→HIGH] on Linux-arm64/riscv64 the `long double` type IS IEEE quad in both `<float.h>` and codegen, but the `__LDBL_*` predefines report `double`.** Predefine (wrong): `runtime/include/mccdefs.h:308-318` maps `__LDBL_MANT_DIG__`→53, `__LDBL_MAX__`→1.79e308, `__LDBL_MAX_EXP__`→1024 in the non-x86 `#else`. Header (quad): `runtime/include/float.h:127-147` (`(__aarch64__&&!__APPLE__)||__riscv`): `LDBL_MANT_DIG 113`, `LDBL_MAX 1.189…e4932L`, `LDBL_MAX_EXP 16384`. Codegen (quad): `arm64-gen.c:1958-1968` (128-bit Q value), `riscv64-gen.c:430,741` (16-byte soft-quad), `MCC_LDOUBLE_SIZE 16`. So `_Static_assert(LDBL_MANT_DIG==__LDBL_MANT_DIG__)` fails (113 vs 53), `__LDBL_MAX__` under-reports by ~4600 orders of magnitude, tgmath-style dispatch keyed on `__LDBL_MANT_DIG__` misroutes quad→double helpers. PLUS an internal self-contradiction inside mccdefs.h alone: `__LDBL_MIN__`=3.362…e-4932L (quad, `:502-508`) while `__LDBL_MAX__`/`MANT_DIG`/`MAX_EXP` stay double (`:315/309/313`) — a nonsensical exponent range even with no header. Distinct from Apple/Win arm64 long-double SIZE issue (T-mac-30029). Fix: add an arm64/riscv64 (non-Apple/non-Win) quad branch to the mccdefs `#else`. `[NEW, MED→HIGH]`. **TASK: T-mac-30086.**

### <a id="r16-wint-sign"></a>`__WINT_MAX__`/`__WINT_MIN__` hardcoded unsigned but `__WINT_TYPE__` is signed `int` on macOS/BSD
- **[MED] on Apple/BSD `__WINT_TYPE__ int` (signed, `runtime/include/mccdefs.h:48-51`) but `__WINT_MAX__ 0xffffffffU`/`__WINT_MIN__ 0U` unconditionally (`:240-241`).** A signed `int` wint_t has range `[-2^31,2^31-1]`, so `_Static_assert(__WINT_MAX__==2147483647)` (true on gcc/clang macOS) fails, `WEOF`=`(wint_t)-1` mishandled against `__WINT_MIN__==0U`, and `_Generic((wint_t)0,unsigned int:…,int:…)` selects `int` contradicting the `0U`/`0xffffffffU` limits. The MAX/MIN pair was written for the Linux `unsigned int` branch and applied unconditionally. Distinct from the PE-only `__SIZEOF_WINT_T__`/`WINT_MAX` `unsigned short` items (INVESTIGATIONS:134/152). Fix: make MAX/MIN follow `__WINT_TYPE__` signedness. `[NEW, MED]`. **TASK: T-mac-30087.**

### <a id="r16-riscv-stackslot"></a>riscv64 stores sub-word/int STACK args narrow (SB/SH/SW) — leaves the rest of the XLEN slot undefined
- **[MED] riscv64 `gfunc_call` aligns each stack slot to XLEN but stores with the original sub-word type (`src/arch/riscv64/riscv64-gen.c:697-710`; `store()` picks SB/SH/SW by size `:455-463`), so the high bytes of the 8-byte slot are undefined; arm64 always STR X (`arm64-gen.c:1316-1319`), i386 pushes 4-byte slots (`i386-gen.c:610-618`), arm uses `str …,[sp,#-4]!` (`arm-gen.c:1059-1069`).** `mcc -S` confirms `sb a0,0(sp)` vs arm64 `str x0,[sp]`. `callee(long a0..a7,signed char x)` — a conformant callee widening from the slot sees garbage high bits. Latent (self-consistent mcc↔mcc; matches upstream TCC). Fix: set the stack-arg store type to `VT_LLONG` before `vstore()`. `[NEW, MED]`. **TASK: T-mac-30088.**

### <a id="r16-pp-if64"></a>`#if`/`#elif` fatally rejects valid controlling constants > 32 bits (uses 32-bit `expr_const`, should use `expr_const64`)
- **[MED, reject-valid/fatal, never a miscompile] `expr_preprocess` evaluates the `#if` string via `c=expr_const()` (`src/mccpp.c:2394`), but `expr_const` (`src/mccgen.c:14987-14993`) wraps `expr_const64` with a hard "constant exceeds 32 bit" error, reissued fatal as "bad preprocessor expression" (`mccpp.c:2403`, `libmcc.c:716`).** `parse_number` (`mccpp.c:3898-3920`) tokenizes the full 64-bit value; only the final controlling value is truncated/rejected. `#if 0x100000000`, `#if 1LL<<40`, `#if ADDR_BASE` (a 64-bit address macro), `#elif 0x200000000` (`:2973`) all error though clang/gcc accept them; internal arithmetic IS 64-bit (`#if 0xFFFFFFFFU+1==0x100000000` is correctly true). Same root as the known array-size cap (INVESTIGATIONS:470, `mccgen.c:9694`) but a DISTINCT, worse-impact consumer (breaks conditional compilation of portable headers, no per-declaration workaround). Fix: evaluate via `expr_const64()` and test the full 64-bit value for non-zero. `[NEW, MED]`. **TASK: T-mac-30089.**

### <a id="r16-low-cluster"></a>LOW consistency cluster (grouped)
- **[LOW] COFF silently clamps section alignment >8192; Mach-O caps at 4096 WITH a warning; ELF is unbounded.** COFF `coff_section_characteristics` clamps `lg` to 13 (`src/objfmt/mccpe.c:2093`) with no diagnostic; Mach-O caps 4096 + `mcc_warning` (`src/objfmt/mccmacho.c:2057-2058`). `_Alignas(16384) char buf[…];` is silently under-aligned on COFF. Fix: diagnose in the COFF path when requested alignment exceeds 8192.
- **[LOW] `__BIGGEST_ALIGNMENT__ 16` (unconditional, `runtime/include/mccdefs.h:247`) vs `MCC_MAX_ALIGN 8` on i386/arm** (`i386-gen.h:41`, `arm-gen.h:96`, consumed `src/mccgen.c:6432` for bare `__attribute__((aligned))`). Bare `aligned` yields 8 while the macro advertises 16 (GNU: bare == `__BIGGEST_ALIGNMENT__`); on arm the macro is also just wrong (gcc-arm reports 8). Explicit `aligned(16)` unaffected. Fix: derive `__BIGGEST_ALIGNMENT__` per-arch from `MCC_MAX_ALIGN`.
- **[LOW] riscv64 union test uses raw `type->ref->type.t == VT_UNION` (`riscv64-gen.c:542`) where arm64 uses masked `IS_UNION` (`arm64-gen.c:987`).** Works today (struct/union Sym `type.t` carries no qualifiers, `mccgen.c:7318`); brittle if a flag is ever OR'd on. Switch riscv64 to `IS_UNION`.
- **[LOW/speculative] arm64-Darwin K&R/old-style `double` args passed in v0 instead of on stack** — the `arm64-gen.c:1328` old-style float→GPR clause is dead on Darwin because `FUNC_OLD` calls `arm64_pcs` with `variadic=0` (`:1268`); Apple treats unprototyped calls like variadic (stack). Near-zero impact; one targeted test only.
- `[NEW]` cluster. **TASK: T-mac-30090.**

### <a id="r16-opt-jit-robust"></a>Optimizer / RIR-replay / embed-JIT semantic drift — VERIFIED ROBUST (negative result)
No new flag/engine-gated miscompile. ~35 differential programs across `-O0..-O4`, `--embed-jit` (`MCC_JIT=0/1`), `-run`, `-fno-replay-fallback` produced identical observable behavior; consistent with the torture-suite result (1 static miscompile pr85156, 0 JIT-introduced). VERIFIED SOUND (read + differential): dead-store elimination (`ast_dse_block` `src/mccast.c:9954`; volatile/escape excluded, alias/back-edge/goto-liveness correct); narrowing (`ast_narrow_binary:9249`; distributive-homomorphism + range-fit for `/ % << >>`); divmagic (`:12089/12165/12255`; INT_MIN/-1 excluded, `|C|<=1` guard); reassociation (`:12803+`; integer-only, FP excluded, unsigned mask guards); IVSR (`:13455`; width-equality + regpure cofactor); TCO (`:10453`; volatile/array/VLA/escape excluded, arg-clobber ordering); if-conversion select/abs (`:12766/12618`; no load/call speculation); cprop (`:9804`; const-only/volatile/escape/setjmp-guarded); inline arg-forwarding (`:3615`); FP contraction (mcc does NOT form FMA even at -O2 → no association drift); embed-JIT/-run matched AOT on setjmp/struct-return/bitfields/varargs/FP/heavy-loops. Two non-bugs: (1) identical anonymous string literals may share storage at -O2+ (`MERGE_CONSTANTS`, `src/mccopt.h:126`) — legal per C11 6.4.5p7 (verified NOT extended to distinct named `const` objects); (2) cprop/DSE/IVSR share T-mac-30046's indirect-`setjmp` blind spot but only affect locals modified between setjmp/longjmp, already indeterminate per C11 7.13.2.1p3 — attach to T-mac-30046, not a new task. `[NEW]` negative.


## Round 17 (2026-08-18) — internal-consistency sweep II: fresh contradiction angles

Six parallel subagents (mac-arm64), continuing the internal-consistency lens of Round 16 on SIX angles rounds 7–16 had not covered: diagnostic/error-gate consistency; runtime-lib↔codegen helper-ABI contract; type/declaration-merging contradictions; cross-consumer layout consistency (parser vs ABI classifier vs DWARF vs static-init); internal buffer-size/cap-constant drift (incl. untrusted object readers); driver option interaction/conflict. Most findings reproduced against `cmake-macos/mcc` and cross-checked vs clang/gcc-16. Two subsystems returned strong negatives (helper-ABI contract; layout cross-consumer). Tasks minted: **T-mac-30091..30097**.

### <a id="r17-elf-loader-oob"></a>ELF `.o` loader trusts file-supplied section/symbol indices — OOB read on a crafted object (SECURITY)
- **[HIGH, SECURITY] `mcc_load_object_file` indexes `shdr[]`/`sm_table[]` (both sized by `ehdr.e_shnum`, `src/objfmt/mccelf.c:3423-3425`) with file-controlled fields never bounded against `e_shnum`.** Unchecked indices: `shdr[ehdr.e_shstrndx]` (`:3427`, then its `sh_offset`/`sh_size` feed `load_data` → attacker-influenced arbitrary-offset read), `shdr[sh->sh_link]` (`:3449`), `shdr[sh->sh_info]` (`:3461`), `sm_table[sh->sh_link/sh_info]` (`:3554,3556`), `sm_table[sym->st_shndx]` (`:3570`, guarded only by `< SHN_LORESERVE 0xff00`, NOT `< e_shnum`), `sm_table[sh->sh_info].offset` (`:3601`). A crafted relocatable ELF (e.g. `e_shnum=3, e_shstrndx=0x4000`, or a symbol `st_shndx=0x1000`) fed via `mcc file.o`/`mcc lib.a`/a dependency dylib → OOB heap read (info disclosure / crash). This is a genuine CROSS-READER drift: the sibling loaders already bound the equivalent index — Mach-O `if(!ns->n_sect||ns->n_sect>nsec)continue;` (`mccmacho.c:3116`), COFF `if(secnum<1||secnum>nsec…)` (`mccpe.c:2584`) and `rl->SymbolTableIndex<(DWORD)nsym` (`:2617`). Distinct from tracked reader work (T-mac-30023 = archive nsyms + COFF reloc; T-lin-10396 = Mach-O exports / DLL export count / load-cmd walk / `load_data` short-read `:3370`). Fix: reject `e_shstrndx>=e_shnum` and clamp `sh_link`/`sh_info`/`st_shndx` against `e_shnum` before every `shdr[]`/`sm_table[]` index, mirroring Mach-O/COFF. `[NEW, HIGH/SECURITY]`. **TASK: T-mac-30091.**

### <a id="r17-strinit-gate"></a>String-array excess-initializer warning ungated — element-count sibling is `-Wexcess-initializers`-gated
- **[MED] `char a[3]="hello";` warns via plain `mcc_warning("initializer-string for array is too long")` (`src/mccgen.c:17187`, always on, only `-w` silences), but the same defect class for element lists uses `mcc_warning_c(warn_excess_initializers)("excess elements in initializer")` (`:16585,17345`).** `-Wno-excess-initializers` suppresses `int b[3]={1,2,3,4};` but NOT the string case; clang groups both under `-Wexcess-initializers`. Fix: route `:17187` through `warn_excess_initializers`. `[NEW, MED]`. **TASK: T-mac-30092.**

### <a id="r17-implicit-int-var"></a>Implicit-int variable declarator ungated + not C99-promoted — siblings are hard errors (updates T-mac-30038)
- **[MED] a FOURTH implicit-int site, the plain variable declarator `oldint` case, uses plain `mcc_warning("type defaults to int")` (`src/mccgen.c:18550`) — ungated (`-Wno-implicit-int` cannot suppress; only `-w`) and NOT promoted to an error in C99+.** Its siblings `:18526` ("return type defaults to 'int'") and `:18443` ("type defaults to 'int' in declaration") are `mcc_warning_c(warn_implicit_int)` and ARE promoted to hard errors by the `removed_in_c99` permerror table (`src/libmcc.c:842-844`, applied `:852-854`). Verified in the default (C99+) std: `x;` → mere warning rc0; `f(){return 0;}` → error; `const c=1;` → error. NB this also corrects the stale claim at INVESTIGATIONS.md:205 (T-mac-30038) that `:18443/:18526` "stay warnings forever" — they are now permerror-promoted. Fix: replace `:18550` with `mcc_warning_c(warn_implicit_int)`. `[NEW, MED]`. **TASK: T-mac-30093.**

### <a id="r17-empty-paren-def"></a>Empty-paren function DEFINITION after a prototype merged as a 0-param prototype (accept-invalid, order-dependent)
- **[MED] `is_compatible_func` treats a `FUNC_OLD` side with no param list as compatible with any prototype (`src/mccgen.c:4406-4412`), and the definition path in `patch_type` forces `func_type=prototyped` but adopts the definition's EMPTY param list (`:1867-1873`) → a 0-parameter prototype.** `int f(int,int); int f(){return 42;}` is accepted (clang/gcc reject "conflicting types"); a following `f(1,2)` then gets a spurious "too many arguments". ORDER-DEPENDENT: `int f(){…}` then `int f(int,int);` merges correctly to 2 params. C 6.7.6.3p14: an empty param list in a DEFINITION means zero params and must not be `FUNC_OLD`-unprototyped. Fix: in `is_compatible_func`/definition merge, treat a definition's `()` as a 0-param prototype and diagnose against a prior N-param prototype. `[NEW, MED]`. **TASK: T-mac-30094.**

### <a id="r17-ptr-array-redecl"></a>Completed array bound behind a pointer not propagated across a redeclaration (reject-valid, order-dependent)
- **[MED] `compare_types` deems an unknown array bound behind a pointer compatible with a known one (`src/mccgen.c:4506,4510-4513`), but `patch_type` completes the bound only for a DIRECT `VT_ARRAY` object (`:1879-1882`); for `VT_PTR` it never reassigns `sym->type.ref`, discarding the completed bound.** `extern int (*p)[]; int (*p)[10]; sizeof(*p)` → spurious "unknown type size" (clang: 40). ORDER-DEPENDENT (reverse works); generalizes to any indirection depth; direct-array control works both ways. Fix: on compatible redeclaration adopt the completed (possibly nested) ref from whichever declaration supplies it, not only top-level `VT_ARRAY`. `[NEW, MED]`. **TASK: T-mac-30095.**

### <a id="r17-std-trigraph"></a>`-std=gnu*` after `-std=c*`/`-ansi` leaves trigraph mode stuck on (driver state not fully overridden)
- **[MED, silent mis-preprocessing] the trigraph flag is written ONLY inside the strict-ISO branch of the `-std` handler (`s->trigraphs = !(cversion>=202311)`, `src/libmcc.c:3024`); the gnu branch (`:2985`) never clears it.** `mcc -std=c99 -std=gnu99 -c f.c` leaves trigraphs ON → `"??="` silently becomes `"#"` etc. (gcc-16: last `-std` wins, trigraphs off, errors on the `?`). `std_strict_ansi` is reset correctly every `-std`; only `trigraphs` leaks. Fix: compute the trigraph default unconditionally on every `-std`: `s->trigraphs = strict_iso && !(cversion>=202311);`. `[NEW, MED]`. **TASK: T-mac-30096.**

### <a id="r17-low-cluster"></a>LOW consistency cluster (grouped)
- **[LOW] array-size OVERFLOW misreported as "incomplete type elements".** For a multi-dim array whose inner `type_size()` overflows (`<0`), `src/mccgen.c:9725-9726` says "declaration of an array of incomplete type elements" though the element type is COMPLETE (just too large); sibling paths for the same defect say "unknown type size" (`:5761`, 1D) and "invalid array size" (`:9704`, dim>INT_MAX). gcc says "size of array is too large" for all. Adjacent to the 32-bit cap root T-mac-30070. Fix: distinguish `type_size()<0` overflow from a truly incomplete element type and unify the message.
- **[LOW/cosmetic] DWARF describes a `long long`/`unsigned long long` bit-field of width ≤32 as `int`/`unsigned int`.** `struct_layout` permanently rewrites the member btype `VT_LLONG→VT_INT` in the pcc path (`src/mccgen.c:6940-6941`) and the DWARF member type ref is emitted from that rewritten type (`src/mccdbg.c:2594`). `DW_AT_bit_size`/`DW_AT_data_bit_offset`/struct `byte_size` are byte-identical to clang and signedness is preserved, so the printed VALUE is correct — only the debug type NAME narrows. All four layout consumers otherwise agree. Fix: describe the bit-field by its declared type in DWARF (carry the narrowed access width separately, as `auxtype` already does).
- **[LOW] disassembler unique-name dedup truncates at 256 → label collision.** `mccdis.c:27-28` `snprintf(buf/*[256]*/,"%s.%d",name,i)`; a generated symbol name near/over ~250 chars loses the `.<idx>` disambiguator so two symbols collapse to one "unique" label in `-S`/disasm. Compiler's own symtab (not attacker input) → display-correctness only. Fix: size from `strlen(name)+16` or fall back to an index-only name.
- **[LOW] spurious "overriding compiler action already specified" for redundant SAME-type flags.** `set_output_type` warns whenever `s->output_type` is already set, not whether it CHANGES (`src/libmcc.c:2929-2931`); `mcc -c -c` and `mcc -c -r` (both → `MCC_OUTPUT_OBJ`) warn needlessly (gcc silent). Fix: `if(s->output_type && s->output_type!=x) mcc_warning(...)`.
- `[NEW]` cluster. **TASK: T-mac-30097.**

### <a id="r17-helper-abi-robust"></a>Runtime-lib ↔ codegen helper-ABI contract — VERIFIED ROBUST (negative result)
No contract drift. Every helper family the emitter lowers to is name/signature/arg-order/ABI-consistent with the bundled runtime, cross-checked per target + empirically on host arm64: wide-int `__mcc_i256_*`/`__mcc_i512_*` (runtime `int256.c`/`int512.c` `#include ../../src/wide256_arith.h` — the SAME kernel as the fold, so runtime==fold by construction; both bundled all targets `CMakeLists.txt:2558`; confirms r16-fold-gen-robust and the "int512 orphaned" note is stale); `__int128` `__multi3/__divti3/…` (x86_64-only, gated `mcc.h:1162`, `int128.c` bundled x86_64-only — matched; refused elsewhere so no dangling refs); soft-quad `__*tf*` (binary128; `f128_t` target-correct; NaN-unordered compare semantics verified; bundled arm64/riscv64); 80-bit `__*xf*` (x86_64 long double); half/bf16 `__mcc_extendhfsf2` etc. (TOK strings carry the `__mcc_` prefix bridging the name — false-alarm dismissed); atomics `__atomic_*_N` + generic aggregate forms (arg order `(size,mem,ret/val,order)` verified); `_Complex` `__mcc_cmul/cdiv{,f,l}` + integer `__mcc_cdivi64/u64` (return-by-pointer, res first); division `__divdi3/__udivdi3/…`; `__mcc_signbit*`. Dismissed near-misses: `complexabi.c` GCC `__multc3/__divtc3` aliases (mcc never emits them); x86_64 self-host lacks `__*tf*` but refuses `__float128` (no refs); arm64/riscv64 old-style `vpush_helper_func` for `__*tf*` dormant (arm64-Darwin long double==8-byte double, unprototyped 128-bit args still register-pass matching runtime). `[NEW]` negative.

### <a id="r17-layout-robust"></a>Cross-consumer struct/bitfield layout (parser vs ABI classifier vs DWARF vs static-init) — VERIFIED ROBUST (negative result)
All four consumers agree on offset/size/alignment/bit-position across ~40 probe shapes on arm64-Darwin, byte-identical to clang/gcc-16 (runtime values, static-init raw bytes via memcmp, pass/return-by-value ABI, dwarfdump bit-offsets/byte-sizes): bit-field storage-unit straddling, `:0` alignment reset, signed bit-fields with negative static-init, `__attribute__((packed))`+straddling+negatives, bit-field-in-union, the `long long:N` 32/33 type-rewrite boundary (no LAYOUT divergence), anonymous struct/union members, flexible array member (sizeof excludes; DWARF byte_size==sizeof; head static-init), empty/zero-size GNU struct members, `_Alignas(16)` member + `aligned(32)` type (incl. spilled-stack passing + HFA). Consumers: `struct_layout` (`src/mccgen.c:6849-7083`), arm64 classifier (`arm64-gen.c:978-1054`), DWARF (`src/mccdbg.c:2506-2600`), static init (`mccgen.c:16871,16940-16955`). The ONLY discrepancy is the cosmetic DWARF bit-field type-name narrowing (r17-low-cluster). Non-findings: `long long:31/32`=-1 printing unsigned matches gcc-16 exactly (implementation-defined, DETAILS:40506); known items (x86_64 packed MEMORY-class INVESTIGATIONS:67, enum DIE byte_size=4 :186, DWARF-2 data_bit_offset :187) confirmed present, not re-reported. `[NEW]` negative.


## Round 18 (2026-08-18) — internal-consistency sweep III: control-flow, VLA lifetime, attributes, linker

Six parallel subagents (mac-arm64), continuing the internal-consistency lens on SIX further angles rounds 7–17 had not covered: control-flow codegen consistency (switch/goto/short-circuit across backends + opt/RIR); VLA/alloca stack-lifetime across exit edges; initializer static-vs-runtime symmetry; function-attribute SEMANTIC application; integer/pointer promotion corners; internal-linker emit-vs-resolve consistency. Most findings reproduced against `cmake-macos/mcc`, cross-checked vs clang/gcc-16 and system `ld`. Two subsystems returned strong negatives (initializers; promotion/conversion). Tasks minted: **T-mac-30098..30102**.

### <a id="r18-macho-reader-weak"></a>Mach-O object READER drops the weak bit (`N_WEAK_DEF`/`N_WEAK_REF`) — external clang/system `.o` with weak symbols fail to link
- **[HIGH] `macho_load_object_file` derives binding from `N_EXT` only (`bind = (n_type&N_EXT)?STB_GLOBAL:STB_LOCAL`, `src/objfmt/mccmacho.c:3100`) and passes it to `set_elf_sym` (`:3124`) WITHOUT ever consulting `n_desc` for `N_WEAK_DEF (0x80)`/`N_WEAK_REF (0x40)` — so every weak external is imported as STRONG (`STB_GLOBAL`).** The shared ELF resolver relies on `STB_WEAK` for strong-beats-weak / first-weak-wins (`src/objfmt/mccelf.c:642-645`); mcc's OWN Mach-O WRITER correctly emits `N_WEAK_DEF` (`convert_symbol` `:1032-1034`), so this is strictly a READ-side drop. Verified end-to-end: two clang-compiled `.o` each with `__attribute__((weak)) int shared(){…}` linked by mcc → `error: '_shared' defined twice`; system `ld` links them (first weak wins, prints `1 1`); weak-def+strong-def → mcc errors, `ld` picks strong. Breaks the pervasive C++/GNU idiom set (inline fns, template instantiations, vtables/typeinfo, weak wrappers — one weak def per TU) → any such external Mach-O `.o` is unlinkable by mcc. Distinct from COFF emit-side weak→strong (T-mac-30019) and the Mach-O WRITE-side visibility/`N_PEXT` gap (INVESTIGATIONS:253). Fix: for `N_EXT` symbols set `bind=STB_WEAK` when `n_desc&(N_WEAK_DEF|N_WEAK_REF)`, and prefer `STT_FUNC`/`STT_OBJECT` from the `N_SECT` section over the blanket `STT_NOTYPE`. NB a resolved undefined-weak-ref would then become a behavior change (currently errors, matching `ld` for a static exe). `[NEW, HIGH]`. **TASK: T-mac-30098.**

### <a id="r18-switch-jt-nocode"></a>Jump-table switch lowering leaves `nocode_wanted` reachable after an exhaustive switch → spurious "might return no value"
- **[MED-HIGH] `gcase_jumptable` ends with `nocode_wanted = nsave` (=0, reachable; `src/mccgen.c:15284`) after its trampoline `gjmp` set `CODE_OFF` (`:15247-15248`), while the compare-chain path ends `return gjmp(dsym)` whose `CODE_OFF()` correctly leaves the fall-through unreachable (`:15322`).** After an exhaustive `switch` (every arm returns + `default`, no `break`) the jump-table path wrongly marks the function end reachable, so `check_func_return` (gated `!nocode_wanted`, `:16049-16050`) fires `error: function might return no value`. Verified: `switch(x){case 0..4:return N; default:return -1;}` → `mcc -O0 -c` clean (compare-chain) but `MCC_SWITCH_JUMPTABLE=1` or `-O13` (which selects the jump table via `switch_jt_env` `:15123-15131`) → hard error; `-w` silences it and the codegen is runtime-correct vs clang. Jump table is only SELECTED at `-O13+`/env, so limited blast radius, but it's a hard error on valid code. Fix: end `gcase_jumptable` in the unreachable state like `gcase` — `nocode_wanted = nsave | CODE_OFF_BIT;`. `[NEW, MED-HIGH]`. **TASK: T-mac-30099.**

### <a id="r18-rir-int128-case"></a>RIR case-value capture truncates `__int128` switch labels to 64 bits (sign-extended high word)
- **[MED, latent] `rir_hook_case((long long)cr->v1,(long long)cr->v2)` (`src/mccgen.c:16281`) passes only the low 64 bits (sink stores two `long long`, `src/mccrir.c:500`), dropping `cr->v1hi/v2hi`; the baseline lowering uses the full 128-bit boundary (`:15304,15314` via `vpush_case(...,v2hi/v1hi)`), but RIR replay RECONSTRUCTS the high word by sign-extension not the captured value (`cr->v1hi = uns?0:(v1>>63)`, `src/mccast.c:6345-6346`).** A `case ((__int128)1<<64):` (hi=1, lo=0) replays as (hi=0, lo=0) → wrong case boundary. `switch(__int128)` is legal (jump table disabled for `VT_INT128` so both paths use `gcase`, on different boundaries). Masked in default builds by the replay-fallback byte-compare (restores baseline bytes); ships wrong only under `-fno-replay-fallback` on an int128-capable target. Read-verified (arm64-macOS rejects `__int128` switch). Fix: widen `rir_hook_case`/`RIR_M_CASE` to carry `v1hi/v2hi` and use them in replay instead of `>>63`. `[NEW, MED]`. **TASK: T-mac-30100.**

### <a id="r18-always-inline"></a>`always_inline` stored but never consulted by the inliner
- **[MED] `__attribute__((always_inline))` sets `ad->f.func_alwinl` (`src/mccgen.c:6386`) but `ast_fn_inlinable` (`src/mccast.c:3031-3061`) never references `func_alwinl` (grep = 0 hits) — it gates on an `-O` env, a `static`/weak requirement, `func_noinl`, and a 64-node cost limit.** So a large `always_inline static inline` fn at `-O2` is NOT inlined (`bl` emitted), and nothing is inlined at `-O0`; no diagnostic either way (gcc errors "inlining failed in call to always_inline"; clang always inlines). This CORRECTS the stale INVESTIGATIONS:237 (T-mac-30041) claim that `always_inline` is "applied" — only `noinline` is applied (`mccast.c:3039`); the two were conflated. Fix: in `ast_fn_inlinable`, when `func_alwinl` is set bypass the env/static/node-limit gates (and ideally error if still un-inlinable), mirroring `func_noinl`. `[NEW, MED]`. **TASK: T-mac-30101.**

### <a id="r18-low-cluster"></a>LOW cluster (grouped)
- **[LOW, accept-invalid + UB] VLA scope-tracker overflow (>512 open VLAs) fail-OPENS the jump-into-VLA-scope constraint check.** Each VLA records an open scope only up to `VLA_TRACK_MAX`=512 (`src/mcc.h:558`); the 513th sets a sticky `vla_track_ovf` (`src/mccgen.c:17751-17754,17776-17779,17829-17832`, reset only at function entry `:18067-18069`), and `vla_scope_open()` then returns 1 ("legal") unconditionally (`:15439-15440`), so the backward-`goto` check (`:16326-16327`) and the `switch` case/default check (`:16283-16295`) never fire. A function with >512 live VLAs + a `goto`/`switch` INTO a VLA's scope compiles clean and runs with an uninitialized VLA pointer (clang: hard error); removing the VLAs, mcc correctly errors — proving the flag disables the check. Fail-open → should be fail-safe. Fix: return 0 (reject) when `vla_track_ovf` is set, or grow the tracker dynamically.
- **[LOW] `noreturn`-that-returns diagnosed for an explicit `return` but not for fall-off-end.** `src/mccgen.c:16052-16053` warns for an explicit `return` in a noreturn fn, but `check_func_return` returns early for `VT_VOID` (`:15054-15056`), so `_Noreturn void f(void){}` (falls off) gets no diagnostic (clang: `-Winvalid-noreturn`). Fix: before the VOID early-return in `check_func_return`, if the fn is noreturn and control can reach the end, warn. Relates to the return-type/warning family (T-mac-30052).
- `[NEW]` cluster. **TASK: T-mac-30102.**

### <a id="r18-init-robust"></a>Initializer static-vs-runtime symmetry — VERIFIED ROBUST (negative result)
The static-init path (`init_putv` `if(sec)` branch, `src/mccgen.c:16887-17017`; zero-fill `init_putz` `:16501`) and the runtime/auto path (`:17019,17030`; zero-fill `:16506/16517`; scalar zero-skip `:17367`) produce identical bytes/values across 12 scenario families, all matching clang byte-exact: unions with mismatched member sizes; designated overlap/out-of-order/sparse; GNU range designators (incl. range spreading a reloc-carrying blob at runtime); overwrite-to-zero (the `DIF_CLEAR` bit is correctly cleared on scalar overwrite `:16780`, so trailing zeros ARE stored); pointer/reloc overwrite; `-0.0` sign-bit preservation; compound literals (file-scope static, block-scope loop-fresh, address-of, as static initializer); `_Complex`-from-real imaginary `+0.0`; bitfields (packing, signed, `:0`, designated); string init (`char`/`char16_t`/`char32_t`/`wchar_t`, NUL-drop, oversized tail-zero); self-referential; FAM; excess-brace/trailing-comma/brace-elision. Reconfirms `#r7-init-robust`, extended to static-vs-runtime symmetry. Only latent item remains the known ≥2 GB `int`-offset overflow (not taskified). `[NEW]` negative.

### <a id="r18-promotion-robust"></a>Integer/pointer promotion & conversion corners — VERIFIED ROBUST (negative result)
No new contradiction across bit-field integer promotion (narrow fields on `long`/`long long` base promote by WIDTH; sign/value match fold vs runtime), enum promotion (plain + C23 fixed underlying), `_Bool`/`char`/`char16_t`/`char32_t`, usual-arithmetic-conversions (equal-rank-unsigned-wins, higher-signed-absorbs, `unsigned long` vs `long long`), null-pointer-constant recognition (`0`/`(void*)0`/`0L`/`'\0'`/enum-0), boolean context (pointer/double/`__int128`/`_BitInt`/bitfield → 0/1 in fold+runtime), shift promotion (result takes promoted LEFT operand; right operand doesn't widen), compound-assign narrowing. `combine_types`/`gen_op`/bit-field precision-N handling (`src/mccgen.c:4540-4994`). One dismissed divergence: wide bit-field (width>32, `long`/`long long` base) promotes under the precision-N model → `(long long)(s.u33-1)` = 2³³−1 — mcc byte-matches gcc-16; clang is the sole outlier (deliberate gcc-matching design, DETAILS commit b3c660f1 / T-mac-30007). `[NEW]` negative.


## Round 19 (2026-08-18) — internal-consistency sweep IV: reg-alloc, FP, mem-builtins, atomics, DWARF, macros

Six parallel subagents (mac-arm64), continuing the internal-consistency lens on SIX further angles: register-allocation/clobber/spill consistency; FP codegen edges (fold vs runtime vs backend); string/mem builtin lowering; atomic/memory-order codegen across backends; DWARF type-DIE + variable-location correctness; preprocessor macro-expansion deep corners. Findings reproduced against `cmake-macos/mcc` (and the `cmake-cross/mcc-*` cross compilers for `-S`), cross-checked vs clang/gcc-16 and via dwarfdump/objdump. THREE subsystems returned strong negatives (register-alloc; macro-expansion incl. C23 `__VA_OPT__`; and largely FP/atomics). Tasks minted: **T-mac-30103..30106**.

### <a id="r19-dwarf-lexblock"></a>DWARF: every local emitted one lexical-block level too shallow — innermost scope empty, shadowed locals resolve wrong
- **[HIGH] `mcc_debug_finish` emits each block's symbols (`for(i=cur->n_sym-1…)`, `src/mccdbg.c:2795-2834`) BEFORE that block's `DW_TAG_lexical_block` header (`:2836-2845`); in DWARF, DIEs before a lexical_block tag are children of the ENCLOSING DIE → every block's variables become children of its PARENT, and the block's real innermost scope is emitted EMPTY.** Compounded: both `mcc_debug_funcstart` (`N_LBRAC` at entry, `:2914`) and the body `{` in `block()` (`src/mccgen.c:16011-16012`, another `N_LBRAC`) open blocks → a redundant whole-function `lexical_block` duplicating the subprogram PC range. `int g(int a){int x=a+1;return x;}` → `x` reported live over `[0x0,0x40)` (whole fn incl. prologue `0x0-0x24` where its slot `[x29,#-4]` holds garbage), inside a redundant block, with the true scope `[0x24,0x34)` emitted empty; clang makes `x` a direct child of the subprogram. Shadowing makes it a WRONG-VALUE bug: `int h(int a){int v=a;{int v=a+100;a=v;}return v+a;}` → both outer and over-scoped inner `v` simultaneously in scope at some PCs, so an innermost-scope lookup resolves the wrong instance. `DW_OP_fbreg` offsets themselves are correct (T-mac-30061 unaffected) — purely the scope nesting contradicts reality. Fix: emit the `DW_TAG_lexical_block` header BEFORE the block's symbol loop, and make the funcstart root block transparent (map to the subprogram, not a wrapper) so funcstart+body-`{` don't double-nest. `[NEW, HIGH]`. **TASK: T-mac-30103.**

### <a id="r19-strmem-constfold"></a>`strlen`/`strcmp`/`memcmp` of string literals not constant-folded — valid ICE-context code rejected (and a VLA-vs-error inconsistency)
- **[MED] no compile-time evaluator exists for `strlen`/`strcmp`/`strncmp`/`memcmp`/`strchr` over string-literal args; the const-expr path (`expr_const64_wide`, `src/mccgen.c:14953-14954`) sees a non-`VT_CONST` value and bails `expect("constant expression")`, whereas libm builtins fold via `builtin_libm_alias` (`:12689`) and bit builtins via `fold_bit_builtin`.** `int g[__builtin_strlen("abcdef")];`, `_Static_assert(__builtin_strlen("abcdef")==6)`, `case __builtin_strlen("xyz"):`, `static const unsigned long L=__builtin_strlen(...)`, `__builtin_strcmp("a","a")==0`, `__builtin_memcmp("ab","ab",2)==0` all rejected (gcc/clang accept — documented ICE fold). Worse, `char buf[__builtin_strlen("hi")+1]` at block scope silently becomes a runtime-sized VLA, so the SAME expression is a VLA in one context and a hard error in another. Runtime value always correct; only the const-fold is missing. Also makes `__builtin_constant_p(__builtin_strlen("abcd"))` return 0 (clang/gcc: 1) — fixed as a side effect. Fix: add a const-fold hook for these over string-literal args before the bail at `:14954`. `[NEW, MED]`. **TASK: T-mac-30104.**

### <a id="r19-arm64-cas-failorder"></a>arm64 `compare_exchange` ignores the FAILURE memory order — missing acquire on a failed CAS (x86_64/riscv64 correct)
- **[LOW-MED] `parse_atomic` passes BOTH success and failure orders to `__atomic_compare_exchange_N` (template `"aplbmm.b"` `src/mccgen.c:10170`, call `:10378-10382`), but the arm64 runtime CAS helpers branch on the SUCCESS order (w3) only — `runtime/lib/atomic.c` `___atomic_compare_exchange_4` (`:1622` `cbnz w3`), CAS_1 (`:1510`), CAS_2 (`:1567`), CAS_8 (`:1677`), and the `__MCC__`-encoded twins — never referencing the failure order (w4).** `compare_exchange_strong_explicit(p,&e,d, relaxed, acquire)` (legal C17) → arm64 takes the plain `ldxr` (non-acquire) branch on failure, so the failed-CAS reload of `*p` lacks acquire and a dependent load may hoist above it. x86_64 satisfies failure=acquire for free (TSO); riscv64 over-strong (`lr.w.aqrl` unconditional). arm64-only under-barrier, esoteric (inverted weaker-success/stronger-failure pair only); compound-assign and `__sync_*` are always seq_cst, unaffected. Corrects the Round-8 claim that riscv64 helpers "branch on the order arg" — they do NOT (unconditional seq_cst). Fix: arm64 CAS helpers take the ordered `ldaxr/stlxr` path when EITHER w3 or w4 is non-relaxed (`max(success,failure)`, matching libatomic). `[NEW, LOW-MED]`. **TASK: T-mac-30105.**

### <a id="r19-low-cluster"></a>LOW cluster (grouped)
- **[LOW] float→(u)int32 out-of-range/NaN CONST-fold wraps a host 64-bit cast while codegen saturates** — `src/mccgen.c:5451-5461` does `c.i=(int64_t)c.ld` (host) then masks to 32 bits; arm64 runtime uses saturating `FCVTZS/FCVTZU`. `(int)1e30f` → fold `-1`, runtime `INT_MAX`. ISO UB, but mcc's fold ≠ mcc's own codegen, and the folded value is host-dependent (x86 host would give a different wrap) → self-host determinism smell. Fix: replicate target saturating conversion (NaN→0) in the `sf` fold branch.
- **[LOW/latent] `gen_opif` non-finite bail is logically backwards + `ieee_finite(double)` called with `long double`** — `src/mccgen.c:4064` `if(!(ieee_finite(f1)||!ieee_finite(f2)) && !CONST_WANTED)` → by De Morgan bails only when f1 non-finite AND f2 finite; intent was "bail if either non-finite". No wrong result on an IEEE host (host arith is IEEE); latent on x86 80-bit-`long double` hosts where a value in `(DBL_MAX,LDBL_MAX]` truncates to double-inf via the `double`-typed `ieee_finite` (`:983`). Fix: `!(ieee_finite(f1)&&ieee_finite(f2))` + widen `ieee_finite` to long double.
- **[LOW] DWARF: register-passed parameter described at its home stack slot across the whole function (no loclist)** — `src/mccdbg.c:2828-2832,2887-2889` emit a single `DW_OP_fbreg s->c` over `[low_pc,high_pc)`; arm64 `-O0` stores arg regs at `0x8` so a prologue breakpoint reads an unspilled slot. clang emits a loclist (reg over prologue, then fbreg). Fix: emit a `DW_AT_location` loclist.
- **[LOW, fidelity] DWARF: static local hoisted to CU scope (not the subprogram); `_Complex` emitted as an anonymous `structure_type` not a `DW_ATE_complex_float` base type; no `const`/`volatile`/`restrict`/`_Atomic` qualifier DIEs** (`src/mccdbg.c:2880` skips non-VT_LOCAL; struct path `:2506-2600`; grep finds no qualifier DIE tags). Layout/address/value all correct — fidelity/idiom gaps vs clang only.
- **[LOW, codegen-quality] x86_64 `-O2` promotion prologue over-saves/duplicates callee registers** — the promotion callee pool overlaps the leaf pool (`ast_promo_leaf_pool`, `src/mccast.c:2853`), so `rbx`/`r12` are saved twice to distinct slots (4 stores/loads/frame) though one local is promoted. Correct (restores mirror saves) but wasteful. Fix: dedupe the prologue save set to registers actually assigned a promoted local.
- **[LOW/UB-only] x86_64 struct assignment uses forward `rep movs` (memcpy) while arm64/riscv64 use `memmove`** — `gen_struct_copy` (`src/arch/x86_64/x86_64-gen.c:3254-3280`, dispatched `src/mccgen.c:6112-6116`) vs the `vpush_helper_func(TOK_memmove)` fallback (`:6108,6128`); overlapping struct self-assignment (UB) copies forward on x86_64 but overlap-safe elsewhere → cross-backend behavior split. Informational.
- `[NEW]` cluster. **TASK: T-mac-30106.**

### <a id="r19-regalloc-robust"></a>Register-allocation / clobber / spill / liveness — VERIFIED ROBUST (negative result)
No new correctness contradiction across all five backends (`-S` verified via `cmake-cross/mcc-*`). Core invariant: every `gfunc_call` calls `save_regs(nb_args+1)` (`x86_64-gen.c:1682`, `arm64-gen.c:1251`, `riscv64-gen.c:789`, `i386-gen.c:568`, `arm-gen.c:1130`) and inline asm `save_regs(0)` (`mccasm.c:1767`), spilling EVERY live value-stack entry (int+FP) before the call/asm — so the caller-saved set can't disagree with what a call clobbers (it's effectively "everything, spilled"); the only thing kept in a reg across a call is a `-fpromote-locals` value pinned into a genuine ABI callee-saved reg (pools map correctly: riscv64 s1-s11, x86_64 rbx/r12-r15, arm64 x19-x28; prologue/epilogue symmetric). Verified CONSISTENT: FP/vector spilled across calls; two-register (long long/32-bit) both halves spilled; `gv` two-reg alloc can't alias; flags materialized to a GPR before a flag-clobbering sibling call; inline-asm clobbers honored (inputs+outputs avoid clobbered regs, callee-saved clobbers save/restore-wrapped); sub-register aliasing tracked by full index; i386 regparm/fastcall no premature clobber. One LOW quality item (x86_64 -O2 over-save) folded into r19-low-cluster; riscv64 `-fpromote-locals` not firing at -O2 is a missed-opt (INFO, not taskified). `[NEW]` negative.

### <a id="r19-macro-robust"></a>Macro-expansion deep corners (incl. C23 `__VA_OPT__`) — VERIFIED ROBUST (negative result)
A 13-file differential battery (`mcc -E` vs clang-21 vs gcc-16, token-normalized) found no new wrong-expansion divergence. CONSISTENT: rescan/blue-paint/reactivation (mutual+indirect recursion, §6.10.3.4 `t(t)(f)(w)`, call-completed-by-next-expansion, paste-formed-macro-name reactivated + self-limiting paste loop); argument prescan + the `#`/`##` RAW-operand exception (raw stays raw, plain fully expanded, incl. mixed use in one replacement); stringize (escaping, wide/prefixed literals, whitespace collapse, empty→`""`, comma-in-parens, trailing-backslash guard); paste (valid/invalid, empty→placemarker, new-macro-name rescan, left-to-right, pp-number/operator glue); variadic (zero-arg, GNU `,##__VA_ARGS__` deletion, named+variadic, boundary paste); **C23 `__VA_OPT__`** given extra scrutiny — all N3096 §6.10.4.1 examples, `##`-inside, paste-boundary, nested-depth-2, multiple-in-one, `#__VA_OPT__`, emptiness computed on the EXPANDED varargs (`va_args_nonempty:4762`) — all byte-identical to clang+gcc; `defined`-produced-by-macro in `#if`; `__COUNTER__`/`__has_include` in macro args. Only pre-known cosmetic `-E` inter-token whitespace (r7-macro-robust) and fail-fast error recovery. `[NEW]` negative. Source: `src/mccpp.c` `macro_arg_subst2:4826`, `va_opt_subst:4799`, `macro_twosharps:5011`, `macro_subst_nested:5405`.


## Round 20 (2026-08-18) — internal-consistency sweep V: GPU, -run/AOT, bit-fields, variadic, bit-builtins, sequencing (+ full-suite reds)

Six parallel subagents (mac-arm64) on GPU device-codegen, `-run`-vs-AOT, bit-field access-codegen, variadic/stdarg, bit-manipulation builtins, and sequence-point/eval-order/`?:`; PLUS triage of the 3 native failures from a full `ctest` run (the DoD check for T-mac-30063). Reproduced against `cmake-macos/mcc` + cross `-S`, cross-checked vs clang/gcc-16. THREE subsystems returned strong negatives (bit-field access; variadic; GPU shader emission — the two device emitters are deliberately mirrored). Tasks minted: **T-mac-30107..30112**. (The T-mac-30063 DoD full-suite run's only reds were pre-existing/environmental — see r20-suite-reds — so 30063 stands DONE.)

### <a id="r20-section-write"></a>Custom `__attribute__((section("__DATA,x")))` writable data — `-run` correct, AOT SIGBUS on write (valid program)
- **[MED-HIGH] a writable global placed in a custom section runs correctly under `-run` but faults under AOT on the first store.** `find_section` always creates the section `SHT_PROGBITS|SHF_ALLOC`, never `SHF_WRITE`, and discards the `SEG,SECT` writability hint (`src/objfmt/mccelf.c:329`; C attribute path `src/mccgen.c:6392-6397`). AOT `collect_sections` then routes the no-`SHF_WRITE` section to `sk_ro_data` (`src/objfmt/mccmacho.c:1885-1888`) → the read-only `__DATA_CONST` segment (`skinfo[] SG_READ_ONLY` `:1455`) → store faults. `-run` buckets it "ro" but the protect loop never enforces RO for that class (`src/mccrun.c:708-763`), so the page stays writable and the store succeeds. `__attribute__((section("__DATA,mycustom"))) int gv=12345; int main(){gv=99;…}` → `-run` prints 12345/99 rc0 (matches clang), AOT rc=138 SIGBUS. Correct = `-run`/clang. Shares the `find_section`-always-`SHF_ALLOC` root with the KNOWN assembler `.section ...,@type` item (INVESTIGATIONS:171) but the C-attribute path + Mach-O `__DATA_CONST` mis-routing + run-vs-AOT SIGBUS on a VALID program are new (T-mac-30049 is the `-run` rodata-not-protected direction, not this). Fix: `find_section`/`section()` must parse `SEG,SECT`, set `SHF_WRITE` for `__DATA*`/mutable objects. `[NEW, MED-HIGH]`. **TASK: T-mac-30107.**

### <a id="r20-metal-membuf"></a>Metal dispatch omits the binding-2 (heap) buffer that Vulkan auto-provisions — unbound-buffer UB on heap-touching kernels
- **[MED] a kernel that touches the heap/globals/printf-ring emits the `_mem` signature (`device …*memb [[buffer(2)]]`) chosen from the AST (`mcc_gpu.h:655/1940-1943` Metal, `:3241-3246` Vulkan), but the two dispatch paths provision buffer 2 differently.** Vulkan binds it on EVERY dispatch, auto-allocating a 1 MiB zeroed default if the host never provided a heap (`src/mccgpu.c:3016,2795-2799,2666,2809-2820`); Metal binds buffer 2 only `if (mcc_mtl_mem)` (`src/mccgpu.c:738-740`), and `mcc_mtl_mem` is allocated only via the host-facing `mcc_gpu_mem_backend` (`:824`), never from the dispatch path (`mtl_bind_buffers` handles only 0/1, `:663-685`). A heap-reading kernel dispatched without a prior `mcc_gpu_mem()` → Vulkan reads defined zeros; Metal leaves `memb` unbound → driver-level UB (fault → silent CPU fallback, or garbage) that the in-kernel `def`/poison oracle cannot catch. Latent behind the host protocol (which normally calls `mcc_gpu_mem()` first) but the paths diverge with no assertion tying them. Fix: Metal dispatch should `mtl_bind_mem(DEFAULT)` + bind buffer 2 whenever the pipeline needs mem, mirroring Vulkan. `[NEW, MED]`. **TASK: T-mac-30108.**

### <a id="r20-parsedepth-sizeof"></a>Deeply-nested `sizeof` chain SIGSEGVs (guard page) instead of the depth diagnostic at a constrained stack
- **[MED, robustness] the `diag.parse-depth` cell (which lowers the stack rlimit to 1024 KiB to prove the depth diagnostic — not the guard page — is reached) fails: `sizeofchain` SIGNAL rc=139 (SIGSEGV), 1 of 17 cases.** The parse-depth cap (`MCC_MAX_PARSE_DEPTH`=512, `src/mccgen.c:252`) is too permissive for the per-frame stack cost of the `sizeof`-operand recursion path, so a `sizeof(sizeof(sizeof(...)))` chain overflows the stack before the depth counter trips. Prior parse-depth work (T-lin-10304 "five holes") did not cover the sizeof-chain frame size. This is a real robustness gap (crash on adversarial/pathological input at a normal stack limit); pre-existing, independent of T-mac-30063. Fix: lower the effective depth cap for the sizeof/unary path, or account for its frame size in the budget, so the diagnostic fires first. `[NEW, MED]`. **TASK: T-mac-30109.**

### <a id="r20-clz-zero"></a>Runtime `__builtin_clz`/`ctz` zero-argument edge disagrees with the const-fold path (and clang)
- **[LOW-MED] `clz(0)`/`ctz(0)` (UB) return DIFFERENT values in the fold vs runtime vs native paths — three answers for the same expression.** Fold (`fold_bit_builtin`, `src/mccgen.c:906/912-921`): `clz(0)`→W (32/64), `ctz(0)`→W. Runtime de Bruijn helpers (`runtime/lib/builtin.c` `CLZI/CLZL:44-58`, `CTZI/CTZL:40-43`) don't special-case 0: `clz(0)`→table[0]=31/63, `ctz(0)`→table[0]=0/0. x86 native `bsf/bsr` (`x86_64-gen.c:2645-2657`) leave dest undefined for 0. clang is self-consistent (32/32). Input is UB so severity capped, but mcc contradicts ITSELF. INVESTIGATIONS:277 marked bit-builtins ROBUST "via fold_bit_builtin" — that missed the runtime zero-edge. Distinct from T-mac-30047 (lowering/link axis). Fix: guard `x==0` in CLZI/CLZL/CTZI/CTZL to return the bit width, matching fold. `[NEW, LOW-MED]`. **TASK: T-mac-30110.**

### <a id="r20-seqpoint"></a>`-Wsequence-point` misses read-vs-write conflicts and is reset per function argument
- **[LOW-MED, diagnostic-only] `seqp_check` warns only when `writes>=2` (`src/mccgen.c:458`), never for a write unsequenced against a value COMPUTATION (C 6.5p2), though reads ARE recorded (`gv` `:2629`) and used only for de-dup; and `seqp_flush` runs after EACH function argument (`:14356,14380`), hiding inter-argument conflicts.** `a[i]=i++`, `x=i+i++`, `x=++i-i`, `a[i++]=i`, `f(i,i++)`, `f(i++,i++)` all compile clean under `-Wsequence-point` (gcc-16/clang all warn — genuine 6.5p2 UB). No miscompile (codegen matches clang); the flag just under-reports the UB it advertises. Fix: warn when a WRITE coexists with any other event (read or write) on the same object, and flush once after all args (not per-arg). `[NEW, LOW-MED]`. **TASK: T-mac-30111.**

### <a id="r20-low-cluster"></a>LOW cluster (grouped)
- **[LOW, test-portability + coverage] `objsec-reject-malformed` false-FAILs on macOS** — the T-mac-30023 regression guard (`tests/objsec/reject-malformed.sh`) crafts a GNU-`ar` symbol-table malformation (60-byte header + `nsyms*4` surgery), but macOS uses BSD `ar` + `__.SYMDEF`, so the surgery doesn't produce the targeted malformation → mcc accepts (rc=0) → "mcc accepted a malformed archive". NOT a security regression: the BSD reader `mcc_load_alacarte_bsd` IS bounded (`ranbytes`/`strbytes`/`strx` all checked, `src/objfmt/mccelf.c:3736-3790`). But the test gives a false red on every mac suite run, and the malformed-BSD-archive path is untested on mac. Fix: skip cleanly on non-GNU-`ar` hosts, or add a BSD-`__.SYMDEF` malformed case.
- **[LOW, soundness] `-run` resolves undefined externals to mcc-internal globals via `RTLD_DEFAULT`** — `mccrun.c:797` (`do_resolve=1`) + `mcchost.c:1462` `dlsym(RTLD_DEFAULT)` sees the compiler's own 545 exported globals; AOT (`mccmacho.c:2549` `do_resolve=0`) correctly errors "unresolved reference". `extern int ast_count(void); main(){return ast_count();}` → `-run` binds mcc's internal `ast_count` (crash) vs AOT link error. Contrived (mcc globals are prefixed, no common-identifier collisions) but a real run-vs-AOT soundness split. Fix: restrict `-run` external resolution to a libc/allowed set.
- **[LOW, guarded] GPU host-pointer import (`mcc_gpu_mem_import`) works on Vulkan, refused-with-reason on Metal** (`src/mccgpu.c:3129-3138` vs `:833-846`) → a kernel on an imported host region device-runs on Vulkan, always CPU-falls-back on Metal. Returns a `why` string so callers gate; likely intended platform limitation.
- **[NOTE, resolved] `mcc_cross_build` full-suite FAIL was a STALE `cmake-cross` dir** — after `rm -rf cmake-cross` + `cmake --preset cross` + build it is green (rc=0); my Round-work bitint edit compiles clean for cross (line 135; the `:511/:775` `-Wpointer-sign` warnings are pre-existing, cross is not `-Werror`). Environmental, not a code defect; no task.
- `[NEW]` cluster. **TASK: T-mac-30112.**

### <a id="r20-suite-reds"></a>T-mac-30063 DoD full-suite run — the only native reds were pre-existing/environmental (30063 stands DONE)
A full `ctest -j8` on cmake-macos (confirming T-mac-30063's DoD) finished with exactly 3 native failures, all independent of the bitint sign-ext fix (o0-baseline object-identity already proved zero codegen drift): `diag.parse-depth` (sizeof-chain SIGSEGV → T-mac-30109), `objsec-reject-malformed` (GNU-`ar` test portability → T-mac-30112), `mcc_cross_build` (stale cross dir, resolved). Everything else passed; the many "Not Run" cells are environment-gated (docker/wine/macho fixtures). T-mac-30063's slice + relevant families (bitint/wide256/exec) + o0-baseline + rir-coverage were already green.

### <a id="r20-bitfield-access-robust"></a>Bit-field access-codegen (extract/insert) — VERIFIED ROBUST (negative result)
No defect and no backend/fold-vs-runtime divergence across all 5 backends (run on arm64 + cross `-S`), byte-identical to clang. The load paths emit `TOK_SAR` for the sign-extension shift (`load_packed_bf:2460`, `_rev:2547`, aligned `:2704`) and `gen_op` rewrites `SAR→SHR` for unsigned combined types (`src/mccgen.c:4886-4888`), so signed fields sign-extend and unsigned zero-fill; store RMW masks to `bit_size`, shifts to `bit_pos`, preserves neighbors (`:6167-6185`). Verified: signed/unsigned full-width, wide base (`long long:40`, `_BitInt` fields), genuine storage-unit STRADDLE (packed), reverse `scalar_storage_order`, write overflow-truncate + neighbor preservation, `++`/compound-assign RMW, read-once lvalue index, volatile access WIDTH matches clang, static-init overflow. Minor non-defects: arm64 two dead straddle instructions; riscv64/i386 unaligned wide access for packed straddle (style, value-correct). `[NEW]` negative.

### <a id="r20-variadic-robust"></a>Variadic / stdarg — VERIFIED ROBUST (negative result)
No new wrong-value divergence across x86_64/arm64/riscv64/i386/arm (native arm64 run vs Apple clang -O0/-O2 byte-identical; cross `-S` all backends). Verified: struct-by-value va_arg (s8/s16/s24/indirect), double/float from stack, `long double` quad, `_BitInt(65)`/`(128)`, sub-int promotion, over-aligned aggregates, boundary (8 fixed + varargs), nested variadic, `va_copy` independence, `va_list` passed to a helper (Darwin by-value semantics match clang). x86_64 SysV prologue offsets match `__va_arg_inline` bounds; riscv64 forces variadic FP→GPR + even-pair alignment for variadic-only; arm32 hard-float variadic falls back to base standard; i386 generic char* walker. Only known bugs remain (T-mac-30045: arm64-ELF size==16/align==16 non-HFA `assert(0)` at `arm64-gen.c:1728` — arm64-Darwin handles the identical case correctly, confirming it's an ELF-only codegen omission; x86_64 `fp_offset+=16`-before-capacity). `[NEW]` negative.


## Round 21 (2026-08-18) — internal-consistency sweep VI: inline emission, _Complex, struct-return, over-align, TLS models, cleanup

Six parallel subagents (mac-arm64) on inline-function emission model, `_Complex` runtime+ABI, struct/union RETURN ABI, over-aligned-local/stack-realign, TLS access-model selection, and `__attribute__((cleanup))` scope-exit. Reproduced against `cmake-macos/mcc` + `cmake-cross/mcc-*` (relocs via llvm-readobj), cross-checked vs clang/gcc-16 (incl. mcc↔clang cross-link on arm64). Two subsystems returned strong negatives (`_Complex` runtime/ABI; over-aligned-local indirect path). Tasks minted: **T-mac-30113..30121**.

### <a id="r21-inline-emission"></a>GNU `extern inline` emits a private local copy (+ `always_inline` wrongly triggers no-emit) — miscompile / link error
- **[HIGH] `extern inline` under GNU semantics is rewritten to `STATIC|INLINE` (`src/mccgen.c:18543-18545`) and `gen_inline_functions` (`:18162`) then emits a PRIVATE local out-of-line copy in every referencing TU, instead of emitting nothing and leaving the un-inlined reference EXTERNAL.** The glibc/kernel idiom (`extern inline __attribute__((gnu_inline)) int k(){…}` header hint + one canonical `int k(){…}` def in a `.c`) → mcc calls its own private copy: `k()`=1 (hint body) vs gcc/clang 2 (canonical), at -O0 and -O2; `&k` differs across TUs (mcc same=0, gcc/clang same=1); two gnu-extern-inline TUs with no canonical def link cleanly under mcc (fabricated `t _k`) but correctly fail under gcc/clang. `nm`: mcc `t _k`, clang/gcc `U _k`. Corrects the stale INVESTIGATIONS:255 "robust" claim (it verified only the flip DIRECTION, not the emitted symbol). **[MED, same site] `sym->f.func_alwinl` is OR'd into the `:18544` GNU-flip condition**, so a C99 `extern inline __attribute__((always_inline)) int q(){…}` (which IS the C99 external definition) emits nothing → `unresolved reference to '_q'` when a sibling's call isn't inlined (gcc/clang: `T _q`, links, correct). Fix: in the GNU branch drop `VT_INLINE` WITHOUT setting `VT_STATIC` and emit no body (keep the ref external); remove `func_alwinl` from the `:18544` condition (keep only `gnu89_inline_semantics()||func_gnuinl`). `[NEW, HIGH]`. **TASK: T-mac-30113.**

### <a id="r21-static-inline-used"></a>`static inline __attribute__((used))` unreferenced is silently not emitted
- **[MED] `gen_inline_functions`' emit predicate (`src/mccgen.c:18162-18163`, `!(VT_INLINE) || ((VT_STATIC) && sym->c)`) consults only `sym->c` (was-referenced), never `sym->a.used`, so an unreferenced `static inline __attribute__((used))` helper is dropped** (mcc `nm`: absent; gcc/clang: `t _used_si`). `used` is exactly the attribute meant to force emission (helpers referenced only from inline asm, or kept for a debugger) → link failure / missing symbol. mcc DOES honor `used` for a non-inline static. Fix: add `|| sym->a.used` (and force the emit path) to the predicate. `[NEW, MED]`. **TASK: T-mac-30114.**

### <a id="r21-riscv-structret"></a>riscv64 aborts (SIGABRT) returning a mixed int+float struct from any non-local lvalue
- **[HIGH] `arch_transfer_ret_regs` asserts `vtop->r == (VT_LOCAL|VT_LVAL)` (`src/arch/riscv64/riscv64-gen.c:1001`), handling only a plain stack-local — so returning a GPR+FPR-pair struct (`{float;int;}`, `{int;float;}`, `{double;long;}`, `{char;float}`, `{float;double}`, …) from a global / `*p` / a call result / a ternary hard-aborts** (`Assertion failed …:1001`, exit 134, at -O0/-O1, both `-S` and `-c`; assertions are compiled into shipped binaries). The classification is CORRECT (`gfunc_sret` returns the `-1` mixed marker `:982-985`, generic dispatch routes it `src/mccgen.c:15014-15017`, and a plain-local return emits the right `flw fa0`+`lw a0`); only value-materialization is missing. x86_64 (`x86_64-gen.c:1424-1490`) and arm64 materialize arbitrary lvalues first. `FI f(void){return g;}` for `struct{float a;int b;}` crashes; `return (local)` works. Fix: port the x86_64 address-materialization prologue into riscv64 `arch_transfer_ret_regs` (or spill to a scratch local before the split), then relax the `:1001` assert. `[NEW, HIGH]`. **TASK: T-mac-30115.**

### <a id="r21-riscv-tls"></a>riscv64 dynamic TLS: `__thread` unbuildable in a shared object + `-fPIC -c` emits non-relocatable local-exec
- **[HIGH] gen/linker contradiction — a `.so` with `__thread` cannot be built by any route.** Gen emits GD (`R_RISCV_TLS_GD_HI20`) only for `pic && (DLL) && !EXE` (`src/arch/riscv64/riscv64-gen.c:170-171`), but the linker REJECTS exactly that (`src/arch/riscv64/riscv64-link.c:443-444` "R_RISCV_TLS_GD_HI20 to shared object not supported") AND rejects local-exec TPREL in a DLL (`:412-415`). `__thread int x=5; int f(){return x;}` → `mcc-riscv64 -fPIC -shared` errors; compile-then-`-shared` also errors. arm64 identical flow links (tlsdesc). **[HIGH, related] the model is gated on `output_type & DLL`, not `pic`** (`:170-171`), so `extern __thread int x; return x;` compiled `-fPIC -c` emits `R_RISCV_TPREL_HI20/LO12_I` (local-exec, fixed TP offset) → wrong when `x` lives in another module (arm64 keys on `pic`, emits TLSDESC for `-fPIC -c`). Fix: gate GD/tlsdesc on `mcc_state->pic`; implement dynamic-TLS resolution (`R_RISCV_TLS_DTPMOD64/DTPREL64`) for DLL output instead of erroring. `[NEW, HIGH]`. **TASK: T-mac-30116.**

### <a id="r21-tls-initial-exec"></a>No backend emits initial-exec TLS — default-visibility `extern __thread` in a dynamic exe gets local-exec
- **[MED-HIGH] every ELF backend emits local-exec (or GD under PIC) for a default-visibility `extern __thread`, never initial-exec (`GOTTPREL`/`TLSIE`), though the arm64 linker already SUPPORTS IE (`src/arch/arm64/arm64-link.c:480-513`) — the generator never emits it (`arm64-gen.c:651-664` emits `TLSLE_ADD_TPREL` for defined, hidden-extern, AND default-vis-extern alike).** In a non-PIC dynamic executable a default-vis extern `x` may be defined in a shared library whose TP offset is unknown at static-link time; local-exec bakes a wrong offset. clang emits `:gottprel:x` (IE) for the default-vis extern, LE only for defined/hidden. arm64 analog of x86_64 T-mac-30043, distinct (this is the MODEL-SELECTION policy: extern-default-vis should be IE). Fix: emit IE (`R_AARCH64_TLSIE_*`/`R_X86_64_GOTTPOFF`/`R_RISCV_TLS_GOT_HI20`) for default-vis external TLS in EXE output; keep LE for defined/hidden/static. `[NEW, MED-HIGH]`. **TASK: T-mac-30117.**

### <a id="r21-tls-addr-staticinit"></a>`&__thread_var` accepted as a load-time-constant static initializer → wrong pointer (all backends)
- **[MED] `init_putv`'s load-time-computability guard (`src/mccgen.c:16907`) doesn't reject `VT_TLS`, so `__thread int t=77; int *p=&t;` is accepted and emits a plain absolute reloc against the TLS symbol** (`:17002` 64-bit / `:17016` 32-bit → `R_X86_64_64`/`R_RISCV_64`/`R_AARCH64_ABS64 _t`), giving `t`'s link-time section offset, NOT its thread-local runtime address (verified native: `p=0x104470008` but `&t=0x600002ffc030`, `p!=&t`). clang/gcc REJECT it ("initializer element is not a compile-time constant" — a TLS address is TP-relative, runtime-only). Fix: in `init_putv`, if the symbol is `VT_TLS`, raise the not-a-constant error. `[NEW, MED]`. **TASK: T-mac-30118.**

### <a id="r21-arm-alignas"></a>arm32 `_Alignas(N>=512)` local drops the realignment mask (NOP) → under-aligned + no stack reserved
- **[MED] `arm-gen.c:2354` emits `bic sp, r0, #(align-1)` via `stuff_const(0xE3C0D000|(r<<16), align-1)`, but `stuff_const` (`:105-152`) returns 0 when `align-1` isn't an ARM 8-bit rotated immediate (encodable only for align≤256), so the instruction assembles to the zero word `andeq r0,r0,r0` = NOP.** That single instruction is the ONLY one that both aligns AND writes back to `sp`; without it, `sp` is never decremented (no allocation) and the stored local pointer is the un-decremented, unaligned `sp` (aliases live frame data; next call clobbers the "local") — worse than mere misalignment. `_Alignas(512) int x; use(&x);` → `sub r0,sp,r0; andeq r0,r0,r0; str sp,[r11,#-8]`. Threshold: 256 ok, 512 NOP. Fix: materialize the mask in a register (like riscv64) or `stuff_const_harder`; at minimum diagnose (arm64 errors). `[NEW, MED]`. **TASK: T-mac-30119.**

### <a id="r21-macho-x64-linker"></a>x86_64 Mach-O internal linker mis-relocates clang-compiled rodata FP constants (+ clang-main link produces no output)
- **[MED] mcc's x86_64 Mach-O internal linker reads clang-compiled `.o` rodata FP constants as 0.** A clang `float retk(float x){return x+3.0f;}` object linked by `mcc-x86_64-osx` → `retk(10)` returns 10 not 13 (the `3.0f` rodata constant relocates to 0); and `clang-main + mcc-lib` linked by mcc produces no output even for plain `int`/`double` (startup/PLT issue). Surfaced while verifying `_Complex` cross-linking (masqueraded as an ABI bug — it is NOT; `_Complex` is robust). arm64 cross-linking mcc↔clang works both directions. Read-through-linker defect on the supported x86_64-macOS target. Fix: audit the x86_64 Mach-O object reader's rodata/`__literal4`/`__literal8` section + `X86_64_RELOC_*` handling for clang-produced objects. `[NEW, MED]`. **TASK: T-mac-30120.**

### <a id="r21-cleanup-jumps"></a>`__attribute__((cleanup))` jump-constraint gaps — computed `goto*` skips the handler; jump-bypasses-init undiagnosed
- **[MED] computed `goto *p` out of a cleanup scope silently SKIPS the handler → resource leak, no diagnostic.** The indirect-goto branch (`src/mccgen.c:16306-16311`) just calls `ggoto()`; unlike the labeled-forward (`:16324-16330`) and backward (`:16335`) branches it runs NO cleanup analysis. `{int x __attribute__((cleanup(dtor)))=7; goto *l;} done:` → handler never runs (gcc/clang: hard error). **[LOW-MED, same root] a jump that BYPASSES a cleanup var's initializer is not diagnosed → the handler fires at scope exit on the INDETERMINATE value (wild-free).** mcc guards jumps into VLA scopes (`:16290-16302,16333-16334,16375-16376`) but has no cleanup analog: `goto label; { int x __attribute__((cleanup(dtor)))=500; label:; }` runs `dtor(&garbage)`; a `switch` whose case labels straddle a cleanup decl likewise. gcc/clang: hard error. `cleanup` is otherwise FULLY implemented and correct for all valid code (verified ~20 exit paths at -O0/-O2/-run: normal/return/break/continue/forward+backward goto/reverse order/final value/VLA-interaction/longjmp-skips/stmt-expr). Both share one root: the jump/scope constraint analysis handles VLAs but not cleanup vars. Fix: extend that check to fire when a crossed scope contains a `cleanup` variable (and run cleanup on the indirect-goto edge). `[NEW, MED]`. **TASK: T-mac-30121.**

### <a id="r21-complex-robust"></a>_Complex runtime arithmetic & ABI — VERIFIED ROBUST (negative result)
No runtime-value or ABI contradiction between backends or vs clang/gcc (arm64 native + x86_64 under Rosetta; mcc↔clang cross-link both directions on arm64; gcc-16 + clang-21 dual oracle). ABI verified: `_Complex float`/`double`/`long double` pass+return (x86_64 SSE eightbyte `x86_64-gen.c:1193`, arm64 2-elt HFA `arm64-gen.c:978`, x86_64 long double memory+st0:st1), register-exhaustion stack spill, `_Complex` in structs/HFAs, variadic `va_arg`. Arithmetic (`* / += -= *= /= == !=`, unary-, `__real__`/`__imag__` lvalues, mixed real↔complex, `CX_LIMITED_RANGE`, `_Complex int`): `* /` over inf/nan/divzero/Smith-scaling inputs mcc==gcc-16 EXACTLY (clang is the outlier). Two LOW non-bugs: `_Complex float` divide sign-of-zero in the inf/underflow corner is arch-inherent (mcc==clang==gcc per-backend); `double`/`long double` divide ~1-ULP vs gcc's `__divdc3` (Annex-G-unspecified; mcc closer to gcc than clang). Helpers `__mcc_cmul/cdiv{,f,l}` + `__mul/div{s,d,x,t}c3` wrappers correct (reconfirms r17-helper-abi-robust). `[NEW]` negative.

### <a id="r21-overalign-robust"></a>Over-aligned local / stack realignment — VERIFIED ROBUST (indirect path; one arm32 bug)
The indirect over-align path (`align>STACK_OVERALIGN_MAX`, routed through `gen_vla_alloc`, `src/mccgen.c:17570-17629`) masks SP correctly on every backend and restores on scope exit (x86_64/arm64/riscv64/i386 all verified `-S`); the direct path's frame-base alignment guarantee holds (rbp/fp/s0 16-aligned, arm r11 8-aligned). ~20 arm64 native runtime `&x&(N-1)==0` probes pass: `_Alignas` 16..8192, `aligned(256)`, over-align+VLA, +alloca, in-loop, two over-aligned locals, across setjmp/longjmp, nested, deep recursion, over-aligned struct + member. Large vectors require only 16-byte align (`_Alignof(vector_size(32/64))`=16 on mcc AND clang). The only defect is arm32 `_Alignas(>=512)` (r21-arm-alignas); the i386 8-byte direct-path under-align folds into T-mac-30051. `[NEW]` negative.


## Round 22 (2026-08-18) — internal-consistency sweep VII: enum, volatile, _Generic, intrinsic-builtins, determinism, wide-strings

Six parallel subagents (mac-arm64) on enum underlying-type/value codegen, `volatile` access semantics, `_Generic` selection, compiler-intrinsic builtins, object-emission determinism, and wide/char16/char32/u8 string+char codegen. Reproduced against `cmake-macos/mcc`, cross-checked vs clang-21/gcc-16 (values, `-S` instruction counts, byte images). One subsystem returned a strong negative (object-emission determinism). Tasks minted: **T-mac-30122..30126**.

### <a id="r22-volatile"></a>`volatile` access defects — discarded-value reads elided; volatile struct assignment lowered to `memmove`
- **[HIGH] a `volatile` lvalue read whose value is DISCARDED emits no load at all (at -O0 AND -O2)** — a volatile lvalue is only materialized (`gv()`) when consumed, so `x;`, `(void)x;`, the left operand of a comma, and a bare deref `*p;` drop the mandatory access. Discard sites with no `gv()`: statement discard `src/mccgen.c:16415` (note `:16412` already special-cases `VT_VOLATILE` to SUPPRESS the unused-value warning — the front end sees the volatile-ness but still drops the access), cast-to-void `:5263-5264` (`if(dbt_bt==VT_VOID) goto done;`), comma-left `:14922`. `volatile int x; (void)x;` → mcc emits ZERO loads (clang emits `ldr wzr,[x8]` for each; the used-read `sink=x;` correctly loads once). Breaks the ubiquitous read-to-clear MMIO idiom. Fix: at each discard site, if `(vtop->r&VT_LVAL)&&(vtop->type.t&VT_VOLATILE)`, `gv()` before dropping. **[MED-HIGH] a `volatile` struct/union assignment is lowered to a `memmove` call** — the aggregate-assignment path (`src/mccgen.c:6098-6138`) has no `VT_VOLATILE` check, so `volatile struct S vs1,vs2; vs1=vs2;` becomes `bl _memmove` (a volatility-unaware byte-blob copy + a call), identical to the non-volatile copy; clang/gcc emit inline element-wise volatile loads/stores. Fix: in the `VT_STRUCT` assignment path, when either operand is `VT_VOLATILE`, emit an inline field/word-granular volatile copy. Strong negatives (verified -S at -O2 vs clang): no CSE-merge of two volatile reads, no loop-hoist (`while(v){}` re-reads), no DCE of a dead volatile store, `?:`/`&&` arm-selection loads exactly once on the taken path, store ordering preserved. `[NEW, HIGH]`. **TASK: T-mac-30122.**

### <a id="r22-enum-u64"></a>Full-64-bit unsigned enumerator (`~0ULL`) sized as `int` → wrong sizeof/struct-layout/ABI
- **[MED] an enumerator value > `LLONG_MAX` that is a sign-extension of a negative int32 (`enum E{MASK=~0ULL};`) leaves the enum sized as `int` (4 bytes) instead of `unsigned long long` (8).** `expr_const64()` returns a signed `long long`, so `~0ULL` becomes `-1`; the min/max scan (`src/mccgen.c:7355,7386-7389`) records `nl=-1,pl=0`, both satisfy `pl==(int)pl && nl==(int)nl` (`:7413-7419`), so the type stays `VT_INT`. `sizeof(enum E)`=4 (clang/gcc 8), `struct{char c; enum E e;}`=8/offsetof 4 (clang/gcc 16/8), `_Generic` picks int (clang/gcc ullong). The VALUE round-trips only by accident (exact sign-extension of the low 32 bits); the SIZE/layout/ABI is wrong. Also fires in mixed lists (`{A=10,B=0xFFFFFFFFFFFFFFFFULL}`). Secondary LOW facet (same root): the pre-C23 int-range pedwarn (`:7380`) also misfires (no warning for `~0ULL` under `-std=c17 -pedantic`; clang/gcc both warn). Fix: capture the enumerator's true unsignedness (a `need_ull` flag when `expr_const64` yielded an unsigned value with bit 63 set) and force `VT_LLONG|VT_LONG|VT_UNSIGNED`. Distinct from enum DIAGNOSTICS (T-mac-30060) and the enum DWARF DIE (INVESTIGATIONS:186). Strong negatives: the whole boundary ladder (`0x7fffffff`→`0x100000000`→`0x7fffffffffffffff`, negatives mixed with large positives, auto-increment across INT_MAX/2^32, `-fshort-enums`/packed, C23 fixed-underlying wrap) all match clang/gcc. `[NEW, MED]`. **TASK: T-mac-30123.**

### <a id="r22-generic-enum"></a>`_Generic` association with an elaborated `enum TAG` misparsed as a C23 fixed-underlying-type
- **[MED] `_Generic((enum E)A, enum E:2, default:9)` is rejected `error: enum type expected` — valid C11.** After reading the enum tag in an association, `struct_decl_nested` (VT_ENUM branch, `src/mccgen.c:7303-7308`, reached via `:14037-14038`→`parse_btype`) sees the association separator `:` and misreads it as the C23 `enum E : underlying` introducer, `next()`s past it, and calls `parse_btype` on the result token (`2`) → `expect("enum type")`. Always a hard error (never a wrong branch — the post-`:` token never starts a valid type). `struct`/`union` associations and single-token typedef names are unaffected. clang/gcc accept and select `2`. Fix: only consume `enum TAG :` as a fixed-underlying-type in a declaration context (or peek that the post-`:` token starts a type-specifier) before `next()`-ing past `:`. Reconfirms the INVESTIGATIONS:218 `_Generic` robust set (qualifier/`_Atomic` strip, non-evaluation, decay, `_BitInt` width-distinctness). `[NEW, MED]`. **TASK: T-mac-30124.**

### <a id="r22-intrinsic-builtins"></a>Intrinsic-builtin defects — `__builtin_expect` result type; `__builtin_assume` no-op; spurious `-Wunused-value`
- **[MED] `__builtin_expect(e,c)` leaves the result with `e`'s type instead of `long`** — `src/mccgen.c:13214-13224` evaluates `e`, pops the hint, and leaves `e` on `vtop` uncast. `sizeof(__builtin_expect((char)1,0))`→1 (clang/gcc 8), `_Generic` picks char (clang/gcc long). Value + side effects of `e` are correct; only the type (affecting `sizeof`/`_Generic`/`typeof` and surrounding width/signedness) is wrong vs BOTH oracles. Fix: `gen_cast` the result to `long`/`VT_LLONG`. **[LOW-MED] `__builtin_assume` is a no-op macro `((void)0)` (`runtime/include/mccdefs.h:1056`, no `TOK_builtin_assume`)** — the operand is never parsed/typechecked (`__builtin_assume(undeclared>0)` compiles clean; clang errors) and no optimizer hint is fed. (`__assume` absent → errors, matching clang/gcc.) Fix: make it a real intrinsic that parses its operand under `nocode_wanted` and feeds the assumption. **[LOW] spurious `-Wunused-value` on `__builtin_unreachable()`/`__builtin_trap()` statements** — both push `VT_VOID` without setting `expr_has_effect` (`:13295-13300`/`:13419-13426`); the warning site (`:16412`) has no VT_VOID exclusion. `if(!x)__builtin_unreachable();` under `-Wall` warns (clang/gcc silent). Fix: set `expr_has_effect=1` in those arms or exclude VT_VOID at `:16412`. Strong negatives: `__builtin_choose_expr` (dead arm not evaluated, chosen-arm lvalue-ness + exact type preserved), `types_compatible_p`, `constant_p`, `assume_aligned`, `prefetch`, `trap` (rc133) all match both oracles. `[NEW, MED]`. **TASK: T-mac-30125.**

### <a id="r22-u8-char"></a>`u8` character constant accepts a non-ASCII/multibyte char with a spec-violating self-inconsistent value
- **[MED] `u8'é'` (and `u8'é'`, `u8'€'`, `u8'\U0001F600'`, `u8'ab'`) is silently accepted** — the u8 char path (`src/mccpp.c:3365-3366`, `char_size=1`) falls into the non-`U` accumulation loop (`:3386-3400`) with NO single-code-unit constraint check for `prefix=='8'`, computing `c=(0xc3<<8)|0xa9=0xc3a9` and storing it while typing the constant `char8_t` (`sizeof`=1, `mccgen.c:12849`). Internal contradiction: `(int)u8'é'` observes `0x0000c3a9` (50089) but a `char8_t v=u8'é'` or `return u8'é'` yields `0xa9` (169). C23 §6.4.4.4 requires a single UTF-8 code unit; gcc-16 errors "character not encodable in a single code unit", clang "character too large". Valid `u8'A'`=65 works. Fix: in `parse_string`, when `prefix=='8'`, error if the decoded sequence yields >1 byte / any code unit >0x7F. Distinct from T-mac-30040 (escape/`u''`/`U''` surrogate items) and the "`u8'x'` ROBUST" note (valid single-byte only). Strong negatives: astral UCN across L/u/U (UTF-16 surrogate pairs, UTF-32, UTF-8 multi-byte) byte-exact; element width/count per data-model; same-prefix + defined-merge concatenation; mixed-prefix concat errors; wide-string array init; BMP char constants; wide multichar matches gcc. `[NEW, MED]`. **TASK: T-mac-30126.**

### <a id="r22-determinism-robust"></a>Object-emission determinism — VERIFIED ROBUST (negative result)
No reproducibility defect beyond the known excluded items. Byte-identical across SEPARATE processes (different ASLR/heap each run) and time delays: `mcc -c` (ELF), `-c -g` (ELF+DWARF), a 150-scrambled-symbol/30-fn stress source, `-O2 -c` (0/20 differ), and the Mach-O ad-hoc-signed executable (varies only when the output BASENAME changes — it's the code-signature identifier — expected). All qsort comparators are total orders on deterministic keys (machosymcmp tie-breaks on original ELF index `mccmacho.c:~1045`, triecmp/bind_rebase_cmp/sym_cmp/case_cmp/sort_eh_table); no time/random/pid/uuid in any emitter (no LC_UUID, no build-id); `section_realloc` zeroes all padding (`mccelf.c:277`); ELF hash is name-keyed; `anon_sym`/`DW_AT_producer`/`MCC_VERSION_STR` all deterministic. Two informational cross-ENV notes (not same-env defects, match gcc/clang defaults): `DW_AT_comp_dir`=`getcwd()` + `STT_FILE`=input path under `-g` (no `-ffile-prefix-map`), and the Mach-O code-sig identifier = output basename. PE emission unreachable on this build (read-verified only). `[NEW]` negative.



## Round 23 (2026-08-18) — internal-consistency sweep VIII: nested-fns, restrict, naked/top-asm, pack, typeof, labels

Six parallel subagents (mac-arm64) on GNU nested functions, `restrict`/alias-optimization soundness, file-scope `asm`/`__attribute__((naked))`, `#pragma pack` stack semantics, `typeof`/`__auto_type`/C23 `auto`/`constexpr` inference, and label-scope/`__label__`/goto. Reproduced against `cmake-macos/mcc` + `cmake-cross/mcc-*`, cross-checked vs clang/gcc-16. TWO subsystems returned strong negatives (nested functions cleanly rejected; the optimizer is alias-sound by construction). Tasks minted: **T-mac-30127..30130**.

### <a id="r23-naked"></a>`__attribute__((naked))` unsupported → full prologue emitted → stack corruption
- **[HIGH] `naked` is not a recognized token (grep `naked` over src/ = nothing); it hits the attribute `default:` arm (`src/mccgen.c:6682`, `mcc_warning_c(warn_attributes)("'%s' attribute ignored")`, silenced by `-w`), and `gfunc_prolog` unconditionally emits the frame (`arm64-gen.c:1500`, `x86_64-gen.c:983,1958`) — no `f.naked` field, no suppression path.** A naked function's hand-written asm body then runs with an mcc-emitted prologue: `__attribute__((naked)) int add42(int x){ __asm__("add w0,w0,#42\n\tret\n"); }` → mcc `-w -S` emits `stp x29,x30,[sp,#-224]!` … then the body's `ret` returns to the caller with `sp` never restored → stack corruption (runtime prints `142` then garbage; clang emits ONLY the asm body). Same on x86_64. Sub-items (same fix): [MED] the return-value check runs on a naked body → spurious `error: function might return no value` (only `-w` gets past it, then miscompiles); [LOW] a naked function with a non-asm C body is silently accepted as an ordinary function (gcc/clang error). File-scope `asm("...")` is CORRECT (positive: `asm_global_instr` `mccasm.c:1812`, emitted into `.text` in source order). Fix: add a `naked` token + `f.naked` flag; skip prologue/epilogue/param-spill/return-check in both backends when set; reject a non-asm naked body. `[NEW, HIGH]`. **TASK: T-mac-30127.**

### <a id="r23-pack-underalign"></a>`#pragma pack(N)` + member `aligned(K < natural)` silently UNDER-aligns the member (ABI mismatch)
- **[MED] with `#pragma pack` active, a member `__attribute__((aligned(K)))` where K is SMALLER than the type's natural alignment is applied (lowering alignment) instead of ignored.** `#pragma pack(8) struct S{char c; int x __attribute__((aligned(2)));};` → mcc `sizeof=6, offsetof(x)=2, _Alignof=2`; gcc/clang `8/4/4` (aligned only RAISES; K=2<natural 4 is ignored; pack(8)≥4 caps nothing). Root: the member-align guard `if (a && (a > align || … || pragma_pack)) align = a;` (`src/mccgen.c:6910-6912`) fires purely on the `|| pragma_pack` disjunct even when `a (2) ≤ align (4)`, so `align = a` LOWERS below natural. The pcc small-aligned cancellation (`:6906-6907`) doesn't zero `a` because `pragma_pack(8) < a(2)` is false. Distinct from the KNOWN no-pack LOW note (INVESTIGATIONS:436, `mccgen.c:7526` — that path correctly keeps natural align, no ABI impact); this is the pack-active path where the too-small value IS applied and DOES change the ABI. Correct rule: effective member align = `min(pack, max(natural, aligned_K))`. Fix: only apply an explicit `aligned` when it raises (`a > align`); drop the `|| pragma_pack`/`|| packed` disjuncts at `:6910-6912`. Strong negatives: push/pop nesting, stack-depth bound (PACK_STACK_SIZE=8), `pack()` reset, pack cancels a smaller member-aligned in gcc/pcc mode, pack + bitfields, nested packed/aligned structs, pack≥natural no-op — all match gcc+clang. `[NEW, MED]`. **TASK: T-mac-30128.**

### <a id="r23-typeof-bitfield"></a>`typeof`/`typeof_unqual` of a bit-field accepted → bit-field-typed object miscompiles
- **[MED] the `TOK_TYPEOF*` arm of `parse_btype` (`src/mccgen.c:9260-9282`) strips storage + (for typeof_unqual) top-level qualifiers but never inspects `VT_BITFIELD`, so `typeof(s.bf)` keeps the bit-field type (with the operand's `bp`/`bs`) as the declared type of a standalone object.** `struct S{int bf:5;}; typeof(s.bf) x = 100; (int)x` → mcc creates a 5-bit-field variable, reads/writes go through bogus bit-field masking, emits `shift count >= width` warnings and yields garbage (`-2005910164`); `sizeof(typeof(s.bf))` then errors "'sizeof' cannot be applied to a bit-field" — mcc made a variable of a type it refuses to size. clang/gcc reject `typeof` of a bit-field outright (constraint violation). The `__auto_type` path already handles this (`auto_type_decay` `:18382-18387`). Fix: `if (type1.t & VT_BITFIELD) mcc_error("'typeof' applied to a bit-field");` in the TOK_TYPEOF* arm. Reconfirms the INVESTIGATIONS:218/334 typeof/auto/constexpr robust set (qualifier keep/strip, array/string/function-call/VLA operand evaluation-once, nested typeof, auto decay, constexpr narrowing) with this one exception. `[NEW, MED]`. **TASK: T-mac-30129.**

### <a id="r23-low-cluster"></a>LOW cluster (grouped)
- **[LOW-MED] `-Wunused-label` entirely missing for ordinary defined-but-unreferenced labels.** `label_pop` (`src/mccgen.c:1508`) warns only when `s->r == LABEL_DECLARED`; a defined ordinary label reaches `LABEL_DEFINED` and never warns, and the option is unrecognized ("unsupported option"). `int main(){ un: return 0; }` → no diagnostic (gcc/clang warn under `-Wall`). Asymmetry: an unused `__label__` DECLARATION does warn. Fix: add `-Wunused-label` and warn on `LABEL_DEFINED && !a.used` (mark `a.used` at goto/`&&` sites).
- **[LOW] duplicate `__label__` in one block silently accepted (accept-invalid).** The `__label__` parse loop (`src/mccgen.c:16027-16036`) `label_push`es with no same-scope duplicate check; `{ __label__ D, D; D: ; }` compiles (gcc/clang error "duplicate label declaration"). Fix: check `label_find` at the current `local_scope` before pushing.
- **[LOW] unmatched `#pragma pack(pop)` is a HARD ERROR** (`src/mccpp.c:2642-2646` `mcc_error("out of pack stack")`) while gcc/clang only warn and continue — and mcc's own `#pragma options align=reset` path only warns (`:2792-2795`), so the two are mutually inconsistent. Rejects code gcc/clang accept. Fix: downgrade to `mcc_warning`, leave the stack unchanged.
- **[LOW] malformed `#pragma pack` (non-power-of-2, >16, MSVC named push) diagnosed only under `-Wunknown-pragmas` (off by default)** (`src/mccpp.c:2662-2663,2813-2814`); gcc/clang warn by default. Safely ignored (no ABI impact), so a missing-diagnostic gap only. Fix: route through an always-on/`-Wpragmas` warning.
- **[LOW] C23 `constexpr` object of pointer type unconditionally rejected** (`src/mccgen.c:18300` `'constexpr' for an object of pointer type is not supported`); `constexpr int *p = nullptr;` / address-constant initializers are valid C23 (clang/gcc accept). Clean rejection (feature gap, no miscompile). Fix: support null/address-constant pointer constexpr.
- `[NEW]` cluster. **TASK: T-mac-30130.**

### <a id="r23-labels-as-values-note"></a>Refinement of T-mac-30062: labels-as-values NOBUILD is `__label__`-specific
- The `980526-1.c` "unresolved reference to `_lbl1`" NOBUILD from the cross-oracle run (T-mac-30062) is precisely the GNU LOCAL-LABEL case: `&&L` where `L` is a `__label__` emits an unresolvable UND symbol, while `&&ordinaryLabel` (single and via a runtime `void*tab[]={&&A,&&B}; goto *tab[i]`) LINKS and runs correctly. Root: `label_pop` (`src/mccgen.c:1504-1528`) defines the label-address symbol via the stale forward-chain `s->jnext` on the `local_label_stack` path (should be the definition offset). `stdarg-4` in T-mac-30062 is a different root (static-function address). Recorded against T-mac-30062; fold the `__label__`-address fix there. `[NEW]` refinement.

### <a id="r23-nested-fn-robust"></a>GNU nested functions — VERIFIED ROBUST (clean rejection, negative result)
mcc does not support nested functions and does not miscompile them: zero trampoline/static-chain infrastructure anywhere; a function definition inside a block is rejected at parse time in the target-independent front-end (`src/mccgen.c:18624-18626`, `mcc_error("cannot use local functions")` when the decl scope `l != VT_CONST`), so behavior is identical across arm64/x86_64/riscv64/i386/arm and compile vs `-run`. Verified: basic frame-capture, 3-level nesting, recursion, address-taken, global-shadowing, `-run` — all a clean diagnostic (line+caret), never an ICE or silent-accept-then-miscompile; a legal in-block prototype is still accepted; `auto int g(void);` cleanly errors. mcc's position = clang (reject), unlike real gcc (accept). `[NEW]` negative.

### <a id="r23-restrict-robust"></a>`restrict` / alias-based optimization — VERIFIED ROBUST (sound by construction, negative result)
mcc never exploits `restrict` or any type-based/points-to alias assumption for optimization (sound; missed-opt, not a miscompile). `restrict` is parsed+stored, never consulted by any pass (extends T-mac-30050 to the optimizer). The only alias analysis, `ast_dep_base_distinct` (`src/mccast.c:14276-14285`), declares two refs distinct only when both bases are DIFFERENT NAMED SYMBOLS (genuinely non-aliasing) — sound; locals/pointer-indirect are conservatively may-alias; no TBAA. Every redundancy/motion pass (CSE/GCSE/LICM/PRE, `ast_cse_regpure` gate `:10592`) is register-only — an `AST_Load` (any pointer deref) and global-symbol refs are never candidates, so no load is hoisted/forwarded across a store. DSE is local-only + escape/call-guarded. The dependence transforms that call `ast_dep_base_distinct` (interchange/fusion/tiling) are triple-gated OFF (`MCC_OPTD_DEV`+`-O12`+hardcoded `allow_indirect=0`) and the `dep-alias-oracle` feeds only diagnostic dumps. Empirically a `restrict`-violating aliasing program is byte-identical at `-O0..-O4` (matching serial semantics; clang exploits restrict and diverges — legal, the program has UB). `[NEW]` negative.



## Round 24 (2026-08-18) — internal-consistency sweep IX: #embed, register-vars, mode/anon, alias/weakref/ifunc, ?:/__extension__, static_assert

Six parallel subagents (mac-arm64) on C23 `#embed`/`__has_embed`, GNU explicit register variables, `__attribute__((mode))` + anonymous struct/union members, `alias`/`weakref`/`ifunc` emission across ELF/Mach-O/COFF, GNU omitted-middle `?:`/`__extension__`/stmt-expr-init, and `_Static_assert` edge cases. Reproduced against `cmake-macos/mcc` + cross builds, cross-checked vs clang/gcc-16. THREE subsystems returned strong negatives (`#embed` works; `mode`+anonymous members; `alias` across all 3 formats). Tasks minted: **T-mac-30133..30138** (plus T-mac-30132 already DONE — static_assert 32-bit cap).

### <a id="r24-static-assert"></a>_Static_assert controlling constant capped at 32 bits — RESOLVED (T-mac-30132)
`do_Static_assert` used 32-bit `expr_const()` → `_Static_assert(1LL<<32,"")` etc. fatally rejected though true; a THIRD distinct consumer of the cap (after T-mac-30089 `#if`, T-mac-30070 array-size). Fixed to `expr_const64_pub()`. See `DETAILS.md#t-mac-30132-static-assert-wide`. Float-controlling-expr rejection + all other static_assert diagnostics verified ISO-correct.

### <a id="r24-extension"></a>`__extension__` fails to silence `-pedantic` for declspecs/declarators/`_Static_assert` — reject-valid under `-pedantic-errors`
- **[HIGH] the declspec parser consumes `__extension__` (`src/mccgen.c:8955-8958`, `ext_seen++; next(); continue;`) but never disables pedantic** — the only two suppression sites are the unary/expr (`:12848-12849`) and initializer-expr (`:17085-17086`) paths. So a pedantic diagnostic originating in the type/declarator/struct/enum/`_Static_assert` machinery fires even when `__extension__` prefixes the construct, and under `-pedantic-errors` becomes a HARD ERROR rejecting valid GNU code. `-std=c89`: `__extension__ long long g=1;` → "ISO C90 does not support 'long long'" (error under -pedantic-errors; gcc silent); `__extension__ _Static_assert(1,"")`, `__extension__ enum{A,B,}` (trailing comma), `__extension__ struct{int fam[];}` (flexible array), `__extension__ int v[n]` (VLA) all the same. The expression/initializer paths (`__extension__ ({...})`, `int x=__extension__({...})`) ARE correctly silenced. Fix: save+zero `warn_pedantic`/`pedantic_errors` when `__extension__` is seen at declaration start (`:8955`/`:9443`), restore after the full declaration — mirroring the expr/init save-restore. `[NEW, HIGH]`. **TASK: T-mac-30133.**

### <a id="r24-regvar"></a>GNU local register variable — unrecognized `asm()` register name silently dropped (arm64); + refine T-mac-30033
- **[MED] a local `register T v asm("name")` whose name mcc's small table doesn't know is silently discarded — no diagnostic, the binding is lost and `v` becomes an ordinary stack local.** `src/mccgen.c:17674` `if (reg >= 0 && reg < MCC_NB_REGS) r = …;` has NO else branch; arm64's table (`arm64-asm.c:230-244`) knows only x0-x30 and v/d/s/h/b 0-7, so `d8-d15`/`v8-v31` (callee-saved SIMD), `sp`, 32-bit `wN`, and gcc `rN` aliases are silently ignored → `register double v asm("d10")` puts the value in d0, not d10 (x86_64 catches an unknown name with a downstream error; arm64 doesn't). Real ABI/syscall-convention break. Fix: error "invalid register name" when the `asm()` name fails to parse / is out of range. **Refinement of T-mac-30033:** the arm64 x19-x30 physical-vs-codegen numbering collision ALSO manifests through this declarator path (`:17673-17675` feeds the physical number straight into `VT_VALMASK`; `register long v asm("x24")` → value in d4/LR), a DISTINCT code path from the asm-operand layer T-mac-30033 already names — apply the `arm64_asm_treg` translation at `:17673` too. (Global register variables are rejected outright at `:18588-18590` — matches clang, safe, no task.) `[NEW, MED]`. **TASK: T-mac-30134.**

### <a id="r24-weakref"></a>`weakref` non-functional — emits a strong global UNDEF to the alias's own name → link failure
- **[MED] `weakref` is not a token → falls to the attribute `default:` arm (`src/mccgen.c:6683`, ignored) → the symbol becomes a STRONG global undef to the ALIAS name, never a weak ref to the target.** `static int wf(void) __attribute__((weakref("g")));` → ELF `wf *UND* STB_GLOBAL` + `R_X86_64_PLT32 wf` (not `g`); same on Darwin-obj/COFF. Three wrongs vs gcc/clang: references the alias name not the target `g`; STRONG not weak → guaranteed link failure (`unresolved reference to '_wf'`); `&wf` const-folds non-null so an `&wf ? wf() : 0` guard mis-compiles to always-call. Fix: add a `weakref` token recording the target, mark the symbol weak-undef aliased to the target (reuse `alias_target` + `a.weak` + `SHN_UNDEF`), or at minimum emit a clean "weakref unsupported" error. Attaches to the T-mac-30072 attribute-drop cluster but warrants its own task (concrete link-break + mis-fold). `[NEW, MED]`. **TASK: T-mac-30135.**

### <a id="r24-ifunc"></a>`ifunc` attribute dropped; ELF `STT_GNU_IFUNC`/`IRELATIVE` backend is unreachable dead code
- **[MED] `ifunc` is not a token → dropped (`src/mccgen.c:6683`) → the aliased symbol is never emitted → `unresolved reference`.** PRECISION: mcc HAS a complete static-ifunc ELF backend — `mcc_prepare_static_ifunc` (`src/objfmt/mccelf.c:1988`), `mcc_fill_static_ifunc` (`:2067`), IPLT stub + `R_*_IRELATIVE` (`:2054,2082`), gated on `ST_TYPE==STT_GNU_IFUNC` (`:2006`) — but `STT_GNU_IFUNC` is NEVER assigned anywhere (grep: only reads), and the assembler's `.type` accepts only `function`/`object` (warns-and-ignores `@gnu_indirect_function`, `mccasm.c:784-798`). So the machinery can never fire. Fix: wire the `ifunc` attribute → `STT_GNU_IFUNC` (ELF) + a clean diagnostic on Mach-O/COFF (gcc errors there), OR accept `.type …,@gnu_indirect_function` in the assembler so the existing backend is reachable. `[NEW, MED]`. **TASK: T-mac-30136.**

### <a id="r24-embed-ifdef"></a>`#ifdef`/`#elifdef __has_embed` report "not defined" — contradicts `defined(__has_embed)`
- **[MED] `#ifdef __has_embed` is false while `#if defined(__has_embed)` is true (self-contradiction).** The ifdef guard (`src/mccpp.c:2948`) whitelists only `__has_include`/`__has_include_next`, omitting `__has_embed`; the `#elifdef` path (`:2999`) uses bare `define_find(tok)` and whitelists NONE of the `__has_*` builtins (so `#elifdef __has_include` is also wrongly false — a pre-existing adjacent gap). Real code guarded by `#ifdef __has_embed` silently takes the no-embed fallback even though mcc fully supports `#embed`. Fix: add `TOK___HAS_EMBED` to the `:2948` guard and give `:2999` the `__has_include`/`__has_include_next`/`__has_embed` whitelist. `#embed`/`__has_embed` are otherwise CORRECT (byte sequence, `limit()`/`prefix`/`suffix`/`if_empty`/`gnu::offset`, `__STDC_EMBED_*` values, clean errors — strong negative). `[NEW, MED]`. **TASK: T-mac-30137.**

### <a id="r24-static-stmtexpr"></a>Block-scope `static` rejects a CONSTANT statement-expression initializer
- **[MED] `int f(void){ static int s = ({ 7; }); }` → mcc `error: constant expected`; gcc AND clang accept (fold the constant-valued stmt-expr).** The stmt-expr `'{'` arm of `unary` (`src/mccgen.c:13050-13057`) result reaches the static-init constant check, which doesn't recognize a constant-folded stmt-expr. mcc correctly handles the automatic case (`int a = ({...});` runs) and correctly rejects a NON-constant static stmt-expr and file-scope stmt-expr. Fix: in the static-init constant path, accept a stmt-expr whose result constant-folded to a literal. `[NEW, MED]`. **TASK: T-mac-30138.**

### <a id="r24-robust"></a>Strong negatives — `#embed`, `mode`+anonymous members, `alias`, omitted-`?:`
- **`#embed`/`__has_embed`**: implemented + correct (byte seq, `limit`/`prefix`/`suffix`/`if_empty`/`gnu::offset`, `__STDC_EMBED_FOUND/EMPTY/NOT_FOUND`=1/2/0, clean file-not-found/oversize/device errors); only the `#ifdef` detection gap above.
- **`__attribute__((mode(QI/HI/SI/DI/TI/word/pointer/SF/DF/XF)))`**: real + correct width mapping/sign/struct-member/param-return; TF and non-x86 TI/XF correctly errored.
- **Anonymous struct/union members**: lookup (incl. nested 2-deep), designated+positional+static init, union aliasing, duplicate/ambiguity diagnosis, value-level ABI — all match clang/gcc.
- **`alias`** (strong+weak, forward+backward, to static/global, var/fn, chained, size copied, defined-later fixup): correct + consistent across ELF, linked Mach-O, COFF (COFF weak-collapse is the known T-mac-30019; mcc's strong-Mach-O-alias is a deliberate superset vs clang/gcc which require weak).
- **GNU omitted-middle `a ?: b`**: common operand evaluated exactly once (function-call side effect, nested, float/pointer/NULL/constant-context), correct usual-arithmetic-conversions, not an lvalue — reconfirms T-mac-30054, no new divergence.
`[NEW]` negatives.


## Round 25 (2026-08-18) — internal-consistency sweep X: designated-init, string/char literals, `_Atomic` type, fn-type compat/K&R, compound literals, switch/case

Six parallel subagents (mac-arm64) on designated initializers, string/char literal encoding, the `_Atomic` type qualifier (type side, distinct from the already-audited `__atomic_*` builtins), function-type compatibility + K&R declarations, compound literals `(T){...}`, and switch/case semantics/codegen. Reproduced against `cmake-macos/mcc`, cross-checked vs clang + gcc-16 (byte/runtime/asm/diagnostic). TWO subsystems returned strong negatives (designated initializers; the classic C99/C11 compound-literal semantics). **2 HIGH** (implicit `_Atomic` scalar access unordered; `switch` on `_Bool` wrong-branch) + 6 MED + a LOW cluster. Tasks minted: **T-mac-30139..30148**.

### <a id="r25-atomic-implicit-unordered"></a>Implicit `_Atomic` scalar load/store emitted as a plain unordered access (no seq_cst) — HIGH
- **[HIGH] a plain read/write of an `_Atomic` scalar (`int x=g;` / `g=v;`) lowers to an ordinary `ldr`/`str` (arm64) or `mov` (x86-64) with NO acquire/release/`dmb`** — atomicity/no-tearing survives for aligned scalars ≤8B, but the C11-required `memory_order_seq_cst` ordering is silently dropped. Root cause: the gating predicate `atomic_store_needs_libcall()` (`src/mccgen.c:10627-10638`), called from the implicit-load gate (`:5206`) and implicit-`=`-store gate (`:14926`), returns nonzero only when `size > MCC_PTR_SIZE && size <= 8 && power-of-two`; on a 64-bit target `MCC_PTR_SIZE==8` makes the set `(8,8]` empty, so the ordered `gen_atomic_load_scalar`/`gen_atomic_store_scalar` (`:10734`/`:10640`, which correctly emit `__atomic_load_N`/`__atomic_store_N`) are DEAD CODE for every scalar. clang/gcc-16 emit `ldar`/`stlr`. Explicit `atomic_load`/`atomic_store` and all compound-assign/`++`/`--` RMW ARE correctly atomic+ordered (via `__atomic_*_fetch_N` CAS loops) — only the implicit object access loses ordering. Fix: route scalars with `size <= MCC_PTR_SIZE` (all lock-free scalars) through the ordered helpers — the size comparison is inverted. C11 6.5.2.3/6.5.16.2p3/5.1.2.4. `[NEW, HIGH]`. **TASK: T-mac-30139.**
- LOW (builtins side, already-audited scope): `ATOMIC_GEN_OP` (`runtime/lib/stdatomic.c:16`) ignores its `memorder` arg (always SEQ_CST CAS + RELAXED initial load) → `*_explicit(...relaxed/acquire...)` RMW is over-synchronized (stronger than requested — safe, not a correctness bug). Folds into the LOW cluster T-mac-30148.

### <a id="r25-switch-bool"></a>`switch` on `_Bool` boolean-normalizes case labels → wrong branch + rejects-valid — HIGH
- **[HIGH] `switch` never integer-promotes its controlling expression (`src/mccgen.c:16259-16265` stores `sw->sv = *vtop` verbatim), so case constants are converted to the raw controlling type via `value64(c,t)` (`:15033`, `t` = un-promoted type set at `:16316`); for a `_Bool` controller `value64` (`:3528`, `if (BTYPE==VT_BOOL) return l1!=0`) collapses every nonzero case constant to 1.** Two symptoms: (a) miscompile — `switch((_Bool)ok){case 3: ...}` with `ok==1` WRONGLY matches `case 3` (mcc returns the case-3 body; clang/gcc take default); (b) rejects-valid — `switch(_Bool x){case 0; case 1; case 2;}` → mcc `error: duplicate case value` (2 folds to 1, collides), clang/gcc compile (only `-Wswitch-bool`). Per C11/C23 6.8.4.2p2 the case constant is converted to the PROMOTED controlling type (`_Bool`→`int`), so 2 and 3 stay distinct and never match a boolean. Fix: integer-promote the switch controlling expression before recording `sw->sv.type`, and use the promoted type for case-constant conversion. `[NEW, HIGH]`. **TASK: T-mac-30140.**

### <a id="r25-switch-bitint"></a>`switch` on `_BitInt(N>64)` rejected — MED (rejects-valid)
- **[MED] `switch(x)` where `x` is `_BitInt(N)` with N>64 → `error: switch value not an integer`** (`src/mccgen.c:16261`, gate `!is_integer_btype(...)`; `is_integer_btype` `:701-704` lists only BYTE/BOOL/SHORT/INT/LLONG/INT128). A wide `_BitInt` is `VT_STRUCT` with `a.is_bitint`, so it fails the integer test even though the case codegen (`gcase`/`case_cmp`) already carries wide values. Cutoff is exactly N>64 (N≤64 stored as VT_LLONG works). clang/gcc `-std=c23` accept. Fix: treat `is_bitint_type` as an integer in the switch controlling-type gate. `[NEW, MED]`. **TASK: T-mac-30141.**

### <a id="r25-c23-empty-paren"></a>C23 `()` == `(void)` breaking change not honored — MED (accepts-invalid, 3 manifestations)
- **[MED] empty `()` in a function declarator is treated as unspecified-params (K&R) under EVERY `-std`** — `src/mccgen.c:9529-9530` (`if (tok==')') { l = 0; }`) has no `cversion` gate, unlike the adjacent C23-gated `TOK_DOTS` arm (`:9531`); `ad->f.func_type = l` at `:9641`. Under `-std=c23`: (a) `int g(); g(1,2);` — mcc no diag, clang/gcc `error: too many arguments`; (b) `int(*p)() = &proto_fn;` — mcc accepts, clang/gcc `error: incompatible function pointer types`; (c) `int g(); int g(int x){...}` — mcc accepts, clang/gcc `error: conflicting types`. C23 6.7.6.3: `()` now specifies a prototype with no params (== `(void)`). Fix: when `cversion>=202311`, treat empty `()` as `(void)` (func_type = prototyped-void). `[NEW, MED]`. **TASK: T-mac-30142.**

### <a id="r25-callconv-silent"></a>`stdcall`/`fastcall`/`thiscall` silently ignored on arm64 (no `-Wignored-attributes`) — MED
- **[MED] recognized-but-target-inapplicable calling-convention tokens go silent on arm64** — the `TOK_STDCALL*` case body is `#if defined(MCC_TARGET_I386)…#endif` and falls through to a bare `break` with no warning (`src/mccgen.c:6574-6580`); `fastcall`/`thiscall`/`regparm` cases are entirely `#ifdef MCC_TARGET_I386` (`:6581-6604`). clang: `warning: '…' calling convention is not supported for this target`; gcc-16: `warning: '…' attribute directive ignored`. Contrast: `ms_abi`/`sysv_abi`/`regparm` (unknown on arm64) DO hit the `default:` `warn_attributes` path and warn correctly — only the conv tokens that are lexed but compiled-out stay silent. Low real-ABI risk (these conventions don't exist on arm64) but the diagnostic both oracles emit is missing. Fix: emit `warn_attributes("'%s' attribute ignored")` in the non-i386 fall-through. `[NEW, MED]`. **TASK: T-mac-30143.**

### <a id="r25-knr-promo-mismatch"></a>K&R definition + promotion-mismatching prototype only pedantic-warns — MED (accepts-invalid by default)
- **[MED] an old-style definition whose promoted parameter type disagrees with a later prototype is accepted at default warning level** — `is_compatible_func` (`src/mccgen.c:4406-4417`) emits `mcc_pedantic(...)` for the promotion mismatch but does NOT `return 0`, so the types are deemed compatible unless `-pedantic-errors`. `int f(a) char a; {…} int f(char a);` (promoted param is `int`, prototype says `char`) → mcc default: no diag; gcc-16/clang: `error: conflicting types for 'f'` unconditionally. C17 6.7.6.3p15 (prototype must agree with the PROMOTED old-style types — a constraint violation, not merely pedantic). Fix: make the promotion-mismatch path a hard incompatibility (`return 0`) rather than a pedantic-only note. `[NEW, MED]`. **TASK: T-mac-30144.**

### <a id="r25-c23-cl-storage"></a>C23 storage-class / `constexpr` compound-literal constraints unenforced — MED (accepts-invalid)
- **[MED] the compound-literal branch parses C23 storage-class specifiers but validates none of their constraints** — `src/mccgen.c:13009` captures the storage class into the type but the `r` fed to `decl_initializer_alloc` (`:13020`/`:13022`) is derived only from scope, never routing through `decl_constexpr` (`:18334`) or a constant-init check. The happy path works (`(static int[]){10,20}` = one static object across calls; `(constexpr int[]){1,2,3}` reads correctly, both match gcc), but mcc ACCEPTS where gcc-16 rejects: `(static int[]){argc,7}` / `(constexpr int[]){argc}` (non-constant init), `&(register int[]){5,6}[0]` (address of register array), block-scope `(thread_local int[]){9}` (auto/thread_local conflict). clang can't parse the C23 syntax at all → gcc-16 is the sole oracle, and it rejects all four. C23 6.7.11p4 + 6.7.1 + 6.3.2.1. Fix: enforce constant-init for static/constexpr, reject address-of-register-array and thread_local-at-block-scope in the compound-literal path. `[NEW, MED]`. **TASK: T-mac-30145.**

### <a id="r25-ucn-wide"></a>Surrogate / out-of-range UCN accepted silently in wide strings & char constants under C23 — MED (accepts-invalid)
- **[MED] `\uD800` (lone surrogate) and `\U00110000` (> 0x10FFFF) are accepted with no diagnostic in `u""`/`U""`/`L""` and `u''`/`U''`/`L''` under `-std=c23`, emitting the surrogate code unit verbatim** — the version gate `src/mccpp.c:3193` disables the check for `cversion>=202311`, and the wide path (`parse_escape_string`→`cstr_wccat`, `:3326`) has no UCN validation; validation exists only in `unicode_to_utf8` (`:301-309`), reached solely by the plain/u8 path (`cstr_u8cat`). clang: `error: invalid universal character` (all stds); gcc-16: `error: not a valid universal character`. The plain/u8 path correctly rejects both; under c11/c17 mcc warns (`-pedantic`)/errors (`-pedantic-errors`) — only the wide paths under C23 are unguarded, yet surrogate/OOR UCNs remain a C23 6.4.3 constraint violation. Fix: validate the code point in the wide path too, and don't disable the check for C23. `[NEW, MED]`. **TASK: T-mac-30146.**

### <a id="r25-widestr-signedness"></a>4-byte wide-string array init ignores element signedness — MED (missing diagnostic)
- **[MED] `int a[] = U"a";` and `unsigned b[] = L"a";` are accepted** — `src/mccgen.c:17180` (`TOK_LSTR` checks `VT_INT` only) and `:17183` (`TOK_U32STR && VT_BTYPE==VT_INT`, no `VT_UNSIGNED`) drop the signedness requirement, unlike the correct `TOK_U16STR` arm (requires `VT_SHORT && VT_UNSIGNED`). `char32_t` is `unsigned int`, `wchar_t` here is `signed int`, so both inits are cross-signedness. clang: `error: initializing wide char array with incompatible wide string literal`; gcc-16: `error: cannot initialize array of 'int' from a string literal with type array of 'unsigned int'`. No miscompile (int/unsigned share representation) — just the missing constraint diagnostic. `short[]=u"a"` / `unsigned[]=U"a"` are handled correctly. Fix: require matching signedness in the 4-byte L/U32 init arms. `[NEW, MED]`. **TASK: T-mac-30147.**

### <a id="r25-low-cluster"></a>LOW cluster — cosmetic diagnostics & over-sync
- Out-of-range case label for a narrow controlling type (`switch(char x){case 256:}`) compiles silently (behavior correct — value can't match); clang/gcc warn "overflow converting case value"/"exceeds maximum value for type" (`src/mccgen.c:3533-3536` narrows only to 32-bit). No K&R old-style-definition deprecation warning even with `-pedantic -Wall -Wextra` (clang `-Wdeprecated-non-prototype`, gcc `-Wold-style-definition`, both default-on). `_Noreturn` on a function-POINTER variable (`_Noreturn void(*r)(void);`) undiagnosed (clang errors, gcc warns). Misleading `\x`-with-no-digits diagnostic ("more hex digits in universal-character-name expected" vs clang/gcc "no following hex digits", `src/mccpp.c:3184`). `int x={.foo=1}` designated-init-on-scalar error wording less specific than clang/gcc (`mccgen.c` ~16776, still correctly rejects). No `-Woverride-init` for `{[0]=1,[0]=2}` (matches gcc-16 default; only clang warns). `ATOMIC_GEN_OP` `memorder` ignored → over-synchronized `*_explicit` RMW (safe). `[NEW, LOW]`. **TASK: T-mac-30148.**

### <a id="r25-robust"></a>Strong negatives — designated initializers, classic compound literals
- **Designated initializers**: 30 probes byte-identical to clang AND gcc-16 — nested `.s.x`, out-of-order/overlapping/last-wins, GNU ranges `[2 ... 5]` (incl. static const-eval + reversed-empty rejection + 100k-elem), mixed designated+positional continuation, union (re-)designation, FAM designators, array-of-struct `.field`, `_Bool`/bitfield/`char[]`-from-string, self-referential sizing, static-vs-block-scope value parity, 2D `[i][j]` ranges, string/pointer relocations inside ranges, enum/const-expr indices; every invalid form correctly rejected. `decl_designator` `src/mccgen.c:16728`. One of the most solid subsystems audited.
- **Compound literals (classic C99/C11)**: lifetime (enclosing-block storage, same address per loop iter), file-scope static-init values + section placement (`.rodata` for const, `.data` otherwise, verified via `nm`), designators/nesting, lvalue-ness (`(struct S){…}.x=5`, `++(int){0}`), `sizeof`/`_Alignof`/`_Generic` decay, by-value pass & return, and all standard diagnostics (dangling-return warning, const-write, VLA-CL, incomplete type, non-constant file-scope element) — all match both oracles. `src/mccgen.c:13005-13071`.
- **`_Atomic` (beyond FINDING 1)**: RMW/compound-assign truly atomic; sizeof/_Alignof of atomic scalars correct; atomic-struct layout mcc==gcc-16 (clang outlier pads to 16/16 — note the mcc/gcc-vs-clang ABI split for atomic aggregates); `_Atomic(T)`==`_Atomic T`; pointer atomics scale by pointee size; lvalue conversion strips `_Atomic`; `<stdatomic.h>` complete; all `_Atomic`-on-array/function/void/qualified diagnostics correct.
- **String/char literals (beyond findings)**: multi-byte encoding of `€`/`😀` (incl. UTF-16 surrogate pair) byte-exact in all prefixes; char constants + multi-char values; cross-prefix concatenation (mixed-prefix rejected, `L"a" "b"`→wchar_t); embedded NUL; `sizeof`/`_Generic` types; u8 `char`→`unsigned char` C17→C23 transition; UCN in identifiers.
- **Function-type compat (beyond findings)**: incompatible fnptr init (return/arg-count/arg-type/variadic) rejected; `void*`↔fnptr GNU-ext with correct `-pedantic-errors` gate; implicit-decl hard error in c17+c23; `_Noreturn` fn that returns warned; fn-returning-fn/array + array-of-fn rejected; K&R default-argument-promotion ABI (float→double, char→int) byte-identical to clang/gcc cross-TU.
- **Switch (beyond findings)**: wide >32-bit case values (0x100000001 vs 0x200000001) full-width match — NO bit-32 truncation; GNU ranges; duplicate detection (exact/singleton-in-range/overlap); multiple-default error; signed/unsigned match; nested/Duff's-device cases; VLA-jump-into-case rejected; enum/const-expr labels; dense jump-table path runtime-correct at the sparse↔dense boundary.
`[NEW]` negatives.


## Round 26 (2026-08-18) — internal-consistency sweep XI: `#line`/location macros, `__builtin_choose_expr`/`types_compatible_p`/`constant_p`, `typeof`/`__auto_type`, flexible array members, `__attribute__((cleanup))`, pointer/array arithmetic

Six parallel subagents (mac-arm64) reproduced against `cmake-macos/mcc`, cross-checked vs clang + gcc-16 (runtime/type/asm/-E/diagnostic). ZERO HIGH; ~9 MED (all accepts-invalid / missing-diagnostic / conservative-weaker, no miscompiles); several strong negatives incl. **pointer arithmetic fully robust** (~40 probes, exact non-pow2 scaling + ptrdiff + 64-bit large-offset indices). Tasks minted: **T-mac-30149..30155**. (The `diag.parse-depth` `sizeofchain` SIGSEGV surfaced by the focused regression is the already-filed **T-mac-30109**, pre-existing + Debug-build frame-size — not re-minted.)

### <a id="r26-constant-p-noopt"></a>`__builtin_constant_p` never folds under optimization — MED (conservative-weaker at -O2)
- **[MED] `__builtin_constant_p(x)` is a pure front-end check (`(vtop->r & (VT_VALMASK|VT_LVAL)) != VT_CONST → 0`, `src/mccgen.c:13327-13334`) that never survives to the optimizer**, so an optimizer-provable constant (a var initialized from a literal, a `const` object, a comma expr) returns 0 at `-O2` where BOTH clang and gcc-16 return 1 (`v`, `v+1`, `cl` const-local, `cg` const-global, `(1,2)` — all mcc=0). mcc's -O2 output is byte-identical to -O0. Standard-legal (GNU: "a return of 0 does not indicate the value is not constant") → CANNOT miscompile, but `constant_p`-driven macros always take the slow runtime path. At -O0 mcc==gcc-16 (both 0) so only -O2 diverges from both. Fix: a `__builtin_constant_p` lowering that the optimizer can re-evaluate. `types_compatible_p` (19/19) + `choose_expr` (type/lvalue/unchosen-arm) fully robust. `[NEW, MED]`. **TASK: T-mac-30149.**

### <a id="r26-typeof-unqual-gnu"></a>`typeof_unqual` (C23 keyword) enabled in pre-C23 GNU dialects — MED (accepts-invalid)
- **[MED] `typeof_unqual` is treated as a keyword in every GNU dialect regardless of C version** — the gate `src/mccgen.c:9280-9283` disables `typeof`/`typeof_unqual` only when `std_strict_ansi && cversion<202311`, and shares one condition for both; `typeof` is a legitimate GNU extension in old modes but `typeof_unqual` is C23-only and was never a GNU extension. `-std=gnu17`/gnu11/gnu99 (even `-pedantic-errors`): `typeof_unqual(x) y;` → mcc rc=0; clang "call to undeclared function", gcc "implicit declaration". Correctly rejected in strict pre-C23 (`-std=c11`/c17) and correctly accepted in c23/gnu23. Fix: gate `typeof_unqual` on `cversion>=202311` independently of the `typeof` GNU-extension path. C23 6.7.2.5. `[NEW, MED]`. **TASK: T-mac-30150.**

### <a id="r26-fam-block-init"></a>Block-scope FAM initializer silently accepted, data dropped — MED (accepts-invalid + silent data loss)
- **[MED] `struct S {int n; char a[];}; struct S s = {1,{2,3,4}};` at BLOCK scope compiles clean and silently discards the FAM initializer** (runtime `n=1`, FAM data gone; no stack clobber). `decl_design_flex` (`src/mccgen.c:16627-16637`) diagnoses via `mcc_pedantic` (off by default) and does not distinguish static/file-scope (a GNU extension both oracles accept) from automatic storage (which both oracles hard-ERROR: clang "initialization of flexible array member is not allowed", gcc "non-static initialization of a flexible array member"). Fix: for automatic-storage FAM init, emit a default error (keep the file-scope GNU-extension path). All other FAM behavior robust (sizeof-ignores-FAM, offsetof, malloc idiom, struct-copy-excludes-FAM, zero-len-vs-FAM, over-align, `_Atomic`, all constraint diagnostics, nesting). C11 6.7.2.1p3/6.7.9. `[NEW, MED]`. **TASK: T-mac-30151.**

### <a id="r26-cleanup-diag"></a>`__attribute__((cleanup))` diagnostic gaps (runtime ordering fully correct) — MED
- **[MED] two missing diagnostics** — (1) the cleanup function's pointee type is never checked against the variable: `void cf(long*)` on an `int` var compiles + RUNS, passing `&int_var` as `long*` → 8-byte read of a 4-byte object → garbage (`src/mccgen.c:17706-17713`/`15423-15433`, call synthesized with no arg-type check); both oracles hard-ERROR. (2) `cleanup` on a static/file-scope/parameter (non-automatic) variable is silently dropped with no warning even under `-Wall` (`:17706-17722`, only the auto-local path wires `all_cleanups`); both oracles warn `-Wignored-attributes`/`-Wattributes`. RUNTIME ORDERING IS FULLY CORRECT — LIFO within/across nested scopes, all exit paths (return/break/continue/goto-leaving), goto-over-decl (matches gcc), longjmp/exit non-execution, VLA interaction, struct-cleanup — all byte-identical to both oracles. Fix: type-check the cleanup arg (error) + warn on non-automatic storage. `[NEW, MED]`. **TASK: T-mac-30152.**

### <a id="r26-missing-loc-macros"></a>`__TIMESTAMP__` and `__FILE_NAME__` predefined macros missing — MED
- **[MED] neither `__TIMESTAMP__` (GNU) nor `__FILE_NAME__` (clang/GCC-12, basename of `__FILE__`) is registered** (`src/mccpp.c:5878-5883` pushes only `__LINE__`/`__FILE__`/`__DATE__`/`__TIME__`/`__COUNTER__`/`__INCLUDE_LEVEL__`; `__BASE_FILE__` synthesized at `:5763`). Both are provided by BOTH oracles; in mcc the tokens pass through undefined and `#ifdef __TIMESTAMP__`/`#ifdef __FILE_NAME__` are false, so real code guarded on them takes the wrong branch. Fix: register `__TIMESTAMP__` (mtime of the main file, honoring `SOURCE_DATE_EPOCH`) and `__FILE_NAME__` (basename of the current file). `[NEW, MED]`. **TASK: T-mac-30153.**

### <a id="r26-line-intmax"></a>`#line`/`__LINE__` beyond INT_MAX — no default diagnostic + wraps to INT_MIN — MED
- **[MED] two related line-counter defects** — (1) `#line N` with N>2147483647 is silently accepted and clamped to 2147483647 with NO default diagnostic (`src/mccpp.c:3053-3062`, overflow warns only under `-pedantic`); clang errors "requires a positive integer argument", gcc warns "line number out of range" by default (C11/C23 6.10.4p3 constraint → a diagnostic is required). (2) `__LINE__` past line 2147483647 WRAPS to −2147483648 (`int line_num` in `src/mcc.h:439`, incremented at `mccpp.c:5345-5346`/`3076`); clang/gcc yield 2147483648. Any use (`assert`, logging) sees a nonsensical negative line. Fix: emit a default diagnostic on the `#line` overflow; widen/saturate the line counter so `__LINE__` never goes negative. `[NEW, MED]`. **TASK: T-mac-30154.**

### <a id="r26-low-cluster"></a>LOW cluster — `-E` linemarker preamble cosmetics
- `-E` preamble differs from gcc: mcc `# 1 "f.c"` / `# 1 "<command line>" 1`; gcc `# 0 "f.c"` / `# 0 "<built-in>"` / `# 0 "<command-line>"` — mcc starts numbering at `# 1` vs gcc `# 0` and writes `<command line>` (space) vs gcc `<command-line>`/`<built-in>`. Push/pop/return flags on real `#include` markers ARE correct and match gcc. Cosmetic; tools that parse linemarkers loosely are unaffected. `[NEW, LOW]`. **TASK: T-mac-30155.**

### <a id="r26-robust"></a>Strong negatives — pointer arithmetic, builtin type-introspection, typeof core, cleanup runtime
- **Pointer arithmetic & array subscripting (FULLY ROBUST, ~40 probes)**: non-power-of-two element scaling uses an exact `gen_op('*')` not a shift (`src/mccgen.c:4826-4855`); `p-q` typed `VT_PTRDIFF_T` then divided by `TOK_PDIV` — exact for arbitrary sizes incl. >2^31-byte offsets (64-bit division); large offsets use 64-bit indices with narrow-index sign-extension (NO 32-bit truncation); 2D/pointer-to-array/runtime-VLA row-major scaling; `void*`/fn-ptr GNU arith + `-pedantic-errors` gate; `n[arr]`, side-effect-once, `NULL+0`, constant-folded ptrdiff in array-size context; all diagnostics match. (Only note: driver lacks `-Wconversion` for the `int d=p-q` truncation warning — separate driver-option scope.)
- **`__builtin_types_compatible_p`** 19/19 exact (qualifier stripping, array≠pointer, char/signed/unsigned distinct, enum-underlying, typedef transparency, function types); **`__builtin_choose_expr`** result-type + lvalue-ness + unchosen-arm-not-evaluated (but still type-checked, matching gcc).
- **`typeof`/`typeof_unqual`/`__auto_type` (beyond the pre-C23 leak)**: qualifier/`_Atomic` preservation, `typeof_unqual` top-level-only stripping, VLA-size-evaluated-exactly-once + no-spurious-eval, array/function/call/string/compound-literal operands, nested/cast/typedef/member contexts, `__auto_type` + C23 `auto` decay, all diagnostics.
- **`__attribute__((cleanup))` runtime**: LIFO ordering, every exit path, nested chains, longjmp/exit non-execution, VLA + struct cleanup — all byte-identical to both oracles.
- **`#line`/location macros core**: `#line N`/`#line N "file"` + subsequent diagnostics, macro-expanded `#line` arg, invalid-`#line` rejection, `__COUNTER__` (per-expansion, paste patterns, cross-include), `__LINE__` at use-site (single/multi-line macro), line-continuation/block-comment counting, `__DATE__`/`__TIME__` formats + `SOURCE_DATE_EPOCH`, `__BASE_FILE__`/`__INCLUDE_LEVEL__`, `#line` restore after nested include, `_Pragma`/`#pragma` not disturbing counting.
`[NEW]` negatives.


## Round 27 (2026-08-18) — internal-consistency sweep XII: `__builtin_*_overflow`/`<stdckdint.h>`, `#pragma` breadth, constructor/destructor priorities, `packed` structs, `alloca`, symbol visibility

Six parallel subagents (mac-arm64) reproduced against `cmake-macos/mcc` (+ the x86_64 ELF cross build for visibility/ctor sections), cross-checked vs clang + gcc-16 (runtime/symbol-table/-E/diagnostic). **2 HIGH** (`<stdckdint.h>` unusable on the native target; hidden/internal visibility leaked as external on Mach-O) + ~11 MED + a LOW cluster. Strong negatives: **`alloca` fully robust** (SP-restore across 2M calls, alloca-as-arg, VLA interaction, setjmp, alignment) and the **overflow builtins' runtime core** + **packed-struct unaligned runtime**. Tasks minted: **T-mac-30156..30168**.

### <a id="r27-stdckdint"></a>`<stdckdint.h>` unusable on aarch64-apple-darwin (hardcodes `__int128`) — HIGH
- **[HIGH, rejects-valid] mcc's own C23 `<stdckdint.h>` cannot be used with mcc on its primary host** — `runtime/include/stdckdint.h` uses `__int128`/`unsigned __int128` in every `ckd_add`/`ckd_sub`/`ckd_mul` (and `__mcc_ckd_min`/`_max`) as the wide intermediate, but mcc rejects the `__int128` TYPE outright on aarch64-apple-darwin in all `-std` modes → `#include <stdckdint.h>` + any `ckd_*` use → `error: '__int128' is not supported on this target`. clang/gcc compile+run (`o=1 r=-294967296`, `__STDC_VERSION_STDCKDINT_H__==202311`). The overflow BUILTINS themselves are `__int128`-free (they use `__mcc_ov_gen`/`unsigned long long` sign-magnitude helpers) and work correctly — only the HEADER's intermediate needs `__int128`. Fix: rewrite `<stdckdint.h>` to reuse the `__int128`-free `__mcc_ov_*` path (or the `_BitInt` wide type) instead of hardcoding `__int128`. C23 §7.20. Related to the standing `__int128`-on-arm64 gap (`#r12-bitint-cast`) but independently fixable at the header. `[NEW, HIGH]`. **TASK: T-mac-30156.**

### <a id="r27-macho-visibility"></a>`hidden`/`internal` visibility leaked as external on Mach-O — HIGH
- **[HIGH, linkage leak] every route to hidden/internal visibility is a no-op on the native Mach-O target** — `convert_symbol` (`src/objfmt/mccmacho.c:995-1038`) sets `N_EXT` for every `STB_GLOBAL`/`STB_WEAK` symbol but never consults `st_other` visibility, and `N_PEXT` (0x10, private_extern) is never emitted anywhere in the Mach-O writer (the computed `vis` at `:966` is used only in a `dprintf`). So `__attribute__((visibility("hidden")))`, `visibility("internal")`, `-fvisibility=hidden`, AND `#pragma GCC visibility` all leave the symbol as a plain external `T`/`D`; clang and gcc-16 emit `private external` (N_PEXT). TU-private symbols are exported into the final image → duplicate-symbol clashes, missed dead-strip, wrong two-level-namespace behavior. Fix: in `convert_symbol`, set `N_PEXT` (and clear/adjust `N_EXT` per Mach-O private_extern semantics) when the symbol's visibility is hidden or internal. The ELF path (`STV_*`) and `-fvisibility=` on ELF are CORRECT (strong negative); only Mach-O leaks. `[NEW, HIGH]`. **TASK: T-mac-30157.**

### <a id="r27-ctor-priority"></a>constructor/destructor priority silently discarded → wrong init order — MED
- **[MED, accepts-invalid + wrong order] `constructor(N)`/`destructor(N)` priorities are parsed then thrown away** — the `ctor_priority:` label (`src/mccgen.c:6388-6393`) evaluates the priority via `expr_const()` and discards it; `struct FuncAttr` (`src/mcc.h:311-324`) has `func_ctor`/`func_dtor` bits but NO priority field; emission uses fixed `.init_array`/`.fini_array` (`:18158-18161`, `mccelf.c:1502-1509` `add_array`) with no `.init_array.NNNNN` priority-suffixed sections. So prioritized constructors run in SOURCE order, not ascending-priority order, and unprioritized-run-after-prioritized is violated — silently, no diagnostic. `((constructor(101)))`/`((constructor(500)))`/unprioritized → mcc runs decl order; clang runs `101,500,NP`. Confirmed lost on ELF too (single `.init_array`, no priority sections). Basic before-main/after-main ordering, destructor-vs-atexit LIFO, and exit()-skip are all CORRECT (strong negatives). Fix: store the priority in FuncAttr and emit `.init_array.NNNNN`/`.fini_array.NNNNN` (ELF) + order the Mach-O `__mod_init_func` entries. `[NEW, MED]`. **TASK: T-mac-30158.**

### <a id="r27-pragma-diagnostic"></a>`#pragma GCC diagnostic push/pop/ignored/error` is a no-op — MED
- **[MED] the warning-control pragma family is unimplemented** — `pragma_parse` (`src/mccpp.c:2601`) has no `GCC diagnostic` handler; it falls to the catch-all `else` (`:2807`) that just warns-as-unknown. So `#pragma GCC diagnostic ignored "-Wsign-compare"` does NOT suppress the warning and `#pragma GCC diagnostic error "-Wunused-variable"` does NOT escalate it (clang/gcc both honor push/pop regions). Widely used to locally silence a warning — mcc ignores it entirely. Fix: implement a push/pop stack of per-warning overrides keyed off the `-W` name. `[NEW, MED]`. **TASK: T-mac-30159.**

### <a id="r27-pragma-poison"></a>`#pragma GCC poison` ignored — banned identifiers compile — MED
- **[MED, accepts-invalid] `#pragma GCC poison banned` has no effect** (`src/mccpp.c:2807` catch-all) — subsequent use of `banned` compiles clean; clang/gcc `error: attempt to use a poisoned identifier`. Same via `_Pragma("GCC poison ...")`. Fix: record poisoned identifiers and error on their use during tokenization. `[NEW, MED]`. **TASK: T-mac-30160.**

### <a id="r27-pragma-warning-error"></a>`#pragma GCC warning/error "..."` ignored — MED
- **[MED] `#pragma GCC error "msg"` and `#pragma GCC warning "msg"` are silently dropped** (`src/mccpp.c:2807`) — the build continues; clang/gcc emit an error (rc=1) / warning with the text. mcc handles only bare `#pragma message`, not the `GCC warning`/`GCC error` forms. Fix: route `GCC warning`/`GCC error` to `mcc_warning`/`mcc_error` with the string operand. `[NEW, MED]`. **TASK: T-mac-30161.**

### <a id="r27-pragma-weak"></a>`#pragma weak` ignored → symbol emitted strong, alias dropped — MED
- **[MED, link correctness] `#pragma weak sym` leaves `sym` a STRONG external** (`nm`: mcc `D _wsym` vs clang/gcc `weak external _wsym`); `#pragma weak alias = target` is dropped entirely (no `alias` symbol emitted). No handler in `pragma_parse` (`:2807`). Causes duplicate-symbol link errors / missing weak-override behavior. Clusters with the `weak`/`weakref` attribute work (T-mac-30135). Fix: add a `weak` pragma handler marking the symbol weak (reuse the `a.weak` path) + weak-alias to target. `[NEW, MED]`. **TASK: T-mac-30162.**

### <a id="r27-pragma-message"></a>`#pragma message` truncates adjacent string concatenation + wrong macro-`_Pragma` location — MED
- **[MED, content loss] `#pragma message("a " "b")` prints only `a `** — the handler (`src/mccpp.c:2693-2716`) reads a single `TOK_STR` (`:2700`) and never concatenates adjacent literals; same truncation for `_Pragma(message("x " y))` from a macro. Additionally a macro-built `_Pragma` reports the file as the internal buffer name `:pragma:` (`mcc_open_bf(s1, ":pragma:", ...)`, `:5531`) rather than the real source location. Fix: concatenate adjacent string literals in the message operand; propagate the real source location for macro `_Pragma`. `[NEW, MED]`. **TASK: T-mac-30163.**

### <a id="r27-pragma-visibility"></a>`#pragma GCC visibility push/pop` unsupported (ELF and Mach-O) — MED
- **[MED] region-based visibility control is unimplemented** (`src/mccpp.c:2807` catch-all) — `#pragma GCC visibility push(hidden)` … `pop` leaves symbols at DEFAULT on ELF (verified via the x86_64 cross symtab: `p_hidden→DEFAULT`, should be HIDDEN) and (compounded by T-mac-30157) has no effect on Mach-O. `-fvisibility=` (command-line default) IS honored on ELF (strong negative) — only the pragma is missing. Fix: add a visibility push/pop stack feeding the same `a.visibility` path the attribute uses. `[NEW, MED]`. **TASK: T-mac-30164.**

### <a id="r27-overflow-p-ice"></a>`__builtin_*_overflow_p` don't constant-fold in ICE contexts — MED
- **[MED, no-fold; gcc-only oracle] the `_p` overflow builtins can't appear in an integer constant expression** — `_Static_assert(__builtin_add_overflow_p(INT_MAX,1,(int)0),"")` / `int a[...__builtin_mul_overflow_p(...)?5:3]` → mcc `error: constant expected`. gcc folds them (accepts); clang has no `__builtin_*_overflow_p` builtin at all, so gcc is the sole oracle. Root: mcc implements `_p` as a statement-expression (macro), which can never be an ICE. Runtime `_p` evaluation is correct. Fix: provide a const-foldable form (real builtin or a fold hook). GNU manual: the `_p` forms are usable in constant expressions. `[NEW, MED]`. **TASK: T-mac-30165.**

### <a id="r27-overflow-float-res"></a>`__builtin_*_overflow` with a non-integer (`float*`) result silently accepted — MED
- **[MED, accepts-invalid → silent wrong result] `float f; __builtin_add_overflow(3,4,&f)`** compiles (mcc rc=0) and produces garbage overflow semantics against a float (`o=0 f=7.0`); clang ("result argument … must be a pointer to a non-const integer type") and gcc ("does not have pointer to integral type") both hard-error. The macro just does `__typeof__(*res)*p=res; *p=(…)__mcc_ovv;` with no integer-type check. (`_Bool*` result: mcc accepts, clang ALSO accepts, only gcc rejects → NOT flagged.) Fix: reject a non-integer pointee for the result argument. `[NEW, MED]`. **TASK: T-mac-30166.**

### <a id="r27-packed-alignof"></a>`_Alignof`/`__alignof__` of a packed member returns natural alignment, not 1 — MED
- **[MED, wrong scalar result] `struct __attribute__((packed)) S{char c; int x;}; _Alignof(s.x)` → mcc 4, clang/gcc 1** — the `_Alignof`-of-expression path (`src/mccgen.c:13239-13250`) uses `type_size`'s natural alignment and only overrides via an explicit `a.aligned` attribute, never consulting the member's packed status (alignment 1). Code dispatching on `__alignof__` (e.g. "aligned enough for a wide copy?") overstates the guarantee. The packed unaligned member ACCESS itself is runtime-correct on arm64 (plain scalar LDR/STR, verified at odd addresses O0/O2 — strong negative). Fix: report alignment 1 for a packed member (and min with the struct's packing) in the alignof path. `[NEW, MED]`. **TASK: T-mac-30167.**

### <a id="r27-low-cluster"></a>LOW cluster — diagnostic/cosmetic
- No `-Waddress-of-packed-member` (and the flag itself is rejected `unsupported option`); clang warns under `-Wall`, gcc by default. `dllexport`/`dllimport` on Mach-O parsed and dropped with no warning (both oracles warn `-Wunknown/ignored-attributes`); `visibility("protected")` on Mach-O accepted silently (both oracles warn "target does not support protected visibility"). `#pragma region`/`endregion` treated as unknown-pragma (warned under `-Wunknown-pragmas`; clang/gcc recognize + stay silent). Unhandled `#pragma GCC …` all print identical `#pragma GCC ignored` (only the first token) — indistinguishable. Redeclaring `__builtin_alloca_with_align` as a prototype fails (`error: ')' expected`) because it is a predefined macro (`mccdefs.h:1193`), not a real builtin — real code virtually never prototypes builtins. `__builtin_*_overflow` with a `_Bool*` result (clang also accepts → gcc-only). `[NEW, LOW]`. **TASK: T-mac-30168.**

### <a id="r27-robust"></a>Strong negatives — alloca, overflow-builtin runtime, packed runtime, ELF visibility, ctor basics
- **`alloca`/`__builtin_alloca` FULLY ROBUST**: usable+freed-at-return, 16-byte aligned, SP restored across a 2,000,000-iteration loop (no unbounded growth), alloca-as-function-argument, two-allocas-in-one-arglist (SP-ordering trap), loop accumulation (distinct non-overlapping), alloca+VLA same-function interaction, conditional alloca, alloca(0)/large, non-constant size, setjmp/longjmp survival, `-fstack-protector-all`, `__builtin_alloca_with_align` alignment + single-eval. arm64 rides the generic VLA lowering (`arm64-gen.c:2870`), no `TOK_alloca` special-case needed.
- **`__builtin_*_overflow` runtime**: correct flag + wrapped stored value for add/sub/mul across int/unsigned/ll/ull at all boundaries; overflow detected against the RESULT type's range incl. narrower (`int→schar/uchar`) and mixed-sign / unsigned-result-from-signed; operands single-evaluated; store happens even on overflow; type-suffixed forms (`sadd`/`umul`/`saddll`/carry family) correct.
- **`packed` structs**: sizeof/offsetof/`_Alignof(struct)` correct for all type mixes; unaligned scalar loads/stores of int/short/long/double/ptr at odd addresses runtime-correct (O0/O2); packed bitfields extract/insert; `packed,aligned(4)`; per-member packed; array stride; nested non-packed inside packed; by-value arg+return ABI; union overlay; struct copy at odd address.
- **ELF visibility + `-fvisibility=`**: `hidden→STV_HIDDEN`, `default`, `protected→STV_PROTECTED`, `internal→STV_INTERNAL` on functions+variables; `-fvisibility=hidden` hides defs but preserves explicit-default and undefined refs; conflicting decl/def visibility merges most-restrictive (`merge_symattr`).
- **`#pragma once`** (dedups incl. via symlink/different path), **`push_macro`/`pop_macro`** round-trip, **`#pragma comment(lib,...)`**, **constructor-before-main / destructor-after-main / dtor-vs-atexit LIFO / exit()-skips-remaining**.
`[NEW]` negatives.


## Round 28 (2026-08-18) — internal-consistency sweep XIII: long double, arm64 HFA/aggregate ABI, `__sync_*` legacy atomics, `_Bool` conversion, `__builtin_mem*`/`str*`, extended FP types

Six parallel subagents (mac-arm64) reproduced against `cmake-macos/mcc`, cross-checked vs clang + gcc-16 (runtime/ABI/symbol/diagnostic; the HFA audit used cross-compiler linkage as the gold test). **3 HIGH** (`_BitInt`→`_Bool` implicit conversion miscompile; `__builtin_mempcpy` link failure; `__builtin_bzero` wrong byte count) + ~10 MED + a LOW cluster. **Strong negative: the arm64 aggregate calling convention is fully robust** (HFA in v-regs, small-struct-in-GP, >16B by-reference, x8 indirect return, V-reg-exhaustion no-split, Apple variadic-float-on-stack — all cross-compiler-correct). Tasks minted: **T-mac-30169..30179**.

### <a id="r28-bitint-bool"></a>Implicit `_BitInt(N>64)` → `_Bool` conversion does not normalize to 0/1 — HIGH
- **[HIGH, miscompile] a `_BitInt` value assigned/initialized/stored into a `_Bool` is truncated to the low limb/byte instead of the C 6.3.1.2 "0 iff ==0, else 1" reduction** — the EXPLICIT `(_Bool)` cast is correct (`gen_bitint_cast`, `src/bitint_slice.h:401`, OR-reduces all limbs) but the implicit sinks — `gen_assign_cast` (`src/mccgen.c:5986`→`gen_cast`) and the initializer/`vstore` store paths — do NOT. `_Bool b = (_BitInt(128))5;` → mcc stores byte **5** (out-of-range boolean; `*(char*)&b`==5); `_Bool b = (_BitInt(128))1<<100;` → mcc **0** (low bits zero); both clang and gcc give **1**. Three distinct broken sinks: initializer (→0), local assignment (→raw value), global assignment (→raw value). N=128 is in-range for all oracles; also holds for mcc-only `_BitInt(192)/(256)`. Distinct from T-mac-30140/30141 (switch paths). Fix: route the `_BitInt`→`_Bool` implicit-conversion path through the same OR-reduction the explicit cast uses. `[NEW, HIGH]`. **TASK: T-mac-30169.**

### <a id="r28-mempcpy"></a>`__builtin_mempcpy` fails to link on macOS — HIGH
- **[HIGH, link failure] `__builtin_mempcpy` emits `bl _mempcpy`, but `mempcpy` is a GNU extension absent from macOS libc** → `error: unresolved reference to '_mempcpy'`. The builtin is a plain prototype whose `__asm__` redirect (`runtime/include/mccdefs.h:715` via the `__BUILTIN` macro at `:657`) targets a nonexistent symbol; clang/gcc inline it (return dst+n). Also breaks `__builtin___mempcpy_chk` (`:821-824`) → the `mempcpy`-family `_FORTIFY` paths. Fix: provide an inline/self-hosted `mempcpy` (e.g. `memcpy` then return `dst+n`) on targets lacking it. `[NEW, HIGH]`. **TASK: T-mac-30170.**

### <a id="r28-bzero"></a>`__builtin_bzero(p, n)` zeros the wrong byte count on macOS — HIGH
- **[HIGH, data corruption] `__builtin_bzero(p, n)` zeros only 1 byte** — the Apple-only macro `#define __builtin_bzero(p, ignored_size) bzero(p, sizeof(*(p)))` (`runtime/include/mccdefs.h:404`, block `:401-406`) DISCARDS the size argument and passes `sizeof(*(char*))`==1; `__builtin_bzero(z, 8)` on an 8-byte buffer leaves 7 bytes non-zero (clang/gcc zero all 8). Secondarily the macro expands to a bare `bzero(...)` so without `#include <strings.h>` it errors `implicit declaration of 'bzero'`. Fix: `#define __builtin_bzero(p, n) __builtin_memset((p), 0, (n))` (a working redirect that needs no extra header). `[NEW, HIGH]`. **TASK: T-mac-30171.**

### <a id="r28-ld-arith-demote"></a>`long double` binary-arithmetic result demotes to `double` — MED
- **[MED, type-system] `a+b` for `long double a,b` has type `double` in mcc, `long double` in clang/gcc** — because arm64-darwin sets `MCC_USING_DOUBLE_FOR_LDOUBLE` (`src/mcc.h:109-111`) so `long double` is `VT_DOUBLE|VT_LONG`, and `combine_types` (`src/mccgen.c:4662-4668`) inspects only `VT_BTYPE`: the `VT_LDOUBLE` branch is dead, the `VT_DOUBLE` branch sets `type.t=VT_DOUBLE` dropping the `VT_LONG` long-double tag. So `+ - * / ?:` on long doubles demote; `_Generic((a+b))` picks `double`, `__typeof__`/`__auto_type` yield `double`, `<tgmath.h>` dispatch on a long-double EXPRESSION picks the non-`l` variant. No numeric/ABI error (LD==double here) — a C23 6.3.1.8 conformance bug. Bare vars, unary `-a`, and `1.0L` literals stay long double (correct). Fix: preserve the `VT_LONG` tag in `combine_types` when either operand is `VT_DOUBLE|VT_LONG`. `[NEW, MED]`. **TASK: T-mac-30172.**

### <a id="r28-sizeof-ld"></a>`__SIZEOF_LONG_DOUBLE__` reports 16, contradicts `sizeof(long double)==8` — MED
- **[MED, wrong macro] `__SIZEOF_LONG_DOUBLE__`==16 while `sizeof(long double)`==8 on arm64-darwin** — `src/mccpp.c:5773` emits the compile-time `MCC_LDOUBLE_SIZE` from `src/arch/arm64/arm64-gen.h:27` (`#define MCC_LDOUBLE_SIZE 16`), not overridden under `MCC_USING_DOUBLE_FOR_LDOUBLE`/MACHO. `type_size`, layout, and every `__LDBL_*`/`<float.h>` range macro correctly reflect the 8-byte double, so this lone macro is self-contradictory; code using it as a byte count for a `long double` object over-reads/over-writes. clang/gcc both report 8. Fix: emit 8 for `__SIZEOF_LONG_DOUBLE__` when long double is the double alias. `[NEW, MED]`. **TASK: T-mac-30173.**

### <a id="r28-sync-nand"></a>`__sync_fetch_and_nand`/`__sync_nand_and_fetch` missing — MED
- **[MED, missing builtin] the two `nand` forms of the legacy `__sync` family are absent** — `runtime/include/mccdefs.h:366-375` defines add/sub/or/and/xor but not nand; `__sync_fetch_and_nand(p,5)` → `error: implicit declaration` → link failure (clang/gcc compile). Fix: add the nand macros (mapping to `__atomic_fetch_nand`/`__atomic_nand_fetch`). The rest of `__sync_*` is robust (real `ldaxr`/`stlxr` atomicity, correct old/new + CAS bool/val semantics, 1/2/4/8-byte widths). `[NEW, MED]`. **TASK: T-mac-30174.**

### <a id="r28-memcmp-fold"></a>`__builtin_memcmp`/`__builtin_constant_p(__builtin_strlen(...))` don't const-fold in ICE — MED
- **[MED, fold gap] `int a[__builtin_memcmp("abc","abc",3)==0?1:-1];` and `int a[__builtin_constant_p(__builtin_strlen("hello"))?1:-1];` → mcc `error: constant expression expected`/`invalid array size`; both oracles fold** — root cause is the libcall-redirect design (`mccdefs.h:657` declares them as plain prototypes, nothing recognizes them in the ICE evaluator). Same root as T-mac-30104 (strlen); the `memcmp` arm + the `constant_p(strlen)` interaction are distinct manifestations. Fix: add fold hooks for `__builtin_memcmp`/`strlen`/etc. over string-literal args in the const evaluator (cluster with T-mac-30104). `[NEW, MED]`. **TASK: T-mac-30175.**

### <a id="r28-f128-static"></a>Static/global `__float128`/`_Float128` initializer rejected — MED
- **[MED, rejects-valid] `__float128 g = 2.5;` (or `_Float128`, or `static` local) → mcc `error: '__float128' conversion is not a load-time constant`** (`src/mccgen.c:5369-5370`, the `DATA_ONLY_WANTED` guard); gcc-16 accepts (clang lacks the type). The quad type otherwise fully works — BSS globals, arrays, struct members, and all local arithmetic are correct (via `__multf3`/`__divtf3`/… libcalls) — only compile-time constant folding into a static initializer is missing (the evaluator can't run the tf-libcall). Fix: fold a `__float128` constant initializer at compile time (host quad arithmetic or a soft-float fold) into the data section. `[NEW, MED]`. **TASK: T-mac-30176.**

### <a id="r28-f128-suffix"></a>`__float128`/`f32`/`f64`/`q` floating literal suffixes unparseable — MED
- **[MED] no way to write a quad or interchange-type floating literal** — the suffix logic (`src/mccpp.c:3641-3658` hex, `:3698-3716` decimal) recognizes only `f16`/`F16`/`F`/`L`; `1.5f128`/`1.5F128`/`1.0q`/`1.0Q`/`1.5f32`/`1.5f64` → `error: invalid number`. gcc-16 accepts. Combined with T-mac-30176 there is no way to spell a compile-time `__float128` constant at all. Fix: parse the `f32`/`f64`/`f128`/`F128`/`q`/`Q` suffixes to the matching FP type. `[NEW, MED]`. **TASK: T-mac-30177.**

### <a id="r28-ext-fp-types"></a>`__fp16`/`_Float32`/`_Float64`/`_Decimal32/64/128` types unsupported — MED cluster
- **[MED, cluster; clean errors] mcc rejects several extended FP TYPES that gcc-16 supports** — `__fp16` (ARM storage-only half; no token; note its arithmetic promotes to `float` unlike `_Float16` which stays `_Float16`), `_Float32`/`_Float64` (C23 interchange types; sizeof 4/8; known overlap with prior-round `_Float32/64` findings; clang also rejects on this target), and `_Decimal32/64/128` (C23 decimal FP; gcc supports, clang rejects; stabs strings exist at `src/mccdbg.c:43-45` but the parser is not wired). All are CLEAN "undeclared/not supported" errors — no crash/miscompile. mcc DOES support `_Float16`/`__bf16`/`__float128`/`_Float128` correctly (semantically solid, cross-module ABI verified). Fix: wire the missing type spellings (at least `__fp16`/`_Float32`/`_Float64` as aliases; `_Decimal*` is a larger feature). `[NEW, MED]`. **TASK: T-mac-30178.**

### <a id="r28-low-cluster"></a>LOW cluster — efficiency/cosmetic
- `__sync_synchronize()` is emulated via a seq_cst `__atomic_fetch_add` on a stack `volatile int` (`mccdefs.h:387-389`) → `bl __atomic_fetch_add_4` instead of an inline `dmb ish`; still a full barrier, correctness OK, just a call + stack access. Extended-type inf/nan/huge_val builtins missing (`src/mcctok.h:280-288` has only `{,f,l}` variants; `__builtin_inff16`/`__builtin_nanf128`/`__builtin_infq` → implicit-declaration error; gcc has them). `__FLT128_MAX__` predefined macro missing though `__float128` is supported (`__FLT16_MAX__` present). C23 `bool b; b++;` no `-Wbool-operation` warning (clang also silent → matches an oracle). `[NEW, LOW]`. **TASK: T-mac-30179.**

### <a id="r28-robust"></a>Strong negatives — arm64 aggregate ABI, __sync atomicity, _Bool core, mem/str runtime, long double, extended-FP-where-supported
- **arm64 (AAPCS64) aggregate calling convention FULLY ROBUST** (cross-compiler-verified mcc↔clang↔gcc, both call directions, native run): HFA of 1-4 float/double members in v0-v3 (pass+return), 5+-member aggregate by reference, small `{int,int}`/`{long,long}`/`char[16]` in GP regs, `>16B` by reference, mixed int+float NOT an HFA, V-register exhaustion → HFA entirely on stack (no split), x8 indirect return, the **Apple variadic-float/HFA-on-stack** rule, `_Complex float/double` HFA-like, nested/array HFA, empty struct, FAM-by-value. `src/arch/arm64/arm64-gen.c:978-1157`.
- **`__sync_*` atomicity**: real `ldaxr`/`stlxr` loops via `runtime/lib/atomic.c`; correct old-vs-new + `bool`/`val` CAS + `lock_test_and_set`/`release` semantics; 1/2/4/8-byte widths; `_Atomic`/`volatile` operands.
- **`_Bool` core conversion**: `(_Bool)256`==1 (and 0x10000, 4G — low-8-bits-zero all → 1, not 0), stored byte is 1, assignment/init/arg/return from ordinary integers+doubles, arithmetic promotion, compound/increment, `_Bool` bitfield normalization, `_Generic`, C23 `bool`/`true`/`false` — all correct (only the `_BitInt` source path, T-mac-30169, is broken).
- **`__builtin_mem*`/`str*` runtime**: memcpy (n=0..64, non-const), memset (byte-replication), memmove (fwd/back overlap), memcmp (sign + unsigned-char + n=0), strcpy/strncpy(padding)/strcat/strncat, strlen/strcmp/strchr/strrchr/memchr, stpcpy/bcopy/bcmp, struct copy, packed/underaligned dest, `*_inline` forms.
- **`long double`**: sizeof/align 8/8, `<float.h>` + `__LDBL_*` macros (53-bit), arithmetic precision + const-fold==runtime, printf `%Lf/%Le/%Lg/%La` varargs, by-value pass+return ABI, conversions, `long double _Complex`, `strtold`, tgmath numeric.
- **Extended FP where supported**: `_Float16`/`__bf16` (sizeof/arith/literals/`_Generic`/ABI/static-init), `__float128`/`_Float128` local arithmetic + comparisons + conversions + cross-module ABI (`__multf3`/`__divtf3`/…), usual-arithmetic-conversion ranks.
`[NEW]` negatives.


## Pending taskification (captured, not yet minted)

Lower-severity or newly-arrived items awaiting a `TODO.md` task in a later loop iteration: Round-15 PIC/PIE `-fno-pic` silently ignored + arm64 static-via-GOT default (`#r15-pic-robust`); Round-15 missing `-Wsizeof-array-decay` (`#r15-offsetof-robust`); Round-14 `__int128` availability on arm64/riscv64/x86_64-PE (`#r14-wideint-abi-robust`, folds into T-mac-30063); Round-13 `<threads.h>` `thrd_nomem` under-mapping + classifier op-table omission (`#r13-threads-robust`); Round-13 effect-oracle `ast_expr_pure` blacklist + `rir_effectful` x86-only atomic detection (`#r13-effect-robust`); Round-12 `_Alignas`-under-align-on-member silent-accept (`#r12-struct-layout-robust`); Round-12 math-builtin arg-multi-eval + dropped-`nan`-payload (KNOWN `DETAILS.md:14956`) + x86_64 FMA libc-fallback (`#r12-math-builtins-robust`); Round-12 arm64/riscv64 `__int128` compiled-out despite ABI support (`#r12-bitint-cast`); Round-9 arm64 `FCMP`-vs-`FCMPE` signaling-relational (`#r9-fp-robust`); Round-9 section-layout LOW trio — Mach-O align clamp / zerofill nonzero offset / ELF 32-bit file-offset overflow (`#r9-section-layout`); type/C23 bit-field gaps (`#type-c23-gaps`); runtime/intrinsics (`#runtime-intrinsics`, partly folded into T-mac-30047); Metal-vs-Vulkan f64 (`#gpu-metal-vs-vulkan-f64`); JIT RWX/`MAP_JIT` (`#jit-rwx-mapjit`); debug-info asymmetries beyond Mach-O unwind (`#debug-info-asymmetry`); Round-7 initializer `int`-offset overflow on ≥2 GB objects (`#r7-init-robust`); Round-7 macro-expansion `-E` whitespace cosmetic (`#r7-macro-robust`). The JIT lazy-vs-sync root cause (`#jit-lazy-vs-sync-kgc`) should be attached to existing **T-lin-10029**, not minted separately. (The Round-1 arm64 `va_arg` assert `#codegen-arm64-vaarg-assert` is now precisely located and folded into **T-mac-30045**.)
