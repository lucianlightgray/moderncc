- [Always commit and push to main](always-commit-push-main.md) — trunk-based: commit fixes and push straight to main (rebase on reject), no branch/PR, don't ask
- [mcc codegen gap is regalloc](mcc-codegen-gap-is-regalloc.md) — mcc runs 2-4x slower than clang because of the leaf-only regalloc cliff, not AST passes; -O60 superopt & -O levels don't help; 24x faster compile is the moat to protect
- [mcc_add_support archive selection](mcc-add-support-archive-selection.md) — how mcc picks plain libmccrt.a vs arch-tagged <arch>-libmccrt.a, and the native-fixpoint vs cross-selfhost tension it navigates
- [Never dismiss micro-optimizations](never-dismiss-micro-optimizations.md) — all opt techniques are valid; ROI must report whether an opt EVER fires, never pre-judge worth
- [.data.rel.local self-miscompile](data-rel-local-selfmiscompile.md) — latent: gcc-built vs mcc-built mcc diverge on RELRO section synthesis when linking an executable; caught by fixpoint-invariant, not yet root-caused
- [win32 ratchet is nondeterministic](win32-ratchet-nondeterministic.md) — mingw ast-verify-ratchet flakes: win32 fidelity gap set differs run-to-run on identical code; no static baseline reliably greens it from a non-mingw host
- [ratchet is not self-host coverage](ratchet-not-selfhost-coverage.md) — the recorder-fidelity ratchet doesn't run a self-host, so an AST gate can green the ratchet while miscompiling mcc itself; run selfhost-fixpoint -O2/-O3/-Os before flipping a gate default-on
- [osx selfhost-jit FIXED](osx-selfhost-jit-sympool-corruption.md) — macOS selfhost-jit PASSES; it was two self-compile miscompiles (storeval-arg vstack underflow b6af0f7f + RANGE landor-invert drop 2c88e797), NOT a JIT bug. selfhost-fixpoint byte-identity can't catch stable miscompiles — add a -run/-O4 leg
- [landor-invert fold bug class](landor-invert-fold-bug-class.md) — AST folds that rewrite LAND/LOR drop AST_FB_LANDOR_INVERT and invert negated conditions; RANGE + BITFLAG both FIXED; tracediff BITFLAG=5 vs =100 found the missed ast_bf_try_if; audit other fold sites for the pattern
- [i386 d2 miscompile — FULLY FIXED](i386-struct-return-loop-miscompile.md) — the i386-codegen-diff d2 -O2 bug was DIVMAGIC (ltemp spill-slot reuse clobbers the live dividend), NOT the struct return; FIXED 2026-07-30 by making get_temp_local_var ltemp-aware (ast_ltemp_overlaps), divmagic default-ON preserved; d1..d14 match gcc -m32
- [FP differential fuzzing](fp-differential-fuzzing.md) — fuzz mcc FP codegen vs clang on arm64 with -ffp-contract=off (bit-exact, no false positives); found+fixed fabs(-0.0) signed-zero bug; the `x<0?-x:x` float-abs bug class
