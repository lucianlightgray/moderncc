- [macОС/arm64 status](macos-arm64-status.md) — build works; long double==double quirk (MCC_USING_DOUBLE_FOR_LDOUBLE), Mach-O TLS/threads.h gaps, diff3 divergences
- [asan-shadow x86 hang](asan-shadow-x86-hang.md) — x86_64 -fasan-shadow heap-test hang = unmapped ShadowGap → reporter nested-fault; fixed by contiguous map; qemu can't run the shadow runtime (not locally reproducible)
- [host-dependent FP fold](host-dependent-fp-fold.md) — negative-float→unsigned constant fold was UB → mcc emitted different i386 code per build-host arch (d3 diff); fixed via int64 fold + de-UB'd the test
- [macOS CI sleep-leak](macos-ci-sleep-leak.md) — BAD_COMMAND storms = fuzz/diff3 shell timeout fallback orphaning `sleep 20` procs (no gtimeout on macOS) → RLIMIT_NPROC; fixed by killing the sleep by PID
- [empty-TU builtin preamble](empty-tu-builtin-preamble.md) — empty-translation-unit diag can't check tok==TOK_EOF at parse start (builtin `<command line>` preamble runs first); still-open dg-error gap
- [embed-jit arm64 vla -O1 crash](embedjit-arm64-vla-o1-crash.md) — MCC_EMBED_JIT arm64 build segfaults on vla/basic.c -O1 → 14 expected exec-*/basic failures (pre-existing, config-specific)
- [mcc JIT unification](mcc-jit-unification.md) — runtime --jit/MCC_JIT/MCC_CONFIG_JIT trigger model; embed-registry pollution bug/fix (mccjit_internal_compile); deferred string-literal JIT
- [arm64 load() r-flag mask](arm64-load-rflag-mask.md) — new front-end VT_* r-field flags must be added to arm64 load()/asm mask or codegen asserts (x86 stays green)
- [build dir prefix](build-dir-prefix.md) — always use cmake-build-* dirs, never plain build/ (reuse cmake-build-debug / cmake-build-cross)
- [exec_runner absolute paths](exec-runner-absolute-paths.md) — manual exec_runner repros need absolute mcc + tests-root args (else spurious file-not-found after its internal `cd work`)
- [cli_runner env filters](cli-runner-env-filters.md) — direct cli_runner runs skip the elf/os/cpu filter tags (need MCC_TEST_* env); use `ctest -R '^cli/'` to avoid spurious "failures" (full suite is 100% green on arm64/macOS)
- [Union init partial zero](union-init-partial-zero.md) — automatic Operand braces only zero the first union member; gcc leaves e.v garbage (dash-s-bytes CI class)
- [arm64 Linux runtime libcalls](arm64-linux-runtime-libcalls.md) — arm64 CI atomics/bounds/complex fails were unresolved libgcc/compiler-rt libcalls (outline-atomics, __unordtf2), not weak-memory; repro via Docker arm64 on Apple Silicon
- [No code comments](no-code-comments.md) — never write code comments anywhere in this repo; rationale goes in commit messages / NOTES / TODO
- [AST-replay SYM_DEBUG debugging](ast-replay-symdbg-debugging.md) — reproduce heap-dependent MCC_AST_REPLAY stale-Sym crashes deterministically with ASan + -DSYM_DEBUG=1
- [reemit faithful gate](reemit-faithful-gate.md) — -O3 bench n/a: ast_reemit_retain must require ast_fn_faithful (reemitting an unfaithful body ships broken code)
- [CI workflows are canonical](ci-workflows-canonical.md) — .github/workflows/*.yml are the hand-edited source (the ci_artifacts.h/`ci emit` generator was removed 2026-07-23); bootstrap via .github/actions/ci; `ci parity` still cross-checks presets against the live yml
- [CST/AST independence](cst-ast-independence.md) — keep separate id spaces; only sanctioned cross-link is the one-way AST→CST cst/CstId reference, never a shared/merged namespace
- [Commit+push to main](commit-push-main.md) — user works on main directly; commit and push there without branching or a PR
- [mccast raw-alloc region](mccast-rawalloc-region.md) — new AstArena-allocating fns must live in the push_macro/undef malloc window in mccast.c (else poison error / free mismatch)
- [embed-jit x86 test + Rosetta](embedjit-x86-test-rosetta.md) — x86_64-PE JIT RUNS under wine (full local verify path via mingw+wine); native Mach-O/Linux x86 RWX hangs under Rosetta; SysV-Linux needs Docker amd64
- [parallelize with agents](parallelize-with-agents.md) — default to fanning out independent work to concurrent subagents (user directive)
- [4B backend-parity landed](4b-backend-parity.md) — branchless select (MCC_AST_SELECT), 64-bit divmagic (MCC_AST_DIVMAGIC), cond?1:0 desync fix, + reemit-driver standalone-strategy hit-count bug fix
- [native-host validation gotchas](native-host-validation-gotchas.md) — RESOLVED: the 7 arm64-macOS exec/ fails (far-libc) + the cross-build ast_alloc_loc break are both fixed (historical)
- [arm64 JIT W^X dual-map](arm64-jit-wx-dualmap.md) — Apple Silicon gates the runtime JIT off (RWX blocked); [2B] arm64 stubs must use host_runmem_alloc dual-map, not RWX/MAP_JIT
- [arm64 JIT dispatch before prologue](arm64-jit-dispatch-before-prologue.md) — arm64 mode-6 stub emitted at func-start (before the 224B frame); all *slot targets must be complete self-contained functions
- [Docker amd64 repro](docker-amd64-repro.md) — reproduce x86_64/Release-only mcc failures from the arm64 host via linux/amd64 Docker; emulation caveats
- [Docker i386 exec](docker-i386-exec.md) — linux/386 Docker runs i686 binaries here; the path to ungating i386-Linux exec tests (qemu-i386/32-bit-wine unavailable); i386-fastcall ABI validated via tools/i386fastcall-docker.sh + docker-gated ctest
- [cross-arch exec matrix](cross-arch-exec-matrix.md) — what runs here: i386 (Docker linux/386), arm64 (native), AND armv7/riscv64 via qemu-user inside a NATIVE-arm64 container (never --platform linux/amd64 — nested emul breaks qemu-arm VA reservation); x86_64 Docker works but no mcc-x86_64 target
- [i386 -fPIC TLS miscompile](i386-fpic-tls-miscompile.md) — mcc-i386 -fPIC TLS segfaults (no GD/LDM codegen; emits GOT32X for global __thread); non-PIC LE works; open bug w/ Docker repro
- [arm64 -run far-call veneer](arm64-run-far-call-veneer.md) — arm64 mcc -run veneers far external CALL26 through .mcc.veneer stubs so it reaches host libc; fixed the arm64/macOS exec far-libc failures
- [emit-size measurement isolation](emit-size-measurement-state-restore.md) — ast_search_emit_size must replay into the scratch Section, never the live text section; in-place measure+rewind leaks state → timing-dependent emitsize/enum miscompile on CI
- [jit selftest-bench tie flake](jit-selftest-bench-tie-flake.md) — tie case is ctest -j contention noise; fix = many SHORT bench rounds (MCC_JIT_BENCH_ROUNDS, min converges), not longer iters
- [Dist diag-rt=off embed-jit build](dist-diagrt-off-embedjit-build.md) — Dist is the only non-amalgamated + diag-rt=off + embed-jit config; surfaces decls stuck behind #if MCC_CONFIG_DIAG_RT>=2 that new unconditional callers need; repro via -DMCC_CONFIG_DIAG_RT=off
- [_FORTIFY_SOURCE neutralized](fortify-source-neutralized.md) — mcc has no _chk builtins; neutralizes _FORTIFY_SOURCE in preprocess_start AFTER -D (header defines can't override -D); else glibc string.h fortify won't compile
- [i386 SSE FP model](i386-sse-fp-model.md) — mcc-i386 uses SSE FP (FLT_EVAL_METHOD=0) not x87; i386 codegen diffs must build gcc reference with -mfpmath=sse -msse2 or FP-rounding programs diverge
- [Docker macOS /tmp mount](docker-macos-tmp-mount.md) — Docker bind mounts here only share /Users; mount workdirs under the repo, never /tmp/scratchpad; inline scripts; exclude cmake-build-*/.git/vendor when copying source
- [gen_op recomputes vtop signedness](gen_op-recomputes-vtop-signedness.md) — AST Binary re-emit ignores the node's recorded type; gen_op derives signedness from operand usual-conversions (S32+U32→unsigned). Bit signed-32 divmagic (fixed a1df06c1); force result signedness with an explicit AST_Convert
- [cross mcc lacks optimizer](cross-mcc-lacks-optimizer.md) — cmake-cross/mcc-i386 & mcc-arm64 built WITHOUT MCC_CONFIG_OPTIMIZER → AST reemit (divmagic/select/promote) compiled out; cross-reemit validation is vacuous unless you hand-build an -opt cross mcc
- [arm64 embed-jit host-wrapper self-recurse](arm64-embedjit-hostwrapper-selfrecurse.md) — FIXED: baked --embed-jit arm64 SIGSEGV was armflush.c __clear_cache built via __builtin___clear_cache (self-call); now inline dc/ic. JIT engine 47/47 clean
- [ratchet is two-sided](ratchet-stacksafe-flaky.md) — ast-verify-ratchet fails on gap-set SHRINK too (baseline gap→faithful = fatal drift); CI #1082/#1086 red = deferred x86_64-linux baseline regen after b100da94; never defer one target's regen
- [ABI differential harness + docker apostrophe gotcha](abidiff-harness-and-docker-apostrophe.md) — abidiff-docker.sh finds caller/callee-asymmetric ABI bugs self-host masks (x86_64 mixed-SSE, riscv64 sret+FP-arg); *-docker.sh bash -c '…' blocks break on apostrophes in comments
- [drive WSL from Git Bash (MSYS guard)](drive-wsl-from-git-bash-msys-guard.md) — host has WSL2 Ubuntu 26.04 (root-only, --no-launch); Git Bash driving wsl.exe needs MSYS_NO_PATHCONV=1 MSYS2_ARG_CONV_EXCL='*' or Unix-path args get mangled
- [native x86 first, Docker for cross](native-x86-first-docker-for-cross.md) — host is x86_64-native; validate x86/x86_64 on-box via native ctest, use Docker/qemu ONLY for non-native triples (riscv64/arm64/…)
- [Windows embed-JIT self-host no-dep](windows-jit-embed-selfhost-nodep.md) — mcc self-hosts an embed-JIT build with NO external windows.h; mccjit_win32.h defines SRWLOCK/CONDITION_VARIABLE/INIT_ONCE locally, guarded by *_INIT macros (real toolchain skips → no-op)
- [PE import-library support](pe-import-library-support.md) — mcc consumes mingw long-import COFF members + fixes COFF .bss-size/section-reloc-double-count bugs; --embed-jit exe RUNS on x86_64-PE under BOTH MCC_JIT=0 and MCC_JIT=1 (self-recompile). Also fixed CRLF trace-gate Windows-CI failure (tools/tracegate.c strip)
- [arm64 MSVC replay/jit failures](arm64-msvc-replay-miscompile.md) — 29 msvc/arm64 CI fails (22 exec-replay run_atexit+errors_and_warnings, 7 jit/selftest). /Od DID NOT FIX IT (disproven 2026-07-18: /Od verified applied yet still fails; wine validation was false-positive). NOT the optimizer; hardware-gated; options = skip-gate arm64/WIN32
- [arm64-PE JIT segfault](arm64-pe-jit-segfault.md) — the 7 jit/selftest SEGFAULTs; /Od did NOT resolve (corrected 2026-07-18); investigation trail (arm64 JIT logic correct on Linux; clang-PE repro dead end)
- [AST verify ratchet & teardown](ast-verify-ratchet-and-teardown.md) — MCC_AST_VERIFY harness, recorder-fidelity ratchet, gap measurement blocking desync/faithfulness teardown
- [moderncc Windows build](moderncc-windows-build.md) — how to build/test mcc on this Windows host (CLion mingw toolchain)
- [moderncc Windows port fixes](moderncc-windows-port-fixes.md) — the Windows/mingw bugs fixed to get ctest green
- [mcc-dynamic vs ONE_SOURCE](mcc-dynamic-onesource.md) — mcc-dynamic is now a self-contained single-source exe (historically needed MCC_ONE_SOURCE=OFF + linked libmcc)
- [MSVC CustomBuild error-scan](msvc-custombuild-error-scan.md) — VS-generator fails a custom target if its output has an `error`-format line, even on exit 0
- [arm64-Windows repro + atomics](arm64-windows-repro-and-atomics.md) — Docker repro of msvc/arm64; why qemu/wine masks weak-memory hangs; mcc atomics = library calls (identical ELF/PE). UPDATE 2026-07-20: wine-arm64 in Docker DOES boot+run mcc arm64-PE exes (c0000135 is only the non-fatal WoW stub) — usable codegen/logic validation path + working recipe; dynamically validated the arm64-PE over-align change
- [moderncc Windows sanitizers](moderncc-windows-sanitizers.md) — what sanitizer tech works for mcc on Windows (MSVC ASan, mingw trap-UBSan) + the alignment-UB gotcha
- [reformat mangled lexical fixture](reformat-mangled-lexical-fixture.md) — CI conformance-native failures = a reformatter corrupting the token-sensitive lexical.c corpus file
- [CI log zip commit ahead](ci-log-zip-commit-ahead.md) — a handed logs_<id>.zip usually ran on a commit ahead of local HEAD; get the SHA + fetch before repro
- [mcctest AST-promote debug](mcctest-ast-promote-debug.md) — what mcctest is, AST-opt env gates, and how to bisect a promote codegen bug (PE link-time layout crash)
- [mingw i686 → native i386](mingw-i686-cross-width-jit.md) — retargeted the i686 cell to native i386 (was mis-targeting x86_64); the copysignf/_Alignas/host-detect i386-Windows fixes
- [mcctest WIN32 msvcrt / i386 excess precision](mcctest-win32-msvcrt-i386-excess-precision.md) — i686-only mcctest mismatch = x87 excess precision (shared msvcrt printf → mismatch means differing bits); fix = -fexcess-precision=standard + a first-diff-line diagnostic
- [JIT dispatcher x86-only guard](jit-dispatcher-x86-only-guard.md) — the §26 AST JIT entry-dispatcher emits raw x86 opcodes (oad/o/g); must stay behind #if i386||x86_64 or all non-x86 builds break; recurs as W-stages expand it
- [predefs-off system-header pedantic](predefs-off-sysheader-pedantic.md) — PREDEFS=OFF loads runtime mccdefs.h (//@ markers → // comments); lexer pedantic diags must exempt system headers or -std=c89 -pedantic-errors dies inside it
- [mcctest ref-crash skip](mcctest-ref-crash-skip.md) — mcctest SKIPs (77) not fails when the *reference* binary crashes; the i686 0xC0000005 is CI Server-2025-msvcrt-specific, not codegen/mcc
- [Windows reference toolchains](windows-reference-toolchains.md) — portable MSVC (cl 19.51.36248, matches CI) + llvm-mstorsjo-msvcrt clang installed locally for PE/msvcrt differential debugging
- [promote PE thunk stale-text](promote-pe-thunk-stale-text.md) — arm64-PE promote hang root cause (AST replay stale .text tail corrupts import-thunk literals) + cross build lacks MCC_CONFIG_OPTIMIZER so promotion is a local no-op until patched
- [Windows JIT-embed port](windows-jit-embed-port.md) — DONE: runtime JIT works on PE (mccjit_win32.h shim + Win64-ABI stubs + PE ctor/-run pipeline), MCC_EMBED_JIT ungated, 32/32 jit selftests; use MCC_HOST_WIN32 not _WIN32 (hostgate invariant)
- [COFF reader / embed-blob](coff-reader-embed-blob.md) — mcc's linker reads COFF objects/archives + short-import (MSVC/LLVM/SDK) members (mccpe.c, PE-only); embed-blob links on Windows; embed fn-selection works on all PE arches; embed-jit blocker is i386 stdcall __imp_ + mingw-leaf, NOT short-import (winlibs mingw = 100% long-import, disproven 2026-07-21)
- [Windows over-align + UBSan](windows-codegen-overalign-ubsan.md) — x86_64-PE auto over-alignment + trap-mode UBSan enabled; the win64 alloca shadow-space gotcha (block in rax=rsp+32, round RAX not RSP)
- [win-open-work: four Windows items](win-open-work-four-items.md) — branch implementing ASan-PE port + embed-jit __imp_ resolver + ftoi AST desync + diff3 anti-false-green; what landed vs toolchain/HW/soak-gated; NOT pushed
- [dwarf single-line prologue-end bug](dwarf-singleline-prologue-end-bug.md) — FIXED 2026-07-23 (387c6da7): x86 single-line prologue-end row materialized via DW_LNS_copy (advance_pc appends no row → flag leaked to next fn); earlier arm64-only "fix" masked it (small prologues fit the special opcode)
- [x86 optimizer differential via Docker](x86-optimizer-differential-docker.md) — how to build an optimizer-enabled cross-mcc + validate AST gates (divmagic/select/promote/...) vs gcc under Docker on this Windows host; the -O1/-O2 gate-firing gotcha, freestanding-leaf-fn pattern, MSYS/bind-mount fixes, tracked templates (i386-divmagic-soak-docker + native-optcheck)
