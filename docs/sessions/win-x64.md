# Session: win-x64  (Windows / x64 / band 50000–69999)

Only win-x64 writes this file. Authoritative session state (replaces the shared
`TODO.md` Sessions-table row; see `INSTRUCTIONS.md` §4.1). Liveness of record is
the commit log; the `Last seen` below is advisory.

Next ID: 50062
Last seen: 2026-08-22T16:05Z
Capabilities: x86_64-windows native (PE) · MSVC/cl + winlibs-ucrt mingw + scoop clang · WSL2 (x86_64-linux native validation, incl. `*-win32` cross-build for o0-baseline rebank) · gpu-vulkan (RTX 2060 — DEVICE-BLOCKED for fault tests without a game-off/TDR window) · NO x86_64-linux/macos native AOT · NO i386-toolchain · NO arm64-woa runner · NO rosetta/qemu
STATUS: 2026-08-22T16:05Z -- **T-lin-10510 VARIABLE-COUNT funnel slice DONE + released OPEN (d236d85f1, P2).** Continued the /goal loop after the const funnel slice (§10.11: complete-slice → continue, not stop). Selected T-lin-10510 again by §11 (tier-5 [S], P2 mission priority, highest locality — I own the hot idiom substrate). First established the CONST funnel family is already COMPLETE: `(x>>C)|(y<<(W-C))` folds to `shld` for free (OR-commutative → SHL-term becomes dest), so no const-SHRD needed. Then landed variable funnel `(x<<n)|(y>>(W-n))` → `shld reg,cl`: new `ast_funnel_var_try` folds to a 3-child AST_OP_FSHL (child0=x,child1=y,child2=n) → new `gen_shld_var`. The 3-operand emit was the crux — pin n→RCX via `gv(RC_RCX)` (stays live so the allocator avoids RCX), then `vswap/gv/vswap` for y and `vrotb(3)/gv/vrott(3)` to rotate x up for its GP load, emit `SHLD-by-CL` (0F A5 /r, rm=dest reg=src), `vtop-=2`. VERIFIED native x86_64-PE (MSVC cmake-release, merged over lin's loop-vec): `shldl %cl`/`shldq %cl`, all 3 flag arms == gcc-O2 across a shift sweep on both widths, negatives DECLINE (signed, mismatched-var, same-base→rol, shrd-shape, flag-off), optfire rotate cell (vfnl/vfnl64) PASS, bswap/slp no-regress, o0/On-neutral, cross-arch clean by construction. Funnel family now covers const(both shapes)+var-shl; sole remaining funnel gap = the SHRD shape for variable count `(x>>n)|(y<<(W-n))`→`shrd reg,cl` (mirror slice). PRIOR (2026-08-22T15:50Z): **T-lin-10510 FUNNEL-SHIFT-LEFT (const) slice DONE + released OPEN (0ac9f006f, P2).** Resumed the previous session's banked WIP (funnel-shift `(x<<C)|(y>>(W-C))`→x86 `shld`). RE-DERIVED clean on the HEAD Windows tree per DETAILS#t-lin-10510-rotate-idiom (the WIP lived only in the base-pinned WSL clone — never whole-file-copied, dual-tree hazard): new `ast_funnel_try` (x86_64-gated, in the `ast_bswap_run` chain after the 3 rotate recognizers) + new `AST_OP_FSHL` 2-child unary (child0=x=dest, child1=y=src, ival=(sbytes<<8)|C) + new `gen_shld` (`0F A4 /r ib`, reg=src/rm=dest). **The banked WIP's `or %cl,%cl` garbage did NOT reproduce** — the recipe in the WIP note was the correct SHLD encoding all along; the WSL-clone bug was a transient artifact. Distinct-unsigned-operand 32/64-bit only (same-base=rotate, handled by the rotate path; 16-bit funnel operates on promoted int, no genuine lowering). VERIFIED native x86_64-PE (canonical MSVC cmake-release, rebuilt on the merged tree over lin's T-lin-10470 loop-vec): fires `shldl`/`shldq` + all 3 flag arms byte-identical to gcc-O2 + negatives DECLINE (same-base→rol, signed, sum≠W, `-fno-rotate-idiom`→scalar) + optfire `rotate` cell extended (fnl/fnlr/fnl64) PASS + `bswap`/`slp` no-regress + o0/On-neutral (default-OFF) + cross-arch clean by construction (all funnel symbols `#if MCC_TARGET_X86_64`, mirroring the verified rotate slices). This completes the **bswap + rotate (const/var, l/r) + funnel-shl** idiom family on win-native. Remaining T-lin-10510 follow-ups are bigger/other-box (funnel-SHRD mirror + variable funnel, arm64 EXTR/REV/ROR=mac, i386=no executor, popcount/clz/ctz-loop, SWAR) or rebank-gated defaults (x*C strength-reduction=lin). BUILD NOTE (banked): the MSVC release build must be launched with the VS env set — `cmd /c` from git-bash mangles `/c` (MSYS path-conv) so the batch never runs (silent no-op, stale mcc.exe); use PowerShell `& cmd /c vcvars.bat && cmake --build` (or `MSYS_NO_PATHCONV=1`). The llvm-mingw `cmake-build-debug` mcc COMPILES+folds correctly (proved the shld bytes) but its output exes SEGV even on hello-world (pre-existing debug-toolchain runtime artifact) — use cmake-release for run-verify. PRIOR (2026-08-22T15:01Z): SESSION HANDOFF (user-directed wrap-up). No active win claims; Windows tree CLEAN, all pushed. THIS SESSION (a long P2 run under /goal): **6 pushed deliverables** — (1) *-win32 o0-baseline rebank (d1022b09c, T-lin-10509 fallout); (2-5) the **bswap + rotate idiom family COMPLETE** under T-lin-10510: bswap→native bswap (e35de8cef), const-rotate→rol imm (c532b842d), var-rotate-left→rol cl (9e3519f2d), var-rotate-right mirror→ror cl (4d3a26990) — all default-OFF, x86_64-gated, verified x86_64-linux (optfire differ + gcc byte-match + no false-positives + ^exec 375/375 + 4 win32 cross-builds) AND native x86_64-PE; (6) lin's T-lin-10470 SLP verified green on native win-PE (optfire-x86_64 82/82). **WIP NOT COMMITTED: constant funnel-shift** `(x<<C)|(y>>(W-C))`→shld — recognizer WORKS (FUN_DBG MATCH confirmed) but gen_shld x86 encoding is wrong (emits `or %cl,%cl` not shld); precise resume map at DETAILS#t-lin-10510-rotate-idiom trailing WIP note (frag/splice in ephemeral scratchpad; recognizer design captured). The funnel WIP lives ONLY in the WSL clone /root/moderncc (uncommitted) — a fresh session should re-derive gen_shld from the DETAILS map, not rely on it. CONSUMED lin's FYIs: T-lin-10470 SLP CLAIM + landing (verified native-PE); rir-coverage-census (no win-floor action pending). LESSONS banked (log): AST-opt re-emit trigger; `>>`=TOK_SAR; 2-child-unary trick; **dual-tree copy hazard** (WSL clone is base-pinned — re-splice diffs, never whole-file-copy); mccopt.h/differs.txt are HOT shared append-points (my rotate-idiom knob collided with lin's SLP — lin fixed markers db445f7ea; I later gated funnel under the EXISTING -frotate-idiom to avoid more churn). Clean bounded default-OFF win-native idiom work now EXHAUSTED; remaining T-lin-10510 follow-ups are bigger/other-box (funnel gen_shld fix, variable funnel, arm64 REV/ROR legs=mac, i386=no executor, popcount/clz/ctz-LOOP, SWAR) or rebank-gated defaults (x*C multi-op strength-reduction = real gap but lin's rebank domain). PRIOR (2026-08-22T14:44Z): **T-lin-10510 bswap + ROTATE IDIOM FAMILY COMPLETE (P2):** bswap (e35de8cef) + rotate all four forms — const-left/right (c532b842d), var-left `rol reg,cl` (9e3519f2d), var-right mirror `(x>>n)|(x<<(W-n))` → new AST_OP_ROTR/gen_rotr_var `ror reg,cl` (4d3a26990). All default-OFF/x86_64-gated, verified x86_64-linux + native PE (optfire-x86_64/rotate PASS all forms). NOTE: lin's SLP commit (07a616f68) botched a merge vs my rotate-idiom knob in mccopt.h+differs.txt; lin resolved it keeping both (db445f7ea) — my rapid commits to those shared files created churn for lin. Confirmed the final rebased tree builds + rotate cell passes. FYI'd fleet via repo (mac to add arm64 REV/ROR/ROL legs). §11 note: the clean bounded default-OFF win-native idiom work is now largely exhausted (bswap+rotate done); remaining follow-ups are bigger builds (funnel/popcount-loop/SWAR), rebank-gated defaults (x*C strength-reduction — lin domain), or other-box legs (arm64/i386). PRIOR (2026-08-22T14:32Z): **T-lin-10510 THREE bit-idiom slices DONE (P2): bswap (e35de8cef) + const-rotate (c532b842d) + VAR-ROTATE (9e3519f2d).** Var-rotate: `(x<<n)|(x>>(W-n))` (n a side-effect-free Ref) → 2-child AST_OP_ROTL / new gen_rotl_var (`rol reg,cl`, reuses the 0xd3 shift-by-CL group + gv2). The AST_Unary replay already emits child0=x, so the emit branch replays child1=n when nchild==2. Verified x86_64-linux (optfire differ const+var + gcc byte-match + ^exec 375/375 + 4 cross-builds) AND native x86_64-PE (optfire-x86_64/rotate PASS). All default-OFF, x86_64-gated. Dual-tree hazard handled by re-splicing the diff onto the HEAD Windows tree (never whole-file-copy the diverged WSL clone). PRIOR (2026-08-22T14:19Z): **T-lin-10510 TWO bit-idiom slices DONE (P2) + re-OPENED: BSWAP (e35de8cef) + CONST-ROTATE (c532b842d).** Rotate: new `-frotate-idiom` folds `(x<<C)|(x>>(W-C))` (unsigned base, const C) → new unary AST_OP_ROTL + gen_rotl (native `rol`, x86_64-gated; same 0xc1 shift-group encoding gen_bswap's 16-bit path uses). Shares the bswap flatten/same-base helpers + ast_bswap_run driver + re-emit flag. Verified x86_64-linux (optfire differ + gcc byte-match + no false-positives + ^exec 375/375 + 4 win32 cross-builds) AND native x86_64-PE (optfire-x86_64/rotate PASS). o0/On-neutral. LESSONS (banked): (1) mcc records `>>` as TOK_SAR generically (signedness resolved at codegen) — a rotate recognizer must accept TOK_SAR + rely on a base-unsigned guard for logical-shift correctness. (2) DUAL-TREE HAZARD: the WSL clone (/root/moderncc) is pinned at an OLD base (c59f1e63e) and does NOT have the fleet's later mccast.c edits — a whole-file WSL→Windows copy REVERTS mac's work (caught mac's T-lin-10471 CSE being deleted pre-commit). FIX: apply only the DIFF (re-run the tab-safe splice scripts, ROOT=Windows path, on a HEAD-reverted file), never whole-file-copy a diverged source. FYI'd fleet via repo (mac to add the arm64 REV/ROR legs). PRIOR (2026-08-22T13:54Z): **T-lin-10510 bswap-from-shifts idiom recognition SLICE DONE (P2, e35de8cef, DETAILS#t-lin-10510-bswap-idiom) + task re-OPENED** for follow-up idioms. New default-OFF `-fbswap-idiom` ast-opt pass folds `((x>>24)&0xff)|((x>>8)&0xff00)|((x<<8)&0xff0000)|((x<<24)&0xff000000)` (any term order, non-volatile scalar Ref base, 2/4/8-byte) → single AST_OP_BSWAP (native bswap/rev, reusing the existing gen_bswap emit). Verified x86_64-linux (WSL2: optfire differ + gcc byte-match + no false-positives + ^exec 375/375 + 4 win32 cross-builds) AND native x86_64-PE (optfire-x86_64/bswap PASS). KEY IMPLEMENTATION LESSON (banked): an AST-opt EXPRESSION rewrite only reaches the object if a transform flag is set — the optimized re-emit `AST_PF_EMIT` (mccast.c ~22037) is gated on `interchanged||fused||…||unrolled||math_inlined`; a rewrite alone is discarded (parse output kept) and at the earlier faithful-trial phase (after ast_run_templates) falls back. Fix: hook in the `if(ast_opt_ok…)` opt phase after ast_loopidiom_run, capture the fire count into a new flag, and add it to both re-emit disjunctions + `!flag` to the faithful-noop negation. Also: reuse of an existing Ref subtree as the new child dangled arena links → -O4 SEGV; build a FRESH Ref copy. FYI'd fleet (mac to add the arm64 REV leg). PENDING (checkpoint, conditional): lin's rir-coverage-census win wide-floor rebank ONLY if a native full run mismatches (3dc76230f). PRIOR (2026-08-22T12:57Z): rebanked the four `*-win32` o0-baseline cross keys (+`x86_64.gated.rir.txt`) for lin's T-lin-10509 `divrem_fuse.c` corpus add, from a fresh WSL2 x86_64-linux cross-build — commit d1022b09c, ungated+gated CHECK both green, host-independence verified vs lin's native bank. Discharges the "win owes *-win32 keys (weak_undef+divrem_fuse)" item mac flagged; T-lin-10509's native-PE verify was already recorded confirmed (26/26). Full narrative + the wsl-`bash -lc` `$?`/PWD measurement lesson: docs/log/win-x64.md. PRIOR (2026-08-22T11:53Z): T-win-50060 DONE+ARCHIVED (mcc -O1 synthetic-alloca SEGV 4823e7f36 + clang -lm harness vacuity cc74f4449); device residual split to T-win-50061 [D] CAP:gpu-vulkan (slicerun GPU-dispatch concurrency stall) + cell quarantined KNOWN_RED(windows). Older checkpoints in git log + docs/log/win-x64.md. Open win-relevant tasks are device/deep/domain-gated per prior §10.11 sweeps: T-win-50061/50019/50003 [D] gpu (game-off window), T-win-50047 (Win64 struct-ABI heisenbug, deep/cdb), T-win-50041 (arm64-woa, no runner), T-win-50059 (win-PE VLA/alloca replay OPTIMIZE, deep keep-path), P2 fold-parity EXHAUSTED vs gcc, remaining OPT-ROUND2 lin-rebank-domain.
