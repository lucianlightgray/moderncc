# Session: win-x64  (Windows / x64 / band 50000–69999)

Only win-x64 writes this file. Authoritative session state (replaces the shared
`TODO.md` Sessions-table row; see `INSTRUCTIONS.md` §4.1). Liveness of record is
the commit log; the `Last seen` below is advisory.

Next ID: 50062
Last seen: 2026-08-22T11:53Z
Capabilities: x86_64-windows native (PE) · MSVC/cl + winlibs-ucrt mingw + scoop clang · WSL2 (x86_64-linux native validation) · gpu-vulkan (RTX 2060 — DEVICE-BLOCKED for fault tests without a game-off/TDR window) · NO x86_64-linux/macos native AOT · NO i386-toolchain · NO arm64-woa runner · NO rosetta/qemu
STATUS: 2026-08-22T11:53Z -- T-win-50060 DONE+ARCHIVED (the [S] fixable work) + split the device residual to T-win-50061 [D]. The cref-oracle-gcc-c-torture-execute cell failed on win-PE for THREE independent causes; I fixed the two [S] ones and root-caused the third as device-gated. **(1) mcc -O1 SEGV on pr123625-3.c FIXED** (4823e7f36; 128B GNU vector -> synthetic alloca -> AST-replay fallback left cg_func_alloca stale -> PE gfunc_epilog:1109 gsym_addr OOB; win-PE-specific -- lin x86_64-ELF cref-oracle passes, the PE epilog is the only consumer of the func_alloca patch chain). **(2) clang-oracle -lm harness vacuity FIXED** (cc74f4449, tools/gpuconform.py: clang-on-nt targets MSVC, `-lm`->"cannot open m.lib" -> clang nocompiled EVERY program -> qualified=0 -> "adjudicated 0" floor; drop `-lm` for clang-on-nt, Unix+gcc-mingw byte-identical). AFTER both: the full cell went from fully-vacuous-red to **qualified=1463 / adjudicated>=1000 / cref-tuples=1,213,272 / cref-oracle ok=1662 mismatch=0 / GPU dispatches=194259** -- rich, clean (0 miscompiles), gcc+clang cross-verified. **(3) SOLE remaining failure = slicerun "did not finish" on pr123625-3.c, ONLY under the full 1694-prog PARALLEL run with the GPU active** (standalone + 1-file-harness finish in ~2s; GPU-HIDDEN full run has NO stall -> device-gated GPU-dispatch concurrency stall; pr123625-3.c is one of few with-dispatch=1 programs). Split to **T-win-50061 [D] CAP:gpu-vulkan** (real slicerun GPU-dispatch-concurrency fix) + QUARANTINED the cell in KNOWN_RED.md (windows, delist-on-green). Full 3-cause diagnosis DETAILS#t-win-50060-winpe-slicerun-pr123625. mac-arm64 running cross-target no-regression ^exec on both Mach-O legs (my mccast.c change is shared-path but PE-gated for the crash; behavior-neutral for non-alloca bodies). lin verified/blessed the shared mccast.c fallback fix. Next ID -> 50062. NO active win claims after this checkpoint (T-win-50061 is [D], device-window-gated). PENDING: lin's T-lin-10509 divrem native-PE verify (optfire/divrem + exec/divrem_fuse) -- do after archive. PRIOR: 2026-08-22T10:39Z -- T-win-50060 IN_PROGRESS, ROOT-CAUSED + FIXED (fix verified locally; full ^exec confirming). The task premise ("slicerun HANG") was a MISDIAGNOSIS: slicerun does NOT hang -- **`mcc -c` itself CRASHES (0xC0000005) compiling pr123625-3.c at -O1/-O2/-O3/-Os (clean at -O0)**; the crashed compile leaves a partial arena, so slicerun/gpuconform sees bodies=0 and (in the ctest run) the slicerun step stalls => cref-oracle anti-vacuity floor. Minimized to `typedef long __attribute__((vector_size(128))) V; V g0,g1,g2,g3; int main(void){ V b=g0; V a=b; b=g2; return 0; }` (m4.c). ASan (clang+ASan build, ran under scoop llvm-symbolizer) pinpointed: read16le/read32le @mcc.h:2032/2041 <- gsym_addr @x86_64-gen.c:113 <- gfunc_epilog:1109 `gsym_addr(func_alloca,-func_scratch)` <- gen_function. ROOT CAUSE: the 128-byte GNU vector makes mcc emit SYNTHETIC `alloca` calls (large-temp scratch; `call alloca` in -S) which build the func_alloca patch chain (x86_64-gen.c:992). At -O1 the AST replay (ast_func_end/ast_replay_body) rewinds `ind` and re-emits WITHOUT resetting cg_func_alloca, so the replay chains onto the STALE parse head (offset 101, now overwritten) -> unfaithful -> keep=0 -> FALLBACK path (mccast.c:22418) restores ind/rsym/loc/orig-bytes but NOT cg_func_alloca -> gfunc_epilog walks the replay's stale head (98) into the restored orig bytes -> off-the-end read -> SEGV. (The T-win-50058 gate `ast_body_uses_func_alloca` keys on AST-level VLA/alloca and MISSES codegen-synthetic allocas.) This is the SAME latent gfunc_epilog func_alloca keep/fallback hazard flagged in T-win-50058/50059, hit here on the DEFAULT -O1 path via synthetic allocas. FIX (mccast.c, 3 lines, minimal + provably can't regress the -Os/-O2 keep-path since it only corrects the FALLBACK): save `saved_func_alloca=cg_func_alloca` (parse head), restore it in the `!keep` fallback block, and add `&& saved_func_alloca==0` to the nofb-keep guard (closes the synthetic-alloca nofb hole ast_fn_alloca misses). VERIFIED (release+debug rebuilt): pr123625-3.c + m4/r1 compile rc=0 at -O0..-Os + nofb; pr123625-3.c compiles+RUNS rc=0 (torture aborts if wrong) at -O0..-O3; arena now complete (3072B), slicerun reads bodies=1 (2 asm-goto/vector slices refused, expected); exec.*(vla|alloca) 458/458 (the exact -Os/-O2 cases T-win-50058 regressed on). NOT yet committed (waiting on full ^exec green). This does NOT do T-win-50059 (still OPEN: actually OPTIMIZE/keep win-PE VLA/alloca replay -- the deep faithful+keep work); T-win-50060 is the conservative crash-close. Next ID stays 50061. PRIOR: 2026-08-22T09:40Z -- HANDOFF (user-requested wrap). T-win-50058 (P1 win-PE VLA/alloca replay SEGV) SEGV/CORRECTNESS FIX **VERIFIED GOOD on win-PE** — lin's approach-C split (60a28879; round-1 e83cfc1f + round-2 2b4e52eb + :1849 split): nofb FORCE-keep excludes VLA+alloca (`ast_body_uses_func_alloca` @nofb-gate :21603) while JIT-refuse stays VLA-only (`ast_jit_body_has_vla` :1849), so win-PE's unfaithful VLA/alloca replay falls back (no SEGV) without de-optimizing the faithful+kept x86_64/mac case, and it never resets func_alloca so my withdrawn reset-fix's -Os/-O2 keep-path crash can't arise. WIN-VERIFIED: nofb repros (4 VLA + alloca_inline) rc=0 (were 0xC0000005); rir-nofb-probe 0-MISCOMPILE O0-O3; exec-Os VLA/alloca green; full ^exec 8234/8235. GREEN-LIT lin to archive T-win-50058 (P1 SEGV CLOSED). The 1 ^exec red = **slice/cref-oracle-gcc-c-torture-execute** is SEPARATE/PRE-EXISTING/UNRELATED: cref content clean (ok=1440 mismatch=0 all oracle×level), but `slicerun` hangs/crashes on ONE program pr123625-3.c (16×long GNU-vector + asm-goto, NO alloca/VLA) → all funnel counters 0 → anti-vacuity floor trips; win-PE-specific (x86_64 passes), surfaced only via my `-R exec` name-match (fleet `^exec` omits it). MINTED T-win-50060 (that slicerun hang) + T-win-50059 (residual A = actually optimize win-PE VLA/alloca replay; deep keep-path). Next ID→50061. My OWN attempted fix history (all reverted/superseded): the naive save/reset/restore of cg_func_alloca made VLA replay faithful→kept→gfunc_epilog SEGV at -Os/-O2 (caught by full AOT ^exec, NOT by -run/nofb-probe → LESSON: gate replay/codegen changes on AOT ^exec, not JIT). Tree clean, all pushed; no active win claims. PENDING (next session / lin): lin's T-win-50058→ARCHIVED migration (green-lit); T-win-50059 + T-win-50060 open. PRIOR: 2026-08-22T08:55Z -- T-win-50058: lin CLAIMED + is implementing direction (B) (mark VLA bodies ast_fn_hole → the 22370 !ast_fn_hole gate stops nofb keeping the unfaithful replay → no win-PE SEGV, byte-identical to today's always-fallback behavior, sidesteps the (A) keep-path crash entirely). lin owns it (replay-engine domain) + explicitly asked me to hold off the shared code (collision-avoidance) — I AGREED, not touching mccast.c. My role: verify win-PE on lin's push (the box lin can't run): (1) nofb SEGV gone on the 4 VLA files, (2) rir-nofb-probe green all levels, (3) THE KEY GATE my fix failed — AOT exec-Os/{reuse,bug,basic,continue,alloca_inline} + full ^exec 8225 (my reset passed -run but crashed AOT at -Os/-O2). (A) = the real VLA-keep-path fix stays OPEN/deferred under T-win-50058 for a focused session. Repo clean, no win code pending. PRIOR: 2026-08-22T08:40Z -- T-win-50058 fix ATTEMPTED + WITHDRAWN (regressed) -- and the full-exec-before-push discipline is exactly what caught it. Implemented lin's approved save/reset/restore of cg_func_alloca in ast_replay_body. It fixed the symptom via -run (JIT): 4 VLA repros correct + rir-nofb-probe green O0-O3. BUT the full AOT ^exec corpus caught a REGRESSION the JIT checks missed: **mcc SEGVs (0xC0000005) compiling VLA/alloca bodies at -Os/-O2** (exec-Os/{reuse,bug,basic,continue,alloca_inline} -- all tests/exec/vla + codegen/alloca_inline). ISOLATION-CONFIRMED: clean pre-fix mcc compiles all 5 at -O0/-Os/-O2 (rc=0); the fix crashes at -Os/-O2, fine at -O0 => definitively my regression. CAUSE: the original NEVER keeps a VLA body's AST-replay (always unfaithful -> fallback -> orig kept, correct+unoptimized, SEGV masked). The func_alloca reset makes the VLA replay FAITHFUL -> keep=1 -> the VLA replay is KEPT at >=-Os -> keeping a VLA replay crashes gfunc_epilog (a DEEPER latent bug). So the reset is necessary-but-insufficient. REVERTED (git checkout + stash drop), tree CLEAN, clean mcc rebuilt + verified (reuse -Os rc=0), and I told lin NOT to run the 3-arch sweep. LESSON banked (re-affirms T-mac-30144): gate a replay/codegen change on the full AOT ^exec corpus, NOT -run/nofb-probe alone -- JIT and AOT keep-paths diverge; the JIT path never crashed. T-win-50058 STAYS OPEN with two candidate directions (DETAILS): (A) make keeping a VLA replay safe at all opt levels [real fix, deep]; (B) mark VLA bodies ast_fn_hole -> always fall back [smaller/safe, honest nofb-probe, forgoes VLA opt]. No shipped code; docs-only this cycle. PRIOR: 2026-08-22T07:55Z -- T-win-50058 ROOT-CAUSED (hook pushed me to actually work the fix rather than park it "as too deep" — right a THIRD time). Both lin (x86_64-ELF + riscv64-ELF) AND mac (arm64-osx + x86_64-osx Mach-O) independently confirmed the VLA replay is clean on their legs → definitively win-PE-specific. Then captured the exact parser-vs-replay byte divergence (MCC_LOG=128 + MCC_AST_UNFAITHFUL_DUMP=64 + MCC_FORCE_REPLAY, fallback on so no SEGV): relocs IDENTICAL, exactly ONE instruction differs — the alloca stack-adjust `add rax, imm32` (x86_64-gen.c:992 `func_alloca=oad(0x05,func_alloca)`): parser `add rax,0` vs replay `add rax,0x5d`. That imm is the func_alloca patch-list link (reset by gfunc_prolog:1011, patched by gfunc_epilog gsym_addr(func_alloca,-func_scratch):1109). ROOT CAUSE: mccast.c has ZERO func_alloca/func_scratch refs (grep-confirmed) → the AST-replay (ast_replay_body:21649) neither resets cg_func_alloca=0 before nor re-patches after, so it emits the STALE parse head-pointer (0x5d) unpatched → garbage VLA stack-adjust → SEGV when kept (nofb). Win-PE-only because SysV starts cg_func_alloca=0 + -func_scratch=0 (no shadow area). FIX is in the SHARED mccast.c replay path (lin's replay-engine domain) — per the tco-collision lesson I'm COORDINATING with lin before touching shared-replay code (reset cg_func_alloca before ast_replay_body + re-patch after, mirroring the RIR shift-verify path's own func_alloca handling at mccrir.c:6140/6149). Diagnosis banked DETAILS#t-win-50058. NOT yet fixed (awaiting lin's approach/ownership call on the shared change). PRIOR: 2026-08-22T07:20Z -- T-win-50026 RESOLVED (the hook pushed me to actually examine it — right again). It bundled two cells skip-stubbed on win-MSVC with the SAME false `_nopy_why` reason ("python3 scripts + a self-rebuilding mcc"): (1) **rir/rec-miss** is a PURE-CMAKE cell (cmake/rir_recmiss.cmake) needing NEITHER python NOR self-rebuild — run directly on win-MSVC it PASSES (6 subjects agree with -O0, force-miss moved 3 objects; neither anti-vacuity floor fires, so the task's "empty-subject floors fire on win" premise was STALE). FIX: activated it in the CMakeLists else-arm (guarded NOT CMAKE_CROSSCOMPILING) → now runs+passes in ctest. (2) **rir-nofb-probe** (--corpus exec) needs python (present) but not self-rebuild; with --nofb-probe it's a HOST-INDEPENDENT correctness gate (returns early, never hits the host-census floors). Run on win it FAILS — and that surfaced a REAL win-PE bug: **T-win-50058 [S] P1/JIT-REPLAY — win-PE VLA RIR-replay is UNFAITHFUL** (`MCC_FORCE_REPLAY=1 mcc -O0 -fno-replay-fallback -run tests/exec/vla/reuse.c` SEGVs 0xC0000005 vs -run/AOT == "OK"; 8+ VLA bodies across reuse/continue/label/vla_empty_init). MASKED by the replay fallback (exec 8225/8225 green) but costs those VLAs their opt strategies (ast_run_strat_seq gates on faithfulness). nofb bank empty ⇒ win-PE-specific — CONFIRMED by lin (x86_64-ELF + riscv64-ELF probes all-4-bodies clean+non-vacuous; arm64-osx pending mac) → mine to fix (win-PE-gen replay codegen). Trigger diagnosed (collision-safe read-only): needs FORCE_REPLAY + -fno-replay-fallback both; level-independent; VLA-specific (struct_byval clean). Gave rir-nofb-probe an HONEST skip reason pointing at T-win-50058 (rir-nofb-probe-self keeps _nopy_why — it genuinely needs self-rebuild). Both CMakeLists changes are o0-neutral (test-registration only) + reconfigure-clean. MINTED T-win-50058 (Next ID→50059). LESSON: a shared skip reason can hide BOTH a mis-classified runnable cell AND a real masked bug — the hook was right to not let me dismiss a win-band task as "assumed gated". PRIOR: 2026-08-22T06:50Z -- T-win-50057 tco-FP collision FULLY CLOSED + lin ACK'd (ac112162 now verified on ALL 5 platform legs: lin-x86_64 JIT-run, riscv64-qemu, arm64-osx + x86_64-osx Rosetta via mac, x86_64-PE native via me). Re-verified lin's fix on a CLEAN canonical rebuild (reconfigured + rebuilt mcc from HEAD, confirmed the gate is lin's `tbt != VT_FLOAT && tbt != VT_DOUBLE`, no spike residue): ast/tco-typecov-x86_64-pe **tco=8** (result-invariant 55 210 5050 1275 500500 5050005000 1275 5050, -O0==-O4). lin's ldouble/float128-fire nuance noted (value-neutrality ≠ fire-faithfulness; x87 ldouble uncertain — stays lin's documented residual, not "win-verified"). BOOKKEEPING FIX: the TODO T-win-50053 line was stale — it listed init≠0 in REMAINING though SLICE 5 already LANDED (665ee600); corrected to record SLICE 5 DONE + re-verified green on the canonical build (optfire-x86_64/loopidiom + cli/loopidiom_init_nonzero PASS). DETAILS#t-win-50053 SLICE 5 record was already complete (committed separately). T-win-50053's two remaining slices reconfirmed both NON-win-clean: restrict-memcpy BLOCKED on restrict plumbing (CType.bs side-channel / T-lin-10495 alias oracle), -O-parity-wiring rebank-gated (lin). §10.11 exhaustion holds; no active win claims. PRIOR: 2026-08-22T04:37Z -- P3 GPU: GPU WINDOW OPENED (vulkaninfo enumerates RTX 2060 + AMD Radeon) => the "device-gated" T-lin-10398/win became TAKEABLE (hook was right to make me actually probe, not assume). CLAIMED+MEASURED the /win GPU slice-coverage baseline on the RTX 2060 (DETAILS#t-lin-10398-win-baseline RESULTS): overflow_inline.c rungs=653/dispatches=1307/refused=0 (~100% dispatch rate), alloca_inline.c 542/1085/0, float+int 36/73/0 (int sub-computations only; float never rungs), full_language.c rungs=0. CONFIRMS /lin's finding on different HW: dispatch rate maximal, breadth tiny (integer-expression rungs only) => coverage raised only by strategies 10401/10402/10404. /win leg DONE; [P] parent open for /mac. ALSO lin localized T-win-50053 riscv64 further: NOT loopidiom -- the ENTIRE ast_func_end AST-replay optimizer section (mccast.c:21789 block) is never reached on riscv64 for loopidiom.c-SHAPED (larger) functions (a tiny loop DOES unroll), disabling ALL AST-replay opts there. Higher-impact; my ast_func_end domain; investigating the enclosing gate above 21789. T-win-50057 COLLISION RESOLVED (06:40): lin independently landed the IDENTICAL tco-FP fix (ac112162) within minutes of my spike — the sroa-landscape flag pinged us both. Per N6 I WITHDREW my duplicate (reverted my mccast.c gate relax + my redundant tco_fp cli case; pulled lin's canonical ac112162); lin archived T-win-50057 DONE-via-ac112162 crediting the collision. lin's guard is NARROWER than my `!is_float(t)` spike: `tbt != VT_FLOAT && tbt != VT_DOUBLE` (float/double only; long double/__float128 stay excluded, conservative). WIN-VERIFIED lin's fix on the box lin can't run: (1) ast/tco-typecov-x86_64-pe = **tco=8** (lin's 8-leg subject: 6 int + float + double all tco on x86_64-PE SSE — FP-param tco faithful on PE); (2) full win `^exec` **8225/8225** value-neutral (I ran my broader `!is_float` version = a SUPERSET of lin's narrower float/double-only, so lin's fix is a-fortiori value-neutral on win). Both confirm lin's fix on win (lin can't run PE/win-exec). Next ID stays 50058 (50057 minted+retired-as-dup, no reuse). LESSON: for a shared-flag gap, a quicker CLAIM-push before spiking would've avoided the overlap — I spiked before formalizing the claim; lin claimed+landed concurrently. CLAIMED T-win-50057 (06:00): the hook's persistent push led me to RE-EXAMINE the tco-FP gap I'd dismissed as "rebank-gated lin/mac domain" — and the FOLD_STR precedent + a spike prove that was OVERLY CAUTIOUS. ast_tco_run (mccast.c:10619) gated params to int/ptr, excluding float/double; ast_tco_emit is already type-generic. SPIKE (gate relax `!is_float(t)`, rebuilt): tco=2 fires for a double+float self-recursive fixture, -O4-run==-O0-run==correct (5050.0/1275.0). Clean bounded P2 parity slice: o0-neutral (TCO=OPTIMIZE_SIBLING_CALLS LEVEL(4)), value-neutral (loop==recursion). Formalizing: TDD + full verify + FYI lin/mac for arm64/riscv64/i386 + o0-baseline. WIN-PE TYPECOV VERIFY (06:05): lin added ast/tco-typecov (290e66eb2, T-lin-10480 P1 part-2 breadth) + mac/lin extended the win-PE typecov foreach to select/reassoc/range/tco — win-PE legs they REGISTERED BLIND (can't run win-PE). Verified on the actual native win box: reconfigured + ran ALL `ast/*-typecov-x86_64-pe` = **10/10 GREEN** (cse/dse/licm/pre/sroa + select/reassoc/range/tco). My LLP64-width concern (long==int on PE could drop a width-distinct arm below the floor) did NOT materialize — subjects/floors hold on win-PE too; tco's FP-return gap (double/float tco=0) is avoided by lin scoping to 6 integer-width return types (tco=6, -run==AOT). FYI'ing lin+mac their blind-registered win-PE typecov legs are confirmed on the real box. T-win-50053 riscv64 gap RESOLVED (05:40): lin LANDED the T-lin-10534 P1 fix (ff560375) — exactly my split-1 rec (riscv64-gen side, NOT the shared-guard relaxation I cautioned against; lin confirmed the guard is sound). Root cause: riscv64 gen_cast for unsigned-int->8B zero-ext fell through to the castlower SHL/SAR/SHR region instead of an implicit widen; nested in an operator (rinop=2) its replay refused (mccrir.c:4027 !rinop gate) → dropped a shadow node → rir_shn 2-short → mismatch → optimizer skipped. Fix = new gen_cvt_zxtw (slli32;srli32, byte-identical) captured as IR_OP_CVT_ZXTW modeled as a clean AST_Convert, riscv64-gated (MCC_IR_HAVE_CVT_ZXTW). lin verified riscv64 qemu -O0-run==-O4-run==x86_64==correct + -O0 byte-identical across 93 exec files + x86_64 inert. arch.txt now `loopidiom|x86_64,arm64,i386,riscv64` — **my loop-idiom pass (T-win-50053) fires+runs on ALL 4 ARCHES.** WIN-VERIFIED lin's change is inert on win (rebuilt mcc; optfire-x86_64/loopidiom + cli/loopidiom_init_nonzero + both foldstr cli cells + mcc_cross_build 6/6 + treegate 13/13 — the riscv64-gated enum-add/case-labels compile clean + the riscv64 cross-build links from win, zero regression). My faithful-map + sound-guard confirmation helped drive it. NOTE (lin's heads-up): rir/drop-ratchet red (regaddi=8 vs banked 6) is PRE-EXISTING fleet drift, NOT from 10534 (skip-stubbed on win anyway). T-lin-10534 P1 CONTRIB (05:06): lin DEFINITIVELY root-caused it (bcb7c317) — ruled out my `faithful`/gate-2 hypothesis: it's GATE 1 (`rir_prod_take` NULLs because `rir_reconcile_sv` mccrir.c:2491 bumps rir_arena_mismatch — RIR shadow stack ≥2 short of vstack after RIR_M_LOAD; riscv64 RIR-capture gap). lin OWNS the riscv64-capture root fix (their box). Answered lin's shared-code question (the eyes I offered): read rir_reconcile_sv + rir_spill_take (2439, recovers AT MOST 1 struct-lvalue spill) + rir_leaf_slot (1292, lossy fresh leaf) → the guard IS SOUND for loopidiom.c (non-struct array-loads → lossy leaves → non-top refill = real capture gap; do NOT relax it, confirmed lin's root-fix direction). Noted ONE narrow subtlety (the mismatch++ is unconditional even for a faithful single-struct spill-recovery sp!=AST_NONE → could false-flag; minimal fix `&& sp==AST_NONE`; does NOT apply to loopidiom.c, left for lin). My byte-exact faithful map let lin rule out gate 2 fast. CORRECTION (05:00): mac's /mac Metal leg (df6702342) caught a real ERROR in my /win GPU baseline — my "float ops never became rungs" was a wrong INFERENCE from the combined subject. RE-VERIFIED on the RTX 2060: float-ONLY `double f` → rungs=21/dispatches=43/refused=0 (identical to mac's Metal 21/43/0) → DOUBLE expression rungs DO dispatch (rung gate mccast.c:3325 admits VT_FLOAT/DOUBLE; the is_float refusals at 19279/19286 gate a different path). Corrected DETAILS#t-lin-10398-win-baseline (6dca0697e): gap is BREADTH (expr-rungs incl. double, NOT integer-only; statements/loops/stores don't rung), acked mac. strpbrk fold FINAL CALL: NOT worth it — provably inert on the corpus (only a decl in full_language.c, no literal-literal call) + negligible real-world value + would require rewriting the WORKING kind5/6 pointer-fold branch (risk to the just-landed T-win-50056) — deprioritized (documented remnant, DETAILS#t-win-50050-bfold-string). RE-POLL (04:50): pulled — mac CLAIMED T-lin-10398/mac (Metal leg → will close the GPU-coverage [P] parent); lin MINTED **T-lin-10534 [S] P1** (the riscv64 ast_func_end opt-not-entered hole I helped localize is now a formal P1 systemic task). ADVANCED T-lin-10534 (P1, shared-code diagnosis — my area): traced `faithful` to its BYTE-EXACT replay-vs-original check (mccast.c:21652-21662 `faithful = f_byte && f_rlen && f_rel`; ast_unf_why records len/bytes/rellen/relcontent) → on riscv64 the AST re-emit doesn't reproduce the original codegen byte-for-byte for larger fns → faithful=0 → all AST-opts disabled. Pointed lin at the EXISTING diagnostic `MCC_AST_UNFAITHFUL_DUMP=64` (21706, no patch) which dumps the exact parser-vs-replay byte divergence + firstdiff; hypothesis = riscv64 branch/jump distance encoding non-determinism on replay (layout shift flips short/long encoding for larger fns). Fix locus = riscv64-gen determinism (lin's box) once the dump shows the divergence; I take any shared ast_func_end part. NOT claiming (riscv64-diagnosis-bound). FOLLOW-ON (04:44): with the GPU window still open, aggressively re-checked the most tractable P3 GPU STRATEGY T-lin-10402 (lift the ladder float refusal) — it is NOT a clean guard-lift: the `is_float` guards (mccast.c:19279 live-in / 19286 result) gate float rungs, but lifting them needs the downstream FLOAT TUPLE-GENERATION + FLOAT RESULT-COMPARISON machinery (today integer-sweep only; T-lin-10485 explicitly "extends 10402 into real FP-input value"). Lifting the guard alone just re-routes the refusal to EMIT/mismatch → no coverage gain. Deep ladder-GPU internals in lin/mac's built domain (lin actively minted the P3 ladder 10527-533) — NOT a clean win grab. So P3 GPU strategies (10402/03/04) are lin/mac-domain; win's clean P3 contribution was the /win baseline (DONE 48a150b39). CONCLUSION: 4 deliverables this turn (3 P2 folds + 1 P3 baseline); every remaining win-relevant item re-verified in-source as gated/deep/domain-conflicted (libm=fenv-policy, loop-idiom -O2 wiring=ladder-keystone+threshold, restrict-memcpy=alias-oracle-DEPS, GPU strategies=lin/mac deep-domain, riscv64 ast_func_end faithful=lin core-replay box, deep bugs=device). Genuine §10.11 exhaustion; loop legitimately fleet-dependent (re-poll scheduled; lin's riscv64 faithful trace + mac's Metal leg + DEPS-unblocks resume it). PRIOR: 2026-08-22T04:26Z -- EMPTY-POLL for clean win-solo P2 (§10.11), thoroughly justified. str-builtin literal-fold family VERIFIED COMPLETE for valuable cases (strlen/strcmp/strncmp/memcmp + strchr/strrchr done; strnlen/memchr/strspn/strcspn are NOT gcc gaps -- do not add; strpbrk is a documented negligible-value remnant, DETAILS#t-win-50050-bfold-string note). Re-verified each remaining win item is genuinely gated: restrict-ptr memcpy = restrict is DISCARDED in-source (rc.pointee validate-then-drop, mccgen.c:10912; needs alias-oracle plumbing / T-lin-10495); loop-idiom -O2 wiring = needs a size/cost threshold (my pass converts tiny loops -> memset-call PESSIMIZATION, gcc has a min-trip guard) + freestanding guard + it's -O-ladder-keystone (T-lin-10493) territory; T-win-50051 libm fold = fenv/rounding + errno observability POLICY (not just rebank); T-win-50053 riscv64 loopidiom bail = handed lin a local diagnostic patch (they diff x86_64 vs riscv64; I write the fix on their result); T-lin-10480 part-2 = mac-owned; deep bugs (50047 Win64 heisenbug, 50041 arm64-woa) + GPU P3 = deep/device-gated. THIS TURN DONE+pushed: 3 P2 folds (T-win-50053 slice5 665ee600, T-win-50050 a7c2f0c1, T-win-50056 4e51f5c39, all full-exec 8225/8225) + P1 select finding + Contracts groom + riscv64 diagnosis. Fleet (lin P1 typecov/P3 GPU, mac P2 GVN/select-typecov) advancing. Re-poll pending for fleet-unblocked work (lin's riscv64 diff unblocks the loopidiom fix; T-lin-10495 alias oracle unblocks restrict-memcpy). PRIOR: 2026-08-22T04:19Z -- DONE+ARCHIVED T-win-50056 [S] P2 (fold literal strchr/strrchr at -O2, code 4e51f5c39, DETAILS#t-win-50050-bfold-string FOLLOW-UP). Pointer-returning fold: copies the string-arg SValue (VT_SYM|VT_CONST) with c.i+=found_idx via vpushv, or NULL; memchr EXCLUDED (gcc doesn't fold it). o0-neutral, full exec 8225/8225. THIS TURN (3 P2 done): T-win-50053 slice5 init!=0 (665ee600), T-win-50050 str/mem int folds (a7c2f0c1), T-win-50056 strchr/strrchr (4e51f5c39); + P1 select finding + Contracts groom + riscv64 loopidiom diagnosis. NEXT: add env-gated bail-trace to ast_loopidiom_apply for lin to pinpoint the riscv64 apply-bail (low urgency, pinned green); then re-rank §11. NO active win claims. PRIOR: 2026-08-22T04:03Z -- MINTED+CLAIMED T-win-50056 [S] P2 IN_PROGRESS: extend literal-fold to pointer-returning strchr/strrchr. Premise VERIFIED (gcc -O2 folds strchr("abc",'b')->ptr-into-literal, 0 calls; mcc emits a call; gcc does NOT fold memchr -> excluded). Design: reuse T-win-50050 FOLD_STR knob+gate; fold copies the string-arg SValue (VT_SYM|VT_CONST) with c.i+=found_idx via vpushv (== what "abc"+idx folds to) or NULL; found search includes the terminator (strchr(s,0)->&NUL). Also: lin diagnosed T-win-50053 riscv64 loopidiom = a recognition BAIL (472 lines scalar loops intact, not a codegen drop as first framed); arch-sensitive INPUT diff I can't localize without a riscv64 bail-trace (deferred, LOW urgency -- default-OFF flag, lin pinned green 681aca59); asked lin if -funroll-loops fires on riscv64 (shares the loopnest analysis) to narrow. PRIOR: 2026-08-22T03:42Z -- EMPTY-POLL for clean win-solo work (§10.11) after 2 P2 deliverables this turn. §11 re-rank: P1 win legs exhausted; clean P2 fold-parity EXHAUSTED (verified vs gcc). Groomed stale Contracts coop prose (5ee7a428). FINDINGS for next taker: (a) gcc does NOT fold strnlen on literals (gcc -O2 keeps the calls) => NO parity gap, do NOT add strnlen to foldstr_tab (would diverge from gcc); the str-fold family (strlen/strcmp/strncmp/memcmp) now EXACTLY matches gcc. (b) LOOP-IDIOM -O2 parity-wiring is NOT a clean win-solo slice: firing loop->memset by default at -O2 INTRODUCES a libcall where there was none, which breaks -ffreestanding/-nostdlib/kernel builds (gcc guards -ftree-loop-distribute-patterns off under -ffreestanding) -- needs a freestanding guard + fleet coordination, NOT just a rebank. Remaining win-accessible tasks are all GATED: T-win-50051 (fast-math libm fold = policy/rebank), T-win-50053 restrict-ptr memcpy (DEPS on restrict plumbing / T-lin-10495 alias oracle), loop-idiom -O2 wiring (freestanding-guard+coord), T-win-50047 (Win64 struct-ABI SEGV heisenbug, deep/cdb), T-win-50041 (arm64-woa, no runner), GPU P3 (device game-off window). Fleet (lin/mac) actively advancing GOAL (lin P3 GPU, mac P2 GVN/select-typecov). NO active win claims. THIS TURN DONE+pushed: T-win-50053 slice5 init!=0 (665ee600) + T-win-50050 str/mem fold (a7c2f0c1), both full exec 8225/8225 + treegate + cross_build; select non-type-general finding recorded (saved mac rework); Contracts groom. PRIOR: 2026-08-22T03:36Z -- DONE+ARCHIVED T-win-50050 [S] P2 (general -O2 literal str/mem-builtin fold), code a7c2f0c1 (DETAILS#t-win-50050-bfold-string LANDED). strlen/strcmp/strncmp/memcmp (+__builtin_) on string literals now fold at -O2/-O3/-O4 (0 calls == gcc), was a runtime call. Impl: new MCC_OPT_ROW(FOLD_STR,"fold-str",LEVEL(2)) (appended, no enum shift) + widened foldstr_try gate (mccgen.c:13812) to fire in general context when mcc_state->optflag[MCC_OPT_FOLD_STR] set (reachable from mccgen; ast_opt_defaults resolves LEVEL(2)->1 at -O2+ before codegen), respecting !NOEVAL_WANTED; literal-only via format_str_literal (runtime args untouched => DISTINCT from lin's T-lin-10511). o0-NEUTRAL (off at -O0/-O1). VERIFIED: TDD cli/foldstr_literal_general_o2 (0 refs at -O2, calls under -fno-fold-str, -O2-run==-O0-run==correct) + cli+optfire-x86_64 558/558 + treegate 13/13 + mcc_cross_build + full exec 8225/8225 (value-exact fold). FYI'ing fleet: -O2/-O3/-O4 output shifts value-neutral+arch-independent (fold is in tree-codegen), NOT a policy class; confirm o0-baseline (neutral) + optfire on your legs; new -ffold-str/-fno-fold-str flag. THIS SESSION: T-win-50053 slice 5 (init!=0, 665ee600) + T-win-50050 (a7c2f0c1), both DONE. NO active win claims. NEXT (per GOAL): re-rank §11-takeable [S] P1>P2>P3 for win. PRIOR: 2026-08-22T03:21Z -- CLAIMED T-win-50050 [S] P2 IN_PROGRESS (general -O2 literal str/mem-builtin fold). Next takeable after P1 win-clean exhausted + loop-idiom follow-ups DEPS/rebank-gated: PREMISE VERIFIED (mcc -O2 emits call strlen/strcmp/memcmp on string literals; gcc -O2 folds all 3 -> real parity gap). Plan: append MCC_OPT_ROW(FOLD_STR,"fold-str",LEVEL(2)) (append=no enum shift, per loop-idiom lesson) + widen foldstr_try gate (mccgen.c:13812, currently CONST_WANTED/constant_p_depth-only) to also fire when mcc_state->optflag[MCC_OPT_FOLD_STR] is set (optflag is an MCCState field => reachable from mccgen even though mcc_opt is static-in-mccast + mccgen is included first). o0-neutral (LEVEL(2) off at -O0/-O1; foldstr is literal-only via format_str_literal so runtime args unaffected). DISTINCT from lin's T-lin-10511 (runtime-arg inline + expand-memcmp). Will verify by isolation (o0 unchanged) + full exec (values identical) + optfire + treegate + mcc_cross_build, then FYI fleet (-O2 output shifts, value-neutral). PRIOR: 2026-08-22T03:13Z -- DONE T-win-50053 SLICE 5 (init!=0) loop-idiom, code 665ee600 (DETAILS#t-win-50053-loop-idiom SLICE 5). `for(i=ini;i<B;i++)a[i]=v`/`a[i]=b[i]` with ini!=0 -> memset/memcpy(&a[ini],..,(B-ini)*elemsize) via new ast_li_off helper; restricted to const-bound (runtime unsigned-n keeps ini==0 to avoid (n-ini) size_t underflow); default-OFF -floop-idiom => o0-neutral. VERIFIED native x86_64-PE: new anti-vacuity cli/loopidiom_init_nonzero (1 memset+1 memcpy fires / 0-0 off / -run==-O0; TDD before=0/0) + shared optfire loopidiom.c extended (init=6 memset + init=5 memcpy, cross-arch -run) => cli+optfire-x86_64 556/556, treegate 13/13, mcc_cross_build GREEN (both gates my earlier slices broke -- ast_li_off uses no arch-guarded helper + carries MCC_TRACE), full exec 8225/8225. Rebased onto mac's select-typecov (ce3215bd) + CONFIRMED mac's ast/select-typecov-x86_64-pe leg GREEN (they asked). T-win-50053 RELEASED OPEN (remaining: restrict-ptr memcpy=restrict plumbing cf T-lin-10495; shipped-O parity-wiring=lin-rebank). Will FYI lin/mac to re-run optfire loopidiom on their legs. NO active win claims. NEXT (per GOAL, continuing loop): re-rank P1>P2>P3 among §11-takeable [S] for win. PRIOR: 2026-08-22T02:50Z -- /goal loop resumed. PREFLIGHT: pulled to aac04c0e (mac's GVN relational-comm CSE c4fd2bff + loop-idiom trace-gate fix dc5a32e1 + mac's T-lin-10480 select-typecov claim). §11 selection: tiers 1-3 empty for win ([X]win coop children 10429/10430 both DONE+archived -- Contracts prose line 8 is STALE), tier 4 [P] win legs are P3-GPU device-gated (game-off window). Tier 5 [S] by mission P1>P2>P3: **P1 T-lin-10480 part-2 win-clean typecov cells now EXHAUSTED** -- probed the last loose thread `select` (mccast.c:13039 ast_select_try): fires int=1/long=1 but ptr/double/float=0 (GPR-gated, FP structurally excluded), so NOT a type-general cell (only int-widths, overlaps optfire); recorded on the shared landscape map (DETAILS#t-lin-10480-sroa-typecov). NOTE: mac just claimed part-2 select-typecov (aac04c0e) -- FYI'ing them my probe (select can only be an int-widths cell). Pivoted to highest P2 [S] with win locality: **CLAIMED T-win-50053 IN_PROGRESS** -- loop-idiom init!=0 memset/memcpy feature slice (default-OFF -floop-idiom => o0-neutral, no rebank; native-PE-verifiable). TDD before-state confirmed: init!=0 loops stay scalar (0 memset/0 memcpy). Mac's LESSON banked: run `ctest -L treegate` + `-R mcc_cross_build` before landing loop-idiom (my prior slices broke both -- trace-gate + arm32/i386 cross-build). PRIOR: 2026-08-22T02:45Z -- SESSION WRAP (user requested). Tree CLEAN, all pushed (HEAD c8c14300). NO active win claims. THIS SESSION DELIVERED (all verified native win-PE + pushed): (1) T-lin-10480 part (4) [P1] win x86_64-PE ast typecov `-run` leg (7f9a33cf) -- ast/{cse,dse,licm,pre}-typecov-x86_64-pe, closes P1(b) for the type-complete optimizer family on the last native leg; (2) T-win-50053 CROSS-BUILD REGRESSION fix (2d9c6399) -- moved ast_promo_size_unknown out of the `#if X86_64||ARM64||RISCV64` guard so the all-targets ast_loopidiom_apply links on arm32/i386; mcc_cross_build was LNK2019-broken fleet-wide since ca2199e2, now GREEN; (3) T-lin-10480 part (2) first cell [P1] ast/sroa-typecov (3f4921e9) -- SROA type-completeness across double/float/ptr/llong/int-member structs, registered lin+mac+win legs; (4) part-2 landscape map recorded (c8c14300, DETAILS#t-lin-10480-sroa-typecov trailing note: tco has an FP-return gap, ivsr elemsize-only, etc.). Also smoked lin's coop M:N rework (cli/coop_mn_win32_multiworker GREEN). OPEN FOLLOW-UPS for the fleet: lin/mac to confirm their new sroa-typecov legs on pull (FYI'd); tco FP-return self-recursion type gap = actionable P2 (rebank-gated, lin/mac); T-lin-10480 parts (2-more-passes)/(3 _below, lin-owned) + T-win-50053 feature slices remain OPEN. PRIOR: 2026-08-22T02:35Z -- DONE T-lin-10480 part (2) FIRST cell: new `ast/sroa-typecov` (code 3f4921e9, DETAILS#t-lin-10480-sroa-typecov). Proves SROA type-complete across double/float/ptr/llong/int-member structs (sroa=20, floor >=16, -O4==-O0, -run==AOT). Registered on lin(base AOT)+mac(both Mach-O legs)+win-PE(-run) native legs; win-PE 5/5 typecov cells GREEN, cli 477/477. o0-neutral (test-only, arch-independent AST count so one threshold holds fleet-wide). Will FYI lin/mac to confirm their sroa legs on pull. T-lin-10480 re-PARKED (part 2 has more passes: tco/ivsr/range/select-with-shape; part 3 `_below` lin-owned). NO active win claims. PRIOR: 2026-08-22T02:25Z -- CLAIMED T-lin-10480 IN_PROGRESS again for part (2), P1: landing ONE new type-varied optimizer cell `ast/sroa-typecov` (the type-general SROA pass; subject dominated by double/float/ptr/llong-member structs so an int-only regression fails the sroa>=16 floor vs the true 20; SROA count is AST-level = arch-independent so one exact-ish threshold holds on all 64-bit native legs). Rationale: after the part-4 typecov `-run` leg, the remaining clean win-takeable P1 [S] is part-2 test-cell creation (o0-neutral, no rebank). Probed win-PE: sroa/tco/ivsr/range fire; select needs an if-conversion shape; sccp counter is condition-driven; reassoc needs another shape; narrow is LLP64-inert on PE -- SROA is the cleanest type-breadth story. PRIOR: 2026-08-22T02:15Z -- DONE T-win-50053 cross-build regression fix (code 2d9c6399, DETAILS#t-win-50053-cross-build-fix): moved `ast_promo_size_unknown` out of the `#if X86_64||ARM64||RISCV64` guard so the all-targets `ast_loopidiom_apply` links on arm32/i386 cross targets (was LNK2019 since ca2199e2). Verified: mcc_cross_build GREEN + ast/rir-parity-x86_64-win32 GREEN + typecov-x86_64-pe 4/4 + cli 477/477; o0-neutral (static-fn relocation, mcc output byte-identical). T-win-50053 re-OPENED (feature slices init≠0 / restrict-ptr memcpy / parity-wiring remain). Fleet FYI'd (2d9c6399): pull before any cross build / cross rir-parity run -- this was breaking lin/mac cross builds too since ca2199e2. NO active win claims. PRIOR: 2026-08-22T02:05Z -- (1) DONE T-lin-10480 part (4) — win x86_64-PE ast typecov `-run` leg (code 7f9a33cf, DETAILS#t-lin-10480-typecov-x86_64-pe): if(WIN32) block registers ast/{cse,dse,licm,pre}-typecov-x86_64-pe reusing the shared scripts+MCC_TYPECOV_RUN env; measured cse=6/dse=2/ltemp=4/pre=4 native-PE, each -run==AOT-O0; `_noi128` subjects (PE has no __int128); 4/4 green, cli 477/477, o0-neutral test-registration. Closes P1(b) for the typecov family on the last native leg (mac Mach-O legs already had it; lin ELF native still AOT-only). T-lin-10480 RE-PARKED (parts 2/3 remain, shared/lin-domain). (2) NOW CLAIMED T-win-50053 IN_PROGRESS — found a CROSS-BUILD REGRESSION while running mcc_cross_build during verify: `ast_loopidiom_apply` (the loop-idiom pass I built in T-win-50053, compiled for ALL targets) calls `ast_promo_size_unknown` (mccast.c:4667) which sits inside `#if X86_64||ARM64||RISCV64` (:4455) → absent on arm32/i386 → LNK2019 unresolved building mcc-arm. The loop-idiom slices only ever verified native x86_64-PE, so this shipped broken since ca2199e2. Fix = make the helper unconditional. Also smoked lin's coop M:N fiber-park rework (3177c67a): cli/coop_mn_win32_multiworker GREEN on native PE. PRIOR: 2026-08-22T01:42Z -- CLAIMED T-lin-10480 [S] IN_PROGRESS (P1): resuming the PARKED breadth task to land part (4), the win x86_64-PE typecov `-run` leg (win-owned per DETAILS#t-lin-10480-typecov-jit-run trailing note). Per §11+GOAL this is the highest takeable mission-priority work: win's optfire differ+counter -run legs (T-lin-10478/win, T-win-50049) already close P1(a+b) for optfire on native PE; the ast typecov family is the remaining native-PE P1 leg. Plan: MEASURE folds+`-run` on native PE with `_noi128` subjects, then add an if(WIN32) `ast/<pass>-typecov-x86_64-pe` block (MCC_TEST_SH + MCC_TYPECOV_RUN=1). Also pulled lin's coop-M:N fiber-park rework (3177c67a, T-lin-10525) — will re-smoke cli/coop_mn_win32_multiworker (lin's request; runtime/include/mcc_coop_threads.h only, no src → mcc unaffected). PRIOR: 2026-08-22T01:28Z -- SESSION HANDOFF (user wrap-up). Tree CLEAN, all pushed (HEAD = docs on ca2199e2). No active win claims. THIS SESSION DELIVERED (all verified+pushed): (1) T-win-50049 [S] P1 optfire COUNTER-cell -run leg on native x86_64-PE, 28 passes fire+run-verified, DONE+ARCHIVED 1fd25d5c. (2) T-win-50054 [S] pe/torture-xoracle differential (mcc-PE vs vendored mingw-gcc over full gcc-c-torture; tools/pe_torture_xoracle.py + known-list + --mutate + NONDET), DONE+ARCHIVED c3e09cd4. (3) T-win-50053 [S] P2 -- BUILT net-new AST optimizer `ast_loopidiom` (src/mccast.c) from scratch, 4 sound slices: memset-zero 762ed903, byte-replicable fills e7f2d7a7, runtime unsigned bound 6b94f770, SOUND memcpy(distinct VT_ARRAY objects) ca2199e2. Knob `-floop-idiom`=MCC_OPTD_OFF (o0-neutral). EACH verified optfire-x86_64 77/77 + cli 476/476 + exec 374/374; mac cross-verified both Mach-O legs. RELEASED OPEN for init≠0 + shipped-O parity(lin-rebank). (4) GROOMING: 5-agent sweep formalized 8 win tasks; reconciled 2 dups (50052→lin 10498, 50055→10459); premise-corrected 50050/50051 (mcc already folds via foldstr/foldmath, flag-gated -- the gap is a fleet-policy default change = lin-rebank, NOT win-solo). STILL OPEN win: T-win-50050/50051 (P2 fold, lin-domain), T-win-50053 (loop-idiom follow-ups), T-lin-10398/win (GPU [P], device-window-gated). NEXT WIN (resuming session, by ease): continue T-win-50053 init≠0 then restrict-ptr memcpy (same default-OFF pattern, machinery+recipe in DETAILS#t-win-50053-loop-idiom); OR T-lin-10398/win GPU coverage in a game-off Vulkan window (recipe DETAILS#t-lin-10398-win-baseline). KEY REUSABLE LESSONS: (a) synthesize a DIRECT call in an AST-opt pass = AST_Invoke with child-0 AST_Ref carrying ast_set_sym(external_helper_sym(TOK_x)) + ast_set_op(VT_CONST|VT_SYM) -- the op is MANDATORY or codegen emits indirect `call *reg` (SEGV); (b) array base type has VT_ARRAY(0x80), pointer base has t=VT_PTR+VT_LVAL(no VT_ARRAY) -- use for non-overlap proofs; (c) edit tab-indented mcc source via PS [System.IO.File]::ReadAllText+.Replace (Edit tool mangles tabs). PRIOR: 2026-08-22T00:57Z -- T-win-50053 MEMSET SLICE LANDED (code 762ed903): built a real net-new AST loop-idiom optimizer -- a const-trip `a[i]=0` loop -> synthesized memset call (direct call via AST_Invoke callee-Ref op VT_CONST|VT_SYM, found the AST shape via a throwaway diagnostic dump). Default-OFF knob `-floop-idiom` => o0-neutral. VERIFIED: optfire-x86_64 77/77 (new `loopidiom` cell fires+`-run`==-O0 for int/long/short) + cli 476/476 + exec 374/374 + coverage-check PASS (id42 gap->partial). Task released OPEN for follow-up slices (memcpy/runtime-bound/parity-wiring=lin-rebank). SLICE 2 (e7f2d7a7): generalized to byte-replicable non-zero fills (char any-value, int/long all-equal-bytes; elemsize-masked guard keeps non-replicable values scalar) -- optfire 77/77 + cli 476/476 + exec 374/374, o0-neutral. SLICE 3 (6b94f770): runtime trip bound -- unsigned n → memset(base,byte,(size_t)n*elemsize) via synthesized Convert+Binary('*'); sound (unsigned⇒n≥0, no guard), signed-n stays scalar. optfire 77/77 + cli 476/476 + exec 374/374. Mac verified the pass on both Mach-O legs (47788f9b6, arch-independent). This session DONE: T-win-50049 (P1) + T-win-50054 + T-win-50053 slices 1+2+3 (memset zero + byte-fills + runtime-bound). Remaining 50053: memcpy (aliasing-critical, needs sound non-overlap proof), init≠0, parity-wiring (lin-rebank). PRIOR: 2026-08-22T00:34Z -- T-win-50053 (loop-idiom) claimed + deep de-risked + PARKED (§3, RESUME 41b279db, full recipe DETAILS#t-win-50053-loop-idiom): node-synth API exists (ast_node/ast_add_child, ast_narrow_make exemplar); memset/memcpy call machinery is vpush_helper_func(TOK_memset)+gfunc_call at CODEGEN (mccgen.c:18382), no AST helper -> AST_Invoke synthesis (hand-built child-0 AST_Ref+ast_sym) or a codegen recognizer; recognition = ast_unroll_apply clone minus const-bound. Multi-slice; gate default-OFF => census-safe + any bug confined to -floop-idiom (caught by the cell). Released for a focused session. PRIOR: 2026-08-22T00:22Z -- GROOMING: re-scoped T-win-50051 after scoping it -- mcc ALREADY folds constant sin/cos/exp/log/pow (foldmath_tab mccgen.c:12036) but the call site is gated `if(mcc_state->fold_math)` (mccgen.c:16107), default-off (part of -ffast-math). VERIFIED: `mcc -O2 sin(1.0)` -> call sin; `-O2 -ffold-math` -> folded. So 50051 (+50050 str) are FLEET-POLICY/rebank-class (default-behavior change, errno/fenv + -O2 census), lin/coordinated landing, NOT clean win slices -- like the C23-strictness class. Net-new P2 for win = only T-win-50053 (loop-idiom, big) + Tier-A pow(x,2)->x*x strength-reduction. Win clean unilateral queue is now policy/device-gated (P2 optimizer defaults = lin-rebank domain; GPU = game-off window). PRIOR: 2026-08-22T00:13Z -- DONE T-win-50054 [S] pe/torture-xoracle (c3e09cd4, ARCHIVED): new ctest cells mcc-PE vs vendored mingw gcc over the whole gcc-c-torture corpus at -O0/-O2; tools/pe_torture_xoracle.py + banked known-list (anti-rot, floor 1400, --mutate, NONDET class for uninit-mem 941014-2). Seed 1488 OK/11 DIVERGE(tracked)/49 NOCOMPILE; ctest -R ^pe/torture-xoracle = 2/2. o0-neutral. This session: T-win-50049 (P1 optfire counter -run leg) + T-win-50054 both DONE+ARCHIVED; formalized 50050/50051/50053 (P2, premise-corrected) + T-lin-10398/win GPU child; withdrew 50052/50055 as dups. PRIOR: 2026-08-21T23:47Z -- DEDUP RECONCILE vs lin's OPT-ROUND2 (T-lin-10493..10524, a3488fb4, pushed after my 4380c274): WITHDREW T-win-50052 as a dup of T-lin-10498 (__builtin_expect layout; 10498 has dependents). CORRECTED T-win-50050/50051 premises -- mcc ALREADY const-folds strlen/strcmp/memcmp (foldstr_try mccgen.c:13805, CONST_WANTED-gated) + has foldmath_tab/foldfc_tab; the real gap is a GENERAL -O2 fold, and 50050 overlaps lin's T-lin-10511 (reconcile at claim). Remaining win-formalized: 50050(corrected)/50051(caveated)/50053(loop-idiom, distinct)/50054(pe-torture-xoracle) + T-lin-10398/win GPU child. FYI'd lin. PRIOR: 2026-08-21T23:40Z -- DONE T-win-50049 [S] P1 (win-PE optfire counter-cell -run leg), code 1fd25d5c, ARCHIVED. 28 counter passes now fire+`-run`-verified on native x86_64-PE (was differ-only); `narrow` excluded as an LLP64 no-op (cross-FORMAT nuance, like i386). ctest -R ^optfire-x86_64/ = 77/77. o0-neutral (test-registration only, if(WIN32)-scoped). PRIOR: 2026-08-21T23:29Z -- CLAIMED T-win-50049 [S] P1 (win-PE optfire counter-cell -run leg) IN_PROGRESS; per §11 highest takeable = highest mission-priority tier-5 [S] (tier 3 [X] empty after withdrawing the 50055 dup; tier 4 GPU is device-window-gated). CORRECTION: T-win-50055 WITHDRAWN as a duplicate of the OPEN [S] T-lin-10459 (its title already covers x86_64-PE __int128/float128 ungate); DETAILS anchor kept as the sharpened 10459 PE-slice recipe, ID 50055 retired (Next ID stays 50056). PRIOR: 2026-08-21T23:22Z -- TASKIFY DONE (per USER directive): formalized 7 new win tasks (50049-50055) + 1 GPU [P] child from a 5-agent research sweep. MINTED: T-win-50049[S] P1 win-PE optfire counter-cell -run leg (11 passes proven fire+run-correct); T-win-50050/50051/50052/50053[S] P2 optimizer gaps (fold string/mem builtins; fold libm; __builtin_expect branch-layout; memcpy/memset loop-idiom -- all win-implementable+native-PE-verifiable, dedup-clean vs 10469-10475/10455); T-win-50054[S] pe/torture-xoracle differential harness (mcc-PE vs gcc-mingw external corpus; 1488 OK/11 tracked-DIVERGE measured); T-win-50055[X]win x86_64-PE __int128/__float128 ungate (10459 S2/S3, parity). Added T-lin-10398/win[P] GPU ladder-coverage child (runnable now, needs game-off Vulkan window). NOT minted (dedup): int128-free typecov subject + win typecov leg = T-lin-10480 residual (1)+(4), mac-OWNED (created noi128 subjects b14fb090) -> FYI'd mac the win-leg recipe + PE-rejects-int128 correction. PRIOR: 2026-08-21T23:05Z -- GROOMING/TASKIFY per USER directive: running 5 research agents (P1 win-PE optfire/typecov gaps + int128-free typecov subject; P2 new optimizer parity gaps; P3 10398/win GPU-coverage feasibility; win-PE differential miscompile sweep vs gcc-mingw; win platform-gap closures toward T-lin-10092/win) to formalize NEW win-x64 tasks. No active win claims. (T-lin-10480 now OWNED by mac-arm64 as of 6ae88012 -- any win typecov leg must be a distinct child.) PRIOR: 2026-08-21T22:33Z -- P1 T-lin-10477 optfire phantom-coverage cells DONE (2c171311 ledger honesty + 8934f442/1c09170c -fswitch-jumptable flag+cell). A source-verified firing-signal map showed 4 audited passes are UNIMPLEMENTED in mcc -> honest reclassification, not a fabricated cell (id10 covered|jt,sccp; id22/id49/id77 partial; id75 base); id79 switch-jumptable DOES exist -> exposed it as a first-class o0/On-neutral `-fswitch-jumptable` knob (SPECIAL default == old env/opt-search) + a real run-verified differ cell (optfire-x86_64/switch_jt PASS via mcc -run on PE). cover3 regen also fixed pre-existing unroll-loops staleness. cli 474/474, exec 374/374, optfire-x86_64 48/48, cover3+coverage-check green. Follow-up 468f367e: switch_jt cell needed -fPIC + i386 arch.txt pin (mac 30866a84e found x86_64-Mach-O refuses a jumptable unless pic; i386 refuses one under pic) -- test-infra only, win-PE re-verified PASS, mac/lin asked to re-run their legs. Earlier this session: T-lin-10478/win (P1 native-PE optfire -run leg, 86da969c). No active win claims. Parent T-lin-10478[P] open for lin/mac legs.