# Session: win-x64  (Windows / x64 / band 50000–69999)

Only win-x64 writes this file. Authoritative session state (replaces the shared
`TODO.md` Sessions-table row; see `INSTRUCTIONS.md` §4.1). Liveness of record is
the commit log; the `Last seen` below is advisory.

Next ID: 50062
Last seen: 2026-08-22T16:57Z
Capabilities: x86_64-windows native (PE) · MSVC/cl + winlibs-ucrt mingw + scoop clang · WSL2 (x86_64-linux native validation, incl. `*-win32` cross-build for o0-baseline rebank) · gpu-vulkan (RTX 2060 — DEVICE-BLOCKED for fault tests without a game-off/TDR window) · NO x86_64-linux/macos native AOT · NO i386-toolchain · NO arm64-woa runner · NO rosetta/qemu
STATUS: 2026-08-22T16:57Z -- No active win claims; tree CLEAN, all pushed. **THIS SESSION (P2, T-lin-10510 bit-idiom recognition):** completed the win-native bit-idiom recognizer family — **bswap** (from-shifts) + **rotate** (const/var × left/right = rol/ror) + **funnel** (const both shapes → `shld imm`, var-left → `shld cl`, var-right → `shrd cl`) [all x86_64-gated] + **popcount-LOOP** (Kernighan `while(x){c++;x&=x-1;}` → straight-line portable-SWAR popcount, ARCH-INDEPENDENT via AST_OP_BITB/BB_POPCOUNT) [175dd7d18]. All default-OFF (`-fbswap-idiom`/`-frotate-idiom`/`-floop-idiom`), o0/On-neutral, verified native x86_64-PE + gcc byte-match + negatives-decline + optfire cells PASS (bswap/rotate/popcountloop) + no-regress. Also captured a `loopidiomed` re-emit flag (loop-idiom transforms previously only stuck by riding another pass's flag; also hardens the memset idiom). **RE-OPENED — remaining T-lin-10510:** clz/ctz-LOOP (→ `gen_bitscan`/`gen_ffs`, mind zero-input), SWAR word-at-a-time; other-box legs arm64 EXTR/REV/ROR/ROL (=mac) + i386 (no local executor); `x*C` strength-reduction (rebank-gated, lin domain). **Open win-relevant tasks (all device/deep/domain-gated per prior §10.11 sweeps):** T-win-50061 [D] gpu-vulkan (slicerun GPU-dispatch stall, KNOWN_RED windows, needs game-off window), T-win-50047 (Win64 struct-ABI heisenbug, deep/cdb), T-win-50041 (arm64-WoA, no runner), T-win-50059 (win-PE VLA/alloca replay opt, deep), T-win-50051/50053 (rebank-gated / lin-coordinated). **ENV NOTE (banked):** drive the MSVC `cmake-release` build from PowerShell (`& cmd /c vcvars64.bat && cmake --build`) — git-bash `cmd /c` mangles the `/c` arg (MSYS path-conv) so the batch silently no-ops (stale mcc.exe); the llvm-mingw `cmake-build-debug` mcc COMPILES + folds correctly (good for fast disasm) but its output exes SEGV even on hello-world, so run-verify on cmake-release. **Full per-checkpoint narrative + lessons → `docs/log/win-x64.md`** (13 dated entries; this STATUS trimmed 2026-08-22 per §4.1 rolling-line hygiene — history was duplicated from the log).
