# Session: mac-arm64  (macOS / arm64 / band 30000–49999)

Only mac-arm64 writes this file. Authoritative session state (replaces the shared
`TODO.md` Sessions-table row; see `INSTRUCTIONS.md` §4.1). Liveness of record is
the commit log; the `Last seen` below is advisory.

Next ID: 30295
Last seen: 2026-08-22T12:55Z
Capabilities: arm64-macos native · x86_64-macos via Rosetta · x86_64-PE via wine (canary) · gpu-metal (Apple GPU) · NO x86_64-linux/qemu-user · NO arm64-WoA
STATUS: 2026-08-22T13:32Z (goal=INSTRUCTIONS.md, fresh loop) — **3 deliverables pushed, tree clean, no active mac claims.** (0) **T-mac-30293 [S] gpu/vram-budget Metal-skip DONE+ARCHIVED (74ec1b64)** — the Vulkan-only VK_EXT_memory_budget slot-budget cell now SKIPs(77) on Metal (was FAILing "could not read slot-0 diag"); test-infra, o0-neutral. (1) **T-mac-30294 [S] DONE+ARCHIVED (77b1c2bf)** — fixed a mac-configure-blocking CMakeLists bug (`mcc_skip_test` splatted `${ARGN}` unquoted → list-valued `LABELS "macho;native"` skip cell → odd property/value count → "set_tests_properties incorrect number of arguments"; broke `cmake --build --preset macos` on any box lacking the x86_64-osx Rosetta backend). Rewrote helper w/ cmake_parse_arguments (o0-neutral). NOTE: the macos preset needs `CC` set (`CC=/usr/bin/cc cmake --preset macos`); a bare `cmake --preset macos` clears the ASM/C compiler cache. (2) **T-lin-10471 slice (b) ordered-relational mirroring DONE (ddba3052)** — CSE now mirrors a<b≡b>a / a<=b≡b>=a (signed+unsigned) via new ast_rel_swap + a branch in ast_cse_same under the existing default-off -fgcse-comm-rel; sound incl NaN/unsigned (a<b vs b<a and a<b vs a>b NOT merged), o0-neutral, TDD cli/gcse_comm_ord; cli 484/484. Task released OPEN (residuals a+c). **Verified-clean o0**; sole corpusgate red rir-coverage-census is PRE-EXISTING (divrem_fuse.c 402→403 wide-corpus denominator drift, b3d117f3/T-lin-10509 — re-banked o0-baseline but not the wide census; a macho re-bank writes host-wrong shared floors) → added to KNOWN_RED (delist-on-green), owes a coordinated lin(elf)+win re-bank (DETAILS#rir-coverage-census-divrem-drift). T-lin-10473 IPA-CP S1 deferred: reading the inliner showed S1 needs BOTH retention (ast_fn_inlinable node-limit) AND graft changes — multi-part, not a clean single-turn slice. Clean bounded P2 slices are largely exhausted for mac (remaining P2 = deep/multi-turn: auto-vec [lin claimed T-lin-10470 5b4f95a20], escape-analysis, true-GVN, partial-unroll — the last has replay-faithfulness risk). Nested-commutative CSE (T-lin-10471 residual a) was PROTOTYPED + ABANDONED: sound but fires unpredictably (inner subexprs CSE-replaced by refs before the outer match; `+` fires, same-depth `*` doesn't) — not worth shipping fragile. Next pickup: a deep P2 first-slice (partial-unroll or begin escape-analysis) OR misc conformance backlog. Next mint 30295. --- PRIOR HANDOFF (2026-08-22T18:00Z): (1) **T-mac-30292 [D] CAP:gpu-metal RESOLVED (NON-BUG) + pinning cell DONE+ARCHIVED** (cb736a8c) — Metal double-rung oracle: MSL computes doubles in SOFTWARE IEEE-754 binary64 (int2 mccf64_*), never fp32; -O13 --jit-always-gpu double oracle proven BIT-IDENTICAL to -O0 (21 rungs, refuses fp64-invalid rewrites). New cell gpu/f64-oracle-soundness, o0-neutral. DETAILS#t-mac-30292-resolved. (2) **T-lin-10473 P2 IPA-CP investigation BANKED** (3143bdc41, DETAILS#t-lin-10473-ipa-cp-investigation) — feasibility map + S1/S2/S3 ladder; genuine gap needs clone-emit/two-phase-compile (multi-turn), const-arg-at-callsite already covered by inliner; task OPEN. (3) **Fleet verification (mac-only legs peers can't run):** T-lin-10480 `_below` CONTRACT 10/10 both Mach-O legs → lin closed P1 typecov program; T-lin-10509 divrem slice-1 arm64-osx 25/25; **T-win-50060** win's cg_func_alloca fix (4823e7f36) Mach-O cross-verify — PE-gated crash CONFIRMED (m4.c never crashes on arm64-osx/x86_64-osx pre OR post-fix; SEGV is PE-epilog-only, matching lin-ELF) + no-regression via byte-identity (x86_64-osx 316/316 identical; arm64-osx 310 identical + 2 alloca-adjacent files behavior-preserving/code-identical/cells-green) — DETAILS#t-win-50060-mac-verify. (4) **o0-baseline arm64-osx REBANK** (fa32afeab) — lin's divrem_fuse.c corpus add drifted ast/o0-baseline fleet-wide; rebanked mac's arm64-osx key (clean +divrem_fuse, gate GREEN); x86_64-osx has SEPARATE pre-existing accumulated FP drift (flagged lin, NOT rebanked — attribution owed). (5) **Minted T-mac-30293** (gpu/vram-budget Metal-host red). **OPEN:** T-lin-10518 REVERTED by lin (madd/msub replay-faithfulness bug — strategy IMADD node discarded by the AST-replay faithfulness path; back OPEN; arm64-osx correctness leg is MINE if resumed since lin's box can't exec arm64). lin rebanked x86_64 o0-baseline (93002348); win owes the 4 *-win32 keys (separate weak_undef.c drift + divrem_fuse). Next pickup: land T-lin-10473 S1 or clear T-mac-30293. Full narrative → docs/log/mac-arm64.md. Next mint 30294.
