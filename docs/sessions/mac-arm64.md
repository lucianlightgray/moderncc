# Session: mac-arm64  (macOS / arm64 / band 30000–49999)

Only mac-arm64 writes this file. Authoritative session state (replaces the shared
`TODO.md` Sessions-table row; see `INSTRUCTIONS.md` §4.1). Liveness of record is
the commit log; the `Last seen` below is advisory.

Next ID: 30294
Last seen: 2026-08-22T14:30Z
Capabilities: arm64-macos native · x86_64-macos via Rosetta · x86_64-PE via wine (canary) · gpu-metal (Apple GPU) · NO x86_64-linux/qemu-user · NO arm64-WoA
STATUS: 2026-08-22T14:30Z (goal=INSTRUCTIONS.md) — **T-mac-30292 [D] CAP:gpu-metal RESOLVED (NON-BUG) + pinning cell DONE+ARCHIVED.** My own mint, my exclusive gpu-metal lane. Drove the Metal double-rung oracle soundness RISK to a definitive answer: the premise (double rungs → fp32 hardware → -O13 oracle mis-certify) is FALSE. The MSL kernel builder computes every double in SOFTWARE IEEE-754 binary64 (int2 pairs mccf64_addsub/mul/cmp/round, msl_prelude_f64 mccgpu.h:1656), never fp32; covers +−*==!=<<=>>= and REFUSES division/%/transcendentals; VT_FLOAT never reaches the device (ast_eval_slice_ftype=0 for non-double). So f64=1 unconditional (mccgpu.c:523) is honest AND inert on the MSL path (mcc_gpu_f64()'s refusals are SPIR-V-only, compiled out on Metal). Empirical (Apple M1 Pro): win's a*b+a/b-a → rungs=21/refused=0; a fp64-precision-sensitive subject (non-distributive/non-associative/cancellation) JIT-runs BIT-IDENTICAL (17 sig-digits) under GPU-armed -O13 -fopt-search --jit-always-gpu -run (21 rungs) vs -O0 CPU — oracle correctly refuses the fp64-invalid distributive rewrite (anti-vacuity: it would move the 8th sig-digit). NO code change (f64=0 would wrongly refuse the faithful emulation). Deliverable: new cell **gpu/f64-oracle-soundness** (tests/gpu/f64_oracle_soundness.sh + CMakeLists), o0-neutral. Verified: cell PASS rungs=21, treegate 13/13, corpusgate 6/6 NO drift, ^gpu suite green. Fleet-registered (skips off-device); lin/win Vulkan legs confirm on pull. **Also minted T-mac-30293** [S] for a pre-existing UNRELATED red: gpu/vram-budget FAILs on Metal (Vulkan-only slot/maxsbrange diag; Metal emits maxBufferLength) — orthogonal, now tracked. NO active mac claims; code+docs pushed; tree clean. Held for lin's incoming T-lin-10480 `_below` CONTRACT verify (both Mach-O legs). Full narrative + priority-ordered pick-up → docs/log/mac-arm64.md. Next mint 30294.
