# Session: lin-x64  (Linux / x64 / band 10000–29999)

Only lin-x64 writes this file. Authoritative session state (replaces the shared
`TODO.md` Sessions-table row; see `INSTRUCTIONS.md` §4.1). Liveness of record is
the commit log (`git log -1 --format=%cI` by this author); the `Last seen` below
is advisory.

Next ID: 10542
Last seen: 2026-08-22T15:27Z
Capabilities: x86_64-linux native · arm64/riscv64/i386 via qemu-user · x86_64-PE via wine (canary) · gpu-vulkan OK (2026-08-22 RE-CHECK: vulkaninfo does NOT hang; enumerates AMD Radeon 610M [RADV] + NVIDIA RTX 5070 Ti; tests/gpu/run.sh full-scale N=2^32 = 9/9 checks green, cpu==gpu differential PASS, half-split 4917 M/s. NOTE the vk host device-score picks the AMD iGPU, not the NVIDIA dGPU — prior "device-blocked: vulkaninfo hangs" was stale)
STATUS (2026-08-22T15:27Z — GOAL-LOOP; P1 EXHAUSTED, working P2 optimizer parity): **T-lin-10470 auto-vec slice 2 LOOP-VECTORIZE DONE + released OPEN (ed77fc96c). NO active lin claims, tree clean.** This session's P2 haul: SLP slice 1 (07a616f68/db445f7ea) + loop-vectorize slice 2 (ed77fc96c) — mcc now vectorizes both straight-line groups AND countable exact-multiple-trip loops → packed SSE (x86_64; -fslp-vectorize/-floop-vectorize, default-off). loop-vec gated #if x86_64&&!PE (arm64/riscv64 SEGV on the scalarized vec-store libcall in a strided loop — packed is x86_64-only anyway; cell pinned x86_64). Plus census unblock (3dc76230f) + T-lin-10541 unlocked-stdio fix (da13de145). NEXT P2: loop-vec REMAINDER loop / runtime-trip (10470 remaining (a)), or round-2 pool 10493..10524 (avoid mac 10471/10473/30291, win 10510). ——— EARLIER: **T-lin-10541 fix DONE+ARCHIVED; SLP slice 1 DONE.** T-lin-10541 (mac-handoff, o0-neutral): Linux #else in mccdefs.h now maps printf_unlocked/fprintf_unlocked/fputs_unlocked → locked __builtin_ forms (glibc dropped the symbols); cli 486/486 green, KNOWN_RED delisted. NEXT: back to P2 — T-lin-10470 loop-vectorize (id37, the bigger half) OR round-2 pool (10493..10524) avoiding mac(10471/10473/30291)/win(10510) locality. ——— EARLIER THIS SESSION: **T-lin-10470 SLP slice 1 DONE + released OPEN, tree clean.** (1) Rebanked wide census elf floors+inventory + delisted rir-coverage-census KNOWN_RED (3dc76230f) — mac's ast_cse_commutative_op lowerable regression accepted (mac's call, P2>P3 proxy); found the drift compound not single-cause, mac agreed option-(a). (2) **T-lin-10470 auto-vectorization/SLP slice 1 LANDED** (07a616f68 + marker-fix db445f7ea): default-off -fslp-vectorize packs N adjacent isomorphic float/double array-object ops → one 16B SSE op via the existing gen_vector_op path (crux: added a `vectorized` flag to the AST_PF_EMIT re-emit gate, else the fold was silently dropped; Load type read via ast_ident_etype not ast_type_t). Verified fires+run==O0 on native/x86_64-elf/arm64/riscv64/i386 + o0/On-neutral + all gates green. Released OPEN (remaining: loop-vectorize, AVX, NEON/RVV, non-array bases, wire-to-shipped-O). (3) Botched the SLP commit once (pull-rebase autostash conflict w/ win's concurrent rotate-idiom → conflict markers in 07a616f68; FIXED in db445f7ea; also: commit-msg backticks executed by bash — use plain msgs). (4) Found+quarantined a PRE-EXISTING lin red: cli/unlocked_stdio_builtins (printf_unlocked unresolved on this glibc; mac's 213d13e22 shimmed only mac/win) → minted T-lin-10541 + KNOWN_RED(linux). NEXT: continue P2 (loop-vectorize 10470 half, or round-2 pool 10493..10524 avoiding mac/win locality). ——— PRIOR: **CLAIMED T-lin-10470 [S] auto-vectorization/SLP** (GOAL's top unclaimed P2 parity gap; mac just finished 10471 GVN a+b→10473 next, win on 10510 bit-idioms — 10470 clear). Approach: reuse existing packed-SSE substrate T-lin-10448 (x86_64_vec16_packed_op via gen_vector_op) by a new default-off ast-opt pass rewriting eligible patterns into GNU-vector-typed ops; SLP-first slice (straight-line, no trip/remainder), loop-vec follow-up. Substrate-map research agent dispatched. Detail → log/lin-x64.md. ——— PRIOR HANDOFF (2026-08-22, superseded above): GOAL-LOOP (P1>P2>P3; goal adds: preferentially finish win/mac asks for lin work). **P1 EXHAUSTED** (T-lin-10480 typecov `_below`+tco-f128 DONE+ARCHIVED fleet-wide; mac concurs). This session, P1→P2: **(1) T-lin-10480 FINISHED+ARCHIVED** (947483a0) — part(3) `_below` off-side boundary (5 non-vacuous differ-cell pairs merge-constants/gcse-join/tree-copy-prop/tree-sroa/zero-init, 5 justified-excluded) + part(2a) tco `__float128` justified-excluded; lin-ELF + mac Mach-O 10/10 both legs. **(2) T-lin-10509 DivRemPairs SLICE 1 LANDED** (b3d117f3) — `-fdivrem-pairs` fuses adjacent `q=a/b;r=a%b`→`a-q*b` (reuse stored quotient; CSE can't, division is trap-excluded). VERIFIED x86_64-ELF 2→1 idiv + native win-PE 26/26 (win) + arm64-osx 25/25 (mac) + arm64-linux cross 2→1 sdiv; riscv64 fires-neutral. **CORRECTED** a bad "riscv64-qemu 48 cells" claim (was x86 host cells — the `qemu-*` presets build a HOST x86 mcc; real cross = `cmake-cross/mcc-<arch>`) → DETAILS#t-lin-10509-verify-correction. Rebanked o0-baseline x86_64 (93002348) for the divrem_fuse.c corpus add (fleet-red I caused); *-win32 keys have a SEPARATE pre-existing weak_undef.c drift (win -fno-common domain) → handed to win. **(3) T-lin-10518 arm64 madd/msub ATTEMPTED+REVERTED** — full impl (AST_OP_IMADD + arch-gated `madd` strategy + gen_imadd) but the strategy-created fused node isn't emitted (gen_imadd never called; plain MUL @all -O) though `__builtin_fma` emits FMADD via the same replay dispatch → faithfulness/emit path discards the strategy IMADD arena; DETAILS#t-lin-10518-madd-attempt, back to OPEN. Local arm64 exec unavailable here (no qemu-static/binfmt, empty vendored sysroot, no arm64-docker-exec) → arm64 behavioral verify is mac's leg. RESUME: divrem slice-2 (expression form, two-output divmod op reading x86 RDX/arm64 msub) + 10518 root-cause. OPEN P2 pool: 10493..10524 (minus 10509). PRIOR (context): landed T-lin-10526 [S] DONE+ARCHIVED (c1037393) — runtime Vulkan compute offload (runtime/include/mcc_gpu_offload.h) + mandelbrot CPU/multi-GPU hybrid via a dynamic work-stealing queue, bit-exact on RTX 5070 Ti + Radeon 610M, compute-only timing (one-time Vulkan setup excluded). It is the DIFFERENTIAL ORACLE for the P3 AST/RIR→GPU codegen ladder (minting oracle-driven child tasks under T-lin-10482 next, from a 3-agent research pass). Then goal loop P1-first. Earlier: fixed T-lin-10525 coop M:N fiber-park deadlock — cooperative M:N scheduler (park the fiber, not the pthread worker), code f747f581, DONE+ARCHIVED. coro-prime-sieve×mcc-coop-mn now completes N=20..2000 (SKIP guard dropped); full tests/benchmarks green mcc==gcc==clang (N=2000). Also RE-VERIFIED gpu-vulkan works here (see Capabilities) — the tests/gpu suite is green; the GPU/device backlog is takeable on this box now (host picks AMD 610M). NEXT STEPS unchanged from below except 10525 now DONE: 32 OPT-ROUND2 tasks (10493..10524) still OPEN; optional ctest-wire tests/benchmarks/run.sh (T-lin-10525 next-step (2)). Prior context: 2026-08-21 (user-driven optimizer-coverage investigation): ran a 3-agent coverage report (mcc opt impls/tests vs gcc/clang/rustc/tcc) then a 4-agent ROUND-2 gap audit beyond T-lin-10468 → minted T-lin-10493..10524 (32 [S] optimizer tasks: ladder re-curation, real -Os, alias oracle, ship DEV loop passes, superopt cost-model, static branch weights + hot/cold-split/partial-inline, ICF/outlining/link-merge, flag-reuse/copy-prop/store-merge/divrempairs/bit-idioms/mem-builtins/attr-inference/globalopt/general-SR, arm64 struct-copy+ldp-stp/atomics/madd/bitfield, FMA/fast-math, superopt-mining/static-PGO/adaptive-window, retire tree-sra) — full catalog DETAILS#t-lin-10493-optimizer-gaps-round2; doc-only, o0-neutral. THEN user asked to port vendor/plb benchmarks to C w/ C11 threads → scope = self-contained kernels, parallelize where natural. **DONE (2026-08-21T23:55Z, pushed):** 11 new kernels in tests/benchmarks/ (nsieve/mandelbrot/coro_prime_sieve/knucleotide/edigits by me; nbody/fasta/lru + fannkuch_redux/binarytrees/merkletrees via 4 agent forks, all self-verified) — 13 kernels total incl. the 2 pre-existing spectral_norm. Each is one <threads.h>-first source building under gcc/clang/mcc-nat/mcc-coop/mcc-coop-mn. run.sh reworked for MULTI-INPUT (list arg, default "1000 2000 4000") + per-run timeout + gcc-vs-clang serial cross-check + SUMMARY. **VERIFY: run.sh "200 500" 1 → 588/588 result cells ok (mcc==gcc==clang), 0 mismatch/hang.** Commits: 339d751cd (nbody/fasta/lru), a1a74579b (fannkuch/binarytrees/merkletrees), 7b5463b7b (my 5 + runner + README). **FINDING → minted T-lin-10525:** mcc-coop-mn (M:N) DEADLOCKS coro-prime-sieve for >nproc-stage pipelines (fiber blocks its pthread worker instead of parking) — correct on all other 8 configs; run.sh SKIPs only that one pairing. DETAILS#t-lin-10525-coop-mn-fiber-park. **NEXT STEPS to resume:** (1) fix T-lin-10525 (park-fiber-not-worker in mcc_coop_threads.h M:N blocking path) then drop the run.sh SKIP guard; (2) OPTIONAL: wire tests/benchmarks/run.sh into ctest as a correctness gate (currently manual — no add_test) so mcc==gcc==clang is enforced in CI; (3) the 32 OPT-ROUND2 tasks (10493..10524) are all OPEN+claimable — highest-ROI first: 10493 ladder re-curation, 10494 real -Os, 10498 static branch weights, 10495 alias oracle. FYI consumed: win-x64 withdrew T-win-50052 (=my 10498 dup), T-win-50050 overlaps my 10511 (merge at claim), their T-win-50051 libm-fold stands (not in round-2). Earlier: resumed the T-lin-10458 type-expansion program this session: (1) closed T-lin-10458 [C] DONE+archived (contract published fc6b3222d, consumed); (2) landed child T-lin-10462 _Imaginary SLICE 1 (2d7d257a8) — lifted the parse refusal, mk_imaginary_type (1-field struct + new SymAttr is_imaginary bit), sizeof/_Generic/compat/errors, o0-neutral, cli/imaginary_type[_errors]; PARKED with RESUME=slice2 (Annex-G arithmetic). Clean resume points remain (all PARKED with recipes): 10459 int128/f128 ungate (deep carry-op, no slice landed), 10460/10461 slice2+ tails, 10462 slice2 (recipe scoped via fork). Two agent forks (2026-08-21) both correctly returned scoped recipes rather than half-baked code: 10462 slice2 (Annex-G imaginary arithmetic, beyond-oracle web) and NEW T-lin-10492 (cross-AOT-link under qemu for the 10479 AOT-vs-JIT oracle on ELF legs — net-new mcc crt-search capability, LOW pri). Earlier this session: T-lin-10476 [C] JIT-coverage matrix DONE (d40e1a903, arm64/riscv64 run-verified; mac's unroll cell verified green on all my legs; win/mac wiring 10478 legs), win alloca(0) ELF confirmed. Log: docs/log/lin-x64.md
